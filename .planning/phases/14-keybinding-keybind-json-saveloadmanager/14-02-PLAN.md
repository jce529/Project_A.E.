---
phase: 14-keybinding-keybind-json-saveloadmanager
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - Assets/SaveSystem/Check.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04]

must_haves:
  truths:
    - "Assets/SaveSystem/Check.md 에 Phase 14 전용 섹션이 존재하고, Phase 11 문서 컨벤션(정적 검사 표 + Play 모드 체크리스트 + 결과 기록)을 따른다"
    - "정적 회귀 검사 12항목이 실제로 실행되고 실측값이 표에 기록된다 (허위 PASS 금지)"
    - "사용자가 Unity Play 모드에서 키 리바인딩 -> keybind.json 생성 -> 재시작 후 유지를 직접 확인할 수 있는 단계별 절차가 문서화된다"
    - "keybind.json 이 save.json 과 독립임을 확인하는 체크 항목이 존재한다 (D-04)"
  artifacts:
    - path: "Assets/SaveSystem/Check.md"
      provides: "Phase 14 키바인딩 keybind.json 위임 검증 문서 (정적 12항목 + Play 모드 체크리스트 4섹션)"
      contains: "## Phase 14"
      min_lines: 160
  key_links:
    - from: "Assets/SaveSystem/Check.md"
      to: "Assets/Player/Script/InputHandler.cs, Assets/SaveSystem/Script/SaveLoadManager.cs"
      via: "정적 회귀 검사 표의 grep 명령"
      pattern: "SaveKeybindings|LoadKeybindings|KeybindPath"
---

<objective>
Phase 14 변경사항(14-01)에 대한 검증 문서를 `Assets/SaveSystem/Check.md` 에 추가하고,
정적 회귀 검사를 실제로 실행해 실측값을 기록한 뒤, Unity Play 모드 실측 검증 체크포인트에서 사용자 확인을 받는다.

Purpose: 이 실행 환경에서는 Unity 컴파일과 런타임 동작을 확인할 수 없다. 정적으로 증명 가능한 것은
자동으로 증명하고, 증명할 수 없는 것(컴파일 성공, 실제 키 저장/복원, 파일 생성)은 사용자에게 명확한
절차를 제시해 넘긴다.

Output: `Assets/SaveSystem/Check.md` 의 Phase 14 섹션 + Play 모드 검증 결과.

**범위 밖:** 코드 수정. 이 플랜은 `Assets/` 아래 `.cs` 파일을 **0줄 변경**한다.
14-01 의 검증에서 결함이 발견되면 이 플랜에서 고치지 말고 보고한 뒤 사용자 판단을 받는다.
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-keybinding-keybind-json-saveloadmanager/14-CONTEXT.md
@.planning/phases/14-keybinding-keybind-json-saveloadmanager/14-01-PLAN.md
@Assets/SaveSystem/Check.md
</context>

<self_consistency_note>
Phase 11 의 정적 게이트 #5 는 `grep -rnE "\basync\b|\bawait\b" Assets/SaveSystem/ | wc -l` 로
**디렉토리 전체**를 훑는다 — `Check.md` 도 그 대상에 포함된다.
**이 플랜에서 Check.md 에 추가하는 문장에 `async` / `await` 리터럴 토큰을 쓰지 말 것.**
"코루틴 전용 컨벤션" 처럼 우회 표현을 쓴다. (STATE.md 에 기록된 자기모순 게이트 사고 4건의 재발 방지)

반대로, 이 플랜의 정적 검사 표는 `PlayerPrefs` / `File.WriteAllText` 같은 토큰을 **문서 안에 적어야 한다**.
따라서 코드 대상 grep 은 반드시 `--include=*.cs` 또는 파일 경로 지정으로 범위를 좁혀야 하며,
`grep -rn ... Assets/` 처럼 문서까지 훑는 형태로 쓰면 이 문서 자체가 매칭돼 게이트가 깨진다.
</self_consistency_note>

<tasks>

<task type="auto">
  <name>Task 1: Check.md 에 Phase 14 섹션 추가 + 정적 회귀 검사 12항목 실행·기록</name>

  <read_first>
    - `Assets/SaveSystem/Check.md` — 편집 대상(현재 118줄, UTF-8, 표준 Edit/Write 툴 안전).
      기존 Phase 11 섹션 구조를 그대로 미러링해야 하므로 전체를 읽을 것:
      `## 검증 대상 변경사항` -> `## 사전 준비` -> `## 섹션 1) 정적 회귀 검사`(표) ->
      `## 섹션 2~5) Play 모드 체크리스트` -> `## 섹션 6) 알려진 한계 / 범위 밖` -> `## 결과 기록`.
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` — 14-01 Task 1 이 추가한 keybind API 4종과
      `Phase14/5. Log Keybind State` ContextMenu 훅의 정확한 이름.
    - `Assets/Player/Script/InputHandler.cs` — 14-01 Task 2 결과 확인 (Read 는 허용, 편집 금지).
    - `.planning/phases/14-keybinding-keybind-json-saveloadmanager/14-CONTEXT.md` — D-01~D-04 원문.
    - 이 플랜의 `<self_consistency_note>` — 금지 토큰.
  </read_first>

  <files>Assets/SaveSystem/Check.md</files>

  <action>
기존 118줄 내용은 **한 줄도 수정하지 않고**, 파일 끝(`## 결과 기록` 섹션 뒤)에
`---` 구분선과 함께 새 대제목 `# Phase 14 — 키바인딩 keybind.json 위임 검증` 을 추가한다.
아래 구조와 내용을 그대로 채운다:

**(1) `## 검증 대상 변경사항`**
- `Assets/SaveSystem/Script/SaveLoadManager.cs` — keybind 파일 I/O API 4종 추가
  (`KeybindFileName` / `KeybindPath` / `HasKeybindFile()` / `SaveKeybindings(string)` / `LoadKeybindings()`)
  + `Phase14/5. Log Keybind State` ContextMenu 훅. 순수 삽입(삭제 0줄).
- `Assets/Player/Script/InputHandler.cs` — `SaveBindingOverrides()` / `LoadBindingOverrides()` 내부 구현을
  로컬 설정 저장소에서 `SaveLoadManager` 경유 파일 I/O 로 교체. 저장소 키 상수 제거.
  25줄 삭제 / 37줄 삽입.
- `Assets/Player/Script/Menu/ControlsSettingsPanel.cs` — **0줄 변경** (호출부 시그니처 불변).

**(2) `## 결정 요약 (14-CONTEXT.md)`** — 표로:

| ID | 결정 |
|---|---|
| D-01 | SaveLoadManager 에는 **파일 I/O만** 위임. Input System 직렬화(`SaveBindingOverridesAsJson` / `LoadBindingOverridesFromJson`)는 InputHandler 가 계속 소유 |
| D-02 | 디스크에 JSON 파일을 쓰는 코드는 프로젝트 전체에서 `SaveLoadManager.cs` 한 곳에만 존재 (이번 phase 가 이 원칙의 첫 적용 사례) |
| D-03 | 기존 로컬 설정 저장소의 리바인딩 값은 마이그레이션하지 않는다 — keybind.json 기준으로 새로 시작 |
| D-04 | `keybind.json` 은 `save.json` 과 완전 독립. `NewGame()`/세이브 삭제가 키 설정을 건드리지 않는다 |

**(3) `## 사전 준비`**
1. Unity 에디터를 열고 컴파일 완료를 기다린다. Console **에러 0건** 확인 (이 실행 환경에서 확인 불가한 유일한 항목).
2. 아무 씬에서나 Play 시작 — `SaveLoadManager` 는 부트스트랩으로 자동 생성된다.
3. Hierarchy 의 `DontDestroyOnLoad` 섹션에서 `SaveLoadManager` 를 선택 → Inspector 톱니바퀴 →
   `Phase14/5. Log Keybind State` 실행 → Console 에 `keybindPath=<절대경로> exists=False length=0` 출력.
   이 경로가 검증 내내 열어볼 파일 위치다
   (Windows: `%userprofile%/AppData/LocalLow/<회사명>/<제품명>/keybind.json`).
4. 일시정지 메뉴 > 컨트롤 탭(`ControlsSettingsPanel`)이 배치된 씬에서 진행한다.

**(4) `## 섹션 1) 정적 회귀 검사`** — 아래 12개 명령을 **실제로 실행하고** 실측값과 판정을 표에 기록한다.
기대값과 다르면 FAIL 로 적고, 절대 임의로 PASS 로 적지 않는다(Phase 9 Plan 3 선례).

| # | 명령 | 기대값 |
|---|------|--------|
| 1 | `grep -c 'public const string KeybindFileName = "keybind.json";' Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 |
| 2 | `grep -c 'public static string KeybindPath' Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 |
| 3 | `grep -c 'public void SaveKeybindings(string json)' Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 |
| 4 | `grep -c 'public string LoadKeybindings()' Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 |
| 5 | `grep -c 'SaveLoadManager.Instance.SaveKeybindings(json);' Assets/Player/Script/InputHandler.cs` | 1 |
| 6 | `grep -c 'SaveLoadManager.Instance.LoadKeybindings();' Assets/Player/Script/InputHandler.cs` | 1 |
| 7 | `grep -c 'PlayerPrefs' Assets/Player/Script/InputHandler.cs` | 0 (D-03) |
| 8 | `grep -rn "InputBindings" --include=*.cs Assets/ \| wc -l` | 0 (D-03, 구 저장소 키 흔적 없음) |
| 9 | `grep -rn "File.WriteAllText\|File.ReadAllText" --include=*.cs Assets/ \| grep -v "SaveSystem/Script/SaveLoadManager.cs" \| wc -l` | 0 (D-02) |
| 10 | `grep -ci "keybind" Assets/SaveSystem/Script/SaveData.cs` | 0 (D-04, 세이브 스키마 오염 없음) |
| 11 | `git status --porcelain Assets/Player/Script/Menu/ControlsSettingsPanel.cs \| wc -l` | 0 (호출부 0줄 변경) |
| 12 | `node -e "const fs=require('fs');const b=fs.readFileSync('Assets/Player/Script/InputHandler.cs','latin1').split('\r\n');console.log(b.filter(l=>/[^\x00-\x7F]/.test(l)).length, b.length)"` | `25 209` (인코딩 무결성 + 최종 줄 수) |

**(5) `## 섹션 2) Play 모드 체크리스트 — 저장 (D-01, D-02)`** — 미체크 `- [ ]` 항목으로:
- Play 시작 → 일시정지 → 컨트롤 탭 → `Jump` 버튼 클릭 → 다른 키(예: `K`)를 누른다
- 오버레이가 닫히고 라벨이 `K` 로 갱신된다
- `Phase14/5. Log Keybind State` 실행 → `exists=True` 이고 `length` 가 0 보다 크다
- 사전 준비 3번의 절대 경로에 **`keybind.json` 파일이 실제로 생성**되어 있고, 메모장으로 열면
  `{"bindings":[...]}` 형태의 JSON 이며 방금 바꾼 키가 들어 있다
- Console 에 `Failed to write keybind file` 에러가 **없다**

**(6) `## 섹션 3) Play 모드 체크리스트 — 복원 (D-01)`**
- Play 를 정지했다가 다시 시작한다
- 컨트롤 탭을 열면 `Jump` 라벨이 여전히 `K` 다 (`InputHandler.Awake()` → `LoadBindingOverrides()` 경로)
- 실제로 `K` 를 눌렀을 때 캐릭터가 점프한다 (라벨만 바뀐 게 아니라 바인딩이 실제 적용됨)
- 원래 키(`Space`)로는 점프하지 않는다
- Console 에 `SaveLoadManager.Instance is null` 경고/에러가 **없다** (부트스트랩 순서 확인)

**(7) `## 섹션 4) Play 모드 체크리스트 — 초기화 & 파일 없음 (D-01)`**
- 컨트롤 탭의 초기화 버튼 클릭 → 모든 라벨이 기본값으로 돌아간다
- `keybind.json` 이 기본 상태로 덮어써진다 (파일 수정 시각 갱신)
- 재시작 후에도 기본 키로 유지된다
- Play 정지 후 `keybind.json` 을 **직접 삭제**하고 다시 Play → 기본 키로 정상 동작하고
  Console 에 에러가 없다 (파일 없음 = 기본값 사용 경로)
- `keybind.json` 내용을 `{{{ 깨진 텍스트` 로 바꾸고 Play → `Failed to read keybind file` 또는
  Input System 경고가 뜨더라도 **게임이 기본 키로 계속 실행**된다 (크래시 없음)

**(8) `## 섹션 5) Play 모드 체크리스트 — save.json 독립성 (D-04)`**
- 키를 `K` 로 바꿔 `keybind.json` 을 만든 상태에서 `Phase11/3. New Game (memory only)` 실행 →
  `keybind.json` 의 내용과 수정 시각이 **변하지 않는다**
- `Phase11/1. Save Now` 로 `save.json` 을 새로 쓴 뒤에도 `keybind.json` 이 변하지 않는다
- `save.json` 을 직접 삭제하고 Play → 키 설정은 여전히 `K` 로 유지된다 (D-04 핵심)

**(9) `## 섹션 6) 알려진 한계 / 범위 밖`**
- 기존 로컬 설정 저장소에 남아 있는 예전 리바인딩 값은 마이그레이션하지 않는다 (D-03).
  출시 전 단계이므로 의도된 동작이며, 이전에 키를 바꿔둔 개발 환경에서는 **첫 실행 시 키가 기본값으로 리셋된 것처럼 보인다.**
- 사운드/그래픽/게임 설정 패널(`SoundSettingsPanel` / `GraphicsSettingsPanel` / `GameSettingsPanel`)과
  `AudioManager` 는 여전히 로컬 설정 저장소를 쓴다 — **키바인딩이 아니므로 이번 phase 범위 밖**이다.
  D-02 원칙을 이들에게 확대 적용할지는 별도 결정 사항.
- `keybind.json` 은 단일 프로필만 지원한다 (프리셋/다중 프로필 없음).
- `InputHandler.cs` 의 한글 주석은 Phase 14 이전부터 이미 훼손된 상태다(U+FFFD 658개).
  이번 편집은 그 훼손을 **늘리지 않았을 뿐** 복구하지 않았다 — 복구는 별도 작업.

**(10) `## 결과 기록`** — 정적 12항목의 실행 일시/커밋 해시와 통과 여부를 서술하고,
Play 모드 실측은 Task 2 결과에 따라 기록한다. 미수행이면 "미수행"이라고 그대로 남긴다.

**하지 말 것:**
- 기존 118줄(Phase 11 섹션) 수정 — 순수 추가만 한다
- `.cs` 파일 수정 (이 플랜은 코드 0줄 변경)
- `async` / `await` 리터럴 토큰 사용 (`<self_consistency_note>` 참고)
- 실행하지 않은 검사를 PASS 로 기록
  </action>

  <verify>
    <automated>
```bash
cd "$(git rev-parse --show-toplevel)"
C=Assets/SaveSystem/Check.md
echo "1 Phase14 heading:   $(grep -c '^# Phase 14' $C)  # expect 1"
echo "2 sections present:  $(grep -cE '^## (검증 대상 변경사항|결정 요약|사전 준비|섹션 [1-6]\)|결과 기록)' $C)  # expect >= 13"
echo "3 static table rows: $(grep -c '^| 1[0-2] \||^| [1-9] |' $C)  # expect >= 12"
echo "4 D-04 section:      $(grep -c 'save.json 독립성' $C)  # expect 1"
echo "5 checkboxes:        $(grep -c '^- \[ \]' $C)  # expect >= 20"
echo "6 no forbidden token:$(grep -cE 'async|await' $C)  # expect 0"
echo "7 phase11 intact:    $(git diff -- $C | grep -c '^-[^-]')  # expect 0 (순수 추가)"
echo "8 no .cs touched:    $(git diff --name-only -- 'Assets/**/*.cs' | wc -l)  # expect 0 for this task's commit"
echo "9 ContextMenu name:  $(grep -c 'Phase14/5. Log Keybind State' $C)  # expect >= 1"
echo "10 line count:       $(wc -l < $C)  # expect >= 160"
```
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c '^# Phase 14' Assets/SaveSystem/Check.md` == 1
    - `Assets/SaveSystem/Check.md` 에 `## 섹션 1) 정적 회귀 검사` 부터 `## 섹션 6)` 까지 Phase 14 하위 섹션이 모두 존재
    - 정적 회귀 검사 표에 12개 항목이 있고, **각 행에 실측값 열과 PASS/FAIL 판정이 채워져 있다** (빈칸/미실행 금지)
    - `grep -c '^- \[ \]' Assets/SaveSystem/Check.md` >= 20 (Play 모드 미체크 항목)
    - `grep -c 'Phase14/5. Log Keybind State' Assets/SaveSystem/Check.md` >= 1
    - `grep -cE 'async|await' Assets/SaveSystem/Check.md` == 0 (Phase 11 정적 게이트 #5 유지)
    - `git diff -- Assets/SaveSystem/Check.md | grep -c '^-[^-]'` == 0 (기존 Phase 11 내용 순수 보존)
    - `wc -l < Assets/SaveSystem/Check.md` >= 160
    - 이 태스크 커밋의 파일 목록이 `Assets/SaveSystem/Check.md` 단 하나 (`git log -1 --name-only`)
    - 정적 검사 12항목 중 FAIL 이 있으면 SUMMARY 에 FAIL 로 기록되어 있다 (허위 PASS 0건)
  </acceptance_criteria>

  <done>
    `Assets/SaveSystem/Check.md` 가 Phase 11 섹션을 온전히 보존한 채 Phase 14 검증 섹션을 갖췄고,
    정적 회귀 검사 12항목이 실제로 실행되어 실측값과 판정이 표에 기록되었다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Unity Play 모드 실측 검증 체크포인트</name>

  <read_first>
    - `Assets/SaveSystem/Check.md` — Task 1 이 추가한 Phase 14 섹션 (사용자에게 제시할 절차의 원문).
  </read_first>

  <what-built>
    - `SaveLoadManager` 에 `keybind.json` 파일 I/O API 4종 + `Phase14/5. Log Keybind State` ContextMenu 훅 (14-01 Task 1)
    - `InputHandler.SaveBindingOverrides()` / `LoadBindingOverrides()` 를 `SaveLoadManager` 경유로 교체,
      로컬 설정 저장소 사용 완전 제거 (14-01 Task 2)
    - `Assets/SaveSystem/Check.md` Phase 14 검증 섹션 + 정적 회귀 검사 12항목 실행 결과 (14-02 Task 1)

    정적으로 증명 가능한 것은 전부 자동 검증했다. **남은 것은 이 실행 환경에서 원리적으로 불가능한 3가지다:**
    Unity 컴파일 성공 여부, 실제 키 리바인딩 동작, 디스크에 파일이 실제로 생기는지.
  </what-built>

  <files>Assets/SaveSystem/Check.md</files>

  <action>
이 태스크는 사용자 확인 게이트다. 실행자가 코드를 작성하지 않는다.
아래 <how-to-verify> 절차를 사용자에게 그대로 제시하고 응답을 기다린 뒤,
응답 내용에 따라 `Assets/SaveSystem/Check.md` 의 Phase 14 체크박스와 `## 결과 기록` 절만 갱신한다.

- "approved" -> 섹션 2)~5) 의 `- [ ]` 를 `- [x]` 로 바꾸고 `## 결과 기록` 에 실측 통과 사실을 서술한다
- "skip" -> 체크박스를 건드리지 않고 `## 결과 기록` 에 "Play 모드 실측 미수행 - 사용자 결정으로 검증 생략" 을 명시한다
- 문제 보고 -> 체크박스를 건드리지 않고 `## 결과 기록` 에 보고된 증상을 원문 그대로 기록한다. **코드를 즉석에서 고치지 않는다.**

어느 경우에도 `.cs` 파일은 0줄 변경이며, 확인되지 않은 항목을 `- [x]` 로 바꾸지 않는다 (Phase 9 Plan 3 선례).
  </action>

  <verify>
    <automated>
```bash
cd "$(git rev-parse --show-toplevel)"
C=Assets/SaveSystem/Check.md
echo "checked:   $(grep -c '^- \[x\]' $C)"
echo "unchecked: $(grep -c '^- \[ \]' $C)"
echo "outcome recorded: $(grep -cE '미수행|검증 생략|Play 모드 실측' $C)  # expect >= 1"
echo "no .cs changed:   $(git diff --name-only -- 'Assets/**/*.cs' | wc -l)  # expect 0"
```
    </automated>
  </verify>

  <how-to-verify>
    `Assets/SaveSystem/Check.md` 의 **Phase 14 섹션 2)~5)** 체크리스트를 순서대로 진행한다. 요약 절차:

    1. **컴파일**: Unity 에디터를 열고 컴파일이 끝날 때까지 대기 → Console 에러 **0건** 확인.
       (`InputHandler` 가 `SaveLoadManager` 를 참조하게 되었으므로 여기서 처음 검증된다.)
    2. **경로 확인**: Play 시작 → Hierarchy `DontDestroyOnLoad` > `SaveLoadManager` 선택 →
       Inspector 톱니바퀴 → `Phase14/5. Log Keybind State` → Console 에 출력된 절대 경로를 탐색기로 연다.
       (첫 실행이면 `exists=False length=0`)
    3. **저장**: 일시정지 → 컨트롤 탭 → `Jump` 재바인딩 버튼 클릭 → `K` 키 입력 →
       라벨이 `K` 로 바뀌고, 2번 경로에 **`keybind.json` 파일이 새로 생성**되었는지 확인.
    4. **복원**: Play 정지 → 다시 Play → 컨트롤 탭 라벨이 여전히 `K` 이고, **실제로 `K` 를 눌러 점프**되는지,
       `Space` 로는 점프가 **안 되는지** 확인.
    5. **초기화 / 파일 없음 / 손상**: 초기화 버튼 → 기본값 복귀 확인 → Play 정지 후 `keybind.json` 삭제 →
       Play → 기본 키로 정상 동작 + Console 에러 없음 → `keybind.json` 내용을 아무 텍스트로 망가뜨린 뒤 Play →
       **크래시 없이 기본 키로 실행**되는지 확인.
    6. **독립성(D-04)**: 키를 `K` 로 바꾼 상태에서 `Phase11/3. New Game (memory only)` 실행 →
       `keybind.json` 의 수정 시각과 내용이 **그대로**인지 확인. `save.json` 을 삭제해도 키 설정이 유지되는지 확인.

    확인이 끝나면 `Assets/SaveSystem/Check.md` 의 해당 `- [ ]` 항목을 `- [x]` 로 바꾸고,
    `## 결과 기록` 에 실측 결과를 적는다. **확인하지 않은 항목은 미체크 그대로 남긴다**
    (Phase 9 Plan 3 선례 — 허위 PASS 금지).

    **문제가 보이면 그대로 알려줄 것.** 특히:
    - 컴파일 에러 → 14-01 의 API 시그니처 불일치
    - `SaveLoadManager.Instance is null` → 부트스트랩 순서 가정이 깨진 것 (설계 재검토 필요)
    - 라벨은 바뀌는데 실제 키가 안 먹음 → Input System 오버라이드 적용 경로 문제
  </how-to-verify>

  <resume-signal>
    "approved" 를 입력하거나, 발견한 문제(Console 에러 원문 / 어느 단계에서 어긋났는지)를 설명해 주세요.
    Play 모드 검증을 지금 건너뛰려면 "skip" 이라고 답해 주세요 — 그 경우 Check.md 에 PASS 로 기록하지 않고
    "검증 생략" 상태로 남깁니다.
  </resume-signal>

  <acceptance_criteria>
    - 사용자가 "approved" / "skip" / 구체적 문제 보고 중 하나로 응답했다
    - "approved" 인 경우: `Assets/SaveSystem/Check.md` Phase 14 섹션 2)~5) 의 항목이 `- [x]` 로 갱신되고
      `## 결과 기록` 에 Play 모드 실측 결과가 서술되어 있다
    - "skip" 인 경우: Check.md 의 항목이 `- [ ]` 그대로이고, `## 결과 기록` 에 "Play 모드 실측 미수행" 이 명시되어 있다
      (`grep -c "미수행\|검증 생략" Assets/SaveSystem/Check.md` >= 1)
    - 문제 보고인 경우: 코드를 즉석에서 고치지 않고, 문제 내용을 SUMMARY 와 STATE.md 에 기록한 뒤 사용자 판단을 받는다
  </acceptance_criteria>

  <done>
    Play 모드 검증 결과가 Check.md 에 사실 그대로 반영되었다 (통과 / 생략 / 결함 중 하나로 명시).
  </done>
</task>

</tasks>

<verification>
```bash
cd "$(git rev-parse --show-toplevel)"
# 이 플랜은 코드 0줄 변경
git diff --stat HEAD~2 -- 'Assets/**/*.cs' | tail -1   # 14-02 커밋 범위에 .cs 변경이 없어야 함
grep -c '^# Phase 14' Assets/SaveSystem/Check.md       # expect 1
grep -cE 'async|await' Assets/SaveSystem/Check.md      # expect 0
```
</verification>

<success_criteria>
1. `Assets/SaveSystem/Check.md` 에 Phase 14 섹션이 존재하고 Phase 11 섹션이 온전히 보존되었다 (삭제 0줄).
2. 정적 회귀 검사 12항목이 실제로 실행되어 실측값과 PASS/FAIL 판정이 표에 기록되었다.
3. Play 모드 체크리스트가 저장 / 복원 / 초기화·파일없음·손상 / save.json 독립성 4개 섹션으로 구성되었다.
4. 사용자가 Play 모드 검증을 수행했거나 명시적으로 생략했으며, 그 사실이 문서에 사실대로 기록되었다.
5. 이 플랜에서 `Assets/` 아래 `.cs` 파일이 0줄 변경되었다.
</success_criteria>

<output>
완료 후 `.planning/phases/14-keybinding-keybind-json-saveloadmanager/14-02-SUMMARY.md` 를 작성한다.
반드시 기록할 것:
- 정적 회귀 검사 12항목의 실측값 표 (FAIL 이 있으면 그대로)
- Play 모드 검증 상태 (통과 / 생략 / 결함 — 사용자 응답 원문 포함)
- STATE.md 갱신 제안: Phase 14 결정 요약(D-01~D-04)과 "InputHandler.cs 는 CP949 가 아니라 이미 U+FFFD 로
  훼손된 UTF-8 이며, 워킹트리가 HEAD 와 달라 `git show HEAD:` 복원 프로토콜을 쓰면 안 된다"는 발견
</output>
