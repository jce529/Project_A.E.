---
phase: 09-camera-zoom-stage-transition
plan: 03
type: execute
wave: 3
depends_on: ["09-01", "09-02"]
files_modified:
  - Assets/Camera/Check.md
autonomous: false
requirements: [D-01, D-03, D-04, D-05, D-06, D-07, D-09, D-10, D-11]
must_haves:
  truths:
    - "Unity 에디터에서 컴파일 에러 0건이다"
    - "플레이어가 트리거에 들어가면 카메라 줌이 5에서 7로 부드럽게 변하고, 나오면 5로 자동 복귀한다"
    - "줌 값과 줌 속도를 Play 중 Inspector 에서 바꾸면 즉시 반영된다"
    - "줌 5 와 줌 7 양쪽 모두에서 카메라 화면이 minX/maxX 경계를 넘지 않는다"
    - "Y축은 여전히 제한 없이 플레이어를 따라간다"
    - "Assets/Camera/Check.md 에 PASS/FAIL 과 씬별 minX/maxX 튜닝 값이 기록된다"
  artifacts:
    - path: "Assets/Camera/Check.md"
      provides: "Phase 9 Play 모드 검증 결과 기록"
      contains: "결과 기록"
  key_links:
    - from: "BossZoomTrigger.OnTriggerEnter2D/OnTriggerExit2D"
      to: "카메라 orthographicSize 실제 변화"
      via: "Play 모드 사람 검증"
      pattern: "결과 기록"
---

<objective>
Plan 09-01(카메라 줌/클램프)과 09-02(트리거 스크립트)의 결과를 정적 검사로 한 번 훑은 뒤,
사용자가 Unity Play 모드에서 실제 동작을 검증하고 `Assets/Camera/Check.md` 에 결과를 기록한다.

Purpose: grep 으로 증명할 수 없는 런타임 동작(부드러운 줌 전환, 이탈 자동 복귀, 줌 7 상태에서의
경계 클램프, Inspector 실시간 튜닝)을 사람이 확인한다. 이 실행 환경에서는 Unity 컴파일/Play 실행이 불가능하다.
Output: `Assets/Camera/Check.md` 의 검증 항목이 전부 체크되고 "결과 기록" / "튜닝 값 기록" 섹션이 채워진다.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md
@.planning/phases/09-camera-zoom-stage-transition/09-01-SUMMARY.md
@.planning/phases/09-camera-zoom-stage-transition/09-02-SUMMARY.md
@Assets/Camera/Script/CameraController.cs
@Assets/Camera/Script/BossZoomTrigger.cs
@Assets/Camera/Check.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정적 회귀 검사 — 결정 준수 및 변경 범위 확인</name>

  <files>(읽기 전용 — 파일 수정 없음)</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` (Plan 09-01 최종 상태 — 전체를 Read 해서 `LateUpdate` 순서를 눈으로 확인)
    - `Assets/Camera/Script/BossZoomTrigger.cs` (Plan 09-02 최종 상태)
    - `.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md` (D-01 ~ D-11 원문)
  </read_first>

  <action>
사용자가 Unity 를 켜기 전에, 코드 레벨에서 자동 검증 가능한 항목을 전부 확인한다. **파일은 수정하지 않는다.**
아래 항목 중 하나라도 실패하면 Task 2 체크포인트로 진행하지 말고, 어떤 항목이 왜 실패했는지 보고한다.

1. `LateUpdate` 실행 순서가 **위치 추종 Lerp → 줌 Lerp → `ApplyXClamp()`** 인지 행 번호로 확인한다
   (RESEARCH Pattern 3 — 클램프가 줌보다 앞서면 이전 프레임의 반폭으로 클램프되어 전환 중 흔들린다).
2. 반폭 공식이 `_cam.orthographicSize * _cam.aspect` 곱셈인지 확인한다 (D-11 / Pitfall 1 — 나눗셈이면 오답).
3. Y축 클램프 코드가 없는지 확인한다: `minY` / `maxY` / `pos.y = Mathf.Clamp` 문자열 0건 (D-09).
4. `DontDestroyOnLoad` / `Destroy(gameObject)` 가 `CameraController.cs` 에 없는지 확인한다
   (씬 로컬 싱글톤 — RESEARCH Pattern 1).
5. 줌 전환이 `zoomSmoothing` 을 쓰고 `smoothing` 을 재사용하지 않는지 확인한다 (D-07).
6. 줌 값이 하드코딩 상수가 아니라 `normalZoom` / `bossZoom` 필드 경유인지 확인한다 (D-05).
7. `BossZoomTrigger.cs` 가 Enter/Exit 양쪽에서 `CompareTag("Player")` 가드 뒤에 `SetBossZoom(true/false)` 를
   호출하는지 확인한다 (D-01 / D-03).
8. 인코딩 게이트: `CameraController.cs` 의 비-ASCII 포함 라인 수가 정확히 5 이고,
   Phase 9 기준 커밋(`184ed58`) 대비 삭제된 라인이 0줄인지 확인한다 (Pitfall 3 — 기존 CP949 한글 주석 보존).
9. 씬 파일이 이번 Phase 에서 수정되지 않았는지 확인한다 (D-08).
   주의: 작업 트리에는 Phase 9 이전부터 커밋되지 않은 씬/스크립트 변경이 존재한다. Phase 9 커밋에
   `Assets/Camera/` 외의 경로가 포함되지 않았는지를 `git log --stat` 으로 확인한다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && C=Assets/Camera/Script/CameraController.cs && B=Assets/Camera/Script/BossZoomTrigger.cs && PL=$(grep -nF 'Vector3.Lerp(transform.position' $C | cut -d: -f1) && ZL=$(grep -nF 'Mathf.Lerp(_cam.orthographicSize' $C | cut -d: -f1) && CL=$(grep -nF 'ApplyXClamp();' $C | tail -1 | cut -d: -f1) && [ "$ZL" -gt "$PL" ] && [ "$CL" -gt "$ZL" ] && grep -qF "float halfWidth = _cam.orthographicSize * _cam.aspect;" $C && [ "$(grep -cF 'orthographicSize / _cam.aspect' $C)" = "0" ] && [ "$(grep -cF 'minY' $C)" = "0" ] && [ "$(grep -cF 'maxY' $C)" = "0" ] && [ "$(grep -cF 'DontDestroyOnLoad' $C)" = "0" ] && [ "$(grep -cF 'Destroy(gameObject)' $C)" = "0" ] && grep -qF "zoomSmoothing * Time.deltaTime" $C && grep -qF "_targetZoom = isBossStage ? bossZoom : normalZoom;" $C && grep -qF "public float normalZoom = 5f;" $C && grep -qF "public float bossZoom = 7f;" $C && grep -qF "public float minX" $C && grep -qF "public float maxX" $C && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $C)" = "5" ] && [ "$(git diff 184ed58 -- $C | grep '^-' | grep -vc '^---')" = "0" ] && [ "$(grep -cF 'other.CompareTag(\"Player\")' $B)" = "2" ] && grep -qF "CameraController.Instance.SetBossZoom(true);" $B && grep -qF "CameraController.Instance.SetBossZoom(false);" $B && [ "$(git log --oneline 184ed58..HEAD --name-only --pretty=format: -- Assets | grep -v '^$' | grep -vc '^Assets/Camera/')" = "0" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - 행 번호 순서: `Vector3.Lerp(transform.position` < `Mathf.Lerp(_cam.orthographicSize` < 마지막 `ApplyXClamp();` (RESEARCH Pattern 3)
    - `grep -qF "float halfWidth = _cam.orthographicSize * _cam.aspect;" Assets/Camera/Script/CameraController.cs` 성공, `grep -cF 'orthographicSize / _cam.aspect'` == 0 (D-11)
    - `grep -cF 'minY'` == 0, `grep -cF 'maxY'` == 0 (D-09 Y축 제외)
    - `grep -cF 'DontDestroyOnLoad'` == 0, `grep -cF 'Destroy(gameObject)'` == 0 (씬 로컬 싱글톤)
    - `grep -qF "zoomSmoothing * Time.deltaTime"` 성공 (D-07 — 줌이 `smoothing` 을 재사용하지 않음)
    - `grep -qF "public float normalZoom = 5f;"` / `"public float bossZoom = 7f;"` / `"public float minX"` / `"public float maxX"` 모두 성공 (D-05, D-10)
    - `grep -cF 'other.CompareTag("Player")' Assets/Camera/Script/BossZoomTrigger.cs` == 2 및 `SetBossZoom(true);` / `SetBossZoom(false);` 각각 존재 (D-01, D-03)
    - `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == 5 (기존 한글 주석 보존)
    - `git diff 184ed58 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == 0 (삽입 전용)
    - `git log 184ed58..HEAD --name-only --pretty=format: -- Assets` 결과에 `Assets/Camera/` 로 시작하지 않는 경로가 0건 (D-08 — 씬/보스 스크립트 미변경)
  </acceptance_criteria>

  <done>정적 검사 전 항목 통과 — LateUpdate 순서, 반폭 공식, Y축 미클램프, 싱글톤 형태, 줌 속도 분리, 트리거 호출부, 인코딩 보존, 변경 경로 범위가 코드 레벨에서 확인되었다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Unity Play 모드 검증 체크포인트 (줌 전환 + X축 클램프)</name>

  <files>Assets/Camera/Check.md</files>

  <read_first>
    - `Assets/Camera/Check.md` (Plan 09-02 Task 2 가 작성한 체크리스트 — 사용자에게 제시할 절차의 원본)
    - `Assets/Camera/Script/CameraController.cs` (Inspector 필드명 확인용)
  </read_first>

  <action>
사용자에게 아래 `<how-to-verify>` 절차를 그대로 제시하고 Play 모드 검증을 요청한 뒤, 응답을 기다린다.
Claude 가 대신 수행할 수 없는 작업이다 (Unity 에디터 실행 + 시각적 관찰 + 씬별 경계값 튜닝).

사용자 응답을 받은 후 Claude 가 할 일:
- "approved" 인 경우: 사용자가 보고한 관찰 내용을 `Assets/Camera/Check.md` 의 "결과 기록" 섹션에 적고,
  각 검증 항목의 `- [ ]` 를 `- [x]` 로 바꾸고, 사용자가 확정한 씬별 `Min X`/`Max X` 값을
  "튜닝 값 기록" 표에 채운 뒤, 상단의 "**상태: 보류**" 문구를 검증 완료 상태로 갱신한다.
- 문제가 보고된 경우: 해당 항목만 `- [ ]` 로 남기고 관찰 내용을 "결과 기록" 에 FAIL 로 기록한 뒤,
  갭 목록을 정리해 `/gsd:plan-phase 9 --gaps` 입력으로 쓸 수 있게 보고한다.
  **임의로 코드를 고치지 말고 먼저 보고할 것.**
- 사용자가 튜닝한 `Min X`/`Max X` 값을 씬에 저장하는 것은 사용자의 선택 사항이며, Claude 가 `.unity`
  파일을 직접 편집하지 않는다 (D-08).
  </action>

  <what-built>
    - Plan 09-01: `CameraController.cs` 에 씬 로컬 싱글톤 `Instance`, `Camera` 캐시,
      Inspector 필드 `normalZoom`(5) / `bossZoom`(7) / `zoomSmoothing`(3) / `minX`(-1000) / `maxX`(1000),
      `SetBossZoom(bool)` 을 추가했고, `LateUpdate` 를 **위치 추종 Lerp → 줌 Lerp → X 클램프** 순서로 확장했다.
      클램프는 `orthographicSize * aspect` 반폭을 반영하며 **Y축은 제한하지 않는다**.
      기존 라인은 한 줄도 삭제/수정하지 않았다(삽입만).
    - Plan 09-02: `BossZoomTrigger.cs` 신규 — `OnTriggerEnter2D` 에서 `SetBossZoom(true)`,
      `OnTriggerExit2D` 에서 `SetBossZoom(false)` 를 `CompareTag("Player")` 가드 뒤에서 호출한다.
      필드가 없는 재사용 컴포넌트다.
    - **아직 하지 않은 것(D-08):** 실제 보스 구역에 트리거 콜라이더를 배치하는 작업. 아래 검증에서는
      임시 테스트용 트리거 오브젝트를 하나 만들어 확인한다.
  </what-built>

  <how-to-verify>
    전체 절차와 체크 항목은 `Assets/Camera/Check.md` 에 정리되어 있습니다. 아래는 동일 내용의 요약입니다.

    **0. 사전 준비**
    1. Unity 에디터를 열고 컴파일이 끝날 때까지 대기합니다. Console 에 **에러 0건**인지 확인해 주세요.
       (에러가 있으면 여기서 중단하고 에러 원문을 알려 주세요.)
    2. `Assets/Scenes/1 stage.unity` 를 엽니다.
    3. Hierarchy 에서 빈 GameObject 를 만들고 이름을 `TempBossZoneTest` 로 바꿉니다.
    4. `TempBossZoneTest` 에 `BoxCollider2D` 를 추가하고 **Is Trigger 를 체크**한 뒤,
       Size 를 플레이어가 확실히 드나들 크기(예: X=10, Y=10)로 설정합니다.
    5. `TempBossZoneTest` 에 `BossZoomTrigger` 컴포넌트를 추가하고, 플레이어가 지나다니는 위치에 배치합니다.
    6. Hierarchy 에서 `Main Camera` 를 선택합니다.

    **1. 필드 노출 확인 (D-05 / D-10)**
    `Main Camera` 의 Camera Controller 컴포넌트에 아래 5개 필드가 보이는지 확인해 주세요:
    `Normal Zoom`(5), `Boss Zoom`(7), `Zoom Smoothing`(3), `Min X`(-1000), `Max X`(1000).

    **2. 기존 동작 회귀 확인**
    Play 를 눌러 카메라가 예전처럼 플레이어를 따라다니는지, 시작 순간 화면이 튀거나 줌이 갑자기
    변하지 않는지 확인해 주세요.

    **3. 줌 전환 (D-01 / D-04 / D-06 / D-03)**
    1. 플레이어를 `TempBossZoneTest` 안으로 이동시킵니다. Inspector 의 Camera > `Size` 값이
       5 → 7 로 **몇 프레임에 걸쳐 부드럽게** 올라가야 합니다 (한 번에 점프하면 FAIL).
    2. 트리거 밖으로 나오면 아무 조작 없이 `Size` 가 7 → 5 로 되돌아와야 합니다.
    3. 빠르게 여러 번 드나들어도 에러 없이 5/7 사이를 오가는지 확인합니다.

    **4. Inspector 튜닝 (D-05 / D-07)**
    1. Play 중 `Boss Zoom` 을 9 로 바꾸고 다시 트리거에 들어가면 9 까지 확대되어야 합니다.
    2. Play 중 `Zoom Smoothing` 을 1 로 낮추면 **줌만** 느려지고, 플레이어 추종 속도(`Smoothing`=5)는
       그대로여야 합니다. (두 값이 서로 영향을 주면 FAIL — D-07 위반)

    **5. X축 클램프 (D-09 / D-10 / D-11) — 이번 Phase 의 핵심 튜닝 구간**
    1. Play 를 멈추고 Scene 뷰에서 맵의 좌/우 끝 월드 X 좌표를 확인한 뒤,
       `Min X` / `Max X` 에 그 값을 넣습니다.
    2. Play 후 플레이어를 맵 **왼쪽 끝**과 **오른쪽 끝**까지 이동시켜, 화면에 맵 바깥의 빈 공간이
       보이지 않는지 확인합니다 (일반 줌 5 상태).
    3. **가장 중요:** 같은 `Min X`/`Max X` 값을 유지한 채 트리거에 들어가 **보스 줌(7) 상태**로 만들고,
       다시 맵 좌/우 끝까지 이동해 빈 공간이 보이지 않는지 확인합니다.
       (줌 7 은 화면이 더 넓어 여기서 새기 쉽습니다. 이 항목이 D-11 의 핵심입니다.)
    4. 위/아래로 이동했을 때 카메라 Y 는 여전히 제한 없이 따라오는지 확인합니다 (D-09 — Y 제한은 이번 범위 아님).
    5. 만약 카메라가 좌우로 거의 못 움직이고 한쪽에 붙어 있다면, `Max X - Min X` 가 화면 폭보다 좁은
       경우입니다. 값을 더 넓게 잡아 주세요 (알려진 동작, 방어 코드 없음).
    6. 최종적으로 사용하기로 한 `Min X` / `Max X` 값을 알려 주시면 `Check.md` 에 기록하겠습니다.

    **6. 정리**
    `TempBossZoneTest` 는 검증용 임시 오브젝트입니다. 씬을 저장하지 않으면 남지 않으며,
    실제 보스 구역 트리거 배치는 원하실 때 직접 진행하시면 됩니다 (D-08 — 이번 Phase 범위 밖).
  </how-to-verify>

  <acceptance_criteria>
    - Unity Console 컴파일 에러 0건임이 사용자에 의해 확인됨
    - `Assets/Camera/Check.md` 의 모든 검증 항목이 `- [x]` 로 체크됨: `grep -c '^- \[ \]' Assets/Camera/Check.md` == 0
    - `grep -c '^- \[x\]' Assets/Camera/Check.md` >= 10
    - "결과 기록" 섹션에 관찰 내용이 기록되어 있고, 안내 문구 `(검증 완료 시 여기에` 가 더 이상 남아 있지 않다
    - "튜닝 값 기록" 표에 최소 1개 씬의 `Min X` / `Max X` 확정 값이 채워져 있다
    - 상단 `**상태: 보류**` 문구가 검증 완료 상태로 갱신되어 `grep -c '상태: 보류' Assets/Camera/Check.md` == 0
    - FAIL 항목이 있는 경우 해당 항목이 `- [ ]` 로 남고 관찰 내용이 FAIL 로 기록되며, 갭 목록이 보고됨
  </acceptance_criteria>

  <resume-signal>모두 정상이면 "approved" 와 함께 확정한 Min X / Max X 값을 알려 주세요. 문제가 있으면 어떤 항목이 어떻게 다른지(가능하면 Console 로그나 관찰한 Size 값과 함께) 알려 주세요.</resume-signal>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Check.md && [ "$(grep -c '^- \[ \]' $F)" = "0" ] && [ "$(grep -c '^- \[x\]' $F)" -ge 10 ] && [ "$(grep -c '상태: 보류' $F)" = "0" ] && [ "$(grep -cF '(검증 완료 시 여기에' $F)" = "0" ] && echo CHECKPOINT_OK</automated>
  </verify>

  <done>사용자가 Unity Play 모드에서 줌 전환(진입/이탈/부드러움/Inspector 튜닝)과 X축 클램프(줌 5 및 줌 7 양쪽)를 검증했고, `Assets/Camera/Check.md` 에 미체크 항목이 하나도 남아 있지 않으며 관찰 결과와 씬별 minX/maxX 확정 값이 기록되었다.</done>
</task>

</tasks>

<verification>
1. Unity 컴파일 에러 0건.
2. `Assets/Camera/Check.md` 에 미체크 항목(`- [ ]`)이 없다.
3. 트리거 진입 시 줌 5 → 7 부드러운 전환, 이탈 시 7 → 5 자동 복귀가 실측으로 확인되었다 (D-01, D-03, D-06).
4. 줌 값/줌 속도가 Play 중 Inspector 로 조정 가능하며 위치 추종 속도와 독립임이 확인되었다 (D-05, D-07).
5. 줌 5 와 줌 7 양쪽에서 화면이 minX/maxX 를 넘지 않고, Y축은 자유롭게 움직임이 확인되었다 (D-09, D-11).
6. Phase 9 커밋 범위가 `Assets/Camera/` 로 한정되어 있다 (D-08).
</verification>

<success_criteria>
- Phase 9 의 두 기능(보스 스테이지 줌 전환, 카메라 X축 제한)이 실제 Play 모드에서 동작함이 확인되었다.
- 씬별 `Min X` / `Max X` 초기 튜닝 값이 문서에 남았다.
- 회귀/미달 항목이 있으면 `/gsd:plan-phase 9 --gaps` 로 넘길 수 있게 문서화되었다.
</success_criteria>

<output>
완료 후 `.planning/phases/09-camera-zoom-stage-transition/09-03-SUMMARY.md` 를 생성하고,
`.planning/STATE.md` 의 Phase 9 상태를 갱신한다.
커밋은 `Assets/Camera/Check.md` 경로만 지정해서 수행한다.
</output>
