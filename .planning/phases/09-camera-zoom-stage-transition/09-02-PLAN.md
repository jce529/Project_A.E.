---
phase: 09-camera-zoom-stage-transition
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - Assets/Camera/Script/BossZoomTrigger.cs
  - Assets/Camera/Check.md
autonomous: true
requirements: [D-01, D-02, D-03, D-08]
must_haves:
  truths:
    - "보스 구역용 트리거 콜라이더에 붙일 수 있는 독립 컴포넌트가 존재한다"
    - "플레이어가 트리거에 들어가면 카메라가 보스 줌으로, 나가면 일반 줌으로 자동 복귀한다"
    - "트리거 스크립트에 보스별 설정 필드가 없어 어느 보스 씬에나 그대로 붙일 수 있다"
    - "Play 모드에서 사람이 확인해야 할 항목이 체크리스트 파일로 정리되어 있다"
  artifacts:
    - path: "Assets/Camera/Script/BossZoomTrigger.cs"
      provides: "OnTriggerEnter2D/OnTriggerExit2D 기반 보스 줌 토글"
      contains: "CameraController.Instance.SetBossZoom"
    - path: "Assets/Camera/Check.md"
      provides: "Phase 9 Play 모드 검증 체크리스트"
      contains: "검증 항목"
  key_links:
    - from: "Assets/Camera/Script/BossZoomTrigger.cs"
      to: "CameraController.SetBossZoom"
      via: "싱글톤 Instance 직접 호출"
      pattern: "CameraController\\.Instance\\.SetBossZoom\\((true|false)\\)"
---

<objective>
Plan 09-01 이 만든 `CameraController.Instance.SetBossZoom(bool)` 을 실제로 호출하는 트리거 컴포넌트
`BossZoomTrigger.cs` 를 신규 작성하고 (D-01, D-02, D-03), Phase 9 전체의 Play 모드 검증 체크리스트
`Assets/Camera/Check.md` 를 작성한다.

Purpose: 씬 전환이 아니라 씬 내부 트리거 콜라이더로 보스 구역 진입/이탈을 감지하는 경로를 완성한다.
Output: 신규 파일 2개. 기존 파일 수정 0건.

**범위 밖 (D-08):** 실제 보스 씬(WaterMonster/WaterSpirit/TutorialBoss)에 트리거 콜라이더 GameObject 를
배치하는 에디터 작업은 이번 Phase 에서 하지 않는다. 씬 파일(`.unity`)을 편집하지 말 것.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md
@.planning/phases/09-camera-zoom-stage-transition/09-RESEARCH.md
@.planning/phases/09-camera-zoom-stage-transition/09-01-SUMMARY.md
@Assets/Camera/Script/CameraController.cs
</context>

<interfaces>
Plan 09-01 이 `Assets/Camera/Script/CameraController.cs` 에 추가한, 이 플랜이 사용할 계약:

```csharp
public class CameraController : MonoBehaviour
{
    // 씬 로컬 싱글톤. Awake()에서 할당됨. DontDestroyOnLoad 없음.
    public static CameraController Instance { get; private set; }

    // Inspector 노출 (기본값): normalZoom = 5f, bossZoom = 7f, zoomSmoothing = 3f
    // Inspector 노출 (기본값): minX = -1000f, maxX = 1000f

    // true  -> 목표 orthographicSize = bossZoom
    // false -> 목표 orthographicSize = normalZoom
    // 멱등(idempotent), 마지막 호출이 이긴다. 반환값 없음.
    public void SetBossZoom(bool isBossStage);
}
```

기존 프로젝트 트리거 선례 (`Assets/map/script/portal.cs`) — 동일한 형태를 따른다:
```csharp
private void OnTriggerEnter2D(Collider2D other)
{
    if (other.CompareTag("Player")) isPlayerNearby = true;
}

private void OnTriggerExit2D(Collider2D other)
{
    if (other.CompareTag("Player")) isPlayerNearby = false;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: BossZoomTrigger.cs 신규 작성</name>

  <files>Assets/Camera/Script/BossZoomTrigger.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — Plan 09-01 적용 후 상태. `SetBossZoom` 시그니처와 `Instance` 선언을 눈으로 확인한다.
    - `Assets/map/script/portal.cs` — `OnTriggerEnter2D`/`OnTriggerExit2D` + `CompareTag("Player")` 선례 (이 파일도 CP949 라 주석이 깨져 보이는 것은 정상).
    - `.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md` — D-01, D-02, D-03, D-08
  </read_first>

  <action>
`Write` 도구로 `Assets/Camera/Script/BossZoomTrigger.cs` 를 아래 내용 **그대로** 신규 생성한다.
신규 파일이므로 인코딩 제약은 없지만, 이 프로젝트의 다른 파일과 섞이지 않도록 **주석은 ASCII 영문만 사용한다**
(아래 내용 그대로 쓰면 자동으로 충족된다).

```csharp
using UnityEngine;

// Put this on a GameObject with a BoxCollider2D (Is Trigger = on) that covers a boss arena (D-01).
// The trigger volumes themselves are placed manually per boss by the designer (D-02) and are
// intentionally NOT created by this phase (D-08).
//
// Zoom sizes and transition speed live on CameraController (normalZoom / bossZoom / zoomSmoothing),
// so this component has no fields and can be dropped on any boss zone as-is.
public class BossZoomTrigger : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        CameraController.Instance.SetBossZoom(true);
    }

    // Leaving the zone reverts to the normal stage zoom immediately, no boss-death event needed (D-03).
    private void OnTriggerExit2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        CameraController.Instance.SetBossZoom(false);
    }
}
```

### 지켜야 할 사항
- **`.meta` 파일을 직접 만들지 말 것.** `Assets/Camera/Script/BossZoomTrigger.cs.meta` 는 Unity 가
  에디터를 열 때 자동 생성한다. GUID 를 손으로 지어내면 나중에 참조가 깨진다.
- 클래스명/파일명은 `BossZoomTrigger` 로 고정한다 (Plan 09-03 의 검증 스크립트와 Check.md 가 이 이름을 참조한다).
- **필드를 추가하지 말 것** — 보스별 줌 값을 트리거에 두면 D-04/D-05 가 정한 "줌 값은 CameraController 의
  Inspector 필드" 구조가 깨진다. 트리거는 필드 0개의 재사용 컴포넌트로 유지한다.
- `CameraController.Instance` 에 대한 null 체크를 추가하지 말 것 — `Portal.cs` 가 `GameManager.Instance` 를
  null 체크 없이 쓰는 기존 관례를 따른다 (RESEARCH Pattern 2 의 명시적 결론).
- 참조 카운팅(겹치는 보스 구역 처리)을 추가하지 말 것 — 마지막 호출이 이기는 단순 방식이 확정 사항이다
  (RESEARCH Open Question 2 결론, 과설계 금지).
- `namespace` 를 넣지 말 것 — 이 프로젝트의 게임플레이 스크립트는 전부 최상위 네임스페이스다.
- 씬 파일(`.unity`)을 수정하지 말 것 (D-08).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/BossZoomTrigger.cs && [ -f $F ] && grep -qF "public class BossZoomTrigger : MonoBehaviour" $F && grep -qF "private void OnTriggerEnter2D(Collider2D other)" $F && grep -qF "private void OnTriggerExit2D(Collider2D other)" $F && [ "$(grep -cF 'other.CompareTag(\"Player\")' $F)" = "2" ] && grep -qF "CameraController.Instance.SetBossZoom(true);" $F && grep -qF "CameraController.Instance.SetBossZoom(false);" $F && [ "$(grep -cF 'namespace' $F)" = "0" ] && [ "$(grep -cF '[SerializeField]' $F)" = "0" ] && [ "$(grep -cF 'public float' $F)" = "0" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "0" ] && [ ! -f "$F.meta" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `Assets/Camera/Script/BossZoomTrigger.cs` 파일이 존재한다
    - `grep -qF "public class BossZoomTrigger : MonoBehaviour" Assets/Camera/Script/BossZoomTrigger.cs` 성공
    - `grep -cF 'other.CompareTag("Player")' Assets/Camera/Script/BossZoomTrigger.cs` == 2 (Enter/Exit 양쪽 가드)
    - `grep -qF "CameraController.Instance.SetBossZoom(true);"` 및 `grep -qF "CameraController.Instance.SetBossZoom(false);"` 성공 (D-01 진입 / D-03 이탈 복귀)
    - `grep -cF 'namespace'` == 0, `grep -cF '[SerializeField]'` == 0, `grep -cF 'public float'` == 0 (필드 없는 재사용 컴포넌트)
    - `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/BossZoomTrigger.cs` == 0 (ASCII 전용)
    - `git status --porcelain Assets/Scenes/` 결과에 이 플랜이 새로 추가한 씬 변경이 없다 (D-08 — 씬 미편집)
    - `[ ! -f Assets/Camera/Script/BossZoomTrigger.cs.meta ]` 성공 — .meta 를 손으로 만들지 않았다 (Unity 가 다음 에디터 실행 시 자동 생성)
  </acceptance_criteria>

  <done>필드 0개의 `BossZoomTrigger` 컴포넌트가 존재하며, Player 태그 가드 뒤에서 진입 시 `SetBossZoom(true)`, 이탈 시 `SetBossZoom(false)` 를 호출한다. 씬 파일은 건드리지 않았다.</done>
</task>

<task type="auto">
  <name>Task 2: Assets/Camera/Check.md — Play 모드 검증 체크리스트 작성</name>

  <files>Assets/Camera/Check.md</files>

  <read_first>
    - `Assets/Enemy/WaterMonster/Check.md` — 이 프로젝트의 Check.md 포맷 선례(상태 표기 / 검증 대상 변경사항 / 사전 준비 / 검증 항목 `- [ ]` / 결과 기록)
    - `Assets/Camera/Script/CameraController.cs` — 실제 노출된 Inspector 필드명을 확인해 체크리스트에 정확히 적는다
    - `Assets/Camera/Script/BossZoomTrigger.cs` — Task 1 결과
  </read_first>

  <action>
`Write` 도구로 `Assets/Camera/Check.md` 를 아래 내용 그대로 생성한다 (이 파일은 신규이므로 UTF-8 한글 사용 가능).

```markdown
# Camera — 스테이지 줌 전환 & X축 이동 제한 Play 모드 검증 체크리스트

**상태: 보류** — Phase 9 결과이며,
`.planning/phases/09-camera-zoom-stage-transition/09-03-PLAN.md` 체크포인트에서 검증한다.

## 검증 대상 변경사항

`Assets/Camera/Script/CameraController.cs` (위치 추종 전용 → 줌 + X축 클램프 추가, 기존 로직은 삽입만 하고 무변경)
`Assets/Camera/Script/BossZoomTrigger.cs` (신규 — 보스 구역 트리거)

| Inspector 필드 | 기본값 | 의미 | 결정 |
|---|---|---|---|
| `normalZoom` | 5 | 일반 스테이지 orthographic size | D-04 / D-05 |
| `bossZoom` | 7 | 보스 구역 orthographic size | D-04 / D-05 |
| `zoomSmoothing` | 3 | 줌 Lerp 속도 (위치 추종 `smoothing`=5 와 별개) | D-06 / D-07 |
| `minX` | -1000 | 카메라 화면 좌측 한계 (월드 X) | D-09 / D-10 |
| `maxX` | 1000 | 카메라 화면 우측 한계 (월드 X) | D-09 / D-10 |

`LateUpdate` 실행 순서: 위치 추종 Lerp → 줌 Lerp → X 클램프.
클램프는 `minX + halfWidth` ~ `maxX - halfWidth` 범위이며 `halfWidth = orthographicSize * aspect` (D-11).
**Y축은 의도적으로 제한하지 않는다 (D-09).**

`minX`/`maxX` 기본값이 넓은(-1000/1000) 이유: 기존 씬의 카메라 동작을 바꾸지 않기 위해서다.
실제 값은 씬마다 사용자가 직접 튜닝한다.

## 사전 준비

1. Unity 에디터를 열고 컴파일이 끝날 때까지 대기한다. Console 에 **에러 0건**인지 확인한다.
2. `Assets/Scenes/1 stage.unity` 를 연다.
3. Hierarchy 에서 빈 GameObject 생성 → 이름 `TempBossZoneTest` 로 변경.
4. `TempBossZoneTest` 에 `BoxCollider2D` 추가 → **Is Trigger 체크** → Size 를 플레이어가 확실히
   드나들 만한 크기로 설정(예: X=10, Y=10).
5. `TempBossZoneTest` 에 `BossZoomTrigger` 컴포넌트를 추가한다.
6. 플레이어가 지나다니는 경로 위에 `TempBossZoneTest` 를 배치한다.
7. Hierarchy 에서 `Main Camera` 선택 → Inspector 에 신규 필드 5개가 보이는지 확인한다.

> 이 `TempBossZoneTest` 오브젝트는 **검증용 임시 오브젝트**다. 검증이 끝나면 삭제해도 되고,
> 실제 보스 구역에 트리거를 배치하는 작업은 D-08 에 따라 사용자가 별도로 진행한다.
> (검증 후 씬을 저장하지 않으면 임시 오브젝트는 남지 않는다.)

## 검증 항목

- [ ] **컴파일 & 필드 노출 (D-05 / D-10)**: Console 에러 0건이고, `Main Camera` Inspector 에
      `Normal Zoom`(5) / `Boss Zoom`(7) / `Zoom Smoothing`(3) / `Min X`(-1000) / `Max X`(1000)
      5개 필드가 모두 보인다.
- [ ] **기존 동작 회귀 없음**: Play 시 카메라가 예전과 동일하게 플레이어를 따라다닌다
      (시작 순간 튀거나 줌이 갑자기 변하지 않는다).
- [ ] **진입 줌 전환 (D-01 / D-04 / D-06)**: 플레이어가 `TempBossZoneTest` 안으로 들어가면
      Main Camera 의 `Size` 가 5 → 7 로 **점프 없이 부드럽게** 증가한다 (Inspector 의 Camera >
      Size 값이 몇 프레임에 걸쳐 변하는 것으로 확인).
- [ ] **이탈 자동 복귀 (D-03)**: 트리거 밖으로 나오면 별도 조작 없이 `Size` 가 7 → 5 로 되돌아온다.
- [ ] **재진입 안정성**: 트리거를 빠르게 여러 번 드나들어도 줌이 5/7 사이에서 정상 동작하고
      중간 값에 멈추거나 에러가 나지 않는다.
- [ ] **Inspector 튜닝 가능 (D-05)**: Play 중 `Boss Zoom` 을 9 로 바꾸고 다시 트리거에 들어가면
      9 로 확대된다 (하드코딩이 아님을 확인).
- [ ] **줌 속도 분리 (D-07)**: Play 중 `Zoom Smoothing` 을 1 로 낮추면 줌 전환만 느려지고,
      플레이어 추종 속도(`Smoothing` = 5)는 그대로다.
- [ ] **X축 클램프 — 일반 줌 (D-09 / D-10 / D-11)**: `Min X` / `Max X` 를 실제 맵 좌우 끝 좌표에
      맞춰 설정한 뒤, 플레이어가 맵 좌/우 끝까지 이동해도 화면에 맵 경계 바깥(빈 공간)이 보이지 않는다.
- [ ] **X축 클램프 — 보스 줌 (D-11 핵심)**: 같은 `Min X` / `Max X` 값에서 **보스 줌(7) 상태로**
      맵 좌/우 끝까지 이동해도 경계 바깥이 보이지 않는다. (줌 7 은 화면이 더 넓어 여기서 새기 쉽다 —
      반드시 확인할 것.)
- [ ] **Y축 무제한 (D-09)**: 플레이어가 위/아래로 이동할 때 카메라 Y 는 여전히 제한 없이 따라간다.
- [ ] **경계가 화면보다 좁을 때(튜닝 노트)**: `Max X - Min X` 가 화면 폭(= `Size * aspect * 2`)보다
      좁게 설정되면 카메라가 한쪽으로 붙는다. 이는 방어 코드를 넣지 않기로 한 알려진 동작이며,
      이 경우 `Min X` / `Max X` 를 더 넓게 잡으면 된다. 보스 줌(7) 기준으로 여유가 있는지 확인한다.

## 튜닝 값 기록

| 씬 | Min X | Max X | 비고 |
|---|---|---|---|
| 1 stage.unity | | | |
| Tutorial Map.unity | | | |

## 결과 기록

(검증 완료 시 여기에 관찰 내용과 각 항목 PASS/FAIL 을 기록한다.)
```

체크리스트 항목의 `- [ ]` 는 **체크되지 않은 상태로 둔다** — Plan 09-03 의 체크포인트에서 사용자 검증 후 채운다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Check.md && [ -f $F ] && grep -qF "## 검증 항목" $F && grep -qF "## 결과 기록" $F && grep -qF "## 사전 준비" $F && [ "$(grep -c '^- \[ \]' $F)" -ge 10 ] && [ "$(grep -c '^- \[x\]' $F)" = "0" ] && grep -qF "BossZoomTrigger" $F && grep -qF "TempBossZoneTest" $F && grep -qF "D-11" $F && grep -qF "D-03" $F && grep -qF "D-07" $F && grep -qF "D-09" $F && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `Assets/Camera/Check.md` 가 존재하고 `## 검증 대상 변경사항` / `## 사전 준비` / `## 검증 항목` / `## 결과 기록` 4개 섹션을 포함한다 (WaterMonster Check.md 포맷 준수)
    - `grep -c '^- \[ \]' Assets/Camera/Check.md` >= 10 (미체크 항목으로 작성됨)
    - `grep -c '^- \[x\]' Assets/Camera/Check.md` == 0 (이 플랜에서 미리 체크하지 않음)
    - 체크리스트에 `D-01`, `D-03`, `D-04`, `D-05`, `D-06`, `D-07`, `D-09`, `D-10`, `D-11` 결정 ID 가 각각 최소 1회 등장한다
    - 임시 트리거 생성 절차(`TempBossZoneTest`, `BoxCollider2D`, `Is Trigger`, `BossZoomTrigger`)가 사전 준비에 명시되어 있다
    - Inspector 필드명 5종(`normalZoom`/`Normal Zoom`, `bossZoom`/`Boss Zoom`, `zoomSmoothing`/`Zoom Smoothing`, `minX`/`Min X`, `maxX`/`Max X`)이 문서에 등장한다
    - 보스 줌(7)에서의 클램프 확인 항목과 Y축 무제한 확인 항목이 각각 별도 항목으로 존재한다
  </acceptance_criteria>

  <done>`Assets/Camera/Check.md` 에 Phase 9 의 Play 모드 검증 항목이 미체크 상태로 정리되어 있고, 임시 트리거 생성 절차와 씬별 minX/maxX 튜닝 값 기록표가 포함되어 있다.</done>
</task>

</tasks>

<verification>
1. `Assets/Camera/Script/BossZoomTrigger.cs` 가 존재하며 `CameraController.Instance.SetBossZoom(true/false)` 를 Enter/Exit 에서 각각 호출한다.
2. 트리거 컴포넌트에 직렬화 필드가 0개다 (어느 보스 구역에나 그대로 재사용 가능, D-02).
3. `Assets/Camera/Check.md` 에 미체크 검증 항목이 10개 이상 있다.
4. `Assets/Scenes/*.unity` 파일이 이 플랜에서 수정되지 않았다 (D-08).
5. `Assets/Camera/Script/CameraController.cs` 가 이 플랜에서 수정되지 않았다 (Plan 09-01 소유 파일).
</verification>

<success_criteria>
- 씬 로드가 아닌 트리거 콜라이더 기반 보스 구역 감지 경로가 코드로 완성되었다 (D-01).
- 이탈 시 자동으로 일반 줌으로 복귀하는 호출이 존재한다 (D-03).
- 보스별 수동 배치를 전제로 한 필드 없는 재사용 컴포넌트다 (D-02).
- 실제 씬 배치는 하지 않았다 (D-08).
</success_criteria>

<output>
완료 후 `.planning/phases/09-camera-zoom-stage-transition/09-02-SUMMARY.md` 를 생성한다.
커밋은 `Assets/Camera/Script/BossZoomTrigger.cs` 와 `Assets/Camera/Check.md` 두 경로만 지정해서 수행한다.
</output>
