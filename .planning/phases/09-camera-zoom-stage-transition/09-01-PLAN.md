---
phase: 09-camera-zoom-stage-transition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Camera/Script/CameraController.cs
autonomous: true
requirements: [D-04, D-05, D-06, D-07, D-09, D-10, D-11]
must_haves:
  truths:
    - "외부 스크립트가 CameraController.Instance.SetBossZoom(true/false) 로 카메라 줌 목표값을 바꿀 수 있다"
    - "일반 줌(5) / 보스 줌(7) / 줌 전환 속도가 Inspector 에서 각각 조정 가능하다"
    - "줌 전환이 즉시 점프가 아니라 Lerp 로 부드럽게 진행된다"
    - "카메라 화면의 좌/우 끝이 minX/maxX 를 넘지 않으며, 줌 5 든 7 이든 반폭이 반영된다"
    - "기존 위치 추종 로직과 기존 한글(CP949) 주석이 한 줄도 삭제/변경되지 않는다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "싱글톤 Instance + 줌 상태/Lerp + X축 클램프"
      contains: "public void SetBossZoom(bool isBossStage)"
  key_links:
    - from: "CameraController.LateUpdate"
      to: "_cam.orthographicSize"
      via: "Mathf.Lerp 줌 보간"
      pattern: "Mathf\\.Lerp\\(_cam\\.orthographicSize, _targetZoom, zoomSmoothing"
    - from: "CameraController.ApplyXClamp"
      to: "transform.position.x"
      via: "orthographicSize * aspect 반폭 반영 Mathf.Clamp"
      pattern: "_cam\\.orthographicSize \\* _cam\\.aspect"
    - from: "CameraController.Awake"
      to: "CameraController.Instance"
      via: "씬 로컬 싱글톤 할당 (DontDestroyOnLoad 없음)"
      pattern: "Instance = this;"
---

<objective>
`Assets/Camera/Script/CameraController.cs` (현재 25줄, 위치 추종 전용)에 두 가지 기능을 **삽입만으로** 추가한다:
1. 씬 로컬 싱글톤 `Instance` + `Camera` 캐시 + Inspector 노출 줌 필드 + `SetBossZoom(bool)` + `LateUpdate` 줌 Lerp (D-04~D-07)
2. `minX`/`maxX` Inspector 필드 + 화면 반폭(`orthographicSize * aspect`)을 반영한 X축 클램프 (D-09~D-11)

Purpose: 보스 스테이지 트리거(Plan 09-02)가 호출할 수 있는 카메라 측 API와 줌/클램프 실행 로직을 먼저 확정한다.
Output: 확장된 `CameraController.cs` 1개 파일. 신규 파일 없음, 씬 편집 없음.

**이 플랜의 CameraController.cs 변경은 100% 라인 삽입이다. 기존 라인은 한 줄도 삭제/수정하지 않는다.**
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md
@.planning/phases/09-camera-zoom-stage-transition/09-RESEARCH.md
@Assets/Camera/Script/CameraController.cs
</context>

<critical_constraints>
## 1. 파일 인코딩 (RESEARCH Pitfall 3 — 최우선 주의사항)

`CameraController.cs` 는 UTF-8 이 아닌 CP949/EUC-KR 로 저장되어 있다. Read 도구로 읽으면 한글 주석이
`���ΰ�` 같은 깨진 문자로 보인다. **이것은 이 파일의 기존 상태이며, 이번 작업으로 고치지 않는다.**

지켜야 할 규칙:
- **Write 도구로 파일 전체를 다시 쓰지 말 것.** 반드시 `Edit` 도구로 대상 라인만 삽입한다.
- **`old_string` 에 깨진 한글 주석 라인을 절대 포함하지 말 것.** 아래 지정된 ASCII 전용 앵커 라인만 사용한다.
- **새로 추가하는 주석은 ASCII 영문만 사용한다 (한글 금지).** CP949 파일에 UTF-8 한글 바이트를 섞으면
  Unity/Visual Studio 에서 새 주석이 깨져 보인다. 이 플랜의 코드 블록에 적힌 영문 주석을 그대로 쓴다.
- 검증 게이트: 작업 후에도 `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs`
  결과가 **정확히 5** 여야 한다 (현재 값 = 5). 5보다 크면 한글을 새로 넣은 것이고, 5보다 작으면
  기존 주석을 훼손한 것이다.

## 2. 작업 트리 상태

리포지토리에는 Phase 9 이전부터 커밋되지 않은 변경(TutorialBoss 스크립트, 메뉴 패널, 씬 등)이 다수 존재한다.
**커밋 시 `git add -A` / `git commit -a` 를 절대 사용하지 말고**, `Assets/Camera/Script/CameraController.cs`
경로만 명시적으로 지정해 커밋한다.

## 3. 범위 밖 (하지 말 것)

- `DontDestroyOnLoad` 추가 금지 — 스테이지마다 자기 카메라를 갖는 구조라 씬 전환 시 카메라가 중복된다 (RESEARCH Pattern 1).
- 중복 인스턴스 `Destroy(gameObject)` 처리 / `OnDestroy` 정리 코드 추가 금지 — 씬당 카메라 1개, 최소 구현 유지.
- Y축 클램프(`minY`/`maxY`) 추가 금지 — D-09 가 명시적으로 이번 범위에서 제외.
- `BoxCollider2D` 기반 bounds 참조 방식 금지 — D-10 이 float 필드 방식을 명시적으로 선택했다.
- `OnTriggerEnter2D`/`OnTriggerExit2D` 를 `CameraController` 에 추가 금지 — 트리거는 Plan 09-02 의 별도 파일.
- 기존 `if (target == null) return;` 가드 라인 수정 금지. (결과적으로 target 미할당 시 줌/클램프도 함께
  멈추지만, 그 경우 카메라 자체가 정지 상태이므로 무해하다. 최소 diff 를 우선한다.)
</critical_constraints>

<verified_facts>
Plan 작성 시 씬 YAML 로 직접 확인한 사실 — 실행자는 재확인할 필요 없다:

- `Assets/Scenes/1 stage.unity` 와 `Assets/Scenes/Tutorial Map.unity` 모두에서 `CameraController`
  (MonoBehaviour fileID 519420033, script guid `3e003a89e96099a498c5212ff3169707`)는 GameObject
  fileID 519420028 = **"Main Camera"** (TagString: MainCamera)에 붙어 있고, **같은 GameObject 에
  `Camera` 컴포넌트(`!u!20 &519420031`)가 존재**한다.
  → `GetComponent<Camera>()` 로 충분하다. `GetComponentInChildren<Camera>()` / `Camera.main` 불필요.
  (09-RESEARCH.md Open Question 1 해소됨.)
- 두 씬 모두 카메라는 `orthographic: 1`, `orthographic size: 5` 로 직렬화되어 있다 → D-04 의 일반 줌 5 와 일치.
</verified_facts>

<tasks>

<task type="auto">
  <name>Task 1: 싱글톤 Instance + Camera 캐시 + 줌 필드/SetBossZoom + LateUpdate 줌 Lerp</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **작업 직전에 반드시 Read 로 현재 내용을 확인한다.**
      Edit 의 `old_string` 은 이 Read 결과에서 그대로 복사한 ASCII 라인이어야 한다 (기억으로 재작성 금지).
    - `.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md` — D-04 ~ D-07 원문
    - `.planning/phases/09-camera-zoom-stage-transition/09-RESEARCH.md` — Pattern 1(싱글톤), Pattern 3(LateUpdate 순서), Pitfall 3(인코딩)
  </read_first>

  <action>
`Edit` 도구로 **3회 삽입**한다. 각 삽입은 지정된 앵커 라인을 `old_string` 으로 삼고,
`new_string` = 앵커 라인 + 새 라인들 (앵커 라인 자체는 절대 변경하지 않는다).

### 삽입 1 — 필드 + 싱글톤 + Awake + SetBossZoom

앵커(`old_string`, 파일에서 유일):
```
    public Vector3 offset = new Vector3(0f, 0f, 10f);
```

`new_string` = 위 앵커 라인 뒤에 아래 블록을 그대로 이어 붙인다 (앞의 빈 줄 포함):
```csharp

    [Header("Stage Zoom")]
    // Orthographic size used on normal stages (D-04 / D-05).
    public float normalZoom = 5f;
    // Orthographic size used while the player is inside a BossZoomTrigger zone (D-04 / D-05).
    public float bossZoom = 7f;
    // Lerp rate for the zoom transition ONLY. Kept separate from 'smoothing' on purpose (D-07).
    public float zoomSmoothing = 3f;

    // Scene-local singleton. BossZoomTrigger calls in through this (D-01).
    // No DontDestroyOnLoad on purpose: every stage scene owns its own camera.
    public static CameraController Instance { get; private set; }

    private Camera _cam;
    private float _targetZoom;

    void Awake()
    {
        Instance = this;
        _cam = GetComponent<Camera>();
        _targetZoom = normalZoom;
    }

    // Called by BossZoomTrigger: true on enter, false on exit (D-01 / D-03).
    // Idempotent, last call wins - repeated calls simply overwrite the target size.
    public void SetBossZoom(bool isBossStage)
    {
        _targetZoom = isBossStage ? bossZoom : normalZoom;
    }
```

### 삽입 2 — Start 에서 시작 줌 스냅

앵커(`old_string`, 파일에서 유일 — `Start()` 안의 라인):
```
        transform.position = target.position + offset;
```

`new_string` = 위 앵커 라인 뒤에 아래 2줄을 이어 붙인다:
```csharp
        // Start already at the normal-stage zoom so frame 1 does not play a transition.
        _cam.orthographicSize = normalZoom;
```

### 삽입 3 — LateUpdate 줌 Lerp

앵커(`old_string`, 파일에서 유일 — `LateUpdate()` 안의 마지막 실행 라인):
```
        transform.position = Vector3.Lerp(transform.position, targetCamPos, smoothing * Time.deltaTime);
```

`new_string` = 위 앵커 라인 뒤에 아래 2줄을 이어 붙인다:
```csharp
        // Zoom Lerp toward the current stage target size (D-06 / D-07).
        _cam.orthographicSize = Mathf.Lerp(_cam.orthographicSize, _targetZoom, zoomSmoothing * Time.deltaTime);
```

### 값 근거 (임의로 바꾸지 말 것)
- `normalZoom = 5f`, `bossZoom = 7f` — D-04 가 지정한 초기값. 씬의 직렬화 값(5)과도 일치.
- `zoomSmoothing = 3f` — D-07 은 "별도 필드"만 요구하고 값은 미지정. 위치 추종(5f)보다 느리게 잡아
  전환이 눈에 보이도록 한 초기 튜닝값이며, Inspector 에서 조정 가능하다.
- `public` 필드 사용 — 이 파일의 기존 스타일(`public Transform target;`, `public float smoothing = 5f;`)과
  동일하게 맞춘 것이다. `[SerializeField] private` 로 바꾸지 말 것.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "public static CameraController Instance { get; private set; }" $F && grep -qF "_cam = GetComponent<Camera>();" $F && grep -qF "_targetZoom = normalZoom;" $F && [ "$(grep -cF 'DontDestroyOnLoad' $F)" = "0" ] && grep -qF "public float normalZoom = 5f;" $F && grep -qF "public float bossZoom = 7f;" $F && grep -qF "public float zoomSmoothing = 3f;" $F && grep -qF "public void SetBossZoom(bool isBossStage)" $F && grep -qF "_targetZoom = isBossStage ? bossZoom : normalZoom;" $F && grep -qF "_cam.orthographicSize = Mathf.Lerp(_cam.orthographicSize, _targetZoom, zoomSmoothing * Time.deltaTime);" $F && grep -qF "_cam.orthographicSize = normalZoom;" $F && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff 184ed58 -- $F | grep '^-' | grep -vc '^---')" = "0" ] && PL=$(grep -nF 'Vector3.Lerp(transform.position' $F | cut -d: -f1) && ZL=$(grep -nF 'Mathf.Lerp(_cam.orthographicSize' $F | cut -d: -f1) && [ "$ZL" -gt "$PL" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "public static CameraController Instance { get; private set; }" Assets/Camera/Script/CameraController.cs` 성공
    - `grep -qF "_cam = GetComponent<Camera>();"` 성공 (`GetComponentInChildren` / `Camera.main` 사용 금지)
    - `grep -cF 'DontDestroyOnLoad' Assets/Camera/Script/CameraController.cs` == 0
    - `grep -cF 'Destroy(gameObject)' Assets/Camera/Script/CameraController.cs` == 0
    - `grep -qF "public float normalZoom = 5f;"` / `grep -qF "public float bossZoom = 7f;"` / `grep -qF "public float zoomSmoothing = 3f;"` 3개 모두 성공 (D-04, D-05, D-07)
    - `grep -qF "public void SetBossZoom(bool isBossStage)"` 및 `grep -qF "_targetZoom = isBossStage ? bossZoom : normalZoom;"` 성공 (D-03 자동 복귀 경로)
    - `grep -qF "_cam.orthographicSize = Mathf.Lerp(_cam.orthographicSize, _targetZoom, zoomSmoothing * Time.deltaTime);"` 성공 (D-06 Lerp, D-07 별도 속도 필드 사용)
    - `grep -nF 'Mathf.Lerp(_cam.orthographicSize'` 의 행 번호 > `grep -nF 'Vector3.Lerp(transform.position'` 의 행 번호 (RESEARCH Pattern 3 순서)
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **삽입 전용 게이트**: `git diff 184ed58 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == 0 (삭제된 라인 0줄)
  </acceptance_criteria>

  <done>`CameraController` 가 `Instance` 싱글톤과 `_cam` 캐시를 갖고, Inspector 에 normalZoom/bossZoom/zoomSmoothing 3개 필드가 노출되며, `SetBossZoom(bool)` 호출로 바뀐 `_targetZoom` 을 향해 `LateUpdate` 에서 위치 추종 이후에 `orthographicSize` 가 Lerp 된다. 기존 라인 삭제 0줄, 비-ASCII 라인 수 5 유지.</done>
</task>

<task type="auto">
  <name>Task 2: minX/maxX 필드 + 화면 반폭 반영 X축 클램프</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **Task 1 적용 후 상태를 다시 Read 한다.** 앵커 라인은 이 Read 결과에서 복사한다.
    - `.planning/phases/09-camera-zoom-stage-transition/09-CONTEXT.md` — D-09 ~ D-11 원문 (Y축 제외 명시 확인)
    - `.planning/phases/09-camera-zoom-stage-transition/09-RESEARCH.md` — Pattern 3(클램프는 마지막), Pitfall 1(반폭 공식 방향), Pitfall 2(min/max 가 화면 폭보다 좁을 때)
  </read_first>

  <action>
`Edit` 도구로 **4회 삽입**한다. Task 1 과 동일하게 앵커 라인은 변경하지 않고 뒤에 이어 붙이기만 한다.

### 삽입 1 — 경계 필드

앵커(`old_string`, Task 1 이 추가한 라인):
```
    public float zoomSmoothing = 3f;
```

`new_string` = 앵커 라인 + 아래 블록:
```csharp

    [Header("Camera X Bounds")]
    // World-space X limits of the visible area (D-09 / D-10).
    // Defaults are intentionally wide so existing scenes keep their current behaviour
    // until minX / maxX are tuned per scene in the Inspector.
    // Y is NOT clamped in this phase by design (D-09).
    public float minX = -1000f;
    public float maxX = 1000f;
```

### 삽입 2 — ApplyXClamp 메서드

앵커(`old_string`, Task 1 이 추가한 `SetBossZoom` 본문 + 닫는 중괄호 — 2줄 그대로):
```
        _targetZoom = isBossStage ? bossZoom : normalZoom;
    }
```

`new_string` = 위 2줄 + 아래 블록:
```csharp

    // Clamps X so the visible left/right edges never pass minX / maxX (D-09 / D-11).
    // Uses the CURRENT orthographicSize so the bound stays correct mid zoom transition.
    private void ApplyXClamp()
    {
        float halfWidth = _cam.orthographicSize * _cam.aspect;
        Vector3 pos = transform.position;
        pos.x = Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth);
        transform.position = pos;
    }
```

### 삽입 3 — Start 호출부

앵커(`old_string`, Task 1 이 Start 에 추가한 라인):
```
        _cam.orthographicSize = normalZoom;
```
> 주의: 이 문자열은 Task 1 이후 파일에서 유일하다 (LateUpdate 쪽은 `Mathf.Lerp(...)` 형태라 다르다).

`new_string` = 앵커 라인 + 아래 1줄:
```csharp
        ApplyXClamp();
```

### 삽입 4 — LateUpdate 호출부 (반드시 줌 Lerp **뒤**)

앵커(`old_string`, Task 1 이 LateUpdate 에 추가한 라인):
```
        _cam.orthographicSize = Mathf.Lerp(_cam.orthographicSize, _targetZoom, zoomSmoothing * Time.deltaTime);
```

`new_string` = 앵커 라인 + 아래 2줄:
```csharp
        // X clamp LAST so it uses this frame's freshly updated orthographicSize.
        ApplyXClamp();
```

### 값·공식 근거 (임의로 바꾸지 말 것)
- `halfWidth = _cam.orthographicSize * _cam.aspect` — D-11 이 명시한 공식 그대로다.
  `/ _cam.aspect` 로 바꾸지 말 것 (RESEARCH Pitfall 1: 좌우 반전된 잘못된 경계가 된다).
- `minX = -1000f`, `maxX = 1000f` — D-09/D-10 은 값을 지정하지 않았다. 기존 씬(`1 stage.unity`,
  `Tutorial Map.unity`)의 카메라에 이 스크립트가 이미 붙어 있으므로, 좁은 기본값(예: -10/10)을 쓰면
  스크립트 저장 즉시 기존 씬의 카메라가 x≈0 부근에 고정되는 회귀가 발생한다. 넓은 기본값은
  사실상 "클램프 없음"이라 기존 동작을 보존하며, 실제 값은 사용자가 씬별로 Inspector 에서 튜닝한다.
- `maxX - minX` 가 화면 폭보다 좁으면 `Mathf.Clamp` 의 min > max 상황이 되어 카메라가 한쪽으로 붙는다.
  **방어 코드를 추가하지 말 것** (요구사항 없음, RESEARCH Pitfall 2 권고). 튜닝 주의사항은
  Plan 09-02 의 Check.md 에 기록된다.
- `minY`/`maxY` 나 `pos.y` 클램프를 추가하지 말 것 (D-09).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "public float minX = -1000f;" $F && grep -qF "public float maxX = 1000f;" $F && grep -qF "private void ApplyXClamp()" $F && grep -qF "float halfWidth = _cam.orthographicSize * _cam.aspect;" $F && grep -qF "pos.x = Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth);" $F && [ "$(grep -cF 'ApplyXClamp();' $F)" = "2" ] && [ "$(grep -cF 'minY' $F)" = "0" ] && [ "$(grep -cF 'maxY' $F)" = "0" ] && [ "$(grep -cF 'pos.y = Mathf.Clamp' $F)" = "0" ] && [ "$(grep -cF 'BoxCollider2D' $F)" = "0" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff 184ed58 -- $F | grep '^-' | grep -vc '^---')" = "0" ] && ZL=$(grep -nF 'Mathf.Lerp(_cam.orthographicSize' $F | cut -d: -f1) && CL=$(grep -nF 'ApplyXClamp();' $F | tail -1 | cut -d: -f1) && [ "$CL" -gt "$ZL" ] && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "public float minX = -1000f;"` / `grep -qF "public float maxX = 1000f;"` 성공 (D-10 float Inspector 필드)
    - `grep -qF "float halfWidth = _cam.orthographicSize * _cam.aspect;"` 성공 — 곱셈이며 나눗셈이 아님 (D-11 / Pitfall 1)
    - `grep -qF "pos.x = Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth);"` 성공
    - `grep -cF 'ApplyXClamp();' Assets/Camera/Script/CameraController.cs` == 2 (Start 1회 + LateUpdate 1회)
    - LateUpdate 내 호출 순서: `grep -nF 'ApplyXClamp();' | tail -1` 의 행 번호 > `grep -nF 'Mathf.Lerp(_cam.orthographicSize'` 의 행 번호 (클램프가 줌 Lerp 뒤)
    - `grep -cF 'minY'` == 0, `grep -cF 'maxY'` == 0, `grep -cF 'pos.y = Mathf.Clamp'` == 0 (D-09 Y축 제외)
    - `grep -cF 'BoxCollider2D' Assets/Camera/Script/CameraController.cs` == 0 (D-10 이 거부한 bounds 참조 방식 아님)
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **삽입 전용 게이트**: `git diff 184ed58 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == 0
  </acceptance_criteria>

  <done>Inspector 에 minX/maxX 가 노출되고, `ApplyXClamp()` 가 `orthographicSize * aspect` 반폭을 반영해 X 만 클램프하며, `Start`(초기 1회)와 `LateUpdate`(줌 Lerp 직후)에서 호출된다. Y 클램프 코드 없음, 기존 라인 삭제 0줄, 비-ASCII 라인 수 5 유지.</done>
</task>

</tasks>

<verification>
1. `Assets/Camera/Script/CameraController.cs` 의 `LateUpdate` 실행 순서가 **위치 추종 Lerp → 줌 Lerp → X 클램프** 이다 (행 번호로 확인).
2. Inspector 노출 신규 필드 5개: `normalZoom`, `bossZoom`, `zoomSmoothing`, `minX`, `maxX` (D-05, D-10).
3. `SetBossZoom(bool)` 이 public 이며 `CameraController.Instance` 로 접근 가능하다 (Plan 09-02 의 진입점).
4. `DontDestroyOnLoad` / `Destroy(gameObject)` / `minY` / `maxY` / `BoxCollider2D` 문자열이 파일에 없다.
5. 인코딩 게이트 통과: 비-ASCII 포함 라인 수 == 5, 삭제된 라인 0줄.
6. 이 플랜에서 수정된 파일은 `Assets/Camera/Script/CameraController.cs` 단 1개이며, 커밋에 다른 미커밋 변경이 섞이지 않았다.
</verification>

<success_criteria>
- 줌 목표값 전환 API(`SetBossZoom`)와 Lerp 기반 줌 전환이 구현되어 있다 (D-01 연결점, D-03, D-06).
- 줌 값과 줌 속도가 하드코딩이 아니라 Inspector 필드다 (D-04, D-05, D-07 — `smoothing` 과 분리).
- X축 클램프가 현재 줌의 화면 반폭을 반영한다 (D-09, D-10, D-11).
- 기존 CP949 한글 주석과 기존 위치 추종 로직이 손상되지 않았다.
</success_criteria>

<output>
완료 후 `.planning/phases/09-camera-zoom-stage-transition/09-01-SUMMARY.md` 를 생성한다.
커밋은 `Assets/Camera/Script/CameraController.cs` 경로만 지정해서 수행한다 (작업 트리에 Phase 9 무관 미커밋 변경 다수 존재).
</output>
