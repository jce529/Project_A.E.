---
phase: 03-explosion-gimmick-teleport
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs
  - Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs
autonomous: true
requirements:
  - REQ-WM-P3-01
  - REQ-WM-X-01

must_haves:
  truths:
    - "Indestructible 웅덩이가 임계치(기본 5)에 도달하면 2초 경고 후 전체 동시 AoE 폭발이 발동한다"
    - "폭발 AoE는 Player 레이어에만 대미지를 준다"
    - "폭발 후 모든 Indestructible 웅덩이가 Pool로 반환되고 카운트가 0으로 리셋된다"
    - "폭발 진행 중 추가 흡수로 인한 중복 폭발이 발생하지 않는다"
  artifacts:
    - path: "Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs"
      provides: "폭발 시퀀스 MonoBehaviour — 경고 + 동시 AoE + Pool Return"
      contains: "class PuddleExplosionController"
    - path: "Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs"
      provides: "Indestructible 웅덩이 참조 목록 + 일괄 반환 API"
      contains: "_indestructiblePuddles"
  key_links:
    - from: "PuddleExplosionController"
      to: "PuddleStackManager.OnThresholdReached"
      via: "event subscription in OnEnable/OnDisable"
      pattern: "OnThresholdReached.*\\+="
    - from: "PuddleExplosionController.ExplodeAllIndestructible"
      to: "Physics2D.OverlapCircleAll"
      via: "Player LayerMask AoE per puddle position"
      pattern: "OverlapCircleAll.*Player"
    - from: "PuddleExplosionController"
      to: "PuddleStackManager.ReturnAllIndestructibleToPool"
      via: "bulk pool return after explosion"
      pattern: "ReturnAllIndestructibleToPool"
---

<objective>
Indestructible 웅덩이 스택이 임계치에 도달하면 2초 경고 후 전체 동시 AoE 폭발을 발동시키고, 폭발 후 모든 Indestructible 웅덩이를 Pool로 반환하여 텐션-해소 사이클을 구현한다.

Purpose: Phase 3의 핵심 기믹인 연쇄 폭발 — 플레이어가 무분별하게 웅덩이를 흡수하면 치명적 AoE로 벌을 받는 리스크-리워드 메커니즘.
Output: PuddleExplosionController.cs (신규), PuddleStackManager.cs (수정)
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-explosion-gimmick-teleport/03-CONTEXT.md
@.planning/phases/03-explosion-gimmick-teleport/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs:
```csharp
namespace WaterMonster.Phase2
{
    public class PuddleStackManager : MonoBehaviour
    {
        public static PuddleStackManager Instance { get; private set; }
        [SerializeField] private int explosionThreshold = 5;
        private int _indestructibleCount = 0;
        public event Action OnThresholdReached;
        public int IndestructibleCount => _indestructibleCount;

        public void RegisterIndestructible(WaterPuddle puddle)  // called by WaterPuddle.SetIndestructible()
        public void UnregisterIndestructible(WaterPuddle puddle) // called by WaterPuddle.OnReturnToPool()
    }
}
```

From Assets/Enemy/WaterMonster/Script/Phase2/WaterPuddle.cs:
```csharp
namespace WaterMonster.Phase2
{
    public class WaterPuddle : MonoBehaviour
    {
        public bool isDestructible = true;
        private SpriteRenderer _sr;
        public void SetIndestructible()       // sets isDestructible=false, changes color, calls RegisterIndestructible
        public void OnReturnToPool()          // resets isDestructible=true, resets color, calls UnregisterIndestructible, SetActive(false)
    }
}
```

From Assets/Enemy/WaterMonster/Script/Phase2/PuddlePool.cs:
```csharp
namespace WaterMonster.Phase2
{
    public class PuddlePool : MonoBehaviour
    {
        public static PuddlePool Instance { get; private set; }
        public int ActiveCount => _pool.Count(p => p.gameObject.activeSelf);
        public WaterPuddle Get(Vector2 position)
        public void Return(WaterPuddle puddle)  // calls puddle.OnReturnToPool()
    }
}
```

From Assets/Enemy/WaterMonster/Script/States/Attacks/WaterMeleeSwipe.cs (AoE damage pattern):
```csharp
var hits = Physics2D.OverlapCircleAll(origin, HitRadius, LayerMask.GetMask("Player"));
foreach (var hit in hits)
{
    var playerStats = hit.GetComponentInParent<PlayerStats>();
    if (playerStats != null)
        playerStats.TakeDamage(damage);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PuddleStackManager에 Indestructible 웅덩이 참조 목록 및 일괄 반환 API 추가</name>
  <files>Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs</files>
  <read_first>
    - Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs
    - Assets/Enemy/WaterMonster/Script/Phase2/WaterPuddle.cs
    - Assets/Enemy/WaterMonster/Script/Phase2/PuddlePool.cs
  </read_first>
  <action>
PuddleStackManager.cs를 수정하여 다음을 추가한다 (per D-05, D-06):

1. 상단에 `using System.Collections.Generic;` 추가.

2. 새 필드 추가:
```csharp
private List<WaterPuddle> _indestructiblePuddles = new List<WaterPuddle>();
```

3. 읽기 전용 프로퍼티 추가 (PuddleExplosionController와 WaterTeleportState가 목록에 접근하기 위해):
```csharp
public IReadOnlyList<WaterPuddle> IndestructiblePuddles => _indestructiblePuddles;
```

4. `RegisterIndestructible(WaterPuddle puddle)` 메서드 수정 — 기존 `_indestructibleCount++` 다음에 `_indestructiblePuddles.Add(puddle);` 추가:
```csharp
public void RegisterIndestructible(WaterPuddle puddle)
{
    _indestructibleCount++;
    _indestructiblePuddles.Add(puddle);
    if (_indestructibleCount >= explosionThreshold)
    {
        OnThresholdReached?.Invoke();
    }
}
```

5. `UnregisterIndestructible(WaterPuddle puddle)` 메서드 수정 — 기존 조건 내에 `_indestructiblePuddles.Remove(puddle);` 추가:
```csharp
public void UnregisterIndestructible(WaterPuddle puddle)
{
    if (!puddle.isDestructible)
    {
        _indestructibleCount = Mathf.Max(0, _indestructibleCount - 1);
        _indestructiblePuddles.Remove(puddle);
    }
}
```

6. 신규 `ReturnAllIndestructibleToPool()` public 메서드 추가:
```csharp
public void ReturnAllIndestructibleToPool()
{
    var toReturn = new List<WaterPuddle>(_indestructiblePuddles);
    foreach (var puddle in toReturn)
    {
        PuddlePool.Instance.Return(puddle);
    }
    // 강제 리셋 — OnReturnToPool의 UnregisterIndestructible이 isDestructible=true 리셋 후 호출되므로
    // 조건문에 걸려 count가 감소되지 않는 기존 버그를 우회 (03-RESEARCH.md Pitfall 5 참조)
    _indestructibleCount = 0;
    _indestructiblePuddles.Clear();
}
```

기존 `using System;`, `namespace WaterMonster.Phase2`, 싱글턴 패턴, `Awake()`, `explosionThreshold`, `IndestructibleCount` 프로퍼티, `OnThresholdReached` 이벤트는 그대로 유지한다.
  </action>
  <verify>
    <automated>grep -n "_indestructiblePuddles" "Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs" && grep -n "ReturnAllIndestructibleToPool" "Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs" && grep -n "IndestructiblePuddles" "Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs"</automated>
  </verify>
  <acceptance_criteria>
    - PuddleStackManager.cs contains `private List<WaterPuddle> _indestructiblePuddles = new List<WaterPuddle>();`
    - PuddleStackManager.cs contains `public IReadOnlyList<WaterPuddle> IndestructiblePuddles =>`
    - PuddleStackManager.cs contains `public void ReturnAllIndestructibleToPool()`
    - RegisterIndestructible contains `_indestructiblePuddles.Add(puddle);`
    - UnregisterIndestructible contains `_indestructiblePuddles.Remove(puddle);`
    - ReturnAllIndestructibleToPool contains `_indestructibleCount = 0;` and `_indestructiblePuddles.Clear();`
    - File contains `using System.Collections.Generic;`
  </acceptance_criteria>
  <done>PuddleStackManager가 Indestructible 웅덩이의 참조 목록을 유지하며, IndestructiblePuddles 읽기전용 프로퍼티와 ReturnAllIndestructibleToPool() 일괄 반환 메서드를 제공한다.</done>
</task>

<task type="auto">
  <name>Task 2: PuddleExplosionController 생성 — 경고 + 동시 AoE + Pool Return 시퀀스</name>
  <files>Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs</files>
  <read_first>
    - Assets/Enemy/WaterMonster/Script/Phase2/PuddleStackManager.cs (Task 1 수정 후 상태)
    - Assets/Enemy/WaterMonster/Script/Phase2/WaterPuddle.cs
    - Assets/Enemy/WaterMonster/Script/States/Attacks/WaterMeleeSwipe.cs
    - Assets/Player/Script/PlayerStats.cs
  </read_first>
  <action>
신규 파일 `Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs` 생성 (per D-01, D-02, D-03, D-04, D-05):

```csharp
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using WaterMonster.Phase2;

public class PuddleExplosionController : MonoBehaviour
{
    [Header("Explosion Settings")]
    [SerializeField] private float warningDuration = 2f;   // D-03: 인스펙터 튜닝
    [SerializeField] private float explosionRadius = 2.5f;  // 각 웅덩이 기준 AoE 반경
    [SerializeField] private float explosionDamage = 50f;   // 치명적 대미지 (튜닝 위임)

    [Header("Warning VFX")]
    [SerializeField] private Color warningColor = Color.red; // 경고 시 웅덩이 색상

    private Coroutine _explosionRoutine;
    private bool _isExploding = false; // Pitfall 1: 중복 폭발 방지

    private void OnEnable()
    {
        if (PuddleStackManager.Instance != null)
            PuddleStackManager.Instance.OnThresholdReached += OnThresholdReached;
    }

    private void OnDisable()
    {
        if (PuddleStackManager.Instance != null)
            PuddleStackManager.Instance.OnThresholdReached -= OnThresholdReached;
    }

    private void OnThresholdReached()
    {
        if (_isExploding) return; // 중복 발화 방지
        if (_explosionRoutine != null) StopCoroutine(_explosionRoutine);
        _explosionRoutine = StartCoroutine(ExplosionSequence());
    }

    private IEnumerator ExplosionSequence()
    {
        _isExploding = true;

        // D-02: 경고 이펙트 — Indestructible 웅덩이의 SpriteRenderer.color를 빨간색으로 변경
        var puddles = PuddleStackManager.Instance.IndestructiblePuddles;
        var snapshot = new List<WaterPuddle>(puddles); // 복사본 (순회 중 변경 방지)

        foreach (var puddle in snapshot)
        {
            if (puddle != null && puddle.gameObject.activeSelf)
            {
                var sr = puddle.GetComponent<SpriteRenderer>();
                if (sr != null) sr.color = warningColor;
            }
        }

        // D-02, D-03: 경고 딜레이 (기본 2초, 인스펙터 튜닝 가능)
        yield return new WaitForSeconds(warningDuration);

        // D-01: 동시 전체 폭발 — 모든 Indestructible 웅덩이 위치에서 AoE
        foreach (var puddle in snapshot)
        {
            if (puddle != null && puddle.gameObject.activeSelf)
            {
                ApplyExplosionDamage(puddle.transform.position);
            }
        }

        // D-05: 폭발 후 전체 Pool Return + count 리셋
        PuddleStackManager.Instance.ReturnAllIndestructibleToPool();

        _isExploding = false;
        _explosionRoutine = null;
    }

    // D-04: 각 웅덩이 위치에서 반경 기반 OverlapCircleAll, Player 레이어에만 대미지 (REQ-WM-X-01)
    private void ApplyExplosionDamage(Vector3 puddlePosition)
    {
        var hits = Physics2D.OverlapCircleAll(puddlePosition, explosionRadius, LayerMask.GetMask("Player"));
        foreach (var hit in hits)
        {
            var playerStats = hit.GetComponentInParent<PlayerStats>();
            if (playerStats != null)
            {
                playerStats.TakeDamage(explosionDamage);
            }
        }
    }
}
```

주요 설계 포인트:
- `_isExploding` 플래그로 경고 딜레이 2초 동안 추가 흡수로 인한 중복 폭발 방지 (RESEARCH Pitfall 1)
- 웅덩이 목록 snapshot으로 순회 중 컬렉션 변경 안전성 확보
- `ApplyExplosionDamage`는 WaterMeleeSwipe.cs의 `OverlapCircleAll + GetComponentInParent<PlayerStats>` 패턴 그대로 적용
- 경고 VFX는 SpriteRenderer.color 변경 (전용 에셋 불필요, RESEARCH VFX 결론 1번)
- 모든 수치(`warningDuration`, `explosionRadius`, `explosionDamage`, `warningColor`)는 `[SerializeField]`로 인스펙터 튜닝 가능
  </action>
  <verify>
    <automated>test -f "Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs" && grep -n "class PuddleExplosionController" "Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs" && grep -n "_isExploding" "Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs" && grep -n "OverlapCircleAll" "Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs" && grep -n "ReturnAllIndestructibleToPool" "Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs
    - Contains `class PuddleExplosionController : MonoBehaviour`
    - Contains `[SerializeField] private float warningDuration = 2f;`
    - Contains `[SerializeField] private float explosionRadius = 2.5f;`
    - Contains `[SerializeField] private float explosionDamage = 50f;`
    - Contains `private bool _isExploding = false;`
    - Contains `if (_isExploding) return;` in OnThresholdReached
    - Contains `Physics2D.OverlapCircleAll(puddlePosition, explosionRadius, LayerMask.GetMask("Player"))`
    - Contains `GetComponentInParent<PlayerStats>()`
    - Contains `playerStats.TakeDamage(explosionDamage)`
    - Contains `PuddleStackManager.Instance.ReturnAllIndestructibleToPool()`
    - Contains `OnThresholdReached += OnThresholdReached` in OnEnable
    - Contains `OnThresholdReached -= OnThresholdReached` in OnDisable
  </acceptance_criteria>
  <done>PuddleExplosionController가 OnThresholdReached 이벤트에 구독하여 2초 경고 후 동시 AoE 폭발을 실행하고, 폭발 후 모든 Indestructible 웅덩이를 Pool로 반환하며, 중복 폭발을 방지한다.</done>
</task>

</tasks>

<verification>
1. PuddleStackManager.cs에 `_indestructiblePuddles` 목록, `IndestructiblePuddles` 프로퍼티, `ReturnAllIndestructibleToPool()` 메서드가 존재한다.
2. PuddleExplosionController.cs가 `WaterMonster.Phase2.PuddleStackManager.OnThresholdReached`를 구독한다.
3. 폭발 AoE가 `LayerMask.GetMask("Player")`를 사용한다 (REQ-WM-X-01).
4. `_isExploding` 플래그로 중복 폭발이 방지된다.
5. 폭발 후 `ReturnAllIndestructibleToPool()` 호출로 count가 0으로 리셋된다 (D-05).
</verification>

<success_criteria>
- Indestructible 웅덩이 임계치 도달 시 경고(색상 변경) -> 2초 대기 -> 동시 AoE 폭발 -> Pool Return 시퀀스가 동작한다
- 폭발 AoE는 Player 레이어에만 영향을 준다
- 폭발 후 모든 Indestructible 웅덩이가 Pool로 반환되고 카운트가 0이다
- 폭발 진행 중 추가 흡수가 발생해도 중복 폭발이 일어나지 않는다
- 리셋 후 다시 흡수하면 스택이 다시 쌓이는 사이클이 반복된다 (D-06)
</success_criteria>

<output>
After completion, create `.planning/phases/03-explosion-gimmick-teleport/03-01-SUMMARY.md`
</output>
