---
phase: 12-camera-shake-on-hit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Camera/Script/CameraController.cs
  - Assets/Player/Script/PlayerStats.cs
  - Assets/Camera/Check.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09]

must_haves:
  truths:
    - "플레이어가 데미지를 받으면 카메라가 랜덤 방향으로 짧게 흔들린다 (D-01, D-02)"
    - "흔들림은 shakeDuration 초에 걸쳐 0으로 선형 감쇠하고 완전히 멈춘다 (D-05)"
    - "감쇠 중 재피격 시 강도가 누적되지 않고 지속시간 타이머만 최대치로 리프레시된다 (D-06)"
    - "보스 구역(줌 확대 상태)에서도 흔들림이 동일하게 동작한다 (D-07)"
    - "흔들림은 경계 클램프와 데드존 재앵커 블록 이후 최종 적용되어, 흔들림 값이 데드존 앵커에 누적되지 않는다 (D-08)"
    - "Inspector 에 새로 노출되는 필드는 shakeMagnitude / shakeDuration 정확히 2개다 (D-09)"
    - "보스가 맞을 때는 카메라가 흔들리지 않는다 - HP.cs 는 0줄 변경이다 (D-01, D-02)"
    - "기존 위치추종 / 데드존 / 동적오프셋 / 피킹 / 줌 / 클램프 동작이 회귀 없이 유지된다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "Hit Shake Inspector 필드 2개 + _shakeTimer 상태 + public Shake() + private ApplyHitShake() + LateUpdate 마지막 호출"
      contains: "private void ApplyHitShake()"
    - path: "Assets/Player/Script/PlayerStats.cs"
      provides: "TakeDamage 오버라이드 내 단일 Shake 호출 지점"
      contains: "CameraController.Instance.Shake();"
    - path: "Assets/Camera/Check.md"
      provides: "Phase 12 정적 회귀 검사 결과 + Play 모드 검증 체크리스트"
      contains: "Phase 12"
  key_links:
    - from: "Assets/Player/Script/PlayerStats.cs"
      to: "CameraController.Shake()"
      via: "base.TakeDamage(dmg) 직후 무-널가드 싱글톤 직접 호출"
      pattern: "CameraController\\.Instance\\.Shake\\(\\);"
    - from: "Assets/Camera/Script/CameraController.cs LateUpdate()"
      to: "ApplyHitShake()"
      via: "LateUpdate 의 마지막 문장, 재앵커 if 블록 바깥 (무조건 실행)"
      pattern: "^        ApplyHitShake\\(\\);"
    - from: "ApplyHitShake()"
      to: "transform.position"
      via: "Random.insideUnitCircle 오프셋을 클램프 이후 최종 가산"
      pattern: "Random\\.insideUnitCircle \\* shakeMagnitude"
---

<objective>
플레이어가 피격당할 때 카메라를 짧게 흔드는 독립 레이어를 기존 `CameraController` 파이프라인
맨 위에 얹는다.

Purpose: 피격에 대한 즉각적인 시각 피드백을 주되, Phase 9~10 에서 쌓아 올린 위치추종/데드존/
동적오프셋/피킹/줌/경계클램프 파이프라인은 단 한 줄도 회귀시키지 않는다.

Output: `CameraController.cs` 에 Inspector 필드 2개 + `Shake()` + `ApplyHitShake()`,
`PlayerStats.TakeDamage` 에 호출 지점 1곳, `Assets/Camera/Check.md` 에 Phase 12 검증 섹션.
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-camera-shake-on-hit/12-CONTEXT.md
@.planning/phases/12-camera-shake-on-hit/12-RESEARCH.md

@Assets/Camera/Script/CameraController.cs
@Assets/Player/Script/PlayerStats.cs
@Assets/Camera/Check.md

<interfaces>
<!-- 실행자가 필요한 계약. 코드베이스 탐색 없이 이 값들을 그대로 사용할 것. -->

CameraController.cs (현재 429줄, `wc -l` 은 마지막 줄 개행 없음으로 428 을 출력):

```csharp
// 씬 로컬 싱글톤 - DontDestroyOnLoad 미사용 (Phase 9 D-01)
public static CameraController Instance { get; private set; }

// 기존 public 진입점들 (Shake() 가 따라야 할 "idempotent, last call wins" 계약)
public void SetZoomZone(bool isZoomZone, float zoomValue)   // 165~173행
public void SetXBounds(float min, float max)                // 186~190행
public void SetYBounds(float min, float max)                // 193~197행  <- Shake() 는 이 뒤에 삽입
private void ApplyBoundsClamp()                             // 202~210행

// LateUpdate() 현재 구조 (405~428행) - 실행 순서가 곧 계약이다
void LateUpdate()
{
    if (target == null) return;
    Vector3 targetCamPos = target.position + offset;
    transform.position = Vector3.Lerp(transform.position, targetCamPos, smoothing * Time.deltaTime);
    if (_isBossZone) ResetNormalStageState(); else ApplyNormalStageCamera();      // 412행
    _cam.orthographicSize = Mathf.Lerp(_cam.orthographicSize, _targetZoom, zoomSmoothing * Time.deltaTime);
    ApplyBoundsClamp();                                                           // 418행
    if (!_isBossZone)                                                             // 423행
    {
        _deadzoneCenterX = transform.position.x + _currentBoxOffsetX;             // 425행
        _deadzoneCenterY = transform.position.y - _currentPeekY;                  // 426행
    }                                                                             // 427행
}                                                                                 // 428행  <- 이 바로 위에 삽입
```

PlayerStats.cs (74줄, UTF-8 + BOM, 인코딩 위험 없음):

```csharp
public class PlayerStats : HP
{
    public override void TakeDamage(float dmg)   // 54행
    {

        base.TakeDamage(dmg); // 부모 클래스(HP)의 원래 데미지 처리 로직 호출   // 57행  <- 이 뒤에 삽입
        Debug.Log("Player has taken damage!");                                  // 58행
    }
}
```
</interfaces>

<encoding_note>
`CameraController.cs` 는 Phase 9~11 에서 "CP949 훼손 위험 파일" 로 취급됐으나, 12-RESEARCH.md 가
직접 바이트 검사로 이 위험을 **하향 조정**했다: 남아 있는 비-ASCII 5줄은 이미 과거 편집에서 리터럴
U+FFFD (`ef bf bd`) 로 영구 변환된 상태이며, 파일 전체가 현재 **유효한 UTF-8** 이다. 따라서 이 플랜에서는
표준 Read/Edit 툴 사용이 안전하다 — Phase 11 의 `git show HEAD:<path>` + 순수 바이트 스크립트 프로토콜은
**불필요하다**.

단, 방어적 습관으로 **이 파일에 새로 추가하는 코드와 주석은 전부 순수 ASCII 로만 작성한다** (한글 금지).
검증 게이트: 작업 후에도 `grep -cP "[^\x00-\x7F]"` 가 정확히 `5` 를 유지해야 한다. 6 이상이면 새 비-ASCII 가
유입된 것이고, 4 이하면 툴 왕복이 기존 바이트를 건드린 것이다.
</encoding_note>

<verification_gate_note>
Phase 9 / 10 / 11 이 **세 번 연속으로** 같은 함정에 빠졌다: 플랜이 삽입하는 주석 텍스트 안에 그 플랜
자신의 검증 게이트가 세는 리터럴 문자열이 들어가서 카운트가 어긋났다 (Phase 9 `DontDestroyOnLoad`,
Phase 10 `deadzoneHeight`, Phase 11 `async`/`await`).

이번 플랜의 아래 태스크들은 그 함정을 **이미 회피하도록 주석 문구가 설계되어 있다**. 아래 코드 블록의
주석 문구를 임의로 바꾸지 말 것 — 특히 새 주석에 다음 리터럴을 절대 넣지 말 것:
`ApplyBoundsClamp`, `_isBossZone`, `ResetNormalStageState`, `_deadzoneCenterX`, `_deadzoneCenterY`,
그리고 `shakeMagnitude` / `shakeDuration` / `_shakeTimer` / `ApplyHitShake` 의 추가 등장.
</verification_gate_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraController 에 Hit Shake 레이어 삽입 (필드 + 상태 + Shake() + ApplyHitShake() + LateUpdate 호출)</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — 전체. 특히 53~71행(Peeking 헤더 그룹 끝 + Instance 선언),
      110~127행(private 상태 필드 블록 끝), 192~202행(SetYBounds 끝 + ApplyBoundsClamp 시작),
      405~428행(LateUpdate 전체). 삽입 전 현재 상태를 반드시 눈으로 확인할 것 — 라인 번호는 이 플랜 작성
      시점 기준이며, 삽입은 라인 번호가 아니라 **앵커 문자열** 기준으로 수행한다.
    - `.planning/phases/12-camera-shake-on-hit/12-CONTEXT.md` — 잠금 결정 D-01~D-09.
    - `.planning/phases/12-camera-shake-on-hit/12-RESEARCH.md` — Architecture Patterns 1~3, Anti-Patterns,
      Pitfall 1(인코딩), Pitfall 4(가드 순서), Pitfall 5(0 나눗셈).
  </read_first>

  <action>
순수 삽입 편집 4곳. **삭제 0줄, 기존 줄 수정 0줄.** 아래 코드 블록을 문자 그대로 삽입한다.

====

**삽입 1 — Inspector 필드 그룹.** 앵커: `    public float peekCancelSpeed = 12f;` (현재 67행) 바로 **뒤**,
`    // Scene-local singleton.` 주석(현재 69행) **앞**에 다음 9줄을 삽입한다:

```csharp

    [Header("Hit Shake")]
    // Peak random offset in world units on a fresh player hit. Fixed value - it does NOT scale
    // with the damage amount (D-04). Keep it small relative to the deadzone box so it reads as
    // a jolt rather than a re-frame. Playtest-tunable.
    public float shakeMagnitude = 0.3f;
    // Seconds the jolt takes to fall from full strength to zero. A new hit while one is still
    // fading refreshes this countdown to full instead of adding to it (D-06). Playtest-tunable.
    public float shakeDuration = 0.25f;
```

====

**삽입 2 — private 상태 필드.** 앵커: `    private float _peekTimer;` (현재 126행) 바로 **뒤**,
`    void Awake()` (현재 128행) **앞**에 다음 3줄을 삽입한다 (`_peekTimer` 뒤의 기존 빈 줄은 그대로 두고
그 빈 줄 **앞**에 넣어도 되고 뒤에 넣어도 되나, 기존 빈 줄을 삭제하지는 말 것):

```csharp
    // Counts down after a player hit. The public trigger reassigns this to the full duration
    // instead of adding to it, so repeated hits refresh the jolt rather than stacking it (D-06).
    private float _shakeTimer;
```

====

**삽입 3 — public 트리거 + private 감쇠/적용 헬퍼.** 앵커: `SetYBounds` 메서드의 닫는 중괄호
(현재 197행 `    }`) 바로 **뒤**, `    // Clamps X and Y so the visible edges` 주석(현재 199행) **앞**에
다음 27줄을 삽입한다:

```csharp

    // Public trigger, called by PlayerStats.TakeDamage right after base.TakeDamage (D-02).
    // Parameterless on purpose: the strength is a fixed Inspector value, never derived from the
    // damage amount (D-04). Idempotent, last call wins - same contract as SetZoomZone and
    // SetXBounds above, and that is exactly what makes a re-hit refresh instead of stack (D-06).
    public void Shake()
    {
        _shakeTimer = shakeDuration;
    }

    // Per-frame decay and apply. Random 2D offset scaled linearly down as the countdown runs
    // out (D-05, no sine wave). This runs as the LAST statement of LateUpdate - after the
    // bounds clamp and after the re-anchor block, and outside the normal-stage-only guard so
    // it fires inside zoom zones too (D-07). The result is deliberately never re-clamped (D-08).
    private void ApplyHitShake()
    {
        if (_shakeTimer <= 0f) return;
        _shakeTimer -= Time.deltaTime;
        // Guarded denominator mirrors the Mathf.Max(Time.deltaTime, 0.0001f) idiom already used
        // further down this file: a designer typing 0 into the Inspector must not yield NaN.
        float t = Mathf.Clamp01(_shakeTimer / Mathf.Max(shakeDuration, 0.0001f));
        Vector2 shakeOffset = Random.insideUnitCircle * shakeMagnitude * t;
        Vector3 pos = transform.position;
        pos.x += shakeOffset.x;
        pos.y += shakeOffset.y;
        transform.position = pos;
    }
```

가드-먼저-그다음-감산 순서는 12-RESEARCH Pitfall 4 대응이다. 순서를 바꾸지 말 것.
`Time.deltaTime` 을 쓴다 — `Time.unscaledDeltaTime` 이 아니다 (일시정지 중에는 카메라 파이프라인 전체가
멈추는 기존 동작과 일치시키기 위함).

====

**삽입 4 — LateUpdate 호출 지점.** 앵커: `LateUpdate()` 내부 재앵커 블록의 닫는 중괄호
(현재 427행 `        }`) 바로 **뒤**, `LateUpdate()` 자신의 닫는 중괄호(현재 428행 `    }`) **앞**에
다음 3줄을 삽입한다:

```csharp
        // Hit jolt is the final layer: unconditional, and placed after the re-anchor above so
        // the random offset can never bleed into the box anchors frame over frame.
        ApplyHitShake();
```

들여쓰기는 공백 8칸이다 (`if (!_isBossZone)` 와 같은 레벨). 이 호출은 반드시 재앵커 `if` 블록
**바깥**에 있어야 한다 — 안에 넣으면 보스 구역에서 흔들림이 죽어 D-07 위반이다.

====

**절대 하지 말 것 (12-RESEARCH Anti-Patterns):**
- `ResetNormalStageState()` 를 건드리지 말 것. 이 메서드는 보스 구역에서 매 프레임 실행되므로,
  여기에 타이머 리셋을 넣으면 보스 구역 흔들림이 조용히 죽어 D-07 을 정면 위반한다.
- `ApplyHitShake()` 뒤에 경계 클램프를 다시 호출하지 말 것 (D-08 은 클램프를 살짝 뚫는 것을 허용한다).
- `Shake()` 에 데미지 매개변수를 추가하지 말 것 (D-04 고정 강도).
- `Mathf.Sin` 기반 주기 진동을 쓰지 말 것 (D-05 명시 배제).
- `_shakeTimer +=` 로 누적하지 말 것 (D-06 은 재대입 리프레시다).
- Inspector 필드를 2개 초과로 추가하지 말 것 — `AnimationCurve`, 주파수, 강도 배율 등 전부 D-09 범위 밖.
- 이 파일의 기존 주석(모지바케 포함)을 "정리" 하지 말 것 — Phase 12 범위 밖이다.
- 새 코드/주석에 한글 등 비-ASCII 문자를 넣지 말 것.

**커밋:** `git add -- Assets/Camera/Script/CameraController.cs` 후 pathspec 을 명시한
`git commit -- Assets/Camera/Script/CameraController.cs` 로 커밋한다. 작업 트리에 이 플랜과 무관한
스테이지된 변경이 이미 존재하므로(Phase 11 Plan 3 에서 인덱스 오염 사고 발생), pathspec 없는
`git commit` 을 절대 실행하지 말 것.
  </action>

  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && f=Assets/Camera/Script/CameraController.cs && \
echo "shakeMagnitude=$(grep -c 'shakeMagnitude' $f) expect 2" && \
echo "shakeDuration=$(grep -c 'shakeDuration' $f) expect 3" && \
echo "_shakeTimer=$(grep -c '_shakeTimer' $f) expect 5" && \
echo "ApplyHitShake=$(grep -c 'ApplyHitShake' $f) expect 2" && \
echo "publicShake=$(grep -c 'public void Shake()' $f) expect 1" && \
echo "nonascii=$(grep -cP '[^\x00-\x7F]' $f) expect 5" && \
echo "ApplyBoundsClamp=$(grep -c 'ApplyBoundsClamp' $f) expect 6" && \
echo "_isBossZone=$(grep -c '_isBossZone' $f) expect 4" && \
echo "ResetNormalStageState=$(grep -c 'ResetNormalStageState' $f) expect 3" && \
echo "_deadzoneCenterX=$(grep -c '_deadzoneCenterX' $f) expect 11" && \
echo "_deadzoneCenterY=$(grep -c '_deadzoneCenterY' $f) expect 11" && \
echo "deletions=$(git diff --numstat HEAD -- $f | cut -f2) expect 0"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "shakeMagnitude" Assets/Camera/Script/CameraController.cs` == `2`
    - `grep -c "shakeDuration" Assets/Camera/Script/CameraController.cs` == `3`
    - `grep -c "_shakeTimer" Assets/Camera/Script/CameraController.cs` == `5`
    - `grep -c "ApplyHitShake" Assets/Camera/Script/CameraController.cs` == `2`
    - `grep -c "public void Shake()" Assets/Camera/Script/CameraController.cs` == `1`
    - `grep -c '\[Header("Hit Shake")\]' Assets/Camera/Script/CameraController.cs` == `1`
    - `grep -c "Random.insideUnitCircle" Assets/Camera/Script/CameraController.cs` == `1`
    - `grep -c "Mathf.Sin" Assets/Camera/Script/CameraController.cs` == `0`
    - `grep -c "AnimationCurve" Assets/Camera/Script/CameraController.cs` == `0`
    - `grep -c "_shakeTimer +=" Assets/Camera/Script/CameraController.cs` == `0`
    - `grep -cP "[^\x00-\x7F]" Assets/Camera/Script/CameraController.cs` == `5` (인코딩 무결성)
    - 회귀 불변: `grep -c "ApplyBoundsClamp"` == `6`, `grep -c "_isBossZone"` == `4`,
      `grep -c "ResetNormalStageState"` == `3`, `grep -c "_deadzoneCenterX"` == `11`,
      `grep -c "_deadzoneCenterY"` == `11`
    - `git diff --numstat HEAD -- Assets/Camera/Script/CameraController.cs` 의 삭제 컬럼 == `0` (순수 삽입)
    - `ApplyHitShake();` 호출이 `LateUpdate()` 의 마지막 문장이다:
      `awk '/void LateUpdate/,/^    }/' Assets/Camera/Script/CameraController.cs | grep -v '^\s*$' | grep -v '^\s*//' | tail -2 | head -1`
      의 출력이 `        ApplyHitShake();` 이다
    - `ApplyHitShake();` 의 들여쓰기가 공백 8칸이다 (재앵커 `if` 블록 **바깥**):
      `grep -c "^        ApplyHitShake();" Assets/Camera/Script/CameraController.cs` == `1`
      (12칸 들여쓰기 `^            ApplyHitShake();` 의 카운트는 `0` 이어야 한다)
    - `ApplyHitShake()` 정의가 `ApplyBoundsClamp()` 정의보다 **앞**에 오지 않아도 무방하나,
      `public void Shake()` 는 `private void ApplyBoundsClamp()` 정의보다 앞에 있다:
      `grep -n "public void Shake()" ...` 의 행번호 < `grep -n "private void ApplyBoundsClamp()" ...` 의 행번호
    - 커밋이 `Assets/Camera/Script/CameraController.cs` 단 1개 파일만 포함한다:
      `git show --stat --name-only --format="" HEAD | grep -v '^$' | wc -l` == `1`
      (`--format=""` 로 커밋 메시지 블록 자체를 지우므로 Co-Authored-By 같은 트레일러로 메시지가
      길어져도 안전하다. `tail -n +5` 는 헤더가 정확히 4줄이라고 가정해 오탐이 나므로 쓰지 말 것)
  </acceptance_criteria>

  <done>
CameraController.cs 에 `shakeMagnitude`/`shakeDuration` Inspector 필드 2개, `_shakeTimer` 상태,
`public void Shake()`, `private void ApplyHitShake()` 가 존재하고, `ApplyHitShake();` 가
`LateUpdate()` 의 마지막 문장으로 재앵커 블록 바깥(8칸 들여쓰기)에 놓여 있다. 삭제 라인 0,
비-ASCII 라인 수 5 유지, 기존 파이프라인 식별자 카운트 전부 불변.
  </done>
</task>

<task type="auto">
  <name>Task 2: PlayerStats.TakeDamage 호출 지점 연결 + 정적 회귀 검사 + Check.md Phase 12 섹션</name>

  <files>Assets/Player/Script/PlayerStats.cs, Assets/Camera/Check.md</files>

  <read_first>
    - `Assets/Player/Script/PlayerStats.cs` — 전체 (74줄). 특히 51~59행 `TakeDamage` 오버라이드와
      62~73행 `RestoreStats`(Phase 11 이 추가한 영어 ASCII 주석 스타일의 선례).
    - `Assets/Script/HP.cs` — **읽기 전용**. `TakeDamage`/`Die` 베이스 구현을 확인해 D-03(마지막 피격에도
      흔들림)이 별도 분기 없이 성립함을 확인만 하고, **단 한 줄도 수정하지 않는다** (D-02).
    - `Assets/Camera/Script/CameraZoomTrigger.cs` 와 `Assets/Camera/Script/CameraBoundsTrigger.cs` —
      `CameraController.Instance` 를 널 가드 없이 직접 호출하는 기존 컨벤션 확인용.
    - `Assets/Camera/Check.md` — 기존 섹션 구조/서술 톤 확인. 특히 파일 끝부분(477행 근처)의
      quick task 260805-q2u 결과 기록 섹션 형식.
    - `Assets/Camera/Script/CameraController.cs` — Task 1 결과 반영본.
  </read_first>

  <action>
**2-A. PlayerStats.cs 호출 지점 삽입 (순수 삽입 4줄, 삭제 0줄)**

앵커: `        base.TakeDamage(dmg); // 부모 클래스(HP)의 원래 데미지 처리 로직 호출` (현재 57행) 바로 **뒤**,
`        Debug.Log("Player has taken damage!");` (현재 58행) **앞**에 다음 4줄을 삽입한다:

```csharp
        // Phase 12 (D-02): player-only camera jolt. Placed after the base damage handling so
        // the killing blow shakes too (D-03). No null guard on the singleton - that matches the
        // unguarded convention already used by CameraZoomTrigger and CameraBoundsTrigger.
        CameraController.Instance.Shake();
```

- 널 가드(`?.`)를 **붙이지 않는다** — 12-RESEARCH Open Question 1 의 권고이자 기존 코드베이스 컨벤션이다.
- `dmg` 값을 `Shake()` 에 전달하지 않는다 (D-04 고정 강도).
- `PlayerStats.cs` 는 진짜 UTF-8(+BOM) 이므로 표준 Edit 툴이 안전하다. 그래도 새 주석은 ASCII 로 쓴다
  (Phase 11 `RestoreStats` 주석과 동일 스타일).
- **여기서 커밋하지 않는다.** `PlayerStats.cs` 와 `Check.md` 는 아래 2-C 에서 **반드시 하나의
  커밋으로 묶어** 올린다 (이유는 2-C 커밋 항목 참조 — 이 태스크의 `HEAD~1` 기준 diff 게이트가
  성립하려면 단일 커밋이어야 한다).

**2-B. 정적 회귀 검사 12항목 실행**

아래 12개 게이트를 실제로 실행하고 실제 출력값을 기록한다. 통과하지 못한 항목은 **PASS 로 허위 기록하지 말
것** (Phase 9 Plan 3 의 확립된 원칙).

| # | 검사 | 명령 | 기대값 |
|---|------|------|--------|
| 1 | 신규 Inspector 필드 정확히 2개 | `grep -c "shakeMagnitude" Assets/Camera/Script/CameraController.cs` / `grep -c "shakeDuration" ...` | 2 / 3 |
| 2 | 공개 트리거 존재 | `grep -c "public void Shake()" Assets/Camera/Script/CameraController.cs` | 1 |
| 3 | 감쇠 헬퍼 정의 + 호출 = 2 | `grep -c "ApplyHitShake" Assets/Camera/Script/CameraController.cs` | 2 |
| 4 | 호출이 재앵커 블록 바깥 (8칸 들여쓰기) | `grep -c "^        ApplyHitShake();" Assets/Camera/Script/CameraController.cs` | 1 |
| 5 | 호출이 12칸 들여쓰기(블록 안)가 아님 | `grep -c "^            ApplyHitShake();" Assets/Camera/Script/CameraController.cs` | 0 |
| 6 | 사인파 미사용 (D-05) | `grep -c "Mathf.Sin" Assets/Camera/Script/CameraController.cs` | 0 |
| 7 | AnimationCurve 미노출 (D-09) | `grep -c "AnimationCurve" Assets/Camera/Script/CameraController.cs` | 0 |
| 8 | 누적 금지 (D-06) | `grep -c "_shakeTimer +=" Assets/Camera/Script/CameraController.cs` | 0 |
| 9 | 재클램프 없음 (D-08) — 클램프 호출 총량 불변 | `grep -c "ApplyBoundsClamp" Assets/Camera/Script/CameraController.cs` | 6 |
| 10 | 보스존 분기 구조 불변 (D-07) | `grep -c "_isBossZone" ...` / `grep -c "ResetNormalStageState" ...` | 4 / 3 |
| 11 | 인코딩 무결성 | `grep -cP "[^\x00-\x7F]" Assets/Camera/Script/CameraController.cs` | 5 |
| 12 | HP.cs 0줄 변경 (D-02) | `git diff --numstat HEAD~2 -- Assets/Script/HP.cs \| wc -l` 및 `grep -c "Shake" Assets/Script/HP.cs` | 0 / 0 |

추가로 PlayerStats 측:
- `grep -c "CameraController.Instance.Shake();" Assets/Player/Script/PlayerStats.cs` == 1
- `grep -c "CameraController" Assets/Player/Script/PlayerStats.cs` == 1
- `git diff --numstat HEAD -- Assets/Player/Script/PlayerStats.cs` 삭제 컬럼 == 0

**2-C. `Assets/Camera/Check.md` 파일 끝에 Phase 12 섹션 추가 (순수 append)**

기존 내용은 한 줄도 수정/삭제하지 않고 파일 **끝에만** 다음 구조로 추가한다 (기존 섹션들과 동일하게 한국어로
작성 — Check.md 는 진짜 UTF-8 문서 파일이므로 한글 안전):

```
# Phase 12 - 피격 시 카메라 흔들림 (Camera Shake on Hit)

## Phase 12 변경사항
  (파일별 변경 요약 - CameraController.cs 4곳 삽입, PlayerStats.cs 1곳 삽입, HP.cs 0줄)

## Phase 12 Inspector 필드
  | 필드 | 기본값 | 의미 | 결정 |
  | shakeMagnitude | 0.3 | 피격 순간 최대 랜덤 오프셋 (월드 유닛) | D-04 / D-09 |
  | shakeDuration | 0.25 | 0 까지 감쇠하는 데 걸리는 시간 (초) | D-06 / D-09 |
  (두 값 모두 플레이테스트로 조정할 초기값이며 잠금된 값이 아님을 명시)

## Phase 12 사전 준비
  (Play 모드 진입 방법, 어떤 씬에서 어떻게 피격당할 수 있는지 - Tutorial Map 기준)

## Phase 12 검증 항목

### 1) 기본 흔들림 (D-01 ~ D-05)
- [ ] 플레이어가 적/보스에게 맞으면 카메라가 즉시 짧게 흔들린다
- [ ] 흔들림이 규칙적인 진동이 아니라 불규칙한 랜덤 떨림으로 보인다 (D-05)
- [ ] 흔들림이 약 0.25초 안에 부드럽게 잦아들고 완전히 멈춘다 (잔떨림 없음)
- [ ] 큰 데미지든 작은 데미지든 흔들림 강도가 동일하다 (D-04)
- [ ] 보스가 맞을 때는 카메라가 전혀 흔들리지 않는다 (D-01)

### 2) 연속 피격 리프레시 (D-06)
- [ ] 흔들림이 잦아드는 도중 다시 맞으면 흔들림이 다시 최대 강도로 시작된다
- [ ] 연타로 맞아도 흔들림이 점점 더 세지지 않는다 (누적 없음)

### 3) 사망 피격 (D-03)
- [ ] 체력을 0으로 만드는 마지막 피격에도 흔들림이 발동한다

### 4) 보스 구역 동작 (D-07)
- [ ] CameraZoomTrigger 안(줌 7 상태)에서 맞아도 흔들림이 정상 발동한다
- [ ] 보스 구역 안에서 흔들림이 끝난 뒤 카메라가 원래 추종 위치로 정확히 복귀한다

### 5) 파이프라인 회귀 (D-08 + Phase 9/10 회귀)
- [ ] 흔들림 도중 및 직후에 카메라가 데드존 밖으로 영구 이탈하지 않는다
- [ ] 흔들림이 끝난 뒤 데드존 박스가 제자리에 있다 (누적 드리프트 없음)
- [ ] 맵 좌/우/상/하 경계에 붙은 상태에서 맞으면 경계를 살짝 뚫었다가 즉시 복귀한다 (D-08 의도된 동작)
- [ ] 피격 후에도 동적 오프셋(달릴 때 시야 열림)과 피킹(위/아래 보기)이 정상 동작한다
- [ ] 일시정지(Time.timeScale = 0) 중에는 흔들림도 멈춘다

### 6) Inspector 튜닝 (D-09)
- [ ] CameraController 인스펙터에 Hit Shake 그룹이 있고 필드가 정확히 2개다
- [ ] shakeDuration 을 0 으로 설정해도 NaN/카메라 정지가 발생하지 않는다 (0 나눗셈 가드)
- [ ] 값을 키우면 흔들림이 눈에 띄게 커지고, 줄이면 작아진다

## Phase 12 결과 기록

### 정적 회귀 검사 (12-01-PLAN.md Task 2, 2026-08-11)
  (위 12항목 표를 실제 실행 결과값과 함께 PASS/FAIL 로 기록)

### Play 모드 실측 결과 (12-01-PLAN.md Task 3)
  (미검증 상태로 남겨두고 Task 3 에서 채운다)

## Phase 12 알려진 한계 (코드로 방어하지 않음)
  - 흔들림은 경계 클램프 이후 적용되므로 맵 가장자리에서 최대 shakeMagnitude 만큼 경계 밖이
    잠깐 보일 수 있다 — 의도된 동작 (D-08)
  - 강도가 데미지에 비례하지 않는다 (D-04)
  - 감쇠 곡선은 선형 고정이며 Inspector 로 바꿀 수 없다 (D-09, AnimationCurve 노출은 후속 Phase)
  - 플레이어 피격만 흔들린다. 보스 피격 흔들림은 이번 Phase 범위 밖 (Deferred)
```

**커밋 (단일 커밋 강제):** `PlayerStats.cs` 와 `Check.md` 를 **반드시 하나의 커밋**으로 올린다.
두 파일을 별도 커밋으로 나누면, 이 태스크의 `git diff --numstat HEAD~1 -- Assets/Player/Script/PlayerStats.cs`
게이트가 baseline 으로 잡는 `HEAD~1` 이 (변경 전 상태가 아니라) **PlayerStats.cs 커밋 그 자체**가 되어
항상 `0 0` 을 출력한다 — 무엇을 커밋했든 통과하는 **조용한 허위 통과**다 (12-RESEARCH Pitfall 3
"잘못된 baseline 커밋", Phase 10 선례와 동일한 실패 유형).

```
git add -- Assets/Player/Script/PlayerStats.cs Assets/Camera/Check.md
git commit -- Assets/Player/Script/PlayerStats.cs Assets/Camera/Check.md
```

작업 트리에 이 플랜과 무관한 스테이지된 변경이 존재하므로 **pathspec 없는 `git commit` 을 절대
실행하지 말 것** (Phase 11 Plan 3 인덱스 오염 사고).
  </action>

  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && \
echo "callsite=$(grep -c 'CameraController.Instance.Shake();' Assets/Player/Script/PlayerStats.cs) expect 1" && \
echo "ccRefs=$(grep -c 'CameraController' Assets/Player/Script/PlayerStats.cs) expect 1" && \
echo "nullGuard=$(grep -c 'CameraController.Instance?.Shake' Assets/Player/Script/PlayerStats.cs) expect 0" && \
echo "hpShake=$(grep -c 'Shake' Assets/Script/HP.cs) expect 0" && \
echo "hpLines=$(wc -l < Assets/Script/HP.cs) expect 107" && \
echo "checkmd=$(grep -c 'Phase 12' Assets/Camera/Check.md) expect >=6" && \
echo "checkmdBoxes=$(awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c '^- \[ \]') expect >=18" && \
echo "psDeletions=$(git diff --numstat HEAD~1 -- Assets/Player/Script/PlayerStats.cs | cut -f2)"
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "CameraController.Instance.Shake();" Assets/Player/Script/PlayerStats.cs` == `1`
    - `grep -c "CameraController" Assets/Player/Script/PlayerStats.cs` == `1` (주석에 중복 등장 없음)
    - `grep -c "Instance?.Shake" Assets/Player/Script/PlayerStats.cs` == `0` (널 가드 미사용, 컨벤션 준수)
    - `CameraController.Instance.Shake();` 가 `base.TakeDamage(dmg);` **뒤**에 온다:
      `grep -n "base.TakeDamage(dmg);" ...` 의 행번호 < `grep -n "CameraController.Instance.Shake();" ...` 의 행번호
    - `grep -c "Shake(dmg" Assets/Player/Script/PlayerStats.cs` == `0` (D-04: 데미지 값 전달 금지)
    - `Assets/Script/HP.cs` 가 0줄 변경: `git status --porcelain -- Assets/Script/HP.cs` 출력이 빈 문자열이고
      `wc -l < Assets/Script/HP.cs` == `107`, `grep -c "Shake" Assets/Script/HP.cs` == `0`
    - `PlayerStats.cs` 와 `Check.md` 가 **하나의 커밋**에 들어 있다 (`HEAD~1` 이 진짜 변경 전 baseline
      임을 보장 — 이게 깨지면 아래 두 diff 게이트가 조용히 허위 통과한다):
      `git show --stat --name-only --format="" HEAD | grep -v '^$' | wc -l` == `2` 이고
      그 출력에 `Assets/Player/Script/PlayerStats.cs` 와 `Assets/Camera/Check.md` 가 모두 포함된다
    - `git diff --numstat HEAD~1 -- Assets/Player/Script/PlayerStats.cs` 의 삭제 컬럼 == `0` (순수 삽입)
    - `Assets/Camera/Check.md` 에 `# Phase 12 - 피격 시 카메라 흔들림 (Camera Shake on Hit)` 헤더가
      정확히 1회 존재한다: `grep -c "^# Phase 12" Assets/Camera/Check.md` == `1`
    - Check.md 에 6개 검증 소섹션이 전부 존재한다:
      `grep -c "^### [1-6])" Assets/Camera/Check.md` 의 결과가 Phase 12 섹션 범위 내에서 `6`
      (`awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c "^### [1-6])"` == `6`)
    - Check.md Phase 12 섹션의 미체크 박스가 18개 이상이다:
      `awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c "^- \[ \]"` >= `18`
    - Check.md 기존 내용이 보존됐다 (순수 append):
      `git diff --numstat HEAD~1 -- Assets/Camera/Check.md` 의 삭제 컬럼 == `0`
    - Check.md 의 "정적 회귀 검사" 표에 12개 항목의 **실제 실행 출력값**이 기록돼 있고,
      실패 항목이 있다면 FAIL 로 표기돼 있다 (허위 PASS 금지)
    - Check.md 의 "Play 모드 실측 결과" 항목이 아직 미검증 상태로 남아 있다 (Task 3 에서 채움)
  </acceptance_criteria>

  <done>
`PlayerStats.TakeDamage` 가 `base.TakeDamage(dmg)` 직후 `CameraController.Instance.Shake()` 를 널 가드
없이 호출한다. `HP.cs` 는 0줄 변경이다. 정적 회귀 12항목이 실제로 실행됐고 그 결과값이
`Assets/Camera/Check.md` 의 새 Phase 12 섹션에 기록됐으며, 같은 섹션에 Play 모드 체크리스트 6개
소섹션(18개 이상 항목)이 미체크 상태로 준비돼 있다.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Unity Play 모드 실측 검증 체크포인트</name>

  <files>Assets/Camera/Check.md</files>

  <read_first>
    - `Assets/Camera/Check.md` — Task 2 가 추가한 `# Phase 12` 섹션 전체 (체크리스트 6개 소섹션).
    - `.planning/phases/12-camera-shake-on-hit/12-CONTEXT.md` — D-01 ~ D-09 (사용자에게 각 항목이 어떤
      결정을 검증하는지 설명할 때 필요).
  </read_first>

  <what-built>
    Claude 가 자동화 완료한 것:
    - `CameraController.cs`: `[Header("Hit Shake")]` + `shakeMagnitude`(0.3) / `shakeDuration`(0.25)
      Inspector 필드, `_shakeTimer` 상태, `public void Shake()`(타이머 최대치 재대입 = D-06 리프레시),
      `private void ApplyHitShake()`(`Random.insideUnitCircle` 랜덤 오프셋 × 선형 감쇠, 0 나눗셈 가드),
      그리고 `LateUpdate()` 마지막 줄의 무조건 호출(재앵커 블록 바깥 = D-07 보스존 포함).
    - `PlayerStats.cs`: `TakeDamage` 오버라이드에서 `base.TakeDamage(dmg)` 직후 `Shake()` 호출 1줄.
    - `Assets/Camera/Check.md`: Phase 12 정적 회귀 결과 + Play 모드 체크리스트.

    Claude 가 할 수 없는 것: Unity 에디터 Play 모드 실행과 "흔들림이 어떤 느낌인지" 의 시각적 판단.
    이 실행 환경에서는 Unity 컴파일조차 확인할 수 없다.
  </what-built>

  <how-to-verify>
    1. Unity 에디터에서 프로젝트를 열고 **콘솔에 컴파일 에러가 0개**인지 먼저 확인한다.
       (에러가 있으면 여기서 중단하고 에러 메시지를 그대로 알려줄 것)
    2. Hierarchy 에서 Main Camera 를 선택해 Inspector 의 `CameraController` 컴포넌트에
       **`Hit Shake` 그룹과 그 아래 필드가 정확히 2개**(`Shake Magnitude` = 0.3, `Shake Duration` = 0.25)
       보이는지 확인한다.
    3. `Assets/Scenes/Tutorial Map.unity` 를 열고 Play 를 누른다.
    4. `Assets/Camera/Check.md` 의 `# Phase 12` 섹션에 있는 6개 소섹션 체크리스트를 순서대로 확인한다:
       - **1) 기본 흔들림** — 적에게 일부러 맞아본다. 흔들리는가? 불규칙한가? 0.25초 안에 완전히 멈추는가?
         보스를 때렸을 때는 흔들리지 **않는지**도 함께 확인.
       - **2) 연속 피격** — 흔들리는 도중 다시 맞아본다. 다시 최대 강도로 시작되는가? 점점 세지지는 않는가?
       - **3) 사망 피격** — 마지막 한 대에 죽을 때도 흔들리는가?
       - **4) 보스 구역** — 보스존(줌 7) 안에서 맞아본다. 흔들리는가? 끝난 뒤 원위치로 복귀하는가?
       - **5) 파이프라인 회귀** — 맵 좌/우/상/하 경계에 붙어서 맞아본다(경계를 살짝 뚫었다 복귀하는 것은
         **의도된 동작**이다). 흔들림 후 데드존이 제자리인가? 달릴 때 시야 열림과 위/아래 보기가
         여전히 되는가? ESC 등으로 일시정지 중에는 흔들림도 멈추는가?
       - **6) Inspector 튜닝** — Play 중에 `Shake Duration` 을 **0 으로** 바꿔본다.
         카메라가 얼어붙거나 사라지지 않아야 한다(NaN 가드 확인). 값을 키우고 줄여 체감 차이를 본다.
    5. 각 항목의 결과를 알려준다. `shakeMagnitude`/`shakeDuration` 기본값이 체감상 과하거나 약하면
       원하는 값을 말해달라 — 코드가 아니라 Inspector 기본값만 조정하면 된다.

    Claude 는 결과를 받아 `Assets/Camera/Check.md` 의 "Play 모드 실측 결과" 항목에 기록한다.
    **통과하지 못한 항목을 PASS 로 기록하지 않는다** (Phase 9 Plan 3 확립 원칙). 검증을 생략하기로
    결정하면 "검증 생략" 으로 명시하고 미체크 항목을 그대로 남긴다.
  </how-to-verify>

  <action>
이 태스크는 사용자 검증 체크포인트다. Claude 가 수행하는 자동화 작업은 다음 3단계뿐이다:

1. 위 `<how-to-verify>` 의 6개 소섹션 절차를 사용자에게 **그대로 제시**하고 실행을 요청한다.
   (사용자가 Unity 에디터를 직접 조작해야 하므로 Claude 가 대신할 수 없다. 이 실행 환경에서는
   Unity 컴파일 확인조차 불가능하다.)

2. 사용자 응답을 받으면 `Assets/Camera/Check.md` 의 Phase 12 섹션을 갱신한다:
   - `### Play 모드 실측 결과 (12-01-PLAN.md Task 3)` 항목에 검증 날짜와 소섹션별 결과
     (PASS / FAIL + 구체 증상 / 검증 생략)를 기록한다.
   - 통과한 항목의 `- [ ]` 만 `- [x]` 로 바꾼다. **통과하지 못했거나 확인하지 않은 항목은
     절대 `- [x]` 로 바꾸지 않는다** (Phase 9 Plan 3 에서 확립된 원칙 — 미검증을 PASS 로
     허위 기록하지 않고 "검증 생략" 상태와 미체크 항목을 그대로 남긴다).

3. 사용자가 튜닝 값을 지정한 경우에만 `Assets/Camera/Script/CameraController.cs` 의
   `shakeMagnitude` / `shakeDuration` **기본값 리터럴 2개만** 수정한다. 다른 줄은 손대지 않는다.
   FAIL 항목이 있으면 수정을 시도하지 말고 `.planning/STATE.md` 의 Active TODOs 에 후속 항목으로
   기록한 뒤 사용자 지시를 기다린다.

커밋은 pathspec 을 명시한다: `git commit -- Assets/Camera/Check.md [Assets/Camera/Script/CameraController.cs]`
  </action>

  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && \
echo "playResult=$(awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c 'Play 모드 실측 결과') expect 1" && \
echo "checkedBoxes=$(awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c '^- \[x\]')" && \
echo "uncheckedBoxes=$(awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c '^- \[ \]')" && \
echo "totalBoxes=$(( $(awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c '^- \[x\]') + $(awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c '^- \[ \]') )) expect >=18" && \
echo "ccChanged=$(git diff --numstat HEAD -- Assets/Camera/Script/CameraController.cs | cut -f1) expect 0 or <=2"
    </automated>
  </verify>

  <acceptance_criteria>
    - 사용자가 6개 소섹션 각각에 대해 통과/실패/생략 중 하나를 명시적으로 응답했다
    - `Assets/Camera/Check.md` 의 "### Play 모드 실측 결과 (12-01-PLAN.md Task 3)" 항목에
      날짜와 함께 실제 응답 결과가 기록됐다:
      `awk '/^# Phase 12/,0' Assets/Camera/Check.md | grep -c "Play 모드 실측 결과"` == `1`
    - 기록된 상태가 다음 중 하나로 명확하다: 전항목 PASS / 일부 FAIL(구체적 증상 포함) / 검증 생략
    - Phase 12 섹션의 체크박스 총합(`- [x]` + `- [ ]`)이 Task 2 가 만든 개수와 동일하다 (>= 18) —
      즉 항목이 삭제되거나 추가되지 않았다
    - 사용자가 통과를 확인하지 않은 항목이 `- [x]` 로 표시돼 있지 않다 (허위 PASS 금지)
    - 사용자가 튜닝 값을 요청한 경우, `CameraController.cs` 의 두 필드 기본값 리터럴만 바뀌었다:
      `git diff --numstat HEAD -- Assets/Camera/Script/CameraController.cs` 의 추가/삭제가 각각 `2` 이하
    - 튜닝 요청이 없었다면 `CameraController.cs` 는 이 태스크에서 0줄 변경이다:
      `git status --porcelain -- Assets/Camera/Script/CameraController.cs` 출력이 빈 문자열
    - FAIL 항목이 있으면 `.planning/STATE.md` 의 Active TODOs 에 후속 항목으로 기록됐다:
      `grep -c "Phase 12" .planning/STATE.md` >= `1`
  </acceptance_criteria>

  <done>
사용자가 Unity Play 모드에서 6개 소섹션을 확인하고 그 결과가 `Assets/Camera/Check.md` 의
"Play 모드 실측 결과" 항목에 정직하게 기록됐다 (전항목 PASS / 일부 FAIL + 증상 / 검증 생략 중 하나로
명확히 구분). 미확인 항목은 미체크 상태로 남아 있고, 튜닝 요청이 있었다면 Inspector 기본값 2개만
반영됐다.
  </done>

  <resume-signal>
    "approved" (전항목 통과) 또는 "생략" (Play 모드 검증 건너뜀) 또는 실패한 항목의 구체적 증상을
    설명해 주세요. 튜닝 값 변경을 원하면 원하는 숫자도 함께 알려주세요.
  </resume-signal>
</task>

</tasks>

<verification>
Phase 전체 검증:

1. **정적 (Task 2 에서 자동 실행됨)**
   - CameraController.cs: 식별자 카운트 11종 전부 기대값 일치, 삭제 라인 0, 비-ASCII 라인 5 유지
   - PlayerStats.cs: 호출 지점 1곳, 삭제 라인 0
   - HP.cs: 0줄 변경 (D-02)
   - 금지 패턴 부재: `Mathf.Sin`(D-05), `AnimationCurve`(D-09), `_shakeTimer +=`(D-06),
     `Shake(dmg`(D-04), `ApplyHitShake()` 뒤 재클램프(D-08)

2. **구조 (Task 1 에서 자동 실행됨)**
   - `ApplyHitShake();` 가 `LateUpdate()` 의 마지막 문장이고 들여쓰기 8칸 (재앵커 블록 바깥) — D-07 + D-08
   - `ResetNormalStageState()` 무수정 (안티패턴 회피)

3. **런타임 (Task 3 사용자 체크포인트)**
   - Unity 컴파일 에러 0
   - 6개 소섹션 18개 이상 체크리스트 항목
</verification>

<success_criteria>
1. 플레이어가 피격당하면 카메라가 랜덤 방향으로 짧게 흔들리고, `shakeDuration` 초 안에 완전히 멈춘다.
2. 감쇠 중 재피격 시 지속시간만 최대치로 리프레시되고 강도는 누적되지 않는다 (D-06).
3. 보스 구역(줌 확대 상태)에서도 흔들림이 동일하게 발동한다 (D-07).
4. 흔들림 오프셋이 경계 클램프와 데드존 재앵커 이후 최종 적용되어, 데드존 앵커에 프레임 단위로
   누적되지 않는다 (D-08).
5. Inspector 신규 노출 필드가 `shakeMagnitude` / `shakeDuration` 정확히 2개다 (D-09).
6. `HP.cs` 는 0줄 변경이며 보스 피격 시에는 흔들리지 않는다 (D-01, D-02).
7. `CameraController.cs` / `PlayerStats.cs` 편집이 전부 순수 삽입(삭제 0줄)이고
   `CameraController.cs` 의 비-ASCII 라인 수가 5로 유지된다.
8. `Assets/Camera/Check.md` 에 Phase 12 정적 회귀 결과와 Play 모드 체크리스트가 기록됐다.
</success_criteria>

<output>
완료 후 `.planning/phases/12-camera-shake-on-hit/12-01-SUMMARY.md` 를 작성한다.

SUMMARY 에 반드시 포함할 것:
- 4곳 삽입의 실제 최종 라인 번호 (후속 작업의 앵커가 된다)
- 정적 회귀 12항목의 실제 출력값
- `shakeMagnitude` / `shakeDuration` 의 최종 확정 값 (튜닝 후 값 포함)
- Play 모드 검증 상태 (전항목 PASS / 일부 FAIL / 생략)
- 12-RESEARCH 가 하향 조정한 CameraController.cs 인코딩 위험 판정이 실제로 맞았는지
  (작업 후 비-ASCII 라인 수가 5를 유지했는지) — 향후 phase 의 판단 근거가 된다
</output>
