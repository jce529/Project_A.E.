---
phase: 08-watermonster-combatstate
plan: 03
type: execute
wave: 3
depends_on: ["08-01", "08-02"]
files_modified:
  - Assets/Enemy/WaterMonster/Check.md
  - Assets/Enemy/WaterSpirit/Check.md
  - Assets/Enemy/Tutorial/TutorialBoss/Check.md
autonomous: false
requirements: [D-01a, D-01c, D-03a, D-04a, D-04b, D-05a, D-06a, D-06b, D-06c]
must_haves:
  truths:
    - "Unity 에디터에서 컴파일 에러 0건이다"
    - "WaterMonster 보스가 랜덤 가중치 기반으로 패턴을 선택하며 WaterWavePush 45초 잠금이 지켜진다"
    - "WaterSpirit 의 완전배제 연속금지가 헬퍼 확장 이후에도 회귀 없이 동작한다"
    - "TutorialBoss 가 이번 변경의 영향을 받지 않는다"
    - "세 보스의 Check.md 에 PASS/FAIL 결과가 기록된다"
  artifacts:
    - path: "Assets/Enemy/WaterMonster/Check.md"
      provides: "WaterMonster 검증 결과 기록"
      contains: "결과 기록"
    - path: "Assets/Enemy/WaterSpirit/Check.md"
      provides: "Phase 7 보류 항목 해소 기록"
      contains: "결과 기록"
    - path: "Assets/Enemy/Tutorial/TutorialBoss/Check.md"
      provides: "TutorialBoss 회귀 없음 기록"
      contains: "결과 기록"
  key_links:
    - from: "Plan 08-02 마이그레이션"
      to: "Phase 7 07-02 보류 체크포인트"
      via: "WaterSpirit/TutorialBoss/WaterMonster 3종 일괄 Play 모드 검증"
      pattern: "결과 기록"
---

<objective>
Phase 7 에서 보류된 Play 모드 검증 체크포인트(07-02)와 Phase 8 의 WaterMonster 마이그레이션 검증을
한 번에 수행한다. 세 보스(WaterSpirit / TutorialBoss / WaterMonster)가 동일한 `CombatState`
패턴 판단 엔진 위에서 정상 동작하는지 사용자가 직접 확인하고, 각 `Check.md` 에 결과를 기록한다.

Purpose: grep 으로는 증명할 수 없는 런타임 동작(랜덤성, 45초 잠금 실측, 광폭화 배율 체감,
페이즈 전환 즉시 반영, WaterSpirit 완전배제 회귀 없음)을 사람이 검증한다.
Output: 세 `Check.md` 의 "결과 기록" 섹션이 채워지고, STATE.md 의 보류 TODO 가 해소된다.
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/08-watermonster-combatstate/08-CONTEXT.md
@.planning/phases/08-watermonster-combatstate/08-01-SUMMARY.md
@.planning/phases/08-watermonster-combatstate/08-02-SUMMARY.md
@Assets/Enemy/WaterMonster/Check.md
@Assets/Enemy/WaterSpirit/Check.md
@Assets/Enemy/Tutorial/TutorialBoss/Check.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 정적 회귀 검사 — 변경 범위와 계약 위반 여부 확인</name>

  <files>(읽기 전용 — 파일 수정 없음)</files>

  <read_first>
    - `Assets/Enemy/NewBoss/Script/States/CombatState.cs` (Plan 08-01 최종 상태)
    - `Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` (Plan 08-02 최종 상태)
    - `Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` (변경 0 라인이어야 함)
    - `Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs` (변경 0 라인이어야 함)
  </read_first>

  <action>
사용자가 Unity 를 켜기 전에, 코드 레벨에서 자동 검증 가능한 회귀 항목을 전부 확인한다.
파일은 수정하지 않는다. 아래 항목 중 하나라도 실패하면 Task 2 체크포인트로 진행하지 말고
어떤 항목이 왜 실패했는지 보고한다.

1. Phase 8 전체에서 변경된 `.cs` 파일이 정확히 2개인지 확인:
   `Assets/Enemy/NewBoss/Script/States/CombatState.cs` 와
   `Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs`.
   기준 커밋은 Phase 8 시작 직전 커밋(`git log --oneline` 에서 `docs(phase-07)` 커밋).
2. `SpiritCombatState.cs` 의 `SelectWeightedPattern(dist, _candidates);` 2-인자 호출이 그대로 남아 있어
   기본값 `lastUsedWeightMultiplier = 0f`(완전배제)로 동작함을 확인.
3. `CombatState.cs` 에 `_lastWaveTime` 문자열이 없고(스테일 주석 제거됨),
   `WaterMonsterCombatState.cs` 에도 `_lastWaveTime` 이 없음을 확인.
4. `WaterMonsterCombatState.cs` 의 사전 가드(`CanSpawnZone`/`CanTeleport`)가 `BuildCandidates` 호출보다
   위에 있음을 행 번호로 확인.
5. `WaterWavePush` 후보에 `cooldownOverride: WaveAttackCooldown` 이 붙어 있고
   `WaveAttackCooldown = 45f` 임을 확인 (D-04a 가 조용히 3f 로 축소되지 않았는지).
6. 세 `Check.md` 파일이 전부 존재함을 확인.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && C=Assets/Enemy/NewBoss/Script/States/CombatState.cs && W=Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs && S=Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs && grep -qF "SelectWeightedPattern(dist, _candidates);" $S && [ "$(grep -cF 'if (LastUsedPatternType == c.StrategyType) continue;' $C)" = "0" ] && grep -qF "if (lastUsedWeightMultiplier <= 0f) continue;" $C && grep -qF "float lastUsedWeightMultiplier = 0f)" $C && [ "$(grep -cF '_lastWaveTime' $C)" = "0" ] && [ "$(grep -cF '_lastWaveTime' $W)" = "0" ] && grep -qF "private const float WaveAttackCooldown = 45f;" $W && grep -qF "cooldownOverride: WaveAttackCooldown" $W && [ "$(grep -nF 'wmc.CanSpawnZone()' $W | cut -d: -f1)" -lt "$(grep -nF 'BuildCandidates(wmc)' $W | cut -d: -f1 | head -1)" ] && [ "$(grep -nF 'wmc.CanTeleport()' $W | cut -d: -f1)" -lt "$(grep -nF 'BuildCandidates(wmc)' $W | cut -d: -f1 | head -1)" ] && grep -qF "baseCooldown * GetPatternCooldownMultiplier()" $C && grep -qF "_isEnraged ? _enrageCooldownMultiplier : 1f;" $W && [ -f Assets/Enemy/WaterMonster/Check.md ] && [ -f Assets/Enemy/WaterSpirit/Check.md ] && [ -f Assets/Enemy/Tutorial/TutorialBoss/Check.md ] && echo TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "SelectWeightedPattern(dist, _candidates);" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 성공 (WaterSpirit 호출부 무변경 → 기본값 완전배제 유지)
    - `grep -cF 'if (LastUsedPatternType == c.StrategyType) continue;' Assets/Enemy/NewBoss/Script/States/CombatState.cs` == 0 그리고 `grep -qF "if (lastUsedWeightMultiplier <= 0f) continue;"` 성공 (완전배제가 조건부로 전환됨)
    - `grep -cF '_lastWaveTime'` == 0 (CombatState.cs 및 WaterMonsterCombatState.cs 양쪽)
    - `grep -qF "private const float WaveAttackCooldown = 45f;"` 및 `grep -qF "cooldownOverride: WaveAttackCooldown"` (WaterMonsterCombatState.cs) 성공
    - `grep -nF 'wmc.CanSpawnZone()'` 와 `grep -nF 'wmc.CanTeleport()'` 의 행 번호가 `grep -nF 'BuildCandidates(wmc)'` 첫 행 번호보다 작다
    - `grep -qF "baseCooldown * GetPatternCooldownMultiplier()" ...CombatState.cs` 및 `grep -qF "_isEnraged ? _enrageCooldownMultiplier : 1f;" ...WaterMonsterCombatState.cs` 성공 (광폭화 배율 경로 연결)
    - `Assets/Enemy/WaterMonster/Check.md`, `Assets/Enemy/WaterSpirit/Check.md`, `Assets/Enemy/Tutorial/TutorialBoss/Check.md` 세 파일 모두 존재
    - `git diff --name-only <phase-07 마지막 커밋>..HEAD -- 'Assets/**/*.cs'` 결과가 `CombatState.cs` 와 `WaterMonsterCombatState.cs` 2개뿐
  </acceptance_criteria>

  <done>정적 검사 전 항목 통과 — WaterSpirit 호출부 무변경, WaterWavePush 45초 잠금 보존, 사전 가드 위치 보존, 광폭화 배율 경로 연결이 코드 레벨에서 확인되었고, 변경된 `.cs` 는 2개뿐이다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 3종 보스 일괄 Play 모드 검증 체크포인트</name>

  <files>Assets/Enemy/WaterMonster/Check.md, Assets/Enemy/WaterSpirit/Check.md, Assets/Enemy/Tutorial/TutorialBoss/Check.md</files>

  <read_first>
    - `Assets/Enemy/WaterMonster/Check.md` (Plan 08-02 Task 2 가 작성한 체크리스트)
    - `Assets/Enemy/WaterSpirit/Check.md` (Phase 7 에서 보류된 체크리스트)
    - `Assets/Enemy/Tutorial/TutorialBoss/Check.md` (Phase 7 에서 보류된 체크리스트)
  </read_first>

  <action>
사용자에게 아래 `<how-to-verify>` 절차를 그대로 제시하고 Play 모드 검증을 요청한 뒤, 응답을 기다린다.
Claude 가 대신 수행할 수 없는 작업이다 (Unity 에디터 Play 모드 실행 + 시각/시간 기반 관찰).

사용자 응답을 받은 후 Claude 가 할 일:
- "approved" 인 경우: 사용자가 보고한 관찰 내용을 `Assets/Enemy/WaterMonster/Check.md`,
  `Assets/Enemy/WaterSpirit/Check.md`, `Assets/Enemy/Tutorial/TutorialBoss/Check.md` 세 파일의
  "결과 기록" 섹션에 기록하고, 각 검증 항목의 `- [ ]` 를 `- [x]` 로 변경한다.
  또한 `Assets/Enemy/WaterSpirit/Check.md` 의 "상태: 보류" 안내 문구를 검증 완료 상태로 갱신한다.
- 문제가 보고된 경우: 해당 항목만 `- [ ]` 로 남기고 관찰 내용을 "결과 기록" 에 FAIL 로 기록한 뒤,
  갭 목록을 정리해 `/gsd:plan-phase 8 --gaps` 입력으로 사용할 수 있게 보고한다.
  임의로 코드를 고치지 말고 먼저 보고할 것.
  </action>

  <what-built>
    - Plan 08-01: `CombatState` 헬퍼에 쿨다운 오버라이드(`PatternCandidate.CooldownOverride`),
      광폭화 배율 훅(`GetPatternCooldownMultiplier`), 직전 패턴 가중치 감쇠 모드
      (`SelectWeightedPattern(..., lastUsedWeightMultiplier)`) 추가. 세 확장 모두 기본값이
      기존 동작이므로 `SpiritCombatState.cs` 는 변경 0 라인.
    - Plan 08-02: `WaterMonsterCombatState` 의 수작업 `List<IAttackStrategy>` + `Random.Range`
      풀-랜덤을 8개 `PatternCandidate` 선언 + `SelectWeightedPattern(dist, candidates, 0.5f)` 로 교체.
      `WaterWavePush` 45초 잠금은 `cooldownOverride` 로, 광폭화 0.5배는
      `GetPatternCooldownMultiplier()` override 로 유지. 장판 스폰/텔레포트 사전 가드,
      `ShouldTransitionToGroggy() => false` 는 원형 그대로.
  </what-built>

  <how-to-verify>
    **0. 사전 준비**
    1. Unity 에디터를 열고 컴파일이 끝날 때까지 대기한다. Console 에 **에러 0건**인지 확인한다.
       (에러가 있다면 여기서 중단하고 에러 원문을 알려 주세요.)
    2. Console 창에서 **Collapse 를 끄고**, 검색창을 이용해 보스별로 필터링한다.

    **1. WaterMonster (신규 마이그레이션) — `Assets/Enemy/WaterMonster/Check.md` 체크리스트대로**
    1. 물괴물 보스 씬을 열고 Play. Console 필터: `WaterMonsterCombatState`
    2. 30초 이상 교전하며 `[WaterMonsterCombatState] 패턴 선택:` 로그 시퀀스를 기록한다.
       - 기대: 고정 순서로 반복되지 않는다. Stop 후 재시작하면 다른 시퀀스가 나온다.
    3. `패턴 선택: WaterWavePush` 가 찍힌 시각을 메모하고, **다음 WaterWavePush 까지 최소 45초**가
       걸리는지 확인한다 (비광폭화 상태).
    4. 같은 패턴명이 연속 두 줄로 나오는 경우가 **가끔은 나오되 드물다**는 것을 확인한다.
       (완전배제가 아니라 0.5배 감쇠이므로 "절대 안 나옴"이면 오히려 잘못된 것이다.)
    5. 플레이어를 보스에 붙였을 때(`거리:` 3.0 이하) `WaterRangedSpit` 이 선택되지 않고,
       멀어졌을 때(3.0 초과) `WaterMeleeSwipe`/`WaterJumpLand` 가 선택되지 않는지 확인한다.
    6. 보스 HP 를 깎아 페이즈 2 진입 → 그 **직후 판단부터** `WaterPrisonAttack` 대신
       `WaterPrisonMapAoe` 가 나오는지 확인. 페이즈 3 진입 후에는 `WaterColorPrison` 로 바뀌는지 확인.
       (전투 상태를 나갔다 들어오지 않아도 즉시 바뀌어야 한다.)
    7. 광폭화 진입 후 `패턴 선택:` 로그 간격이 짧아지고, `WaterWavePush` 재등장 간격이
       약 22.5초로 줄어드는지 확인한다.
    8. 광폭화 중 장판이 기존과 동일하게 스폰되고, 파괴 불가 웅덩이 2개 이상 + 텔레포트 쿨다운 충족 시
       `WaterTeleportState` 로 전환되는지 확인한다 (사전 가드 회귀 없음).
    9. `사용 가능한 패턴 후보 없음` 로그가 프레임마다 쏟아지지 않는지 확인한다.

    **2. WaterSpirit (회귀 검증) — `Assets/Enemy/WaterSpirit/Check.md` 체크리스트대로**
    1. 물의 정령 보스 씬을 열고 Play. Console 필터: `SpiritCombatState`
    2. `Check.md` 의 D-03 랜덤성 / D-05 연속 사용 금지 / D-04 강제 체인 / 후보 고갈 스팸 없음 /
       D-07 Stage 2 사이클 항목을 순서대로 확인한다.
    3. **가장 중요한 회귀 항목**: 같은 패턴명이 연속 두 줄로 나오는 경우가 **단 한 번도 없어야 한다**
       (WaterMonster 와 달리 WaterSpirit 은 완전배제 유지). 한 번이라도 연속으로 나오면
       Plan 08-01 의 기본값 처리가 잘못된 것이므로 즉시 보고해 주세요.

    **3. TutorialBoss (영향 없음 확인) — `Assets/Enemy/Tutorial/TutorialBoss/Check.md` 체크리스트대로**
    1. 튜토리얼 보스 씬을 열고 Play 하여 `Check.md` 항목을 확인한다.
    2. 기대: 이번 변경과 무관하게 기존과 동일하게 동작한다.

    **4. 결과 기록**
    각 `Check.md` 의 "결과 기록" 섹션에 관찰한 로그 원문과 항목별 PASS/FAIL 을 기록해 주세요
    (직접 기록이 번거로우시면 관찰 결과를 알려 주시면 제가 세 파일에 기록하겠습니다).
  </how-to-verify>

  <acceptance_criteria>
    - Unity Console 컴파일 에러 0건
    - `Assets/Enemy/WaterMonster/Check.md` 의 모든 검증 항목이 `- [x]` 로 체크되고 "결과 기록" 섹션에 관찰 로그가 기록됨
    - `Assets/Enemy/WaterSpirit/Check.md` 의 모든 검증 항목이 `- [x]` 로 체크되고, D-05 연속 사용 금지 항목이 PASS(완전배제 유지)로 기록됨
    - `Assets/Enemy/Tutorial/TutorialBoss/Check.md` 의 모든 검증 항목이 `- [x]` 로 체크됨
    - 세 파일 각각에 대해 `grep -c '^- \[ \]' <file>` == 0 (미체크 항목 없음)
    - FAIL 항목이 있는 경우 해당 항목과 관찰 내용이 파일에 기록되고, 후속 갭 클로저 대상으로 보고됨
  </acceptance_criteria>

  <resume-signal>세 보스 모두 이상 없으면 "approved" 라고 입력해 주세요. 문제가 있으면 어떤 보스의 어떤 항목이 어떻게 다른지(가능하면 Console 로그 원문과 함께) 알려 주세요.</resume-signal>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && [ "$(grep -c '^- \[ \]' Assets/Enemy/WaterMonster/Check.md)" = "0" ] && [ "$(grep -c '^- \[ \]' Assets/Enemy/WaterSpirit/Check.md)" = "0" ] && [ "$(grep -c '^- \[ \]' Assets/Enemy/Tutorial/TutorialBoss/Check.md)" = "0" ] && [ "$(grep -c '^- \[x\]' Assets/Enemy/WaterMonster/Check.md)" -ge 8 ] && echo CHECKPOINT_OK</automated>
  </verify>

  <done>사용자가 Unity Play 모드에서 WaterSpirit / TutorialBoss / WaterMonster 3종을 검증했고, 세 `Check.md` 에 미체크 항목(`- [ ]`)이 하나도 남아 있지 않으며 관찰 로그와 PASS/FAIL 이 "결과 기록" 섹션에 기록되었다.</done>
</task>

</tasks>

<verification>
1. Unity 컴파일 에러 0건.
2. 세 `Check.md` 에 미체크 항목(`- [ ]`)이 남아 있지 않다.
3. WaterSpirit 의 연속 사용 금지가 완전배제로 동작함이 로그로 확인되었다 (Plan 08-01 회귀 없음).
4. WaterWavePush 45초(광폭화 22.5초) 잠금이 실측으로 확인되었다 (D-04a/D-04b).
5. 페이즈 전환 직후 프리즌 변형이 즉시 교체됨이 확인되었다 (D-06c).
</verification>

<success_criteria>
- 세 보스 전부 Play 모드 검증 완료, 각 `Check.md` 에 PASS/FAIL 기록됨.
- Phase 7 의 보류 항목(07-02 체크포인트, STATE.md Active TODO)이 해소됨.
- 회귀 발견 시 갭 목록이 명확히 문서화되어 `/gsd:plan-phase 8 --gaps` 입력으로 사용 가능.
</success_criteria>

<output>
완료 후 `.planning/phases/08-watermonster-combatstate/08-03-SUMMARY.md` 를 생성하고,
`.planning/STATE.md` 의 Active TODO 에서 Phase 7 Plan 2 보류 항목을 해소 처리한다.
</output>
