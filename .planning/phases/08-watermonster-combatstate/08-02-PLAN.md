---
phase: 08-watermonster-combatstate
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs
  - Assets/Enemy/WaterMonster/Check.md
autonomous: true
requirements: [D-01a, D-01b, D-01c, D-02a, D-03a, D-04a, D-04b, D-05a, D-06a, D-06b, D-06c]
must_haves:
  truths:
    - "WaterMonster 의 패턴 선택이 수작업 List+Random.Range 가 아니라 CombatState.SelectWeightedPattern 을 통과한다"
    - "WaterWavePush 는 45초(광폭화 시 22.5초) 재사용 잠금을 유지한다"
    - "광폭화 중에는 _decisionTimer 뿐 아니라 패턴별 개별 쿨다운도 0.5배로 단축된다"
    - "직전 사용 패턴은 제외되지 않고 가중치 0.5배로 후보에 남는다"
    - "전투 도중 페이즈가 바뀌면 다음 판단부터 해당 페이즈의 프리즌 변형이 후보에 오른다"
    - "장판 스폰/텔레포트 사전 가드는 후보 목록 구성 이전 최상단 early-return 으로 남아 있다"
    - "SpiritCombatState.cs / Stage2CombatState.cs / CombatState.cs 는 이 플랜에서 변경되지 않는다"
  artifacts:
    - path: "Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs"
      provides: "PatternCandidate 기반 후보 선언 + GetPatternCooldownMultiplier override"
      contains: "protected override float GetPatternCooldownMultiplier()"
    - path: "Assets/Enemy/WaterMonster/Check.md"
      provides: "WaterMonster Play 모드 검증 체크리스트 (Plan 08-03 입력)"
      contains: "WaterMonsterCombatState"
  key_links:
    - from: "WaterMonsterCombatState.SelectAttackStrategy"
      to: "CombatState.SelectWeightedPattern"
      via: "감쇠 배율 LastUsedWeightDecay 전달"
      pattern: "SelectWeightedPattern\\(dist, candidates, LastUsedWeightDecay\\)"
    - from: "WaterWavePush 후보"
      to: "CombatState._patternReadyAt"
      via: "cooldownOverride: WaveAttackCooldown (45f)"
      pattern: "cooldownOverride: WaveAttackCooldown"
    - from: "WaterMonsterCombatState._enrageCooldownMultiplier"
      to: "CombatState.CommitSelection"
      via: "GetPatternCooldownMultiplier() override"
      pattern: "_isEnraged \\? _enrageCooldownMultiplier : 1f"
    - from: "WaterMonsterController.IsPhase2/IsPhase3"
      to: "후보 목록의 프리즌 변형 슬롯"
      via: "BuildCandidates 를 SelectAttackStrategy 매 호출마다 재구성 (Enter 캐싱 금지)"
      pattern: "BuildCandidates\\(wmc\\)"
---

<objective>
`WaterMonsterCombatState.SelectAttackStrategy` 의 수작업 `List<IAttackStrategy>` + `Random.Range`
풀-랜덤 선택을 Plan 08-01 에서 확장한 `CombatState` 헬퍼 기반 후보 선언으로 교체하고,
검증용 `Check.md` 체크리스트를 작성한다.

Purpose: 세 보스(WaterSpirit / TutorialBoss / WaterMonster)가 동일한 패턴 판단 엔진을 공유하게
만들어 Phase 7 D-08b 이월 항목을 종결하고, 보류 중인 일괄 Play 모드 검증(Plan 08-03)을 가능하게 한다.
Output: 마이그레이션된 `WaterMonsterCombatState.cs` + `Assets/Enemy/WaterMonster/Check.md`.

**의도적으로 수용하는 동작 변화 (RESEARCH Pitfall 6 — 검토 시 스코프 크립으로 오인하지 말 것):**
마이그레이션 이전에는 `WaterWavePush` 만 재사용 잠금이 있었고 나머지 패턴
(`WaterGeyser`/`WaterMeleeSwipe`/`WaterJumpLand`/`WaterRangedSpit`/프리즌 3종)은
매 판단마다 즉시 재선택될 수 있었다. 헬퍼의 `CommitSelection` 을 채택하면 모든 패턴이
자신의 `IAttackStrategy.Cooldown` 값만큼 자동 자가 쿨다운을 갖게 된다:
WaterGeyser 3s / WaterWavePush 45s(오버라이드) / WaterMeleeSwipe 1.4s / WaterJumpLand 4s /
WaterRangedSpit 2.0s / WaterPrisonAttack 5s / WaterPrisonMapAoe 8s / WaterColorPrison 10s.
이는 Phase 7 에서 `SpiritCombatState` 가 같은 헬퍼를 채택했을 때와 동일한 선례이며,
D-02a("균등 가중치 유지")는 가중치 균등성에 관한 것이지 자가 쿨다운 제거를 요구하지 않는다.
**이 값들은 새로 발명한 것이 아니라 기존 각 전략 클래스에 이미 선언되어 있는 값이다 — 변경 금지.**
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-watermonster-combatstate/08-CONTEXT.md
@.planning/phases/08-watermonster-combatstate/08-RESEARCH.md
@.planning/phases/08-watermonster-combatstate/08-01-SUMMARY.md
@Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs
@Assets/Enemy/NewBoss/Script/States/CombatState.cs
@Assets/Enemy/WaterSpirit/Check.md

<interfaces>
<!-- 실행자가 필요로 하는 계약 전부. 코드베이스 탐색 없이 이 내용만으로 작업 가능해야 한다. -->

Plan 08-01 이후의 CombatState 헬퍼 (protected 멤버):
```csharp
protected class PatternCandidate
{
    public PatternCandidate(System.Func<IAttackStrategy> factory, float weight,
                            float? minDistance = null, float? maxDistance = null,
                            float? cooldownOverride = null);
}
protected System.Type LastUsedPatternType { get; }
protected virtual float GetPatternCooldownMultiplier();          // 기본 1f — 여기서 override 한다
protected IAttackStrategy SelectWeightedPattern(float dist, IReadOnlyList<PatternCandidate> candidates,
                                                float lastUsedWeightMultiplier = 0f);
protected IAttackStrategy ForceSelectPattern(IAttackStrategy strategy);
protected float _decisionTimer;
public virtual void Enter(BossController boss);   // LastUsedPatternType = null; _patternReadyAt.Clear();
```

WaterMonsterController 에서 사용하는 멤버 (전부 기존 그대로, 변경 금지):
```csharp
public bool IsPhase2 { get; private set; }        // HP 임계 통과 시 전투 도중 true 로 전환
public bool IsPhase3 { get; private set; }        // HP 임계 통과 시 전투 도중 true 로 전환
public bool IsEnraged { get; private set; }
public bool CanSpawnZone();
public void SpawnRandomZone();
public void RecordZoneTime();
public bool CanTeleport();
```

각 공격 전략의 기존 `IAttackStrategy.Cooldown` 값 (전부 확인 완료 — 절대 수정하지 말 것):
| 전략 | Cooldown | 비고 |
|---|---|---|
| `WaterGeyser` | 3f | |
| `WaterWavePush` | 3f | 실제 재사용 잠금 45f 는 별개 값 → cooldownOverride 로 전달 |
| `WaterMeleeSwipe` | 1.4f | |
| `WaterJumpLand` | 4f | |
| `WaterRangedSpit` | 2.0f | |
| `WaterPrisonAttack` | 5f (virtual) | 페이즈 1 |
| `WaterPrisonMapAoe` | 8f (override, `: WaterPrisonAttack` 상속) | 페이즈 2 |
| `WaterColorPrison` | 10f | 페이즈 3 |

인스턴스 수명 사실 (검증 완료): `WaterTeleportState` 와 `WaterMonsterController.Update()` 인터셉트는
모두 `new WaterMonsterCombatState()` 로 새 인스턴스를 만든다. 따라서 구 `_lastWaveTime = -999f`
초기화와 신 `CombatState.Enter()` 의 `_patternReadyAt.Clear()` 는 동일한 리셋 시점을 갖는다 — 회귀 없음.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WaterMonsterCombatState 를 PatternCandidate 후보 선언 데이터 레이어로 교체</name>

  <files>Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs</files>

  <read_first>
    - `Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` (수정 대상 — 현재 전체 내용 필수 확인)
    - `Assets/Enemy/NewBoss/Script/States/CombatState.cs` (Plan 08-01 이 확장한 최신 헬퍼 시그니처 확인)
    - `Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` (참고 구현 — 단 `Enter()` 1회 캐싱 방식은 **복사 금지**, D-06c)
    - `Assets/Enemy/WaterMonster/Script/WaterMonsterController.cs` (`IsPhase2`/`IsPhase3`/`IsEnraged`/`CanSpawnZone`/`CanTeleport` 시그니처 확인)
  </read_first>

  <action>
`Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` 전체를 아래 내용으로 교체한다.
다른 파일은 단 한 글자도 수정하지 않는다.

```csharp
// Phase 1+3 — WaterMonsterCombatState
// Phase 1: ShouldTransitionToGroggy override (MaxWater=0 support)
// Phase 3: Add teleport condition to SelectAttackStrategy (D-12, D-13)
// Phase 8: 수작업 pool-random 을 CombatState 범용 헬퍼(PatternCandidate + SelectWeightedPattern)로 교체

using System.Collections.Generic;
using UnityEngine;
using WaterMonster.Phase2;

public class WaterMonsterCombatState : CombatState
{
    [SerializeField] private float _enrageCooldownMultiplier = 0.5f;
    private bool _isEnraged = false;

    // D-02a: 모든 패턴 균등 가중치 — 기존 uniform pool-random 과 동일한 체감을 유지한다.
    //        이번 페이즈에서 패턴 간 밸런싱(차등 가중치)은 하지 않는다.
    private const float PatternWeight = 1.0f;

    // D-01a~c: 직전 사용 패턴은 완전배제(Phase 7 D-05)가 아니라 가중치 0.5배로 감쇠시킨다.
    //          감쇠는 직전 1개 패턴에만 적용되며 누적되지 않는다.
    private const float LastUsedWeightDecay = 0.5f;

    // D-04a: WaterWavePush 특수 재사용 잠금(45초). WaterWavePush.Cooldown(3f) 과는 별개 값이므로
    //        PatternCandidate 의 cooldownOverride 로 전달한다. 이 값을 strategy.Cooldown 에
    //        의존시키면 잠금이 3초로 조용히 줄어든다.
    private const float WaveAttackCooldown = 45f;

    // D-06a: 근접(<= 3.0) / 원거리(> 3.0) 분기 경계값.
    private const float MeleeRange = 3.0f;

    private bool _noCandidateLogged;

    public void SetEnraged(bool value) { _isEnraged = value; }

    public override void Enter(BossController boss)
    {
        base.Enter(boss);
        // 광폭화 트리거가 다른 State에서 발생했을 수 있으므로 Controller에서 상태 읽기
        if (boss is WaterMonsterController wmc)
            _isEnraged = wmc.IsEnraged;
        _noCandidateLogged = false;
    }

    public override void Execute(BossController boss)
    {
        base.Execute(boss);
        // 광폭화 시 쿨다운 배율 적용
        // base.Execute가 _decisionTimer = attack.Cooldown 으로 설정한 직후
        // 배율을 곱해 실질적 대기 시간 감소
        if (_isEnraged)
        {
            _decisionTimer *= _enrageCooldownMultiplier;
        }
    }

    // D-03a: 광폭화 시 전체 판단 대기(_decisionTimer) 뿐 아니라 패턴별 개별 쿨다운
    //        (CombatState._patternReadyAt)에도 동일한 배율을 적용한다.
    // D-04b: 이에 따라 WaveAttackCooldown 45초도 광폭화 중에는 22.5초로 함께 단축된다.
    protected override float GetPatternCooldownMultiplier()
        => _isEnraged ? _enrageCooldownMultiplier : 1f;

    protected override bool ShouldTransitionToGroggy(BossController boss)
    {
        return false;
    }

    protected override IAttackStrategy SelectAttackStrategy(BossController boss, float dist)
    {
        if (!(boss is WaterMonsterController wmc))
            return base.SelectAttackStrategy(boss, dist);

        // D-05a: 장판 스폰 / 텔레포트 전환은 "공격 패턴 후보"가 아니라 상태 전환 트리거다.
        //        후보 목록 구성보다 앞선 최상단 early-return 으로 기존 흐름 그대로 유지한다.
        if (_isEnraged && wmc.CanSpawnZone())
        {
            wmc.SpawnRandomZone();
            wmc.RecordZoneTime();
            _decisionTimer = 1.0f;
            return null;
        }

        if (PuddleStackManager.Instance != null
            && PuddleStackManager.Instance.IndestructibleCount >= 2
            && wmc.CanTeleport())
        {
            boss.ChangeState(new WaterTeleportState());
            return null;
        }

        // D-06c: 페이즈(IsPhase2/IsPhase3)는 전투 도중 HP 임계치로 전환되므로 후보 목록을
        //        Enter() 에 캐싱하지 않고 매 판단마다 새로 구성한다.
        //        (SpiritCombatState 의 Enter()-1회-캐싱 방식을 복사하면 페이즈 전환 후에도
        //         이전 페이즈의 프리즌 패턴이 계속 후보에 남는다.)
        //        마이그레이션 이전 코드도 매 호출마다 pool 을 새로 만들었으므로 할당 프로파일 동일.
        List<PatternCandidate> candidates = BuildCandidates(wmc);

        // D-01d: 감쇠 배율을 전달한다. 0f 를 넘기면 WaterSpirit 과 같은 완전배제가 되므로 주의.
        IAttackStrategy attack = SelectWeightedPattern(dist, candidates, LastUsedWeightDecay);

        if (attack == null)
        {
            // 전 후보가 쿨다운/거리로 막힌 일시적 상황. 매 프레임 재호출되므로 1회만 로그.
            if (!_noCandidateLogged)
            {
                Debug.Log($"[WaterMonsterCombatState] 사용 가능한 패턴 후보 없음 (거리: {dist:F1}) — 쿨다운 해제까지 대기");
                _noCandidateLogged = true;
            }
            return null;
        }

        _noCandidateLogged = false;
        Debug.Log($"[WaterMonsterCombatState] 패턴 선택: {attack.GetType().Name} (거리: {dist:F1})");
        return attack;
    }

    // D-06a: 근접 dist <= 3.0 → WaterMeleeSwipe / WaterJumpLand, 원거리 dist > 3.0 → WaterRangedSpit.
    //        헬퍼의 MinDistance/MaxDistance 는 양끝 포함 판정이므로 dist 가 정확히 3.0 인 순간에만
    //        양쪽이 동시에 후보가 된다 — 실측 확률상 무시 가능한 경계 차이로 수용한다(RESEARCH Pitfall 5).
    // D-06b: 프리즌 변형 3종은 현재 페이즈에 해당하는 1개만 후보에 오른다 (상호 배타, 기존 동작과 동일).
    private List<PatternCandidate> BuildCandidates(WaterMonsterController wmc)
    {
        var candidates = new List<PatternCandidate>
        {
            new PatternCandidate(() => new WaterGeyser(), PatternWeight),
            new PatternCandidate(() => new WaterWavePush(), PatternWeight, cooldownOverride: WaveAttackCooldown),
            new PatternCandidate(() => new WaterMeleeSwipe(), PatternWeight, maxDistance: MeleeRange),
            new PatternCandidate(() => new WaterJumpLand(), PatternWeight, maxDistance: MeleeRange),
            new PatternCandidate(() => new WaterRangedSpit(), PatternWeight, minDistance: MeleeRange),
        };

        if (wmc.IsPhase3)
            candidates.Add(new PatternCandidate(() => new WaterColorPrison(), PatternWeight));      // 페이즈 3: 패턴 7
        else if (wmc.IsPhase2)
            candidates.Add(new PatternCandidate(() => new WaterPrisonMapAoe(), PatternWeight));     // 페이즈 2: 패턴 5
        else
            candidates.Add(new PatternCandidate(() => new WaterPrisonAttack(), PatternWeight));     // 페이즈 1: 패턴 4

        return candidates;
    }
}
```

**고아 정리 (CLAUDE.md §3 — 본인 변경으로 고립된 것만 제거):**
- `private float _lastWaveTime = -999f;` 필드 → 삭제 (`_patternReadyAt` 이 대체)
- `Time.time - _lastWaveTime >= WaveAttackCooldown` 게이트와 `if (selected is WaterWavePush) _lastWaveTime = Time.time;` 기록 → 삭제
- `private const float WaveAttackCooldown = 45f;` → **삭제하지 말 것.** `cooldownOverride` 값으로 계속 사용된다.
- `using System.Collections.Generic;` / `using UnityEngine;` / `using WaterMonster.Phase2;` → 전부 계속 필요 (List / Debug·SerializeField / PuddleStackManager)
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs && [ "$(grep -cF '_lastWaveTime' $F)" = "0" ] && [ "$(grep -cF 'Random.Range' $F)" = "0" ] && [ "$(grep -cF 'List<IAttackStrategy>' $F)" = "0" ] && [ "$(grep -cF 'new PatternCandidate(' $F)" = "8" ] && grep -qF "private const float WaveAttackCooldown = 45f;" $F && grep -qF "private const float LastUsedWeightDecay = 0.5f;" $F && grep -qF "private const float PatternWeight = 1.0f;" $F && grep -qF "private const float MeleeRange = 3.0f;" $F && grep -qF "cooldownOverride: WaveAttackCooldown" $F && grep -qF "SelectWeightedPattern(dist, candidates, LastUsedWeightDecay)" $F && grep -qF "protected override float GetPatternCooldownMultiplier()" $F && grep -qF "_isEnraged ? _enrageCooldownMultiplier : 1f;" $F && [ "$(grep -cF 'maxDistance: MeleeRange' $F)" = "2" ] && [ "$(grep -cF 'minDistance: MeleeRange' $F)" = "1" ] && grep -qF "wmc.SpawnRandomZone();" $F && grep -qF "wmc.RecordZoneTime();" $F && grep -qF "boss.ChangeState(new WaterTeleportState());" $F && grep -qF "PuddleStackManager.Instance.IndestructibleCount >= 2" $F && grep -qF "protected override bool ShouldTransitionToGroggy(BossController boss)" $F && grep -qF "_decisionTimer *= _enrageCooldownMultiplier;" $F && [ "$(grep -nF 'wmc.CanTeleport()' $F | cut -d: -f1)" -lt "$(grep -nF 'BuildCandidates(wmc)' $F | cut -d: -f1 | head -1)" ] && [ "$(grep -cF 'wmc.IsPhase3' $F)" = "1" ] && [ "$(grep -cF 'wmc.IsPhase2' $F)" = "1" ] && [ "$(grep -cF '_candidates' $F)" = "0" ] && git diff --exit-code -- Assets/Enemy/NewBoss/Script/States/CombatState.cs Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/WaterMonsterController.cs Assets/Enemy/WaterMonster/Script/Phase3/WaterTeleportState.cs && echo TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -cF '_lastWaveTime' ...WaterMonsterCombatState.cs` == 0 (고아 필드 제거)
    - `grep -cF 'Random.Range' ...WaterMonsterCombatState.cs` == 0 그리고 `grep -cF 'List<IAttackStrategy>'` == 0 (수작업 풀-랜덤 제거)
    - `grep -cF 'new PatternCandidate(' ...WaterMonsterCombatState.cs` == 8 (공통 5 + 페이즈별 3)
    - `grep -qF "private const float WaveAttackCooldown = 45f;"` 성공 그리고 `grep -qF "cooldownOverride: WaveAttackCooldown"` 성공 (D-04a 45초 잠금 유지)
    - `grep -qF "private const float LastUsedWeightDecay = 0.5f;"` 성공 그리고 `grep -qF "SelectWeightedPattern(dist, candidates, LastUsedWeightDecay)"` 성공 (D-01c 0.5배 감쇠)
    - `grep -qF "protected override float GetPatternCooldownMultiplier()"` 성공 그리고 `grep -qF "_isEnraged ? _enrageCooldownMultiplier : 1f;"` 성공 (D-03a)
    - `grep -cF 'maxDistance: MeleeRange'` == 2 (Swipe, JumpLand) 그리고 `grep -cF 'minDistance: MeleeRange'` == 1 (RangedSpit)
    - `grep -cF 'wmc.IsPhase3'` == 1 그리고 `grep -cF 'wmc.IsPhase2'` == 1 (프리즌 변형 상호 배타, D-06b)
    - `grep -cF '_candidates'` == 0 (Enter() 캐싱 필드 없음 — D-06c 매 호출 재구성)
    - `grep -nF 'wmc.CanTeleport()'` 의 행 번호 < `grep -nF 'BuildCandidates(wmc)'` 의 첫 행 번호 (사전 가드가 후보 구성보다 앞, D-05a)
    - `grep -qF "wmc.SpawnRandomZone();"`, `grep -qF "wmc.RecordZoneTime();"`, `grep -qF "boss.ChangeState(new WaterTeleportState());"`, `grep -qF "PuddleStackManager.Instance.IndestructibleCount >= 2"` 전부 성공 (사전 가드 원형 보존)
    - `grep -qF "protected override bool ShouldTransitionToGroggy(BossController boss)"` 성공 그리고 해당 메서드가 `return false;` 유지 (범위 외 항목 불변)
    - `git diff --exit-code -- Assets/Enemy/NewBoss/Script/States/CombatState.cs Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/WaterMonsterController.cs Assets/Enemy/WaterMonster/Script/Phase3/WaterTeleportState.cs` 종료코드 0
  </acceptance_criteria>

  <done>`WaterMonsterCombatState` 가 8개 `PatternCandidate` 만 선언하는 데이터 레이어가 되고, 판단은 전부 `CombatState.SelectWeightedPattern(dist, candidates, 0.5f)` 이 담당하며, WaterWavePush 45초 잠금과 광폭화 0.5배 배율이 `_patternReadyAt` 경로까지 도달한다. 사전 가드 2종과 `ShouldTransitionToGroggy() => false` 는 원형 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: WaterMonster Play 모드 검증 체크리스트 작성</name>

  <files>Assets/Enemy/WaterMonster/Check.md</files>

  <read_first>
    - `Assets/Enemy/WaterSpirit/Check.md` (동일 포맷을 따를 것 — 제목/사전준비/검증항목/결과기록 4개 섹션)
    - `Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` (Task 1 이 방금 작성한 최종 코드 — 로그 문자열과 상수값을 여기서 확인해 체크리스트에 정확히 옮길 것)
  </read_first>

  <action>
`Assets/Enemy/WaterMonster/Check.md` 를 신규 생성한다. `Assets/Enemy/WaterSpirit/Check.md` 와 동일한
4개 섹션 구조(검증 대상 변경사항 / 사전 준비 / 검증 항목 / 결과 기록)를 따르되, 아래 내용을 담는다.
Unity `.meta` 파일은 Unity 에디터가 자동 생성하므로 수동으로 만들지 않는다.

포함해야 할 내용:

1. **제목**: `# WaterMonster — 패턴 판단 로직 Play 모드 검증 체크리스트`
2. **상태 안내**: Phase 8 마이그레이션 결과이며, WaterSpirit / TutorialBoss / WaterMonster 3종을
   `.planning/phases/08-watermonster-combatstate/08-03-PLAN.md` 체크포인트에서 일괄 검증한다고 명시.
3. **검증 대상 변경사항** — 후보 표 (정확히 아래 값):

   | 패턴 | 가중치 | 거리 조건 | 재사용 잠금 |
   |---|---|---|---|
   | WaterGeyser | 1.0 | 없음 | 3s |
   | WaterWavePush | 1.0 | 없음 | **45s (cooldownOverride)** |
   | WaterMeleeSwipe | 1.0 | `dist <= 3.0` | 1.4s |
   | WaterJumpLand | 1.0 | `dist <= 3.0` | 4s |
   | WaterRangedSpit | 1.0 | `dist >= 3.0` | 2.0s |
   | WaterPrisonAttack | 1.0 | 없음 (페이즈 1 전용) | 5s |
   | WaterPrisonMapAoe | 1.0 | 없음 (페이즈 2 전용) | 8s |
   | WaterColorPrison | 1.0 | 없음 (페이즈 3 전용) | 10s |

   추가로: 직전 사용 패턴은 완전배제가 아니라 가중치 0.5배 감쇠(D-01), 광폭화 중 모든 개별
   쿨다운 0.5배(D-03, WavePush 45s → 22.5s), 장판 스폰/텔레포트 사전 가드는 변경 없음(D-05).

4. **사전 준비**: (1) Unity 컴파일 에러 0건 확인, (2) 물괴물 보스 씬 열기,
   (3) Console Collapse 끄고 검색창에 `WaterMonsterCombatState` 입력해 필터링.

5. **검증 항목** — 체크박스 목록, 각 항목에 관찰할 로그 문자열을 명시:
   - [ ] **D-02 랜덤성**: 30초 이상 교전 → `[WaterMonsterCombatState] 패턴 선택:` 로그가 고정 순서로
         반복되지 않는다. Stop 후 재시작한 두 번째 시퀀스가 첫 번째와 다르다.
   - [ ] **D-01 가중치 감쇠(완전배제 아님)**: 같은 패턴명이 연속 두 줄로 나오는 경우가 **드물게라도
         발생한다**(0.5배 감쇠이지 금지가 아님). 다만 연속 발생 빈도가 눈에 띄게 낮다.
   - [ ] **D-04 WaterWavePush 45초 잠금**: `패턴 선택: WaterWavePush` 가 찍힌 뒤 최소 45초 동안
         다시 등장하지 않는다 (비광폭화 상태에서 측정).
   - [ ] **D-03/D-04b 광폭화 배율**: 광폭화 진입 후 `WaterWavePush` 재등장 간격이 약 22.5초로
         줄어들고, 전반적인 `패턴 선택:` 로그 간격도 짧아진다.
   - [ ] **D-06a 거리 분기**: 플레이어가 보스에 붙어 있을 때(`거리: 3.0` 이하) `WaterRangedSpit` 이
         선택되지 않고, 멀리 떨어졌을 때(`거리: 3.0` 초과) `WaterMeleeSwipe`/`WaterJumpLand` 가
         선택되지 않는다.
   - [ ] **D-06b/c 페이즈 전환 반영**: 페이즈 1 에서 `WaterPrisonAttack` 만 등장 → HP 임계로 페이즈 2
         진입 직후부터 `WaterPrisonMapAoe` 로 교체 → 페이즈 3 진입 직후부터 `WaterColorPrison` 로 교체.
         전투를 나갔다 들어오지 않아도 즉시 바뀌어야 한다(Enter 캐싱 없음).
   - [ ] **D-05 사전 가드 회귀 없음**: 광폭화 중 장판이 기존과 동일하게 스폰되고, 파괴 불가 웅덩이
         2개 이상 + 텔레포트 쿨다운 충족 시 `WaterTeleportState` 로 전환된다.
   - [ ] **후보 고갈 스팸 없음**: `사용 가능한 패턴 후보 없음` 로그가 프레임마다 쏟아지지 않는다.
   - [ ] **WaterSpirit 회귀 없음**: `SpiritCombatState` 의 연속 사용 금지가 여전히 **완전배제**로
         동작한다 (같은 패턴명 연속 두 줄이 단 한 번도 나오지 않는다) — Plan 08-01 의 헬퍼 확장이
         기본값으로 기존 동작을 유지하는지 확인하는 회귀 항목.

6. **결과 기록**: 검증 완료 시 관찰된 로그 원문과 항목별 PASS/FAIL 을 기록한다는 안내.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Enemy/WaterMonster/Check.md && [ -f $F ] && grep -qF "WaterMonsterCombatState" $F && grep -qF "45s" $F && grep -qF "22.5" $F && grep -qF "0.5" $F && grep -qF "WaterPrisonAttack" $F && grep -qF "WaterPrisonMapAoe" $F && grep -qF "WaterColorPrison" $F && grep -qF "WaterWavePush" $F && grep -qF "WaterGeyser" $F && grep -qF "WaterMeleeSwipe" $F && grep -qF "WaterJumpLand" $F && grep -qF "WaterRangedSpit" $F && grep -qF "SpiritCombatState" $F && grep -qF "WaterTeleportState" $F && [ "$(grep -c '^- \[ \]' $F)" -ge 8 ] && git diff --exit-code -- Assets/Enemy/WaterSpirit/Check.md Assets/Enemy/Tutorial/TutorialBoss/Check.md && echo TASK2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `Assets/Enemy/WaterMonster/Check.md` 파일이 존재한다
    - `grep -c '^- \[ \]' Assets/Enemy/WaterMonster/Check.md` >= 8 (검증 항목 8개 이상, 미체크 상태)
    - 8종 패턴 클래스명이 전부 등장한다: `WaterGeyser`, `WaterWavePush`, `WaterMeleeSwipe`, `WaterJumpLand`, `WaterRangedSpit`, `WaterPrisonAttack`, `WaterPrisonMapAoe`, `WaterColorPrison`
    - `grep -qF "45s"` 및 `grep -qF "22.5"` 성공 (D-04a / D-04b 수치가 체크리스트에 명시됨)
    - `grep -qF "SpiritCombatState"` 성공 (WaterSpirit 완전배제 회귀 검증 항목 포함)
    - `grep -qF "WaterTeleportState"` 성공 (D-05 사전 가드 회귀 검증 항목 포함)
    - `git diff --exit-code -- Assets/Enemy/WaterSpirit/Check.md Assets/Enemy/Tutorial/TutorialBoss/Check.md` 종료코드 0 (기존 체크리스트 미변경)
  </acceptance_criteria>

  <done>`Assets/Enemy/WaterMonster/Check.md` 가 8종 패턴의 가중치/거리조건/재사용잠금 표와 D-01~D-06 별 Play 모드 검증 항목 8개 이상을 담고 있으며, WaterSpirit 완전배제 회귀 항목을 포함한다.</done>
</task>

</tasks>

<verification>
1. 변경 파일이 정확히 2개다: `git status --porcelain` 에 `WaterMonsterCombatState.cs`(M) 와
   `Assets/Enemy/WaterMonster/Check.md`(??) 만 나타난다 (`.meta` 파일은 Unity 가 생성 — 허용).
2. `CombatState.cs` / `SpiritCombatState.cs` / `Stage2CombatState.cs` / `WaterMonsterController.cs` /
   `WaterTeleportState.cs` 변경 0 라인.
3. 마이그레이션 전후 후보 구성 대조:
   - 이전 pool: Geyser + (WavePush, 45s 게이트) + (dist<=3 ? Swipe+JumpLand : RangedSpit) + 페이즈별 프리즌 1
   - 이후 후보: 동일 8종 선언, 거리/페이즈 조건은 헬퍼 필터가 담당 → 임의 시점의 유효 후보 집합이 동일하다.
4. Unity 에디터 컴파일 에러 0건 (에디터 미가용 시 Plan 08-03 체크포인트로 이월).
</verification>

<success_criteria>
- `WaterMonsterCombatState.SelectAttackStrategy` 에 `Random.Range` / `List<IAttackStrategy>` 가 없다.
- WaterWavePush 후보가 `cooldownOverride: WaveAttackCooldown`(45f)로 선언된다.
- `GetPatternCooldownMultiplier()` override 가 광폭화 시 `_enrageCooldownMultiplier`(0.5f)를 반환한다.
- `SelectWeightedPattern` 호출이 `LastUsedWeightDecay`(0.5f)를 3번째 인자로 전달한다.
- 후보 목록이 `Enter()` 가 아니라 `SelectAttackStrategy` 매 호출마다 재구성된다.
- 장판 스폰/텔레포트 사전 가드가 후보 구성 코드보다 위에 원형 그대로 존재한다.
- `Assets/Enemy/WaterMonster/Check.md` 가 생성되어 Plan 08-03 의 입력이 된다.
</success_criteria>

<output>
완료 후 `.planning/phases/08-watermonster-combatstate/08-02-SUMMARY.md` 를 생성한다.
</output>
