---
phase: 08-watermonster-combatstate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/NewBoss/Script/States/CombatState.cs
autonomous: true
requirements: [D-01d, D-03b, D-04a]
must_haves:
  truths:
    - "PatternCandidate 는 IAttackStrategy.Cooldown 과 무관한 재사용 잠금 시간을 선언할 수 있다"
    - "파생 CombatState 가 _patternReadyAt 에 기록되는 쿨다운에 배율을 곱할 수 있다"
    - "SelectWeightedPattern 이 직전 사용 패턴을 완전배제 또는 가중치 감쇠 중 하나로 처리할 수 있다"
    - "SpiritCombatState.cs 는 단 한 글자도 바뀌지 않고, 기존 완전배제 동작이 그대로 유지된다"
  artifacts:
    - path: "Assets/Enemy/NewBoss/Script/States/CombatState.cs"
      provides: "CooldownOverride 필드 + GetPatternCooldownMultiplier 가상 훅 + lastUsedWeightMultiplier 파라미터"
      contains: "protected virtual float GetPatternCooldownMultiplier() => 1f;"
  key_links:
    - from: "CombatState.SelectWeightedPattern"
      to: "CombatState.CommitSelection"
      via: "eligible[i].CooldownOverride 전달"
      pattern: "CommitSelection\\(eligible\\[.*\\].Factory\\(\\), eligible\\[.*\\].CooldownOverride\\)"
    - from: "CombatState.CommitSelection"
      to: "_patternReadyAt"
      via: "baseCooldown * GetPatternCooldownMultiplier()"
      pattern: "baseCooldown \\* GetPatternCooldownMultiplier\\(\\)"
    - from: "SpiritCombatState.SelectAttackStrategy"
      to: "CombatState.SelectWeightedPattern"
      via: "2-인자 호출이 기본값(완전배제)으로 계속 컴파일됨 — 회귀 없음"
      pattern: "SelectWeightedPattern\\(dist, _candidates\\)"
---

<objective>
Phase 7 에서 만든 `CombatState` 범용 패턴 판단 헬퍼에 Phase 8 이 필요로 하는 세 가지 확장점을
추가한다: (1) 패턴별 재사용 잠금 시간을 `IAttackStrategy.Cooldown` 과 분리하는 쿨다운 오버라이드,
(2) 파생 클래스가 그 잠금 시간에 배율을 곱할 수 있는 가상 훅, (3) 직전 사용 패턴을 "완전배제"
대신 "가중치 감쇠"로 처리하는 선택적 모드.

Purpose: WaterMonster 가 WaterWavePush 의 45초 특수 잠금(D-04)과 광폭화 배율(D-03)을 유지하면서
헬퍼를 재사용할 수 있게 하고, WaterSpirit 의 기존 완전배제 동작(Phase 7 D-05)은 손대지 않고 공존시킨다.
Output: `CombatState.cs` 단일 파일의 순수 확장 (기존 호출부 시그니처 호환).

**중요 — 이 플랜에서 절대 하지 않는 것:**
- `SpiritCombatState.cs` / `Stage2CombatState.cs` / `WaterMonsterCombatState.cs` 수정 (0 라인)
- `Enter` / `Execute` / `ShouldTransitionToGroggy` / 기본 `SelectAttackStrategy` 동작 변경
- 새 파라미터의 기본값 변경 (기본값은 반드시 "기존 동작 그대로"여야 한다)
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-watermonster-combatstate/08-CONTEXT.md
@.planning/phases/08-watermonster-combatstate/08-RESEARCH.md
@Assets/Enemy/NewBoss/Script/States/CombatState.cs
@Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs

<interfaces>
<!-- 실행자가 필요로 하는 현재 계약. 코드베이스 탐색 없이 이 내용만으로 작업 가능해야 한다. -->

Assets/Enemy/NewBoss/Script/States/Attacks/IAttackStrategy.cs:
```csharp
public interface IAttackStrategy
{
    float Cooldown { get; }
    string AnimationName { get; }
    void ExecuteAttack(BossController boss);
}
```

CombatState.cs 현재 상태 (변경 대상, 라인 18~44 / 145~193):
```csharp
protected class PatternCandidate
{
    public readonly System.Func<IAttackStrategy> Factory;
    public readonly System.Type StrategyType;
    public readonly float Weight;
    public readonly float? MinDistance;
    public readonly float? MaxDistance;

    public PatternCandidate(System.Func<IAttackStrategy> factory, float weight,
                            float? minDistance = null, float? maxDistance = null)
    { /* ... StrategyType = factory().GetType(); */ }
}

protected System.Type LastUsedPatternType { get; private set; }
private readonly Dictionary<System.Type, float> _patternReadyAt = new Dictionary<System.Type, float>();

protected IAttackStrategy SelectWeightedPattern(float dist, IReadOnlyList<PatternCandidate> candidates);
protected IAttackStrategy ForceSelectPattern(IAttackStrategy strategy);
private IAttackStrategy CommitSelection(IAttackStrategy strategy);   // _patternReadyAt[t] = Time.time + strategy.Cooldown;
```

유일한 기존 외부 호출부 — 이 두 줄은 변경 없이 계속 컴파일되어야 한다:
```csharp
// SpiritCombatState.cs:39
return ForceSelectPattern(new SpiritWakeRepel());
// SpiritCombatState.cs:42
IAttackStrategy attack = SelectWeightedPattern(dist, _candidates);
// SpiritCombatState.cs:68-71  (PatternCandidate 4개, cooldownOverride 미사용)
new PatternCandidate(() => new SpiritCharge(), ChargeWeight),
new PatternCandidate(() => new SpiritWakeRepel(), WakeRepelWeight, maxDistance: spirit.RepelRange),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PatternCandidate 쿨다운 오버라이드 + 광폭화 배율 가상 훅 추가</name>

  <files>Assets/Enemy/NewBoss/Script/States/CombatState.cs</files>

  <read_first>
    - `Assets/Enemy/NewBoss/Script/States/CombatState.cs` (수정 대상 — 현재 전체 내용 확인 필수)
    - `Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` (유일한 기존 호출부 — 시그니처 호환 확인용)
    - `Assets/Enemy/NewBoss/Script/States/Attacks/IAttackStrategy.cs` (`Cooldown` 계약 확인)
  </read_first>

  <action>
`Assets/Enemy/NewBoss/Script/States/CombatState.cs` 를 아래 4곳만 수정한다. 다른 메서드
(`Enter` / `Execute` / `Exit` / `ShouldTransitionToGroggy` / 기본 `SelectAttackStrategy` /
`ForceSelectPattern`)는 한 줄도 건드리지 않는다.

**(1) `PatternCandidate` 에 `CooldownOverride` 필드 추가** — 기존 필드 선언 블록 뒤(`MaxDistance` 다음)에
아래를 추가하고, 생성자에 5번째 선택 파라미터를 추가한다. 기존 4-인자/2-인자 호출부는 그대로 컴파일되어야 한다:

```csharp
        // Phase 8 (D-04a): _patternReadyAt 에 기록할 재사용 잠금 시간을 IAttackStrategy.Cooldown
        // 과 분리한다. null = strategy.Cooldown 사용(기존 동작).
        public readonly float? CooldownOverride;

        public PatternCandidate(System.Func<IAttackStrategy> factory, float weight,
                                float? minDistance = null, float? maxDistance = null,
                                float? cooldownOverride = null)
        {
            Factory = factory;
            Weight = weight;
            MinDistance = minDistance;
            MaxDistance = maxDistance;
            CooldownOverride = cooldownOverride;
            // D-06a: 게이팅 키를 슬롯 인덱스가 아닌 "전략 타입"으로 1회 확정한다.
            StrategyType = factory().GetType();
        }
```

**(2) 쿨다운 배율 가상 훅 추가** — `_patternReadyAt` 딕셔너리 선언 바로 다음 줄에 아래를 추가한다:

```csharp
    // Phase 8 (D-03a): _patternReadyAt 에 기록할 쿨다운에 곱할 배율.
    // 기본 1f = 배율 없음(기존 동작). 광폭화처럼 쿨다운을 단축하는 보스가 override 한다.
    protected virtual float GetPatternCooldownMultiplier() => 1f;
```

**(3) `CommitSelection` 을 오버라이드 + 배율 지원으로 교체** — 기존 메서드 전체를 아래로 치환한다:

```csharp
    // 선택 확정 처리 — 연속사용금지 기준값과 해당 패턴의 쿨다운 만료 시각을 갱신한다.
    // Phase 8: cooldownOverride 가 있으면 strategy.Cooldown 대신 그 값을 쓰고(D-04a),
    //          GetPatternCooldownMultiplier() 배율을 곱한다(D-03a).
    private IAttackStrategy CommitSelection(IAttackStrategy strategy, float? cooldownOverride = null)
    {
        System.Type t = strategy.GetType();
        LastUsedPatternType = t;
        float baseCooldown = cooldownOverride ?? strategy.Cooldown;
        _patternReadyAt[t] = Time.time + baseCooldown * GetPatternCooldownMultiplier();
        return strategy;
    }
```

**(4) `SelectWeightedPattern` 내부의 `CommitSelection` 호출 2곳에 오버라이드를 전달** — 현재 라인 172 와 176:

```csharp
            if (roll <= cumulative) return CommitSelection(eligible[i].Factory(), eligible[i].CooldownOverride);
```
```csharp
        return CommitSelection(eligible[eligible.Count - 1].Factory(), eligible[eligible.Count - 1].CooldownOverride);
```

`ForceSelectPattern` 의 `CommitSelection(strategy)` 호출은 인자를 추가하지 않고 그대로 둔다
(오버라이드 없음 → `strategy.Cooldown` 사용, 단 배율은 적용됨 — 의도된 동작).

**(5) 스테일 주석 정리 (CLAUDE.md §3)** — 현재 41~43행 주석은 다음 플랜에서 삭제될
`WaterMonsterCombatState._lastWaveTime` 을 지목하고 있다. 필드명 참조를 제거해 아래로 치환한다:

```csharp
    // 패턴별 쿨다운 만료 시각. Execute() 에 early-return 분기가 있어 매 프레임 감산 방식은
    // 조용히 누락될 수 있으므로, Time.time 절대 시각 비교를 사용한다.
```
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Enemy/NewBoss/Script/States/CombatState.cs && grep -qF "public readonly float? CooldownOverride;" $F && grep -qF "float? cooldownOverride = null)" $F && grep -qF "CooldownOverride = cooldownOverride;" $F && grep -qF "protected virtual float GetPatternCooldownMultiplier() => 1f;" $F && grep -qF "private IAttackStrategy CommitSelection(IAttackStrategy strategy, float? cooldownOverride = null)" $F && grep -qF "float baseCooldown = cooldownOverride ?? strategy.Cooldown;" $F && grep -qF "_patternReadyAt[t] = Time.time + baseCooldown * GetPatternCooldownMultiplier();" $F && [ "$(grep -cF 'eligible[i].CooldownOverride' $F)" = "1" ] && [ "$(grep -cF 'eligible[eligible.Count - 1].CooldownOverride' $F)" = "1" ] && [ "$(grep -cF 'Time.time + strategy.Cooldown' $F)" = "0" ] && [ "$(grep -cF '_lastWaveTime' $F)" = "0" ] && grep -qF "return CommitSelection(strategy);" $F && git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs && echo TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "public readonly float? CooldownOverride;" Assets/Enemy/NewBoss/Script/States/CombatState.cs` 성공
    - `grep -qF "float? cooldownOverride = null)" ...CombatState.cs` 성공 (PatternCandidate 생성자 5번째 파라미터)
    - `grep -qF "protected virtual float GetPatternCooldownMultiplier() => 1f;" ...CombatState.cs` 성공
    - `grep -qF "private IAttackStrategy CommitSelection(IAttackStrategy strategy, float? cooldownOverride = null)" ...CombatState.cs` 성공
    - `grep -qF "float baseCooldown = cooldownOverride ?? strategy.Cooldown;" ...CombatState.cs` 성공
    - `grep -qF "_patternReadyAt[t] = Time.time + baseCooldown * GetPatternCooldownMultiplier();" ...CombatState.cs` 성공
    - `grep -cF 'Time.time + strategy.Cooldown' ...CombatState.cs` == 0 (구 무조건 경로 제거됨)
    - `grep -cF 'eligible[i].CooldownOverride' ...CombatState.cs` == 1 이고 `grep -cF 'eligible[eligible.Count - 1].CooldownOverride'` == 1
    - `grep -qF "return CommitSelection(strategy);" ...CombatState.cs` 성공 (ForceSelectPattern 은 1-인자 호출 유지)
    - `grep -cF '_lastWaveTime' ...CombatState.cs` == 0 (스테일 주석 참조 제거됨)
    - `git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` 종료코드 0
  </acceptance_criteria>

  <done>`CombatState.cs` 가 `PatternCandidate.CooldownOverride`, `GetPatternCooldownMultiplier()` 가상 훅, 오버라이드/배율을 반영하는 `CommitSelection` 을 보유하며, `SpiritCombatState.cs` 의 기존 2-인자/4-인자 호출부는 수정 없이 컴파일된다.</done>
</task>

<task type="auto">
  <name>Task 2: SelectWeightedPattern 에 직전 패턴 가중치 감쇠 모드 추가</name>

  <files>Assets/Enemy/NewBoss/Script/States/CombatState.cs</files>

  <read_first>
    - `Assets/Enemy/NewBoss/Script/States/CombatState.cs` (Task 1 이 이미 수정한 상태를 반드시 다시 읽을 것)
    - `Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` (2-인자 호출부 `SelectWeightedPattern(dist, _candidates)` 가 기본값으로 완전배제를 유지해야 함)
  </read_first>

  <action>
`SelectWeightedPattern` 메서드 하나만 아래 전체 내용으로 치환한다. 다른 곳은 손대지 않는다.

핵심 설계 (D-01d — Claude 재량으로 확정): 별도 오버로드가 아니라 **기본값이 있는 3번째 파라미터**
`float lastUsedWeightMultiplier = 0f` 를 추가한다. `0f`(기본) = Phase 7 의 완전배제 그대로,
`0f` 초과 = 배제하지 않고 해당 후보 가중치에만 배율을 곱한다. 기본값이 기존 동작이므로
`SpiritCombatState.cs:42` 의 2-인자 호출은 한 글자도 바뀌지 않는다.

가중치가 후보별로 달라지므로 룰렛에 쓸 실효 가중치를 `List<float> eligibleWeights` 에 병행 저장한다
(기존처럼 `eligible[i].Weight` 를 그대로 쓰면 감쇠가 룰렛에 반영되지 않는 버그가 된다).

```csharp
    // D-01/D-03: 조건(거리 선택적 + 연속금지 + 쿨다운)을 통과한 후보 중 가중치 랜덤으로 1개 선택.
    // 통과 후보가 없으면 null — 기존 계약대로 Execute() 가 다음 프레임에 재시도한다.
    // Phase 8 (D-01d): lastUsedWeightMultiplier 로 "직전 사용 패턴" 처리 방식을 고른다.
    //   0f (기본) = 완전배제 — Phase 7 D-05a, SpiritCombatState 기존 동작 그대로.
    //   0f 초과   = 배제하지 않고 해당 후보의 가중치에만 배율을 곱한다 (WaterMonster D-01c: 0.5f).
    //   감쇠는 "직전 1개 패턴"에만 적용된다 — 누적 감쇠가 아니다 (D-01b).
    protected IAttackStrategy SelectWeightedPattern(float dist, IReadOnlyList<PatternCandidate> candidates,
                                                    float lastUsedWeightMultiplier = 0f)
    {
        if (candidates == null || candidates.Count == 0) return null;

        List<PatternCandidate> eligible = new List<PatternCandidate>();
        List<float> eligibleWeights = new List<float>();
        float totalWeight = 0f;

        for (int i = 0; i < candidates.Count; i++)
        {
            PatternCandidate c = candidates[i];

            if (c.MinDistance.HasValue && dist < c.MinDistance.Value) continue; // 거리 하한 (D-02b)
            if (c.MaxDistance.HasValue && dist > c.MaxDistance.Value) continue; // 거리 상한 (D-02b)
            if (_patternReadyAt.TryGetValue(c.StrategyType, out float readyAt) && Time.time < readyAt) continue; // 쿨다운

            float weight = c.Weight;
            if (LastUsedPatternType == c.StrategyType)
            {
                if (lastUsedWeightMultiplier <= 0f) continue;   // 완전배제 (D-05a, 기본 동작)
                weight *= lastUsedWeightMultiplier;             // 가중치 감쇠 (D-01a~c)
            }
            if (weight <= 0f) continue;

            eligible.Add(c);
            eligibleWeights.Add(weight);
            totalWeight += weight;
        }

        if (eligible.Count == 0 || totalWeight <= 0f) return null;

        float roll = UnityEngine.Random.Range(0f, totalWeight);
        float cumulative = 0f;
        for (int i = 0; i < eligible.Count; i++)
        {
            cumulative += eligibleWeights[i];
            if (roll <= cumulative) return CommitSelection(eligible[i].Factory(), eligible[i].CooldownOverride);
        }

        // 부동소수 경계 보정: 루프에서 못 골랐어도 null 로 새지 않도록 마지막 후보를 선택한다.
        return CommitSelection(eligible[eligible.Count - 1].Factory(), eligible[eligible.Count - 1].CooldownOverride);
    }
```

주의: 거리/쿨다운/연속금지 필터의 판정 순서가 기존(min→max→연속금지→쿨다운)에서
(min→max→쿨다운→연속금지/감쇠)로 바뀌지만, 완전배제 모드에서는 전부 `continue` 이므로
결과는 동일하다. 감쇠 모드에서 실효 가중치를 계산하려면 이 순서가 필요하다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Enemy/NewBoss/Script/States/CombatState.cs && grep -qF "float lastUsedWeightMultiplier = 0f)" $F && grep -qF "if (lastUsedWeightMultiplier <= 0f) continue;" $F && grep -qF "weight *= lastUsedWeightMultiplier;" $F && grep -qF "List<float> eligibleWeights = new List<float>();" $F && grep -qF "eligibleWeights.Add(weight);" $F && grep -qF "cumulative += eligibleWeights[i];" $F && grep -qF "totalWeight += weight;" $F && [ "$(grep -cF 'cumulative += eligible[i].Weight;' $F)" = "0" ] && [ "$(grep -cF 'totalWeight += c.Weight;' $F)" = "0" ] && [ "$(grep -cF 'if (LastUsedPatternType == c.StrategyType) continue;' $F)" = "0" ] && git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs && grep -qF "SelectWeightedPattern(dist, _candidates);" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs && echo TASK2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "float lastUsedWeightMultiplier = 0f)" ...CombatState.cs` 성공 (기본값 0f = 완전배제)
    - `grep -qF "if (lastUsedWeightMultiplier <= 0f) continue;" ...CombatState.cs` 성공
    - `grep -qF "weight *= lastUsedWeightMultiplier;" ...CombatState.cs` 성공
    - `grep -qF "List<float> eligibleWeights = new List<float>();" ...CombatState.cs` 성공
    - `grep -qF "cumulative += eligibleWeights[i];" ...CombatState.cs` 성공 (룰렛이 실효 가중치를 사용)
    - `grep -cF 'cumulative += eligible[i].Weight;' ...CombatState.cs` == 0 그리고 `grep -cF 'totalWeight += c.Weight;'` == 0 (구 원본 가중치 경로 제거됨)
    - `grep -cF 'if (LastUsedPatternType == c.StrategyType) continue;' ...CombatState.cs` == 0 (무조건 완전배제 라인 제거됨)
    - `grep -qF "SelectWeightedPattern(dist, _candidates);" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 성공 (2-인자 호출부 그대로)
    - `git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` 종료코드 0
  </acceptance_criteria>

  <done>`SelectWeightedPattern` 이 `lastUsedWeightMultiplier` 기본값 `0f` 로 기존 완전배제를 유지하면서, `0f` 초과 값이 주어지면 직전 사용 패턴을 배제하지 않고 실효 가중치에만 배율을 적용해 룰렛에 반영한다. `SpiritCombatState.cs` 는 변경 0 라인.</done>
</task>

</tasks>

<verification>
1. `Assets/Enemy/NewBoss/Script/States/CombatState.cs` 외 다른 `.cs` 파일이 변경되지 않았다:
   `git diff --name-only -- 'Assets/**/*.cs'` 결과가 `CombatState.cs` 한 개뿐이다.
2. Unity 에디터에서 컴파일 에러 0건 (실행자가 Unity 를 열 수 없으면 Plan 08-03 체크포인트로 이월).
3. 새로 추가된 모든 파라미터/훅의 기본값이 "기존 동작"이다:
   `cooldownOverride = null`, `lastUsedWeightMultiplier = 0f`, `GetPatternCooldownMultiplier() => 1f`.
</verification>

<success_criteria>
- `CombatState.PatternCandidate` 가 `CooldownOverride` 를 선언하고 5-인자 생성자를 노출한다.
- `CombatState.GetPatternCooldownMultiplier()` 가 `protected virtual` 로 존재하고 기본 `1f` 를 반환한다.
- `CombatState.CommitSelection` 이 `cooldownOverride ?? strategy.Cooldown` 에 배율을 곱해 `_patternReadyAt` 에 기록한다.
- `CombatState.SelectWeightedPattern` 이 3번째 선택 파라미터로 완전배제/감쇠를 전환한다.
- `SpiritCombatState.cs` / `Stage2CombatState.cs` / `WaterMonsterCombatState.cs` 변경 0 라인.
</success_criteria>

<output>
완료 후 `.planning/phases/08-watermonster-combatstate/08-01-SUMMARY.md` 를 생성한다.
</output>
