---
phase: 15-load-timing-and-load-scope
plan: 04
type: execute
wave: 2
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - .planning/phases/15-load-timing-and-load-scope/Check.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10]
must_haves:
  truths:
    - "D-01~D-10 각각이 코드에 반영됐거나(구현) 의도적으로 미구현(no-op)임이 문서로 추적된다"
    - "정적 회귀 검사가 계획 밖 파일 변경 0건을 증명한다"
    - "사용자가 Unity Play 모드에서 사망 자동 로드 / 세이브 없는 사망 / 로드 실패 폴백 / 격파 보스 미등장을 직접 확인할 체크리스트가 존재한다"
  artifacts:
    - path: ".planning/phases/15-load-timing-and-load-scope/Check.md"
      provides: "Phase 15 정적 회귀 결과 + Play 모드 체크리스트 + D-03/D-10 no-op 기록"
      contains: "## D-03 / D-10 (의도적 미구현)"
  key_links:
    - from: ".planning/phases/15-load-timing-and-load-scope/Check.md"
      to: "15-01 / 15-02 / 15-03 의 실제 소스 변경"
      via: "grep 가능한 정적 회귀 항목표"
      pattern: "PASS|FAIL"
---

<objective>
Phase 15 의 세 구현 플랜(15-01/02/03)이 남긴 변경을 정적으로 회귀 검사하고, 사용자가 Unity Play 모드에서 직접 확인할 체크리스트를 문서로 남긴 뒤 실측 검증을 받는다. D-03(FallZone 무수정)과 D-10(MapGimmickState/Items 스텁 유지)이 **의도적 미구현**임을 명시적으로 기록한다.

Purpose: 이 프로젝트에는 자동화된 테스트 하네스가 없다. 회귀 방어선은 (a) grep 으로 확인 가능한 소스 조건과 (b) 사용자가 Play 모드에서 확인하는 체크리스트 두 가지뿐이다.
Output: `.planning/phases/15-load-timing-and-load-scope/Check.md` 신규 작성 + 사용자 실측 검증.
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 Task 1 을 실행하고, Task 2 는 사용자(사람)가 Unity 에디터에서 수행한다.
</execution_context>

<context>
@.planning/phases/15-load-timing-and-load-scope/15-CONTEXT.md
@.planning/phases/15-load-timing-and-load-scope/15-01-PLAN.md
@.planning/phases/15-load-timing-and-load-scope/15-02-PLAN.md
@.planning/phases/15-load-timing-and-load-scope/15-03-PLAN.md
@bug/BUG-002-player-health-bounds-inverted.md
@bug/BUG-003-watermonster-not-wired.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 정적 회귀 검사 실행 + Phase 15 Check.md 작성</name>
  <files>.planning/phases/15-load-timing-and-load-scope/Check.md</files>

  <read_first>
    - `.planning/phases/12-camera-shake-on-hit/Check.md` (이 프로젝트의 phase Check.md 서술 형식 — 요약 / 무엇이 바뀌었나 / 정적 회귀 검사 / 현재 상태)
    - `Assets/Player/Script/PlayerStats.cs` (15-01 결과)
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` (15-02 결과)
    - `Assets/Enemy/WaterSpirit/Script/SpiritStats.cs`, `Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs`, `Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` (15-03 결과)
    - `Assets/Player.prefab` (15-01 Task 3 결과)
  </read_first>

  <action>
(1) 아래 16개 정적 회귀 검사를 **실제로 실행하고 각각의 실제 출력값을 기록**한다. 추정값을 쓰지 말 것. 하나라도 기대값과 다르면 Check.md 에 FAIL 로 적고 그 자리에서 보고한다 (스스로 소스를 고쳐 맞추지 말 것 — 어느 플랜이 어긋났는지가 정보다).

| # | 결정 | 명령 | 기대값 |
|---|------|------|--------|
| 1 | D-01 | `grep -c "public override void Die()" Assets/Player/Script/PlayerStats.cs` | 1 |
| 2 | D-01 | `grep -c "base.Die()" Assets/Player/Script/PlayerStats.cs` | 0 |
| 3 | D-01 | `grep -c "mgr.LoadGame();" Assets/Player/Script/PlayerStats.cs` | 1 |
| 4 | D-02 | `grep -c "SceneManager.LoadScene(SceneManager.GetActiveScene().name);" Assets/Player/Script/PlayerStats.cs` | 1 |
| 5 | D-03 | `git diff --numstat HEAD -- Assets/map/script/FallZone.cs` | 빈 출력 |
| 6 | D-04 | `grep -c "AbortLoadToMainMenu(" Assets/SaveSystem/Script/SaveLoadManager.cs` | 6 (선언 1 + 호출 5) |
| 7 | D-04 | `grep -c 'public const string MainMenuSceneName = "MainMenu";' Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 |
| 8 | D-05 | `grep -c "File.Delete" Assets/SaveSystem/Script/SaveLoadManager.cs` | 0 |
| 9 | D-05 | `grep -c "File.WriteAllText" Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 (`Save()` 안에만) |
| 10 | D-06/07 | `grep -rn "IsBossDefeated" --include=*.cs Assets/ \| wc -l` | 4 (선언 1 + 보스 3) |
| 11 | D-06/07 | `grep -rn "SaveOnBossDefeated" --include=*.cs Assets/ \| wc -l` | 4 (선언 1 + 보스 3, Phase 11 무회귀) |
| 12 | D-07 | `grep -c "WallToUnlock.UnlockWall();" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` | 1 |
| 13 | D-08 | `grep -c "float fixedMax = Mathf.Clamp(savedMaxHealth, 1f, fixedMaxTotal);" Assets/Player/Script/PlayerStats.cs` | 1 |
| 14 | D-09 | `grep -c "^  maxHealth: 100$" Assets/Player.prefab` | 1 |
| 15 | D-09 | `grep -c "^  maxTotalHealth: 200$" Assets/Player.prefab` | 1 |
| 16 | D-10 | `grep -rn "MapGimmickState\|_data.Items" --include=*.cs Assets/ \| wc -l` | Phase 11 시점과 동일 (신규 소비자 0건 — `SaveLoadManager.EnsureCollections()` 안의 스텁 초기화만 존재) |

추가로 인코딩 무결성 검사 1건:
- `grep -rc $'\xef\xbf\xbd' Assets/Player/Script/PlayerStats.cs Assets/SaveSystem/Script/SaveLoadManager.cs Assets/Enemy/WaterSpirit/Script/SpiritStats.cs Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` → 5개 파일 모두 0.

그리고 범위 검사 1건:
- Phase 15 의 커밋들이 바꾼 파일 합집합이 정확히 다음 6개인지 확인: `Assets/Player/Script/PlayerStats.cs`, `Assets/Player.prefab`, `Assets/SaveSystem/Script/SaveLoadManager.cs`, `Assets/Enemy/WaterSpirit/Script/SpiritStats.cs`, `Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs`, `Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs`. (실행 전부터 워킹트리에 있던 무관한 미커밋 변경은 이 집합에 포함되지 않아야 하며, 절대 스테이징하지 않는다.)

(2) `.planning/phases/15-load-timing-and-load-scope/Check.md` 를 **신규 생성**한다. 정확히 다음 섹션 구조를 갖는다 (내용은 실제 검사 결과로 채운다):

```
# Phase 15 — 로드 시점 및 로드 범위 정의 검증 상태

**요약:** (1~3문장. 무엇이 구현됐고 무엇이 아직 실측 미검증인지)

## 무엇이 바뀌었나
(파일 6개 각각 한 줄씩 — 어떤 결정 ID 때문에 무엇이 바뀌었는지)

## 정적 회귀 검사 (YYYY-MM-DD, N/18 PASS)
(위 16개 + 인코딩 1 + 범위 1 = 18항목 표. 컬럼: # / 결정 / 검사 / 기대값 / 실제값 / 판정)

## D-03 / D-10 (의도적 미구현)
- **D-03**: `Assets/map/script/FallZone.cs` 무수정. 낙하는 사망이 아니라 데미지 페널티이며
  `PlayerRespawn.RespawnPosition()` 으로 위치만 되돌린다. 낙하 데미지로 HP 가 0 이 되면 그때
  `PlayerStats.Die()` 의 D-01 경로로 자연히 넘어간다. 검증: 위 표 #5.
- **D-10**: `SaveData.MapGimmickState` 와 `SaveData.Items` 는 스텁 유지. 두 필드 모두 쓰기 훅이
  없어(Phase 11 D-03 / D-03b) 복원할 데이터가 존재하지 않는다. 저장 쪽이 먼저 생겨야 복원을
  논할 수 있다. 검증: 위 표 #16 (신규 소비자 0건).

## Play 모드 체크리스트 (사용자 직접 확인)
(아래 (3) 의 5개 소섹션)

## 현재 상태
(정적 검사 결과 요약 + Play 모드 미검증 항목 명시)
```

(3) "Play 모드 체크리스트" 섹션에는 다음 5개 소섹션을 체크박스 목록으로 넣는다. 각 항목은 **정확한 기대값**을 포함해야 한다("정상 동작함" 같은 주관적 서술 금지):

**1) 사망 자동 로드 (D-01)**
- [ ] `Tutorial Map` 진입 → 체크포인트 활성화(S키)로 세이브 생성 → Console 에 `[SaveLoadManager] Saved to ...` 1건
- [ ] 체크포인트에서 떨어진 위치로 이동한 뒤 적/낙하로 HP 를 0 으로 만든다
- [ ] Player GameObject 가 Hierarchy 에서 사라지지 않는다 (`Destroy` 안 됨)
- [ ] 게임오버 UI 가 뜨지 않는다
- [ ] 저장된 씬이 다시 로드되고 플레이어가 저장한 체크포인트 위치에 서 있다
- [ ] Console 에 `[SaveLoadManager] Restored stats: ` 1건, 체력이 저장 당시 값이다
- [ ] 로드가 **1회만** 발생한다 (`Restored stats:` 가 2건 이상 찍히지 않는다 — `_deathHandled` 가드)

**2) 세이브 없는 사망 → 현재 씬 재시작 (D-02)**
- [ ] `Application.persistentDataPath` 의 `save.json` / `save_1.json` / `save_2.json` 을 모두 다른 폴더로 **이동**(삭제 아님, 복구 가능하게) 한다
- [ ] 새 게임으로 `Tutorial Map` 진입 후 체크포인트를 켜지 않은 채 HP 0 으로 만든다
- [ ] 메인메뉴로 나가지 **않고** `Tutorial Map` 이 처음부터 다시 시작된다
- [ ] 플레이어 체력이 최대치(100)로 리셋돼 있다
- [ ] 검증 후 옮겨둔 세이브 파일을 원위치로 되돌린다

**3) 로드 실패 → 메인메뉴 (D-04, D-05)**
- [ ] `save.json` 을 텍스트 에디터로 열어 **사본을 먼저 백업**한 뒤 내용을 `{` 한 글자로 바꿔 손상시킨다
- [ ] 메인메뉴 → 이어하기 → 슬롯 1 선택
- [ ] Console 에 `[SaveLoadManager] Load failed (save file unreadable or malformed: ...) - returning to MainMenu. Save file left untouched.` 가 찍힌다
- [ ] 화면이 `MainMenu` 씬으로 돌아온다 (멈춰 있지 않다)
- [ ] `save.json` 의 내용이 여전히 `{` 그대로다 — 지워지거나 덮어써지지 않았다 (D-05)
- [ ] 백업본으로 되돌린 뒤, 이번엔 `SceneName` 값을 `"NoSuchScene"` 으로 바꾼다 → 이어하기 → Console 에 `LoadSceneAsync returned null for scene 'NoSuchScene' - not registered in Build Settings?` 후 `MainMenu` 복귀
- [ ] 검증 후 백업본을 원위치로 되돌린다

**4) 격파 보스 미등장 (D-06, D-07)**
- [ ] `Tutorial Map` 에서 TutorialBoss 를 격파한다 → Console 에 `[SaveLoadManager] Saved to ...`
- [ ] 격파 후 체크포인트를 한 번 더 활성화해 씬/스폰포인트를 갱신한다
- [ ] 메인메뉴로 나갔다가 이어하기로 같은 슬롯을 로드한다
- [ ] TutorialBoss GameObject 가 Hierarchy 에서 비활성(SetActive false) 상태다 — 보스가 다시 나타나지 않는다
- [ ] 보스방 벽(`WallToUnlock` 이 가리키는 오브젝트)과 그 `wallTilemap` 이 둘 다 비활성이다 — 통과할 수 있다
- [ ] 클리어 패널(`ClearPanel`)이 다시 뜨지 않고 게임이 Paused 로 전환되지 않는다
- [ ] 보스 체력바가 화면에 나타나지 않는다
- [ ] (참고) WaterSpirit / WaterMonster 는 현재 배치된 씬이 없어 이번 실측 대상이 아니다 — BUG-003 본체는 별도 페이즈

**5) 체력 불변식 보정 (D-08, D-09)**
- [ ] `save.json` 을 백업한 뒤 `"Health": 999, "MaxHealth": 400, "MaxTotalHealth": 200` 으로 손으로 고친다
- [ ] 이어하기로 로드한다
- [ ] Console 에 `[PlayerStats] RestoreStats corrected out-of-range saved values: 999/400 (maxTotal 200) -> 200/200 (maxTotal 200).` 이 찍힌다
- [ ] Inspector 의 PlayerStats 가 `health 200 / maxHealth 200 / maxTotalHealth 200` 이다
- [ ] 새 게임으로 `Tutorial Map` 진입 시 Inspector 가 `maxHealth 100 / maxTotalHealth 200` 이고 하트 UI 가 총 40개 생성·20개 활성 상태다
- [ ] `AddHealth()` 를 호출하면 `maxHealth` 가 101 로 증가한다 (BUG-002 해소 확인)
- [ ] 검증 후 백업본을 원위치로 되돌린다

(4) **소스 코드는 한 줄도 수정하지 않는다.** 이 태스크는 문서 전용이다.
(5) `Assets/SaveSystem/Check.md` 는 **건드리지 않는다** — 실행 시점에 이미 커밋되지 않은 변경이 워킹트리에 존재하므로, 이 파일을 스테이징하면 무관한 변경까지 함께 커밋된다 (Phase 11 Plan 3 사고와 동일 계열).
  </action>

  <verify>
    <automated>test -f .planning/phases/15-load-timing-and-load-scope/Check.md && grep -c "정적 회귀 검사" .planning/phases/15-load-timing-and-load-scope/Check.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/15-load-timing-and-load-scope/Check.md` 파일이 존재한다.
    - `grep -c "^## 무엇이 바뀌었나$" .planning/phases/15-load-timing-and-load-scope/Check.md` 가 `1` 을 출력한다.
    - `grep -c "^## D-03 / D-10 (의도적 미구현)$" .planning/phases/15-load-timing-and-load-scope/Check.md` 가 `1` 을 출력한다.
    - `grep -c "^## Play 모드 체크리스트" .planning/phases/15-load-timing-and-load-scope/Check.md` 가 `1` 을 출력한다.
    - `grep -c "^- \[ \]" .planning/phases/15-load-timing-and-load-scope/Check.md` 가 `30` 이상이다 (5개 소섹션의 체크박스 전부 존재).
    - Check.md 의 정적 회귀 표에 `D-01` `D-02` `D-03` `D-04` `D-05` `D-06` `D-07` `D-08` `D-09` `D-10` 10개 결정 ID 가 전부 등장한다 (`for d in 01 02 03 04 05 06 07 08 09 10; do grep -c "D-$d" .planning/phases/15-load-timing-and-load-scope/Check.md; done` 이 전부 1 이상).
    - Check.md 의 정적 회귀 표 각 행에 "실제값" 컬럼이 채워져 있고 `PASS` 또는 `FAIL` 판정이 적혀 있다 (빈칸/`TBD` 금지).
    - `git status --porcelain Assets/` 에 이 태스크로 인한 신규 변경이 0건이다 (문서 전용 태스크).
    - `git status --porcelain Assets/SaveSystem/Check.md` 의 상태가 이 태스크 실행 전과 동일하다.
    - 커밋 1개. pathspec 은 `.planning/phases/15-load-timing-and-load-scope/Check.md` 만.
  </acceptance_criteria>

  <done>18개 정적 회귀 항목의 실제 측정값과 판정이 기록된 Check.md 가 존재하고, D-03/D-10 이 의도적 미구현으로 명시돼 있으며, 사용자가 따라 할 수 있는 Play 모드 체크리스트 5개 소섹션이 들어 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Unity Play 모드 실측 검증 (사용자)</name>
  <files>.planning/phases/15-load-timing-and-load-scope/Check.md</files>

  <read_first>
    - `.planning/phases/15-load-timing-and-load-scope/Check.md` (Task 1 이 작성한 체크리스트)
  </read_first>

  <what-built>
    - `PlayerStats.Die()` override — 사망 시 마지막 세이브 자동 로드, 세이브 없으면 현재 씬 재시작 (D-01/D-02)
    - `PlayerStats.RestoreStats()` 체력 불변식 강제 보정 (D-08)
    - `Player.prefab` `maxHealth: 100` (D-09)
    - `SaveLoadManager.AbortLoadToMainMenu()` — 로드 실패 5분기 전부 메인메뉴 복귀, 세이브 파일 무보존 삭제/덮어쓰기 없음 (D-04/D-05)
    - 보스 3종의 `IsBossDefeated()` 자가 제거 가드 + TutorialBoss 보스방 벽 해제 (D-06/D-07)
  </what-built>

  <action>
사용자가 Unity 에디터에서 아래 how-to-verify 절차를 수행한다. 실행 에이전트는 소스 코드를 수정하지 않고 대기한다.
사용자가 결과를 회신하면 다음을 수행한다:
1. 통과한 항목의 체크박스를 `.planning/phases/15-load-timing-and-load-scope/Check.md` 의 "Play 모드 체크리스트" 섹션에서 `- [ ]` -> `- [x]` 로 바꾼다.
2. 실패 항목은 미체크로 두되 바로 아래 줄에 `  - FAIL: <사용자가 보고한 Console 로그 원문>` 을 추가한다.
3. "## 현재 상태" 섹션을 실측 결과 요약으로 갱신한다 (통과 N건 / 실패 M건 / 미검증 K건을 숫자로 명시).
4. `docs(15): record phase 15 play mode verification` 으로 커밋한다. pathspec 은 `.planning/phases/15-load-timing-and-load-scope/Check.md` 하나만.
  </action>

  <how-to-verify>
1. Unity 를 열고 Console 에 컴파일 에러가 0건인지 확인한다.
2. `.planning/phases/15-load-timing-and-load-scope/Check.md` 의 "Play 모드 체크리스트" 5개 소섹션을 순서대로 수행하며 체크박스를 채운다.
3. **검증 전에 `Application.persistentDataPath` 의 세이브 파일 3개를 반드시 다른 폴더로 백업한다** (체크리스트 2/3/5 가 세이브 파일을 손상시키거나 옮긴다). Phase 11/14 검증 때도 같은 절차를 썼다.
4. 실패한 항목이 있으면 그 항목 번호와 Console 로그 전문을 그대로 보고한다.
5. WaterSpirit / WaterMonster 보스 항목은 배치된 씬이 없어 이번 실측 대상이 아니다 — 건너뛴다.
  </how-to-verify>

  <acceptance_criteria>
    - Unity Console 컴파일 에러 0건.
    - Check.md 의 Play 모드 체크박스가 (통과) 채워지거나 (실패) 실패 사유와 함께 미체크로 남는다.
    - 체크리스트 1) 의 "로드가 1회만 발생" 항목이 명시적으로 확인된다.
    - 체크리스트 3) 의 "세이브 파일 내용이 그대로다" 항목이 명시적으로 확인된다 (D-05 는 이 항목으로만 증명된다).
    - 백업해둔 세이브 파일 3개가 검증 후 원위치로 복구된다.
  </acceptance_criteria>

  <verify>
    <automated>grep -c "^- \[x\]" .planning/phases/15-load-timing-and-load-scope/Check.md</automated>
  </verify>

  <resume-signal>모두 통과했으면 "approved", 실패 항목이 있으면 항목 번호 + Console 로그를 보고해 주세요.</resume-signal>

  <done>Play 모드 체크리스트 결과가 Check.md 에 기록되고, 실패 항목은 버그 문서 또는 후속 gap-closure 플랜으로 이관된다.</done>
</task>

</tasks>

<verification>
1. `.planning/phases/15-load-timing-and-load-scope/Check.md` 존재 및 필수 섹션 4개 존재.
2. 정적 회귀 18항목 전부 실제값 기록됨.
3. `git status --porcelain Assets/` 에 이 플랜으로 인한 변경 0건.
4. 사용자 실측 결과가 Check.md 에 반영됨.
</verification>

<success_criteria>
- D-01~D-10 전부가 "구현됨" 또는 "의도적 미구현"으로 추적 가능하다.
- 정적 회귀 검사에 FAIL 이 없거나, FAIL 이 있다면 그 원인 플랜이 특정돼 있다.
- 사용자가 Play 모드 체크리스트를 수행하고 결과를 확정했다.
</success_criteria>

<output>
완료 후 `.planning/phases/15-load-timing-and-load-scope/15-04-SUMMARY.md` 를 작성하고, 통과 시 `bug/BUG-002-player-health-bounds-inverted.md` 의 "완료 조건" 체크박스와 "해결 기록"(검증 일자·수정 커밋)을 갱신할지 사용자에게 확인한다.
</output>
