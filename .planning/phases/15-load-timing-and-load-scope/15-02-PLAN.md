---
phase: 15-load-timing-and-load-scope
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/SaveSystem/Script/SaveLoadManager.cs
autonomous: true
requirements: [D-04, D-05]
must_haves:
  truths:
    - "세이브 파일이 손상돼 읽거나 역직렬화할 수 없으면 메인메뉴로 돌아간다"
    - "저장된 SceneName 이 비었거나 Build Settings 에서 해석되지 않으면 메인메뉴로 돌아간다"
    - "씬 로드 후 PlayerStats.Instance 가 null 이면 메인메뉴로 돌아간다"
    - "로드가 실패해도 세이브 파일은 삭제되거나 덮어써지지 않는다"
    - "메인메뉴 씬 이름은 SaveLoadManager 안의 단일 상수로만 존재한다"
  artifacts:
    - path: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      provides: "MainMenuSceneName 상수 + AbortLoadToMainMenu(reason) 실패 폴백 + 5개 실패 분기 배선"
      contains: "private void AbortLoadToMainMenu(string reason)"
  key_links:
    - from: "SaveLoadManager.LoadGame()"
      to: "AbortLoadToMainMenu()"
      via: "동기 3개 실패 분기 (읽기 예외 / null 역직렬화 / 빈 SceneName)"
      pattern: "AbortLoadToMainMenu\\("
    - from: "SaveLoadManager.LoadSceneAndRestoreRoutine() / ApplyPlayerStatsFromSave()"
      to: "AbortLoadToMainMenu()"
      via: "코루틴 2개 실패 분기 (LoadSceneAsync null / PlayerStats null)"
      pattern: "AbortLoadToMainMenu\\("
    - from: "AbortLoadToMainMenu()"
      to: "Assets/Scenes/MainMenu.unity"
      via: "SceneManager.LoadScene(MainMenuSceneName)"
      pattern: "SceneManager\\.LoadScene\\(MainMenuSceneName\\)"
---

<objective>
Phase 15 결정 D-04 / D-05 를 `SaveLoadManager` 에 구현한다.

- D-04: 로드가 어느 단계에서든 실패하면 메인메뉴로 돌려보낸다. 지금처럼 `Debug.LogError` 후 조용히 `return` 해서 사용자가 아무 반응도 못 받는 상태로 남기지 않는다.
- D-05: 로드 실패 시 세이브 파일을 지우거나 덮어쓰지 않는다. 수동 복구 여지를 남긴다.

Purpose: 현재 `LoadGame()` 과 `LoadSceneAndRestoreRoutine()` / `ApplyPlayerStatsFromSave()` 에는 로그만 찍고 조용히 빠져나가는 실패 분기가 5개 있다. 이어하기 버튼을 눌렀는데 화면이 그대로거나, 씬은 바뀌었는데 체력이 복원되지 않은 채 방치되는 상태가 된다.
Output: `SaveLoadManager.cs` 수정 (상수 1개 + 메서드 1개 신규 + 실패 분기 5곳 배선).
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 실행한다. 아래 내용만으로 완결되어야 하며, CONTEXT.md 를 다시 읽지 않아도 실행 가능해야 한다.
</execution_context>

<context>
@.planning/phases/15-load-timing-and-load-scope/15-CONTEXT.md
@Assets/SaveSystem/Script/SaveLoadManager.cs
@Assets/Script/MainMenuUI.cs
</context>

<interfaces>
<!-- 실행자가 필요로 하는 계약. 코드베이스 탐색 없이 이 블록만 보고 작업할 것. -->

메인메뉴 씬 이름은 확정값 `"MainMenu"` 다. 근거: `ProjectSettings/EditorBuildSettings.asset` 의 인덱스 0 항목이 `Assets/Scenes/MainMenu.unity` 다. Build Settings 에 등록된 씬은 `MainMenu`, `1 stage`, `Tutorial Map` 세 개뿐이다.

`SaveLoadManager.cs` 는 이미 `using UnityEngine.SceneManagement;` 를 포함한다 (파일 상단). using 추가 불필요.

이 플랜에서 **수정하지 않는** 주변 메서드:
```csharp
public bool HasSaveFile()               // 파일 존재 여부
public bool HasSaveFile(int slot)
public SaveData PeekSlotData(int slot)  // 자체 try/catch 로 null 반환 - 여기는 손대지 않는다
public void Save()                      // File.WriteAllText - 실패 경로에서 절대 호출 금지 (D-05)
public void NewGame()
private void EnsureCollections()
```
</interfaces>

<encoding_note>
`Assets/SaveSystem/Script/SaveLoadManager.cs` 는 **UTF-8(BOM 없음) + CRLF, 전체 ASCII** 파일이다. 새로 추가하는 주석·문자열도 전부 ASCII 로만 작성해 파일이 계속 순수 ASCII 로 남게 한다. 비-ASCII 문자를 넣지 말 것.
</encoding_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: MainMenuSceneName 상수 + AbortLoadToMainMenu() 폴백 신설, LoadGame() 의 동기 실패 3분기 배선 (D-04, D-05)</name>
  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>

  <read_first>
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` 전체 (특히 `public const string SaveFileName = "save.json";` 선언 위치와 `LoadGame()` 302~337행)
    - `Assets/Script/MainMenuUI.cs` (메인메뉴 씬이 `SaveLoadManager.Instance` 를 어떻게 소비하는지 — 복귀 후 Load 버튼 상태가 `HasSaveFile(slot)` 로 다시 계산된다는 점 확인)
  </read_first>

  <action>
(1) `public const string SaveFileName = "save.json";` 선언 **바로 아래**에 상수를 추가한다. 값은 정확히 `"MainMenu"` 다:

```csharp

    // Phase 15 (D-04): the single place that names the main menu scene. It is index 0 in
    // EditorBuildSettings (Assets/Scenes/MainMenu.unity) and is the only screen that can
    // recover from a broken save, so every load failure funnels back here.
    public const string MainMenuSceneName = "MainMenu";
```

(2) `LoadGame()` 메서드 **바로 아래**(= `EnsureCollections()` 선언 바로 위)에 실패 폴백 메서드를 신설한다:

```csharp
    // Phase 15 (D-04): every load failure ends here instead of a silent return, so the player
    // always gets a visible outcome instead of a frozen menu or a half-restored scene.
    // D-05: this performs NO file write and NO delete - the possibly corrupt save file is left
    // exactly as it is so it can still be recovered by hand.
    private void AbortLoadToMainMenu(string reason)
    {
        Debug.LogError("[SaveLoadManager] Load failed (" + reason + ") - returning to " +
                       MainMenuSceneName + ". Save file left untouched.");
        SceneManager.LoadScene(MainMenuSceneName);
    }
```

(3) `LoadGame()` 안의 **동기 실패 분기 3곳**을 배선한다. 각 분기는 기존 `Debug.LogError(...)` 한 줄을 `AbortLoadToMainMenu(...)` 호출로 **교체**한다 (`return;` 은 그대로 유지).

3-a. `catch (System.Exception e)` 블록:
```csharp
        catch (System.Exception e)
        {
            AbortLoadToMainMenu("save file unreadable or malformed: " + e.Message);
            return;
        }
```

3-b. `if (loaded == null)` 블록:
```csharp
        if (loaded == null)
        {
            AbortLoadToMainMenu("save file deserialized to null");
            return;
        }
```

3-c. `if (string.IsNullOrEmpty(_data.SceneName))` 블록:
```csharp
        if (string.IsNullOrEmpty(_data.SceneName))
        {
            AbortLoadToMainMenu("saved SceneName is empty");
            return;
        }
```

(4) **`if (!HasSaveFile())` 가드는 손대지 않는다.** 지금의 `Debug.LogWarning(...) + return` 을 그대로 둔다. 이것은 로드 실패가 아니라 "로드할 게 없음" 이라는 선행 조건이고, 그 경우의 사용자 흐름은 D-02(현재 씬 재시작, 15-01 플랜의 `PlayerStats.Die()`)가 이미 소유한다. 여기서 메인메뉴로 보내면 D-02 와 충돌한다.

(5) D-05 준수: 이 태스크에서 추가하는 코드에는 `File.Delete`, `File.WriteAllText`, `Save()` 호출이 하나도 없어야 한다. 기존 `_data = loaded;` 대입 라인도 그대로 둔다 (파일에 쓰지 않으므로 D-05 위반이 아니다).
  </action>

  <verify>
    <automated>dotnet build "Projeect_A.E.sln" --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'public const string MainMenuSceneName = "MainMenu";' Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c "private void AbortLoadToMainMenu(string reason)" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c "SceneManager.LoadScene(MainMenuSceneName);" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (씬 이름 문자열이 상수 밖에 중복 등장하지 않는다).
    - `grep -c 'AbortLoadToMainMenu("save file unreadable or malformed: " + e.Message);' Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c 'AbortLoadToMainMenu("save file deserialized to null");' Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c 'AbortLoadToMainMenu("saved SceneName is empty");' Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c "LoadGame aborted - no save file at" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (HasSaveFile 가드 무변경).
    - `grep -c "Failed to read save file" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (구 로그 제거됨).
    - `grep -c "Save file deserialized to null - aborting load" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다.
    - `grep -c "Saved SceneName is empty - aborting load" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다.
    - `grep -c "File.Delete" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (D-05).
    - `grep -c "File.WriteAllText" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 — 유일한 발생 위치는 `Save()` 안이며, `AbortLoadToMainMenu` 본문 안에는 없다.
    - `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 가 에러 0 으로 끝난다.
    - 커밋 1개. pathspec 은 `Assets/SaveSystem/Script/SaveLoadManager.cs` 만.
  </acceptance_criteria>

  <done>`LoadGame()` 의 동기 실패 3분기가 모두 메인메뉴 복귀로 끝나고, 세이브 파일 쓰기/삭제는 발생하지 않는다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 코루틴 실패 2분기 배선 — LoadSceneAsync null / 씬 로드 후 PlayerStats.Instance null (D-04)</name>
  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>

  <read_first>
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` (Task 1 수정 직후 상태. 특히 `LoadSceneAndRestoreRoutine()` 351~373행, `ApplyPlayerStatsFromSave()` 375~390행)
  </read_first>

  <action>
Task 1 이 만든 `AbortLoadToMainMenu(string reason)` 를 코루틴 쪽 실패 분기 2곳에도 배선한다.

(1) `LoadSceneAndRestoreRoutine()` 안의 `if (op == null)` 블록을 다음으로 교체한다. `yield break;` 는 유지한다 (코루틴이므로 `return;` 을 쓸 수 없다):

```csharp
        AsyncOperation op = SceneManager.LoadSceneAsync(sceneName);
        if (op == null)
        {
            AbortLoadToMainMenu("LoadSceneAsync returned null for scene '" + sceneName +
                                "' - not registered in Build Settings?");
            yield break;
        }
```

(2) `ApplyPlayerStatsFromSave()` 안의 `if (ps == null)` 블록을 다음으로 교체한다:

```csharp
        PlayerStats ps = PlayerStats.Instance;
        if (ps == null)
        {
            AbortLoadToMainMenu("PlayerStats.Instance is null after scene load");
            return;
        }
```

(3) `ApplyPlayerStatsFromSave()` 의 성공 경로 마지막에 있는 `Debug.Log("[SaveLoadManager] Restored stats: ...")` 는 **그대로 둔다**. 이 태스크에서 로그 정리를 하지 않는다.

(4) `CapturePlayerStats()` 안의 `Debug.LogWarning("[SaveLoadManager] PlayerStats.Instance is null - player stats not captured.")` 는 **그대로 둔다**. 그것은 저장(Capture) 경로이지 로드 실패가 아니다 — 저장 중에 메인메뉴로 튕기면 안 된다.

(5) `LoadSceneAndRestoreRoutine()` 의 `PlayerSpawner.targetSpawnPointName = spawnPointName;` 라인과 `yield return op;` 라인은 수정하지 않는다 (Phase 11 D-05 가 잠근 좌표 복원 경로).
  </action>

  <verify>
    <automated>dotnet build "Projeect_A.E.sln" --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "AbortLoadToMainMenu(\"LoadSceneAsync returned null for scene '\" + sceneName +" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c 'AbortLoadToMainMenu("PlayerStats.Instance is null after scene load");' Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다.
    - `grep -c "AbortLoadToMainMenu(" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `6` 을 출력한다 (선언 1 + 호출 5).
    - `grep -c "yield break;" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (구조 유지).
    - `grep -c "stats NOT restored" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (구 로그 제거됨).
    - `grep -c "Is it registered in Build Settings?" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (구 로그 제거됨).
    - `grep -c "player stats not captured" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (저장 경로 무변경).
    - `grep -c "PlayerSpawner.targetSpawnPointName = spawnPointName;" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (Phase 11 D-05 무회귀).
    - `grep -c "Restored stats: " Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (성공 경로 로그 무변경).
    - `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 가 에러 0 으로 끝난다.
    - 커밋 1개. pathspec 은 `Assets/SaveSystem/Script/SaveLoadManager.cs` 만.
  </acceptance_criteria>

  <done>씬 이름 해석 실패와 씬 로드 후 `PlayerStats.Instance` null 두 경우 모두 메인메뉴로 복귀하며, 조용히 `return` 하는 로드 실패 경로가 남아 있지 않다.</done>
</task>

</tasks>

<verification>
1. `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 에러 0.
2. `grep -n "return;\|yield break;" Assets/SaveSystem/Script/SaveLoadManager.cs` 로 확인했을 때, `LoadGame()` / `LoadSceneAndRestoreRoutine()` / `ApplyPlayerStatsFromSave()` 안의 모든 조기 종료가 (a) `HasSaveFile()` 가드이거나 (b) `AbortLoadToMainMenu(...)` 직후다.
3. `git diff --name-only` 로 확인했을 때 이 플랜이 바꾼 파일은 `Assets/SaveSystem/Script/SaveLoadManager.cs` 하나뿐이다.
4. `grep -c "MainMenu" Assets/SaveSystem/Script/SaveLoadManager.cs` 결과에서 `"MainMenu"` 문자열 리터럴은 상수 선언 1곳에만 존재한다.
</verification>

<success_criteria>
- `MainMenuSceneName` 상수가 `SaveLoadManager` 에 단 하나 존재한다.
- 로드 실패 5분기 전부가 `AbortLoadToMainMenu()` 를 거친다.
- `HasSaveFile()` 가드는 기존 동작(경고 후 return)을 유지한다.
- 실패 경로에서 파일 쓰기/삭제가 0건이다.
- 컴파일 에러 0, 계획 밖 파일 변경 0.
</success_criteria>

<output>
완료 후 `.planning/phases/15-load-timing-and-load-scope/15-02-SUMMARY.md` 를 작성한다.
</output>
