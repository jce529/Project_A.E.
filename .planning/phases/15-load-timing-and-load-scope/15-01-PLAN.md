---
phase: 15-load-timing-and-load-scope
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Player/Script/PlayerStats.cs
  - Assets/Player.prefab
autonomous: true
requirements: [D-01, D-02, D-08, D-09]
must_haves:
  truths:
    - "플레이어 HP가 0이 되어도 플레이어 GameObject가 Destroy되지 않는다"
    - "세이브 파일이 있는 상태에서 죽으면 마지막 세이브가 자동으로 로드된다 (게임오버 화면 없음)"
    - "세이브 파일이 없는 상태에서 죽으면 현재 씬이 처음부터 다시 시작된다"
    - "역전된 체력값이 든 세이브를 로드해도 0 < health <= maxHealth <= maxTotalHealth 가 성립한다"
    - "Player 프리팹의 기본 시작 최대 체력은 100, 성장 상한은 200이다"
  artifacts:
    - path: "Assets/Player/Script/PlayerStats.cs"
      provides: "Die() override (사망 시 자동 로드) + RestoreStats() 불변식 보정"
      contains: "public override void Die()"
    - path: "Assets/Player.prefab"
      provides: "maxHealth 100 / maxTotalHealth 200 직렬화 기본값"
      contains: "maxHealth: 100"
  key_links:
    - from: "Assets/Player/Script/PlayerStats.cs"
      to: "SaveLoadManager.LoadGame()"
      via: "Die() 안에서 HasSaveFile() 확인 후 호출"
      pattern: "mgr\\.LoadGame\\(\\)"
    - from: "Assets/Player/Script/PlayerStats.cs"
      to: "SceneManager.LoadScene(현재 씬)"
      via: "세이브 부재 시 D-02 재시작 경로"
      pattern: "SceneManager\\.LoadScene\\(SceneManager\\.GetActiveScene\\(\\)\\.name\\)"
---

<objective>
Phase 15 결정 D-01 / D-02 / D-08 / D-09 를 `PlayerStats` 와 `Player.prefab` 에 구현한다.

- D-01: 플레이어 HP가 0이 되면 즉시 마지막 세이브를 자동 로드한다. 게임오버 화면·새 GameState 없음.
- D-02: 세이브 파일이 없는 상태에서 죽으면 메인메뉴가 아니라 **현재 씬을 처음부터 재시작**한다.
- D-08: `RestoreStats()` 가 저장값을 무조건 신뢰하지 않고 `0 < health <= maxHealth <= maxTotalHealth` 를 강제 보정한다.
- D-09: `Player.prefab` 의 시작 `maxHealth` 를 100 으로 확정한다 (`maxTotalHealth` 는 이미 200).

Purpose: 현재 상태에서는 플레이어가 죽으면 `HP.Die()` 가 `Destroy(gameObject)` 를 실행해 플레이어가 씬에서 사라지고 아무 복구 경로도 없다. 또 `Player.prefab` 은 `maxHealth(400) > maxTotalHealth(200)` 이라 `AddHealth()` 성장이 애초에 동작하지 않는다 (BUG-002).
Output: `PlayerStats.cs` 수정(메서드 2개 + using 1줄 + 필드 1개), `Player.prefab` 1줄 수정.
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 실행한다. 아래 내용만으로 완결되어야 하며, CONTEXT.md 를 다시 읽지 않아도 실행 가능해야 한다.
</execution_context>

<context>
@.planning/phases/15-load-timing-and-load-scope/15-CONTEXT.md
@Assets/Player/Script/PlayerStats.cs
@Assets/Script/HP.cs
@Assets/SaveSystem/Script/SaveLoadManager.cs
@Assets/Player.prefab
</context>

<interfaces>
<!-- 실행자가 필요로 하는 계약. 코드베이스 탐색 없이 이 블록만 보고 작업할 것. -->

Assets/Script/HP.cs (PlayerStats 의 부모 클래스, **CP949 인코딩 — 절대 수정 금지**):
```csharp
public class HP : MonoBehaviour
{
    [SerializeField] protected float health;
    [SerializeField] protected float maxHealth;
    public event Action OnDeath;          // 현재 플레이어 쪽 구독자 0건
    public bool ManualDeath = false;
    public float Health { get { return health; } }
    public float MaxHealth { get { return maxHealth; } }
    protected virtual void Awake()        // health = maxHealth 강제
    public virtual void TakeDamage(float dmg)   // health <= 0 이면 Die() 호출
    public virtual void Die()             // OnDeath?.Invoke(); ManualDeath 면 return; 아니면 Destroy(gameObject)
    protected void ClampHealth()          // health = Mathf.Clamp(health, 0, maxHealth) + onHealthChangedCallback
    public virtual void Heal(float amount)
}
```

Assets/SaveSystem/Script/SaveLoadManager.cs (이미 존재, 이 플랜에서 수정하지 않음):
```csharp
public static SaveLoadManager Instance { get; private set; }   // RuntimeInitializeOnLoadMethod(BeforeSceneLoad) 로 부트스트랩됨
public bool HasSaveFile()                                       // 현재 슬롯 파일 존재 여부
public void LoadGame()                                          // 씬 비동기 로드 + 스폰 복원 + 스탯 복원
public int CurrentSlot { get; private set; }
```

Assets/Player.prefab 의 PlayerStats 컴포넌트 블록 (script guid `ec0b64c0b1dccc64d91eee52d85267bd`, 현재 392~397행):
```yaml
  health: 50
  maxHealth: 400
  hitColor: {r: 1, g: 0, b: 0, a: 1}
  flashDuration: 0.1
  ManualDeath: 0
  maxTotalHealth: 200
```
</interfaces>

<encoding_note>
- `Assets/Player/Script/PlayerStats.cs` 는 **UTF-8 with BOM (`EF BB BF`) + CRLF** 이고 한국어 주석을 포함한다. 편집 후에도 BOM·CRLF·기존 한국어 주석 바이트가 그대로 유지되어야 한다. 이 저장소는 Phase 9/10/11 에서 인코딩 왕복으로 비-ASCII 바이트가 U+FFFD 로 훼손된 사고 이력이 있다.
- **이번 플랜에서 새로 추가하는 주석/문자열은 전부 ASCII 로만 작성한다.**
- `Assets/Script/HP.cs` 는 CP949 인코딩이다. **이 플랜에서 절대 수정하지 않는다** (읽기만 허용).
- `Assets/Player.prefab` 은 순수 ASCII YAML 이다. 지정된 1줄 외에는 어떤 줄도 바뀌면 안 된다.
</encoding_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: RestoreStats() 에 체력 불변식 강제 보정 추가 (D-08)</name>
  <files>Assets/Player/Script/PlayerStats.cs</files>

  <read_first>
    - `Assets/Player/Script/PlayerStats.cs` (전체 — 현재 `RestoreStats` 는 65~76행)
    - `Assets/Script/HP.cs` (읽기 전용 — `health`/`maxHealth` 가 protected 이고 `ClampHealth()` 가 `0..maxHealth` 로 자른다는 사실 확인용)
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` 의 `ApplyPlayerStatsFromSave()` (유일한 호출자, 시그니처 변경 금지 확인용)
  </read_first>

  <action>
`Assets/Player/Script/PlayerStats.cs` 의 기존 `RestoreStats(float, float, float)` 메서드 **본문만** 교체한다. 메서드 시그니처(`public void RestoreStats(float savedHealth, float savedMaxHealth, float savedMaxTotalHealth)`)는 한 글자도 바꾸지 않는다 — `SaveLoadManager.ApplyPlayerStatsFromSave()` 가 3인자로 호출하고 있다.

보정 규칙(확정): **성장 상한 `maxTotalHealth` 가 기준점(anchor)** 이다. `maxHealth` 를 상한 쪽으로 끌어내리고, `health` 를 `maxHealth` 쪽으로 끌어내린다. 하한은 세 값 모두 1 이다(`0 < health` 를 만족시키기 위해).
- `fixedMaxTotal = Mathf.Max(1f, savedMaxTotalHealth)`
- `fixedMax     = Mathf.Clamp(savedMaxHealth, 1f, fixedMaxTotal)`
- `fixedHealth  = Mathf.Clamp(savedHealth, 1f, fixedMax)`

보정이 실제로 발생한 경우에만 `Debug.LogWarning` 을 남긴다 (2026-08-20 프로젝트 정책: 추적용 `Debug.Log` 는 금지, 진단용 `LogWarning`/`LogError` 는 유지).

기존 주석 블록(65~69행의 "Phase 11 (D-03c)..." 로 시작하는 4줄)은 삭제하지 말고 그대로 두고, 그 아래에 Phase 15 주석을 덧붙인다.

교체 후 메서드 전체는 정확히 다음과 같아야 한다:

```csharp
    // Phase 11 (D-03c): restore saved stats from SaveLoadManager.
    // Additive only - HP.health / HP.maxHealth are protected and PlayerStats.maxTotalHealth
    // is private with a getter-only property, so an external caller has no other way in.
    // Assignment order matters: maxHealth must be set BEFORE health, because ClampHealth()
    // clamps health into 0..maxHealth.
    //
    // Phase 15 (D-08): saved values are never trusted as-is. The growth ceiling
    // maxTotalHealth is the anchor: maxHealth is pulled down to it and health is pulled down
    // to maxHealth, with a floor of 1 on all three, so 0 < health <= maxHealth <=
    // maxTotalHealth always holds after a load. This also normalises BUG-002 style inverted
    // saves - a save holding maxHealth 400 with maxTotalHealth 200 loads back as 200 / 200.
    public void RestoreStats(float savedHealth, float savedMaxHealth, float savedMaxTotalHealth)
    {
        float fixedMaxTotal = Mathf.Max(1f, savedMaxTotalHealth);
        float fixedMax = Mathf.Clamp(savedMaxHealth, 1f, fixedMaxTotal);
        float fixedHealth = Mathf.Clamp(savedHealth, 1f, fixedMax);

        if (fixedMaxTotal != savedMaxTotalHealth || fixedMax != savedMaxHealth || fixedHealth != savedHealth)
        {
            Debug.LogWarning("[PlayerStats] RestoreStats corrected out-of-range saved values: " +
                             savedHealth + "/" + savedMaxHealth + " (maxTotal " + savedMaxTotalHealth + ")" +
                             " -> " + fixedHealth + "/" + fixedMax + " (maxTotal " + fixedMaxTotal + ").");
        }

        maxTotalHealth = fixedMaxTotal;
        maxHealth = fixedMax;
        health = fixedHealth;
        ClampHealth();
    }
```

대입 순서(`maxTotalHealth` -> `maxHealth` -> `health` -> `ClampHealth()`)는 Phase 11 D-03c 가 잠근 순서다. 바꾸지 말 것.
`Assets/Script/HP.cs` 는 수정하지 않는다. `PlayerStats.AddHealth()` / `Heal()` / `TakeDamage()` 도 이 태스크에서 수정하지 않는다.
  </action>

  <verify>
    <automated>dotnet build "Projeect_A.E.sln" --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public void RestoreStats(float savedHealth, float savedMaxHealth, float savedMaxTotalHealth)" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다 (시그니처 무변경).
    - `grep -c "float fixedMaxTotal = Mathf.Max(1f, savedMaxTotalHealth);" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "float fixedMax = Mathf.Clamp(savedMaxHealth, 1f, fixedMaxTotal);" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "float fixedHealth = Mathf.Clamp(savedHealth, 1f, fixedMax);" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "RestoreStats corrected out-of-range saved values" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -A 3 "maxTotalHealth = fixedMaxTotal;" Assets/Player/Script/PlayerStats.cs` 의 출력이 `maxTotalHealth = fixedMaxTotal;` -> `maxHealth = fixedMax;` -> `health = fixedHealth;` -> `ClampHealth();` 순서다.
    - `git status --porcelain Assets/Script/HP.cs` 가 빈 출력이다 (HP.cs 무수정).
    - `head -c 3 Assets/Player/Script/PlayerStats.cs | xxd -p` 가 `efbbbf` 를 출력한다 (BOM 보존).
    - `grep -c $'\xef\xbf\xbd' Assets/Player/Script/PlayerStats.cs` 가 `0` 을 출력한다 (U+FFFD 훼손 없음).
    - `file Assets/Player/Script/PlayerStats.cs` 출력에 `CRLF` 가 포함된다 (줄끝 보존).
    - `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 가 에러 0 으로 끝난다.
    - 커밋 1개. pathspec 은 `Assets/Player/Script/PlayerStats.cs` 만.
  </acceptance_criteria>

  <done>`RestoreStats()` 가 저장값 3개를 `0 < health <= maxHealth <= maxTotalHealth` 로 보정한 뒤에만 필드에 대입하고, 보정이 일어난 경우 경고 로그를 남긴다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: PlayerStats 에 Die() override 추가 — 사망 시 자동 로드 / 세이브 없으면 씬 재시작 (D-01, D-02)</name>
  <files>Assets/Player/Script/PlayerStats.cs</files>

  <read_first>
    - `Assets/Player/Script/PlayerStats.cs` (Task 1 수정 직후 상태)
    - `Assets/Script/HP.cs` (읽기 전용 — `public virtual void Die()` 가 `OnDeath?.Invoke()` 후 `ManualDeath` 가 false 면 `Destroy(gameObject)` 를 실행한다는 사실 확인)
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` (`HasSaveFile()`, `LoadGame()` 시그니처 확인)
    - `Assets/Script/MainMenuUI.cs` (동기 `SceneManager.LoadScene(string)` 이 이 프로젝트의 씬 전환 관행임을 확인)
    - `Assets/map/script/FallZone.cs` (읽기 전용 — D-03 에 따라 **무수정**임을 확인만 할 것)
  </read_first>

  <action>
훅 지점 확정(설계 결정 완료, 대안 검토 금지): **`PlayerStats` 가 `HP.Die()` 를 `override` 한다.**
- `HP.OnDeath` 이벤트 구독 방식은 쓰지 않는다 — 구독만으로는 `HP.Die()` 뒤에 이어지는 `Destroy(gameObject)` 를 막지 못한다.
- `Player.prefab` 의 `ManualDeath` 플래그를 1 로 바꾸는 방식도 쓰지 않는다 — 사망 정책이 프리팹 직렬화 값과 코드로 쪼개지고 씬별 오버라이드로 조용히 뒤집힐 수 있다.
- `Assets/Script/HP.cs` 는 CP949 파일이므로 건드리지 않는다.
- `base.Die()` 는 **호출하지 않는다** (호출하면 `Destroy(gameObject)` 가 실행된다). `HP.OnDeath` 의 플레이어 쪽 구독자는 현재 0건이므로 이벤트가 발화되지 않아도 회귀가 없다.

`Assets/Player/Script/PlayerStats.cs` 에 다음 3가지를 추가한다.

(1) 파일 상단 `using UnityEngine;` 바로 다음 줄에 추가:
```csharp
using UnityEngine.SceneManagement;
```

(2) `public float MaxTotalHealth { get { return maxTotalHealth; } }` 선언(28행 부근) 바로 아래에 필드 추가:
```csharp

    // Phase 15 (D-01): re-entrancy guard for Die(). The scene load kicked off below is
    // asynchronous, so the dying player object survives for a few more frames and any further
    // TakeDamage() would call Die() again and start a second load.
    private bool _deathHandled = false;
```

(3) 클래스 안, `RestoreStats(...)` 메서드 **바로 앞**에 새 메서드를 추가:
```csharp
    // Phase 15 (D-01): HP hits 0 -> immediately reload the last save. No game over screen and
    // no new GameStateManager state - LoadGame() already reloads the scene, applies the spawn
    // point and restores stats in one shot.
    // Deliberately does NOT call base.Die(): HP.Die() would Destroy(gameObject) and the player
    // would vanish from the scene. Overriding here (rather than flipping the prefab's
    // ManualDeath flag) keeps the whole death policy in one code path and leaves HP.cs - a
    // CP949-encoded shared file - untouched.
    public override void Die()
    {
        if (_deathHandled) return;
        _deathHandled = true;

        SaveLoadManager mgr = SaveLoadManager.Instance;
        if (mgr != null && mgr.HasSaveFile())
        {
            mgr.LoadGame();
            return;
        }

        // D-02: no save file yet (the player never activated a checkpoint). The run is not
        // over - restart the current scene instead of ejecting to the main menu. Synchronous
        // LoadScene matches MainMenuUI / SlotSelectPanel / SignpostPortal; the asynchronous
        // path exists only inside SaveLoadManager's own load routine.
        SceneManager.LoadScene(SceneManager.GetActiveScene().name);
    }
```

절대 하지 말 것:
- `Assets/map/script/FallZone.cs` 수정 (D-03: 낙하는 사망이 아니라 페널티다. 낙하 데미지로 HP가 0이 되면 위 `Die()` 로 자연히 넘어간다).
- `Assets/Script/HP.cs` 수정.
- `Assets/Player.prefab` 의 `ManualDeath` 값 변경 (`ManualDeath: 0` 그대로 유지).
- `GameStateManager` 에 새 상태 추가 (D-01 이 명시적으로 배제).
- `PlayerStats.TakeDamage()` 수정 (Phase 12 의 `CameraController.Instance.Shake();` 라인은 그대로 둔다).
  </action>

  <verify>
    <automated>dotnet build "Projeect_A.E.sln" --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "using UnityEngine.SceneManagement;" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "public override void Die()" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "private bool _deathHandled = false;" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "if (_deathHandled) return;" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "mgr.LoadGame();" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "SceneManager.LoadScene(SceneManager.GetActiveScene().name);" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다.
    - `grep -c "base.Die()" Assets/Player/Script/PlayerStats.cs` 가 `0` 을 출력한다.
    - `grep -c "CameraController.Instance.Shake();" Assets/Player/Script/PlayerStats.cs` 가 `1` 을 출력한다 (Phase 12 회귀 없음).
    - `git status --porcelain Assets/Script/HP.cs Assets/map/script/FallZone.cs` 가 빈 출력이다 (D-03: FallZone 무수정, HP.cs 무수정).
    - `grep -c "ManualDeath: 0" Assets/Player.prefab` 가 `1` 을 출력한다 (프리팹 사망 플래그 무변경).
    - `head -c 3 Assets/Player/Script/PlayerStats.cs | xxd -p` 가 `efbbbf` 를 출력한다.
    - `grep -c $'\xef\xbf\xbd' Assets/Player/Script/PlayerStats.cs` 가 `0` 을 출력한다.
    - `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 가 에러 0 으로 끝난다.
    - 커밋 1개. pathspec 은 `Assets/Player/Script/PlayerStats.cs` 만.
  </acceptance_criteria>

  <done>`PlayerStats.Die()` 가 `Destroy(gameObject)` 를 실행하지 않고, 세이브가 있으면 `SaveLoadManager.LoadGame()` 을, 없으면 현재 씬 재로드를 정확히 1회만 수행한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Player.prefab 기본 체력값 확정 — maxHealth 100 / maxTotalHealth 200 (D-09)</name>
  <files>Assets/Player.prefab</files>

  <read_first>
    - `Assets/Player.prefab` 의 380~400행 (script guid `ec0b64c0b1dccc64d91eee52d85267bd` 인 `MonoBehaviour` 블록 = PlayerStats 컴포넌트)
    - `bug/BUG-002-player-health-bounds-inverted.md` (값 확정 근거)
  </read_first>

  <action>
`Assets/Player.prefab` 에서 script guid 가 `ec0b64c0b1dccc64d91eee52d85267bd` (= `Assets/Player/Script/PlayerStats.cs`) 인 `MonoBehaviour` 블록 안의 **`maxHealth` 값 1줄만** 바꾼다.

변경 전 (현재 393행):
```yaml
  maxHealth: 400
```
변경 후:
```yaml
  maxHealth: 100
```

**그 외 어떤 줄도 건드리지 않는다:**
- `health: 50` — 그대로 둔다. `HP.Awake()` 가 런타임에 `health = maxHealth` 로 덮으므로 직렬화 값은 표시용이고, 50 은 이미 `0 < 50 <= 100` 을 만족한다.
- `maxTotalHealth: 200` — 이미 D-09 의 목표값이다. 그대로 둔다.
- `ManualDeath: 0` — 그대로 둔다 (Task 2 의 `Die()` override 가 이 플래그를 무의미하게 만든다).
- `hitColor`, `flashDuration` — 그대로 둔다.

**실행 전 필독 — `Assets/Player.prefab` 에는 이미 커밋되지 않은 변경이 1줄 존재한다:**
```
-  attackBox: {fileID: 0}
+  attackBox: {fileID: 2283162787729905416, guid: 709704cd680fc3b4ab4d0b240ce3406d, type: 3}
```
이것은 BUG-001(프리팹 기본값의 빈 attackBox) 관련 변경으로 **이 플랜의 산출물이 아니다.** 따라서:
1. 편집 전에 `git diff --numstat -- Assets/Player.prefab` 을 실행해 기준값(현재 `1  1`)을 기록해 둔다.
2. `maxHealth` 1줄 수정 후 같은 명령의 결과가 기준값보다 정확히 `+1 / +1` 증가했는지 확인한다 (즉 `2  2`).
3. `git commit -- Assets/Player.prefab` 은 이 attackBox 변경까지 함께 커밋하게 된다. **커밋 메시지 본문에 "이 커밋에는 이 플랜과 무관한 기존 미커밋 변경(attackBox 기본값 채움)이 함께 포함됨" 을 반드시 한 줄 적고, 실행 보고에도 명시한다.** attackBox 줄을 되돌리거나 stash 하지 말 것 — 되돌리면 사용자의 기존 작업이 사라진다.

**씬 파일은 하나도 수정하지 않는다.** `Assets/Scenes/Tutorial Map.unity` 는 Player 인스턴스의 `maxHealth` 를 이미 100 으로 오버라이드하고 있어 값이 일치하므로 오버라이드를 제거할 필요가 없다. 나머지 9개 스테이지 씬은 오버라이드가 없어 이번 프리팹 변경만으로 400 -> 100 이 되며, 이것이 D-09 가 의도한 변경이다.
  </action>

  <verify>
    <automated>git diff --numstat -- Assets/Player.prefab</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^  maxHealth: 100$" Assets/Player.prefab` 가 `1` 을 출력한다.
    - `grep -c "^  maxHealth: 400$" Assets/Player.prefab` 가 `0` 을 출력한다.
    - `grep -c "^  maxTotalHealth: 200$" Assets/Player.prefab` 가 `1` 을 출력한다.
    - `grep -c "^  health: 50$" Assets/Player.prefab` 가 `1` 을 출력한다 (무변경).
    - `grep -c "^  ManualDeath: 0$" Assets/Player.prefab` 가 `1` 을 출력한다 (무변경).
    - `git diff --numstat -- Assets/Player.prefab` 의 추가/삭제 줄 수가 편집 전 기준값(`1  1`)보다 정확히 1씩 늘어난 `2  2` 다 (기존 attackBox 미커밋 변경 1줄 + 이번 maxHealth 1줄).
    - `git diff -- Assets/Player.prefab | grep "^[+-]" | grep -v "^[+-][+-]"` 의 출력이 정확히 4줄이며, 그중 2줄은 `attackBox` (기존 변경), 2줄은 `maxHealth: 400` / `maxHealth: 100` 이다.
    - 커밋 메시지에 무관한 기존 변경이 함께 포함됐다는 사실이 명시돼 있다.
    - `Assets/Scenes/` 아래 `.unity` 파일 중 이 태스크로 인해 새로 변경된 파일이 0개다 (실행 전 이미 존재하던 미커밋 씬 변경은 건드리지도, 스테이징하지도 말 것).
    - 커밋 1개. pathspec 은 `Assets/Player.prefab` 만.
  </acceptance_criteria>

  <done>`Player.prefab` 이 `maxHealth: 100`, `maxTotalHealth: 200` 을 직렬화하고 있어 `AddHealth()` 의 `maxHealth < maxTotalHealth` 조건이 성립한다.</done>
</task>

</tasks>

<verification>
1. `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 에러 0.
2. 이 플랜이 만든 커밋 3개의 변경 파일 합집합이 `Assets/Player/Script/PlayerStats.cs` 와 `Assets/Player.prefab` 2개뿐이다.
3. `git status --porcelain Assets/Script/HP.cs Assets/map/script/FallZone.cs` 빈 출력 (D-03 무수정 계약).
4. `grep -c "MapGimmickState\|Items" Assets/Player/Script/PlayerStats.cs` 가 `0` 이다 (D-10: 범위 밖 항목이 새어 들어오지 않음).
</verification>

<success_criteria>
- `PlayerStats.Die()` 가 존재하고 `base.Die()` 를 호출하지 않는다.
- 세이브 유무에 따라 `LoadGame()` / 현재 씬 재로드로 분기한다.
- `RestoreStats()` 가 세 값을 보정한 뒤 대입한다.
- `Player.prefab` 이 `maxHealth: 100` / `maxTotalHealth: 200` 이다.
- 컴파일 에러 0, 계획 밖 파일 변경 0.
</success_criteria>

<output>
완료 후 `.planning/phases/15-load-timing-and-load-scope/15-01-SUMMARY.md` 를 작성한다.
</output>
