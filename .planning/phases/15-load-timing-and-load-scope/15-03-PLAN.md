---
phase: 15-load-timing-and-load-scope
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/WaterSpirit/Script/SpiritStats.cs
  - Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs
  - Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs
autonomous: true
requirements: [D-06, D-07]
must_haves:
  truths:
    - "이미 격파한 보스는 세이브를 로드해 그 씬으로 돌아가도 다시 등장하지 않는다"
    - "격파 판정은 각 보스가 자기 ID로 IsBossDefeated() 를 조회해 스스로 수행한다 (로드 경로가 보스 목록을 순회하지 않는다)"
    - "TutorialBoss 를 이미 격파한 세이브로 돌아오면 보스가 사라진 채 보스방 벽이 열려 있어 진행이 막히지 않는다"
    - "격파 기록이 없는 보스는 기존과 완전히 동일하게 등장한다"
  artifacts:
    - path: "Assets/Enemy/WaterSpirit/Script/SpiritStats.cs"
      provides: "WaterSpirit 자가 제거 Awake 가드"
      contains: "IsBossDefeated(\"WaterSpirit\")"
    - path: "Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs"
      provides: "WaterMonster 자가 제거 Awake 가드"
      contains: "IsBossDefeated(\"WaterMonster\")"
    - path: "Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs"
      provides: "TutorialBoss 자가 제거 Awake 가드 + 보스방 벽 해제"
      contains: "IsBossDefeated(\"TutorialBoss\")"
  key_links:
    - from: "각 보스 스크립트의 Awake()"
      to: "SaveLoadManager.IsBossDefeated(bossId)"
      via: "씬 로드 직후 자기 ID 조회"
      pattern: "IsBossDefeated\\(\""
    - from: "TutorialBossController.Awake()"
      to: "InteractableWall.UnlockWall()"
      via: "격파 상태 복원 시 보스방 벽도 함께 해제"
      pattern: "WallToUnlock\\.UnlockWall\\(\\)"
---

<objective>
Phase 15 결정 D-06 / D-07 을 구현한다.

- D-06: 씬 로드 시 `BossProgress` 를 읽어 이미 격파된 보스는 등장시키지 않는다.
- D-07: 판정 주체는 **보스 쪽**이다. 로드 경로가 보스 목록을 알고 순회하는 구조가 아니라, 각 보스가 자기 ID로 `SaveLoadManager.Instance.IsBossDefeated()` 를 조회해 스스로 물러난다 (Phase 11 D-01: 매니저는 로직만 소유, 호출자가 직접 호출).

Purpose: `SaveOnBossDefeated()` 는 3곳에서 호출되지만 짝이 되는 `IsBossDefeated()` 의 호출자는 현재 **0건**이다. 격파 정보는 기록만 되고 아무도 읽지 않아서, 잡은 보스가 로드 후 부활한다 (BUG-003 의 로드 측 항목).
Output: 보스 스크립트 3개에 자가 제거 가드 추가. `SaveLoadManager.cs` 는 수정하지 않는다.

**범위 밖(명시적 금지):** WaterMonster 보스 프리팹 제작, 씬 배치, `BossHealthBarController` 배선 — BUG-003 의 본체이며 별도 페이즈다. 이 플랜은 코드 가드만 넣는다.
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 실행한다. 아래 내용만으로 완결되어야 하며, CONTEXT.md 를 다시 읽지 않아도 실행 가능해야 한다.
</execution_context>

<context>
@.planning/phases/15-load-timing-and-load-scope/15-CONTEXT.md
@bug/BUG-003-watermonster-not-wired.md
@Assets/SaveSystem/Script/SaveLoadManager.cs
@Assets/Enemy/WaterSpirit/Script/SpiritStats.cs
@Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs
@Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs
@Assets/Enemy/Tutorial/TutorialBoss/State/TutorialDeadState.cs
@Assets/map/script/InteractableWall.cs
</context>

<interfaces>
<!-- 실행자가 필요로 하는 계약. 코드베이스 탐색 없이 이 블록만 보고 작업할 것. -->

Assets/SaveSystem/Script/SaveLoadManager.cs (이 플랜에서 **수정하지 않음**):
```csharp
public static SaveLoadManager Instance { get; private set; }
// RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad) 로 부트스트랩되므로
// 어떤 씬의 Awake() 보다 먼저 존재한다.

public void SaveOnBossDefeated(string bossId)   // _data.BossProgress[bossId] = true; Save();
public bool IsBossDefeated(string bossId)       // BossProgress.TryGetValue -> bool. 없으면 false
```

**보스 ID 확정값 3개** (기존 `SaveOnBossDefeated` 호출부의 문자열 리터럴과 정확히 일치해야 함):
| 보스 | ID 문자열 | 기존 저장 호출부 |
|------|-----------|------------------|
| 튜토리얼 보스 | `"TutorialBoss"` | `Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs:304` (`HandleDeath()`) |
| 물의 정령 | `"WaterSpirit"` | `Assets/Enemy/WaterSpirit/Script/SpiritStats.cs:67` (`Die()`) |
| 물괴물 | `"WaterMonster"` | `Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs:77` (`Die()`) |
`Assets/Enemy/Boss` 계열(우드보스)은 `SaveOnBossDefeated` 를 호출하지 않으므로 이 플랜의 대상이 아니다. 건드리지 말 것.

Assets/Enemy/NewBoss/Script/BossStatesSystem.cs (부모 클래스):
```csharp
public class BossStatsSystem : MonoBehaviour
{
    public float MaxHealth = 1000f;
    protected float _currentHealth;
    void Start() { _currentHealth = MaxHealth; _currentWater = MaxWater; }  // private, non-virtual
    protected virtual void Update()
    protected virtual void Die()   // SpiritStats / WaterMonsterStats 가 override 중
}
```
**주의:** 부모의 `Start()` 는 `private` 이고 `virtual` 이 아니다. 자식에서 `Start()` 를 새로 선언하면 Unity 생명주기 호출이 모호해진다. 그래서 이 플랜의 가드는 **`Awake()`** 에 넣는다 — `BossStatsSystem` 에는 `Awake()` 가 없으므로 자식에 새로 선언해도 충돌하지 않는다.

Assets/map/script/InteractableWall.cs:
```csharp
public class InteractableWall : MonoBehaviour
{
    public GameObject wallTilemap;
    public void UnlockWall()   // wallTilemap.SetActive(false); gameObject.SetActive(false);
}
```

Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs (namespace `TutorialBoss`):
```csharp
public InteractableWall WallToUnlock;   // 보스 사망 시 열릴 벽 (Inspector 연결)
public GameObject ClearPanel;           // 클리어 UI
protected override void Awake()         // 134행: _hp = GetComponent<HP>(); base.Awake(); ... Kinematic 설정
private void Start()                    // 145행: ManualDeath=true, OnDeath 구독, ChangeState(TutorialIdleState)
private void HandleDeath()              // 298행: StopAllCoroutines() -> SaveOnBossDefeated("TutorialBoss") -> ChangeState(TutorialDeadState)
```
</interfaces>

<encoding_note>
대상 3개 파일은 모두 **UTF-8(BOM 없음) + CRLF** 이고 한국어 주석을 포함한다 (`file` 명령 기준 "Unicode text, UTF-8 text, with CRLF line terminators"). 편집 후에도 기존 한국어 주석 바이트가 그대로 유지되어야 한다. 이 저장소는 Phase 11 에서 인코딩 왕복으로 한국어 주석이 U+FFFD 로 훼손된 사고 이력이 있다. **새로 추가하는 주석·문자열은 전부 ASCII 로만 작성한다.**
</encoding_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SpiritStats / WaterMonsterStats 에 격파 시 자가 제거 Awake 가드 추가 (D-06, D-07)</name>
  <files>Assets/Enemy/WaterSpirit/Script/SpiritStats.cs, Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs</files>

  <read_first>
    - `Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` (전체 71줄 — `Die()` 가 `gameObject.SetActive(false)` 로 끝난다는 점, `SaveOnBossDefeated("WaterSpirit")` 문자열 확인)
    - `Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` (전체 86줄 — 같은 구조, `SaveOnBossDefeated("WaterMonster")`)
    - `Assets/Enemy/NewBoss/Script/BossStatesSystem.cs` (부모에 `Awake()` 가 없고 `Start()` 가 private 이라는 점 확인)
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` 의 `IsBossDefeated(string)` 과 `Bootstrap()` (매니저가 씬 Awake 보다 먼저 존재함을 확인)
  </read_first>

  <action>
제거 방식 확정(설계 결정 완료, 대안 검토 금지): **`gameObject.SetActive(false)`** 를 쓴다. `Destroy` 도 스폰 차단도 쓰지 않는다. 이유는 두 보스의 기존 `Die()` 가 정확히 `gameObject.SetActive(false)` 로 끝나기 때문이다 — 격파 상태 복원은 "이미 죽은 직후" 와 같은 상태여야 한다.

배치 위치 확정: `Stats` 클래스의 **`Awake()`** (부모 `BossStatsSystem` 에 `Awake()` 가 없어 충돌 없음). `Start()` 는 부모가 private 으로 이미 갖고 있으므로 쓰지 않는다. 보스 ID 문자열은 같은 파일의 `Die()` 안 `SaveOnBossDefeated(...)` 리터럴과 **글자 단위로 동일**해야 한다.

(1) `Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` — `private bool _stage2Triggered = false;` (6행) 바로 아래, `protected virtual void Reset()` (8행) 바로 위에 다음을 삽입:

```csharp

    // Phase 15 (D-06/D-07): boss-side judgement. A boss already recorded as defeated must not
    // come back after a load, so it removes itself the moment the scene brings it up. The load
    // path never walks a boss list - each boss asks with its own id, which is the same literal
    // this class writes in Die() below.
    // SetActive(false) - not Destroy - because that is exactly what Die() does.
    private void Awake()
    {
        if (SaveLoadManager.Instance != null && SaveLoadManager.Instance.IsBossDefeated("WaterSpirit"))
        {
            gameObject.SetActive(false);
        }
    }
```

(2) `Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` — `public void SetEnraged(bool value) { _isEnraged = value; }` (19행) 바로 아래, `protected override void Update()` (21행) 바로 위에 다음을 삽입:

```csharp

    // Phase 15 (D-06/D-07): boss-side judgement. A boss already recorded as defeated must not
    // come back after a load, so it removes itself the moment the scene brings it up. The load
    // path never walks a boss list - each boss asks with its own id, which is the same literal
    // this class writes in Die() below.
    // SetActive(false) - not Destroy - because that is exactly what Die() does.
    private void Awake()
    {
        if (SaveLoadManager.Instance != null && SaveLoadManager.Instance.IsBossDefeated("WaterMonster"))
        {
            gameObject.SetActive(false);
        }
    }
```

절대 하지 말 것:
- 두 파일의 기존 `Die()` / `TakeDamage()` / `Reset()` / `Update()` / `SpendHpCost()` / `OnHealed()` 본문 수정.
- `Assets/SaveSystem/Script/SaveLoadManager.cs` 수정.
- `Assets/Enemy/NewBoss/Script/BossStatesSystem.cs` 나 `BossController.cs` 수정 (공용 기반 클래스 — 회귀 위험).
- WaterMonster 보스 프리팹 제작이나 씬 배치 (BUG-003 본체, 별도 페이즈).
  </action>

  <verify>
    <automated>dotnet build "Projeect_A.E.sln" --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'SaveLoadManager.Instance.IsBossDefeated("WaterSpirit")' Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` 가 `1` 을 출력한다.
    - `grep -c 'SaveLoadManager.Instance.IsBossDefeated("WaterMonster")' Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 가 `1` 을 출력한다.
    - `grep -c "private void Awake()" Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` 가 `1` 을 출력한다.
    - `grep -c "private void Awake()" Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 가 `1` 을 출력한다.
    - `grep -c "void Start()" Assets/Enemy/WaterSpirit/Script/SpiritStats.cs Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 가 두 파일 모두 `0` 을 출력한다 (부모의 private Start 와 충돌하는 선언 없음).
    - `grep -c 'SaveOnBossDefeated("WaterSpirit")' Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` 가 `1` 을 출력한다 (Phase 11 저장 경로 무회귀).
    - `grep -c 'SaveOnBossDefeated("WaterMonster")' Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 가 `1` 을 출력한다.
    - `git status --porcelain Assets/SaveSystem/Script/SaveLoadManager.cs Assets/Enemy/NewBoss/Script/BossStatesSystem.cs Assets/Enemy/NewBoss/Script/BossController.cs` 가 빈 출력이다.
    - `grep -c $'\xef\xbf\xbd' Assets/Enemy/WaterSpirit/Script/SpiritStats.cs Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 가 두 파일 모두 `0` 을 출력한다 (한국어 주석 훼손 없음).
    - `grep -c "IsDummy" Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` 가 `2` 를 출력한다 (기존 분신 분기 무회귀).
    - `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 가 에러 0 으로 끝난다.
    - 커밋 1개. pathspec 은 `Assets/Enemy/WaterSpirit/Script/SpiritStats.cs Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 두 개만.
  </acceptance_criteria>

  <done>격파 기록이 있는 WaterSpirit / WaterMonster 는 씬에 올라오자마자 스스로 비활성화되고, 기록이 없으면 기존과 동일하게 동작한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: TutorialBossController 에 자가 제거 Awake 가드 + 보스방 벽 해제 추가 (D-06, D-07)</name>
  <files>Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs</files>

  <read_first>
    - `Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` (특히 `WallToUnlock`/`ClearPanel` 필드 선언 54~58행, `protected override void Awake()` 134~144행, `private void Start()` 145~167행, `HandleDeath()` 298~306행)
    - `Assets/Enemy/Tutorial/TutorialBoss/State/TutorialDeadState.cs` (사망 연출이 2.5초 뒤 `WallToUnlock.UnlockWall()` 을 호출하고 `ClearPanel` 을 켠다는 사실 확인 — 벽 해제만 복원해야 하는 근거)
    - `Assets/map/script/InteractableWall.cs` (`UnlockWall()` 이 `wallTilemap.SetActive(false)` + 자기 `SetActive(false)` 를 한다는 사실 확인)
  </read_first>

  <action>
`Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 의 기존 `protected override void Awake()` (134행) **본문 맨 앞**에 격파 가드를 삽입한다. `base.Awake()` 보다 **먼저** 판정하고 `return` 해야 한다 — 이미 죽은 보스에 Rigidbody 설정이나 상태머신 초기화를 할 이유가 없다.

교체 후 `Awake()` 전체는 정확히 다음과 같아야 한다 (들여쓰기는 8칸, 이 파일은 namespace 안이라 클래스 멤버가 8칸 들여쓰기다):

```csharp
        protected override void Awake()
        {
            // Phase 15 (D-06/D-07): boss-side judgement. If this boss is already recorded as
            // defeated, it must not come back after a load. The load path never walks a boss
            // list - this boss asks with its own id, the same literal HandleDeath() writes.
            if (SaveLoadManager.Instance != null && SaveLoadManager.Instance.IsBossDefeated("TutorialBoss"))
            {
                // The wall is the boss room exit: TutorialDeadState opens it 2.5s after the
                // kill. Restoring the defeated state without it would lock the player out of
                // the rest of the map. ClearPanel is deliberately NOT shown - that one-shot
                // clear UI also pauses the game and belongs to the moment of the kill only.
                if (WallToUnlock != null) WallToUnlock.UnlockWall();
                gameObject.SetActive(false);
                return;
            }

            _hp = GetComponent<HP>();
            base.Awake();

            // 보스는 물리 힘/중력의 영향을 받지 않아야 함 → Kinematic 설정
            var rb = GetComponent<Rigidbody2D>();
            if (rb != null)
                rb.bodyType = RigidbodyType2D.Kinematic;
        }
```

**기존 한국어 주석("보스는 물리 힘/중력의 영향을 받지 않아야 함 → Kinematic 설정")과 화살표 문자 `→` 는 원래 바이트 그대로 보존해야 한다.** 이 줄을 다시 타이핑하지 말고 원본을 그대로 둔 채 위쪽에만 삽입할 것.

절대 하지 말 것:
- `private void Start()` 수정 (`ManualDeath = true` / `OnDeath += HandleDeath` / `ChangeState(TutorialIdleState)` 전부 그대로).
- `HandleDeath()` 수정 (`SaveOnBossDefeated("TutorialBoss")` 호출 유지).
- `Assets/Enemy/Tutorial/TutorialBoss/State/TutorialDeadState.cs` 수정.
- `Assets/map/script/InteractableWall.cs` 수정.
- `BossHealthBarController.cs` 수정 — 이미 `bossController == null` 과 `bossStats == null` 을 `Update()` 에서 가드하고, 비활성화된 보스는 `TargetFound` 가 false 로 남아 체력바가 켜지지 않는다. 손댈 필요가 없다.
- 씬 파일(`.unity`) 수정.
  </action>

  <verify>
    <automated>dotnet build "Projeect_A.E.sln" --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'SaveLoadManager.Instance.IsBossDefeated("TutorialBoss")' Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `1` 을 출력한다.
    - `grep -c "if (WallToUnlock != null) WallToUnlock.UnlockWall();" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `1` 을 출력한다.
    - `grep -c "gameObject.SetActive(false);" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `1` 을 출력한다.
    - `grep -A 2 'IsBossDefeated("TutorialBoss")' Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 의 출력이 `base.Awake();` 보다 앞에 위치한다 (`grep -n "IsBossDefeated\|base.Awake();"` 로 줄 번호를 비교해 확인).
    - `grep -c "ClearPanel.SetActive" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `0` 을 출력한다 (클리어 UI 를 로드 시 띄우지 않음).
    - `grep -c 'SaveOnBossDefeated("TutorialBoss")' Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `1` 을 출력한다 (Phase 11 저장 경로 무회귀).
    - `grep -c "_hp.ManualDeath = true;" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `1` 을 출력한다 (Start 무변경).
    - `grep -c "Kinematic" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `2` 를 출력한다 (기존 주석 + 코드 유지).
    - `grep -c $'\xef\xbf\xbd' Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 가 `0` 을 출력한다 (한국어 주석 훼손 없음).
    - `git status --porcelain Assets/Enemy/Tutorial/TutorialBoss/State/TutorialDeadState.cs Assets/map/script/InteractableWall.cs` 가 빈 출력이다.
    - `git diff --numstat -- Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 의 삭제 줄 수가 `0` 이다 (순수 삽입 diff — 기존 한국어 주석 줄이 재작성되지 않았다는 증거).
    - `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 가 에러 0 으로 끝난다.
    - 커밋 1개. pathspec 은 `Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` 만.
  </acceptance_criteria>

  <done>격파 기록이 있는 TutorialBoss 는 씬 진입 즉시 비활성화되고 보스방 벽이 열려 있어 플레이어가 갇히지 않는다. 클리어 UI 는 다시 뜨지 않는다.</done>
</task>

</tasks>

<verification>
1. `dotnet build "Projeect_A.E.sln" --nologo -v quiet` 에러 0.
2. `grep -rn "IsBossDefeated" --include=*.cs Assets/` 결과가 정확히 4건이다: `SaveLoadManager.cs` 선언 1 + 보스 3종 호출 3.
3. `grep -rn "SaveOnBossDefeated" --include=*.cs Assets/` 결과가 여전히 4건이다 (선언 1 + 호출 3, Phase 11 무회귀).
4. 이 플랜이 바꾼 파일이 `files_modified` 3개뿐이다. `.unity` / `.prefab` 변경 0건.
5. 각 보스의 ID 문자열이 같은 파일 안의 `SaveOnBossDefeated` 리터럴과 정확히 일치한다.
</verification>

<success_criteria>
- 보스 3종이 각자 자기 ID로 `IsBossDefeated()` 를 조회한다 (`IsBossDefeated()` 의 첫 소비자 생성).
- 격파 기록이 있으면 `gameObject.SetActive(false)` 로 스스로 물러난다.
- TutorialBoss 는 벽도 함께 연다.
- `SaveLoadManager.cs`, 공용 기반 클래스, 씬/프리팹 파일 변경 0건.
- 컴파일 에러 0.
</success_criteria>

<output>
완료 후 `.planning/phases/15-load-timing-and-load-scope/15-03-SUMMARY.md` 를 작성한다.
</output>
