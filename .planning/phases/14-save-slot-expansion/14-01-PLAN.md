---
phase: 14-save-slot-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/SaveSystem/Script/SaveLoadManager.cs
autonomous: true
requirements: [D-06, D-07]
must_haves:
  truths:
    - "기존 플레이어의 save.json 이 이름 변경/삭제/복사 없이 슬롯 0 으로 그대로 읽힌다 (마이그레이션 코드 0줄)"
    - "슬롯 1/2 는 save_1.json / save_2.json 으로 저장되며, 한 슬롯 저장이 다른 슬롯 파일을 건드리지 않는다"
    - "슬롯 3개의 데이터를 한 번에 조회해도 진행 중인 _data 와 CurrentSlot 이 변하지 않는다"
    - "게임플레이 중 저장 트리거 4곳(Checkpoint + 보스 3종)이 0줄 변경으로 현재 선택된 슬롯에 저장한다"
    - "NewGameInSlot 은 디스크를 즉시 쓰지 않는다 (Phase 11 D-06 lazy-write 계약 유지)"
  artifacts:
    - path: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      provides: "슬롯 인식 경로 해석 + 비파괴 조회 + 슬롯 진입점"
      contains: "public const int SlotCount = 3;"
      exports: ["SlotCount", "CurrentSlot", "GetSavePath", "SelectSlot", "HasSaveFile(int)", "PeekSlotData", "NewGameInSlot", "LoadSlot"]
  key_links:
    - from: "SaveLoadManager.SavePath"
      to: "SaveLoadManager.GetSavePath(CurrentSlot)"
      via: "instance property getter"
      pattern: "return GetSavePath\\(CurrentSlot\\);"
    - from: "SaveLoadManager.PeekSlotData"
      to: "JsonConvert.DeserializeObject<SaveData>"
      via: "read-only deserialize that never assigns _data"
      pattern: "return JsonConvert\\.DeserializeObject<SaveData>\\(json, JsonSettings\\);"
    - from: "SaveLoadManager.NewGameInSlot"
      to: "SelectSlot + NewGame"
      via: "delegation only, no disk write"
      pattern: "SelectSlot\\(slot\\);"
---

<objective>
Phase 11 의 단일 슬롯 `SaveLoadManager` 를 3슬롯 인식 매니저로 확장한다.

Purpose: 슬롯 UI(Plan 02/03)가 의존할 데이터 계층 계약을 먼저 확정한다. 슬롯 차원은
"모든 메서드에 `int slot` 인자를 추가"하는 방식이 아니라 **`CurrentSlot` 필드 + 슬롯 파생 경로**
방식으로 넣는다. 그래야 `Checkpoint.cs` 와 보스 3종의 기존 무인자 호출
(`SaveAtCheckpoint`/`SaveOnBossDefeated`)이 **0줄 변경**으로 살아남는다 (CLAUDE.md 3번 정밀한 변경).

Output: `SaveLoadManager.cs` 단일 파일의 순수 확장. `SaveData.cs` 는 0줄 변경(스키마 자체가
이미 슬롯 1개의 형태), 저장 트리거 4곳도 0줄 변경.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-save-slot-expansion/14-CONTEXT.md
@.planning/phases/14-save-slot-expansion/14-RESEARCH.md
@Assets/SaveSystem/Script/SaveLoadManager.cs
@Assets/SaveSystem/Script/SaveData.cs

<interfaces>
<!-- 실행자가 알아야 할 현재 계약. 코드베이스 탐색 없이 이 블록만으로 작업 가능해야 한다. -->

Assets/SaveSystem/Script/SaveData.cs (이 플랜에서 0줄 변경):
```csharp
public class SaveData
{
    public int SaveVersion = 1;
    public string SceneName = "";
    public string SpawnPointName = "";
    public PlayerStatsSaveData PlayerStats = new PlayerStatsSaveData();
    public Dictionary<string, bool> BossProgress = new Dictionary<string, bool>();
    public Dictionary<string, bool> MapGimmickState = new Dictionary<string, bool>();
    public List<string> Items = new List<string>();
}
```

Assets/SaveSystem/Script/SaveLoadManager.cs 현재 상태 (편집 대상):
```csharp
public const string SaveFileName = "save.json";                       // line 50

private static readonly JsonSerializerSettings JsonSettings = ...;    // line 57 (재사용 가능)
private SaveData _data = new SaveData();                              // line 63
public SaveData Data { get { return _data; } }                        // line 66

public static string SavePath                                          // line 68-71  <-- static, 인스턴스화 대상
{
    get { return Path.Combine(Application.persistentDataPath, SaveFileName); }
}

public bool HasSaveFile()          { return File.Exists(SavePath); }   // line 73-76  (본문 무변경)
public void NewGame()              { _data = new SaveData(); ... }     // line 80-86  (본문 무변경)
public void Save()                 { ... File.WriteAllText(SavePath, json); ... }  // line 89-95 (본문 무변경)
public void SaveAtCheckpoint(string checkpointName) { ... Save(); }    // line 99-104 (본문 무변경)
public void SaveOnBossDefeated(string bossId)       { ... Save(); }    // line 110-117 (본문 무변경)
public void LoadGame()             { ... _data = loaded; ... }         // line 210-246 (본문 무변경)
private void DebugLogState()       { Debug.Log("[SaveLoadManager] path=" + SavePath + ...); } // line 320-332
```

`SavePath` 외부 호출자 검증 결과 (이 플랜 작성 시점 grep 실측):
`SaveLoadManager.SavePath` / `.Instance.SavePath` 를 참조하는 코드는 `Assets/` 어디에도 없다.
`Assets/Editor/Tools/CombatPrefabGenerator.cs` 의 `GetSavePath(string)` 은 **동명이인**(그 클래스
자신의 private 메서드, 인자 타입도 string)으로 무관하다. 따라서 static → instance 전환은 안전하다.
</interfaces>

<encoding_note>
`Assets/SaveSystem/Script/SaveLoadManager.cs` 는 **순수 ASCII** 파일이다 (실측:
`grep -cP "[^\x00-\x7F]" ...` == 0). Phase 11 Plan 3 의 CP949 훼손 사고는 이 파일에는
해당되지 않는다. **다만 이 플랜에서 추가하는 모든 주석/문자열도 반드시 ASCII 로만 작성해야 한다** —
인수 기준이 비-ASCII 라인 수 0 을 요구한다.
</encoding_note>

<verification_gate_note>
이 프로젝트는 "플랜의 검증 게이트가 플랜 자신이 지시한 주석 문구와 충돌하는" 사고가
Phase 9 Plan 1(DontDestroyOnLoad), Phase 10 Plan 1(deadzoneHeight), Phase 11 Plan 2(async/await)
세 번 반복됐다. 이번 플랜의 회귀 게이트를 깨지 않으려면 **새로 작성하는 주석 안에 다음 문자열을
쓰지 말 것**:

- `File.WriteAllText` — 게이트가 이 파일 내 등장 횟수 == 2 를 요구한다.
  ("no direct disk write here" 처럼 우회 표현을 쓸 것)
- `_data = ` (등호 앞뒤 공백 포함) — 게이트가 등장 횟수 == 3 을 요구한다.
- `async` / `await` — `Assets/SaveSystem/` 전체 0건 컨벤션 (Phase 11 게이트 #5).
</verification_gate_note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 슬롯 경로 해석 코어 (SlotCount / CurrentSlot / GetSavePath / SelectSlot / HasSaveFile(int))</name>

  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>

  <read_first>
    - Assets/SaveSystem/Script/SaveLoadManager.cs (편집 대상 — 현재 상태를 반드시 먼저 읽을 것)
    - .planning/phases/14-save-slot-expansion/14-CONTEXT.md (D-06 슬롯별 별도 파일, D-07 파일명 규칙)
    - .planning/phases/14-save-slot-expansion/14-RESEARCH.md ("File-per-slot naming" 및 "Slot dimension" 절)
  </read_first>

  <action>
D-06/D-07 을 확정 구현한다. 아래 변경을 **정확히 이 내용으로** 수행한다.

**(1) 파일 헤더 주석의 스테일 D-02 문구 갱신 (1줄 교체)**

현재 3번째 줄:
```
//            D-02 (single slot: one save.json under Application.persistentDataPath),
```
을 다음 2줄로 교체한다 (ASCII only):
```
//            D-02 (one JSON file per save under Application.persistentDataPath; Phase 14
//                  extended this to 3 slots - save.json = slot 0, save_1.json, save_2.json),
```

**(2) `SaveFileName` 상수 바로 아래(현재 line 50 다음)에 슬롯 코어를 삽입한다**

```csharp
    // ---- Phase 14: save slots (D-06 file per slot, D-07 naming) -----------------
    // Slot 0 deliberately keeps the original "save.json" filename, so a player's
    // existing Phase 11 file simply IS slot 0. Nothing is renamed, copied or migrated,
    // which is the structural answer to D-07's absolute floor ("the existing save must
    // never disappear") - there is no migration routine that could fail halfway.
    public const int SlotCount = 3;

    // The active slot. Gameplay save triggers (Checkpoint.cs and the boss death sites)
    // never pass a slot: by the time gameplay runs, the main menu already picked one.
    // Default 0 preserves the exact Phase 11 behaviour for anything that runs before
    // slot selection exists (entering Play mode directly in a gameplay scene, the
    // ContextMenu debug hooks below).
    public int CurrentSlot { get; private set; }

    private static string GetSlotFileName(int slot)
    {
        return slot == 0 ? SaveFileName : "save_" + slot + ".json";
    }

    public string GetSavePath(int slot)
    {
        return Path.Combine(Application.persistentDataPath, GetSlotFileName(slot));
    }

    public void SelectSlot(int slot)
    {
        if (slot < 0 || slot >= SlotCount)
        {
            Debug.LogError("[SaveLoadManager] SelectSlot: slot index out of range: " + slot);
            return;
        }
        CurrentSlot = slot;
    }

    public bool HasSaveFile(int slot)
    {
        if (slot < 0 || slot >= SlotCount) return false;
        return File.Exists(GetSavePath(slot));
    }
```

**(3) `SavePath` 를 static 프로퍼티에서 인스턴스 프로퍼티로 바꾼다 (현재 line 68-71)**

기존:
```csharp
    public static string SavePath
    {
        get { return Path.Combine(Application.persistentDataPath, SaveFileName); }
    }
```
교체 후:
```csharp
    // Now slot-aware. Verified before this change: no code outside this file references
    // SaveLoadManager.SavePath, so dropping "static" breaks no caller.
    public string SavePath
    {
        get { return GetSavePath(CurrentSlot); }
    }
```

**(4) 그 외 기존 메서드 본문은 단 한 줄도 바꾸지 않는다.**
`HasSaveFile()`(무인자), `NewGame()`, `Save()`, `SaveAtCheckpoint()`, `SaveOnBossDefeated()`,
`IsBossDefeated()`, `LoadGame()`, `EnsureCollections()`, `LoadSceneAndRestoreRoutine()`,
`ApplyPlayerStatsFromSave()`, `CapturePlayerStats()`, 그리고 settings 관련 블록 전부 무변경.
이들은 이미 `SavePath` 프로퍼티를 경유하므로 (3)만으로 자동으로 슬롯 인식이 된다.

**(5) `SettingsPath` / `SettingsFileName` / `SaveSettings` / `LoadSettings` 는 절대 건드리지 않는다.**
`setting.json` 은 진행도와 완전히 별개 파일·별개 모델이라 슬롯화 대상이 아니다 (CONTEXT.md
"동시 진행 중인 변경" 절). `SettingsPath` 는 `public static` 그대로 유지한다.

**금지 사항 (게이트 충돌 방지):** 새로 쓰는 주석에 `File.WriteAllText`, `_data = `,
`async`, `await` 문자열을 포함하지 말 것. 새 코드·주석은 전부 ASCII 로만 작성할 것.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/SaveSystem/Script/SaveLoadManager.cs && echo "slotcount=$(grep -c 'public const int SlotCount = 3;' $F) currentslot=$(grep -c 'public int CurrentSlot { get; private set; }' $F) getsavepath=$(grep -c 'public string GetSavePath(int slot)' $F) instpath=$(grep -c 'public string SavePath' $F) staticpath=$(grep -c 'public static string SavePath' $F) settingspath=$(grep -c 'public static string SettingsPath' $F) selectslot=$(grep -c 'public void SelectSlot(int slot)' $F) hasslot=$(grep -c 'public bool HasSaveFile(int slot)' $F) hasnoarg=$(grep -c 'public bool HasSaveFile()' $F) write=$(grep -c 'File.WriteAllText' $F) datassign=$(grep -c '_data = ' $F) nonascii=$(grep -cP '[^\x00-\x7F]' $F) asyncq=$(grep -cE '\basync\b|\bawait\b' $F)"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'public const int SlotCount = 3;' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public int CurrentSlot { get; private set; }' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public string GetSavePath(int slot)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'return slot == 0 ? SaveFileName : "save_" + slot + ".json";' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public string SavePath' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public static string SavePath' Assets/SaveSystem/Script/SaveLoadManager.cs` == 0 (static 제거됨)
    - `grep -c 'return GetSavePath(CurrentSlot);' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public static string SettingsPath' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1 (settings 는 무변경)
    - `grep -c 'public void SelectSlot(int slot)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public bool HasSaveFile(int slot)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public bool HasSaveFile()' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1 (무인자 버전 그대로 존재)
    - `grep -c 'public const string SaveFileName = "save.json";' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1 (슬롯 0 파일명 불변 — 마이그레이션 없음)
    - `grep -c 'File.WriteAllText' Assets/SaveSystem/Script/SaveLoadManager.cs` == 2 (Save + SaveSettings 만; 신규 디스크 쓰기 없음)
    - `grep -c '_data = ' Assets/SaveSystem/Script/SaveLoadManager.cs` == 3 (필드 초기화 + NewGame + LoadGame 만)
    - `grep -cP '[^\x00-\x7F]' Assets/SaveSystem/Script/SaveLoadManager.cs` == 0 (순수 ASCII 유지)
    - `grep -cE '\basync\b|\bawait\b' Assets/SaveSystem/Script/SaveLoadManager.cs` == 0
    - `git status --porcelain Assets/SaveSystem/Script/SaveData.cs` 출력이 비어 있음 (스키마 0줄 변경)
    - `git status --porcelain Assets/map/script/Checkpoint.cs Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs Assets/Enemy/WaterSpirit/Script/SpiritStats.cs Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` 출력이 비어 있음 (저장 트리거 4곳 0줄 변경)
  </acceptance_criteria>

  <done>
`SaveLoadManager` 가 슬롯 인덱스를 파일 경로로 해석할 수 있고(`GetSavePath(0)` → `save.json`,
`GetSavePath(1)` → `save_1.json`, `GetSavePath(2)` → `save_2.json`), 기존 무인자
`Save()`/`LoadGame()`/`HasSaveFile()` 이 `CurrentSlot` 기준으로 자동 동작한다. 저장 트리거 4곳과
`SaveData.cs` 는 0줄 변경이다.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 비파괴 조회 + 슬롯 진입점 (PeekSlotData / NewGameInSlot / LoadSlot) + Phase14 디버그 훅</name>

  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>

  <read_first>
    - Assets/SaveSystem/Script/SaveLoadManager.cs (Task 1 반영 후 상태를 다시 읽을 것)
    - .planning/phases/14-save-slot-expansion/14-RESEARCH.md ("Common Pitfalls" Pitfall 2·3 — Peek vs Load 혼동, NewGame lazy-write 계약)
    - Assets/SaveSystem/Check.md (Phase 11 ContextMenu 검증 훅 서술 형식)
  </read_first>

  <action>
슬롯 선택 화면(Plan 02)이 소비할 3개 공개 API 와 Play 모드 검증용 디버그 훅을 추가한다.

**(1) `IsBossDefeated(string)` 메서드 바로 아래, `// ---- Settings (setting.json) ----` 주석
블록 **앞**에 다음을 삽입한다** (settings 영역을 침범하지 말 것):

```csharp
    // ---- Phase 14: slot-select screen API ---------------------------------------

    // Read-only peek used by the slot select screen, which must show progress for all
    // 3 slots at once. This deliberately leaves the live memory cache and CurrentSlot
    // alone - that is the whole difference from LoadGame(), which replaces the cache
    // and kicks off a scene load. Never call LoadGame() just to display a slot card.
    public SaveData PeekSlotData(int slot)
    {
        if (!HasSaveFile(slot)) return null;

        try
        {
            string json = File.ReadAllText(GetSavePath(slot));
            return JsonConvert.DeserializeObject<SaveData>(json, JsonSettings);
        }
        catch (System.Exception e)
        {
            Debug.LogError("[SaveLoadManager] PeekSlotData failed for slot " + slot + ": " + e.Message);
            return null;
        }
    }

    // D-02 / D-03 entry point: "start a new game in slot N".
    // This MUST stay "select the slot, then reuse the existing NewGame() body" and must
    // NOT touch the file on disk. Phase 11 D-06 defers the overwrite to the next real
    // save trigger; writing here would wipe the slot before the player ever reaches a
    // checkpoint, which would make the D-04 confirmation dialog pointless.
    public void NewGameInSlot(int slot)
    {
        SelectSlot(slot);
        NewGame();
    }

    // D-01 entry point: "continue the game stored in slot N".
    public void LoadSlot(int slot)
    {
        SelectSlot(slot);
        LoadGame();
    }
```

**(2) 기존 `DebugLogState()` 의 첫 `Debug.Log(` 인자 줄에 슬롯 정보를 덧붙인다 (1줄 수정)**

기존:
```csharp
        Debug.Log("[SaveLoadManager] path=" + SavePath +
```
교체 후:
```csharp
        Debug.Log("[SaveLoadManager] slot=" + CurrentSlot +
                  " path=" + SavePath +
```

**(3) 파일 맨 끝 `DebugLogSettings()` 메서드 뒤, 클래스 닫는 중괄호 앞에 Phase14 훅 4개를 추가한다**

```csharp
    [ContextMenu("Phase14/1. Log All Slots")]
    private void DebugLogAllSlots()
    {
        for (int slot = 0; slot < SlotCount; slot++)
        {
            SaveData d = PeekSlotData(slot);
            Debug.Log("[SaveLoadManager] slot " + slot +
                      " path=" + GetSavePath(slot) +
                      " exists=" + HasSaveFile(slot) +
                      " scene=" + (d != null ? d.SceneName : "-") +
                      " bossProgress=" + (d != null && d.BossProgress != null ? d.BossProgress.Count : 0));
        }
        Debug.Log("[SaveLoadManager] CurrentSlot=" + CurrentSlot);
    }

    [ContextMenu("Phase14/2. Select Slot 0")]
    private void DebugSelectSlot0() { SelectSlot(0); Debug.Log("[SaveLoadManager] CurrentSlot=" + CurrentSlot); }

    [ContextMenu("Phase14/3. Select Slot 1")]
    private void DebugSelectSlot1() { SelectSlot(1); Debug.Log("[SaveLoadManager] CurrentSlot=" + CurrentSlot); }

    [ContextMenu("Phase14/4. Select Slot 2")]
    private void DebugSelectSlot2() { SelectSlot(2); Debug.Log("[SaveLoadManager] CurrentSlot=" + CurrentSlot); }
```

이 4개 훅은 UI 가 아직 씬에 배선되기 전(Plan 03 Task 3)에도 Play 모드에서 슬롯 1/2 에 실제
세이브 파일을 만들어 볼 수 있게 해주며, `Assets/SaveSystem/Check.md` 의 Phase 14 체크리스트가
이 훅을 전제로 작성된다.

**금지 사항 (게이트 충돌 방지):** 새 주석에 `File.WriteAllText`, `_data = `, `async`, `await`
문자열을 포함하지 말 것. 전부 ASCII 로만 작성할 것. `PeekSlotData` 안에서 `_data` 나
`CurrentSlot` 에 **대입하지 말 것**.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/SaveSystem/Script/SaveLoadManager.cs && echo "peek=$(grep -c 'public SaveData PeekSlotData(int slot)' $F) newinslot=$(grep -c 'public void NewGameInSlot(int slot)' $F) loadslot=$(grep -c 'public void LoadSlot(int slot)' $F) write=$(grep -c 'File.WriteAllText' $F) datassign=$(grep -c '_data = ' $F) hooks=$(grep -c 'ContextMenu("Phase14/' $F) deser=$(grep -c 'JsonConvert.DeserializeObject<SaveData>(json, JsonSettings)' $F) nonascii=$(grep -cP '[^\x00-\x7F]' $F) asyncq=$(grep -cE '\basync\b|\bawait\b' $F)"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'public SaveData PeekSlotData(int slot)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public void NewGameInSlot(int slot)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'public void LoadSlot(int slot)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'File.WriteAllText' Assets/SaveSystem/Script/SaveLoadManager.cs` == 2 — **핵심 회귀 게이트**: `NewGameInSlot` 이 디스크를 즉시 쓰지 않음을 증명 (Pitfall 3)
    - `grep -c '_data = ' Assets/SaveSystem/Script/SaveLoadManager.cs` == 3 — **핵심 회귀 게이트**: `PeekSlotData` 가 메모리 캐시를 오염시키지 않음을 증명 (Pitfall 2)
    - `grep -c 'JsonConvert.DeserializeObject<SaveData>(json, JsonSettings)' Assets/SaveSystem/Script/SaveLoadManager.cs` == 2 (`LoadGame` 1 + `PeekSlotData` 1 — 새 직렬화 경로를 만들지 않고 기존 호출을 그대로 재사용)
    - `grep -c 'ContextMenu("Phase14/' Assets/SaveSystem/Script/SaveLoadManager.cs` == 4
    - `grep -c '"\[SaveLoadManager\] slot=" + CurrentSlot' Assets/SaveSystem/Script/SaveLoadManager.cs` == 1
    - `grep -c 'ContextMenu("Phase11/' Assets/SaveSystem/Script/SaveLoadManager.cs` == 4 (Phase 11 훅 4개 그대로 보존)
    - `grep -cP '[^\x00-\x7F]' Assets/SaveSystem/Script/SaveLoadManager.cs` == 0
    - `grep -cE '\basync\b|\bawait\b' Assets/SaveSystem/Script/SaveLoadManager.cs` == 0
    - `git diff --numstat HEAD -- Assets/SaveSystem/Script/SaveLoadManager.cs` 의 삭제 라인 수 <= 5 (헤더 D-02 주석 1줄 + SavePath 프로퍼티 3줄 + DebugLogState 1줄 = 순수 삽입 위주 편집)
    - `git status --porcelain Assets/` 결과에 `SaveLoadManager.cs` **외의 파일이 없음**
  </acceptance_criteria>

  <done>
슬롯 선택 화면이 필요로 하는 3개 API 가 존재한다: `PeekSlotData(slot)` 은 파일을 읽어
`SaveData` 를 돌려주되 `_data`/`CurrentSlot` 을 건드리지 않고, `NewGameInSlot(slot)` 은
슬롯 선택 후 메모리만 리셋하며(디스크 미기록), `LoadSlot(slot)` 은 슬롯 선택 후 기존 로드
코루틴을 그대로 탄다. Inspector 톱니바퀴에 `Phase14/` 메뉴 4개가 노출된다.
  </done>
</task>

</tasks>

<verification>
1. 정적 게이트: 위 두 태스크의 acceptance_criteria 전 항목.
2. 파일 경로 매핑 검증 (Play 모드, Plan 03 체크포인트에서 함께 수행):
   `Phase14/1. Log All Slots` 실행 → Console 에 3줄이 뜨고 slot 0 의 path 가
   `...\save.json` (기존 파일명 그대로), slot 1/2 가 `...\save_1.json` / `...\save_2.json`.
3. Unity 컴파일 에러 0건 (Plan 03 체크포인트에서 확인 — 이 실행 환경에서는 컴파일 불가).
</verification>

<success_criteria>
- `SaveLoadManager` 가 3슬롯을 인식하고, 슬롯 0 이 기존 `save.json` 을 그대로 가리킨다 (D-06/D-07).
- 마이그레이션 코드가 0줄이다 — `File.Move`/`File.Copy`/`File.Delete` 가 이 파일에 존재하지 않는다.
- 저장 트리거 4곳(`Checkpoint.cs`, `TutorialBossController.cs`, `SpiritStats.cs`,
  `WaterMonsterStats.cs`)과 `SaveData.cs` 가 0줄 변경이다.
- `SaveLoadManager.cs` 가 순수 ASCII 로 유지된다.
</success_criteria>

<output>
완료 후 `.planning/phases/14-save-slot-expansion/14-01-SUMMARY.md` 작성.
</output>
