---
phase: 14-save-slot-expansion
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - Assets/Script/OverwriteConfirmPanel.cs
  - Assets/Script/SlotSelectPanel.cs
autonomous: true
requirements: [D-01, D-04, D-05]
must_haves:
  truths:
    - "슬롯 선택 화면이 3개 슬롯의 씬 이름과 격파 보스 수를 동시에 보여준다"
    - "빈 슬롯은 '빈 슬롯' + '새 게임 시작' 으로, 데이터 있는 슬롯은 씬 이름 + '격파 보스 N/3' 로 표시된다"
    - "이어하기 의도로 열었을 때 빈 슬롯 카드는 숨겨지지 않고 비활성(선택 불가)이 된다"
    - "데이터가 있는 슬롯을 새 게임 대상으로 고르면 예외 없이 덮어쓰기 확인창이 먼저 뜬다"
    - "확인창에서 '취소'를 누르면 어떤 상태도 바뀌지 않고 창만 닫힌다"
    - "화면을 다시 열 때마다 카드가 최신 디스크 상태로 갱신된다 (첫 활성화 시점 캐시가 아님)"
  artifacts:
    - path: "Assets/Script/OverwriteConfirmPanel.cs"
      provides: "D-04/D-05 덮어쓰기 확인 다이얼로그 (단일 목적)"
      contains: "이 슬롯을 덮어쓰고 새 게임을 시작하시겠습니까?"
      min_lines: 50
    - path: "Assets/Script/SlotSelectPanel.cs"
      provides: "3슬롯 카드 + Load/NewGame 의도 분기 + 확인창 라우팅"
      contains: "public enum Intent { Load, NewGame }"
      min_lines: 100
  key_links:
    - from: "Assets/Script/SlotSelectPanel.cs"
      to: "SaveLoadManager.PeekSlotData"
      via: "OnEnable 시 3슬롯 비파괴 조회"
      pattern: "PeekSlotData\\(slot\\)"
    - from: "Assets/Script/SlotSelectPanel.cs"
      to: "Assets/Script/OverwriteConfirmPanel.cs"
      via: "점유 슬롯 + NewGame 의도일 때 Open(slot, callback)"
      pattern: "overwriteConfirmPanel\\.Open\\(slot, StartNewGameInSlot\\);"
    - from: "Assets/Script/SlotSelectPanel.cs"
      to: "SaveLoadManager.LoadSlot / NewGameInSlot"
      via: "카드 클릭 확정 시 진입점 호출"
      pattern: "mgr\\.(LoadSlot|NewGameInSlot)\\(slot\\)"
---

<objective>
슬롯 선택 화면과 덮어쓰기 확인 다이얼로그를 신규 MonoBehaviour 2개로 작성한다.

Purpose: D-01(이어하기는 항상 슬롯 화면), D-04/D-05(덮어쓰기는 예외 없이 확인창)를 만족하는
UI 로직 계층. 이 프로젝트에는 확인 다이얼로그 패턴이 존재하지 않으므로(grep 실측: 게임플레이
코드에 Confirm/Dialog 컴포넌트 0건) **이 한 용도만을 위한 최소 컴포넌트**를 만든다 — 범용
모달 프레임워크를 만들지 않는다 (CLAUDE.md 2번 단순성 우선, 14-RESEARCH "Don't Hand-Roll").

Output: 신규 파일 2개. 기존 파일 0줄 변경. 씬 배선(GameObject 생성/OnClick 연결)은 이 플랜
범위 밖이며 Plan 03 의 체크포인트에서 사용자가 수행한다 (Phase 9 D-08 선례와 동일).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-save-slot-expansion/14-CONTEXT.md
@.planning/phases/14-save-slot-expansion/14-UI-SPEC.md
@.planning/phases/14-save-slot-expansion/14-01-SUMMARY.md
@Assets/Player/Script/Menu/GameSettingsPanel.cs
@Assets/Player/Script/Menu/PauseMenu.cs

<interfaces>
<!-- Plan 01 이 만든 계약. 이 블록만으로 작업 가능해야 한다. -->

Assets/SaveSystem/Script/SaveLoadManager.cs (Plan 01 완료 후):
```csharp
public static SaveLoadManager Instance { get; private set; }   // RuntimeInitializeOnLoadMethod 로 자동 생성됨
public const int SlotCount = 3;
public int CurrentSlot { get; private set; }
public string GetSavePath(int slot);
public void SelectSlot(int slot);
public bool HasSaveFile(int slot);       // 파일 존재 여부만 (파싱 실패해도 true)
public bool HasSaveFile();               // CurrentSlot 기준
public SaveData PeekSlotData(int slot);  // 비파괴 조회. 파일 없음 또는 파싱 실패 시 null
public void NewGameInSlot(int slot);     // SelectSlot + NewGame (메모리만, 디스크 미기록)
public void LoadSlot(int slot);          // SelectSlot + LoadGame (씬 비동기 로드 시작)
public SaveData Data { get; }
```

Assets/SaveSystem/Script/SaveData.cs (변경 없음):
```csharp
public class SaveData
{
    public int SaveVersion;
    public string SceneName;                        // 예: "1 stage", "Tutorial Map"
    public string SpawnPointName;
    public PlayerStatsSaveData PlayerStats;
    public Dictionary<string, bool> BossProgress;   // 격파 시에만 true 로 추가됨 -> Count == 격파 수
    public Dictionary<string, bool> MapGimmickState;
    public List<string> Items;
}
```

기존 프로젝트 UI 컨벤션 (그대로 따를 것):
```csharp
// Assets/Player/Script/Menu/PauseMenu.cs  — 패널 토글 방식 (씬 전환 아님)
private void Open()  { gameObject.SetActive(true);  }
private void Close() { gameObject.SetActive(false); }

// Assets/Player/Script/Menu/GameSettingsPanel.cs — 재활성화마다 최신 상태 반영
private void OnEnable()
{
    var s = SaveLoadManager.CurrentSettings;
    ...   // Start() 가 아니라 OnEnable() 인 이유: 재토글되는 패널의 Start() 는 1회만 실행됨
}
```
</interfaces>

<doc_drift_note>
**선행 문서 2곳에 스테일 값이 남아 있다 — 코드에 반영하지 말 것.**

`14-UI-SPEC.md` Copywriting Contract 의 슬롯 카드 body 행과 `14-RESEARCH.md` 296번째 줄은
격파 보스 표기를 `"격파 보스 {BossProgress.Count}/4"` 로 적고 있다. 이 **/4 는 틀렸다.**

Phase 11 당시에는 보스가 4종(TutorialBoss / WoodBoss / WaterSpirit / WaterMonster)이었으나,
Phase 13 정리 라운드(커밋 `9c752d2`)가 `Assets/Enemy/Tutorial/WoodBoss/` 디렉터리를 통째로
삭제했다. 이 저장소에서 실측한 결과:

```
$ grep -rn "SaveOnBossDefeated(" --include=*.cs Assets/ | grep -v SaveLoadManager.cs
Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs:304:  ... SaveOnBossDefeated("TutorialBoss");
Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs:77:          ... SaveOnBossDefeated("WaterMonster");
Assets/Enemy/WaterSpirit/Script/SpiritStats.cs:67:                  ... SaveOnBossDefeated("WaterSpirit");
```

`BossProgress` 에 4번째 키를 써넣을 수 있는 코드 경로가 존재하지 않으므로 카드가 "4/4"에
도달하는 것은 물리적으로 불가능하다. ROADMAP.md 의 Phase 14 성공 기준 #9 역시 0줄 변경 계약
파일을 보스 3종으로 명시하고 있다.

따라서 `TotalBossCount` 는 **3** 이고 표시 문구는 `"격파 보스 N/3"` 이다.
UI-SPEC/RESEARCH 의 `/4` 를 보고 코드를 4로 "고치지" 말 것 — 그 두 문서는 이 페이즈에서
재실행되지 않는 참고 문서이며, 이 플랜이 최신 실측 기준이다.
</doc_drift_note>

<encoding_note>
두 파일 모두 **신규 생성**이므로 인코딩 사고 위험이 없다. **UTF-8 로 저장할 것**
(기존 `PauseMenu.cs`/`GameSettingsPanel.cs` 와 동일). 한글 리터럴은 UI-SPEC 의 문구를
**한 글자도 바꾸지 말고 그대로** 넣는다 — 인수 기준이 정확한 문자열 일치를 요구한다.

`.meta` 파일은 직접 만들지 않는다. Unity 에디터가 다음 실행 시 자동 생성한다.
</encoding_note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: OverwriteConfirmPanel.cs 신규 — D-04/D-05 덮어쓰기 확인 다이얼로그</name>

  <files>Assets/Script/OverwriteConfirmPanel.cs</files>

  <read_first>
    - .planning/phases/14-save-slot-expansion/14-UI-SPEC.md ("Copywriting Contract" 표 — 확인창 문구 4종이 여기서 잠금됨)
    - .planning/phases/14-save-slot-expansion/14-CONTEXT.md (D-04, D-05)
    - Assets/Player/Script/Menu/PauseMenu.cs (SetActive 기반 패널 토글 컨벤션)
  </read_first>

  <action>
`Assets/Script/OverwriteConfirmPanel.cs` 를 신규 생성한다. 전체 내용은 다음과 같다
(문구는 14-UI-SPEC.md 의 Copywriting Contract 에서 잠긴 값을 그대로 옮긴 것):

```csharp
using UnityEngine;
using TMPro;

// Phase 14 D-04 / D-05: "occupied slot -> new game" overwrite confirmation.
// Deliberately single-purpose. This is NOT a reusable modal framework: nothing else in
// the project needs one yet (14-RESEARCH "Don't Hand-Roll" / CLAUDE.md simplicity rule).
public class OverwriteConfirmPanel : MonoBehaviour
{
    // 14-UI-SPEC Copywriting Contract. Kept as consts (not Inspector strings) so the
    // locked D-04 wording cannot drift by hand-editing a TMP_Text in the scene.
    public const string TitleCopy   = "슬롯 덮어쓰기";
    public const string BodyCopy    = "이 슬롯을 덮어쓰고 새 게임을 시작하시겠습니까?";
    public const string ConfirmCopy = "덮어쓰고 시작";
    public const string CancelCopy  = "취소";

    [Header("Texts")]
    public TMP_Text titleText;
    public TMP_Text bodyText;
    public TMP_Text confirmButtonText;
    public TMP_Text cancelButtonText;

    private int _pendingSlot = -1;
    private System.Action<int> _onConfirm;

    // GameSettingsPanel convention: refresh on every (re-)activation, not in Start().
    private void OnEnable()
    {
        if (titleText          != null) titleText.text          = TitleCopy;
        if (bodyText           != null) bodyText.text           = BodyCopy;
        if (confirmButtonText  != null) confirmButtonText.text  = ConfirmCopy;
        if (cancelButtonText   != null) cancelButtonText.text   = CancelCopy;
    }

    // Called by SlotSelectPanel right before the panel becomes visible.
    public void Open(int slot, System.Action<int> onConfirm)
    {
        _pendingSlot = slot;
        _onConfirm = onConfirm;
        gameObject.SetActive(true);
    }

    // "덮어쓰고 시작" Button OnClick. Closes first, then invokes, so the callback is free
    // to start a scene load without this panel still being active on the way out.
    public void OnClickConfirm()
    {
        int slot = _pendingSlot;
        System.Action<int> callback = _onConfirm;
        Close();
        if (slot >= 0 && callback != null) callback(slot);
    }

    // "취소" Button OnClick. D-05: the safe path changes nothing at all.
    public void OnClickCancel()
    {
        Close();
    }

    private void Close()
    {
        _pendingSlot = -1;
        _onConfirm = null;
        gameObject.SetActive(false);
    }
}
```

**설계상 반드시 지켜야 하는 것들:**
- 이 컴포넌트는 `SaveLoadManager` 를 **직접 호출하지 않는다**. 무엇을 덮어쓸지는 호출자
  (`SlotSelectPanel`)가 콜백으로 넘긴다 — 확인창이 세이브 로직을 알면 안 된다.
- `_pendingSlot` 기본값이 `-1` 이고 `OnClickConfirm` 이 `slot >= 0` 을 확인하므로,
  `Open()` 없이 패널이 그냥 활성화돼도 아무 일도 일어나지 않는다.
- 확인 버튼 라벨은 `"확인"` 이 아니라 `"덮어쓰고 시작"` 이다 (14-UI-SPEC 이 명시적으로
  일반 라벨 "확인"을 거부하고 행동 명시 라벨로 잠금).
- 색상/레이아웃(파괴적 빨강 `#8C2E2E`/`#B84747`/`#611F1F`, 배경 `#0F141F` 96%, 버튼 최소
  높이 64px, 패널 내부 여백 24px)은 **코드가 아니라 씬의 Image/Button 컴포넌트**에서 설정한다.
  Plan 03 의 Check.md 배선 가이드가 이 값들을 다룬다 — 이 태스크에서 코드로 색을 칠하지 말 것.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Script/OverwriteConfirmPanel.cs && test -f $F && echo "body=$(grep -c '이 슬롯을 덮어쓰고 새 게임을 시작하시겠습니까?' $F) title=$(grep -c '슬롯 덮어쓰기' $F) confirm=$(grep -c '덮어쓰고 시작' $F) okgeneric=$(grep -c '= "확인"' $F) open=$(grep -c 'public void Open(int slot, System.Action<int> onConfirm)' $F) mgr=$(grep -c 'SaveLoadManager' $F) lines=$(wc -l < $F)"</automated>
  </verify>

  <acceptance_criteria>
    - `Assets/Script/OverwriteConfirmPanel.cs` 파일이 존재한다
    - `grep -c 'public const string BodyCopy    = "이 슬롯을 덮어쓰고 새 게임을 시작하시겠습니까?";' Assets/Script/OverwriteConfirmPanel.cs` == 1 (D-04 잠금 문구 정확 일치)
    - `grep -c '"슬롯 덮어쓰기"' Assets/Script/OverwriteConfirmPanel.cs` == 1
    - `grep -c '"덮어쓰고 시작"' Assets/Script/OverwriteConfirmPanel.cs` == 1
    - `grep -c '"취소"' Assets/Script/OverwriteConfirmPanel.cs` == 1
    - `grep -c '= "확인"' Assets/Script/OverwriteConfirmPanel.cs` == 0 (UI-SPEC 이 거부한 일반 라벨 미사용)
    - `grep -c 'public void Open(int slot, System.Action<int> onConfirm)' Assets/Script/OverwriteConfirmPanel.cs` == 1
    - `grep -c 'public void OnClickConfirm()' Assets/Script/OverwriteConfirmPanel.cs` == 1
    - `grep -c 'public void OnClickCancel()' Assets/Script/OverwriteConfirmPanel.cs` == 1
    - `grep -c 'SaveLoadManager' Assets/Script/OverwriteConfirmPanel.cs` == 0 (확인창은 세이브 로직을 모른다)
    - `grep -c 'private void OnEnable()' Assets/Script/OverwriteConfirmPanel.cs` == 1 (Start() 아님 — 재토글 패널 컨벤션)
    - `grep -c 'gameObject.SetActive(false);' Assets/Script/OverwriteConfirmPanel.cs` == 1 (Close() 단일 경로)
    - `git status --porcelain Assets/` 결과에 `OverwriteConfirmPanel.cs` 외의 항목이 없다
  </acceptance_criteria>

  <done>
`OverwriteConfirmPanel` 이 존재하고, `Open(slot, callback)` 으로 열려 "덮어쓰고 시작" 시
콜백을 슬롯 인덱스와 함께 호출하며, "취소" 시 아무 상태도 바꾸지 않고 닫힌다. UI-SPEC 의
4개 문구가 코드 상수로 고정되어 있다.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SlotSelectPanel.cs 신규 — 3슬롯 카드 + Load/NewGame 의도 분기</name>

  <files>Assets/Script/SlotSelectPanel.cs</files>

  <read_first>
    - Assets/Script/OverwriteConfirmPanel.cs (Task 1 산출물 — Open 시그니처를 실제로 확인할 것)
    - Assets/SaveSystem/Script/SaveLoadManager.cs (Plan 01 반영 후 — PeekSlotData/HasSaveFile(int)/LoadSlot/NewGameInSlot 실제 시그니처 확인)
    - Assets/SaveSystem/Script/SaveData.cs (SceneName / BossProgress 필드 확인)
    - .planning/phases/14-save-slot-expansion/14-UI-SPEC.md (Copywriting Contract — 카드 문구 6종. **단, 이 문서의 "격파 보스 N/4" 는 스테일 값이다 — 위 doc_drift_note 참고, 실제 값은 3**)
    - Assets/Player/Script/Menu/GameSettingsPanel.cs (OnEnable 갱신 컨벤션)
  </read_first>

  <action>
`Assets/Script/SlotSelectPanel.cs` 를 신규 생성한다. 전체 내용은 다음과 같다:

```csharp
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;
using TMPro;

// Phase 14 D-01 ~ D-05: main-menu slot select screen (3 cards).
// Same-scene toggled panel, mirroring PauseMenu / GameSettingsPanel - not a separate scene.
public class SlotSelectPanel : MonoBehaviour
{
    // Which button the player pressed to get here. The same 3 cards behave differently:
    // Load  -> picking an occupied slot continues it; empty cards are disabled.
    // NewGame -> picking ANY slot starts fresh; occupied ones must clear D-04 first.
    public enum Intent { Load, NewGame }

    // 14-UI-SPEC Copywriting Contract.
    public const string TitleCopy         = "슬롯 선택";
    public const string EmptyBodyCopy     = "빈 슬롯";
    public const string CorruptHeadCopy   = "세이브 데이터를 읽을 수 없습니다";
    public const string CorruptBodyCopy   = "파일이 손상되었을 수 있습니다. 이 슬롯에 새 게임을 시작해주세요.";
    public const string NewGameCtaCopy    = "새 게임 시작";
    public const string ContinueCtaCopy   = "이어하기";
    public const string BossProgressLabel = "격파 보스 ";

    // TutorialBoss / WaterSpirit / WaterMonster - the only ids any code path can ever
    // write into BossProgress. Phase 11 shipped a 4th (WoodBoss), but Phase 13 cleanup
    // (commit 9c752d2) deleted Assets/Enemy/Tutorial/WoodBoss/ entirely, so "N/4" could
    // never reach 4/4. Verified by grep: SaveOnBossDefeated( has exactly 3 call sites.
    public const int TotalBossCount = 3;

    [Header("Panel")]
    public TMP_Text titleText;

    [Header("Slot cards - array index IS the slot index (size 3)")]
    public Button[] slotButtons = new Button[3];
    public TMP_Text[] slotLabelTexts = new TMP_Text[3];
    public TMP_Text[] slotBodyTexts = new TMP_Text[3];
    public TMP_Text[] slotCtaTexts = new TMP_Text[3];

    [Header("Overwrite confirm (D-04)")]
    public OverwriteConfirmPanel overwriteConfirmPanel;

    [Header("New game target scene")]
    [SerializeField] private string newGameSceneName = "Tutorial Map";

    private Intent _intent = Intent.Load;

    // D-01: "이어하기" always lands here. Nothing auto-loads.
    public void OpenForLoad()
    {
        _intent = Intent.Load;
        gameObject.SetActive(true);
        RefreshAllCards();
    }

    // D-03: reached only when all 3 slots are occupied.
    public void OpenForNewGame()
    {
        _intent = Intent.NewGame;
        gameObject.SetActive(true);
        RefreshAllCards();
    }

    // Back Button OnClick
    public void OnClickBack()
    {
        gameObject.SetActive(false);
    }

    // GameSettingsPanel convention: re-read disk state on every activation, so a slot
    // saved during the previous play session is never shown from a stale first-open cache.
    private void OnEnable()
    {
        RefreshAllCards();
    }

    private void RefreshAllCards()
    {
        if (titleText != null) titleText.text = TitleCopy;
        for (int slot = 0; slot < SaveLoadManager.SlotCount; slot++)
        {
            RefreshCard(slot);
        }
    }

    private void RefreshCard(int slot)
    {
        SaveLoadManager mgr = SaveLoadManager.Instance;
        bool fileExists = mgr != null && mgr.HasSaveFile(slot);

        // Peek, never LoadGame: displaying a card must not disturb the live save state.
        SaveData data = fileExists ? mgr.PeekSlotData(slot) : null;

        SetText(slotLabelTexts, slot, "슬롯 " + (slot + 1));

        string body;
        string cta;
        if (!fileExists)
        {
            body = EmptyBodyCopy;
            cta = NewGameCtaCopy;
        }
        else if (data == null)
        {
            // File exists but will not parse. Treated as overwrite-eligible, not a dead end.
            body = CorruptHeadCopy + "\n" + CorruptBodyCopy;
            cta = NewGameCtaCopy;
        }
        else
        {
            int bossCount = data.BossProgress != null ? data.BossProgress.Count : 0;
            body = data.SceneName + "\n" + BossProgressLabel + bossCount + "/" + TotalBossCount;
            cta = _intent == Intent.Load ? ContinueCtaCopy : NewGameCtaCopy;
        }

        SetText(slotBodyTexts, slot, body);
        SetText(slotCtaTexts, slot, cta);

        // Load intent: empty / unreadable cards are disabled, NOT hidden - matching the
        // existing MainMenuUI loadGameButton.interactable treatment.
        bool interactable = _intent == Intent.NewGame || data != null;
        if (slot < slotButtons.Length && slotButtons[slot] != null)
        {
            slotButtons[slot].interactable = interactable;
        }
    }

    private static void SetText(TMP_Text[] arr, int index, string value)
    {
        if (arr != null && index < arr.Length && arr[index] != null) arr[index].text = value;
    }

    // Each slot card Button OnClick is wired to this with its own index (0 / 1 / 2).
    public void OnClickSlot(int slot)
    {
        SaveLoadManager mgr = SaveLoadManager.Instance;
        if (mgr == null)
        {
            Debug.LogError("[SlotSelectPanel] SaveLoadManager.Instance is null.");
            return;
        }

        if (_intent == Intent.Load)
        {
            if (!mgr.HasSaveFile(slot)) return;   // defensive: card should already be disabled
            gameObject.SetActive(false);
            mgr.LoadSlot(slot);                   // D-01
            return;
        }

        if (mgr.HasSaveFile(slot))
        {
            // D-04 / D-05: an occupied slot ALWAYS goes through the confirm dialog.
            // There is no branch here that overwrites without asking.
            if (overwriteConfirmPanel == null)
            {
                Debug.LogError("[SlotSelectPanel] overwriteConfirmPanel is not assigned - refusing to overwrite slot " + slot + ".");
                return;
            }
            overwriteConfirmPanel.Open(slot, StartNewGameInSlot);
            return;
        }

        StartNewGameInSlot(slot);
    }

    private void StartNewGameInSlot(int slot)
    {
        SaveLoadManager mgr = SaveLoadManager.Instance;
        if (mgr == null)
        {
            Debug.LogError("[SlotSelectPanel] SaveLoadManager.Instance is null.");
            return;
        }
        gameObject.SetActive(false);
        mgr.NewGameInSlot(slot);                  // memory reset only; disk write is deferred
        SceneManager.LoadScene(newGameSceneName);
    }
}
```

**설계상 반드시 지켜야 하는 것들:**
- 손상된 세이브 카드는 UI-SPEC 오류 상태 계약대로 **2줄**(`CorruptHeadCopy` + `\n` +
  `CorruptBodyCopy`)로 렌더링한다 — 정상 슬롯 카드가 `SceneName` + `\n` + 진행도로 2줄인 것과
  같은 형태다. 파일은 존재하지만 파싱에 실패한 슬롯은 막다른 길이 아니라 **덮어쓰기 가능한**
  슬롯으로 취급하므로 CTA 는 "새 게임 시작" 이다.
- 격파 보스 총합은 `TotalBossCount` **3** 이다 (보스 3종). 하드코딩 `"/4"` 를 쓰지 말 것.
- `PeekSlotData` 만 쓰고 `LoadGame()`/`Data` 로 카드를 채우지 말 것 (14-RESEARCH Pitfall 2 —
  카드 표시가 진행 중인 메모리 캐시를 오염시키면 안 된다).
- `OnClickSlot` 의 NewGame 분기에서 `HasSaveFile(slot) == true` 인데 확인창을 건너뛰는
  경로를 **절대 만들지 말 것** (D-05). `overwriteConfirmPanel` 이 미배선이면 덮어쓰지 않고
  에러 로그만 남기고 반환한다.
- `_intent` 를 `SetActive(true)` **전에** 설정한다 — `OnEnable()` 이 그 시점에 이미 올바른
  의도로 카드를 그린다. `RefreshAllCards()` 중복 호출은 이미 활성 상태인 패널을 다시 열 때를
  위한 저렴한 방어책이며 무해하다.
- 색/여백/폰트 크기는 코드가 아니라 씬에서 설정한다 (Plan 03 배선 가이드).
- `newGameSceneName` 기본값 `"Tutorial Map"` 은 기존 `MainMenuUI.OnClickStart()` 가 쓰던
  씬 이름과 동일하며, `ProjectSettings/EditorBuildSettings.asset` 에 등록되어 있음을 확인했다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Script/SlotSelectPanel.cs && test -f $F && echo "intent=$(grep -c 'public enum Intent { Load, NewGame }' $F) peek=$(grep -c 'PeekSlotData(slot)' $F) loadgame=$(grep -c 'LoadGame()' $F) confirmopen=$(grep -c 'overwriteConfirmPanel.Open(slot, StartNewGameInSlot);' $F) loadslot=$(grep -c 'mgr.LoadSlot(slot);' $F) newinslot=$(grep -c 'mgr.NewGameInSlot(slot);' $F) onenable=$(grep -c 'private void OnEnable()' $F) slotcount=$(grep -c 'SaveLoadManager.SlotCount' $F) lines=$(wc -l < $F)"</automated>
  </verify>

  <acceptance_criteria>
    - `Assets/Script/SlotSelectPanel.cs` 파일이 존재한다
    - `grep -c 'public enum Intent { Load, NewGame }' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c 'public void OpenForLoad()' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c 'public void OpenForNewGame()' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c 'public void OnClickSlot(int slot)' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c 'PeekSlotData(slot)' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c 'LoadGame()' Assets/Script/SlotSelectPanel.cs` == 0 — **핵심 게이트**: Pitfall 2 (카드 표시가 LoadGame 을 부르면 안 됨)
    - `grep -c 'overwriteConfirmPanel.Open(slot, StartNewGameInSlot);' Assets/Script/SlotSelectPanel.cs` == 1 — **D-04 핵심 링크**
    - `grep -c 'mgr.NewGameInSlot(slot);' Assets/Script/SlotSelectPanel.cs` == 1 (덮어쓰기 실행 경로가 정확히 1곳 — 확인 콜백 뒤에만 존재)
    - `grep -c 'mgr.LoadSlot(slot);' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c 'SaveLoadManager.SlotCount' Assets/Script/SlotSelectPanel.cs` == 1 (하드코딩 3 대신 상수 사용)
    - `grep -c 'private void OnEnable()' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"슬롯 선택"' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"빈 슬롯"' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"새 게임 시작"' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"이어하기"' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"격파 보스 "' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"세이브 데이터를 읽을 수 없습니다"' Assets/Script/SlotSelectPanel.cs` == 1
    - `grep -c '"파일이 손상되었을 수 있습니다. 이 슬롯에 새 게임을 시작해주세요."' Assets/Script/SlotSelectPanel.cs` == 1 (UI-SPEC 오류 상태 2줄 계약)
    - `grep -c 'public const int TotalBossCount = 3;' Assets/Script/SlotSelectPanel.cs` == 1 (보스 3종 — 4 아님, 아래 doc_drift_note 참고)
    - `grep -c 'TotalBossCount = 4' Assets/Script/SlotSelectPanel.cs` == 0 (스테일 값 재유입 방지)
    - `grep -c 'newGameSceneName = "Tutorial Map"' Assets/Script/SlotSelectPanel.cs` == 1
    - `git status --porcelain Assets/` 결과가 `Assets/Script/OverwriteConfirmPanel.cs` 와 `Assets/Script/SlotSelectPanel.cs` 두 개만 (기존 파일 0줄 변경)
  </acceptance_criteria>

  <done>
`SlotSelectPanel` 이 3개 카드를 `PeekSlotData` 로 채우고(빈 슬롯/손상 슬롯/정상 슬롯 3가지
표시 상태), 이어하기 의도에서는 빈 카드를 비활성화하며, 새 게임 의도에서 점유 슬롯을 고르면
반드시 `OverwriteConfirmPanel` 을 거쳐서만 `NewGameInSlot` 에 도달한다.
  </done>
</task>

</tasks>

<verification>
1. 정적 게이트: 위 두 태스크의 acceptance_criteria 전 항목.
2. 덮어쓰기 무확인 경로 부재 증명:
   `grep -n "NewGameInSlot" Assets/Script/SlotSelectPanel.cs` 의 유일한 호출이
   `StartNewGameInSlot` 안에 있고, `StartNewGameInSlot` 의 호출자가 (a) 확인창 콜백,
   (b) `HasSaveFile(slot) == false` 인 빈 슬롯 경로 — 정확히 2곳뿐이다.
3. Unity 컴파일 에러 0건 (Plan 03 체크포인트에서 확인).
</verification>

<success_criteria>
- 신규 파일 2개가 존재하고, 기존 파일은 0줄 변경이다.
- D-04/D-05 위반 경로(확인 없이 점유 슬롯 덮어쓰기)가 코드 상 존재하지 않는다.
- 슬롯 카드 표시가 `SaveData` 기존 필드(`SceneName`, `BossProgress.Count`)만 사용한다 —
  신규 스키마 필드 0개 (CONTEXT.md Deferred Ideas 준수).
</success_criteria>

<output>
완료 후 `.planning/phases/14-save-slot-expansion/14-02-SUMMARY.md` 작성.
</output>
