---
phase: 20-audio-centralization
plan: 06
type: execute
wave: 5
depends_on: ["20-04", "20-05"]
files_modified:
  - Assets/Audio/Check.md
autonomous: false
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "The project compiles in Unity with zero errors after the whole phase"
    - "All new .cs and asset files have Unity-generated .meta files committed"
    - "A verification checklist exists that a human can walk through in Play mode"
    - "The user has confirmed volume sliders, environment filtering and persistence actually work at runtime"
  artifacts:
    - path: "Assets/Audio/Check.md"
      provides: "Static regression results + Play mode checklist for the audio system"
  key_links:
    - from: "Unity batchmode import"
      to: "Assets/Audio + Assets/Script"
      via: "compile gate, zero error CS"
      pattern: "error CS"
---

<objective>
Prove the phase actually works: run a Unity batch-mode import/compile gate, commit the `.meta` files Unity generated for the new assets, write `Assets/Audio/Check.md`, and get human confirmation in Play mode.

Purpose: Every prior plan in this phase was verified by grep alone. Nothing has been compiled by Unity and nothing has produced a sound. This plan closes that gap, mirroring the pattern Phase 17 Plan 02 established (batch import gate, then Check.md, then a human Play-mode checkpoint).
Output: `Assets/Audio/Check.md` plus committed `.meta` files.
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-audio-centralization/20-CONTEXT.md
@.planning/phases/20-audio-centralization/20-04-SUMMARY.md
@.planning/phases/20-audio-centralization/20-05-SUMMARY.md
@Assets/Item/Check.md
@Assets/Script/AudioManager.cs
@Assets/Script/PersistentManagers.cs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unity batch-mode import and compile gate</name>
  <files>Assets/Audio/Script/AudioCue.cs.meta, Assets/Script/PersistentManagers.cs.meta, Assets/Audio.meta, Assets/Audio/Script.meta, Assets/Audio/Resources.meta</files>
  <read_first>
    - .planning/phases/17-*/17-02-SUMMARY.md — the exact Unity 6000.3.10f1 batch-mode invocation that worked in this repo (`-batchmode -quit -nographics -projectPath`), including the editor executable path and how the log was scanned. Reuse it verbatim rather than re-deriving it.
    - .planning/phases/20-audio-centralization/20-04-SUMMARY.md — whether the scene-cleanup batch run already succeeded, which confirms the invocation still works.
  </read_first>
  <action>
Run the Unity editor in batch mode against the project so it imports the new assets, generates `.meta` files, and compiles all scripts:

```
<UnityPath>/Unity.exe -batchmode -quit -nographics -projectPath "C:/Users/MSI/Projeect_A.E/Projeect_A.E" -logFile "$SCRATCH/phase20-import.log"
```

Then:
1. Confirm the process exit code is 0.
2. Scan the log for compile errors: any line matching `error CS`. There must be zero. If there are any, fix the source and re-run — do not proceed to Task 2 with a red compile.
3. Scan the log for lines matching `Missing` / `missing script` referencing the two scenes edited in Plan 20-04. There must be zero.
4. Scan the log for `DontDestroyOnLoad only works for root GameObjects`. There must be zero — its presence would mean a nested persistent object still exists.
5. `git status --porcelain Assets` will now show the `.meta` files Unity generated. Commit exactly these and nothing else:
   - `Assets/Audio.meta`, `Assets/Audio/Script.meta`, `Assets/Audio/Resources.meta`
   - `Assets/Audio/Script/AudioCue.cs.meta`
   - `Assets/Script/PersistentManagers.cs.meta`
   - `Assets/Audio/Resources/Master.mixer.meta` if Plan 20-01 did not already commit it
6. If Unity re-serialized unrelated files (Phase 17 saw `ProjectSettings/EditorBuildSettings.asset` get a newline normalisation with no content diff), do NOT commit them. Record the observation in Check.md instead, exactly as Phase 17 Plan 02 did.

Note the batch run cannot enter Play mode, so it proves compilation and import only. Runtime behaviour is Task 3's job.
  </action>
  <verify>
    <automated>cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && grep -c "error CS" "$SCRATCH/phase20-import.log"; test -f Assets/Audio/Script/AudioCue.cs.meta && test -f Assets/Script/PersistentManagers.cs.meta && echo metas-present</automated>
  </verify>
  <acceptance_criteria>
    - Unity exit code is 0.
    - `grep -c "error CS" "$SCRATCH/phase20-import.log"` returns 0.
    - `grep -ci "DontDestroyOnLoad only works for root" "$SCRATCH/phase20-import.log"` returns 0.
    - These files all exist: `Assets/Audio.meta`, `Assets/Audio/Script.meta`, `Assets/Audio/Resources.meta`, `Assets/Audio/Script/AudioCue.cs.meta`, `Assets/Script/PersistentManagers.cs.meta`, `Assets/Audio/Resources/Master.mixer.meta`.
    - `git ls-files Assets/Audio/Script/AudioCue.cs.meta Assets/Script/PersistentManagers.cs.meta` lists both (they are committed, not just present).
    - `git status --porcelain ProjectSettings` is empty (no unrelated ProjectSettings churn committed).
  </acceptance_criteria>
  <done>Unity imports the phase's new assets and compiles the whole project with zero errors, and every new file's generated .meta is committed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write Assets/Audio/Check.md</name>
  <files>Assets/Audio/Check.md</files>
  <read_first>
    - Assets/Item/Check.md — the format established by Phase 17: a static-regression results section with PASS rows, a numbered Play-mode checklist with unchecked boxes, an import-side-effects section, and a known-limitations section. Mirror that structure exactly.
    - All five prior SUMMARY files in `.planning/phases/20-audio-centralization/` — for the actual static results to record and for the deviations each plan reported.
  </read_first>
  <action>
Create `Assets/Audio/Check.md` with four sections. Do not hand-write its `.meta`; if Unity has not generated one yet, note it and let the next import create it.

**Section 1 — Static regression results (filled in now, all PASS/FAIL recorded honestly).** One row per item, with the command used and the observed value:
- `grep -rn "PlaySFX" --include=*.cs Assets/` returns 0 lines (D-10 cleanup; the method had zero call sites).
- `grep -rln "AudioManager" --include=*.unity Assets/` returns 0 files (D-13 scene cleanup).
- `grep -c "DontDestroyOnLoad" Assets/Script/AudioManager.cs` returns 0; the same grep on `PersistentManagers.cs` returns 1.
- `grep -c "Instantiate" Assets/Script/AudioManager.cs` returns 0 (D-06 fixed pool).
- `grep -c "Mathf.Max(linear, 0.0001f)" Assets/Script/AudioManager.cs` returns 1 (D-10 dB clamp).
- `SetBGMVolume(float value)` / `SetSFXVolume(float value)` signatures unchanged; `SoundSettingsPanel.cs` diff empty (D-10).
- `EnvironmentManager.cs` contains no `bgmSource` / `lowPassFilter` / `ChangeBGMCutoff` and zero U+FFFD bytes (D-12, CP949 gate).
- `Assets/Player/Script/InputHandler.cs` diff empty (BUG-007 out of scope, untouched).
- Unity batch compile: 0 `error CS`.

**Section 2 — Play mode checklist (unchecked boxes, for the user).** Number these and state the expected result for each:
1. Enter Play mode in `Tutorial Map`. Console shows no `DontDestroyOnLoad only works for root GameObjects` warning and no `Master.mixer not found` error.
2. In the Hierarchy, a root GameObject named `PersistentManagers` exists under `DontDestroyOnLoad`, carrying an `AudioManager` component, with 16 child `PooledSource_0..15` GameObjects and one `BGMSource` child.
3. No second `AudioManager` exists anywhere in the Hierarchy.
4. Open the pause menu > Sound tab. Drag the BGM slider. The `Master.mixer`'s `BGM` group volume moves in the Audio Mixer window and the audible BGM level changes.
5. Drag the BGM slider fully to 0. Audio is silent, and the mixer shows -80 dB, not `-Infinity` / `NaN`.
6. Drag the SFX slider. The `SFX` group volume moves.
7. Change the water level so `EnvironmentManager` crosses a threshold (ratio above 0.66, between 0.33 and 0.66, below 0.33). The BGM audibly muffles/unmuffles at each transition and the background sprites recolour — same behaviour as before this phase.
8. The BGM does NOT restart from the beginning at a state transition (the song keeps playing through).
9. Transition to another scene. `PersistentManagers` survives with the same `AudioManager` instance, and the BGM/SFX slider values are still what you set (not reset to 100%).
10. Return to the pause menu after the scene change. The sliders still show your values and still work.

**Section 3 — Import side effects.** Record anything Unity re-serialized during Task 1 that was deliberately not committed.

**Section 4 — Known limitations.** Record these three honestly:
- Eleven `.unity` files still contain inert serialized `bgmSource` / `lowPassFilter` references for `EnvironmentManager`; the C# fields are gone, so they are ignored at runtime and Unity will strip them the next time each scene is saved. Deliberately not hand-cleaned (Plan 20-05).
- No `AudioCue` `.asset` instances exist yet and no gameplay code calls `Play(...)`. Wiring real monster/attack/hit/boss sounds is explicitly future work, not this phase. The pool and cooldown/steal logic therefore have no Play-mode coverage beyond compiling.
- `InputHandler` is still not on `PersistentManagers`; BUG-007 remains open by design.
  </action>
  <verify>
    <automated>cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && test -f Assets/Audio/Check.md && grep -c "^- \[ \]\|^[0-9]*\. " Assets/Audio/Check.md</automated>
  </verify>
  <acceptance_criteria>
    - `Assets/Audio/Check.md` exists.
    - It contains all four section headings: `grep -c "^## "` returns at least 4.
    - The Play-mode checklist has at least 10 numbered or checkbox items.
    - It mentions all three known limitations: `grep -ci "BUG-007"` >= 1, `grep -ci "AudioCue"` >= 1, `grep -ci "bgmSource"` >= 1.
    - Every static-regression row states an actual observed value, not a placeholder: `grep -ci "TBD\|TODO\|N/A"` returns 0.
    - No item is marked PASS that was not actually run — cross-check each listed command by re-running it.
  </acceptance_criteria>
  <done>Assets/Audio/Check.md records the real static regression results, a 10-item Play mode checklist, import side effects, and the three known limitations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Play mode verification</name>
  <read_first>
    - Assets/Audio/Check.md — written in Task 2. Its Section 2 checklist is the exact script the user follows; keep it open to transcribe their per-item verdict back into it.
    - .planning/STATE.md — the Session Continuity entry for 2026-09-14, which records that Console Error Pause must be turned off before entering Play mode because BUG-008 throws and auto-pauses.
  </read_first>
  <what-built>
    The full Phase 20 audio infrastructure: `AudioCue` ScriptableObject, `Master.mixer` with Master/BGM/SFX/UI groups and three exposed dB parameters, a 16-voice `AudioSource` pool with per-cue cooldown and priority stealing, mixer-backed volume control preserving the existing `SetBGMVolume`/`SetSFXVolume` signatures, `AudioManager.SetEnvironmentState` owning BGM playback and the low-pass filter, and a `PersistentManagers` bootstrap that creates `AudioManager` on a parentless `DontDestroyOnLoad` root before any scene loads.

    Everything verifiable without a running game has already been automated: grep gates on every plan, and a Unity batch-mode compile/import gate with zero errors. What remains genuinely requires hearing sound and watching the Hierarchy.
  </what-built>
  <how-to-verify>
    Open Unity, load `Assets/Scenes/Tutorial Map.unity`, and walk through the 10-item Play mode checklist in `Assets/Audio/Check.md` (Section 2). Before pressing Play, turn OFF Console > Error Pause — STATE.md records that BUG-008 throws an exception that auto-pauses Play mode and will interrupt you.

    Report each item as pass or fail. Items 4-6 need the Audio Mixer window open (Window > Audio > Audio Mixer, select `Master`) so you can watch the group volumes move as you drag the sliders.
  </how-to-verify>
  <resume-signal>Report results per item, e.g. "1-6 pass, 7 fails: filter never changes", or "all pass".</resume-signal>
  <files>Assets/Audio/Check.md</files>
  <action>
Present the 10-item checklist from `Assets/Audio/Check.md` Section 2 to the user, with the Error Pause warning first. Wait for their report.

On receiving it, edit `Assets/Audio/Check.md`:
- Tick `[x]` only the items the user explicitly reported as passing.
- Leave failing items unticked and append the user's exact failure description beside them.
- Mark any item the user skipped as `검증 생략` — never as PASS. This follows the Phase 9 Plan 3 precedent recorded in STATE.md, where a skipped Play-mode verification was deliberately not falsified.
- Append a `## Play 모드 검증 결과` subsection with the date and the per-item verdict.

For any failure, write a bug note at `.planning/phases/20-audio-centralization/bugs/BUG-0XX-<slug>.md` describing the symptom and the suspected plan/task. Do NOT attempt a fix inside this checkpoint — a fix is a new plan.
  </action>
  <verify>
    <automated>cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && grep -c "Play 모드 검증 결과" Assets/Audio/Check.md</automated>
  </verify>
  <acceptance_criteria>
    - Every checkbox in Check.md Section 2 is updated to reflect the user's actual report — checked for pass, left unchecked with a failure note for fail.
    - A "results" subsection is appended to Check.md recording the date and the verdict per item.
    - No item is marked passed without an explicit user report. If the user skips items, they are recorded as "검증 생략" (verification skipped), following the precedent set in Phase 9 Plan 3 — never as PASS.
    - Any failure is filed as a bug note under `.planning/phases/20-audio-centralization/bugs/` rather than silently fixed inside this checkpoint.
  </acceptance_criteria>
  <done>The user has reported Play-mode results for all 10 items and Check.md records them verbatim, with skips marked as skipped rather than passed.</done>
</task>

</tasks>

<verification>
- Unity batch compile: zero `error CS`.
- All new `.meta` files committed.
- `Assets/Audio/Check.md` exists with real recorded values and the user's Play-mode verdict.
- `git diff HEAD~N -- Assets/Player/Script/InputHandler.cs` across the whole phase is empty.
</verification>

<success_criteria>
The phase compiles in Unity, every new asset is properly imported and committed, and a human has confirmed that volume sliders, environment filtering and cross-scene persistence actually work — or the gaps are recorded honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/20-audio-centralization/20-06-SUMMARY.md`
</output>
