---
phase: 20-audio-centralization
plan: 05
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - Assets/Script/EnvironmentManager.cs
autonomous: true
requirements: [D-12]
must_haves:
  truths:
    - "EnvironmentManager still classifies water ratio into Alive/Neutral/Withered exactly as before"
    - "EnvironmentManager still recolours backgroundRenderers exactly as before"
    - "EnvironmentManager no longer holds or touches any AudioSource or AudioLowPassFilter"
    - "A state change reaches the audio system only through AudioManager.SetEnvironmentState"
    - "The file's surviving Korean comments are byte-identical to HEAD"
  artifacts:
    - path: "Assets/Script/EnvironmentManager.cs"
      provides: "State judgment + background colour only; audio delegated"
      contains: "AudioManager.Instance?.SetEnvironmentState(state);"
  key_links:
    - from: "Assets/Script/EnvironmentManager.cs"
      to: "AudioManager.SetEnvironmentState"
      via: "ApplyEnvironmentEffects delegation"
      pattern: "SetEnvironmentState\\(state\\)"
---

<objective>
Strip all direct audio manipulation out of `EnvironmentManager` and replace it with a single `AudioManager.Instance?.SetEnvironmentState(state)` call (D-12). State judgment (water ratio to Alive/Neutral/Withered) and background recolouring stay exactly where they are.

Purpose: Completes the single-owner rule for audio. `AudioManager` already holds the cutoff constants, the BGM `AudioSource` and the `AudioLowPassFilter` after Plan 20-02; this plan removes the duplicate ownership on the other side.
Output: `Assets/Script/EnvironmentManager.cs`, edited surgically at byte level.

**This file is CP949-encoded.** The encoding rule in Task 1 is not optional.
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-audio-centralization/20-CONTEXT.md
@.planning/phases/20-audio-centralization/20-RESEARCH.md
@.planning/phases/20-audio-centralization/20-02-SUMMARY.md
@Assets/Script/EnvironmentManager.cs
@CLAUDE.md

<interfaces>
<!-- From Plan 20-02, already in Assets/Script/AudioManager.cs -->
```csharp
public class AudioManager : MonoBehaviour
{
    public static AudioManager Instance { get; private set; }
    public enum EnvironmentState { None, Alive, Neutral, Withered }
    public void SetEnvironmentState(EnvironmentState state);
}
```
AudioManager already holds these constants, moved verbatim from EnvironmentManager's Inspector defaults:
`AliveCutoff = 22000f`, `NeutralCutoff = 5000f`, `WitheredCutoff = 1000f`.
`SetEnvironmentState` already early-returns on an unchanged state and on `EnvironmentState.None`, and already contains the "resume BGM if stopped, then set cutoff" logic moved from `ChangeBGMCutoff`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delegate environment audio to AudioManager (CP949-safe edit)</name>
  <files>Assets/Script/EnvironmentManager.cs</files>
  <read_first>
    - Assets/Script/EnvironmentManager.cs — the full 107-line file. Its Korean comments render as mojibake in any UTF-8 reader; that is expected and is NOT corruption. Do not "fix" them.
    - Assets/Script/AudioManager.cs — confirm `public enum EnvironmentState` and `public void SetEnvironmentState(EnvironmentState state)` exist as written by Plan 20-02.
    - .planning/phases/20-audio-centralization/20-RESEARCH.md — "Pitfall 4" (CP949 corruption) and "Pitfall 3" (orphaned Inspector references).
    - .planning/STATE.md — the Phase 11 Plan 3 entry documenting that Read/Edit round-trips silently destroyed non-ASCII bytes in `Checkpoint.cs` and `WoodBossController.cs`, and that the mitigation is `git show HEAD:<path>` plus byte-level scripted editing.
  </read_first>
  <action>
ENCODING RULE, read before editing. `Assets/Script/EnvironmentManager.cs` is CP949, not UTF-8. The standard Read-then-Edit tool round-trip re-encodes the file as UTF-8 and silently replaces every Korean byte with U+FFFD. This has already destroyed two files in this repo. Therefore:

- Extract the original bytes first: `git show HEAD:Assets/Script/EnvironmentManager.cs > $SCRATCH/env_orig.cs`.
- Perform the edit with a byte-level script (Python opening both files in `'rb'`/`'wb'` mode, matching on ASCII byte patterns). Never use the Edit tool on this file.
- Every line you INSERT must be pure ASCII.
- Every line containing Korean bytes must be either left completely untouched or deleted whole. Never partially rewrite a line containing Korean.

THE EDITS. Seven changes, identified by ASCII content match (line numbers shift as you go):

1. Add a type alias immediately after the first line `using UnityEngine;`. Insert these ASCII lines:
```csharp

// D-12: the state enum now lives on AudioManager, which owns the cutoff mapping.
// Aliasing instead of renaming every reference keeps the diff on this CP949-encoded
// file down to ASCII-only lines - see Pitfall 4 in 20-RESEARCH.md.
using EnvironmentState = AudioManager.EnvironmentState;
```

2. Delete the line `    public AudioSource bgmSource;` (currently line 7).

3. Delete the line `    public AudioLowPassFilter lowPassFilter;` together with the Korean comment line directly above it (currently lines 9-10). Collapse any resulting double blank line.

4. Delete the entire cutoff block: the `[Header(...)]` line, the Korean explanatory comment, and the three fields `public float aliveCutoff = 22000f;`, `public float neutralCutoff = 5000f;`, `public float witheredCutoff = 1000f;` (currently lines 18-22). These values are now constants on `AudioManager`; leaving them here would give the Inspector a second, silently-ignored source of truth.

5. Delete the line `    private enum EnvironmentState { None, Alive, Neutral, Withered }` (currently line 24). The alias from edit 1 makes every remaining `EnvironmentState` reference in the file resolve to `AudioManager.EnvironmentState` with no further text changes. Keep the next line, `    private EnvironmentState currentState = EnvironmentState.None;`, exactly as it is.

6. In `ApplyEnvironmentEffects`, delete the three `ChangeBGMCutoff(...)` call lines (each carries a Korean trailing comment — delete the whole line, comment included):
   - `                ChangeBGMCutoff(aliveCutoff); ...`
   - `                ChangeBGMCutoff(neutralCutoff); ...`
   - `                ChangeBGMCutoff(witheredCutoff); ...`
   Then insert these pure-ASCII lines immediately after the method's opening brace `    {` and before `        Color targetColor = Color.white;`:
```csharp
        // D-12: AudioManager owns BGM playback and the low-pass cutoff mapping.
        // EnvironmentManager only judges the state and reports it.
        AudioManager.Instance?.SetEnvironmentState(state);

```
   One call outside the switch replaces three inside it, because `SetEnvironmentState` already switches on the state internally.

7. Delete the entire `ChangeBGMCutoff` method plus the Korean doc comment above it — from that comment line through the method's closing brace (currently lines 92-106). Its body was moved verbatim into `AudioManager.SetEnvironmentState` by Plan 20-02.

WHAT MUST NOT CHANGE: `waterController`, `backgroundRenderers`, `aliveColor`/`neutralColor`/`witheredColor`, `Start()`, `Update()`, the whole of `UpdateEnvironmentState()` including the 0.66f and 0.33f thresholds, and the `targetColor` / `backgroundRenderers` recolouring block at the end of `ApplyEnvironmentEffects`.

SCENE SIDE-EFFECT, DO NOT ACT ON IT: eleven `.unity` files serialize `EnvironmentManager`'s now-removed `bgmSource` and `lowPassFilter` references. Unity strips orphaned serialized fields the next time each scene is saved, and the orphan entries are inert until then. Do NOT hand-edit eleven scene YAML files to clean them up — that is far more risk than inert entries are worth, and no code references them any more. Record it as a known-harmless leftover in the summary, for Plan 20-06's Check.md.
  </action>
  <verify>
    <automated>cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && grep -c "SetEnvironmentState(state);" Assets/Script/EnvironmentManager.cs; grep -c "bgmSource" Assets/Script/EnvironmentManager.cs; grep -c "ChangeBGMCutoff" Assets/Script/EnvironmentManager.cs; grep -cP "\xEF\xBF\xBD" Assets/Script/EnvironmentManager.cs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AudioManager.Instance?.SetEnvironmentState(state);" Assets/Script/EnvironmentManager.cs` returns exactly 1.
    - `grep -c "bgmSource" Assets/Script/EnvironmentManager.cs` returns 0.
    - `grep -c "lowPassFilter" Assets/Script/EnvironmentManager.cs` returns 0.
    - `grep -c "AudioLowPassFilter" Assets/Script/EnvironmentManager.cs` returns 0.
    - `grep -c "ChangeBGMCutoff" Assets/Script/EnvironmentManager.cs` returns 0.
    - `grep -c "aliveCutoff\|neutralCutoff\|witheredCutoff\|22000f\|5000f\|1000f" Assets/Script/EnvironmentManager.cs` returns 0.
    - `grep -c "using EnvironmentState = AudioManager.EnvironmentState;"` returns 1.
    - `grep -c "private enum EnvironmentState"` returns 0, while `grep -c "private EnvironmentState currentState = EnvironmentState.None;"` still returns 1.
    - Judgment logic intact: `grep -c "waterRatio > 0.66f"` returns 1 and `grep -c "waterRatio > 0.33f"` returns 1.
    - Colour logic intact: `grep -c "sr.color = targetColor;"` returns 1 and `grep -c "aliveColor\|neutralColor\|witheredColor"` returns 6 (3 declarations + 3 uses).
    - ENCODING GATE 1 — no replacement characters introduced: `grep -cP "\xEF\xBF\xBD" Assets/Script/EnvironmentManager.cs` returns 0.
    - ENCODING GATE 2 — surviving Korean bytes are unchanged. Run: extract `git show HEAD:Assets/Script/EnvironmentManager.cs`, filter both HEAD and working copies to only lines containing a byte >= 0x80, and diff those two filtered streams. Every line in the new file's filtered stream must appear verbatim in HEAD's filtered stream (the new file may have FEWER such lines because of deletions, but must contain no line that is not byte-identical to one in HEAD).
    - ENCODING GATE 3 — no added line carries non-ASCII: every `+` line in `git diff -- Assets/Script/EnvironmentManager.cs` matches `^\+[\x00-\x7F]*$`.
    - `git status --porcelain Assets | grep -v "EnvironmentManager.cs"` is empty (no scene files hand-edited).
  </acceptance_criteria>
  <done>EnvironmentManager keeps its water-ratio judgment and background recolouring, has zero audio fields or audio calls other than one `AudioManager.Instance?.SetEnvironmentState(state)`, and every surviving Korean byte is identical to HEAD.</done>
</task>

</tasks>

<verification>
- `grep -rn "lowPassFilter\|AudioLowPassFilter" --include=*.cs Assets/` shows hits only in `Assets/Script/AudioManager.cs`.
- `Assets/Script/EnvironmentManager.cs` is the only file in the diff.
- The three encoding gates above all pass.
</verification>

<success_criteria>
`EnvironmentManager` judges environment state and colours the background; it has no knowledge of AudioSources, filters or cutoff numbers, and reports state to `AudioManager` through one call — with its CP949 encoding provably intact.
</success_criteria>

<output>
After completion, create `.planning/phases/20-audio-centralization/20-05-SUMMARY.md`
</output>
