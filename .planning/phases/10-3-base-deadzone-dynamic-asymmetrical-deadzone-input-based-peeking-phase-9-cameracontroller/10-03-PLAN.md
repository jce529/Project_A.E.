---
phase: 10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - Assets/Camera/Script/CameraController.cs
autonomous: true
requirements: [D-08, D-09, D-10, D-11, D-12, D-13]
must_haves:
  truths:
    - "플레이어가 땅에 서서 멈춘 채 위 방향 입력을 0.5초 이상 유지하면 카메라가 천천히 위로 시야를 옮긴다"
    - "아래 방향 입력을 유지하면 카메라가 아래로 시야를 옮긴다"
    - "이동을 시작하거나 대시/피격으로 급격히 밀려나면 피킹이 즉시 취소되고 원위치로 빠르게 복귀한다"
    - "공중에 있으면(IsGrounded false) 피킹이 발동하지 않는다"
    - "컷신/사망 등으로 movementLocked 인 동안에는 입력이 들어와도 피킹이 발동하지 않는다"
    - "씬을 여러 번 오가도 InputHandler 구독이 누적되지 않는다"
    - "보스 구역에서는 피킹이 비활성화되고 Phase 9 레거시 추종만 남는다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "InputHandler 구독 + movementLocked/IsGrounded 가드 + 피킹 SmoothDamp"
      contains: "private void UpdatePeekOffset(float targetSpeed)"
  key_links:
    - from: "CameraController.OnEnable"
      to: "InputHandler.Instance.OnMoveEvent"
      via: "이벤트 구독 (D-08)"
      pattern: "InputHandler\\.Instance\\.OnMoveEvent \\+= HandleMoveInput;"
    - from: "CameraController.OnDisable"
      to: "InputHandler.Instance.OnMoveEvent"
      via: "구독 해제 - 씬 전환 누수 방지 (D-09)"
      pattern: "InputHandler\\.Instance\\.OnMoveEvent -= HandleMoveInput;"
    - from: "UpdatePeekOffset"
      to: "PlayerController.movementLocked / IsGrounded()"
      via: "캐시된 _playerRef 로 가드 (D-09 / D-12)"
      pattern: "_playerRef\\.movementLocked"
    - from: "UpdatePeekOffset"
      to: "transform.position.y"
      via: "_currentPeekY SmoothDamp 가산"
      pattern: "p\\.y = _followBaseY \\+ _currentPeekY;"
---

<objective>
Plan 10-01/10-02 위에 **Input-based Peeking(수동 시야 이동)** 레이어를 얹는다. 플레이어가 접지 상태로 정지한 채 수직 입력을 `peekThreshold` 이상 유지하면 카메라 Y 가 `SmoothDamp` 로 `peekDistance` 만큼 이동하고, 이동/대시/피격이 감지되면 즉시 취소되어 빠르게 복귀한다 (D-08 ~ D-13).

Purpose: 플레이어가 아래 지형/위 발판을 미리 확인할 수 있게 한다. 일반 스테이지에서 카메라 Y 를 의도적으로 움직이는 유일한 메커니즘이다 (Plan 10-01 잠금 가정 A1).
Output: 확장된 `CameraController.cs` 1개 파일. 신규 파일 없음, 씬 편집 없음, **`PlayerController.cs`/`InputHandler.cs` 무수정**.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-RESEARCH.md
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-01-PLAN.md
@Assets/Camera/Script/CameraController.cs
</context>

<interfaces>
<!-- 실행자가 그대로 사용할 기존 공개 API. 코드베이스 탐색 불필요. -->

`Assets/Player/Script/InputHandler.cs` (수정 금지, 읽기 전용):
```csharp
public class InputHandler : MonoBehaviour
{
    public static InputHandler Instance { get; private set; }   // Awake 에서 DontDestroyOnLoad
    public event Action<Vector2> OnMoveEvent;                   // 26행. moveAction.performed / canceled 양쪽에서 발화
}
```
- `moveAction.performed` 는 현재 입력 벡터를, `moveAction.canceled` 는 `Vector2.zero` 를 흘려보낸다 (114-115행).
- `movementLocked` 여부와 **무관하게 항상** 발화한다 → 소비 지점에서 가드 필요 (D-09).

`Assets/Player/Script/PlayerController.cs` (수정 금지, 읽기 전용):
```csharp
public class PlayerController : MonoBehaviour
{
    public bool movementLocked = false;   // 46행, public
    public bool IsGrounded();             // 234행, public. Raycast 기반 접지 판정
    public float dashSpeed = 20f;         // 29행 (peekCancelSpeed 기준값 근거)
    public float runSpeed = 7f;           // 15행 (peekCancelSpeed 기준값 근거)
    // isDashing / isKnockedBack 은 private 이며 접근자 추가 금지 (D-11)
}
```
`PlayerController` 는 `OnMove(Vector2)` 에서 `movementLocked` 검사 없이 무조건 `moveInput` 을 갱신한다(106-108행). 이 플랜의 `HandleMoveInput` 도 같은 관례를 따라 "값만 캐시"하고, 가드는 소비 지점(`UpdatePeekOffset`)에서 적용한다.
</interfaces>

<critical_constraints>
## 1. 파일 인코딩 (Plan 10-01/10-02 와 동일 — 최우선)

`CameraController.cs` 는 CP949 다. `Write` 금지, `Edit` 만 사용, `old_string` 에 깨진 한글 주석 포함 금지, 새 주석은 ASCII 영문만.

- 게이트 1: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == **5**
- 게이트 2 (삭제 라인에 한글 없음):
  `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]'` == **0**
- 게이트 3 (누적 삭제 라인 수): Plan 10-02 에서 2줄, 이 플랜에서 1줄(`        p.y = _followBaseY;`)을 수정하므로
  `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == **3**

## 2. 읽기 전용 파일 (절대 수정 금지)

`Assets/Player/Script/PlayerController.cs`, `Assets/Player/Script/InputHandler.cs` 는 **0줄 변경**이다 (D-04, D-08 ~ D-11).
- `isDashing` / `isKnockedBack` 에 public 접근자 추가 금지 — D-11 이 명시적으로 금지했다. 대시/피격은 **이동량 급증 프록시**로만 감지한다.
- `moveInput` / `rigid` 를 노출시키지 말 것 — 수직 입력은 `InputHandler.Instance.OnMoveEvent` 구독으로만 얻는다 (D-08).
- 검증: `git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs` 결과가 비어 있어야 한다.

## 3. 구독 누수 (10-RESEARCH Pitfall 3)

`InputHandler` 는 `DontDestroyOnLoad` 이고 `CameraController` 는 씬 로컬(씬 전환 시 파괴)이다. `OnEnable` 에서 구독하고 `OnDisable` 에서 반드시 해제하지 않으면, 씬을 오갈 때마다 델리게이트 목록이 무한히 커진다.
추가로 **첫 씬에서는 `CameraController.OnEnable` 이 `InputHandler.Awake` 보다 먼저 실행될 수 있다**(오브젝트 간 실행 순서 미정의). 그 경우 `InputHandler.Instance` 가 null 이라 구독이 조용히 실패하고 피킹이 영영 동작하지 않는다. 그래서 `Start` 에서 한 번 더 시도하는 `TryBindMoveEvent()` 구조를 쓴다.

## 4. 범위 밖 (하지 말 것)

- Y축 데드존 추가 금지 — 피킹은 **오프셋 가산**이지 데드존이 아니다 (Plan 10-01 잠금 가정 A1).
- 대시/피격을 상태 플래그로 구분하려는 시도 금지 (D-11).
- 수평 피킹(좌우 시야 이동) 추가 금지 — 사용자 수식은 수직 전용이다.
- 데드존 하드컷 / 동적 오프셋 로직 변경 금지 (Plan 10-01, 10-02 결과물).
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: InputHandler.OnMoveEvent 구독 라이프사이클 + PlayerController 참조 캐시</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **Plan 10-02 적용 후 현재 상태를 반드시 Read 한다.** 앵커는 이 Read 결과에서 복사한다.
    - `Assets/Player/Script/InputHandler.cs` — `OnMoveEvent` 선언(26행)과 `Awake` 의 `DontDestroyOnLoad`(68행) 확인. **수정 금지.**
    - `Assets/Player/Script/PlayerController.cs` — `Start`/`OnDestroy` 의 구독/해제 대칭 관례(72-86행), `movementLocked`(46행), `IsGrounded()`(234행) 확인. **수정 금지.**
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-RESEARCH.md` — Pitfall 3(구독 누수), Pitfall 5(GetComponent 캐시)
  </read_first>

  <action>
`Edit` 도구로 **3회 삽입**한다. 전부 앵커 뒤 삽입이며 기존 라인은 변경하지 않는다.

### 삽입 1 — 입력/참조 상태 필드

앵커(`old_string`, 파일에서 유일 — 필드 선언부, 4칸 들여쓰기):
```
    private float _deadzonePushSign;
```

`new_string` = 앵커 + 아래 블록:
```csharp
    // Latest raw move input from the InputHandler bus (D-08). Cached as-is: the movementLocked
    // guard is applied where it is consumed, exactly like PlayerController.OnMove does (D-09).
    private Vector2 _lastMoveInput;
    // True once the OnMoveEvent subscription is live, so it is bound and released exactly once.
    private bool _moveEventBound;
    // Cached once so LateUpdate never calls GetComponent (10-RESEARCH Pitfall 5).
    private PlayerController _playerRef;
    // Target position of the previous frame. Single shared movement signal used for the
    // idle check (D-10) and the dash / knockback spike proxy (D-11).
    private Vector3 _lastTargetPos;
```

### 삽입 2 — OnEnable / OnDisable / 바인딩 헬퍼 / 이벤트 핸들러

앵커(`old_string`, 2줄 연속 — `Awake` 본문 마지막 줄과 닫는 중괄호, 파일에서 유일):
```
        _targetZoom = normalZoom;
    }
```

`new_string` = 앵커 2줄 + 아래 블록:
```csharp

    // InputHandler is DontDestroyOnLoad while this camera is scene local, so the subscription
    // MUST be released on disable or the delegate list grows on every scene load (D-08 / D-09).
    void OnEnable()
    {
        TryBindMoveEvent();
    }

    void OnDisable()
    {
        if (!_moveEventBound) return;
        if (InputHandler.Instance != null)
            InputHandler.Instance.OnMoveEvent -= HandleMoveInput;
        _moveEventBound = false;
    }

    // OnEnable can run before InputHandler.Awake in the very first scene, so Start retries.
    private void TryBindMoveEvent()
    {
        if (_moveEventBound || InputHandler.Instance == null) return;
        InputHandler.Instance.OnMoveEvent += HandleMoveInput;
        _moveEventBound = true;
    }

    // Dumb latest-value cache, mirroring PlayerController.OnMove which also stores input
    // unconditionally. All gating happens in UpdatePeekOffset (D-09).
    private void HandleMoveInput(Vector2 input)
    {
        _lastMoveInput = input;
    }
```

### 삽입 3 — Start 에서 재바인딩 + PlayerController 캐시

앵커(`old_string`, 2줄 연속 — Plan 10-01 이 넣은 Start 초기화, 파일에서 유일):
```
        // Park the deadzone box and the follow baseline on the camera's start position.
        ResetNormalStageState();
```

`new_string` = 앵커 2줄 + 아래 3줄:
```csharp
        // Retry in case InputHandler.Instance was not ready yet during OnEnable (D-08).
        TryBindMoveEvent();
        _playerRef = target.GetComponent<PlayerController>();
```

### 설계 근거 (임의로 바꾸지 말 것)
- `Start`/`OnDestroy` 가 아니라 `OnEnable`/`OnDisable` 쌍을 쓴다 — D-09 가 명시적으로 지정한 라이프사이클이다.
- `HandleMoveInput` 안에서 `movementLocked` 를 검사하지 말 것 — 잠금이 풀린 뒤 마지막 입력이 사라져 버리는 stale 문제가 생기고, `PlayerController.OnMove` 의 기존 관례와도 어긋난다 (10-RESEARCH Pattern 4).
- `_playerRef` 가 null 이어도(플레이어에 `PlayerController` 가 없는 테스트 씬 등) 예외가 나면 안 된다 — Task 2 의 가드가 null 을 "피킹 불가"로 처리한다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "private Vector2 _lastMoveInput;" $F && grep -qF "private bool _moveEventBound;" $F && grep -qF "private PlayerController _playerRef;" $F && grep -qF "private Vector3 _lastTargetPos;" $F && grep -qF "void OnEnable()" $F && grep -qF "void OnDisable()" $F && grep -qF "InputHandler.Instance.OnMoveEvent += HandleMoveInput;" $F && grep -qF "InputHandler.Instance.OnMoveEvent -= HandleMoveInput;" $F && [ "$(grep -cF 'OnMoveEvent +=' $F)" = "1" ] && [ "$(grep -cF 'OnMoveEvent -=' $F)" = "1" ] && grep -qF "_playerRef = target.GetComponent<PlayerController>();" $F && [ "$(grep -cF 'GetComponent<PlayerController>()' $F)" = "1" ] && [ "$(grep -cF 'TryBindMoveEvent();' $F)" = "2" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "2" ] && [ "$(git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs | wc -l)" = "0" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - 상태 필드 4개 존재: `private Vector2 _lastMoveInput;`, `private bool _moveEventBound;`, `private PlayerController _playerRef;`, `private Vector3 _lastTargetPos;`
    - `grep -cF 'OnMoveEvent +=' Assets/Camera/Script/CameraController.cs` == 1 이고 `grep -cF 'OnMoveEvent -=' ...` == 1 (구독/해제 정확히 1쌍, D-09)
    - `grep -qF "void OnEnable()"` / `grep -qF "void OnDisable()"` 성공 — `Start`/`OnDestroy` 로 대체하지 않았다
    - `grep -cF 'TryBindMoveEvent();' Assets/Camera/Script/CameraController.cs` == 2 (`OnEnable` 1 + `Start` 1)
    - `grep -cF 'GetComponent<PlayerController>()' Assets/Camera/Script/CameraController.cs` == 1 — `Start` 에서 1회만 캐시, `LateUpdate` 계열 메서드 안에 `GetComponent` 호출 없음 (Pitfall 5)
    - `HandleMoveInput` 본문에 `movementLocked` 문자열이 없다 (가드는 소비 지점에서, Pattern 4)
    - **읽기 전용 파일 무수정**: `git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs` 출력 0줄
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **누적 삭제 라인**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == `2` (이 태스크는 삽입 전용이므로 10-02 의 2줄에서 증가하지 않는다)
  </acceptance_criteria>

  <done>`CameraController` 가 `OnEnable`/`OnDisable` 대칭으로 `InputHandler.OnMoveEvent` 를 구독/해제하고, 최신 입력을 `_lastMoveInput` 에 캐시하며, `_playerRef` 를 `Start` 에서 1회 캐시한다. `PlayerController.cs`/`InputHandler.cs` 는 0줄 변경.</done>
</task>

<task type="auto">
  <name>Task 2: 피킹 Inspector 필드 + UpdatePeekOffset 헬퍼 + 카메라 Y 합성/상태 초기화</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **Task 1 적용 후 상태를 다시 Read 한다.**
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md` — `<specifics>` 3번(피킹 조건식/타이머/복귀), D-09 ~ D-13
    - `Assets/Player/Script/PlayerController.cs` — `dashSpeed = 20f`(29행), `runSpeed = 7f`(15행) 확인. `peekCancelSpeed = 12f` 기본값의 근거다. **수정 금지.**
  </read_first>

  <action>
`Edit` 도구로 **5회** 편집한다. 이 중 **1회는 기존 ASCII 라인 1줄 수정**(`p.y = _followBaseY;`)이고 나머지 4회는 삽입이다.

### 편집 1 (삽입) — 피킹 Inspector 필드

앵커(`old_string`, 파일에서 유일):
```
    public float offsetHoldDuration = 0.4f;
```

`new_string` = 앵커 + 아래 블록:
```csharp

    [Header("Peeking (normal stages only)")]
    // Seconds the vertical input must be held while idle before peeking starts (D-13, user value).
    public float peekThreshold = 0.5f;
    // World units the camera shifts vertically at full input deflection (D-13).
    public float peekDistance = 3f;
    // SmoothDamp time while peeking out, and the faster one used for the snap back (D-13).
    public float peekSmoothTime = 0.35f;
    public float peekReturnSmoothTime = 0.12f;
    // Target speed (world units per second) at or below which the target counts as stopped.
    // This is the Velocity == 0 proxy from the frame delta of target.position (D-10).
    public float idleSpeedThreshold = 0.05f;
    // Target speed that instantly cancels peeking. Dash / knockback proxy - the cause is NOT
    // distinguished (D-11). PlayerController dashSpeed is 20 and runSpeed is 7, so 12 sits
    // between them: running never trips it, dashing and knockback always do.
    public float peekCancelSpeed = 12f;
```

### 편집 2 (삽입) — 피킹 상태 필드

앵커(`old_string`, 파일에서 유일 — Task 1 이 추가한 필드):
```
    private Vector3 _lastTargetPos;
```

`new_string` = 앵커 + 아래 블록:
```csharp
    // Current vertical peek offset added on top of _followBaseY.
    private float _currentPeekY;
    // SmoothDamp state. MUST be a persistent field, never a local.
    private float _peekVelocityY;
    // How long all peek conditions have held continuously (D-13 threshold timer).
    private float _peekTimer;
```

### 편집 3 (삽입) — UpdatePeekOffset 메서드 (OnDrawGizmos 앞)

앵커(`old_string`, 파일에서 유일 — Gizmo 주석 첫 줄):
```
    // Editor only deadzone visualization (D-03). Zero runtime cost in a build.
```

`new_string` = 아래 메서드 블록 + 빈 줄 + 위 앵커 라인 (즉 앵커 **앞에** 삽입):
```csharp
    // Input based peeking (D-08 - D-13). All four conditions must hold together:
    // input not locked (D-09), grounded (D-12), target effectively stopped (D-10),
    // and a held vertical input. A speed spike cancels it immediately (D-11).
    private void UpdatePeekOffset(float targetSpeed)
    {
        bool inputUsable = _playerRef != null && !_playerRef.movementLocked;
        bool grounded = _playerRef != null && _playerRef.IsGrounded();
        bool idle = targetSpeed <= idleSpeedThreshold;
        bool cancelled = targetSpeed >= peekCancelSpeed;
        bool holding = inputUsable && grounded && idle && !cancelled && Mathf.Abs(_lastMoveInput.y) > 0.1f;

        float targetPeek = 0f;
        if (holding)
        {
            _peekTimer += Time.deltaTime;
            if (_peekTimer > peekThreshold)
                targetPeek = _lastMoveInput.y * peekDistance;
        }
        else
        {
            _peekTimer = 0f;
        }

        // Returning to 0 uses the faster smooth time so the view snaps back on cancel.
        float smoothTime = (targetPeek == 0f) ? peekReturnSmoothTime : peekSmoothTime;
        _currentPeekY = Mathf.SmoothDamp(_currentPeekY, targetPeek, ref _peekVelocityY, smoothTime);
    }

    // Editor only deadzone visualization (D-03). Zero runtime cost in a build.
```

### 편집 4 (삽입 + 라인 수정) — ApplyNormalStageCamera 합성

4-a) 앵커(`old_string`, 2줄 연속 — 파일에서 유일):
```
        UpdateDeadzoneCenter();
        UpdateDynamicOffset();
```

`new_string`:
```csharp
        float dt = Mathf.Max(Time.deltaTime, 0.0001f);
        float targetSpeed = (target.position - _lastTargetPos).magnitude / dt;
        UpdateDeadzoneCenter();
        UpdateDynamicOffset();
        UpdatePeekOffset(targetSpeed);
```

4-b) **라인 수정** — `old_string`(파일에서 유일):
```
        p.y = _followBaseY;
```

`new_string`:
```csharp
        p.y = _followBaseY + _currentPeekY;
```

4-c) 앵커(`old_string`, 파일에서 유일 — `ApplyNormalStageCamera` 의 마지막 대입):
```
        transform.position = p;
```

`new_string` = 앵커 + 아래 2줄:
```csharp
        // Consumed by next frame's speed proxy. Updated AFTER all three helpers have read it.
        _lastTargetPos = target.position;
```

### 편집 5 (삽입) — 보스 구역 진입 시 피킹 상태 초기화

앵커(`old_string`, 2줄 연속 — `ResetNormalStageState` 안, 파일에서 유일):
```
        _offsetHoldTimer = 0f;
        _deadzonePushSign = 0f;
```

`new_string` = 앵커 2줄 + 아래 4줄:
```csharp
        _currentPeekY = 0f;
        _peekVelocityY = 0f;
        _peekTimer = 0f;
        _lastTargetPos = target.position;
```

### 값·수식 근거 (임의로 바꾸지 말 것)
- `targetPeek = _lastMoveInput.y * peekDistance;` — 사용자 원본 수식 `TargetPeeking = Input.Y × PeekDistance` 그대로다. `Mathf.Sign()` 으로 바꾸지 말 것(아날로그 스틱 부분 입력이 그대로 반영되는 것이 원안이다).
- `Mathf.Abs(_lastMoveInput.y) > 0.1f` — `Input.Y != 0` 조건의 데드존 처리. 스틱 노이즈로 피킹이 켜지는 것을 막는다.
- `targetSpeed` 는 프레임 델타를 `Time.deltaTime` 으로 나눈 **초당 월드 유닛** 이다. 프레임당 거리로 비교하면 프레임레이트에 따라 임계값이 달라진다.
- `idleSpeedThreshold = 0.05f`, `peekCancelSpeed = 12f`, `peekDistance = 3f`, `peekSmoothTime = 0.35f`, `peekReturnSmoothTime = 0.12f` — 사용자가 수치를 지정하지 않은 항목(10-RESEARCH Open Question 2/3)의 초기값이며 전부 Inspector 튜닝 대상이다. `peekThreshold = 0.5f` 만 사용자 제시값이다 (D-13).
- 취소 시 `peekReturnSmoothTime`(더 빠름)을 쓰는 것은 `<specifics>` 3번의 "빠른 SmoothDamp 로 복귀"를 반영한 것이다 (10-RESEARCH Open Question 3).
- `_lastTargetPos` 갱신은 `ApplyNormalStageCamera` **끝**에서 1회만 한다. 헬퍼들이 읽기 전에 갱신하면 델타가 항상 0 이 되어 피킹이 상시 켜진다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "public float peekThreshold = 0.5f;" $F && grep -qF "public float peekDistance = 3f;" $F && grep -qF "public float peekSmoothTime = 0.35f;" $F && grep -qF "public float peekReturnSmoothTime = 0.12f;" $F && grep -qF "public float idleSpeedThreshold = 0.05f;" $F && grep -qF "public float peekCancelSpeed = 12f;" $F && grep -qF "private void UpdatePeekOffset(float targetSpeed)" $F && grep -qF "_playerRef.movementLocked" $F && grep -qF "_playerRef.IsGrounded()" $F && grep -qF "targetPeek = _lastMoveInput.y * peekDistance;" $F && grep -qF "_currentPeekY = Mathf.SmoothDamp(_currentPeekY, targetPeek, ref _peekVelocityY, smoothTime);" $F && grep -qF "float targetSpeed = (target.position - _lastTargetPos).magnitude / dt;" $F && grep -qF "p.y = _followBaseY + _currentPeekY;" $F && [ "$(grep -cF 'p.y = _followBaseY;' $F)" = "0" ] && [ "$(grep -cF '_lastTargetPos = target.position;' $F)" = "2" ] && [ "$(grep -cF 'isDashing' $F)" = "0" ] && [ "$(grep -cF 'isKnockedBack' $F)" = "0" ] && PK=$(grep -nF 'UpdatePeekOffset(targetSpeed);' $F | cut -d: -f1) && LT=$(grep -nF '_lastTargetPos = target.position;' $F | tail -1 | cut -d: -f1) && [ "$LT" -gt "$PK" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "3" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]')" = "0" ] && [ "$(git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs | wc -l)" = "0" ] && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - Inspector 필드 6개 존재: `peekThreshold = 0.5f`(D-13 사용자 값), `peekDistance = 3f`, `peekSmoothTime = 0.35f`, `peekReturnSmoothTime = 0.12f`, `idleSpeedThreshold = 0.05f`, `peekCancelSpeed = 12f` — 전부 `public float` 선언이며 하드코딩 상수가 아니다
    - `grep -qF "private void UpdatePeekOffset(float targetSpeed)"` 성공
    - 4개 조건이 모두 코드에 존재: `_playerRef.movementLocked`(D-09), `_playerRef.IsGrounded()`(D-12), `targetSpeed <= idleSpeedThreshold`(D-10), `Mathf.Abs(_lastMoveInput.y) > 0.1f`
    - `grep -qF "targetSpeed >= peekCancelSpeed"` 성공 (D-11 급증 프록시)
    - `grep -cF 'isDashing' Assets/Camera/Script/CameraController.cs` == 0 이고 `grep -cF 'isKnockedBack' ...` == 0 (D-11 — 상태 플래그 참조 금지)
    - `grep -qF "targetPeek = _lastMoveInput.y * peekDistance;"` 성공 — 사용자 원본 수식 그대로
    - `grep -qF "p.y = _followBaseY + _currentPeekY;"` 성공이고 `grep -cF "p.y = _followBaseY;"` == 0
    - `grep -cF '_lastTargetPos = target.position;' Assets/Camera/Script/CameraController.cs` == 2 (`ApplyNormalStageCamera` 끝 1 + `ResetNormalStageState` 1)
    - **순서 검사**: `UpdatePeekOffset(targetSpeed);` 행 번호 < 마지막 `_lastTargetPos = target.position;` 행 번호 (델타 소비 후 갱신)
    - `ResetNormalStageState` 안에 `_currentPeekY = 0f;` / `_peekVelocityY = 0f;` / `_peekTimer = 0f;` 존재 (D-15)
    - **읽기 전용 파일 무수정**: `git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs` 출력 0줄
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **누적 삭제 라인 게이트**: `git diff ef6f164 -- ... | grep '^-' | grep -vc '^---'` == `3`, 그리고 삭제 라인 중 비-ASCII 포함 라인 수 == `0`
  </acceptance_criteria>

  <done>접지 + 정지 + 수직 입력 유지 0.5초 조건에서 카메라 Y 가 `peekDistance` 만큼 SmoothDamp 로 이동하고, 이동/급증 감지 시 `peekReturnSmoothTime` 으로 빠르게 복귀한다. 보스 구역에서는 피킹 상태가 매 프레임 0 으로 초기화된다. 누적 삭제 라인 3줄(전부 ASCII), 비-ASCII 라인 수 5 유지.</done>
</task>

</tasks>

<verification>
1. `ApplyNormalStageCamera` 실행 순서: 프레임 델타/속도 계산 → `UpdateDeadzoneCenter()` → `UpdateDynamicOffset()` → `UpdatePeekOffset(targetSpeed)` → X/Y 합성 → `_lastTargetPos` 갱신.
2. `LateUpdate` 전체 순서: 레거시 Lerp → 보스/일반 분기 → 줌 Lerp → X 클램프 → 데드존 앵커 재동기화 (D-16, D-17 보존).
3. Inspector 신규 노출 필드 6개: `peekThreshold`, `peekDistance`, `peekSmoothTime`, `peekReturnSmoothTime`, `idleSpeedThreshold`, `peekCancelSpeed`.
4. `OnMoveEvent` 구독 1회 / 해제 1회, `GetComponent<PlayerController>()` 호출 1회 (Start 전용).
5. `Assets/Player/Script/PlayerController.cs`, `Assets/Player/Script/InputHandler.cs` 변경 0줄.
6. 인코딩 게이트 통과: 비-ASCII 포함 라인 수 == 5, 누적 삭제 라인 3줄이며 그중 비-ASCII 포함 0줄.
</verification>

<success_criteria>
- 수직 입력을 `InputHandler.Instance.OnMoveEvent` 구독으로만 읽는다 (D-08, `PlayerController.cs` 무수정).
- `movementLocked`(D-09) + `IsGrounded()`(D-12) + 정지 근사(D-10) + 입력 유지 4조건이 모두 코드에 존재한다.
- 대시/피격은 원인 구분 없이 이동량 급증 프록시로만 취소된다 (D-11, `isDashing`/`isKnockedBack` 미참조).
- `peekThreshold`/`peekDistance` 를 포함한 모든 튜닝 값이 Inspector 필드다 (D-13).
- 씬 전환 시 구독 누수가 없다 (`OnEnable`/`OnDisable` 대칭).
</success_criteria>

<output>
완료 후 `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-03-SUMMARY.md` 를 생성한다.
커밋은 `Assets/Camera/Script/CameraController.cs` 경로만 지정해서 수행한다.
</output>
