---
phase: 10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - Assets/Camera/Script/CameraController.cs
autonomous: true
requirements: [D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "플레이어가 오른쪽으로 달려 데드존 경계를 밀면 카메라가 진행 방향(오른쪽) 시야를 더 넓게 확보한다"
    - "왼쪽으로 달리면 반대로 왼쪽 시야가 열린다"
    - "오프셋은 데드존 경계를 실제로 밀고 있을 때만 발동한다 (별도 속도 임계값 없음)"
    - "플레이어가 멈춰도 오프셋이 즉시 0 으로 튀지 않고 유지시간 후 부드럽게 복귀한다"
    - "오프셋 전환은 SmoothDamp 이며 데드존 하드컷 자체는 여전히 Lerp 없이 즉시 계산된다"
    - "보스 구역에서는 오프셋이 0 으로 초기화되어 Phase 9 레거시 추종에 영향이 없다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "동적 비대칭 오프셋 (SmoothDamp + 유지 타이머)"
      contains: "private void UpdateDynamicOffset()"
  key_links:
    - from: "UpdateDeadzoneCenter"
      to: "UpdateDynamicOffset"
      via: "_deadzonePushSign (경계 밀기 방향, D-05)"
      pattern: "_deadzonePushSign = 1f;"
    - from: "UpdateDynamicOffset"
      to: "_currentBoxOffsetX"
      via: "Mathf.SmoothDamp (D-07)"
      pattern: "Mathf\\.SmoothDamp\\(_currentBoxOffsetX, targetOffsetX, ref _offsetVelocityX, offsetSmoothTime\\)"
    - from: "ApplyNormalStageCamera"
      to: "transform.position.x"
      via: "박스 중심에서 오프셋만큼 앞서 나가기"
      pattern: "p\\.x = _deadzoneCenterX - _currentBoxOffsetX;"
---

<objective>
Plan 10-01 이 만든 하드컷 데드존 위에 **Dynamic Asymmetrical Deadzone(동적 비대칭 오프셋)** 레이어를 얹는다. 플레이어가 데드존 경계를 밀고 있는 동안 데드존 박스 중심이 진행 방향 반대편으로 `SmoothDamp` 오프셋되어, 결과적으로 카메라가 진행 방향 시야를 더 확보한다 (D-04 ~ D-07).

Purpose: 메트로배니아 이동 중 "가는 쪽이 더 보이는" 카메라를 만든다.
Output: 확장된 `CameraController.cs` 1개 파일. 신규 파일 없음, 씬 편집 없음, `PlayerController.cs`/`InputHandler.cs` 무수정.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-RESEARCH.md
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-01-PLAN.md
@Assets/Camera/Script/CameraController.cs
</context>

<critical_constraints>
## 1. 파일 인코딩 (Plan 10-01 과 동일 — 최우선)

`CameraController.cs` 는 CP949 다. `Write` 금지, `Edit` 만 사용, `old_string` 에 깨진 한글 주석 포함 금지, 새 주석은 ASCII 영문만.

- 게이트 1: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == **5**
- 게이트 2 (삭제 라인에 한글 없음):
  `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]'` == **0**
- 게이트 3 (이 플랜의 허용 삭제 라인 수): 이 플랜은 기존 ASCII 라인 **정확히 2줄**을 수정한다(아래 명시). 따라서
  `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == **2**
  이며, 그 2줄은 반드시 다음이어야 한다:
  - `        p.x = _deadzoneCenterX;`
  - `        if (!_isBossZone) _deadzoneCenterX = transform.position.x;`
  다른 라인이 삭제 목록에 나타나면 잘못된 편집이다.

## 2. 작업 트리 상태

`git add -A` / `git commit -a` 금지. `Assets/Camera/Script/CameraController.cs` 경로만 명시해 커밋한다.

## 3. 부호 규약 (가장 실수하기 쉬운 지점 — 반드시 읽을 것)

10-CONTEXT.md 의 사용자 원문은 두 군데가 서로 어긋난다:
- `<domain>` / `<specifics>` 2번: "**데드존 박스 중심**이 이동 방향 반대편으로 오프셋되어 **진행 방향 시야 확보**", "우측으로 달리면 오프셋이 좌측(-)으로 이동하여 **우측 시야가 열림**" → 오프셋의 주체는 **박스**다.
- `<specifics>` 최종 통합 공식: `FinalCameraPosition = BaseDeadzonePosition + CurrentOffset + PeekingOffset` → 오프셋을 **카메라**에 그대로 더하면 우측 이동 시 카메라가 좌측으로 가서 "우측 시야가 열림"과 정반대가 된다.

**확정(잠금 가정 A2): 사용자가 반복해서 서술한 의도("진행 방향 시야 확보")를 따른다.**
- `_currentBoxOffsetX` 는 사용자 수식 그대로 `-(pushDir * maxOffsetDistance)` 로 계산한다 (박스 중심의 오프셋).
- 카메라 X 는 `_deadzoneCenterX - _currentBoxOffsetX` 다 (박스 중심에서 오프셋을 **뺀다**).
- 검산: 오른쪽으로 달림 → `pushDir = +1` → `_currentBoxOffsetX = -1.5` → 카메라 X = 중심 + 1.5 → 카메라가 오른쪽으로 앞서 나감 → **우측 시야가 열린다**. 의도와 일치.
- 부호를 반대로 구현하면 Plan 10-04 의 Play 모드 체크리스트("오른쪽으로 달리면 오른쪽이 더 보인다")에서 반드시 걸린다.

## 4. 범위 밖 (하지 말 것)

- `PlayerController.cs`, `InputHandler.cs` 수정 금지 — 이동 방향은 데드존 경계 밀기 방향(`_deadzonePushSign`)에서만 얻는다 (D-04, D-05).
- **별도 속도 임계값으로 오프셋을 발동시키지 말 것** — D-05 는 "데드존 경계를 실제로 밀고 있을 때만"을 명시했다. `rigidbody.velocity`, 프레임 델타 속도 등으로 발동 조건을 만들면 D-05 위반이다.
- 데드존 하드컷 계산(`UpdateDeadzoneCenter`)에 Lerp/SmoothDamp 추가 금지 (D-14).
- 피킹/입력 구독 코드 추가 금지 (Plan 10-03 담당).
- Y축 오프셋 추가 금지 (Plan 10-01 잠금 가정 A1).
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: 오프셋 Inspector 필드 + 밀기 방향 추적 + UpdateDynamicOffset 헬퍼</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **Plan 10-01 적용 후 현재 상태를 반드시 Read 한다.** 앵커는 이 Read 결과에서 복사한다 (기억으로 재작성 금지).
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md` — D-04 ~ D-07 원문 및 `<specifics>` 2번 수식
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-RESEARCH.md` — Pattern 3(유지 후 감쇠 타이머), `Mathf.SmoothDamp` 시그니처(ref velocity 는 반드시 영속 필드)
  </read_first>

  <action>
`Edit` 도구로 **6회 삽입**한다. 전부 앵커 뒤에 이어 붙이는 삽입이며 기존 라인은 변경하지 않는다.

### 삽입 1 — 오프셋 Inspector 필드

앵커(`old_string`, 파일에서 유일):
```
    public float deadzoneHeight = 2f;
```

`new_string` = 앵커 + 아래 블록:
```csharp

    [Header("Dynamic Offset (normal stages only)")]
    // How far the deadzone box shifts opposite the movement direction, in world units (D-05).
    // The camera ends up leading the player by this much while an edge is being pushed.
    public float maxOffsetDistance = 1.5f;
    // SmoothDamp time for the offset transition (D-07). Larger = lazier look ahead.
    public float offsetSmoothTime = 0.35f;
    // Seconds the offset is held after the push stops, before it eases back to 0 (D-06).
    public float offsetHoldDuration = 0.4f;
```

### 삽입 2 — 오프셋 상태 필드

앵커(`old_string`, 파일에서 유일 — 필드 선언부):
```
    private float _followBaseY;
```

`new_string` = 앵커 + 아래 블록:
```csharp
    // Deadzone box offset, computed with the user formula -(pushDir * maxOffsetDistance).
    // The camera sits at _deadzoneCenterX MINUS this value, so a negative offset (running
    // right) pushes the camera right and opens up the view ahead (assumption A2 in 10-02-PLAN).
    private float _currentBoxOffsetX;
    // SmoothDamp state. MUST be a persistent field, never a local (Unity SmoothDamp contract).
    private float _offsetVelocityX;
    // Counts down after the push stops so the offset lingers before easing back (D-06).
    private float _offsetHoldTimer;
    // -1 while pushing the left edge, +1 while pushing the right edge, 0 while resting (D-05).
    private float _deadzonePushSign;
```

### 삽입 3 — 밀기 방향 초기화 (UpdateDeadzoneCenter 진입부)

앵커(`old_string`, 파일에서 유일):
```
        float px = target.position.x;
```

`new_string` = 앵커 + 아래 1줄:
```csharp
        _deadzonePushSign = 0f;
```

### 삽입 4 — 좌측 경계 밀기 방향

앵커(`old_string`, 파일에서 유일, 12칸 들여쓰기 그대로):
```
            _deadzoneCenterX = px + halfW;
```

`new_string` = 앵커 + 아래 1줄:
```csharp
            _deadzonePushSign = -1f;
```

### 삽입 5 — 우측 경계 밀기 방향

앵커(`old_string`, 파일에서 유일, 12칸 들여쓰기 그대로):
```
            _deadzoneCenterX = px - halfW;
```

`new_string` = 앵커 + 아래 1줄:
```csharp
            _deadzonePushSign = 1f;
```

### 삽입 6 — UpdateDynamicOffset 메서드 (OnDrawGizmos 앞에 삽입)

앵커(`old_string`, 파일에서 유일 — Plan 10-01 Task 3 이 넣은 Gizmo 주석 첫 줄):
```
    // Editor only deadzone visualization (D-03). Zero runtime cost in a build.
```

`new_string` = 아래 메서드 블록 + 빈 줄 + 위 앵커 라인 (즉 앵커 **앞에** 삽입):
```csharp
    // Dynamic asymmetrical deadzone (D-05 / D-06 / D-07). The offset only builds while the
    // target actually pushes a deadzone edge - there is no separate speed threshold (D-05).
    // After the push stops the offset is held for offsetHoldDuration, then eases back to 0 (D-06).
    private void UpdateDynamicOffset()
    {
        float targetOffsetX;
        if (_deadzonePushSign != 0f)
        {
            targetOffsetX = -(_deadzonePushSign * maxOffsetDistance);
            _offsetHoldTimer = offsetHoldDuration;
        }
        else if (_offsetHoldTimer > 0f)
        {
            _offsetHoldTimer -= Time.deltaTime;
            targetOffsetX = _currentBoxOffsetX;
        }
        else
        {
            targetOffsetX = 0f;
        }
        _currentBoxOffsetX = Mathf.SmoothDamp(_currentBoxOffsetX, targetOffsetX, ref _offsetVelocityX, offsetSmoothTime);
    }

    // Editor only deadzone visualization (D-03). Zero runtime cost in a build.
```

### 값 근거 (임의로 바꾸지 말 것)
- `maxOffsetDistance = 1.5f`, `offsetSmoothTime = 0.35f`, `offsetHoldDuration = 0.4f` — 사용자가 수치를 지정하지 않은 항목이다(10-CONTEXT "Claude's Discretion", 10-RESEARCH Open Question 2). 플레이테스트로 조정될 초기값이며 반드시 Inspector 필드로 노출한다. 하드코딩 상수로 숨기지 말 것.
- `targetOffsetX = -(_deadzonePushSign * maxOffsetDistance);` — 사용자 원본 수식 `TargetOffset = -(MoveDirection × MaxOffsetDistance)` 그대로다. 부호를 바꾸지 말 것 (카메라 쪽에서 빼는 것으로 방향을 맞춘다, 잠금 가정 A2).
- 유지 구간에서 `targetOffsetX = _currentBoxOffsetX;` 로 두는 이유: SmoothDamp 목표를 현재값으로 고정해 "그 자리에 머무르게" 하기 위함이다. 여기서 0 을 넣으면 D-06 의 유지 동작이 사라진다.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "public float maxOffsetDistance = 1.5f;" $F && grep -qF "public float offsetSmoothTime = 0.35f;" $F && grep -qF "public float offsetHoldDuration = 0.4f;" $F && grep -qF "private float _currentBoxOffsetX;" $F && grep -qF "private float _offsetVelocityX;" $F && grep -qF "private float _offsetHoldTimer;" $F && grep -qF "private float _deadzonePushSign;" $F && grep -qF "private void UpdateDynamicOffset()" $F && grep -qF "targetOffsetX = -(_deadzonePushSign * maxOffsetDistance);" $F && grep -qF "_offsetHoldTimer = offsetHoldDuration;" $F && grep -qF "_offsetHoldTimer -= Time.deltaTime;" $F && grep -qF "_currentBoxOffsetX = Mathf.SmoothDamp(_currentBoxOffsetX, targetOffsetX, ref _offsetVelocityX, offsetSmoothTime);" $F && [ "$(grep -cF '_deadzonePushSign = -1f;' $F)" = "1" ] && [ "$(grep -cF '_deadzonePushSign = 1f;' $F)" = "1" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "0" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - Inspector 필드 3개 존재: `grep -qF "public float maxOffsetDistance = 1.5f;"`, `grep -qF "public float offsetSmoothTime = 0.35f;"`, `grep -qF "public float offsetHoldDuration = 0.4f;"` (D-06/D-07 튜닝 대상)
    - 상태 필드 4개 존재: `_currentBoxOffsetX`, `_offsetVelocityX`, `_offsetHoldTimer`, `_deadzonePushSign` (모두 `private float` 선언)
    - `grep -qF "targetOffsetX = -(_deadzonePushSign * maxOffsetDistance);"` 성공 — 사용자 원본 수식과 문자 단위로 동일한 부호 구조
    - `grep -qF "_currentBoxOffsetX = Mathf.SmoothDamp(_currentBoxOffsetX, targetOffsetX, ref _offsetVelocityX, offsetSmoothTime);"` 성공 (D-07, ref 인자가 영속 필드)
    - `grep -cF '_deadzonePushSign = -1f;'` == 1 이고 `grep -cF '_deadzonePushSign = 1f;'` == 1 — 좌/우 각각 1회씩만 대입
    - `UpdateDeadzoneCenter` 안에 `Lerp` / `SmoothDamp` 문자열이 없다 (D-14 하드컷 유지)
    - `grep -cF 'velocity' Assets/Camera/Script/CameraController.cs` 결과에 `rigid`/`Rigidbody` 기반 참조가 없다 — 즉 `grep -cF 'Rigidbody' Assets/Camera/Script/CameraController.cs` == 0 (D-04 프레임 델타/경계 밀기 외의 속도 소스 금지)
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **이 태스크는 삽입 전용**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == `0` (수정 라인은 Task 2 에서만 발생)
  </acceptance_criteria>

  <done>`UpdateDeadzoneCenter` 가 밀기 방향(`_deadzonePushSign`)을 기록하고, `UpdateDynamicOffset` 이 유지 타이머 + SmoothDamp 로 `_currentBoxOffsetX` 를 갱신한다. 아직 카메라 위치에는 반영되지 않는다(Task 2). 삭제 라인 0줄, 비-ASCII 라인 수 5 유지.</done>
</task>

<task type="auto">
  <name>Task 2: 오프셋을 카메라 X 합성에 반영 + 보스 구역 상태 초기화 + 클램프 재동기화 보정</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **Task 1 적용 후 상태를 다시 Read 한다.**
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-02-PLAN.md` — 이 파일의 `<critical_constraints>` §3 부호 규약(A2)을 다시 확인한 뒤 편집한다.
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md` — D-15(보스 구역 전면 비활성화), D-17(클램프 마지막)
  </read_first>

  <action>
`Edit` 도구로 **4회** 편집한다. 이 중 **2회는 기존 ASCII 라인 1줄 수정**이고(허용됨, `<critical_constraints>` §1 게이트 3 참조), 2회는 삽입이다.

### 편집 1 (삽입) — ApplyNormalStageCamera 에서 오프셋 갱신 호출

앵커(`old_string`, 파일에서 유일 — `ApplyNormalStageCamera` 첫 줄):
```
        UpdateDeadzoneCenter();
```

`new_string`:
```csharp
        UpdateDeadzoneCenter();
        UpdateDynamicOffset();
```

### 편집 2 (라인 수정 1/2) — 카메라 X 합성

`old_string`(파일에서 유일):
```
        p.x = _deadzoneCenterX;
```

`new_string`:
```csharp
        p.x = _deadzoneCenterX - _currentBoxOffsetX;
```

> **부호 주의**: 반드시 `-` 다. `+` 로 쓰면 진행 방향의 반대쪽 시야가 열려 D-05 의도가 뒤집힌다 (A2).

### 편집 3 (삽입) — 보스 구역에서 오프셋 상태 초기화

앵커(`old_string`, 파일에서 유일 — `ResetNormalStageState` 본문 2번째 줄):
```
        _followBaseY = transform.position.y;
```

`new_string` = 앵커 + 아래 4줄:
```csharp
        _currentBoxOffsetX = 0f;
        _offsetVelocityX = 0f;
        _offsetHoldTimer = 0f;
        _deadzonePushSign = 0f;
```

### 편집 4 (라인 수정 2/2) — 클램프 후 앵커 재동기화에 오프셋 반영

`old_string`(파일에서 유일):
```
        if (!_isBossZone) _deadzoneCenterX = transform.position.x;
```

`new_string`:
```csharp
        if (!_isBossZone) _deadzoneCenterX = transform.position.x + _currentBoxOffsetX;
```

> 근거: 카메라 X = 중심 - 오프셋 이므로, 클램프로 카메라가 이동했다면 중심 = 카메라 X + 오프셋 으로 역산해야 다음 프레임 판정이 일관된다. 이 보정이 없으면 맵 좌우 끝에서 카메라가 한동안 반응하지 않는 "죽은 이동 구간"이 생긴다 (D-17).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "p.x = _deadzoneCenterX - _currentBoxOffsetX;" $F && [ "$(grep -cF 'p.x = _deadzoneCenterX;' $F)" = "0" ] && grep -qF "if (!_isBossZone) _deadzoneCenterX = transform.position.x + _currentBoxOffsetX;" $F && grep -qF "UpdateDynamicOffset();" $F && [ "$(grep -cF 'UpdateDynamicOffset' $F)" = "2" ] && grep -qF "_currentBoxOffsetX = 0f;" $F && grep -qF "_offsetVelocityX = 0f;" $F && grep -qF "_offsetHoldTimer = 0f;" $F && DZ=$(grep -nF 'UpdateDeadzoneCenter();' $F | head -1 | cut -d: -f1) && DO=$(grep -nF 'UpdateDynamicOffset();' $F | head -1 | cut -d: -f1) && PX=$(grep -nF 'p.x = _deadzoneCenterX - _currentBoxOffsetX;' $F | cut -d: -f1) && [ "$DO" -gt "$DZ" ] && [ "$PX" -gt "$DO" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "2" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]')" = "0" ] && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "p.x = _deadzoneCenterX - _currentBoxOffsetX;"` 성공이고 `grep -cF "p.x = _deadzoneCenterX;"` == 0 (부호 규약 A2 — `+` 로 되어 있으면 실패로 간주)
    - `grep -qF "if (!_isBossZone) _deadzoneCenterX = transform.position.x + _currentBoxOffsetX;"` 성공 (D-17 역산 보정)
    - `grep -cF 'UpdateDynamicOffset' Assets/Camera/Script/CameraController.cs` == 2 (메서드 정의 1 + `ApplyNormalStageCamera` 호출 1)
    - **호출 순서**: `UpdateDeadzoneCenter();` 행 번호 < `UpdateDynamicOffset();` 호출 행 번호 < `p.x = _deadzoneCenterX - _currentBoxOffsetX;` 행 번호 (밀기 방향이 먼저 계산되어야 오프셋이 그 프레임 방향을 반영)
    - `ResetNormalStageState` 안에 `_currentBoxOffsetX = 0f;` / `_offsetVelocityX = 0f;` / `_offsetHoldTimer = 0f;` / `_deadzonePushSign = 0f;` 4줄이 모두 존재 (D-15 보스 구역 전면 비활성화)
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **삭제 라인 게이트**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == `2` 이고, 그 2줄은 `p.x = _deadzoneCenterX;` 와 `if (!_isBossZone) _deadzoneCenterX = transform.position.x;` 뿐이다
    - **한글 보존 게이트**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]'` == `0`
  </acceptance_criteria>

  <done>일반 스테이지에서 카메라 X = `_deadzoneCenterX - _currentBoxOffsetX` 로 합성되어 진행 방향 시야가 열리고, 보스 구역 진입 시 오프셋 상태가 매 프레임 0 으로 초기화되어 Phase 9 동작에 영향이 없다. 삭제 라인 정확히 2줄(둘 다 ASCII), 비-ASCII 라인 수 5 유지.</done>
</task>

</tasks>

<verification>
1. `ApplyNormalStageCamera` 실행 순서: `UpdateDeadzoneCenter()` → `UpdateDynamicOffset()` → X/Y 합성.
2. Inspector 신규 노출 필드 3개: `maxOffsetDistance`(1.5), `offsetSmoothTime`(0.35), `offsetHoldDuration`(0.4).
3. 오프셋 발동 조건이 `_deadzonePushSign != 0f` 뿐이다 — 별도 속도 임계값 코드가 없다 (D-05).
4. 데드존 하드컷 경로(`UpdateDeadzoneCenter`)에 여전히 Lerp/SmoothDamp 가 없다 (D-14).
5. 인코딩 게이트 통과: 비-ASCII 포함 라인 수 == 5, 삭제 라인 중 비-ASCII 포함 0줄, 총 삭제 라인 2줄.
6. `git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs` 가 비어 있다.
</verification>

<success_criteria>
- 데드존 경계를 밀 때만 오프셋이 발동한다 (D-05).
- 오프셋이 `SmoothDamp` 로 부드럽게 전환되고, 멈춘 뒤 `offsetHoldDuration` 만큼 유지된 후 복귀한다 (D-06, D-07).
- 진행 방향으로 시야가 열린다 — 오른쪽 이동 시 카메라가 오른쪽으로 앞서 나간다 (A2 부호 규약).
- 보스 구역에서는 오프셋이 카메라에 전혀 영향을 주지 않는다 (D-15).
</success_criteria>

<output>
완료 후 `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-02-SUMMARY.md` 를 생성한다.
커밋은 `Assets/Camera/Script/CameraController.cs` 경로만 지정해서 수행한다.
</output>
