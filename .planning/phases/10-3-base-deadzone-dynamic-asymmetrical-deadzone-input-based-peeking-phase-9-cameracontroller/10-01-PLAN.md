---
phase: 10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Camera/Script/CameraController.cs
autonomous: true
requirements: [D-01, D-02, D-03, D-14, D-15, D-16, D-17]
must_haves:
  truths:
    - "일반 스테이지에서 플레이어가 데드존 박스 안에서 움직이는 동안 카메라 X 는 전혀 움직이지 않는다"
    - "플레이어가 박스 좌/우 경계를 밀고 나가면 카메라가 밀려난 만큼 즉시(하드컷) 따라붙는다"
    - "보스 구역(SetBossZoom(true)) 안에서는 데드존이 완전히 꺼지고 Phase 9 의 레거시 Lerp 추종이 그대로 동작한다"
    - "데드존 박스 크기를 Inspector 에서 조정할 수 있고 Scene 뷰에 박스 윤곽이 Gizmo 로 보인다"
    - "줌 Lerp 와 X 클램프는 두 경로 모두에서 Phase 9 그대로 마지막에 실행된다"
    - "CameraController.cs 의 기존 CP949 한글 주석이 한 줄도 변경/삭제되지 않는다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "_isBossZone 분기 + 하드컷 데드존 X + 데드존 Gizmo"
      contains: "private void UpdateDeadzoneCenter()"
  key_links:
    - from: "CameraController.SetBossZoom"
      to: "CameraController.LateUpdate 분기"
      via: "_isBossZone 필드"
      pattern: "_isBossZone = isBossStage;"
    - from: "CameraController.LateUpdate"
      to: "ApplyNormalStageCamera / ResetNormalStageState"
      via: "보스 구역 여부 분기 (D-15)"
      pattern: "if \\(_isBossZone\\) ResetNormalStageState\\(\\);"
    - from: "ApplyNormalStageCamera"
      to: "transform.position.x"
      via: "_deadzoneCenterX 하드컷 대입 (Lerp 없음, D-14)"
      pattern: "p\\.x = _deadzoneCenterX;"
---

<objective>
`Assets/Camera/Script/CameraController.cs` (현재 79줄, Phase 9 완료 상태)에 **Base Deadzone(하드컷) + 보스 구역 분기 + 데드존 Gizmo** 를 삽입만으로 추가한다.

Purpose: Phase 10 의 3개 레이어(데드존 / 동적 오프셋 / 피킹) 중 가장 아래 레이어와, 나머지 두 레이어가 올라탈 실행 구조(`ApplyNormalStageCamera` / `ResetNormalStageState` 헬퍼 + `_isBossZone` 분기)를 먼저 확정한다.
Output: 확장된 `CameraController.cs` 1개 파일. 신규 파일 없음, 씬 편집 없음, `PlayerController.cs`/`InputHandler.cs` 무수정.

**이 플랜의 CameraController.cs 변경은 100% 라인 삽입이다. 기존 라인은 한 줄도 삭제/수정하지 않는다.**
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-RESEARCH.md
@Assets/Camera/Script/CameraController.cs
</context>

<critical_constraints>
## 1. 파일 인코딩 (Phase 9 에서 이미 겪은 문제 — 최우선 주의사항)

`CameraController.cs` 는 UTF-8 이 아닌 CP949 로 저장되어 있다. Read 로 읽으면 한글 주석이 `���ΰ�` 같은 깨진 문자로 보인다. **이것은 이 파일의 기존 상태이며 이번 작업으로 고치지 않는다.**

- **`Write` 도구로 파일 전체를 다시 쓰지 말 것.** 반드시 `Edit` 도구로 지정된 앵커에만 삽입한다.
- **`old_string` 에 깨진 한글 주석 라인을 절대 포함하지 말 것.** 아래 지정된 ASCII 전용 앵커만 사용한다.
- **새로 추가하는 주석은 ASCII 영문만 사용한다 (한글 금지).** 이 플랜의 코드 블록에 적힌 영문 주석을 그대로 쓴다.
- 검증 게이트 1: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == **5** (현재 값 = 5)
- 검증 게이트 2 (삭제된 라인에 한글 없음):
  `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]'` == **0**
- 이 플랜에 한해 추가 게이트 3 (완전 삽입 전용):
  `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == **0**
  (Plan 10-02/10-03 은 ASCII 라인 1~2줄을 수정하므로 게이트 3 이 완화되지만, **이 플랜은 삭제 0줄이어야 한다.**)

`ef6f164` 는 Phase 9 종료 시점 커밋이며, 이 파일의 baseline 이다.

## 2. 작업 트리 상태

리포지토리에 Phase 10 과 무관한 미커밋 변경(TutorialBoss, 메뉴 패널, 씬 등)이 다수 존재한다.
**`git add -A` / `git commit -a` 를 절대 사용하지 말고**, `Assets/Camera/Script/CameraController.cs` 경로만 명시해 커밋한다.

## 3. 잠금 가정 (Locked Assumptions — 실행 중 뒤집지 말 것)

10-RESEARCH.md 가 Open Question 으로 남긴 항목을 이 플랜이 확정한다. 실행자는 재논의하지 말고 아래대로 구현한다.

**A1 — 하드컷 데드존은 X축 전용이다.**
사용자 원본 수식(10-CONTEXT.md `<specifics>` 1번)은 전부 X 기준이고, `<deferred>` 는 "Y축 데드존은 이번 Phase 범위 밖"이라고 명시했다. 따라서:
- `deadzoneWidth` 만 실제로 카메라 X 이동을 게이트한다.
- `deadzoneHeight` 는 **Gizmo 표시 및 Inspector 완결성 전용**이며 이번 Phase 에서 카메라 이동을 게이트하지 않는다 (D-01 이 "가로/세로 별도 필드"를 요구했으므로 필드 자체는 존재한다).
- 카메라 Y 는 기존과 동일하게 `smoothing` Lerp 로 타겟을 계속 추종한다 (일반 스테이지에서 Y 동작 회귀 없음). Y 를 하드컷으로 바꾸지 말 것 — 점프할 때마다 카메라가 딱딱 끊긴다.
- 일반 스테이지에서 카메라 Y 를 별도로 움직이는 유일한 메커니즘은 Plan 10-03 의 Peeking 이다.

**A2 — 기존 레거시 Lerp 라인은 건드리지 않고, 그 결과를 덮어쓰는 방식으로 분기한다.**
`LateUpdate` 의 기존 2줄(`Vector3 targetCamPos = ...` / `transform.position = Vector3.Lerp(...)`)을 `if` 블록으로 감싸거나 들여쓰기를 바꾸면 삭제 라인이 발생한다. 대신 그 아래에 분기 1줄을 삽입해, 일반 스테이지에서는 `ApplyNormalStageCamera()` 가 X/Y 를 덮어쓴다. 보스 구역에서는 아무것도 덮어쓰지 않으므로 Phase 9 동작이 바이트 단위로 보존된다 (D-15). 레거시 Lerp 가 매 프레임 항상 실행되는 것은 의도된 설계다 (Z 축 추종도 이 라인이 계속 담당한다).

## 4. 범위 밖 (하지 말 것)

- `PlayerController.cs`, `InputHandler.cs` 수정 금지 (D-04, D-08~D-11).
- 이 플랜에서 동적 오프셋(`SmoothDamp`)·피킹·입력 구독 코드 추가 금지 — 각각 Plan 10-02, 10-03 담당이다.
- Y축 데드존 / `minY`·`maxY` 클램프 추가 금지 (A1, 10-CONTEXT `<deferred>`).
- 줌 Lerp(`normalZoom`/`bossZoom`/`zoomSmoothing`) 및 `ApplyXClamp()` 본문 수정 금지 (D-16, D-17 — 변경 없음이 요구사항).
- Cinemachine 도입 금지.
- 데드존 박스를 줌 비율로 스케일하는 코드 금지 (D-02 — 고정 월드 크기).
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: _isBossZone 상태 + 데드존 Inspector 필드 + 내부 상태 필드 + Start 초기화</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **작업 직전에 반드시 Read 로 현재 내용을 확인한다.** `Edit` 의 `old_string` 은 이 Read 결과에서 그대로 복사한 ASCII 라인이어야 한다 (기억으로 재작성 금지).
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md` — D-01, D-02, D-15 원문
    - `.planning/phases/09-camera-zoom-stage-transition/09-01-PLAN.md` — CP949 삽입 전용 편집 프로토콜(§1 파일 인코딩)의 원전
  </read_first>

  <action>
`Edit` 도구로 **4회 삽입**한다. 각 삽입은 지정된 앵커를 `old_string` 으로 삼고, `new_string` = 앵커 + 새 라인들이다 (앵커 라인 자체는 절대 변경하지 않는다).

### 삽입 1 — 데드존 Inspector 필드

앵커(`old_string`, 파일에서 유일):
```
    public float maxX = 1000f;
```

`new_string` = 앵커 + 아래 블록(앞 빈 줄 포함):
```csharp

    [Header("Deadzone (normal stages only)")]
    // Deadzone box size in WORLD units. Fixed size, never scaled by zoom (D-01 / D-02).
    // deadzoneWidth gates camera X movement. deadzoneHeight is Gizmo / Inspector only in
    // this phase - the hard cut deadzone is X axis only (locked assumption A1 in 10-01-PLAN).
    public float deadzoneWidth = 3f;
    public float deadzoneHeight = 2f;
```

### 삽입 2 — 내부 상태 필드

앵커(`old_string`, 파일에서 유일):
```
    private float _targetZoom;
```

`new_string` = 앵커 + 아래 블록:
```csharp

    // True while the player is inside a BossZoomTrigger zone (D-15). SetBossZoom stores it
    // so LateUpdate can bypass the whole deadzone pipeline and keep the Phase 9 behaviour.
    private bool _isBossZone;

    // Resting X of the deadzone box center. The camera parks here instead of chasing the
    // target every frame - this is what makes the box feel "sticky" (D-14 hard cut).
    private float _deadzoneCenterX;
    // Un-peeked Y baseline the camera follows with the legacy 'smoothing' rate.
    // Kept separate from transform.position.y so later offset layers cannot feed back into it.
    private float _followBaseY;
```

### 삽입 3 — SetBossZoom 본문에 boss zone 상태 저장

앵커(`old_string`, 파일에서 유일):
```
        _targetZoom = isBossStage ? bossZoom : normalZoom;
```

`new_string` = 앵커 + 아래 2줄:
```csharp
        // Store the zone state itself, not just the zoom value - LateUpdate branches on it (D-15).
        _isBossZone = isBossStage;
```

### 삽입 4 — Start 초기화

앵커(`old_string`, 2줄 연속 — `ApplyXClamp();` 는 파일에 2번 나오므로 반드시 앞 라인을 함께 지정한다):
```
        _cam.orthographicSize = normalZoom;
        ApplyXClamp();
```

`new_string` = 앵커 2줄 + 아래 2줄:
```csharp
        // Park the deadzone box and the follow baseline on the camera's start position.
        ResetNormalStageState();
```

> `ResetNormalStageState()` 는 Task 2 에서 정의한다. Task 1 만 적용한 중간 상태에서는 컴파일되지 않으므로 **Task 1 과 Task 2 는 연속으로 수행하고 커밋은 Task 2 이후 1회로 묶는다.**

### 값 근거 (임의로 바꾸지 말 것)
- `deadzoneWidth = 3f`, `deadzoneHeight = 2f` — 사용자가 수치를 지정하지 않은 항목이다(10-RESEARCH Open Question 2). 플레이테스트로 조정될 초기값이며 Inspector 에서 튜닝한다. 하드코딩 상수로 숨기지 말 것.
- `public` 필드 — 이 파일의 기존 스타일(`public float normalZoom = 5f;`)과 동일. `[SerializeField] private` 로 바꾸지 말 것.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "public float deadzoneWidth = 3f;" $F && grep -qF "public float deadzoneHeight = 2f;" $F && grep -qF "private bool _isBossZone;" $F && grep -qF "private float _deadzoneCenterX;" $F && grep -qF "private float _followBaseY;" $F && grep -qF "_isBossZone = isBossStage;" $F && grep -qF "ResetNormalStageState();" $F && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "0" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "public float deadzoneWidth = 3f;" Assets/Camera/Script/CameraController.cs` 성공 (D-01)
    - `grep -qF "public float deadzoneHeight = 2f;" Assets/Camera/Script/CameraController.cs` 성공 (D-01)
    - `grep -qF "private bool _isBossZone;"` 성공 (D-15, 10-RESEARCH Pitfall 2)
    - `grep -qF "_isBossZone = isBossStage;"` 성공 — `SetBossZoom` 본문 안에 있어야 하며 시그니처는 변경 금지
    - `grep -c "public void SetBossZoom(bool isBossStage)" Assets/Camera/Script/CameraController.cs` == 1 (시그니처 무변경 — `BossZoomTrigger.cs` 호출부 보존)
    - `grep -qF "private float _deadzoneCenterX;"` / `grep -qF "private float _followBaseY;"` 성공
    - `grep -cF "_targetZoom == bossZoom" Assets/Camera/Script/CameraController.cs` == 0 (float 동등 비교로 보스 구역 판정 금지 — 10-RESEARCH Anti-Pattern)
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **삽입 전용 게이트**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == `0`
  </acceptance_criteria>

  <done>`CameraController` 가 `_isBossZone` 불리언을 `SetBossZoom` 안에서 저장하고, Inspector 에 `deadzoneWidth`/`deadzoneHeight` 가 노출되며, `_deadzoneCenterX`/`_followBaseY` 상태 필드와 Start 초기화 호출이 삽입되어 있다. 기존 라인 삭제 0줄, 비-ASCII 라인 수 5 유지. (컴파일은 Task 2 완료 후 성립)</done>
</task>

<task type="auto">
  <name>Task 2: 하드컷 데드존 헬퍼 3종 + LateUpdate 보스/일반 분기 + 클램프 후 앵커 재동기화</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **Task 1 적용 후 상태를 다시 Read 한다.** 앵커는 이 Read 결과에서 복사한다.
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md` — `<specifics>` 1번 Base Deadzone 원본 수식, D-14, D-15, D-17
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-RESEARCH.md` — Pattern 2(카메라 기준 박스), Anti-Patterns(데드존 자체를 Lerp 하지 말 것)
  </read_first>

  <action>
`Edit` 도구로 **3회 삽입**한다.

### 삽입 1 — 헬퍼 메서드 3종 (ApplyXClamp 메서드 뒤)

앵커(`old_string`, 3줄 연속 — 파일에서 유일):
```
        pos.x = Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth);
        transform.position = pos;
    }
```

`new_string` = 앵커 3줄 + 아래 블록:
```csharp

    // Hard cut deadzone (D-14). The camera does not move at all while the target stays inside
    // the box; when the target pushes an edge the box center snaps by exactly the overrun.
    // X axis only - camera Y keeps following the target (locked assumption A1 in 10-01-PLAN).
    // Do NOT Lerp this: smoothing it would open a gap between the box edge and the camera,
    // which defeats the "completely still inside the box" goal (D-14).
    private void UpdateDeadzoneCenter()
    {
        float halfW = deadzoneWidth * 0.5f;
        float px = target.position.x;
        if (px < _deadzoneCenterX - halfW)
        {
            _deadzoneCenterX = px + halfW;
        }
        else if (px > _deadzoneCenterX + halfW)
        {
            _deadzoneCenterX = px - halfW;
        }
    }

    // Normal stage camera composition. Runs AFTER the legacy follow Lerp in LateUpdate and
    // overwrites its X and Y, which is why that legacy line stays byte identical for the
    // boss path (D-15). Z is still handled by the legacy line above.
    private void ApplyNormalStageCamera()
    {
        UpdateDeadzoneCenter();
        _followBaseY = Mathf.Lerp(_followBaseY, target.position.y + offset.y, smoothing * Time.deltaTime);
        Vector3 p = transform.position;
        p.x = _deadzoneCenterX;
        p.y = _followBaseY;
        transform.position = p;
    }

    // Boss zones bypass the deadzone pipeline entirely (D-15). Syncing the anchors to the
    // legacy camera position every frame means returning to a normal stage does not jump.
    // Also used once from Start to seed the anchors.
    private void ResetNormalStageState()
    {
        _deadzoneCenterX = transform.position.x;
        _followBaseY = transform.position.y;
    }
```

### 삽입 2 — LateUpdate 분기 (레거시 Lerp 라인 바로 뒤)

앵커(`old_string`, 파일에서 유일):
```
        transform.position = Vector3.Lerp(transform.position, targetCamPos, smoothing * Time.deltaTime);
```

`new_string` = 앵커 + 아래 3줄:
```csharp
        // Normal stages overwrite the legacy follow result with the deadzone pipeline (D-14).
        // Boss zones leave the legacy Lerp above untouched, exactly as in Phase 9 (D-15).
        if (_isBossZone) ResetNormalStageState(); else ApplyNormalStageCamera();
```

### 삽입 3 — X 클램프 이후 앵커 재동기화 (LateUpdate 맨 끝)

앵커(`old_string`, 2줄 연속 — `ApplyXClamp();` 중복 방지를 위해 주석 라인을 함께 지정한다. LateUpdate 쪽에만 이 주석이 있다):
```
        // X clamp LAST so it uses this frame's freshly updated orthographicSize.
        ApplyXClamp();
```

`new_string` = 앵커 2줄 + 아래 2줄:
```csharp
        // Re-anchor on the clamped position so the camera responds immediately when the
        // target walks back from a clamped map edge instead of eating dead travel (D-17).
        if (!_isBossZone) _deadzoneCenterX = transform.position.x;
```

### 설계 근거 (임의로 바꾸지 말 것)
- `UpdateDeadzoneCenter` 는 `transform.position.x` 가 아니라 **자체 상태 `_deadzoneCenterX`** 를 기준으로 판정한다. Plan 10-02 가 카메라 X 를 `_deadzoneCenterX` 에서 오프셋만큼 떼어놓을 때, 박스 기준을 카메라 위치로 잡으면 오프셋이 박스를 끌고 다니는 되먹임(feedback)이 생기기 때문이다.
- `_followBaseY` 도 같은 이유로 `transform.position.y` 와 분리한다 (Plan 10-03 의 피킹 오프셋 되먹임 방지).
- `if (_isBossZone) ResetNormalStageState(); else ApplyNormalStageCamera();` 를 **한 줄**로 쓴 이유는 없다 — 여러 줄 `if/else` 블록으로 써도 무방하다. 단 기존 레거시 2줄을 감싸거나 들여쓰기를 바꾸는 형태는 금지다(삭제 라인 발생).
- 데드존 계산에 `Vector3.Lerp` / `Mathf.Lerp` / `SmoothDamp` 를 절대 쓰지 말 것 (D-14).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "private void UpdateDeadzoneCenter()" $F && grep -qF "private void ApplyNormalStageCamera()" $F && grep -qF "private void ResetNormalStageState()" $F && grep -qF "float halfW = deadzoneWidth * 0.5f;" $F && grep -qF "_deadzoneCenterX = px + halfW;" $F && grep -qF "_deadzoneCenterX = px - halfW;" $F && grep -qF "p.x = _deadzoneCenterX;" $F && grep -qF "p.y = _followBaseY;" $F && grep -qF "if (_isBossZone) ResetNormalStageState(); else ApplyNormalStageCamera();" $F && grep -qF "if (!_isBossZone) _deadzoneCenterX = transform.position.x;" $F && [ "$(grep -cF 'minY' $F)" = "0" ] && [ "$(grep -cF 'maxY' $F)" = "0" ] && LEG=$(grep -nF 'Vector3.Lerp(transform.position, targetCamPos' $F | cut -d: -f1) && BR=$(grep -nF 'if (_isBossZone) ResetNormalStageState();' $F | cut -d: -f1) && ZL=$(grep -nF 'Mathf.Lerp(_cam.orthographicSize' $F | cut -d: -f1) && CL=$(grep -nF 'ApplyXClamp();' $F | tail -1 | cut -d: -f1) && [ "$BR" -gt "$LEG" ] && [ "$ZL" -gt "$BR" ] && [ "$CL" -gt "$ZL" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "0" ] && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "private void UpdateDeadzoneCenter()"` / `"private void ApplyNormalStageCamera()"` / `"private void ResetNormalStageState()"` 3개 모두 성공
    - `grep -qF "float halfW = deadzoneWidth * 0.5f;"` 성공 (D-01 박스 반폭)
    - `grep -qF "_deadzoneCenterX = px + halfW;"` 및 `grep -qF "_deadzoneCenterX = px - halfW;"` 성공 — 사용자 원본 수식(좌 경계 초과 → 좌로, 우 경계 초과 → 우로)
    - `grep -qF "p.x = _deadzoneCenterX;"` 성공 — 하드컷 대입이며 `Lerp`/`SmoothDamp` 를 거치지 않음 (D-14)
    - `UpdateDeadzoneCenter` 메서드 본문 안에 `Lerp` 또는 `SmoothDamp` 문자열이 없다 (D-14 하드컷)
    - `grep -qF "if (_isBossZone) ResetNormalStageState(); else ApplyNormalStageCamera();"` 성공 (D-15)
    - `grep -qF "if (!_isBossZone) _deadzoneCenterX = transform.position.x;"` 성공 (D-17 클램프 후 재동기화)
    - **행 번호 순서**: 레거시 `Vector3.Lerp(transform.position, targetCamPos` < 보스 분기 라인 < `Mathf.Lerp(_cam.orthographicSize` < 마지막 `ApplyXClamp();` (D-16 / D-17 순서 보존)
    - `grep -cF 'minY'` == 0, `grep -cF 'maxY'` == 0 (Y축 확장 금지, A1)
    - `grep -cF 'deadzoneHeight' Assets/Camera/Script/CameraController.cs` == 1 (Task 1 의 필드 선언 1곳뿐 — 이번 Phase 에서 Y 게이트에 사용하지 않음, A1). Task 3 의 Gizmo 추가 후에는 2가 된다.
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **삽입 전용 게이트**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == `0`
  </acceptance_criteria>

  <done>일반 스테이지에서 카메라 X 가 `_deadzoneCenterX` 하드컷으로만 움직이고, 보스 구역에서는 Phase 9 레거시 Lerp 결과가 그대로 유지되며, 줌 Lerp → X 클램프 → 앵커 재동기화 순서가 성립한다. 기존 라인 삭제 0줄, 비-ASCII 라인 수 5 유지.</done>
</task>

<task type="auto">
  <name>Task 3: 데드존 박스 OnDrawGizmos 시각화</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — Task 2 적용 후 상태를 다시 Read 한다.
    - `Assets/Enemy/WaterMonster/Script/Phase2/WeatherController.cs` — 이 프로젝트의 기존 Gizmo 관례(반투명 `DrawCube` + `DrawWireCube` 조합) 확인용. 44-56행 부근.
    - `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-CONTEXT.md` — D-03 원문
  </read_first>

  <action>
`Edit` 도구로 **1회 삽입**한다.

앵커(`old_string`, 3줄 연속 — Task 2 가 추가한 `ResetNormalStageState` 본문과 닫는 중괄호):
```
        _deadzoneCenterX = transform.position.x;
        _followBaseY = transform.position.y;
    }
```

`new_string` = 앵커 3줄 + 아래 블록:
```csharp

    // Editor only deadzone visualization (D-03). Zero runtime cost in a build.
    // In play mode the box is drawn at its actual resting center; in edit mode it falls back
    // to the camera transform so the size can still be eyeballed before pressing Play.
    private void OnDrawGizmos()
    {
        float centerX = Application.isPlaying ? _deadzoneCenterX : transform.position.x;
        Vector3 center = new Vector3(centerX, transform.position.y, 0f);
        Vector3 size = new Vector3(deadzoneWidth, deadzoneHeight, 0f);
        Gizmos.color = new Color(1f, 1f, 0f, 0.12f);
        Gizmos.DrawCube(center, size);
        Gizmos.color = Color.yellow;
        Gizmos.DrawWireCube(center, size);
    }
```

### 설계 근거
- 박스 중심은 `_deadzoneCenterX` 다. Plan 10-02 가 카메라를 이 중심에서 오프셋만큼 떼어놓아도 Gizmo 는 그대로 유효하다 (박스는 월드에 고정, 카메라가 박스 기준으로 앞서 나가는 구조).
- `deadzoneHeight` 는 여기서만 실제로 쓰인다 (A1 — Y 게이트에는 사용하지 않는다).
- Gizmo 색상은 Claude 재량 영역이다. 노란색 유지 권장(다른 시스템 Gizmo 와 구분).
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "private void OnDrawGizmos()" $F && grep -qF "Gizmos.DrawWireCube(center, size);" $F && grep -qF "Vector3 size = new Vector3(deadzoneWidth, deadzoneHeight, 0f);" $F && grep -qF "Application.isPlaying ? _deadzoneCenterX : transform.position.x;" $F && [ "$(grep -cF 'deadzoneHeight' $F)" = "2" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff ef6f164 -- $F | grep '^-' | grep -vc '^---')" = "0" ] && echo T3_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "private void OnDrawGizmos()" Assets/Camera/Script/CameraController.cs` 성공 (D-03)
    - `grep -qF "Gizmos.DrawWireCube(center, size);"` 및 `grep -qF "Gizmos.DrawCube(center, size);"` 성공
    - `grep -qF "Vector3 size = new Vector3(deadzoneWidth, deadzoneHeight, 0f);"` 성공 — 박스 크기가 Inspector 필드에서 직접 온다 (하드코딩 금지)
    - `grep -cF 'deadzoneHeight' Assets/Camera/Script/CameraController.cs` == 2 (필드 선언 1 + Gizmo 사용 1. 3 이상이면 A1 위반 — Y 게이트에 쓰인 것)
    - `grep -qF "Application.isPlaying ? _deadzoneCenterX : transform.position.x;"` 성공
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == `5`
    - **삽입 전용 게이트**: `git diff ef6f164 -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -vc '^---'` == `0`
  </acceptance_criteria>

  <done>Scene 뷰에서 `deadzoneWidth`/`deadzoneHeight` 크기의 노란 박스가 카메라(플레이 중에는 데드존 중심) 위치에 그려지며, Inspector 값 변경이 즉시 반영된다. 기존 라인 삭제 0줄, 비-ASCII 라인 수 5 유지.</done>
</task>

</tasks>

<verification>
1. `LateUpdate` 실행 순서: 레거시 위치 추종 Lerp → 보스/일반 분기 → 줌 Lerp → X 클램프 → 데드존 앵커 재동기화 (행 번호로 확인).
2. Inspector 신규 노출 필드 2개: `deadzoneWidth`(3), `deadzoneHeight`(2).
3. `SetBossZoom(bool isBossStage)` 시그니처 무변경 — `Assets/Camera/Script/BossZoomTrigger.cs` 는 이번 플랜에서 0줄 변경이다.
4. 인코딩 게이트 통과: 비-ASCII 포함 라인 수 == 5, 삭제 라인 0줄.
5. `git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs` 결과가 비어 있다 (읽기 전용 참조 파일 무수정, D-04/D-08).
6. 이 플랜에서 수정된 파일은 `Assets/Camera/Script/CameraController.cs` 단 1개다.
</verification>

<success_criteria>
- 보스 구역 여부가 `_isBossZone` 불리언으로 보존되고 `LateUpdate` 가 이 값으로 분기한다 (D-15).
- 일반 스테이지 카메라 X 가 하드컷 데드존으로만 움직인다 — 데드존 계산 경로에 Lerp/SmoothDamp 없음 (D-14).
- 보스 구역 경로가 Phase 9 코드와 동작상 동일하다 (레거시 Lerp 결과를 아무도 덮어쓰지 않음, D-15/D-16).
- 데드존 박스가 고정 월드 크기이며 Gizmo 로 확인 가능하다 (D-01, D-02, D-03).
- 기존 CP949 한글 주석과 Phase 9 로직이 손상되지 않았다.
</success_criteria>

<output>
완료 후 `.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-01-SUMMARY.md` 를 생성한다.
커밋은 `Assets/Camera/Script/CameraController.cs` 경로만 지정해서 수행한다 (작업 트리에 Phase 10 무관 미커밋 변경 다수 존재). Task 1 단독으로는 컴파일되지 않으므로 커밋은 Task 2 이후에 한다.
</output>
