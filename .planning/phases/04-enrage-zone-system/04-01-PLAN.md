---
phase: 04-enrage-zone-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/WaterMonster/Script/Phase4/SpeedUpZone.cs
  - Assets/Enemy/WaterMonster/Script/Phase4/SlowDownZone.cs
  - Assets/Player/Script/PlayerController.cs
  - Assets/Enemy/NewBoss/Script/States/CombatState.cs
  - Assets/Enemy/NewBoss/Script/BossStatesSystem.cs
autonomous: true
requirements: [REQ-WM-P4-01, REQ-WM-X-01]

must_haves:
  truths:
    - "SpeedUpZone 진입 시 플레이어 이동 속도가 증가한다"
    - "SlowDownZone 진입 시 플레이어 이동 속도가 감소한다"
    - "Zone 퇴장 시 원래 이동 속도로 복원된다"
    - "Zone은 Player 레이어에만 영향을 준다"
    - "CombatState._decisionTimer가 protected로 접근 가능하다"
    - "BossStatsSystem.Update()가 virtual로 오버라이드 가능하다"
  artifacts:
    - path: "Assets/Enemy/WaterMonster/Script/Phase4/SpeedUpZone.cs"
      provides: "Player speed buff trigger zone"
      contains: "class SpeedUpZone"
    - path: "Assets/Enemy/WaterMonster/Script/Phase4/SlowDownZone.cs"
      provides: "Player speed debuff trigger zone"
      contains: "class SlowDownZone"
    - path: "Assets/Player/Script/PlayerController.cs"
      provides: "speedModifier field for zone integration"
      contains: "speedModifier"
    - path: "Assets/Enemy/NewBoss/Script/States/CombatState.cs"
      provides: "protected _decisionTimer for subclass access"
      contains: "protected float _decisionTimer"
    - path: "Assets/Enemy/NewBoss/Script/BossStatesSystem.cs"
      provides: "virtual Update for subclass override"
      contains: "protected virtual void Update"
  key_links:
    - from: "SpeedUpZone.cs"
      to: "PlayerController.cs"
      via: "speedModifier field modification"
      pattern: "speedModifier"
    - from: "SlowDownZone.cs"
      to: "PlayerController.cs"
      via: "speedModifier field modification"
      pattern: "speedModifier"
---

<objective>
Zone 프리팹 스크립트(SpeedUpZone, SlowDownZone) 신규 작성, PlayerController에 speedModifier 통합, 베이스 클래스 접근자 수정.

Purpose: Phase 4의 장판 시스템과 광폭화 쿨다운 배율 적용을 위한 기반을 마련한다. Zone 스크립트가 코드베이스에 존재하지 않으므로 신규 작성이 필요하고, CombatState._decisionTimer와 BossStatsSystem.Update()의 접근 제한자를 수정해야 Plan 02에서 광폭화 로직을 구현할 수 있다.
Output: SpeedUpZone.cs, SlowDownZone.cs 신규 파일 + PlayerController/CombatState/BossStatesSystem 수정
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-enrage-zone-system/04-CONTEXT.md
@.planning/phases/04-enrage-zone-system/04-RESEARCH.md

<interfaces>
<!-- PlayerController speed fields (line 14-15, 25, 147-149) -->
From Assets/Player/Script/PlayerController.cs:
```csharp
public float defaultSpeed = 3f;
public float runSpeed = 7f;
private float maxSpeed;
// in HandleGroundMovement():
maxSpeed = isRunning ? runSpeed : defaultSpeed;
rigid.linearVelocity = new Vector2(moveInput.x * maxSpeed, rigid.linearVelocity.y);
```

From Assets/Enemy/NewBoss/Script/States/CombatState.cs:
```csharp
private float _decisionTimer;  // line 5 — needs to become protected
```

From Assets/Enemy/NewBoss/Script/BossStatesSystem.cs:
```csharp
void Update()  // line 32 — needs to become protected virtual void Update()
```

From Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs (layer filter pattern):
```csharp
if (other.gameObject.layer != LayerMask.NameToLayer("Player")) return;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zone 스크립트 신규 작성 + PlayerController speedModifier 추가</name>
  <files>
    Assets/Enemy/WaterMonster/Script/Phase4/SpeedUpZone.cs
    Assets/Enemy/WaterMonster/Script/Phase4/SlowDownZone.cs
    Assets/Player/Script/PlayerController.cs
  </files>
  <read_first>
    Assets/Player/Script/PlayerController.cs
    Assets/Enemy/WaterMonster/Script/Phase3/PuddleExplosionController.cs
    Assets/Enemy/WaterMonster/Script/Phase2/PuddleSpawner.cs
  </read_first>
  <action>
1. **PlayerController.cs 수정** (Assets/Player/Script/PlayerController.cs):
   - Line 15 (`public float runSpeed = 7f;`) 아래에 새 필드 추가:
     ```csharp
     [HideInInspector] public float speedModifier = 1f;
     ```
   - `HandleGroundMovement()` 메서드(line 145-149)에서 `maxSpeed` 계산을 수정:
     ```csharp
     maxSpeed = (isRunning ? runSpeed : defaultSpeed) * speedModifier;
     ```
     기존: `maxSpeed = isRunning ? runSpeed : defaultSpeed;`

2. **SpeedUpZone.cs 신규 생성** (Assets/Enemy/WaterMonster/Script/Phase4/SpeedUpZone.cs):
   ```csharp
   using UnityEngine;

   namespace WaterMonster.Phase4
   {
       public class SpeedUpZone : MonoBehaviour
       {
           [SerializeField] private float speedMultiplier = 1.5f;

           private void OnTriggerEnter2D(Collider2D other)
           {
               // REQ-WM-X-01: Player 레이어에만 적용
               if (other.gameObject.layer != LayerMask.NameToLayer("Player")) return;
               var pc = other.GetComponentInParent<PlayerController>();
               if (pc != null) pc.speedModifier = speedMultiplier;
           }

           private void OnTriggerExit2D(Collider2D other)
           {
               if (other.gameObject.layer != LayerMask.NameToLayer("Player")) return;
               var pc = other.GetComponentInParent<PlayerController>();
               if (pc != null) pc.speedModifier = 1f;
           }
       }
   }
   ```

3. **SlowDownZone.cs 신규 생성** (Assets/Enemy/WaterMonster/Script/Phase4/SlowDownZone.cs):
   ```csharp
   using UnityEngine;

   namespace WaterMonster.Phase4
   {
       public class SlowDownZone : MonoBehaviour
       {
           [SerializeField] private float speedMultiplier = 0.5f;

           private void OnTriggerEnter2D(Collider2D other)
           {
               // REQ-WM-X-01: Player 레이어에만 적용
               if (other.gameObject.layer != LayerMask.NameToLayer("Player")) return;
               var pc = other.GetComponentInParent<PlayerController>();
               if (pc != null) pc.speedModifier = speedMultiplier;
           }

           private void OnTriggerExit2D(Collider2D other)
           {
               if (other.gameObject.layer != LayerMask.NameToLayer("Player")) return;
               var pc = other.GetComponentInParent<PlayerController>();
               if (pc != null) pc.speedModifier = 1f;
           }
       }
   }
   ```

**중요:** Zone 프리팹에는 Collider2D (IsTrigger = true) 가 필요하다. 프리팹 셋업은 Plan 02 또는 에디터 작업에서 처리.
  </action>
  <verify>
    <automated>grep -n "speedModifier" "Assets/Player/Script/PlayerController.cs" && grep -rn "class SpeedUpZone" "Assets/Enemy/WaterMonster/Script/Phase4/" && grep -rn "class SlowDownZone" "Assets/Enemy/WaterMonster/Script/Phase4/" && grep -n "LayerMask.NameToLayer" "Assets/Enemy/WaterMonster/Script/Phase4/SpeedUpZone.cs"</automated>
  </verify>
  <acceptance_criteria>
    - PlayerController.cs contains `public float speedModifier = 1f`
    - PlayerController.cs HandleGroundMovement contains `* speedModifier`
    - SpeedUpZone.cs contains `class SpeedUpZone : MonoBehaviour`
    - SpeedUpZone.cs contains `LayerMask.NameToLayer("Player")`
    - SpeedUpZone.cs contains `pc.speedModifier = speedMultiplier`
    - SlowDownZone.cs contains `class SlowDownZone : MonoBehaviour`
    - SlowDownZone.cs contains `LayerMask.NameToLayer("Player")`
    - SlowDownZone.cs contains `pc.speedModifier = speedMultiplier`
    - Both zone files use `OnTriggerEnter2D` and `OnTriggerExit2D`
    - Both zone files restore `pc.speedModifier = 1f` on exit
  </acceptance_criteria>
  <done>SpeedUpZone/SlowDownZone 스크립트가 Player 레이어만 필터링하며 speedModifier로 이동 속도를 수정/복원한다. PlayerController에 speedModifier 필드가 추가되어 maxSpeed 계산에 반영된다.</done>
</task>

<task type="auto">
  <name>Task 2: CombatState._decisionTimer protected 승격 + BossStatsSystem.Update() virtual 승격</name>
  <files>
    Assets/Enemy/NewBoss/Script/States/CombatState.cs
    Assets/Enemy/NewBoss/Script/BossStatesSystem.cs
  </files>
  <read_first>
    Assets/Enemy/NewBoss/Script/States/CombatState.cs
    Assets/Enemy/NewBoss/Script/BossStatesSystem.cs
  </read_first>
  <action>
1. **CombatState.cs 수정** (Assets/Enemy/NewBoss/Script/States/CombatState.cs):
   - Line 5: `private float _decisionTimer;` 를 `protected float _decisionTimer;` 로 변경.
   - 한 줄 변경만. 다른 코드 수정 없음.

2. **BossStatesSystem.cs 수정** (Assets/Enemy/NewBoss/Script/BossStatesSystem.cs):
   - Line 32: `void Update()` 를 `protected virtual void Update()` 로 변경.
   - 한 줄 변경만. 메서드 본문 수정 없음.

**이유:** Plan 02에서 WaterMonsterCombatState가 `_decisionTimer`에 접근해 광폭화 쿨다운 배율을 적용해야 하고, WaterMonsterStats가 `Update()`를 오버라이드해 광폭화 tick 소모를 추가해야 한다. Phase 1에서 `TakeDamage`를 virtual로 변경한 선례와 동일한 최소 침습 수정.
  </action>
  <verify>
    <automated>grep -n "protected float _decisionTimer" "Assets/Enemy/NewBoss/Script/States/CombatState.cs" && grep -n "protected virtual void Update" "Assets/Enemy/NewBoss/Script/BossStatesSystem.cs"</automated>
  </verify>
  <acceptance_criteria>
    - CombatState.cs line 5 contains `protected float _decisionTimer;` (not `private`)
    - BossStatesSystem.cs contains `protected virtual void Update()` (not plain `void Update()`)
    - No other lines in either file are modified
  </acceptance_criteria>
  <done>CombatState._decisionTimer가 protected로 승격되어 서브클래스에서 접근 가능하고, BossStatsSystem.Update()가 virtual로 승격되어 WaterMonsterStats에서 override 가능하다.</done>
</task>

</tasks>

<verification>
- `grep -rn "speedModifier" Assets/Player/Script/PlayerController.cs` — 필드 존재 + maxSpeed 계산에 사용
- `grep -rn "class SpeedUpZone" Assets/Enemy/WaterMonster/Script/Phase4/` — 파일 존재
- `grep -rn "class SlowDownZone" Assets/Enemy/WaterMonster/Script/Phase4/` — 파일 존재
- `grep -n "protected float _decisionTimer" Assets/Enemy/NewBoss/Script/States/CombatState.cs` — 접근자 변경 확인
- `grep -n "protected virtual void Update" Assets/Enemy/NewBoss/Script/BossStatesSystem.cs` — 접근자 변경 확인
</verification>

<success_criteria>
- SpeedUpZone.cs, SlowDownZone.cs 파일이 Assets/Enemy/WaterMonster/Script/Phase4/ 에 존재
- 두 Zone 스크립트 모두 Player 레이어 필터링 (LayerMask.NameToLayer("Player"))
- PlayerController.speedModifier 필드가 maxSpeed 계산에 곱해짐
- CombatState._decisionTimer가 protected
- BossStatsSystem.Update()가 protected virtual
</success_criteria>

<output>
After completion, create `.planning/phases/04-enrage-zone-system/04-01-SUMMARY.md`
</output>
