---
phase: 06-spirit-boss-stage2-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/WaterSpirit/Script/SpiritStats.cs
  - Assets/Enemy/WaterSpirit/Script/SpiritController.cs
autonomous: true
requirements: [CORE-03, S2-05]
must_haves:
  truths:
    - "보스 HP가 MaxHealth*0.5f 이하로 최초 도달 시 SpiritController.OnStage2Trigger()가 정확히 1회 호출된다"
    - "_stage2Triggered=true 이후에는 동일 조건에서 OnStage2Trigger()가 재호출되지 않는다"
    - "IsDummy=true 인 SpiritStats는 TakeDamage 호출 시 _currentHealth 가 변하지 않는다 (데미지 0)"
    - "IsDummy=false 인 진짜 보스는 TakeDamage 호출 시 정상적으로 _currentHealth 가 차감된다"
    - "SpiritController.Update() 인터셉트가 _isStage2=true 이고 CurrentState 가 plain CombatState 이면 Stage2CombatState 로 교체한다"
  artifacts:
    - path: "Assets/Enemy/WaterSpirit/Script/SpiritStats.cs"
      provides: "HP 50% 트리거 + IsDummy 데미지 0 분기"
      contains: "_currentHealth <= MaxHealth * 0.5f"
    - path: "Assets/Enemy/WaterSpirit/Script/SpiritController.cs"
      provides: "DummyPrefab 필드 + Stealth/Teleport 파라미터 + OnStage2Trigger 콜백 + Stage2 상태 인터셉트"
      contains: "public GameObject DummyPrefab"
  key_links:
    - from: "SpiritStats.TakeDamage"
      to: "SpiritController.OnStage2Trigger"
      via: "GetComponent<SpiritController>().OnStage2Trigger()"
      pattern: "OnStage2Trigger\\("
    - from: "SpiritController.Update"
      to: "Stage2CombatState"
      via: "ChangeState(new Stage2CombatState())"
      pattern: "new Stage2CombatState\\(\\)"
---

<objective>
정령 보스의 스테이지 2 전환 인프라를 구축한다. SpiritStats에 HP 50% 임계 체크와 IsDummy 데미지 0 분기를 추가하고, SpiritController에 Stage2 진입에 필요한 모든 [SerializeField] 필드와 콜백/플래그/Update 인터셉트 확장을 구현한다.

Purpose: Plan 02의 Stage2CombatState/SpiritStealth가 의존할 모든 contract(필드/메서드/플래그)를 먼저 확정한다. 이 계획은 "스테이지 2 전환 발동" 그 자체와 "분신/진짜 데미지 분기"라는 데이터 분기 contract를 책임진다.

Output:
- SpiritStats.cs (수정) — HP 50% 가드 + IsDummy 가드
- SpiritController.cs (수정) — DummyPrefab + Stealth/Teleport SerializeField + _isStage2 플래그 + OnStage2Trigger() + Update 인터셉트 확장
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-spirit-boss-stage2-system/06-CONTEXT.md
@Assets/Enemy/WaterSpirit/Script/SpiritController.cs
@Assets/Enemy/WaterSpirit/Script/SpiritStats.cs
@Assets/Enemy/NewBoss/Script/BossStatesSystem.cs

<interfaces>
<!-- Existing contracts that must be preserved or extended -->

From Assets/Enemy/NewBoss/Script/BossStatesSystem.cs:
```csharp
public class BossStatsSystem : MonoBehaviour
{
    public float MaxHealth = 1000f;
    protected float _currentHealth;
    public float CurrentHealth => _currentHealth;
    protected void InvokeOnDamageTaken();
    public void TakeDamageInfo(DamageInfo info);
    protected virtual void TakeDamage(DamageInfo info);
    protected virtual void Die();
}
```

From Assets/Enemy/WaterSpirit/Script/SpiritStats.cs (existing — to be modified):
```csharp
public class SpiritStats : BossStatsSystem
{
    public bool IsDummy { get; set; } = false;
    protected virtual void Reset() { MaxWater = 0f; }
    protected override void TakeDamage(DamageInfo info)
    {
        if (info.amount <= 0f) return;
        _currentHealth -= info.amount;
        InvokeOnDamageTaken();
        if (_currentHealth <= 0f) { _currentHealth = 0f; Die(); }
    }
    protected override void Die() { gameObject.SetActive(false); }
}
```

From Assets/Enemy/WaterSpirit/Script/SpiritController.cs (existing — to be modified):
```csharp
public class SpiritController : BossController
{
    public float RepelRange, ChargeRange, ChargeWindup, ChargeSpeed, OvershotDistance, ChargeDamage;
    public float RepelDamage, RepelForce, ProjectileDamage;
    public GameObject ProjectilePrefab;
    public bool IsCharging { get; private set; }
    public bool IsDummy => (Stats as SpiritStats)?.IsDummy ?? false;
    public void SetCharging(bool value);
    public void SetVelocity(Vector2 vel);
    protected override void Update()
    {
        base.Update();
        if (CurrentState != null && CurrentState.GetType() == typeof(CombatState))
            ChangeState(new SpiritCombatState());
    }
}
```

From CONTEXT.md:
- D-01b: HP 50% 1회 가드 — `_stage2Triggered` bool (SpiritController에 위치 결정)
- D-01c: 50% 도달 시 `SpiritController.OnStage2Trigger()` 콜백 호출
- D-04b/D-05b: `[SerializeField] StealthDuration`, `MinTeleportRadius`, `MaxTeleportRadius` Inspector 노출
- D-06a: `[SerializeField] DummyPrefab` Inspector 노출
- D-07a: `IsDummy == true` 면 데미지 0 처리
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: SpiritStats에 HP 50% 트리거와 IsDummy 데미지 가드 추가</name>
  <files>Assets/Enemy/WaterSpirit/Script/SpiritStats.cs</files>
  <read_first>
    - Assets/Enemy/WaterSpirit/Script/SpiritStats.cs (현재 TakeDamage 구조 확인)
    - Assets/Enemy/NewBoss/Script/BossStatesSystem.cs (MaxHealth, _currentHealth, InvokeOnDamageTaken 시그니처 확인)
    - .planning/phases/06-spirit-boss-stage2-system/06-CONTEXT.md (D-01, D-07)
  </read_first>
  <behavior>
    - Test 1 (IsDummy 가드): IsDummy=true 인 SpiritStats에 TakeDamage(info{amount=50}) 호출 시 _currentHealth 가 변하지 않아야 한다. Debug.Log("[SpiritStats] 분신 피격 무시") 형태의 로그가 출력되어야 한다.
    - Test 2 (정상 데미지): IsDummy=false 인 진짜 보스에 TakeDamage(info{amount=50}) 호출 시 _currentHealth 가 50 차감된다.
    - Test 3 (HP 50% 1회 트리거): MaxHealth=1000, _currentHealth=600 상태에서 TakeDamage(info{amount=150}) 호출 시 _currentHealth=450, _currentHealth <= MaxHealth*0.5f 조건 만족 → SpiritController.OnStage2Trigger() 가 정확히 1회 호출된다.
    - Test 4 (1회 가드): 위 상태 이후 추가 TakeDamage(info{amount=50}) 호출에서는 OnStage2Trigger() 가 재호출되지 않는다.
    - Test 5 (분신은 트리거 안 함): IsDummy=true 인 경우 어떤 데미지를 받아도 OnStage2Trigger() 가 호출되지 않는다 (데미지 0이므로 HP 변화 자체가 없음).
  </behavior>
  <action>
SpiritStats.cs 의 `TakeDamage(DamageInfo info)` 를 다음 정확한 코드로 교체한다:

```csharp
protected override void TakeDamage(DamageInfo info)
{
    // D-01b: Guard clause
    if (info.amount <= 0f) return;

    // D-07a: 분신 (IsDummy == true) 은 데미지를 받지 않음 (S2-05)
    if (IsDummy)
    {
        Debug.Log($"[SpiritStats] 분신 피격 무시 (IsDummy=true). 데미지: {info.amount}");
        return;
    }

    // Direct health reduction, bypassing barrier logic entirely
    _currentHealth -= info.amount;
    Debug.Log($"[SpiritStats] 피격! 데미지: {info.amount}, 남은 체력: {_currentHealth}/{MaxHealth}");

    // Fire event for counter-attacks/behavior changes
    InvokeOnDamageTaken();

    // CORE-03 (D-01a, D-01b, D-01c): HP 50% 이하 최초 도달 시 Stage 2 전환 (1회 가드)
    if (!_stage2Triggered && _currentHealth > 0f && _currentHealth <= MaxHealth * 0.5f)
    {
        _stage2Triggered = true;
        Debug.Log($"[SpiritStats] HP 50% 임계 도달 → Stage 2 전환 트리거. ({_currentHealth}/{MaxHealth})");

        var spiritController = GetComponent<SpiritController>();
        if (spiritController != null)
        {
            spiritController.OnStage2Trigger();
        }
        else
        {
            Debug.LogWarning("[SpiritStats] SpiritController 컴포넌트를 찾을 수 없어 Stage 2 트리거 실패.");
        }
    }

    if (_currentHealth <= 0f)
    {
        _currentHealth = 0f;
        Die();
    }
}
```

추가로 클래스 내부 필드로 `private bool _stage2Triggered = false;` 를 IsDummy 프로퍼티 바로 아래에 선언한다.

주의:
- `Reset()` 와 `Die()` 메서드는 변경하지 않는다.
- `Debug.Log` 메시지의 한국어 텍스트와 포맷을 정확히 위와 같이 유지한다 (테스트 가능성).
- D-01b 의 1회 가드는 _stage2Triggered=true 설정으로 보장된다 — 추가 조건 없이 단일 bool 만 사용한다.
- HP가 50% 이하지만 _currentHealth <= 0 인 경우 Die() 처리만 우선되어야 한다 (조건 분리 순서 유지: 트리거 → 사망 체크 순서).
  </action>
  <verify>
    <automated>grep -n "if (IsDummy)" "Assets/Enemy/WaterSpirit/Script/SpiritStats.cs" && grep -n "_stage2Triggered" "Assets/Enemy/WaterSpirit/Script/SpiritStats.cs" && grep -n "OnStage2Trigger()" "Assets/Enemy/WaterSpirit/Script/SpiritStats.cs" && grep -n "MaxHealth \* 0.5f" "Assets/Enemy/WaterSpirit/Script/SpiritStats.cs"</automated>
  </verify>
  <acceptance_criteria>
    - SpiritStats.cs 에 정확한 문자열 `if (IsDummy)` 가 TakeDamage 메서드 내부에 존재한다 (1회 grep 매치)
    - SpiritStats.cs 에 정확한 문자열 `private bool _stage2Triggered = false;` 가 클래스 필드로 존재한다
    - SpiritStats.cs 에 정확한 문자열 `_currentHealth <= MaxHealth * 0.5f` 가 존재한다
    - SpiritStats.cs 에 정확한 문자열 `spiritController.OnStage2Trigger()` 가 존재한다
    - SpiritStats.cs 에 정확한 문자열 `Debug.Log($"[SpiritStats] 분신 피격 무시 (IsDummy=true). 데미지: {info.amount}");` 가 존재한다
    - SpiritStats.cs 에 정확한 문자열 `_stage2Triggered = true;` 가 존재한다 (1회 가드 설정)
    - SpiritStats.cs 의 `Reset()` 메서드 본문은 `MaxWater = 0f;` 만 포함 (기존 그대로)
    - SpiritStats.cs 의 `Die()` 메서드 본문은 `gameObject.SetActive(false);` 만 포함 (기존 그대로)
  </acceptance_criteria>
  <done>
    SpiritStats.TakeDamage 가 IsDummy 분기 + HP 50% 1회 트리거 + InvokeOnDamageTaken + Die 순서를 모두 수행한다. CORE-03 (HP 50% 1회 전환) 및 S2-05 (진짜만 데미지 정상 적용) requirement 의 데이터 분기 책임이 SpiritStats 레이어에서 완성된다.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SpiritController에 DummyPrefab/Stealth 파라미터/OnStage2Trigger/Stage2 인터셉트 확장</name>
  <files>Assets/Enemy/WaterSpirit/Script/SpiritController.cs</files>
  <read_first>
    - Assets/Enemy/WaterSpirit/Script/SpiritController.cs (현재 필드/Update 인터셉트 구조 확인)
    - Assets/Enemy/NewBoss/Script/BossController.cs (ChangeState, CurrentState, Target 시그니처 확인)
    - Assets/Enemy/WaterMonster/Script/WaterMonsterController.cs (Update 인터셉트 패턴 참고 — 이미 구현된 패턴 모방)
    - .planning/phases/06-spirit-boss-stage2-system/06-CONTEXT.md (D-02c, D-04b, D-05b, D-06a)
  </read_first>
  <behavior>
    - Test 1 (DummyPrefab 노출): SpiritController 인스펙터에 DummyPrefab 필드가 [SerializeField] public 으로 노출된다.
    - Test 2 (Stealth/Teleport 파라미터): StealthDuration, MinTeleportRadius, MaxTeleportRadius 가 [SerializeField] public float 로 노출되며 기본값을 가진다.
    - Test 3 (OnStage2Trigger 콜백): SpiritStats.TakeDamage 에서 호출 가능한 public 메서드 OnStage2Trigger() 가 존재하고, 호출 시 _isStage2=true 로 설정 후 ChangeState(new Stage2CombatState()) 를 실행한다.
    - Test 4 (Stage2 인터셉트): _isStage2=true 이고 CurrentState 가 plain CombatState (또는 SpiritCombatState 가 아닌 부모 CombatState) 인 경우 Update() 에서 Stage2CombatState 로 교체한다. _isStage2=false 인 경우는 기존대로 SpiritCombatState 로 교체한다.
    - Test 5 (그로기 → Stage2 복귀): _isStage2=true 상태에서 GroggyState.Execute() 가 ChangeState(new CombatState()) 를 호출하여 plain CombatState 가 된 다음 프레임의 Update() 에서 Stage2CombatState 로 교체된다.
  </behavior>
  <action>
SpiritController.cs 를 다음 변경사항으로 정확히 수정한다:

(A) 클래스 상단 [Header("Projectile (S1-02)")] 블록 직후에 다음 [SerializeField] 블록을 추가한다:

```csharp
[Header("Stage 2 Settings (Phase 6)")]
[SerializeField] public GameObject DummyPrefab;             // D-06a: 분신 프리팹
[SerializeField] [Range(0.1f, 3f)] public float StealthDuration = 0.5f;  // D-04b
[SerializeField] [Range(1f, 10f)] public float MinTeleportRadius = 3f;   // D-05b
[SerializeField] [Range(1f, 15f)] public float MaxTeleportRadius = 6f;   // D-05b
```

(B) `IsCharging` 프로퍼티 아래, `IsDummy` 프로퍼티 위에 다음 필드와 프로퍼티를 추가한다:

```csharp
// CORE-03: Stage 2 활성화 플래그 (한번 true 가 되면 보스 사망까지 유지)
public bool IsStage2 { get; private set; } = false;
```

(C) `SetVelocity` 메서드 바로 아래에 다음 콜백 메서드를 추가한다:

```csharp
// D-01c, D-02b: SpiritStats.TakeDamage 에서 HP 50% 도달 시 호출되는 콜백
public void OnStage2Trigger()
{
    if (IsStage2) return;  // 추가 안전장치 (SpiritStats 의 _stage2Triggered 와 이중 가드)
    IsStage2 = true;
    Debug.Log("[SpiritController] Stage 2 진입 트리거 발동 → Stage2CombatState 전환");
    ChangeState(new Stage2CombatState());
}
```

(D) `Update()` 의 인터셉트 분기를 다음으로 정확히 교체한다:

```csharp
protected override void Update()
{
    base.Update();

    // D-02c: 상태 인터셉트 — Stage 2 활성 여부에 따라 Stage2CombatState / SpiritCombatState 분기
    // 분신은 자체 Stage 2 사이클 없이 SpiritCombatState 만 실행 (S2-04: 동일 패턴, 데미지 0)
    if (CurrentState != null && CurrentState.GetType() == typeof(CombatState))
    {
        if (IsStage2 && !IsDummy)
        {
            ChangeState(new Stage2CombatState());
        }
        else
        {
            ChangeState(new SpiritCombatState());
        }
    }
}
```

주의:
- 기존 `OnTriggerEnter2D` 메서드는 변경하지 않는다 (SpiritCharge 의 IsCharging 데미지 판정 유지).
- `RepelRange, ChargeRange, ChargeWindup, ChargeSpeed, OvershotDistance, ChargeDamage, RepelDamage, RepelForce, ProjectilePrefab, ProjectileDamage` 등 기존 [SerializeField] 필드는 그대로 보존한다.
- IsDummy 프로퍼티 정의도 그대로 보존한다 (`public bool IsDummy => (Stats as SpiritStats)?.IsDummy ?? false;`).
- Stage2CombatState 클래스는 Plan 02 에서 작성되므로, 이 시점에서는 컴파일 에러를 막기 위해 Plan 02 가 동일 커밋군에 포함되어야 한다. 그러나 이 PLAN(Plan 01)을 단독 커밋하면 컴파일 에러가 발생하므로, Plan 01 마지막에 `Stage2CombatState.cs` 의 빈 스텁 파일을 추가로 생성한다 (Task 3 으로 분리하지 않고 본 Task의 마지막 단계에서 처리).

(E) `Stage2CombatState` 컴파일 스텁 파일 생성 — Plan 02 에서 본격 구현되기 전 컴파일 가능성 확보를 위함:

새 파일 `Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs` 를 다음 정확한 내용으로 생성한다:

```csharp
using UnityEngine;

// Phase 6 Plan 01 — 컴파일 스텁. Plan 02 에서 본격 구현됨.
// SpiritController.OnStage2Trigger() 가 ChangeState(new Stage2CombatState()) 를 호출하므로
// SpiritCombatState 를 상속한 빈 클래스가 먼저 존재해야 한다.
public class Stage2CombatState : SpiritCombatState
{
    public override void Enter(BossController boss)
    {
        base.Enter(boss);
        Debug.Log("[Stage2CombatState] STUB Enter (Plan 02에서 본격 구현 예정)");
    }
}
```

주의: `<files>` 필드는 SpiritController.cs 단일이지만, 본 Task 는 Stage2CombatState.cs 스텁 파일도 추가 생성한다. 이는 컴파일 무결성 확보를 위한 최소 변경이며, Plan 02 에서 본격 확장된다.
  </action>
  <verify>
    <automated>grep -n "DummyPrefab" "Assets/Enemy/WaterSpirit/Script/SpiritController.cs" && grep -n "MinTeleportRadius" "Assets/Enemy/WaterSpirit/Script/SpiritController.cs" && grep -n "OnStage2Trigger" "Assets/Enemy/WaterSpirit/Script/SpiritController.cs" && grep -n "IsStage2" "Assets/Enemy/WaterSpirit/Script/SpiritController.cs" && test -f "Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs" && grep -n "class Stage2CombatState : SpiritCombatState" "Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs"</automated>
  </verify>
  <acceptance_criteria>
    - SpiritController.cs 에 정확한 문자열 `[SerializeField] public GameObject DummyPrefab;` 이 존재한다
    - SpiritController.cs 에 정확한 문자열 `public float StealthDuration = 0.5f;` 이 존재한다 (값 정확히 0.5f)
    - SpiritController.cs 에 정확한 문자열 `public float MinTeleportRadius = 3f;` 이 존재한다
    - SpiritController.cs 에 정확한 문자열 `public float MaxTeleportRadius = 6f;` 이 존재한다
    - SpiritController.cs 에 정확한 문자열 `public bool IsStage2 { get; private set; } = false;` 이 존재한다
    - SpiritController.cs 에 정확한 문자열 `public void OnStage2Trigger()` 메서드 시그니처가 존재한다
    - SpiritController.cs 에 정확한 문자열 `ChangeState(new Stage2CombatState());` 가 OnStage2Trigger 메서드와 Update 인터셉트 두 곳에 모두 존재한다 (총 2회 매치)
    - SpiritController.cs 에 정확한 문자열 `if (IsStage2 && !IsDummy)` 가 Update 인터셉트 내부에 존재한다 — Stage2 인터셉트 조건
    - SpiritController.cs 의 기존 `IsDummy` 프로퍼티 (`public bool IsDummy => (Stats as SpiritStats)?.IsDummy ?? false;`) 는 변경 없이 유지된다
    - SpiritController.cs 의 기존 `OnTriggerEnter2D` 메서드는 변경 없이 유지된다 (정확한 문자열 `if (!IsCharging || _hasHitPlayerThisCharge) return;` 존재)
    - 새 파일 `Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs` 가 존재한다
    - Stage2CombatState.cs 에 정확한 문자열 `public class Stage2CombatState : SpiritCombatState` 가 존재한다
  </acceptance_criteria>
  <done>
    SpiritController 가 Stage 2 진입에 필요한 모든 [SerializeField] 입력(DummyPrefab/StealthDuration/Min,MaxTeleportRadius)과 콜백(OnStage2Trigger)과 상태 인터셉트(_isStage2 분기)를 보유한다. Stage2CombatState 스텁이 존재하여 Plan 01 단독 커밋 시점에도 Unity 프로젝트가 컴파일된다. CORE-03 의 트리거 → 상태 전환 contract 가 완성된다.
  </done>
</task>

</tasks>

<verification>
- 두 파일 모두 grep 기반 acceptance_criteria 통과
- Unity 에디터에서 SpiritController Inspector 에 새 필드 4종 (DummyPrefab/StealthDuration/Min/MaxTeleportRadius) 가시적
- Unity 컴파일 에러 0건 (Stage2CombatState 스텁이 존재하므로)
- SpiritStats.TakeDamage 가 IsDummy 분기 + HP 50% 1회 트리거 + 정상 데미지 처리 모두 단일 메서드 내에서 흐름 유지
- 분신 (IsDummy=true) 에는 HP 트리거가 발동하지 않는다 (IsDummy 가드가 _stage2Triggered 검사 이전에 return)
</verification>

<success_criteria>
- 모든 acceptance_criteria 통과
- Phase 6 의 모든 후속 plan(02) 이 의존하는 contract (DummyPrefab 필드, OnStage2Trigger 콜백, IsStage2 플래그, IsDummy 데미지 가드) 가 확정됨
- Requirement CORE-03 (HP 50% 1회 전환 트리거) 의 데이터 분기와 콜백 발화 책임 완료
- Requirement S2-05 (분신 데미지 0) 의 데이터 분기 책임 완료
</success_criteria>

<output>
실행 완료 후 `.planning/phases/06-spirit-boss-stage2-system/06-01-SUMMARY.md` 를 생성한다 (템플릿: `.claude/get-shit-done/templates/summary.md`).
</output>
