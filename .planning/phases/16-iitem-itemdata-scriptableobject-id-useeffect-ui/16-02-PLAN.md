---
phase: 16-iitem-itemdata-scriptableobject-id-useeffect-ui
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - Assets/Item/Check.md
  - Assets/Item/Check.md.meta
  - Assets/Item/Script/ItemData.cs.meta
  - Assets/Item/Script/IItem.cs.meta
  - Assets/Item/HealthPotion.asset
  - Assets/Item/HealthPotion.asset.meta
  - Assets/Item/AncientKey.asset
  - Assets/Item/AncientKey.asset.meta
autonomous: false
requirements: [D-02, D-03, D-07]
must_haves:
  truths:
    - "Unity 6000.3.10f1 이 Assets/Item/ 을 에러 없이 임포트한다 — ItemData.cs 가 컴파일되고 .asset 2개가 missing script 없이 로드된다"
    - "HealthPotion 에셋의 UseEffect 를 호출하면 플레이어 체력이 실제로 20 회복된다 (D-02/D-03 end-to-end 증명)"
    - "AncientKey 에셋의 UseEffect 를 호출하면 아무 일도 일어나지 않고 예외도 없다"
    - "Assets/Item/Check.md 만 읽으면 Phase 17 개발자가 이 스키마를 어떻게 검증하는지 알 수 있다"
  artifacts:
    - path: "Assets/Item/Check.md"
      provides: "Phase 16 정적 회귀 결과표 + Unity 에디터/Play 모드 검증 체크리스트 + 결과 기록 섹션"
      contains: "Phase16: Use On Player"
      min_lines: 60
    - path: "Assets/Item/Check.md.meta"
      provides: "Check.md 의 TextScriptImporter meta (Assets/SaveSystem/Check.md.meta 와 동일 형식)"
      contains: "TextScriptImporter:"
  key_links:
    - from: "Assets/Item/Check.md"
      to: "ItemData.UseOnPlayerFromInspector() ContextMenu 훅"
      via: "Play 모드 체크리스트가 이 훅을 유일한 UseEffect 호출 경로로 지정"
      pattern: "Phase16: Use On Player"
    - from: "Unity 배치모드 임포트 로그"
      to: "Assets/Item/*.asset"
      via: "손수 작성한 YAML 이 실제 에디터 임포트를 통과하는지 확인"
      pattern: "error CS|missing|Could not"
---

<objective>
Plan 16-01 이 만든 `ItemData` 스키마와 손수 작성한 `.asset` YAML 2개가 **실제 Unity 에디터에서 동작하는지** 검증하고, 그 절차를 `Assets/Item/Check.md` 에 남긴다.

왜 별도 플랜인가: 16-01 의 `.asset` 파일은 Unity 에디터를 거치지 않고 손으로 쓴 YAML 이다. guid 한 글자만 틀려도 에디터에서 "missing script" 로 뜨는데, 이건 grep 으로는 절대 잡히지 않는다. 또 D-02/D-03 이 요구하는 "UseEffect 가 실제로 회복시킨다"는 정적 검사로 증명할 수 없다 — Play 모드 실측이 유일한 증거다.

Purpose: D-07(예시 에셋이 "실제로 동작함을 눈으로 확인")과 D-02/D-03(UseEffect 를 끝까지 구현)의 **증거**를 확보한다.
Output: `Assets/Item/Check.md` (+ meta) 신규 1개, Unity 배치모드 임포트 로그 결과, 사용자 Play 모드 실측 결과.
</objective>

<execution_context>
이 플랜은 마지막 태스크에 **사용자 체크포인트(Play 모드 실측)** 를 포함하므로 `autonomous: false` 다. Task 1~2 는 자율 실행 가능하고, Task 3 에서 반드시 멈춘다.
</execution_context>

<context>
@.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/16-CONTEXT.md
@.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/16-01-PLAN.md
@Assets/Item/Script/ItemData.cs
@Assets/Item/HealthPotion.asset
@Assets/Item/AncientKey.asset
@Assets/SaveSystem/Check.md
</context>

<interfaces>
<!-- 실행자가 필요로 하는 구체값 전부. 탐색 불필요. -->

**환경 (계획 단계에서 실측 확인함):**

| 항목 | 값 |
|---|---|
| 프로젝트 루트 | `C:/Users/MSI/Projeect_A.E/Projeect_A.E` |
| `ProjectSettings/ProjectVersion.txt` | `m_EditorVersion: 6000.3.10f1` |
| 설치된 Unity | `C:/Program Files/Unity/Hub/Editor/6000.3.10f1/Editor/Unity.exe` (존재 확인됨) |
| 기존 컴파일 산출물 | `Library/ScriptAssemblies/Assembly-CSharp.dll` (존재) |

**`dotnet build` 는 이 저장소에서 컴파일 게이트로 쓸 수 없다 (계획 단계 실측):**
`dotnet build Assembly-CSharp.csproj` 는 우리 코드와 무관한 **기존** 패키지 에러 2건
(`com.unity.ai.assistant` 의 `PreviewElementFactory.cs` / `SelectGeneratedAssetsFunctionCallElement.cs`,
`error CS0118: 'Image'은(는) 네임스페이스이지만 형식처럼 사용됩니다`) 때문에 항상 실패하고,
그 여파로 `Assembly-CSharp` 자체가 **스킵되어 `Temp/bin/Debug/Assembly-CSharp.dll` 이 생성되지 않는다**.
즉 "Assembly-CSharp 관련 에러 0건"은 거짓 양성(false pass)이다. 그래서 이 플랜은 `dotnet build` 대신
**Unity 배치모드 임포트**를 컴파일 게이트로 쓴다 — 스크립트 컴파일과 `.asset` YAML 임포트를 동시에 검증한다.

**16-01 이 고정한 guid (Check.md 에 기록할 값):**

| 파일 | guid |
|---|---|
| `Assets/Item/Script/ItemData.cs.meta` | `501b19c5008706d0a3f2bf69aacf52f6` |
| `Assets/Item/HealthPotion.asset.meta` | `1ca8a1cec6cae3595e071afccaf8623e` |
| `Assets/Item/AncientKey.asset.meta` | `65cc29b1f44b856171d66dbe8ddec240` |
| `Assets/Item/Check.md.meta` (이 플랜이 신규 할당) | `05511fb05942d6634e84eccc8a623d1f` |

**예시 에셋 값 (Check.md 체크리스트가 대조할 기대값):**

| 에셋 | id | type | effectType | amount |
|---|---|---|---|---|
| `HealthPotion.asset` | `health_potion_01` | `Consumable` (YAML 0) | `Heal` (YAML 0) | `20` |
| `AncientKey.asset` | `ancient_key_01` | `Progression` (YAML 1) | `Heal` (YAML 0, 미사용) | `0` |

**`ItemData` 의 에디터 전용 검증 훅 (16-01 Task 1 이 추가):**
```csharp
#if UNITY_EDITOR
    [ContextMenu("Phase16: Use On Player")]
    private void UseOnPlayerFromInspector()
    {
        UseEffect(FindAnyObjectByType<PlayerInteraction>());
    }
#endif
```
Play 모드에서 `.asset` 을 선택하고 Inspector 헤더의 점 3개(⋮) 또는 우클릭 → `Phase16: Use On Player`.
인벤토리도 월드 픽업도 없는 이 phase 에서 `UseEffect` 를 호출할 수 있는 **유일한** 경로다.

**`Assets/SaveSystem/Check.md.meta` 형식 (Check.md.meta 가 따라야 할 형식):**
```
fileFormatVersion: 2
guid: <32 hex>
TextScriptImporter:
  externalObjects: {}
  userData: 
  assetBundleName: 
  assetBundleVariant: 
```
</interfaces>

<encoding_note>
- `Assets/Item/Check.md` 는 이 프로젝트의 다른 Check.md 와 동일하게 **한국어 본문 + UTF-8(BOM 없음)** 로 쓴다. `.md` 는 컴파일 대상이 아니므로 CP949 사고 이력과 무관하다.
- `Assets/Item/Check.md.meta` 는 순수 ASCII.
- 16-01 이 만든 `.cs` / `.asset` 파일은 이 플랜에서 **내용을 수정하지 않는다**. 예외는 Task 1 의 Unity 배치모드 임포트가 자동으로 재직렬화(normalize)한 경우뿐이며, 그때는 Unity 가 쓴 결과를 그대로 커밋한다.
</encoding_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Unity 배치모드 임포트 게이트 — ItemData.cs 컴파일 + 손수 작성한 .asset YAML 임포트 실검증</name>
  <files>Assets/Item/Script/ItemData.cs.meta, Assets/Item/Script/IItem.cs.meta, Assets/Item/HealthPotion.asset, Assets/Item/HealthPotion.asset.meta, Assets/Item/AncientKey.asset, Assets/Item/AncientKey.asset.meta</files>

  <read_first>
    - `Assets/Item/Script/ItemData.cs` (16-01 산출물 — 컴파일 대상)
    - `Assets/Item/HealthPotion.asset`, `Assets/Item/AncientKey.asset` (16-01 산출물 — 임포트 대상, `m_Script` guid 확인)
    - `Assets/Item/Script/ItemData.cs.meta` (guid 가 `.asset` 의 `m_Script` 와 일치하는지 대조)
    - `ProjectSettings/ProjectVersion.txt` (에디터 버전이 `6000.3.10f1` 인지 확인 — 다른 버전으로 배치 실행하면 프로젝트 전체가 업그레이드되는 파괴적 변경이 일어난다)
  </read_first>

  <action>
**(0) 선행 조건 확인 — 건너뛰지 말 것.**
Unity 에디터가 이 프로젝트를 열고 있으면 배치 실행이 프로젝트 락 때문에 실패한다. 먼저 확인한다:
```bash
ls -la Temp/UnityLockfile 2>/dev/null && echo "LOCK PRESENT - Unity may be open"
```
락 파일이 있고 Unity 가 실제로 떠 있으면 **여기서 멈추고 사용자에게 "Unity 에디터를 닫아 주세요"라고 요청한다.** 강제로 진행하지 않는다.

**(1) 배치모드 임포트 실행.**
```bash
LOG="${TEMP:-/tmp}/unity-phase16-import.log"
rm -f "$LOG"
"/c/Program Files/Unity/Hub/Editor/6000.3.10f1/Editor/Unity.exe" \
  -batchmode -quit -nographics \
  -projectPath "C:/Users/MSI/Projeect_A.E/Projeect_A.E" \
  -logFile "$LOG"
echo "UNITY_EXIT=$?"
```
첫 임포트는 수 분이 걸릴 수 있다. 타임아웃을 10분(600000ms) 으로 잡는다.
**반드시 `-projectPath` 를 명시한다** — 생략하면 Unity 가 마지막으로 연 다른 프로젝트를 건드릴 수 있다.

**(2) 로그 판정.** 아래 3개를 모두 확인한다:
```bash
grep -nE "error CS[0-9]+" "$LOG" | grep -i "Assets" ; echo "---(위 출력이 비어야 함: 게임 코드 컴파일 에러 0건)"
grep -niE "Assets/Item" "$LOG" | grep -iE "error|missing|could not|failed" ; echo "---(위 출력이 비어야 함: Item 임포트 에러 0건)"
grep -niE "referenced script.*(missing|could not be loaded)" "$LOG" ; echo "---(위 출력이 비어야 함: m_Script guid 정상 해석)"
```
`Library/PackageCache/` 경로의 기존 패키지 경고/에러는 이 플랜의 책임이 아니다 — 위 판정은 `Assets/` 경로로 한정한다.

**(3) 임포트 결과 반영.** Unity 가 `Assets/Item/` 아래 파일을 재직렬화했을 수 있다:
```bash
git status --porcelain Assets/Item
```
- `Assets/Item/` 안의 변경만 나오면 → Unity 표준화 결과이므로 **그대로 커밋**한다 (특히 `IItem.cs.meta` 에 `MonoImporter` 블록이 채워질 수 있다. guid `1a97ea82c14ac9e449d2baeb51e3ba6b` 가 유지되는지 반드시 확인 — guid 가 바뀌었다면 되돌리고 사용자에게 보고한다).
- `Assets/Item/` **밖**의 변경(예: `ProjectSettings/`, 다른 `Assets/` 하위, `Packages/manifest.json`)은 이 플랜의 범위가 아니다. **절대 커밋하지 말고** 목록만 기록해 Task 2 의 Check.md `## 임포트 부작용` 절에 남긴다.
- `Library/`, `Temp/`, `Logs/` 는 gitignore 대상이므로 무시한다.

**(4) 결과 기록.** 다음 값을 Task 2 에서 쓸 수 있게 메모한다: Unity 종료 코드, 위 3개 grep 의 출력(있으면 원문), `Assets/Item/` 밖 변경 파일 목록, `Library/ScriptAssemblies/Assembly-CSharp.dll` 의 갱신 시각.

**(5) 커밋.** `Assets/Item/` 안에서 실제로 바뀐 파일만 pathspec 으로 명시해 커밋 1개. 바뀐 게 없으면 커밋하지 않는다 (그게 정상 — 우리가 쓴 YAML 이 Unity 출력과 일치했다는 뜻).

**락 때문에 배치 실행을 못 한 경우:** 이 태스크를 SKIPPED 로 기록하고 Task 2 의 Check.md 정적 회귀 표에 `배치모드 임포트: SKIPPED (Unity 에디터 열림)` 로 남긴 뒤 Task 3 체크포인트로 넘어간다. 사용자가 에디터에서 직접 하는 검증이 같은 증거를 제공한다.
  </action>

  <verify>
    <automated>grep -cE "error CS[0-9]+" "${TEMP:-/tmp}/unity-phase16-import.log" | head -1; grep -nE "error CS[0-9]+" "${TEMP:-/tmp}/unity-phase16-import.log" | grep -c "Assets/"</automated>
  </verify>

  <acceptance_criteria>
    - Unity 배치 프로세스가 종료 코드 `0` 으로 끝난다 (또는 락으로 인해 실행하지 못했음이 명시적으로 기록된다).
    - `grep -nE "error CS[0-9]+" "$LOG" | grep -c "Assets/"` 가 `0` 을 출력한다 (게임 코드 컴파일 에러 0건).
    - `grep -niE "Assets/Item" "$LOG" | grep -ciE "error|missing|could not|failed"` 가 `0` 을 출력한다.
    - `grep -ciE "referenced script.*(missing|could not be loaded)" "$LOG"` 가 `0` 을 출력한다 (`.asset` 의 `m_Script` guid 가 정상 해석됨 = 손수 쓴 YAML 통과).
    - `grep -c "guid: 1a97ea82c14ac9e449d2baeb51e3ba6b" Assets/Item/Script/IItem.cs.meta` 가 `1` 을 출력한다 (Unity 가 재직렬화해도 기존 guid 보존).
    - `grep -c "guid: 501b19c5008706d0a3f2bf69aacf52f6" Assets/Item/Script/ItemData.cs.meta` 가 `1` 을 출력한다.
    - `grep -c "m_Script: {fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3}" Assets/Item/HealthPotion.asset` 가 `1` 을 출력한다 (임포트 후에도 참조 유지).
    - `grep -c "^  id: health_potion_01$" Assets/Item/HealthPotion.asset` 가 `1`, `grep -c "^  amount: 20$" Assets/Item/HealthPotion.asset` 가 `1` 을 출력한다 (Unity 가 값을 날리지 않았다).
    - `git status --porcelain -- ':!Assets/Item' ':!.planning'` 출력에 이 태스크가 새로 만든 변경이 없다 (`Assets/Item/` 밖 0건 커밋).
    - 커밋이 있다면 `git show --name-only HEAD` 의 파일이 전부 `Assets/Item/` 아래다.
  </acceptance_criteria>

  <done>Unity 6000.3.10f1 이 `Assets/Item/` 을 컴파일 에러 0건 / 임포트 에러 0건 / missing script 0건으로 받아들였음이 로그로 증명되었다 (또는 에디터 락으로 실행 불가함이 기록되었다).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Assets/Item/Check.md 작성 — 정적 회귀 결과표 + Play 모드 체크리스트 10항목</name>
  <files>Assets/Item/Check.md, Assets/Item/Check.md.meta</files>

  <read_first>
    - `Assets/SaveSystem/Check.md` (서술 형식의 기준 — 검증 대상 변경사항 / 사전 준비 / 체크리스트 / 결과 기록 4개 절 구성을 그대로 따른다)
    - `Assets/SaveSystem/Check.md.meta` (`TextScriptImporter` meta 형식)
    - `Assets/Item/Script/ItemData.cs` (Inspector 에 노출되는 필드 이름/순서를 정확히 옮겨 적기 위해)
    - Task 1 이 기록한 Unity 배치모드 임포트 결과
  </read_first>

  <action>
**(1) `Assets/Item/Check.md` 를 신규 생성한다.** 한국어 본문, UTF-8(BOM 없음). 아래 5개 절을 모두 포함한다.

`# Item — Phase 16 아이템 코어(ItemData ScriptableObject) 검증 체크리스트`

`## 검증 대상 변경사항` — 다음 표를 그대로 싣는다:

| 파일 | 내용 |
|---|---|
| `Assets/Item/Script/IItem.cs` | Phase 16 이전 완료. `void UseEffect(PlayerInteraction player)`. **무수정** |
| `Assets/Item/Script/ItemData.cs` | 신규. 프로젝트 최초 ScriptableObject. `ItemData : ScriptableObject, IItem` + `ItemType`/`ConsumableEffectType` enum + `UseEffect` 본체 + 에디터 전용 ContextMenu 훅 |
| `Assets/Item/HealthPotion.asset` | 신규. 소모품 예시 — `health_potion_01` / Consumable / Heal / 20 |
| `Assets/Item/AncientKey.asset` | 신규. 진행아이템 예시 — `ancient_key_01` / Progression |

그리고 guid 표(`<interfaces>` 블록의 4행 표)를 그대로 싣는다. 이유도 한 줄 적는다: `.asset` 이 에디터를 거치지 않고 손으로 작성됐기 때문에 guid 대조가 이 phase 의 핵심 검증 포인트라는 것.

`## 정적 회귀 검사 결과` — 아래 8항목을 표로 싣고 각 행에 Task 1 시점의 실제 결과(PASS/FAIL/SKIPPED)를 채운다:

1. `ItemData : ScriptableObject, IItem` 존재 (D-01)
2. `[SerializeField]` 필드 정확히 4개, `displayName`/`icon`/`description` 0건 (D-06)
3. `ItemType` = `Consumable` / `Progression` 2값, 서브클래스 0건 (D-04)
4. `ConsumableEffectType` = `Heal` 1값 (D-03)
5. `UseEffect` 가 `player.GetComponent<PlayerStats>().Heal(amount)` 를 호출하고 널 가드가 0건 (D-02/D-03 + 프로젝트 관행)
6. `.asset` 2개의 `m_Script` guid 가 `ItemData.cs.meta` guid 와 일치 (D-07)
7. `Assets/Player/` 와 `Assets/Item/Script/IItem.cs` 0줄 변경
8. Unity 6000.3.10f1 배치모드 임포트: 컴파일 에러 0 / Item 임포트 에러 0 / missing script 0

`## 사전 준비` — 다음 4단계:
1. Unity 에디터로 프로젝트를 열고 컴파일 완료까지 대기. Console 에러 0건 확인.
2. Project 창에서 `Assets/Item` 폴더를 연다.
3. 플레이어가 배치된 씬(예: `Tutorial Map.unity`)을 연다.
4. `Assets/Item` 우클릭 → `Create` → `Items` → `Item Data` 메뉴가 존재하는지 확인한다(이 메뉴가 없으면 컴파일이 안 된 것).

`## Play 모드 체크리스트` — 미체크 박스 `[ ]` 로 아래 10항목:

- [ ] 1. Project 창 `Assets/Item` 에 `HealthPotion`, `AncientKey` 두 에셋이 보이고, 아이콘이 깨진 스크립트(물음표)가 아니다.
- [ ] 2. `HealthPotion` 선택 시 Inspector 에 필드가 정확히 **4개**(`Id`, `Type`, `Effect Type`, `Amount`)만 보인다. 이름/아이콘/설명 필드가 없다 (D-06).
- [ ] 3. `HealthPotion` 값이 `Id = health_potion_01`, `Type = Consumable`, `Effect Type = Heal`, `Amount = 20` 이다.
- [ ] 4. `AncientKey` 값이 `Id = ancient_key_01`, `Type = Progression`, `Amount = 0` 이다.
- [ ] 5. `Type` 드롭다운을 열면 `Consumable` / `Progression` 두 개뿐이고, `Effect Type` 드롭다운은 `Heal` 하나뿐이다 (D-03/D-04).
- [ ] 6. Play 를 시작한다. 플레이어가 적 공격이나 낙하로 피해를 입어 **현재 체력 < 최대 체력** 상태를 만든다. 체력 UI 값을 적어 둔다.
- [ ] 7. Play 중 `HealthPotion.asset` 을 선택하고 Inspector 헤더의 ⋮(또는 헤더 우클릭) → `Phase16: Use On Player` 를 클릭한다. → **체력이 정확히 20 회복**된다(최대 체력에 걸리면 최대치에서 멈춘다). Console 예외 0건 (D-02/D-03 end-to-end).
- [ ] 8. 같은 방식으로 `AncientKey.asset` 에서 `Phase16: Use On Player` 를 클릭한다. → **체력 변화 0**, Console 예외 0건 (D-02 진행아이템 no-op).
- [ ] 9. 6~8을 체력이 가득 찬 상태에서 한 번 더 반복한다. → `HealthPotion` 사용 시 체력이 최대치를 넘지 않는다(`PlayerStats.ClampHealth()` 동작 확인).
- [ ] 10. Play 를 종료한다. Console 에러 0건. `git status --porcelain Assets/Item` 이 비어 있다 (Play 모드가 `.asset` 값을 디스크에 바꿔 쓰지 않았다 — SO 는 Play 중 변경이 에디터에 남을 수 있으므로 반드시 확인).

`## 알려진 한계 / 범위 밖` — 다음을 명시한다:
- 인벤토리 자료구조·슬롯·스택은 Phase 17.
- 월드 아이템 픽업과 `PlayerInteraction` 배선은 Phase 17 — 그래서 이 phase 에서는 `Phase16: Use On Player` ContextMenu 훅이 `UseEffect` 의 유일한 호출 경로다.
- 세이브/로드 연동(`SaveData.Items`)은 Phase 18.
- 아이콘/표시 이름/설명 등 UI 메타데이터는 UI phase (D-06 의도적 배제).
- `id` 중복/공백 검증 도구 없음 — 수동 관리 (D-05).
- 진행아이템의 `Effect Type`/`Amount` 필드는 Inspector 에 계속 보이지만 무시된다. 조건부 숨김은 Phase 17 과제.

Task 1 에서 `Assets/Item/` 밖 변경이 관측됐다면 `## 임포트 부작용` 절을 추가해 파일 목록과 "이번 phase 에서 커밋하지 않음"을 기록한다.

`## 결과 기록` — 검증 일자 / 검증자 / PASS-FAIL 요약을 적을 빈 칸.

**(2) `Assets/Item/Check.md.meta` 를 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다:**

```
fileFormatVersion: 2
guid: 05511fb05942d6634e84eccc8a623d1f
TextScriptImporter:
  externalObjects: {}
  userData: 
  assetBundleName: 
  assetBundleVariant: 
```

**(3) 커밋**: `Assets/Item/Check.md` 와 `Assets/Item/Check.md.meta` 만 pathspec 으로 명시해 커밋 1개.
  </action>

  <verify>
    <automated>test -f Assets/Item/Check.md &amp;&amp; grep -c "Phase16: Use On Player" Assets/Item/Check.md &amp;&amp; grep -c "^- \[ \] 10\." Assets/Item/Check.md</automated>
  </verify>

  <acceptance_criteria>
    - `Assets/Item/Check.md` 가 존재하고 `wc -l` 이 `60` 이상이다.
    - `grep -c "^- \[ \] " Assets/Item/Check.md` 가 정확히 `10` 을 출력한다 (Play 모드 체크리스트 10항목, 전부 미체크).
    - `grep -c "Phase16: Use On Player" Assets/Item/Check.md` 가 `2` 이상이다 (7번/8번 항목).
    - `grep -c "health_potion_01" Assets/Item/Check.md` 와 `grep -c "ancient_key_01" Assets/Item/Check.md` 가 각각 `1` 이상이다.
    - `grep -c "501b19c5008706d0a3f2bf69aacf52f6" Assets/Item/Check.md` 가 `1` 이상이다 (guid 표 존재).
    - `grep -cE "^## " Assets/Item/Check.md` 가 `6` 이상이다 (검증 대상 / 정적 회귀 / 사전 준비 / Play 모드 / 알려진 한계 / 결과 기록).
    - `grep -cE "Phase 17|Phase 18" Assets/Item/Check.md` 가 `3` 이상이다 (범위 밖 항목이 어느 phase 소관인지 명시).
    - `grep -c "guid: 05511fb05942d6634e84eccc8a623d1f" Assets/Item/Check.md.meta` 가 `1`, `grep -c "TextScriptImporter:" Assets/Item/Check.md.meta` 가 `1` 을 출력한다.
    - `file Assets/Item/Check.md.meta` 출력이 `ASCII text` 이다.
    - `git status --porcelain Assets/Item/Script Assets/Item/*.asset` 가 빈 출력이다 (16-01 산출물 무변경).
    - 커밋 1개. pathspec 은 `Assets/Item/Check.md` 와 `Assets/Item/Check.md.meta` 뿐.
  </acceptance_criteria>

  <done>`Assets/Item/Check.md` 가 Phase 9~15 의 Check.md 관행대로 정적 회귀 결과표 + Play 모드 체크리스트 10항목 + 범위 밖 명시를 담고 있고, Phase 17 개발자가 이 문서만 읽고 스키마 검증을 재현할 수 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Play 모드 실측 체크포인트 — HealthPotion 이 실제로 20 회복시키는지 확인 (D-02/D-03/D-07)</name>
  <files>Assets/Item/Check.md (결과 기록 절 갱신)</files>

  <read_first>
    - `Assets/Item/Check.md` (Task 2 산출물 — 사용자에게 안내할 절차의 원본)
    - `Assets/Item/Script/ItemData.cs` (사용자가 질문할 경우 대비)
  </read_first>

  <what-built>
Plan 16-01 이 프로젝트 최초의 ScriptableObject `ItemData` 와 예시 에셋 2개를 만들었고, 이 플랜 Task 1 이 Unity 배치모드 임포트로 컴파일/임포트 에러 0건을 확인했으며, Task 2 가 `Assets/Item/Check.md` 검증 문서를 작성했습니다.

Claude 가 자동화로 확인한 것: 스크립트 구조(D-01/D-04/D-05/D-06), `.asset` guid 참조 무결성(D-07), Unity 임포트 성공.
Claude 가 자동화로 확인할 수 **없는** 것: **`UseEffect` 가 실제로 플레이어를 회복시키는지**(D-02/D-03). 이건 Play 모드에서만 증명됩니다.
  </what-built>

  <how-to-verify>
`Assets/Item/Check.md` 의 `## 사전 준비` 4단계와 `## Play 모드 체크리스트` 10항목을 순서대로 진행해 주세요. 핵심만 요약하면:

1. Unity 에디터를 열고 컴파일이 끝날 때까지 기다린 뒤 Console 에러가 0건인지 봅니다.
2. Project 창 `Assets/Item` 에 `HealthPotion`, `AncientKey` 두 에셋이 **물음표 아이콘 없이** 보이는지 확인합니다. (물음표가 보이면 손으로 쓴 guid 가 틀린 것 → 즉시 알려 주세요.)
3. `HealthPotion` 을 선택해 Inspector 에 `Id / Type / Effect Type / Amount` **4개 필드만** 보이고 값이 `health_potion_01 / Consumable / Heal / 20` 인지 확인합니다.
4. 플레이어가 있는 씬에서 **Play 를 시작**하고, 적에게 맞거나 떨어져서 체력을 깎습니다. 현재 체력을 기억합니다.
5. Play 중에 `HealthPotion.asset` 을 클릭하고, Inspector 맨 위 컴포넌트 헤더의 **⋮ 버튼(또는 헤더 우클릭)** → **`Phase16: Use On Player`** 를 누릅니다.
   → 체력이 **정확히 20 회복**되어야 합니다.
6. 같은 방법으로 `AncientKey.asset` 에서 `Phase16: Use On Player` 를 누릅니다.
   → **아무 일도 일어나지 않아야** 합니다 (체력 변화 0, Console 예외 0).
7. 체력이 가득 찬 상태에서 5번을 한 번 더 해서 최대 체력을 넘지 않는지 봅니다.
8. Play 를 끄고 Console 에러 0건인지 확인합니다.

확인하시는 대로 `Assets/Item/Check.md` 의 체크박스를 채우고 `## 결과 기록` 절에 날짜와 결과를 적겠습니다.
  </how-to-verify>

  <action>
이 태스크는 **사용자 응답 없이 진행하지 않는다.** 실행자가 할 일은 정확히 다음 3가지다:

1. 위 `<what-built>` / `<how-to-verify>` 내용을 사용자에게 그대로 제시하고 **멈춘다**. 사용자를 대신해 어떤 항목도 PASS 로 추정하지 않는다 (Phase 9 Plan 3 선례 — 검증을 생략했을 때 PASS 로 허위 기록하지 않고 "검증 생략" 상태를 그대로 남겼다).
2. 사용자 응답을 받으면 `Assets/Item/Check.md` 의 `## Play 모드 체크리스트` 체크박스를 응답대로 `- [ ]` -> `- [x]` 로 바꾼다. 통과하지 못했거나 확인하지 않은 항목은 `- [ ]` 그대로 두고, 항목 끝에 ` — FAIL: <증상>` 또는 ` — 미확인: <사유>` 를 덧붙인다.
3. `## 결과 기록` 절에 `검증 일자` / `검증자` / `PASS n / FAIL n / 미확인 n` 요약을 채운다. FAIL 이 있으면 증상을 그대로 옮겨 적고 이 phase 를 완료로 표시하지 않는다.

FAIL 발생 시 1차 진단 힌트 (원인별):
- 에셋 아이콘이 물음표 / Inspector 가 비어 있음 -> `.asset` 의 `m_Script` guid 와 `Assets/Item/Script/ItemData.cs.meta` 의 guid 불일치. `grep -oE '[0-9a-f]{32}'` 로 두 값을 대조한다.
- `Phase16: Use On Player` 메뉴가 안 보임 -> `#if UNITY_EDITOR` 블록 누락이거나 컴파일 실패. Console 을 먼저 확인한다.
- 메뉴는 보이는데 NullReferenceException -> 씬에 `PlayerInteraction` 이 없거나 `PlayerStats` 가 같은 GameObject 에 없다. 씬/프리팹 문제이지 `ItemData` 문제가 아니다.
- 체력이 20 이 아닌 값만큼 회복 -> `HealthPotion.asset` 의 `amount` 값을 확인한다.

커밋은 `Assets/Item/Check.md` 1개 파일만 pathspec 으로 명시한다.
  </action>

  <verify>
    <automated>grep -c "^- \[x\] " Assets/Item/Check.md; grep -c "검증 일자" Assets/Item/Check.md</automated>
  </verify>

  <acceptance_criteria>
    - 사용자가 체크리스트 10항목 각각에 대해 PASS / FAIL / 미확인 중 하나를 응답했다.
    - 7번 항목(HealthPotion 사용 시 체력 +20)의 결과가 명시적으로 기록되었다 — 이 phase 의 핵심 증거이므로 "미확인"으로 남길 경우 그 사유를 Check.md 에 적는다.
    - `Assets/Item/Check.md` 의 `## 결과 기록` 절에 검증 일자와 PASS/FAIL 요약이 채워졌고, 실패 항목이 있으면 증상과 함께 기록되었다.
    - `grep -c "^- \[x\] " Assets/Item/Check.md` 의 값 + 미확인으로 남긴 항목 수의 합이 `10` 이다.
    - 실패가 있으면 `.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/` 에 후속 조치가 기록되고, 이 phase 를 임의로 완료 처리하지 않는다 (Phase 9 Plan 3 선례: 미검증을 PASS 로 허위 기록하지 않는다).
    - Check.md 갱신 커밋 1개. pathspec 은 `Assets/Item/Check.md` 뿐.
  </acceptance_criteria>

  <done>사용자가 Play 모드에서 10항목을 실제로 확인했고, 그 결과(PASS/FAIL/미확인)가 `Assets/Item/Check.md` 의 체크박스와 `## 결과 기록` 절에 사실 그대로 반영되었다.</done>

  <resume-signal>10항목 결과를 알려 주세요. 전부 통과했으면 "approved" 라고만 하셔도 됩니다. 문제가 있으면 몇 번 항목이 어떻게 실패했는지 알려 주세요.</resume-signal>
</task>

</tasks>

<verification>
1. `ls Assets/Item/Check.md Assets/Item/Check.md.meta` — 둘 다 존재.
2. Unity 배치모드 로그에서 `Assets/` 경로의 `error CS` 0건, `Assets/Item` 관련 임포트 에러 0건.
3. `grep -c "^- \[ \] " Assets/Item/Check.md` — Task 2 직후 10, Task 3 이후에는 체크된 항목만큼 감소.
4. `git status --porcelain -- ':!.planning'` 가 이 phase 범위 밖의 새 변경을 담고 있지 않다.
5. `git log --oneline -4 -- Assets/Item` 이 16-01 커밋 2개 + 이 플랜 커밋들만 보여준다.
</verification>

<success_criteria>
- Unity 6000.3.10f1 이 `Assets/Item/` 을 컴파일 에러 0 / 임포트 에러 0 / missing script 0 으로 받아들였다.
- `Assets/Item/Check.md` 가 정적 회귀 결과 + Play 모드 체크리스트 10항목 + 범위 밖 명시를 담고 존재한다.
- 사용자가 Play 모드에서 `HealthPotion` 사용 시 체력 +20, `AncientKey` 사용 시 변화 0 을 실제로 확인했다 (또는 미확인 사유가 Check.md 에 명시되었다).
- 이 플랜이 `Assets/Item/` 밖의 어떤 파일도 커밋하지 않았다.
</success_criteria>

<output>
완료 후 `.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/16-02-SUMMARY.md` 를 작성한다.
</output>
