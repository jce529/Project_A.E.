---
phase: 16-iitem-itemdata-scriptableobject-id-useeffect-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Item/Script/ItemData.cs
  - Assets/Item/Script/ItemData.cs.meta
  - Assets/Item/HealthPotion.asset
  - Assets/Item/HealthPotion.asset.meta
  - Assets/Item/AncientKey.asset
  - Assets/Item/AncientKey.asset.meta
  - Assets/Item.meta
  - Assets/Item/Script.meta
  - Assets/Item/Script/IItem.cs.meta
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "ItemData 라는 ScriptableObject 가 존재하고 IItem 을 직접 구현한다 (별도 런타임 아이템 클래스 없음)"
    - "ItemData 에셋은 id(string) / type(Consumable|Progression) / effectType(Heal) / amount(float) 네 필드만 직렬화한다"
    - "type=Consumable + effectType=Heal 인 ItemData 의 UseEffect(player) 를 호출하면 기존 PlayerStats.Heal(amount) 가 실제로 호출되어 플레이어 체력이 amount 만큼 회복된다"
    - "type=Progression 인 ItemData 의 UseEffect(player) 는 아무 일도 하지 않고 예외도 내지 않는다"
    - "Assets/Item/ 아래에 소모품(Heal) 1개 + 진행아이템 1개의 실제 .asset 파일이 존재하며, 각각 ItemData 스크립트를 가리킨다"
  artifacts:
    - path: "Assets/Item/Script/ItemData.cs"
      provides: "프로젝트 최초 ScriptableObject. ItemData : ScriptableObject, IItem + ItemType/ConsumableEffectType enum + UseEffect 본체 + 에디터 전용 검증 훅"
      contains: "public class ItemData : ScriptableObject, IItem"
      min_lines: 40
    - path: "Assets/Item/Script/ItemData.cs.meta"
      provides: ".asset 파일들이 m_Script 로 참조할 고정 guid"
      contains: "guid: 501b19c5008706d0a3f2bf69aacf52f6"
    - path: "Assets/Item/HealthPotion.asset"
      provides: "소모품(Heal) 예시 에셋 — id=health_potion_01, amount=20"
      contains: "id: health_potion_01"
    - path: "Assets/Item/AncientKey.asset"
      provides: "진행아이템 예시 에셋 — id=ancient_key_01, type=1"
      contains: "id: ancient_key_01"
  key_links:
    - from: "Assets/Item/Script/ItemData.cs"
      to: "PlayerStats.Heal(float)"
      via: "UseEffect 안에서 player.GetComponent<PlayerStats>().Heal(amount) — 널 가드 없음 (프로젝트 관행)"
      pattern: "player\\.GetComponent<PlayerStats>\\(\\)\\.Heal\\(amount\\)"
    - from: "Assets/Item/HealthPotion.asset"
      to: "Assets/Item/Script/ItemData.cs"
      via: "m_Script guid 참조"
      pattern: "m_Script: \\{fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3\\}"
    - from: "Assets/Item/AncientKey.asset"
      to: "Assets/Item/Script/ItemData.cs"
      via: "m_Script guid 참조"
      pattern: "m_Script: \\{fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3\\}"
---

<objective>
Phase 16 잠금 결정 D-01 ~ D-07 을 `Assets/Item/` 아래에 구현한다. 이 플랜이 **이 프로젝트 최초의 ScriptableObject** 를 만든다.

- D-01: `ItemData : ScriptableObject, IItem` — `ItemData` 가 `IItem` 을 직접 구현한다. 별도 런타임 아이템 인스턴스 클래스는 만들지 않는다.
- D-02: `UseEffect()` 는 스텁이 아니라 끝까지 구현한다. 소모품은 효과를 실제로 적용하고, 진행아이템은 의도적 no-op.
- D-03: 소모품 effectType 은 이번 phase 에 `Heal` 하나뿐. `amount`(float) 와 함께 **기존** `PlayerStats.Heal(float)` 를 호출한다. `PlayerStats` 에 새 메서드를 만들지 않는다.
- D-04: `ItemType` enum 2값(`Consumable` / `Progression`). SO 서브클래싱 없이 단일 클래스 + enum 분기.
- D-05: `id` 는 `[SerializeField] string id` 수동 입력. 자동 생성/중복 검증 도구 없음.
- D-06: 필드는 `id` / `type` / `effectType` / `amount` **4개뿐**. `displayName`/`icon`/`description` 금지.
- D-07: 실제 `.asset` 파일 2개(소모품 Heal 1 + 진행아이템 1)를 `Assets/Item/` 에 만든다.

Purpose: Phase 17(인벤토리)과 Phase 18(세이브 연동)이 참조할 아이템 정적 데이터 스키마를 확정한다. 지금은 인벤토리도 월드 픽업도 없으므로, 이 플랜이 만드는 `.asset` 2개가 Phase 17 의 테스트 데이터가 된다.
Output: 신규 파일 6개(`ItemData.cs` + meta, `.asset` 2개 + meta 2개). 기존 소스 코드 0줄 변경.
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 실행할 수 있다. 아래 내용만으로 완결되어야 하며, CONTEXT.md / RESEARCH.md 를 다시 읽지 않아도 실행 가능하다.
</execution_context>

<context>
@.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/16-CONTEXT.md
@.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/16-RESEARCH.md
@Assets/Item/Script/IItem.cs
@Assets/Player/Script/PlayerInteraction.cs
@Assets/Player/Script/PlayerStats.cs
</context>

<interfaces>
<!-- 실행자가 필요로 하는 계약 전부. 코드베이스 탐색 없이 이 블록만 보고 작업할 것. -->

`Assets/Item/Script/IItem.cs` (전체 — **수정 금지**, 이 시그니처를 그대로 구현해야 한다):
```csharp
public interface IItem
{
    void UseEffect(PlayerInteraction player);
}
```

`Assets/Player/Script/PlayerInteraction.cs` (**수정 금지**, 관련 부분만 발췌):
```csharp
[DisallowMultipleComponent]
public class PlayerInteraction : MonoBehaviour   // MonoBehaviour 이므로 GetComponent<T>() 사용 가능
{
    public MonoBehaviour CurrentTarget { get; private set; }
    public bool TryInteract()
}
```
Player.prefab 상에서 `PlayerInteraction` 과 `PlayerStats` 는 **같은 GameObject** 에 붙어 있다 —
따라서 `player.GetComponent<PlayerStats>()` 가 유효하다.

`Assets/Player/Script/PlayerStats.cs` (**수정 금지 — 재사용만**, 관련 부분만 발췌):
```csharp
public class PlayerStats : HP
{
    public static PlayerStats Instance { get; }        // FindAnyObjectByType 기반 지연 초기화

    // 37행. Heal effectType 이 호출할 기존 메서드. 새 Heal 로직을 만들지 않는다.
    public override void Heal(float amount)
    {
        health += amount;
        ClampHealth();
    }

    // 75행. 프로젝트의 "널 가드 없음" 관행 근거:
    //   CameraController.Instance.Shake();   <- 널 체크 없이 바로 호출
    public override void TakeDamage(float dmg)
}
```

**고정 guid (이 플랜에서 새로 할당 — 임의로 바꾸지 말 것):**

| 파일 | guid |
|---|---|
| `Assets/Item/Script/ItemData.cs.meta` | `501b19c5008706d0a3f2bf69aacf52f6` |
| `Assets/Item/HealthPotion.asset.meta` | `1ca8a1cec6cae3595e071afccaf8623e` |
| `Assets/Item/AncientKey.asset.meta` | `65cc29b1f44b856171d66dbe8ddec240` |

세 guid 모두 현재 `Assets/` / `ProjectSettings/` / `Packages/` 전체에서 충돌 0건임을 계획 단계에서 확인했다.
`.asset` 의 `m_Script` 가 `ItemData.cs.meta` 의 guid 를 가리켜야 하므로, **먼저 Task 1 의 .meta 를
쓰고 나서 Task 2 의 .asset 을 써야 한다.**
</interfaces>

<encoding_note>
- 이 플랜이 만드는 **모든 신규 파일은 순수 ASCII, UTF-8(BOM 없음), LF 줄바꿈**이다. 형제 파일
  `Assets/Item/Script/IItem.cs` 가 정확히 그 형식이다(`file` 출력: `ASCII text`).
- 주석·문자열에 한글을 쓰지 않는다. 이 저장소는 Phase 9/10/11 에서 인코딩 왕복으로 비-ASCII
  바이트가 U+FFFD 로 훼손된 사고 이력이 있어, 신규 파일은 ASCII 로만 작성하는 것이 관행이다.
- `Assets/Player/Script/PlayerStats.cs` 는 UTF-8 with BOM + 한글 주석 파일이다. **이 플랜에서
  절대 열어서 수정하지 않는다** (읽기만 허용).
- `.meta` / `.asset` 파일의 `userData: ` / `m_EditorClassIdentifier: ` 뒤 **후행 공백 1칸은 Unity 가
  직접 출력하는 형식**이다. 그대로 적는다(없어도 Unity 는 동작하지만, 그러면 최초 임포트 때
  Unity 가 파일을 다시 써서 불필요한 diff 가 생긴다).
</encoding_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ItemData.cs 신규 작성 (D-01 ~ D-06) + .meta guid 고정</name>
  <files>Assets/Item/Script/ItemData.cs, Assets/Item/Script/ItemData.cs.meta, Assets/Item.meta, Assets/Item/Script.meta, Assets/Item/Script/IItem.cs.meta</files>

  <read_first>
    - `Assets/Item/Script/IItem.cs` (구현해야 하는 인터페이스 — 4줄, 수정 금지)
    - `Assets/Player/Script/PlayerStats.cs` (37행 `public override void Heal(float amount)` 시그니처 확인 + 75행 `CameraController.Instance.Shake()` 무-널가드 관행 확인. **읽기 전용**)
    - `Assets/Player/Script/PlayerInteraction.cs` (MonoBehaviour 임을 확인 → `GetComponent<PlayerStats>()` 가능. **읽기 전용**)
    - `Assets/Item/Script/IItem.cs.meta` (현재 내용 확인용 — 이 태스크에서 **내용을 바꾸지 않고 git 에 추가만** 한다)
  </read_first>

  <action>
**(1) `Assets/Item/Script/ItemData.cs` 를 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다:**

```csharp
using UnityEngine;

// Phase 16 (D-01): the project's first ScriptableObject. ItemData implements IItem directly -
// there is no separate runtime item instance class, so the static data and the use logic live
// in one file. Phase 17's inventory will hold ItemData references and call UseEffect on them.
[CreateAssetMenu(menuName = "Items/Item Data", fileName = "New Item")]
public class ItemData : ScriptableObject, IItem
{
    // D-04: exactly two kinds. No SO subclass hierarchy - a single class branches on this enum.
    public enum ItemType
    {
        Consumable,
        Progression
    }

    // D-03: Heal is the only consumable effect in this phase. Future effects are added here.
    public enum ConsumableEffectType
    {
        Heal
    }

    // D-05: manually typed into the Inspector. No auto-generation and no duplicate validation
    // this phase. Kept as a string because Phase 18 will store it in SaveData as ItemSaveEntry.itemId.
    [SerializeField] private string id;

    // D-06: minimal schema. No displayName / icon / description until a UI layer exists.
    [SerializeField] private ItemType type;

    // Consumable-only parameters. Leave them at their defaults on a Progression item.
    [SerializeField] private ConsumableEffectType effectType;
    [SerializeField] private float amount;

    // The one accessor this phase needs: without it `id` would be a write-only field. Phase 17
    // can add Type / amount accessors when the inventory actually needs them.
    public string Id => id;

    // D-02: fully implemented, not a stub. Consumables apply their effect for real; Progression
    // items are an intentional no-op until Phase 17+ gives them meaning.
    public void UseEffect(PlayerInteraction player)
    {
        if (type != ItemType.Consumable) return;

        switch (effectType)
        {
            case ConsumableEffectType.Heal:
                // D-03: reuse the existing PlayerStats.Heal(float) - no new PlayerStats method.
                // No null guard on GetComponent: that matches the unguarded convention already
                // used by PlayerStats.TakeDamage -> CameraController.Instance.Shake().
                player.GetComponent<PlayerStats>().Heal(amount);
                break;
        }
    }

#if UNITY_EDITOR
    // Phase 16 verification hook. There is no inventory and no world pickup yet (both Phase 17),
    // so this is the only way to invoke UseEffect and prove D-02/D-03 end to end. Enter Play
    // mode, select the asset, then use the Inspector's context menu (three dots / right-click
    // the header) -> "Phase16: Use On Player". Same pattern as the SaveLoadManager ContextMenu
    // hooks added in Phase 11 / Phase 14.
    [ContextMenu("Phase16: Use On Player")]
    private void UseOnPlayerFromInspector()
    {
        UseEffect(FindAnyObjectByType<PlayerInteraction>());
    }
#endif
}
```

금지 사항(위반 시 이 태스크는 실패):
- `displayName` / `icon` / `description` / `stackable` / `maxStack` 등 어떤 추가 필드도 넣지 않는다 (D-06).
- `ConsumableItemData` / `ProgressionItemData` 같은 서브클래스를 만들지 않는다 (D-04).
- `if (stats != null)` / `?.` 같은 널 가드를 넣지 않는다 (프로젝트 관행, RESEARCH Pitfall 2).
- `private PlayerStats _cached;` 같은 컴포넌트 캐시 필드를 만들지 않는다 (RESEARCH Pitfall 1 — SO 가 씬 오브젝트 참조를 들고 있으면 씬 재로드 후 stale 참조가 된다).
- `Assets/Player/Script/PlayerStats.cs` 와 `Assets/Item/Script/IItem.cs` 는 한 글자도 바꾸지 않는다.

**(2) `Assets/Item/Script/ItemData.cs.meta` 를 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다** (guid 는 Task 2 의 `.asset` 들이 참조하므로 반드시 이 값이어야 한다):

```
fileFormatVersion: 2
guid: 501b19c5008706d0a3f2bf69aacf52f6
MonoImporter:
  externalObjects: {}
  serializedVersion: 2
  defaultReferences: []
  executionOrder: 0
  icon: {instanceID: 0}
  userData: 
  assetBundleName: 
  assetBundleVariant: 
```

(`userData:` / `assetBundleName:` / `assetBundleVariant:` 뒤에 각각 후행 공백 1칸. `Assets/Player/Script/PlayerStats.cs.meta` 와 동일한 형식이다.)

**(3) 이미 워킹트리에 untracked 로 존재하는 `.meta` 3개를 커밋에 포함한다 — 내용은 절대 수정하지 않는다:**
- `Assets/Item.meta`
- `Assets/Item/Script.meta`
- `Assets/Item/Script/IItem.cs.meta`

이유: `IItem.cs` 는 커밋 58ac900 으로 들어갔는데 그 `.meta` 가 빠져 있어, 다른 기기에서 클론하면 Unity 가 새 guid 를 발급하고 이 플랜이 만드는 `.asset` 참조 체계와 어긋날 수 있다. 세 파일 모두 **내용 수정 없이 git add 만** 한다 (CLAUDE.md 3번 "정밀한 변경" — 기존 파일 임의 개선 금지). `Assets/Item/Script/IItem.cs.meta` 가 Unity 표준보다 짧은 형태(`MonoImporter` 블록 없음)인 것은 알고 있으며, Unity 가 최초 임포트 때 스스로 채운다. 이 태스크에서 고치지 않는다.

**(4) 커밋**: 위 5개 파일만 pathspec 으로 명시해 커밋 1개. 와일드카드나 `-A` / `.` 금지.
  </action>

  <verify>
    <automated>test -f Assets/Item/Script/ItemData.cs &amp;&amp; grep -c "public class ItemData : ScriptableObject, IItem" Assets/Item/Script/ItemData.cs</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "public class ItemData : ScriptableObject, IItem" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다 (D-01).
    - `grep -c "\[CreateAssetMenu(menuName = \"Items/Item Data\", fileName = \"New Item\")\]" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다.
    - `grep -c "public void UseEffect(PlayerInteraction player)" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다 (IItem 시그니처 일치).
    - `grep -c "player.GetComponent<PlayerStats>().Heal(amount);" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다 (D-03 키 링크).
    - `grep -c "if (type != ItemType.Consumable) return;" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다 (D-02 진행아이템 no-op 분기).
    - `grep -cE "^\s*\[SerializeField\]" Assets/Item/Script/ItemData.cs` 가 정확히 `4` 를 출력한다 (D-06 — 필드 4개, 그 이상 금지).
    - `grep -cE "displayName|icon|description|stackable|maxStack" Assets/Item/Script/ItemData.cs` 가 `0` 을 출력한다 (D-06 금지 필드 부재). *주의: `icon: {instanceID: 0}` 는 `.meta` 쪽에만 있고 `.cs` 에는 없어야 한다.*
    - `grep -cE "!= null|\?\." Assets/Item/Script/ItemData.cs` 가 `0` 을 출력한다 (널 가드 없음 — 프로젝트 관행).
    - `grep -cE "class (Consumable|Progression)ItemData" Assets/Item/Script/ItemData.cs` 가 `0` 을 출력한다 (D-04 서브클래스 금지).
    - `grep -c "Consumable," Assets/Item/Script/ItemData.cs` 가 `1`, `grep -c "        Progression" Assets/Item/Script/ItemData.cs` 가 `1`, `grep -c "        Heal" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다 (enum 값이 정확히 D-04/D-03 대로).
    - `grep -c "public string Id => id;" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다.
    - `grep -c "\[ContextMenu(\"Phase16: Use On Player\")\]" Assets/Item/Script/ItemData.cs` 가 `1`, `grep -c "#if UNITY_EDITOR" Assets/Item/Script/ItemData.cs` 가 `1`, `grep -c "#endif" Assets/Item/Script/ItemData.cs` 가 `1` 을 출력한다.
    - `file Assets/Item/Script/ItemData.cs` 출력이 `ASCII text` 이고 `CRLF` / `BOM` 을 포함하지 않는다.
    - `grep -c "guid: 501b19c5008706d0a3f2bf69aacf52f6" Assets/Item/Script/ItemData.cs.meta` 가 `1` 을 출력한다.
    - `grep -c "MonoImporter:" Assets/Item/Script/ItemData.cs.meta` 가 `1` 을 출력한다.
    - `git status --porcelain Assets/Player Assets/Item/Script/IItem.cs` 가 빈 출력이다 (PlayerStats.cs / PlayerInteraction.cs / IItem.cs 0줄 변경).
    - `git diff HEAD --stat -- Assets/Item/Script/IItem.cs.meta Assets/Item.meta Assets/Item/Script.meta` 가 "신규 추가"만 보여주고 기존 바이트 변경이 없다 (`git show :Assets/Item/Script/IItem.cs.meta | wc -c` 가 `59`).
    - 커밋 1개. pathspec 은 위 5개 파일만. `git show --stat --name-only HEAD` 출력에 그 5개 외의 파일이 없다.
  </acceptance_criteria>

  <done>`ItemData.cs` 가 `IItem` 을 구현하는 ScriptableObject 로 존재하고, 필드 4개 / enum 2개 / UseEffect 본체 / 에디터 전용 검증 훅을 갖추며, `.meta` guid 가 `501b19c5008706d0a3f2bf69aacf52f6` 로 고정되어 Task 2 의 `.asset` 이 참조할 수 있다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 예시 .asset 2개 손수 작성 — HealthPotion(Consumable/Heal) + AncientKey(Progression) (D-07)</name>
  <files>Assets/Item/HealthPotion.asset, Assets/Item/HealthPotion.asset.meta, Assets/Item/AncientKey.asset, Assets/Item/AncientKey.asset.meta</files>

  <read_first>
    - `Assets/Item/Script/ItemData.cs` (Task 1 결과 — 직렬화 필드명이 정확히 `id` / `type` / `effectType` / `amount` 인지, enum 선언 순서가 `Consumable`(0) / `Progression`(1) 및 `Heal`(0) 인지 확인. YAML 의 정수값이 이 순서에 의존한다)
    - `Assets/Item/Script/ItemData.cs.meta` (Task 1 결과 — `m_Script` 에 넣을 guid 를 이 파일에서 그대로 복사)
    - `Assets/Settings/Renderer2D.asset` 의 첫 12줄 (이 프로젝트에 실재하는 ScriptableObject `.asset` YAML 헤더 형식 참고)
    - `Assets/Settings/Renderer2D.asset.meta` (`NativeFormatImporter` + `mainObjectFileID: 11400000` 형식 참고)
  </read_first>

  <action>
Unity 에디터를 배치 실행하지 않고 `.asset` YAML 을 직접 작성한다. Unity 의 `.asset` 은 `m_Script` 가 스크립트 `.meta` 의 guid 를 가리키는 평문 YAML 이므로, guid 만 맞으면 에디터가 그대로 임포트한다. (에디터 임포트 실검증은 Plan 16-02 에서 수행한다.)

**(1) `Assets/Item/HealthPotion.asset` 을 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다:**

```
%YAML 1.1
%TAG !u! tag:unity3d.com,2011:
--- !u!114 &11400000
MonoBehaviour:
  m_ObjectHideFlags: 0
  m_CorrespondingSourceObject: {fileID: 0}
  m_PrefabInstance: {fileID: 0}
  m_PrefabAsset: {fileID: 0}
  m_GameObject: {fileID: 0}
  m_Enabled: 1
  m_EditorHideFlags: 0
  m_Script: {fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3}
  m_Name: HealthPotion
  m_EditorClassIdentifier: 
  id: health_potion_01
  type: 0
  effectType: 0
  amount: 20
```

값 근거: `type: 0` = `ItemType.Consumable`(enum 첫 값), `effectType: 0` = `ConsumableEffectType.Heal`, `amount: 20` = 회복량 20. `m_EditorClassIdentifier: ` 뒤 후행 공백 1칸.

**(2) `Assets/Item/HealthPotion.asset.meta` 를 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다:**

```
fileFormatVersion: 2
guid: 1ca8a1cec6cae3595e071afccaf8623e
NativeFormatImporter:
  externalObjects: {}
  mainObjectFileID: 11400000
  userData: 
  assetBundleName: 
  assetBundleVariant: 
```

**(3) `Assets/Item/AncientKey.asset` 을 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다:**

```
%YAML 1.1
%TAG !u! tag:unity3d.com,2011:
--- !u!114 &11400000
MonoBehaviour:
  m_ObjectHideFlags: 0
  m_CorrespondingSourceObject: {fileID: 0}
  m_PrefabInstance: {fileID: 0}
  m_PrefabAsset: {fileID: 0}
  m_GameObject: {fileID: 0}
  m_Enabled: 1
  m_EditorHideFlags: 0
  m_Script: {fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3}
  m_Name: AncientKey
  m_EditorClassIdentifier: 
  id: ancient_key_01
  type: 1
  effectType: 0
  amount: 0
```

값 근거: `type: 1` = `ItemType.Progression`. `effectType`/`amount` 는 소모품 전용 파라미터이므로 진행아이템에서는 기본값(0)으로 둔다 — RESEARCH Pitfall 5 가 경고한 "진행아이템에 쓰레기 값이 남는" 상황을 예시 에셋부터 피한다.

**(4) `Assets/Item/AncientKey.asset.meta` 를 신규 생성한다. 파일 전체 내용은 정확히 다음과 같다:**

```
fileFormatVersion: 2
guid: 65cc29b1f44b856171d66dbe8ddec240
NativeFormatImporter:
  externalObjects: {}
  mainObjectFileID: 11400000
  userData: 
  assetBundleName: 
  assetBundleVariant: 
```

**(5) 커밋**: 위 4개 파일만 pathspec 으로 명시해 커밋 1개.

주의: 4개 파일 모두 순수 ASCII / LF / BOM 없음. `Assets/Item/Script/ItemData.cs` 는 이 태스크에서 다시 수정하지 않는다.
  </action>

  <verify>
    <automated>test -f Assets/Item/HealthPotion.asset &amp;&amp; test -f Assets/Item/AncientKey.asset &amp;&amp; grep -c "guid: 501b19c5008706d0a3f2bf69aacf52f6" Assets/Item/HealthPotion.asset Assets/Item/AncientKey.asset</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "m_Script: {fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3}" Assets/Item/HealthPotion.asset` 가 `1` 을 출력한다.
    - `grep -c "m_Script: {fileID: 11500000, guid: 501b19c5008706d0a3f2bf69aacf52f6, type: 3}" Assets/Item/AncientKey.asset` 가 `1` 을 출력한다.
    - 위 두 줄의 guid 가 `Assets/Item/Script/ItemData.cs.meta` 의 guid 와 동일하다: `test "$(grep -oE '[0-9a-f]{32}' Assets/Item/Script/ItemData.cs.meta | head -1)" = "501b19c5008706d0a3f2bf69aacf52f6"` 가 성공한다.
    - `head -3 Assets/Item/HealthPotion.asset` 이 `%YAML 1.1` / `%TAG !u! tag:unity3d.com,2011:` / `--- !u!114 &11400000` 3줄이다 (`AncientKey.asset` 도 동일).
    - `grep -c "^  id: health_potion_01$" Assets/Item/HealthPotion.asset` 가 `1`, `grep -c "^  type: 0$" Assets/Item/HealthPotion.asset` 가 `1`, `grep -c "^  effectType: 0$" Assets/Item/HealthPotion.asset` 가 `1`, `grep -c "^  amount: 20$" Assets/Item/HealthPotion.asset` 가 `1` 을 출력한다.
    - `grep -c "^  id: ancient_key_01$" Assets/Item/AncientKey.asset` 가 `1`, `grep -c "^  type: 1$" Assets/Item/AncientKey.asset` 가 `1`, `grep -c "^  amount: 0$" Assets/Item/AncientKey.asset` 가 `1` 을 출력한다.
    - `grep -c "^  m_Name: HealthPotion$" Assets/Item/HealthPotion.asset` 가 `1`, `grep -c "^  m_Name: AncientKey$" Assets/Item/AncientKey.asset` 가 `1` 을 출력한다 (m_Name 이 파일명과 일치).
    - `grep -c "guid: 1ca8a1cec6cae3595e071afccaf8623e" Assets/Item/HealthPotion.asset.meta` 가 `1`, `grep -c "guid: 65cc29b1f44b856171d66dbe8ddec240" Assets/Item/AncientKey.asset.meta` 가 `1` 을 출력한다.
    - `grep -c "mainObjectFileID: 11400000" Assets/Item/HealthPotion.asset.meta Assets/Item/AncientKey.asset.meta` 가 두 파일 모두 `1` 을 출력한다.
    - 네 파일 모두 `file <path>` 출력이 `ASCII text` 이다.
    - `ls Assets/Item/*.asset | wc -l` 가 `2` 를 출력한다 (에셋 2개뿐 — D-07 의 "1~2개" 상한 준수).
    - `git status --porcelain Assets/Item/Script` 가 빈 출력이다 (Task 1 산출물 무변경).
    - 커밋 1개. pathspec 은 위 4개 파일만.
  </acceptance_criteria>

  <done>`Assets/Item/` 에 소모품(Heal, amount 20) 1개 + 진행아이템 1개의 `.asset` 파일이 각자 `.meta` 와 함께 존재하고, 둘 다 `ItemData.cs` 의 guid 를 `m_Script` 로 참조한다.</done>
</task>

</tasks>

<verification>
플랜 전체 정적 회귀 (전부 저장소 루트에서 실행):

1. `ls Assets/Item/Script/ItemData.cs Assets/Item/Script/ItemData.cs.meta Assets/Item/HealthPotion.asset Assets/Item/HealthPotion.asset.meta Assets/Item/AncientKey.asset Assets/Item/AncientKey.asset.meta` — 6개 전부 존재.
2. `grep -c "player.GetComponent<PlayerStats>().Heal(amount);" Assets/Item/Script/ItemData.cs` == 1 — D-03 키 링크.
3. `grep -rc "501b19c5008706d0a3f2bf69aacf52f6" Assets/Item/HealthPotion.asset Assets/Item/AncientKey.asset Assets/Item/Script/ItemData.cs.meta` — 세 파일 모두 1.
4. `git diff HEAD~2 --name-only` 가 `Assets/Item/` 아래 9개 파일만 나열한다 (`Assets/Player/`, `Assets/Script/`, `Assets/SaveSystem/` 0건).
5. `grep -rn "ScriptableObject" Assets --include=*.cs | grep -v "Assets/Item/Script/ItemData.cs" | wc -l` == 0 — `ItemData` 가 프로젝트 유일의 ScriptableObject 임을 재확인(RESEARCH 전제).
6. `grep -rc $'\xef\xbf\xbd' Assets/Item/*.asset Assets/Item/Script/*.cs` — 전부 0 (U+FFFD 훼손 없음).
</verification>

<success_criteria>
- `ItemData : ScriptableObject, IItem` 이 존재하고 `UseEffect(PlayerInteraction)` 를 구현한다 (D-01).
- `UseEffect` 가 소모품일 때만 효과를 적용하고 진행아이템에서는 즉시 return 한다 (D-02).
- Heal 분기가 `player.GetComponent<PlayerStats>().Heal(amount)` 로 **기존** 메서드를 재사용하며 널 가드가 없다 (D-03).
- `ItemType` 이 `Consumable` / `Progression` 2값이고 서브클래스가 없다 (D-04).
- `id` 가 `[SerializeField] private string id;` 수동 입력 필드다 (D-05).
- `[SerializeField]` 필드가 정확히 4개이고 표시용 메타데이터 필드가 0개다 (D-06).
- `Assets/Item/` 에 `.asset` 2개가 존재하고 둘 다 `ItemData` guid 를 참조한다 (D-07).
- `Assets/` 아래 기존 파일 중 내용이 바뀐 파일이 0개다 (CLAUDE.md 3번 정밀한 변경).
</success_criteria>

<output>
완료 후 `.planning/phases/16-iitem-itemdata-scriptableobject-id-useeffect-ui/16-01-SUMMARY.md` 를 작성한다.
</output>
