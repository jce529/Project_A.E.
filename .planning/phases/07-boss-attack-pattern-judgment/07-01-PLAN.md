---
phase: 07-boss-attack-pattern-judgment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Enemy/NewBoss/Script/States/CombatState.cs
  - Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]

must_haves:
  truths:
    - "정령 보스의 패턴 선택이 고정 라운드로빈이 아니라, 조건을 만족하는 후보들 중 가중치 랜덤으로 이루어진다"
    - "직전에 실행한 패턴과 동일한 패턴은 다음 판단에서 제외된다 (D-04 강제 체인 제외)"
    - "SpiritExhaustion 실행 직후에는 조건 평가를 건너뛰고 반드시 SpiritWakeRepel 이 선택된다"
    - "SpiritCharge / SpiritFarProjectile / SpiritExhaustion 은 거리 조건 없이 선택 가능하다"
    - "SpiritWakeRepel 은 일반 판단 풀에서는 플레이어가 RepelRange(1.5) 이내일 때만 선택된다"
    - "Stage2CombatState 와 WaterMonsterCombatState 는 단 한 줄도 변경되지 않는다"
  artifacts:
    - path: "Assets/Enemy/NewBoss/Script/States/CombatState.cs"
      provides: "범용 PatternCandidate 후보 클래스 + SelectWeightedPattern / ForceSelectPattern 헬퍼 + 타입 키 기반 쿨다운·연속금지 추적"
      contains: "protected class PatternCandidate"
    - path: "Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs"
      provides: "정령 보스 패턴 후보 4종 선언 + D-04 Exhaustion→WakeRepel 강제 체인"
      contains: "BuildCandidates"
  key_links:
    - from: "Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs"
      to: "CombatState.SelectWeightedPattern"
      via: "SelectAttackStrategy override 가 후보 목록을 넘겨 판단 위임"
      pattern: "SelectWeightedPattern\\(dist, _candidates\\)"
    - from: "Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs"
      to: "CombatState.ForceSelectPattern"
      via: "LastUsedPatternType == typeof(SpiritExhaustion) 체인 분기"
      pattern: "ForceSelectPattern\\(new SpiritWakeRepel\\(\\)\\)"
    - from: "Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs"
      to: "SpiritCombatState.SelectAttackStrategy"
      via: "base.SelectAttackStrategy(boss, dist) 호출 — 변경 없이 null/non-null 계약만 사용 (D-07b)"
      pattern: "base\\.SelectAttackStrategy\\(boss, dist\\)"
---

<objective>
`CombatState` 에 재사용 가능한 범용 패턴 후보 평가 헬퍼(거리 선택적 조건 + 쿨다운 + 연속사용금지 + 가중치 랜덤)를 추가하고, `SpiritCombatState` 의 조건 없는 고정 라운드로빈 배열을 이 헬퍼 기반 후보 선언으로 교체한다.

Purpose: Phase 5 D-03a 가 원래 의도했던 "조건 기반 패턴 판단"을 되살리되, TutorialBoss 의 고정 우선순위가 아니라 가중치 랜덤(D-03)으로 구현한다. 헬퍼는 향후 다른 보스(WaterMonster 등)가 재사용할 수 있도록 보스 종속성 없이 설계한다(D-01c).
Output: `CombatState.cs` 에 `PatternCandidate` / `SelectWeightedPattern` / `ForceSelectPattern` 추가, `SpiritCombatState.cs` 가 후보 4종만 선언하는 얇은 데이터 레이어로 전환.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-boss-attack-pattern-judgment/07-CONTEXT.md
@.planning/phases/07-boss-attack-pattern-judgment/07-RESEARCH.md
@.claude/CLAUDE.md

## 플래너 확정 사항 (실행자는 이 결정을 그대로 따를 것)

### 결정 1 — `SpiritFarProjectile` 에 거리 조건을 걸지 않는다 (RESEARCH.md 권고에서 의도적으로 벗어남)

07-RESEARCH.md 는 `minDistance: spirit.ChargeRange` (5.0) 를 권고했으나, 플래너가 실제 소스를 검증한 결과 **채택하지 않는다**. 이유 2가지:

1. **CONTEXT.md D-02a 위반.** D-02a 는 "`SpiritCharge`, `SpiritFarProjectile` 은 거리 조건 없이 쿨다운 + 연속금지만으로 판단한다"로 잠긴 결정이다. `minDistance` 부여는 이 잠긴 결정과 정면으로 충돌한다.
2. **실제로 영구 사용 불가가 된다.** `CombatState.Execute()` 는 `dist > boss.AttackRange + 1.0f` 이면 `SelectAttackStrategy` 를 호출하지 않고 Chase/Idle 로 전환한다. `Assets/Resources/Spirit Clone.prefab` 의 `AttackRange: 2.5` 이므로 `SelectAttackStrategy` 가 호출되는 구간은 `dist <= 3.5` 뿐이다. `minDistance = ChargeRange = 5.0` 은 이 구간과 절대 겹치지 않아 `SpiritFarProjectile` 이 영구히 선택 불가가 된다.

따라서 `ChargeRange` 는 **재활용하지 않고 손대지 않는다**. `ChargeRange` 는 이번 변경이 만든 orphan 이 아니라 기존부터 있던 미사용 필드이므로, CLAUDE.md 3항("기존에 있던 데드 코드는 언급만 하고 삭제하지 마세요")에 따라 삭제·수정하지 않는다.

### 결정 2 — `SpiritExhaustion` 에도 거리 조건을 걸지 않는다 (RESEARCH.md Open Question 1 해소)

RESEARCH.md 가 미해결로 남긴 질문에 대해 플래너가 **"거리 조건 없음"** 으로 확정한다. 근거: `SpiritExhaustion.ExecuteAttack` 은 `boss.StopMove()` 만 수행하며 플레이어 위치를 전혀 참조하지 않는다. 또한 이 패턴의 존재 목적은 D-04 체인(취약 → 급습)의 시작점이므로 플레이어 위치와 무관하게 발동해야 한다. 부수 효과로 거리 무조건 후보가 3개(Charge/Exhaustion/FarProjectile) 확보되어 RESEARCH.md Pitfall 1(후보 고갈로 인한 null 스핀)이 구조적으로 완화된다.

### 결정 3 — 거리 조건은 `SpiritWakeRepel` 에만 건다

`SpiritWakeRepel` 은 내부에서 `SpiritRepel` 을 호출하고, `SpiritRepel` 은 `Physics2D.OverlapCircleAll(origin, spirit.RepelRange, ...)` 로 판정하므로 `RepelRange`(1.5) 밖에서는 실질적으로 아무 효과가 없다. 따라서 `maxDistance: spirit.RepelRange` 를 부여한다(D-02b 의 "근접 패턴에는 거리 조건" 항목).

### 결정 4 — 쿨다운은 `Time.time` 절대 시각 비교 방식

`CombatState.Execute()` 는 공격 중(`return`) / 쿨타임 중(`return`) 두 개의 early-return 분기를 가지므로 매 프레임 감산 방식 타이머는 조용히 누락된다. 프로젝트에 이미 존재하는 `WaterMonsterCombatState._lastWaveTime` 과 동일하게 `Time.time` 타임스탬프 비교를 사용한다.

### 결정 5 — 추적 키는 슬롯 인덱스가 아닌 `System.Type`

이번 페이즈가 고치는 버그가 정확히 "인덱스 기반 장부(`_patternIndex`)" 이므로 절대 인덱스로 되돌아가지 않는다. `strategy.GetType()` 을 키로 사용한다(D-06a: `SpiritCharge` 중복 슬롯이 자연히 후보 1개로 정리됨).

## 현재 코드 계약 (검증 완료 — 유지해야 함)

<interfaces>
// Assets/Enemy/NewBoss/Script/States/Attacks/IAttackStrategy.cs
public interface IAttackStrategy
{
    void ExecuteAttack(BossController boss);
    float Cooldown { get; }
    string AnimationName { get; }
}

// Assets/Enemy/NewBoss/Script/States/CombatState.cs — 현재 시그니처
protected virtual bool ShouldTransitionToGroggy(BossController boss);
protected virtual IAttackStrategy SelectAttackStrategy(BossController boss, float dist);
// null 반환 = "이번 프레임 공격 시작 안 함". _decisionTimer 가 이미 <= 0 이므로
// 다음 프레임에 SelectAttackStrategy 가 다시 호출된다. Stage2CombatState 가 이 계약에
// 의존하므로 반드시 유지할 것.

// Assets/Enemy/WaterSpirit/Script/SpiritController.cs — 거리 조건에 쓸 기존 필드
public float RepelRange = 1.5f;   // 프리팹 값 1.5 — SpiritRepel 판정 반경과 동일
public float ChargeRange = 5.0f;  // 기존부터 미사용(dead). 이번 페이즈에서 손대지 않음.

// 각 전략의 Cooldown (그대로 유지, 조정하지 않음)
SpiritCharge.Cooldown        => 3.0f
SpiritExhaustion.Cooldown    => 2.5f
SpiritWakeRepel.Cooldown     => 1.5f
SpiritFarProjectile.Cooldown => 2.5f
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CombatState 에 범용 패턴 후보 평가 헬퍼 추가 (D-01, D-02b, D-03, D-04, D-05)</name>

  <read_first>
    - Assets/Enemy/NewBoss/Script/States/CombatState.cs (수정 대상 — 전체 읽을 것)
    - Assets/Enemy/NewBoss/Script/States/Attacks/IAttackStrategy.cs (Cooldown/AnimationName 계약)
    - Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs (Time.time 쿨다운 + 풀 랜덤 기존 선례. 읽기만 할 것 — 절대 수정 금지, D-08b)
    - Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs (쿨다운/연속금지 게이팅 스타일 참고. 고정 우선순위 방식은 채택하지 않음 — D-03a)
    - .planning/phases/07-boss-attack-pattern-judgment/07-CONTEXT.md (D-01 ~ D-08 잠금 결정)
  </read_first>

  <files>Assets/Enemy/NewBoss/Script/States/CombatState.cs</files>

  <action>
`Assets/Enemy/NewBoss/Script/States/CombatState.cs` 를 아래 4개 지점만 수정한다. 기존 `Enter`/`Execute`/`ShouldTransitionToGroggy`/`SelectAttackStrategy`/`Exit` 의 기존 로직은 **한 줄도 변경하지 않는다**(추가만 한다). 기존 주석·포맷팅도 건드리지 않는다.

**(1) using 추가 — 1번 줄 `using UnityEngine;` 위에 다음 한 줄만 추가:**

```csharp
using System.Collections.Generic;
```

주의: `using System;` 은 추가하지 **말 것**. 추가하면 `System.Random` 과 `UnityEngine.Random` 이 충돌해 CS0104 가 발생한다. `System.Type` / `System.Func` 는 전부 정규화 이름으로 쓴다 (기존 `SpiritCombatState.cs` 도 `System.Func<IAttackStrategy>[]` 로 정규화 표기를 쓰고 있어 프로젝트 스타일과 일치한다).

**(2) 필드/중첩 클래스 추가 — 기존 `private const float MaxAttackDuration = 0.5f;` 줄 바로 아래에 삽입:**

```csharp

    // ─── Phase 7 (D-01): 범용 패턴 후보 평가 헬퍼 ───────────────────────────
    // 파생 State 는 후보 목록만 선언하면 되고, 판단(거리 선택적 조건 + 쿨다운 +
    // 연속사용금지 + 가중치 랜덤)은 이 베이스 클래스가 담당한다.
    protected class PatternCandidate
    {
        public readonly System.Func<IAttackStrategy> Factory;
        public readonly System.Type StrategyType;
        public readonly float Weight;
        public readonly float? MinDistance; // null = 하한 없음 (D-02b: 거리 조건은 선택적)
        public readonly float? MaxDistance; // null = 상한 없음 (D-02b: 거리 조건은 선택적)

        public PatternCandidate(System.Func<IAttackStrategy> factory, float weight,
                                float? minDistance = null, float? maxDistance = null)
        {
            Factory = factory;
            Weight = weight;
            MinDistance = minDistance;
            MaxDistance = maxDistance;
            // D-06a: 게이팅 키를 슬롯 인덱스가 아닌 "전략 타입"으로 1회 확정한다.
            StrategyType = factory().GetType();
        }
    }

    // 직전에 실행한 패턴 타입 — D-05a 연속사용금지 판정 및 D-04 체인 트리거에 사용된다.
    protected System.Type LastUsedPatternType { get; private set; }

    // 패턴별 쿨다운 만료 시각. Execute() 에 early-return 분기가 있어 매 프레임 감산 방식은
    // 조용히 누락될 수 있으므로, WaterMonsterCombatState._lastWaveTime 과 동일하게
    // Time.time 절대 시각 비교를 사용한다.
    private readonly Dictionary<System.Type, float> _patternReadyAt = new Dictionary<System.Type, float>();
```

**(3) `Enter` 에 추적값 초기화 2줄 추가 — 기존 `_attackWaitTimer = 0;` 줄 바로 아래에 삽입:**

```csharp

        // Phase 7: 패턴 추적값 초기화 (기존 SpiritCombatState._patternIndex = 0 과 동일한 세션 단위 리셋)
        LastUsedPatternType = null;
        _patternReadyAt.Clear();
```

**(4) 헬퍼 메서드 3개 추가 — 기존 `protected virtual IAttackStrategy SelectAttackStrategy(...)` 메서드 블록이 끝난 직후, `public virtual void Exit(BossController boss)` 앞에 삽입:**

```csharp

    // D-01/D-03: 조건(거리 선택적 + 연속금지 + 쿨다운)을 통과한 후보 중 가중치 랜덤으로 1개 선택.
    // 통과 후보가 없으면 null — 기존 계약대로 Execute() 가 다음 프레임에 재시도한다.
    protected IAttackStrategy SelectWeightedPattern(float dist, IReadOnlyList<PatternCandidate> candidates)
    {
        if (candidates == null || candidates.Count == 0) return null;

        List<PatternCandidate> eligible = new List<PatternCandidate>();
        float totalWeight = 0f;

        for (int i = 0; i < candidates.Count; i++)
        {
            PatternCandidate c = candidates[i];

            if (c.MinDistance.HasValue && dist < c.MinDistance.Value) continue; // 거리 하한 (D-02b)
            if (c.MaxDistance.HasValue && dist > c.MaxDistance.Value) continue; // 거리 상한 (D-02b)
            if (LastUsedPatternType == c.StrategyType) continue;                // 연속사용금지 (D-05a)
            if (_patternReadyAt.TryGetValue(c.StrategyType, out float readyAt) && Time.time < readyAt) continue; // 쿨다운

            eligible.Add(c);
            totalWeight += c.Weight;
        }

        if (eligible.Count == 0 || totalWeight <= 0f) return null;

        float roll = UnityEngine.Random.Range(0f, totalWeight);
        float cumulative = 0f;
        for (int i = 0; i < eligible.Count; i++)
        {
            cumulative += eligible[i].Weight;
            if (roll <= cumulative) return CommitSelection(eligible[i].Factory());
        }

        // 부동소수 경계 보정: 루프에서 못 골랐어도 null 로 새지 않도록 마지막 후보를 선택한다.
        return CommitSelection(eligible[eligible.Count - 1].Factory());
    }

    // D-04: 강제 체인용 — 후보 평가/거리/쿨다운/연속사용금지를 전부 우회하고 지정 전략을 확정한다.
    protected IAttackStrategy ForceSelectPattern(IAttackStrategy strategy)
    {
        if (strategy == null) return null;
        return CommitSelection(strategy);
    }

    // 선택 확정 처리 — 연속사용금지 기준값과 해당 패턴의 쿨다운 만료 시각을 갱신한다.
    private IAttackStrategy CommitSelection(IAttackStrategy strategy)
    {
        System.Type t = strategy.GetType();
        LastUsedPatternType = t;
        _patternReadyAt[t] = Time.time + strategy.Cooldown;
        return strategy;
    }
```

`UnityEngine.Random.Range` 는 정규화 표기를 유지할 것 (향후 누군가 `using System;` 을 추가해도 깨지지 않도록). `System.Random` 은 사용 금지 — 이 코드베이스는 `UnityEngine.Random` 만 사용한다.
  </action>

  <acceptance_criteria>
    - `grep -c "using System;" Assets/Enemy/NewBoss/Script/States/CombatState.cs` 결과가 `0` 이다 (CS0104 Random 모호성 방지).
    - `grep -q "^using System.Collections.Generic;" Assets/Enemy/NewBoss/Script/States/CombatState.cs` 가 성공한다.
    - 파일에 `protected class PatternCandidate` 가 정확히 1회 존재한다.
    - `PatternCandidate` 에 `public readonly float? MinDistance;` 와 `public readonly float? MaxDistance;` 가 모두 존재한다 (D-02b: 거리 조건 optional).
    - `PatternCandidate` 생성자에 `StrategyType = factory().GetType();` 가 존재한다 (타입 키 게이팅, D-06a).
    - `protected System.Type LastUsedPatternType { get; private set; }` 가 존재한다.
    - `private readonly Dictionary<System.Type, float> _patternReadyAt` 가 존재한다.
    - `protected IAttackStrategy SelectWeightedPattern(float dist, IReadOnlyList<PatternCandidate> candidates)` 시그니처가 정확히 일치한다.
    - `protected IAttackStrategy ForceSelectPattern(IAttackStrategy strategy)` 가 존재한다.
    - `private IAttackStrategy CommitSelection(IAttackStrategy strategy)` 가 존재하고, 그 안에 `LastUsedPatternType = t;` 와 `_patternReadyAt[t] = Time.time + strategy.Cooldown;` 이 모두 있다.
    - `grep -c "UnityEngine.Random.Range" Assets/Enemy/NewBoss/Script/States/CombatState.cs` 결과가 `1` 이고, `grep -c "System.Random" ...` 결과가 `0` 이다.
    - `_patternReadyAt` 접근에 `Time.time` 비교(`Time.time < readyAt`)가 사용되고, `-= Time.deltaTime` 형태의 패턴별 감산 타이머가 **추가되지 않았다** (기존 `_decisionTimer -= Time.deltaTime;` 1줄은 그대로 남아있어야 함 — `grep -c -- "-= Time.deltaTime" ` 결과가 `2` 이하이며 기존 `_decisionTimer`/`_attackWaitTimer` 줄만 해당).
    - `Enter()` 안에 `LastUsedPatternType = null;` 과 `_patternReadyAt.Clear();` 가 존재한다.
    - 기존 `protected virtual IAttackStrategy SelectAttackStrategy(BossController boss, float dist)` 의 본문 3줄(`RangedPokeAttack` / `CanUseHeavyAttack` / `LightAttack`)이 변경되지 않았다 — `git diff` 상 해당 메서드 본문에 변경 라인이 없어야 한다.
    - `git diff --stat -- Assets/Enemy/NewBoss/Script/States/CombatState.cs` 에서 삭제(-) 라인이 0 이다 (순수 추가 변경).
  </acceptance_criteria>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "protected class PatternCandidate" Assets/Enemy/NewBoss/Script/States/CombatState.cs && grep -q "protected IAttackStrategy SelectWeightedPattern(float dist, IReadOnlyList<PatternCandidate> candidates)" Assets/Enemy/NewBoss/Script/States/CombatState.cs && grep -q "protected IAttackStrategy ForceSelectPattern(IAttackStrategy strategy)" Assets/Enemy/NewBoss/Script/States/CombatState.cs && grep -q "private IAttackStrategy CommitSelection(IAttackStrategy strategy)" Assets/Enemy/NewBoss/Script/States/CombatState.cs && grep -q "protected System.Type LastUsedPatternType { get; private set; }" Assets/Enemy/NewBoss/Script/States/CombatState.cs && [ "$(grep -c '^using System;' Assets/Enemy/NewBoss/Script/States/CombatState.cs)" = "0" ] && [ "$(git diff --numstat -- Assets/Enemy/NewBoss/Script/States/CombatState.cs | cut -f2)" = "0" ] && echo TASK1_OK</automated>
  </verify>

  <done>`CombatState.cs` 가 순수 추가(삭제 라인 0)로 `PatternCandidate` 중첩 클래스와 `SelectWeightedPattern` / `ForceSelectPattern` / `CommitSelection` 헬퍼, `LastUsedPatternType` / `_patternReadyAt` 추적 필드를 보유하며, 기존 `SelectAttackStrategy` 기본 구현과 `Execute` 흐름은 그대로다.</done>
</task>

<task type="auto">
  <name>Task 2: SpiritCombatState 를 후보 선언 데이터 레이어로 교체 (D-01b, D-02a, D-03b, D-04, D-06a)</name>

  <read_first>
    - Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs (수정 대상 — 전체 교체 전에 현재 내용 확인)
    - Assets/Enemy/NewBoss/Script/States/CombatState.cs (Task 1 에서 추가한 헬퍼의 정확한 시그니처 확인)
    - Assets/Enemy/WaterSpirit/Script/SpiritController.cs (RepelRange / ChargeRange / AttackRange 상속값 확인)
    - Assets/Enemy/WaterSpirit/Script/States/Attacks/SpiritCharge.cs (Cooldown 3.0, 자가 텔레포트 확인)
    - Assets/Enemy/WaterSpirit/Script/States/Attacks/SpiritExhaustion.cs (Cooldown 2.5, StopMove 만 수행 — 거리 무관 확인)
    - Assets/Enemy/WaterSpirit/Script/States/Attacks/SpiritWakeRepel.cs (Cooldown 1.5, 내부에서 SpiritRepel 호출)
    - Assets/Enemy/WaterSpirit/Script/States/Attacks/SpiritRepel.cs (RepelRange 판정 반경 — maxDistance 근거)
    - Assets/Enemy/WaterSpirit/Script/States/Attacks/SpiritFarProjectile.cs (Cooldown 2.5, 자가 텔레포트 확인)
    - Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs (읽기 전용 — base.SelectAttackStrategy 의 null/non-null 계약 유지 확인용. 수정 금지, D-07)
  </read_first>

  <files>Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs</files>

  <action>
`Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 의 **전체 내용**을 아래로 교체한다. 기존 `_pattern` 정적 배열과 `_patternIndex` 필드는 이번 변경이 직접 orphan 으로 만드는 요소이므로 삭제한다 (CLAUDE.md 3항 "자신의 변경으로 고립된 요소만 제거"). `ShouldTransitionToGroggy => false` 오버라이드는 반드시 그대로 유지한다 (정령 보스는 베리어/그로기가 없음).

```csharp
using System.Collections.Generic;
using UnityEngine;

// Phase 7 (D-01b): 판단 로직은 CombatState 의 범용 헬퍼가 담당하고,
// 이 클래스는 정령 보스의 패턴 후보 목록만 선언하는 얇은 데이터 레이어다.
public class SpiritCombatState : CombatState
{
    // D-03b: 가중치 튜닝 상수 (밸런싱 시 선택 로직을 건드리지 않고 이 값만 조정)
    private const float ChargeWeight = 1.0f;
    private const float ExhaustionWeight = 0.6f;   // D-04 체인으로 2패턴 분량을 소비하므로 낮게 설정
    private const float WakeRepelWeight = 1.0f;
    private const float FarProjectileWeight = 1.0f;

    private List<PatternCandidate> _candidates;
    private bool _noCandidateLogged;

    public override void Enter(BossController boss)
    {
        base.Enter(boss);
        _candidates = (boss is SpiritController spirit) ? BuildCandidates(spirit) : null;
        _noCandidateLogged = false;
        Debug.Log("[SpiritCombatState] 정령 보스 전투 상태 진입");
    }

    protected override bool ShouldTransitionToGroggy(BossController boss) => false;

    protected override IAttackStrategy SelectAttackStrategy(BossController boss, float dist)
    {
        if (!(boss is SpiritController)) return null;
        if (_candidates == null) return null;

        // D-04: 직전 실행 패턴이 SpiritExhaustion 이면 후보 평가를 건너뛰고
        //       SpiritWakeRepel 을 무조건 실행한다 (거리/쿨다운/연속금지 전부 우회 — D-04c > D-05b).
        //       WakeRepel 실행 후에는 LastUsedPatternType 이 갱신되어 일반 판단 풀로 복귀한다(D-04b).
        if (LastUsedPatternType == typeof(SpiritExhaustion))
        {
            _noCandidateLogged = false;
            Debug.Log("[SpiritCombatState] 체인 발동: SpiritExhaustion → SpiritWakeRepel (강제)");
            return ForceSelectPattern(new SpiritWakeRepel());
        }

        IAttackStrategy attack = SelectWeightedPattern(dist, _candidates);

        if (attack == null)
        {
            // 전 후보가 쿨다운/연속금지/거리로 막힌 일시적 상황. 매 프레임 재호출되므로 1회만 로그.
            if (!_noCandidateLogged)
            {
                Debug.Log($"[SpiritCombatState] 사용 가능한 패턴 후보 없음 (거리: {dist:F1}) — 쿨다운 해제까지 대기");
                _noCandidateLogged = true;
            }
            return null;
        }

        _noCandidateLogged = false;
        Debug.Log($"[SpiritCombatState] 패턴 선택: {attack.GetType().Name} (거리: {dist:F1})");
        return attack;
    }

    // D-02a: SpiritCharge / SpiritFarProjectile 은 공격 전 스스로 텔레포트하므로 거리 조건 없음.
    // SpiritExhaustion 도 StopMove 만 수행하고 플레이어 위치를 참조하지 않으므로 거리 조건 없음.
    // 근접형 SpiritWakeRepel(→SpiritRepel) 만 RepelRange 상한을 건다 (D-02b).
    // D-06a: SpiritCharge 는 정확히 후보 1개로만 등록한다 (구 배열의 0/4번 중복 슬롯 제거).
    private List<PatternCandidate> BuildCandidates(SpiritController spirit)
    {
        return new List<PatternCandidate>
        {
            new PatternCandidate(() => new SpiritCharge(), ChargeWeight),
            new PatternCandidate(() => new SpiritExhaustion(), ExhaustionWeight),
            new PatternCandidate(() => new SpiritWakeRepel(), WakeRepelWeight, maxDistance: spirit.RepelRange),
            new PatternCandidate(() => new SpiritFarProjectile(), FarProjectileWeight),
        };
    }
}
```

**절대 하지 말 것:**
- `Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs` 수정 (D-07 — 변경 불필요함이 검증됨)
- `Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` 수정 (D-08b — 명시적 범위 외)
- `SpiritController.ChargeRange` 삭제/수정/재활용 (기존 데드 필드 — 위 "결정 1" 참조)
- 각 전략의 `Cooldown` 값 변경 (RESEARCH.md Pitfall 2: 쿨다운이 코루틴 실행시간과의 유일한 상호배제 가드이므로 낮추면 안 됨)
- 새 공격 패턴 클래스 생성 (D-06b)
  </action>

  <acceptance_criteria>
    - `grep -c "_patternIndex" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 결과가 `0` 이다 (인덱스 장부 완전 제거).
    - `grep -c "System.Func<IAttackStrategy>\[\]" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 결과가 `0` 이다 (라운드로빈 배열 제거).
    - `private List<PatternCandidate> BuildCandidates(SpiritController spirit)` 가 존재한다.
    - `BuildCandidates` 안에 `new PatternCandidate(` 가 정확히 **4회** 등장한다.
    - `grep -c "new SpiritCharge()" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 결과가 `1` 이다 (D-06a: 중복 슬롯 제거).
    - `maxDistance: spirit.RepelRange` 가 정확히 1회 존재하고, 그 줄에 `new SpiritWakeRepel()` 이 함께 있다.
    - `grep -c "minDistance" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 결과가 `0` 이다 (D-02a: Charge/FarProjectile 거리 조건 없음, 결정 1/2 반영).
    - `grep -c "ChargeRange" Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs` 결과가 `0` 이다.
    - `LastUsedPatternType == typeof(SpiritExhaustion)` 분기가 존재하고, 그 분기 안에서 `ForceSelectPattern(new SpiritWakeRepel())` 를 반환한다 (D-04).
    - D-04 체인 분기가 `SelectWeightedPattern(` 호출보다 **앞에** 위치한다 (D-04c: 일반 후보 평가보다 우선).
    - `protected override bool ShouldTransitionToGroggy(BossController boss) => false;` 가 그대로 유지된다.
    - `SelectAttackStrategy` 시그니처가 `protected override IAttackStrategy SelectAttackStrategy(BossController boss, float dist)` 로 변경 없이 유지되고, 조건 불충족 시 `null` 을 반환할 수 있다 (D-07b 계약 유지).
    - `git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs` 가 종료코드 0 (변경 없음, D-07).
    - `git diff --exit-code -- Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs` 가 종료코드 0 (변경 없음, D-08b).
    - `git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/SpiritController.cs` 가 종료코드 0 (ChargeRange 미변경).
    - `git status --porcelain` 에 새로 생성된 `.cs` 파일이 없다 (신규 패턴 클래스 미생성, D-06b).
  </acceptance_criteria>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs && [ "$(grep -c '_patternIndex' $F)" = "0" ] && [ "$(grep -c 'new PatternCandidate(' $F)" = "4" ] && [ "$(grep -c 'new SpiritCharge()' $F)" = "1" ] && [ "$(grep -c 'minDistance' $F)" = "0" ] && grep -q 'maxDistance: spirit.RepelRange' $F && grep -q 'LastUsedPatternType == typeof(SpiritExhaustion)' $F && grep -q 'ForceSelectPattern(new SpiritWakeRepel())' $F && grep -q 'ShouldTransitionToGroggy(BossController boss) => false;' $F && [ "$(grep -n 'typeof(SpiritExhaustion)' $F | cut -d: -f1)" -lt "$(grep -n 'SelectWeightedPattern(dist' $F | cut -d: -f1)" ] && git diff --exit-code -- Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs Assets/Enemy/WaterSpirit/Script/SpiritController.cs && echo TASK2_OK</automated>
  </verify>

  <done>`SpiritCombatState.cs` 가 라운드로빈 배열/인덱스 없이 4개 `PatternCandidate` 만 선언하고, D-04 강제 체인을 일반 판단보다 먼저 처리하며, `Stage2CombatState.cs` / `WaterMonsterCombatState.cs` / `SpiritController.cs` 는 변경되지 않았다.</done>
</task>

</tasks>

<verification>

## 1. 컴파일 검증 (Unity Editor)

Unity 에디터에서 프로젝트를 열고 Console 에 컴파일 에러가 0건인지 확인한다.
자동화 대안 (에디터가 닫혀 있을 때만 가능, 수 분 소요):

```bash
"C:/Program Files/Unity/Hub/Editor/6000.3.10f1/Editor/Unity.exe" \
  -batchmode -quit -nographics \
  -projectPath "C:/Users/chang/Downloads/Projeect_A.E (2) (1)/Projeect_A.E" \
  -logFile - 2>&1 | grep -i "error CS" ; echo "exit=$?"
```

`error CS` 매칭이 하나도 없으면 통과. 특히 다음 3가지를 확인:
- CS0104 (`Random` 모호) → `using System;` 이 잘못 추가되었다는 뜻
- CS0122 (`LastUsedPatternType` 접근 불가) → protected 지정 누락
- CS0246 (`Dictionary` / `IReadOnlyList` 미발견) → `using System.Collections.Generic;` 누락

## 2. 범위 격리 검증

```bash
git status --porcelain -- Assets/
```

`Assets/` 하위 변경 파일이 정확히 다음 2개(+ .meta 없음)여야 한다:
- `Assets/Enemy/NewBoss/Script/States/CombatState.cs`
- `Assets/Enemy/WaterSpirit/Script/States/SpiritCombatState.cs`

## 3. D-07 호환성 재확인 (코드 변경 없음, 읽기만)

`Stage2CombatState.SelectAttackStrategy` 가 `SpiritCombatState` 로부터 사용하는 것은 오직 반환값의 null/non-null 여부뿐임을 재확인한다:

```bash
grep -n "base.SelectAttackStrategy\|_patternsExecuted" Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs
```

기대: `base.SelectAttackStrategy(boss, dist)` 1회 호출, `if (strategy != null) _patternsExecuted++` 유지. 어떤 전략 타입인지 검사하는 코드가 없어야 한다.

**알려진/수용된 상호작용 (SUMMARY 에 기록할 것):** Stage 2 에서 `SpiritExhaustion` 이 3번째 일반 패턴으로 실행되면, 다음 호출은 `_patternsExecuted >= 3` 조건이 먼저 걸려 헤비콤보로 진입하므로 D-04 체인(WakeRepel 강제)이 그 사이클에서는 선점된다. 이는 D-07a("Stage2 카운터 로직 그대로 유지")를 지키기 위한 필연적 귀결이며 의도된 동작으로 수용한다.

## 4. 신규 후보 고갈 동작 확인 (정적 추론)

거리 조건 없는 후보가 3개(Charge/Exhaustion/FarProjectile) 이므로, 어떤 거리에서도 최소 3개 후보가 거리 게이트를 통과한다. 연속금지로 최대 1개가 빠지므로 후보가 0이 되려면 남은 2개가 동시에 쿨다운이어야 하며, 이는 짧은 순간에 그치고 자동 해소된다 (RESEARCH.md Pitfall 1 완화 확인).

</verification>

<success_criteria>

- [ ] `CombatState.cs` 에 `PatternCandidate` / `SelectWeightedPattern` / `ForceSelectPattern` / `CommitSelection` / `LastUsedPatternType` / `_patternReadyAt` 가 존재한다 (D-01a)
- [ ] `PatternCandidate` 의 거리 조건이 `float?` 로 선택적이다 (D-02b)
- [ ] 선택은 조건 통과 후보들 중 누적 가중치 룰렛 랜덤이다 (D-03a)
- [ ] `SpiritCharge` / `SpiritFarProjectile` / `SpiritExhaustion` 후보에 거리 조건이 없다 (D-02a + 플래너 결정 1·2)
- [ ] `SpiritWakeRepel` 후보에만 `maxDistance: spirit.RepelRange` 가 있다 (D-02b)
- [ ] `LastUsedPatternType == typeof(SpiritExhaustion)` 일 때 무조건 `SpiritWakeRepel` 이 반환되고, 이 분기가 후보 평가보다 앞에 있다 (D-04a/c)
- [ ] 강제 체인은 쿨다운/연속금지/거리를 전부 우회한다 (`ForceSelectPattern` 경로, D-04a, D-05b)
- [ ] 체인 이후 `LastUsedPatternType` 이 `SpiritWakeRepel` 로 갱신되어 일반 풀로 복귀한다 (D-04b)
- [ ] `SelectWeightedPattern` 이 `LastUsedPatternType == c.StrategyType` 후보를 제외한다 (D-05a)
- [ ] `SpiritCharge` 후보가 정확히 1개이며 `_pattern` / `_patternIndex` 가 완전히 제거되었다 (D-06a)
- [ ] `Stage2CombatState.cs` 가 변경되지 않았고 `_patternsExecuted` 계약이 유지된다 (D-07)
- [ ] `WaterMonsterCombatState.cs` 와 `SpiritController.cs` 가 변경되지 않았다 (D-08b + 플래너 결정 1)
- [ ] Unity Console 컴파일 에러 0건
- [ ] `Assets/` 하위 변경 파일이 정확히 2개

</success_criteria>

<output>
완료 후 `.planning/phases/07-boss-attack-pattern-judgment/07-01-SUMMARY.md` 를 작성한다.

반드시 기록할 항목:
- 플래너 결정 1(ChargeRange 미재활용) 과 결정 2(Exhaustion 거리 조건 없음) 의 최종 적용 결과
- 확정한 가중치 값 (Charge 1.0 / Exhaustion 0.6 / WakeRepel 1.0 / FarProjectile 1.0) 과 튜닝 위치(`SpiritCombatState` 상수)
- `<verification>` 3번의 "알려진/수용된 상호작용" (Stage 2 헤비콤보가 D-04 체인을 선점하는 경우)
- 후속 페이즈 후보: `WaterMonsterCombatState` 를 동일 헬퍼로 마이그레이션 (CONTEXT.md Deferred)
</output>
