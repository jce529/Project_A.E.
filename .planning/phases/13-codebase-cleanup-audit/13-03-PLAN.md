---
phase: 13-codebase-cleanup-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-08, D-10]

must_haves:
  truths:
    - "Assets/Player, Assets/Camera, Assets/SaveSystem 의 32개 .cs 파일 전부가 스캔 커버리지 표에 나열된다"
    - "Phase 9~12에서 반복 수정되고 Play 모드로 검증된 파일(CameraController.cs, PlayerStats.cs, PlayerController.cs, InputHandler.cs, SaveLoadManager.cs 등)의 항목은 고위험 하위 섹션에 분리 기재된다"
    - "InputHandler.cs 와 PlayerInputHandler.cs 의 중복/사용 여부가 D-07 또는 D-09 후보로 판정되어 기재된다"
    - "이 범위의 CP949 인코딩 파일 4개가 별도 섹션에 나열된다 (D-04)"
    - "소스 .cs 파일은 단 한 줄도 수정되지 않는다"
  artifacts:
    - path: ".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md"
      provides: "Scope C 발견 항목 fragment"
      contains: "## D-07 — 죽은 코드"
      min_lines: 80
  key_links:
    - from: "13-FINDINGS-C-player-camera-savesystem.md"
      to: "Assets/Player/Script/PlayerInputHandler.cs"
      via: "D-07/D-09 판정 (InputHandler.cs 와의 중복·사용 여부)"
      pattern: "PlayerInputHandler"
---

<objective>
Scope C(`Assets/Player/**` + `Assets/Camera/**` + `Assets/SaveSystem/**`, 32개 .cs / 3359줄)를 대상으로
죽은 코드(D-07) / 디버그·주석 잔재(D-08) / 과도한 복잡도(D-10)를 스캔해 **보고서 fragment 한 개**를 만든다.

Purpose: 이 범위는 프로젝트에서 **가장 최근에 가장 많이 수정된** 코드다 — Phase 9~12 + quick task
260804-q6h / 260805-m41 / 260805-q2u / 260809-h9k 가 전부 `CameraController.cs`(470줄, 프로젝트 최장 파일)에
누적되었고, `PlayerStats.cs` 는 Phase 11(RestoreStats)과 Phase 12(카메라 흔들림 트리거)를 동시에 받았다.
따라서 데드 코드로 보이는 것 중 상당수가 **의도적으로 남긴 확장점**일 수 있으므로 위험 티어링이 특히 중요하다.
Output: `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md`

**보고서 전용 phase다. `Assets/` 아래 어떤 파일도 생성/수정/삭제하지 않는다 (CONTEXT.md D-01/D-02).**
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md

<scan_scope>
정확히 이 32개 파일 (`find Assets/Player Assets/Camera Assets/SaveSystem -name "*.cs" -type f | sort`):
```
Assets/Camera/Script/CameraBoundsTrigger.cs
Assets/Camera/Script/CameraController.cs
Assets/Camera/Script/CameraZoomTrigger.cs
Assets/Player/Animation/PlayerIdle.cs
Assets/Player/Script/AttackBox.cs
Assets/Player/Script/GameStateManager.cs
Assets/Player/Script/InputHandler.cs
Assets/Player/Script/LadderMovement.cs
Assets/Player/Script/Menu/ControlsSettingsPanel.cs
Assets/Player/Script/Menu/GameSettingsPanel.cs
Assets/Player/Script/Menu/GraphicsSettingsPanel.cs
Assets/Player/Script/Menu/PauseMenu.cs
Assets/Player/Script/Menu/PauseMenuTabController.cs
Assets/Player/Script/Menu/SoundSettingsPanel.cs
Assets/Player/Script/PlayerAnimator.cs
Assets/Player/Script/PlayerAttack.cs
Assets/Player/Script/PlayerAttackBase.cs
Assets/Player/Script/PlayerAttackDamager.cs
Assets/Player/Script/PlayerController.cs
Assets/Player/Script/PlayerInputHandler.cs
Assets/Player/Script/PlayerSpeedDisplay.cs
Assets/Player/Script/PlayerStats.cs
Assets/Player/Script/PrisonDebuff.cs
Assets/Player/Script/SkillScript/FlashSlice.cs
Assets/Player/Script/SkillScript/WaveSlice.cs
Assets/Player/Script/UI/InGameHUDController.cs
Assets/Player/Script/UI/PlayerHealthUI.cs
Assets/Player/Script/UI/PlayerUI.cs
Assets/Player/Script/UI/PlayerWaterUI.cs
Assets/Player/Script/WaterController.cs
Assets/SaveSystem/Script/SaveData.cs
Assets/SaveSystem/Script/SaveLoadManager.cs
```
</scan_scope>

<risk_tier_rule>
**고위험 하위 섹션** 에 넣을 파일 (Phase 9~12 및 quick task에서 수정되었거나 그 동작에 직접 의존하는 파일):
```
Assets/Camera/Script/CameraController.cs
Assets/Camera/Script/CameraBoundsTrigger.cs
Assets/Camera/Script/CameraZoomTrigger.cs
Assets/SaveSystem/Script/SaveLoadManager.cs
Assets/SaveSystem/Script/SaveData.cs
Assets/Player/Script/PlayerStats.cs
Assets/Player/Script/PlayerController.cs
Assets/Player/Script/InputHandler.cs
```
근거(STATE.md Key Decisions / ROADMAP Phase 9~12):
- `CameraController.cs` 는 Phase 9(줌/클램프) → Phase 10(데드존 3종) → 260804-q6h(Y 데드존) →
  260805-m41/q2u/260809-h9k(구역 경계·Y 경계·Exit/Enter 경쟁상태) → Phase 12(Hit Shake) 가 전부 누적된 파일이며,
  사용자와 함께 Play 모드 실측 검증까지 마쳤다. `LateUpdate` 의 실행 **순서**(위치추종/데드존 → 줌 Lerp →
  경계 클램프 → 데드존 재앵커 → Hit Shake)가 계약이다.
- `PlayerController.cs` / `InputHandler.cs` 는 Phase 10에서 **0줄 변경**이 명시적 성공 기준이었다
  (`isDashing`/`isKnockedBack` 접근자 추가 금지). 여기 있는 private 멤버가 "안 쓰여 보인다"고 지우면 안 된다.
- `PlayerStats.cs` 는 Phase 11 `RestoreStats(float,float,float)` (additive 전용 유일 외부 쓰기 경로) +
  Phase 12 `TakeDamage` → `CameraController.Instance.Shake()` 트리거를 받았다.
- `SaveLoadManager.cs` / `SaveData.cs` 는 Phase 11 산출물이며, `MapGimmickState` / `Items` 는
  **스키마만 존재하고 채우는 코드가 없는 의도된 스텁**이다 (CONTEXT.md 참조 `Assets/SaveSystem/Check.md`, D-03/D-03b).
  이 두 멤버를 "미사용 = 데드"로 보고하면 오판이다 — 보고하되 Reason에 `의도된 확장 스텁 (Phase 11 D-03/D-03b) — 삭제 권장 아님` 을 적는다.

**일반 하위 섹션** 에 넣을 파일: 위 8개를 제외한 나머지 24개 전부.
</risk_tier_rule>

<false_positive_rules>
1. **Unity 매직 메서드** — 절대 D-07로 보고 금지:
   `Awake` `OnEnable` `Start` `FixedUpdate` `Update` `LateUpdate` `OnDisable` `OnDestroy`
   `OnTriggerEnter2D` `OnTriggerExit2D` `OnTriggerStay2D` `OnCollisionEnter2D` `OnCollisionExit2D`
   `OnCollisionStay2D` `OnDrawGizmos` `OnDrawGizmosSelected` `OnGUI` `OnValidate` `Reset`
   `OnApplicationQuit` `OnBecameVisible` `OnBecameInvisible` `OnAnimatorMove`
   (`CameraController.OnDrawGizmos` 는 데드존 박스 Gizmo — Phase 10 산출물이다.)

2. **Inspector 직렬화 필드** — `Player.prefab` / 각 씬에서 값이 주입된다. 필수 확인:
   ```bash
   grep -rl "<심볼명>" Assets --include="*.unity" --include="*.prefab" --include="*.asset" 2>/dev/null
   ```
   이 범위는 UI 패널·메뉴가 많아 Inspector 참조 비율이 특히 높다.

3. **UnityEvent / Button.onClick 바인딩** — `Menu/*.cs` 의 `public void OnClickXxx()` 류는 씬의
   Button `m_OnClick` 에 문자열로 바인딩된다:
   ```bash
   grep -rn "<메서드명>" Assets --include="*.unity" --include="*.prefab" 2>/dev/null
   ```
   결과가 있으면 D-07로 보고하지 않는다.

4. **애니메이션 이벤트** — `PlayerAnimator.cs` / `PlayerAttack.cs` 의 메서드는 `.anim`/`.controller` 에서
   문자열로 호출될 수 있다:
   ```bash
   grep -rl "<심볼명>" Assets --include="*.anim" --include="*.controller" 2>/dev/null
   ```
   (`Assets/idle.anim`, `Assets/Unit.controller`, `Assets/Targets.controller`, `Assets/Rarm_solver_Target.controller` 존재)

5. **속성 기반 진입점** — `[ContextMenu]` `[RuntimeInitializeOnLoadMethod]` `[MenuItem]`.
   `SaveLoadManager` 는 `[RuntimeInitializeOnLoadMethod]` 부트스트랩과 `[ContextMenu]` 검증 훅을 쓴다.

6. **문서화된 의도적 스텁/유지 (보고하되 "삭제 권장 아님" 명시)**:
   - `SaveData` 의 `MapGimmickState` / `Items` — Phase 11 D-03/D-03b 의도된 스텁
   - `CameraController` 의 Phase 9 레거시 `Vector3.Lerp` 추종 경로 — 보스 구역 전용 분기로 살아있다 (Phase 10 D-15)
</false_positive_rules>

<encoding_hazard>
이 범위의 CP949(비-UTF-8) 파일은 정확히 4개다:
```
Assets/Player/Script/PlayerAnimator.cs
Assets/Player/Script/PlayerSpeedDisplay.cs
Assets/Player/Script/UI/PlayerUI.cs
Assets/Player/Script/WaterController.cs
```
`Assets/Camera/**` 와 `Assets/SaveSystem/**` 에는 CP949 파일이 0개다.
단, `CameraController.cs` 는 UTF-8이지만 **비-ASCII(한글) 주석 라인이 5줄** 존재하며 Phase 10/12에서
"비-ASCII 라인 수 5 유지"가 회귀 게이트였다 — 이 사실을 CP949 섹션 하단 비고에 적는다.

Read 툴로 CP949 파일을 읽으면 한글 주석이 깨져 보이지만 **줄번호와 ASCII 코드는 정확하다**.
절대 Edit/Write 하지 않는다.
</encoding_hazard>

<fragment_schema>
Plan 02와 동일 스키마. 항목 ID 접두사는 `C-`. 제목은
`# Phase 13 Audit — Findings C: Player / Camera / SaveSystem`.
헤더 메타: `**Files scanned:** 32`, `**Risk tier:** 혼합 — Phase 9~12 수정 파일 8종 = 고위험, 나머지 24종 = 일반`.
필수 헤딩: `## D-07 — 죽은 코드`, `### D-07 일반 항목`, `### D-07 회귀 위험 높음 — 신중 검토 필요`,
`## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드`, `### D-08 일반 항목`, `### D-08 회귀 위험 높음 — 신중 검토 필요`,
`## D-10 — 과도하게 긴/복잡한 함수 (권장)`, `### D-10 일반 항목`, `### D-10 회귀 위험 높음 — 신중 검토 필요`,
`## D-04 — CP949 인코딩 위험 파일`, `## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)`,
`## 스캔 커버리지`, `## 요약`.
항목이 없는 하위 섹션에는 `(없음)` 한 줄.
</fragment_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fragment 스켈레톤 + CP949 섹션 + D-07 죽은 코드 스캔</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-04~D-07 잠금 결정
    - `.planning/STATE.md` — `### Key Decisions` 전체 (특히 Phase 9~12 + quick task 260804/260805/260809 항목)
    - `.planning/ROADMAP.md` — Phase 9 / 10 / 11 / 12 의 `Success Criteria` 절 (무엇이 계약인지)
    - `Assets/SaveSystem/Check.md` `## 섹션 6) 알려진 한계 / 범위 밖` (MapGimmickState/Items 스텁 근거)
    - `<scan_scope>` 의 32개 .cs 파일 전부
  </read_first>

  <action>
    1. `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md` 를
       `<fragment_schema>` 의 13개 헤딩 전부와 헤더 메타 4줄(`**Files scanned:** 32` 포함)로 새로 만든다.
       Task 2/3이 채울 섹션은 `(없음)` 으로 둔다.

    2. `## D-04 — CP949 인코딩 위험 파일` 표를 지금 채운다. `<encoding_hazard>` 의 4개 경로를
       `| # | File | 비고 |` 로 1~4행 기록하고, 표 위에 다음 문장을 넣는다:
       `> 이 파일들은 CP949 인코딩이다. 실제 수정 단계에서는 표준 Read/Edit 왕복이 비-ASCII 바이트를 U+FFFD로 훼손시키므로 \`git show HEAD:<path>\` + 순수 바이트 스크립트 프로토콜이 필요하다 (CONTEXT.md D-04, STATE.md Phase 11 Plan 3 기록).`
       표 아래에 다음 한 줄을 추가한다:
       `> \`Assets/Camera/Script/CameraController.cs\` 는 UTF-8이지만 비-ASCII(한글) 주석 라인이 5줄 존재하며, Phase 10/12에서 "비-ASCII 라인 수 5 유지"가 회귀 게이트였다 — 수정 시 동일 게이트를 재적용할 것.`

    3. D-07 스캔:
       a. `grep -nE '^\s*(public|private|protected|internal|static|readonly|const|\[SerializeField\])' <file>`
       b. `grep -rown --include="*.cs" "\b<심볼명>\b" Assets | wc -l` — 결과 1이면 데드 후보
       c. `<false_positive_rules>` 6개 규칙으로 거른다. 규칙 2/3/4는 실제로
          `.unity` `.prefab` `.asset` `.anim` `.controller` grep을 돌려 확인한다.
       d. `<risk_tier_rule>` 의 8개 고위험 파일 목록에 따라 일반/고위험 표로 나눠 기록한다.

    4. **필수 판정 항목** — 아래 3건은 결과가 무엇이든 반드시 fragment에 흔적을 남긴다:
       - `Assets/Player/Script/PlayerInputHandler.cs` 와 `Assets/Player/Script/InputHandler.cs`:
         두 파일 각각의 클래스가 프로젝트/씬/프리팹에서 실제로 참조되는지 확인한다.
         한쪽이 참조 0건이면 D-07 표에 행을 추가하고, 둘 다 살아있는데 로직이 겹치면
         `## D-09 후보 관찰` 에 불릿으로 적는다.
       - `SaveData` 의 `MapGimmickState` / `Items` 멤버: D-07 **고위험** 표에 넣되 Reason에
         `의도된 확장 스텁 (Phase 11 D-03/D-03b) — 삭제 권장 아님` 을 적는다.
       - `Assets/Player/Script/UI/` 4종(`InGameHUDController` `PlayerHealthUI` `PlayerUI` `PlayerWaterUI`)
         각각이 씬/프리팹에서 실제로 붙어 있는지 확인해, 참조 0건이면 D-07 일반 표에 기록한다.

    5. 추가 D-07 유형: 주석 처리된 코드 블록(`grep -nE '^\s*//.*[;{}]' <file>` —
       기준 확인 지점: `SaveLoadManager.cs` 3건, `CameraController.cs` 3건, `PlayerAttack.cs` 2건,
       `CameraBoundsTrigger.cs` 1건), 사용되지 않는 using, 참조 0건 클래스 전체.

    **금지:** `Assets/` 아래 어떤 파일도 Write/Edit 하지 않는다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md"; test -f "$F" || exit 1; for h in "## D-07 — 죽은 코드" "### D-07 일반 항목" "### D-07 회귀 위험 높음 — 신중 검토 필요" "## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드" "### D-08 일반 항목" "### D-08 회귀 위험 높음 — 신중 검토 필요" "## D-10 — 과도하게 긴/복잡한 함수 (권장)" "### D-10 일반 항목" "### D-10 회귀 위험 높음 — 신중 검토 필요" "## D-04 — CP949 인코딩 위험 파일" "## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)" "## 스캔 커버리지" "## 요약"; do grep -qF "$h" "$F" || { echo "MISSING: $h"; exit 1; }; done; for f in Assets/Player/Script/PlayerAnimator.cs Assets/Player/Script/PlayerSpeedDisplay.cs Assets/Player/Script/UI/PlayerUI.cs Assets/Player/Script/WaterController.cs; do grep -qF "$f" "$F" || { echo "CP949 UNLISTED: $f"; exit 1; }; done; grep -qF "PlayerInputHandler" "$F" || { echo "MISSING PlayerInputHandler verdict"; exit 1; }; grep -qF "의도된 확장 스텁 (Phase 11 D-03/D-03b) — 삭제 권장 아님" "$F" || { echo "MISSING stub note"; exit 1; }; echo "D-07 rows: $(grep -c '^| C-D07-' "$F")"; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - fragment 파일이 존재하고 verify의 **13개 헤딩 문자열**이 전부 존재한다.
    - `grep -c "^\*\*Files scanned:\*\* 32$" <fragment>` == 1
    - CP949 4개 경로(`PlayerAnimator.cs` `PlayerSpeedDisplay.cs` `UI/PlayerUI.cs` `WaterController.cs`)가 전부 fragment에 존재한다.
    - `grep -c "> 이 파일들은 CP949 인코딩이다" <fragment>` == 1
    - `grep -c "비-ASCII 라인 수 5 유지" <fragment>` == 1
    - `grep -c "^| C-D07-" <fragment>` >= 6
    - `grep -cP '^\| C-D07-\d{2} \| Assets/(Player|Camera|SaveSystem)/[^|]+\.cs \| \d' <fragment>` 가 `grep -c "^| C-D07-" <fragment>` 와 같다.
    - `grep -c "PlayerInputHandler" <fragment>` >= 1 (D-07 행이든 D-09 후보 불릿이든 판정 흔적이 있다)
    - `grep -c "의도된 확장 스텁 (Phase 11 D-03/D-03b) — 삭제 권장 아님" <fragment>` >= 1
    - 고위험 D-07 표의 모든 행 경로가 `<risk_tier_rule>` 의 8개 파일 목록 안에 있다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>fragment가 13개 헤딩 + CP949 4행 표 + D-07 6행 이상(위험 티어 분리)을 갖추고, PlayerInputHandler 판정과 SaveData 스텁 예외 표기가 있으며, `Assets/` 는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: D-08 디버그/주석 잔재 스캔 (위험 티어 분리)</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md` — Task 1 결과
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-08 정의
    - `grep -rln --include="*.cs" "Debug\.Log" Assets/Player Assets/Camera Assets/SaveSystem` 결과 파일 전부 (기준: 13개 파일)
  </read_first>

  <action>
    1. **Debug 로그 전수 수집** (기준값: Player 31건 + Camera 0건 + SaveSystem 11건 = 42건, 13개 파일):
       ```bash
       grep -rn --include="*.cs" "Debug\.Log\|Debug\.LogWarning\|Debug\.LogError" Assets/Player Assets/Camera Assets/SaveSystem
       ```
       **파일 단위로 1행씩** 기록한다 (여러 줄은 Line(s) 칼럼에 쉼표 나열). Kind = `Debug.Log` / `Debug.LogWarning` / `Debug.LogError`.
       Reason에는 `개발용 상태 추적 — 제거 권장` 또는 `오류 진단용 — 유지 권장` 판단을 적는다.
       `SaveLoadManager.cs` 의 로그는 저장/로드 실패 진단 성격이 강하므로 개별로 판단해서 적는다.
       `Assets/Camera/` 는 `Debug.Log` 가 **0건**이다 — 표 아래에
       `> \`Assets/Camera/**\` 는 Debug.Log 0건 (Phase 9~12 내내 로그를 넣지 않은 상태 유지).` 한 줄을 적는다.

    2. **TODO/FIXME/HACK 주석**:
       ```bash
       grep -rni --include="*.cs" "TODO\|FIXME\|HACK\|XXX" Assets/Player Assets/Camera Assets/SaveSystem
       ```
       프로젝트 전체 기준값 0건. 이 범위도 0건이면
       `> TODO/FIXME/HACK 주석: 이 범위에서 0건 (프로젝트 전체도 0건).` 한 줄을 적는다.

    3. **주석 처리된 코드**: Task 1에서 찾은 것 중 디버그/테스트 성격인 것을 Kind=`주석처리 코드` 로
       D-08 표에도 행을 추가한다. 기준 확인 지점: `SaveLoadManager.cs`(3건), `CameraController.cs`(3건),
       `PlayerAttack.cs`(2건), `CameraBoundsTrigger.cs`(1건).
       `CameraController.cs` / `CameraBoundsTrigger.cs` 항목은 반드시 **고위험** 표에 넣는다.

    4. 항목을 `<risk_tier_rule>` 의 8개 고위험 파일 목록에 따라 일반/고위험 표로 나눈다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md"; N=$(grep -c "^| C-D08-" "$F"); echo "D-08 rows: $N"; test "$N" -ge 13 || { echo "FAIL: too few D-08 rows"; exit 1; }; MISS=0; for f in $(grep -rl --include="*.cs" "Debug\.Log" Assets/Player Assets/Camera Assets/SaveSystem); do grep -qF "$f" "$F" || { echo "UNCOVERED: $f"; MISS=1; }; done; test "$MISS" -eq 0 && echo "ALL Debug.Log FILES COVERED"; grep -qF "Assets/Camera/**\` 는 Debug.Log 0건" "$F" && echo "camera-zero-note OK"; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^| C-D08-" <fragment>` >= 13
    - `Debug.Log` 를 포함하는 **13개 파일 경로 전부**가 fragment 안에 존재한다 (verify의 `ALL Debug.Log FILES COVERED`).
    - `grep -cP '^\| C-D08-\d{2} \| Assets/(Player|Camera|SaveSystem)/[^|]+\.cs \| \d' <fragment>` 가 `grep -c "^| C-D08-" <fragment>` 와 같다.
    - `grep -c "는 Debug.Log 0건" <fragment>` == 1
    - `grep -c "TODO/FIXME/HACK 주석: 이 범위에서 0건" <fragment>` == 1 또는 `grep -c "| TODO |" <fragment>` >= 1
    - `grep -c "CameraController.cs.*주석처리 코드" <fragment>` >= 1 이고 그 행이 `### D-08 회귀 위험 높음 — 신중 검토 필요` 섹션 안에 있다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>D-08 표가 13행 이상이고 위험 티어별로 분리되어 있으며, Debug.Log 를 가진 13개 파일이 전부 등장하고 Camera 0건 사실이 명시되어 있다.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 복잡도 + D-09 후보 + 커버리지 표 + 요약</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md` — Task 1/2 결과
    - `Assets/Camera/Script/CameraController.cs` (470줄 — 프로젝트 최장 파일, `LateUpdate` 실행 순서가 계약)
    - `Assets/Player/Script/PlayerAttack.cs` (375줄), `Assets/Player/Script/PlayerController.cs` (298줄), `Assets/SaveSystem/Script/SaveLoadManager.cs` (263줄), `Assets/Player/Script/InputHandler.cs` (197줄)
    - `.planning/ROADMAP.md` Phase 10 / Phase 12 `Success Criteria` (LateUpdate 순서 계약 원문)
  </read_first>

  <action>
    1. **D-10 복잡도 스캔.** 판정 기준(둘 중 하나라도 초과):
       - 메서드 본문 **40줄 초과**
       - 메서드 내 분기 토큰 합계 **15 초과** (`if` `else if` `for` `foreach` `while` `switch` `case` `&&` `||` `?:`)
       `<read_first>` 의 5개 장문 파일을 우선 검사하고 나머지도 확인:
       ```bash
       find Assets/Player Assets/Camera Assets/SaveSystem -name "*.cs" -exec wc -l {} + | sort -rn
       ```
       Metric 칼럼에 `{본문 줄수}줄 / 분기 {N}`, Line(s) 칼럼에 `{시작줄}-{끝줄}` 을 적는다.
       `## D-10 — 과도하게 긴/복잡한 함수 (권장)` 헤딩 바로 아래에
       `> D-10은 권장 수준 관찰이다. 실제 수정은 사용자 승인 필수 (CONTEXT.md D-10).` 한 줄을 넣는다.

       **`CameraController.LateUpdate` 는 먼저 실측한다** (본문 줄수 + 분기 토큰 수를 위 기준으로 정확히 센다).
       - 실측값이 기준(40줄 초과 또는 분기 15 초과)을 **실제로 넘으면** 다른 항목과 동일한 `C-D10-nn` 표 형식으로
         고위험 표에 1행 추가하고, 실측 Metric 값을 그대로 적는다.
       - 기준을 **넘지 않으면** 표 행으로 넣지 않는다. 대신 표 아래에 별도 각주로
         (표와 같은 `Metric` 칼럼 형식을 쓰지 않는 산문 문장) 다음을 그대로 포함한다:
         `> 참고 — CameraController.LateUpdate 는 D-10 수치 기준을 충족하지 않지만(실측: {줄수}줄 / 분기 {N}),
         Phase 9~12가 누적된 파이프라인 순서 계약이 있다: LateUpdate 실행 순서(위치추종/데드존 → 줌 Lerp →
         경계 클램프 → 데드존 재앵커 → Hit Shake)가 계약이므로 분해 시 순서 보존 필수. 수치 기준이 아닌
         설계 취약성 판단이므로 다른 D-10 항목과 구분해 표기함.`
       두 경우 모두 "LateUpdate 실행 순서(위치추종/데드존 → 줌 Lerp → 경계 클램프 → 데드존 재앵커 → Hit Shake)가
       계약이므로 분해 시 순서 보존 필수" 문장은 fragment 어딘가에 (표 행이든 각주든) 정확히 1회 등장해야 한다.

    2. **D-09 후보 관찰.** 각 불릿에 `파일경로:줄번호` 를 2곳 이상 포함한다. 반드시 확인할 후보:
       - `Assets/Player/Script/InputHandler.cs` vs `Assets/Player/Script/PlayerInputHandler.cs`
       - `Assets/Player/Script/PlayerAttack.cs` / `PlayerAttackBase.cs` / `PlayerAttackDamager.cs` / `AttackBox.cs` 4종의 책임 경계와 중복
       - `Assets/Player/Script/UI/` 4종(`InGameHUDController` `PlayerHealthUI` `PlayerUI` `PlayerWaterUI`)의 HP/수분 바 갱신 로직 중복
       - `Assets/Player/Script/Menu/` 6종 설정 패널의 공통 저장/로드 골격
       - `Assets/Camera/Script/CameraBoundsTrigger.cs` vs `CameraZoomTrigger.cs` 의 `OnTriggerEnter2D`/`OnTriggerExit2D` 골격

       **다음은 문서화된 의도적 설계이므로 후보로 올리지 않는다:**
       - `CameraZoomTrigger` 가 필드 0개인 것 (Phase 9 D-04/D-05 — 줌 값은 CameraController Inspector 소유,
         트리거는 어느 보스 구역에나 드롭 가능해야 함)
       - `CameraController` 의 보스존 레거시 Lerp 경로와 일반 스테이지 데드존 경로가 분리된 것 (Phase 10 D-15)
       - `SaveLoadManager` 가 async/await 대신 코루틴을 쓰는 것 (프로젝트 무-async 컨벤션)
       후보가 없으면 `- (없음)`.

    3. **커버리지 표.** `## 스캔 커버리지` 아래 `| # | File | Lines | Risk | Scanned |` 표에
       `<scan_scope>` 의 **32개 파일 전부**를 1~32행으로 기록한다. Risk 칼럼은 `<risk_tier_rule>` 의
       8개 목록에 있으면 `고위험`, 아니면 `일반`. Scanned는 `yes`.

    4. **요약 표.** `## 요약` 아래 `| 카테고리 | 항목 수 | 고위험 항목 수 |` 표를 실제 행 수와 일치하게 채운다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md"; C=$(grep -cP '^\| \d+ \| Assets/(Player|Camera|SaveSystem)/[^|]+\.cs \| \d+ \| (고위험|일반) \| yes \|' "$F"); echo "coverage rows: $C"; test "$C" -eq 32 || { echo "FAIL: expected 32"; exit 1; }; MISS=0; for f in $(find Assets/Player Assets/Camera Assets/SaveSystem -name "*.cs" -type f | sort); do grep -qF "$f" "$F" || { echo "UNCOVERED: $f"; MISS=1; }; done; test "$MISS" -eq 0 || exit 1; H=$(grep -cP '^\| \d+ \| Assets/[^|]+\.cs \| \d+ \| 고위험 \| yes \|' "$F"); echo "high-risk rows: $H"; test "$H" -eq 8 || { echo "FAIL: expected 8 high-risk"; exit 1; }; test $(grep -c "^| C-D10-" "$F") -ge 4 || exit 1; grep -qF "LateUpdate 실행 순서(위치추종/데드존 → 줌 Lerp → 경계 클램프 → 데드존 재앵커 → Hit Shake)가 계약이므로 분해 시 순서 보존 필수" "$F" && echo "LateUpdate note OK"; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - 커버리지 표 행이 정확히 **32개**이고 정규식 `^\| \d+ \| Assets/(Player|Camera|SaveSystem)/[^|]+\.cs \| \d+ \| (고위험|일반) \| yes \|` 에 전부 매치한다.
    - 32개 파일 경로가 전부 fragment 안에 존재한다 (누락 0).
    - 커버리지 표의 `고위험` 행이 정확히 **8개**이고, 그 8개 경로가 `<risk_tier_rule>` 목록과 정확히 일치한다.
    - `grep -c "^| C-D10-" <fragment>` >= 4
    - `grep -cP '^\| C-D10-\d{2} \| Assets/[^|]+\.cs \| \d+-\d+ \|' <fragment>` 가 `grep -c "^| C-D10-" <fragment>` 와 같다.
    - `grep -c "LateUpdate 실행 순서(위치추종/데드존 → 줌 Lerp → 경계 클램프 → 데드존 재앵커 → Hit Shake)가 계약이므로 분해 시 순서 보존 필수" <fragment>` == 1
    - `grep -c "> D-10은 권장 수준 관찰이다. 실제 수정은 사용자 승인 필수 (CONTEXT.md D-10)." <fragment>` == 1
    - `## D-09 후보 관찰` 에 불릿이 3개 이상이거나 `- (없음)` 한 줄이 있다.
    - `## 요약` 표의 항목 수가 `grep -c "^| C-D07-"` / `"^| C-D08-"` / `"^| C-D10-"` 결과와 일치한다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>fragment가 D-07/D-08/D-10 표 + CP949 4행 + D-09 후보 + 32행 커버리지 표(고위험 정확히 8행) + 요약 표를 모두 갖추고, `Assets/` 는 0줄 변경이다.</done>
</task>

</tasks>

<verification>
```bash
F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md"
test -f "$F" && test $(wc -l < "$F") -ge 80
grep -cP '^\| \d+ \| Assets/(Player|Camera|SaveSystem)/[^|]+\.cs \| \d+ \| (고위험|일반) \| yes \|' "$F"   # == 32
grep -cP '^\| \d+ \| Assets/[^|]+\.cs \| \d+ \| 고위험 \| yes \|' "$F"                                     # == 8
grep -cP '^\| C-D(07|08|10)-\d{2} \| Assets/[^|]+\.cs \| \d' "$F"                                          # 모든 항목에 file:line
grep -c "PlayerInputHandler" "$F"                                                                           # >= 1
git status --porcelain Assets   # 출력 없어야 함
```
</verification>

<success_criteria>
- Scope C 32개 파일이 커버리지 표에 100% 등장하고 고위험 행이 정확히 8개다.
- D-07 >= 6행, D-08 >= 13행, D-10 >= 4행이며 모든 행에 경로와 숫자 줄번호가 있다.
- `CameraController.LateUpdate` 의 실행 순서 계약이 D-10 항목 Reason에 명시되어 있다.
- `SaveData.MapGimmickState`/`Items` 가 `의도된 확장 스텁 — 삭제 권장 아님` 으로 기재되어 오판이 방지되었다.
- CP949 4개 파일 + `CameraController.cs` 비-ASCII 5줄 게이트가 D-04 섹션에 기록되어 있다.
- `git status --porcelain Assets` 가 비어 있다 — 소스 코드 0줄 변경.
</success_criteria>

<output>
완료 후 `.planning/phases/13-codebase-cleanup-audit/13-03-SUMMARY.md` 를 생성한다.
카테고리별/위험티어별 건수와 Plan 05가 이어받을 D-09 후보를 명시한다.
</output>
