---
phase: 13-codebase-cleanup-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md
autonomous: true
requirements: [D-03, D-04, D-05, D-06, D-07, D-08, D-10]

must_haves:
  truths:
    - "Assets/Enemy 아래 NewBoss/Tutorial/Boss/Monster_Alpha/Script 5개 트리의 42개 .cs 파일 전부가 스캔 커버리지 표에 나열된다"
    - "NewBoss(공유 기반 클래스)와 Tutorial(Play 모드 검증 보스) 항목은 고위험 하위 섹션에, Boss/Monster_Alpha/Enemy/Script 항목은 일반 하위 섹션에 분리 기재된다"
    - "이미 알려진 고아 코드 WoodBossStatSystem.cs 가 참조 0건으로 재확인되어 D-07에 기재된다"
    - "이 범위의 CP949 인코딩 파일 16개가 별도 섹션에 나열된다 (D-04)"
    - "소스 .cs 파일은 단 한 줄도 수정되지 않는다"
  artifacts:
    - path: ".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md"
      provides: "Scope B 발견 항목 fragment (D-07/D-08/D-10 + CP949 목록 + D-09 후보 + 커버리지 표)"
      contains: "## D-07 — 죽은 코드"
      min_lines: 90
  key_links:
    - from: "13-FINDINGS-B-newboss-tutorial-legacy.md"
      to: "Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossStatSystem.cs"
      via: "D-07 표에서 알려진 고아 코드 재확인"
      pattern: "WoodBossStatSystem"
---

<objective>
Scope B(`Assets/Enemy/NewBoss/**` + `Assets/Enemy/Tutorial/**` + `Assets/Enemy/Boss/**` +
`Assets/Enemy/Monster_Alpha/**` + `Assets/Enemy/Script/**`, 42개 .cs / 3388줄)를 대상으로
죽은 코드(D-07) / 디버그·주석 잔재(D-08) / 과도한 복잡도(D-10)를 스캔해 **보고서 fragment 한 개**를 만든다.

Purpose: 이 범위는 **위험도가 섞여 있다** — `NewBoss/`(모든 보스가 상속하는 공유 기반 클래스 +
State/Attack 인터페이스)와 `Tutorial/`(Play 모드 검증된 TutorialBoss/WoodBoss)은 고위험,
`Boss/`·`Monster_Alpha/`·`Script/`(초기 프로토타입 계열)는 일반 위험이다. 위험 티어를 항목마다
정확히 나눠서 사용자가 승인 판단을 다르게 할 수 있게 한다 (D-05/D-06).
Output: `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md`

**보고서 전용 phase다. `Assets/` 아래 어떤 파일도 생성/수정/삭제하지 않는다 (CONTEXT.md D-01/D-02).**
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md

<scan_scope>
정확히 이 5개 트리의 `*.cs` 파일 전부 (42개):
- `Assets/Enemy/NewBoss/**/*.cs` — 12개 / 716줄 — **고위험** (모든 보스의 공유 기반 클래스)
- `Assets/Enemy/Tutorial/**/*.cs` — 20개 / 1867줄 — **고위험** (Play 모드 검증된 TutorialBoss/WoodBoss)
- `Assets/Enemy/Boss/Script/*.cs` — 4개 / 267줄 — 일반
- `Assets/Enemy/Monster_Alpha/Script/*.cs` — 5개 / 510줄 — 일반
- `Assets/Enemy/Script/*.cs` — 1개 / 28줄 (`EnemyHitBox.cs`) — 일반

파일 목록 생성 명령:
```bash
find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -type f | sort
```
</scan_scope>

<risk_tier_rule>
<!-- 항목마다 어느 하위 섹션에 넣을지 결정하는 규칙. 애매하면 고위험 쪽으로 보낸다. -->

**고위험 하위 섹션 (`### D-0X 회귀 위험 높음 — 신중 검토 필요`)** 에 넣을 파일 경로 접두사:
- `Assets/Enemy/NewBoss/` — `BossController.cs` / `BossStatesSystem.cs` / `States/*` / `States/Attacks/*`.
  Phase 7/8에서 `CombatState.cs` 에 `PatternCandidate` / `SelectWeightedPattern` 헬퍼가 추가되었고,
  WaterMonster·WaterSpirit·TutorialBoss가 전부 이 기반 클래스를 상속한다. 여기 손대면 보스 4종이 동시에 깨진다.
- `Assets/Enemy/Tutorial/` — TutorialBoss / WoodBoss. Play 모드 검증됨(STATE.md Active TODOs 참고).
  추가로 Phase 11에서 `HandleDeath()` 에 `SaveLoadManager` 저장 트리거가 삽입된 파일들이 여기 있다.

**일반 하위 섹션 (`### D-0X 일반 항목`)** 에 넣을 파일 경로 접두사:
- `Assets/Enemy/Boss/`
- `Assets/Enemy/Monster_Alpha/`
- `Assets/Enemy/Script/`
</risk_tier_rule>

<false_positive_rules>
<!-- Unity 프로젝트에서 "참조 0건"이 곧 "죽은 코드"가 아닌 경우들. -->

1. **Unity 매직 메서드** — 절대 D-07로 보고 금지:
   `Awake` `OnEnable` `Start` `FixedUpdate` `Update` `LateUpdate` `OnDisable` `OnDestroy`
   `OnTriggerEnter2D` `OnTriggerExit2D` `OnTriggerStay2D` `OnCollisionEnter2D` `OnCollisionExit2D`
   `OnCollisionStay2D` `OnDrawGizmos` `OnDrawGizmosSelected` `OnGUI` `OnValidate` `Reset`
   `OnApplicationQuit` `OnBecameVisible` `OnBecameInvisible` `OnAnimatorMove`

2. **Inspector 직렬화 필드** — `public` / `[SerializeField] private` 필드는 씬/프리팹에서 값이 주입될 수 있다:
   ```bash
   grep -rl "<심볼명>" Assets --include="*.unity" --include="*.prefab" --include="*.asset" 2>/dev/null
   ```
   결과가 있으면 D-07로 보고하지 않는다. 이 범위는 `Tutorial Boss.prefab` 등 프리팹 의존이 많으니 특히 주의.

3. **인터페이스 구현 멤버** — `IBossState`(`Enter`/`Execute`/`Exit`), `IAttackStrategy` 구현 멤버는
   인터페이스 디스패치로 호출된다. 직접 호출부가 없어도 죽은 코드가 아니다.
   단, **인터페이스 자체가 아무 데서도 안 쓰이면** 그건 D-07 대상이다.

4. **애니메이션 이벤트 호출 메서드**:
   ```bash
   grep -rl "<심볼명>" Assets --include="*.anim" --include="*.controller" 2>/dev/null
   ```
   이 범위는 `Unit.controller` / `Targets.controller` / `Rarm_solver_Target.controller` 가 있어 특히 필요하다.

5. **속성 기반 진입점** — `[ContextMenu]` `[RuntimeInitializeOnLoadMethod]` `[MenuItem]`.

6. **`protected virtual` 확장점** — `BossController` / `BossStatesSystem` 의 `protected virtual` 멤버는
   하위 클래스(WaterMonster/Spirit/Tutorial/WoodBoss)에서 override 될 수 있다.
   `grep -rn "override .*<심볼명>" Assets --include="*.cs"` 로 확인하고, override 가 하나라도 있으면 보고하지 않는다.
</false_positive_rules>

<known_orphans>
<!-- CONTEXT.md `<code_context>` 와 Assets/SaveSystem/Check.md 107~108행에 이미 기록된 항목.
     "재확인 차원으로 포함" 이 D-07의 명시 요구사항이다 (CONTEXT.md D-07). -->

이 범위에 해당하는 것: **`Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossStatSystem.cs`**
- 검증 명령: `grep -rn --include="*.cs" "WoodBossStatSystem" Assets` → **참조 0건** (선언부조차 클래스명으로 검색되지 않는 상태)
- 씬/프리팹 확인도 필수: `grep -rl "WoodBossStatSystem" Assets --include="*.unity" --include="*.prefab"`
- D-07 고위험 표에 반드시 1행으로 넣고, Reason 칼럼에
  `기존 식별 고아 코드 재확인 (Assets/SaveSystem/Check.md:108) — 참조 0건` 을 적는다.
- 이 파일은 CP949 인코딩이므로 Read 툴로 열면 한글 주석이 깨져 보인다. **줄번호는 정확하다.**

(`Portal.cs` / `GameManager.NextSpawnPointName` 는 `Assets/map/` 소속이라 Plan 04 담당이다.)
</known_orphans>

<encoding_hazard>
<!-- CONTEXT.md D-04. 이 범위는 프로젝트 CP949 파일 46개 중 16개를 갖고 있다 — 가장 많다. -->

이 범위의 CP949(비-UTF-8) 파일 16개 — **전부 fragment의 CP949 섹션에 나열해야 한다**:
```
Assets/Enemy/Boss/Script/HandCollision.cs
Assets/Enemy/Monster_Alpha/Script/patorl.cs
Assets/Enemy/NewBoss/Script/States/Attacks/HeavyAttack.cs
Assets/Enemy/NewBoss/Script/States/Attacks/IAttackStrategy.cs
Assets/Enemy/NewBoss/Script/States/Attacks/LightAttack.cs
Assets/Enemy/NewBoss/Script/States/Attacks/RangedPokeAttack.cs
Assets/Enemy/NewBoss/Script/States/CounterState.cs
Assets/Enemy/NewBoss/Script/States/GroggyState.cs
Assets/Enemy/NewBoss/Script/States/IBossState.cs
Assets/Enemy/NewBoss/Script/States/IdleState.cs
Assets/Enemy/Tutorial/TutorialBoss/Resource/Script/Rootspikevisual.cs
Assets/Enemy/Tutorial/TutorialBoss/Resource/Script/SeedProjectile.cs
Assets/Enemy/Tutorial/WoodBoss/BossController/State/IdleState.cs
Assets/Enemy/Tutorial/WoodBoss/BossController/State/RootSpikeStrategy.cs
Assets/Enemy/Tutorial/WoodBoss/BossController/State/WoodBossChaseState.cs
Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossStatSystem.cs
```
재생성 명령 (검증용):
```bash
find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -type f -print0 \
  | while IFS= read -r -d '' f; do iconv -f UTF-8 -t UTF-8 "$f" >/dev/null 2>&1 || printf '%s\n' "$f"; done | sort
```

**주의:** 이 파일들을 Read 툴로 읽으면 한글 주석이 U+FFFD 로 깨져 보이지만 **줄번호와 ASCII 코드는 정확하다**.
스캔·줄번호 기록에는 문제가 없다. 절대 Edit/Write 하지 않는다 — 이 phase는 어차피 보고서 전용이다.
</encoding_hazard>

<fragment_schema>
<!-- Plan 01/03/04와 동일 스키마 + CP949 섹션 1개 추가. 항목 ID 접두사는 `B-`. -->

```markdown
# Phase 13 Audit — Findings B: NewBoss / Tutorial / Legacy Enemies

**Scope:** `Assets/Enemy/NewBoss/**`, `Assets/Enemy/Tutorial/**`, `Assets/Enemy/Boss/**`, `Assets/Enemy/Monster_Alpha/**`, `Assets/Enemy/Script/**`
**Files scanned:** 42
**Risk tier:** 혼합 — NewBoss/Tutorial = 고위험, Boss/Monster_Alpha/Script = 일반
**Generated:** 2026-08-19

## D-07 — 죽은 코드
### D-07 일반 항목
| ID | File | Line(s) | Symbol | Reason |
### D-07 회귀 위험 높음 — 신중 검토 필요
| ID | File | Line(s) | Symbol | Reason |

## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드
### D-08 일반 항목
| ID | File | Line(s) | Kind | Reason |
### D-08 회귀 위험 높음 — 신중 검토 필요
| ID | File | Line(s) | Kind | Reason |

## D-10 — 과도하게 긴/복잡한 함수 (권장)
### D-10 일반 항목
| ID | File | Line(s) | Symbol | Metric | Reason |
### D-10 회귀 위험 높음 — 신중 검토 필요
| ID | File | Line(s) | Symbol | Metric | Reason |

## D-04 — CP949 인코딩 위험 파일
| # | File | 비고 |

## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)
- ...

## 스캔 커버리지
| # | File | Lines | Risk | Scanned |

## 요약
| 카테고리 | 항목 수 | 고위험 항목 수 |
```
항목이 없는 하위 섹션에는 `(없음)` 한 줄을 적는다 — 헤딩은 항상 존재해야 한다.
</fragment_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fragment 스켈레톤 + D-07 죽은 코드 스캔 (위험 티어 분리)</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-04~D-07 잠금 결정
    - `.planning/PROJECT.md` — `## Key Architectural Decisions` + `## Known Base Assets (재사용 대상)`
    - `.planning/STATE.md` — `### Key Decisions` 전체
    - `Assets/SaveSystem/Check.md` 100~110행 — 기존 식별 고아 코드 기록 원문
    - Scope B의 42개 .cs 파일 (`find` 명령 결과 전부)
  </read_first>

  <action>
    1. 출력 파일 `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md` 를
       `<fragment_schema>` 의 **모든 헤딩을 포함해** 새로 만든다. 반드시 존재해야 하는 헤딩(정확한 문자열):
       `# Phase 13 Audit — Findings B: NewBoss / Tutorial / Legacy Enemies`,
       `## D-07 — 죽은 코드`, `### D-07 일반 항목`, `### D-07 회귀 위험 높음 — 신중 검토 필요`,
       `## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드`, `### D-08 일반 항목`, `### D-08 회귀 위험 높음 — 신중 검토 필요`,
       `## D-10 — 과도하게 긴/복잡한 함수 (권장)`, `### D-10 일반 항목`, `### D-10 회귀 위험 높음 — 신중 검토 필요`,
       `## D-04 — CP949 인코딩 위험 파일`,
       `## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)`, `## 스캔 커버리지`, `## 요약`.
       헤더 메타 4줄은 `<fragment_schema>` 에 적힌 그대로 쓴다 (`**Files scanned:** 42`).

    2. `## D-04 — CP949 인코딩 위험 파일` 표를 지금 채운다. `<encoding_hazard>` 의 16개 경로를
       `| # | File | 비고 |` 형식으로 1~16행 기록하고, 표 바로 위에 다음 문장을 넣는다:
       `> 이 파일들은 CP949 인코딩이다. 실제 수정 단계에서는 표준 Read/Edit 왕복이 비-ASCII 바이트를 U+FFFD로 훼손시키므로 \`git show HEAD:<path>\` + 순수 바이트 스크립트 프로토콜이 필요하다 (CONTEXT.md D-04, STATE.md Phase 11 Plan 3 기록).`
       비고 칼럼에는 각 파일이 고위험 티어인지 일반 티어인지 (`고위험` / `일반`) 적는다.

    3. D-07 스캔을 수행한다. 파일 하나씩:
       a. `grep -nE '^\s*(public|private|protected|internal|static|readonly|const|\[SerializeField\])' <file>`
       b. 각 심볼: `grep -rown --include="*.cs" "\b<심볼명>\b" Assets | wc -l` — 결과 1이면 데드 후보
       c. `<false_positive_rules>` 6개 규칙으로 거른다. 규칙 2/4/6은 실제로 grep을 돌려 확인한다.
       d. 살아남은 항목을 `<risk_tier_rule>` 에 따라 **일반 / 고위험 표로 나눠서** 기록한다.

    4. `<known_orphans>` 의 `WoodBossStatSystem.cs` 를 D-07 **고위험** 표에 반드시 1행 넣고,
       Reason 칼럼에 정확히 `기존 식별 고아 코드 재확인 (Assets/SaveSystem/Check.md:108) — 참조 0건` 을 적는다.

    5. 추가 D-07 유형도 같은 표에 넣는다:
       - 주석 처리된 코드 블록: `grep -nE '^\s*//.*[;{}]' <file>` (설명 문장 제외, 실제 코드만)
       - 사용되지 않는 using
       - 참조 0건 클래스 전체 (특히 `Assets/Enemy/Boss/` `Assets/Enemy/Monster_Alpha/` 의 초기 프로토타입 계열 —
         `hand.cs` `patorl.cs` `bullet.cs` `enemy.cs` 등이 현재 씬에서 쓰이는지 씬/프리팹 grep으로 확인)

    **금지:** `Assets/` 아래 어떤 파일도 Write/Edit 하지 않는다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md"; test -f "$F" || exit 1; for h in "## D-07 — 죽은 코드" "### D-07 일반 항목" "### D-07 회귀 위험 높음 — 신중 검토 필요" "## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드" "### D-08 일반 항목" "### D-08 회귀 위험 높음 — 신중 검토 필요" "## D-10 — 과도하게 긴/복잡한 함수 (권장)" "### D-10 일반 항목" "### D-10 회귀 위험 높음 — 신중 검토 필요" "## D-04 — CP949 인코딩 위험 파일" "## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)" "## 스캔 커버리지" "## 요약"; do grep -qF "$h" "$F" || { echo "MISSING: $h"; exit 1; }; done; MISS=0; find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -type f -print0 | while IFS= read -r -d '' f; do iconv -f UTF-8 -t UTF-8 "$f" >/dev/null 2>&1 || grep -qF "$f" "$F" || echo "CP949 UNLISTED: $f"; done; grep -c "^| B-D07-" "$F"; grep -c "WoodBossStatSystem" "$F"; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - fragment 파일이 존재하고 위 verify의 **13개 헤딩 문자열**이 전부 존재한다.
    - `grep -c "^\*\*Files scanned:\*\* 42$" <fragment>` == 1
    - CP949 표에 16행이 있다: `grep -cP '^\| \d+ \| Assets/Enemy/[^|]+\.cs \|' <fragment>` >= 16, 그리고
      `iconv` 판정으로 얻은 16개 경로가 **하나도 빠짐없이** fragment 안에 존재한다 (verify의 `CP949 UNLISTED` 출력 0줄).
    - `grep -c "> 이 파일들은 CP949 인코딩이다" <fragment>` == 1
    - `grep -c "^| B-D07-" <fragment>` >= 8
    - `grep -cP '^\| B-D07-\d{2} \| Assets/Enemy/[^|]+\.cs \| \d' <fragment>` 가 `grep -c "^| B-D07-" <fragment>` 와 같다.
    - `grep -c "WoodBossStatSystem.*기존 식별 고아 코드 재확인 (Assets/SaveSystem/Check.md:108) — 참조 0건" <fragment>` == 1
    - `### D-07 일반 항목` 과 `### D-07 회귀 위험 높음 — 신중 검토 필요` **양쪽 모두** 최소 1행을 갖거나 `(없음)` 을 갖는다 (헤딩 뒤가 비어있지 않다).
    - 고위험 표의 모든 행 경로가 `Assets/Enemy/NewBoss/` 또는 `Assets/Enemy/Tutorial/` 로 시작한다.
    - 일반 표의 모든 행 경로가 `Assets/Enemy/Boss/`, `Assets/Enemy/Monster_Alpha/`, `Assets/Enemy/Script/` 중 하나로 시작한다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>fragment가 13개 규정 헤딩 + CP949 16행 표 + D-07 8행 이상(위험 티어별로 올바른 표에 분리)을 갖추고, WoodBossStatSystem 고아 재확인 행이 있으며, `Assets/` 는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: D-08 디버그/주석 잔재 스캔 (위험 티어 분리)</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md` — Task 1 결과
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-08 정의
    - `grep -rln --include="*.cs" "Debug\.Log" Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script` 로 나온 파일 전부 (기준: 30개 파일)
  </read_first>

  <action>
    fragment의 `### D-08 일반 항목` / `### D-08 회귀 위험 높음 — 신중 검토 필요` 두 표를 `<risk_tier_rule>` 에 따라 나눠 채운다.

    1. **Debug 로그 전수 수집** (이 범위 기준값: NewBoss 14 + Tutorial 43 + Boss 2 + Monster_Alpha 7 = 66건, 30개 파일):
       ```bash
       grep -rn --include="*.cs" "Debug\.Log\|Debug\.LogWarning\|Debug\.LogError" \
         Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script
       ```
       **파일 단위로 1행씩** 표에 기록한다 (여러 줄은 Line(s) 칼럼에 `12, 40, 77` 처럼 나열).
       Kind = `Debug.Log` / `Debug.LogWarning` / `Debug.LogError`.
       Reason에는 판단을 적는다: `개발용 상태 추적 — 제거 권장` 또는 `오류 진단용 — 유지 권장`.

    2. **TODO/FIXME/HACK 주석**:
       ```bash
       grep -rni --include="*.cs" "TODO\|FIXME\|HACK\|XXX" Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script
       ```
       프로젝트 전체 기준값이 **0건**이다. 이 범위도 0건이면 `### D-08 일반 항목` 표 아래에
       `> TODO/FIXME/HACK 주석: 이 범위에서 0건 (프로젝트 전체도 0건).` 한 줄을 적는다.

    3. **주석 처리된 테스트/디버그 코드**: 기준 확인 지점 —
       `Assets/Enemy/Tutorial/WoodBoss/BossController/State/VineSwingStrategy.cs` (주석-코드 라인 4건),
       `Assets/Enemy/NewBoss/Script/BossController.cs` (2건),
       `Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossController.cs` (1건),
       `Assets/Enemy/Tutorial/WoodBoss/BossController/State/FloorSweepStrategy.cs` (1건),
       `Assets/Enemy/Tutorial/TutorialBoss/State/TutorialDeadState.cs` (1건),
       `Assets/Enemy/Tutorial/TutorialBoss/Resource/Script/SeedProjectile.cs` (2건),
       `Assets/Enemy/Boss/Script/BossHealthBarController.cs` (1건).
       각각 실제로 열어보고 **주석 처리된 코드**인지(설명 문장이 아닌지) 확인한 뒤 Kind=`주석처리 코드` 로 행을 추가한다.
       `VineSwingStrategy.cs` 는 4건이므로 반드시 포함되어야 한다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md"; N=$(grep -c "^| B-D08-" "$F"); echo "D-08 rows: $N"; test "$N" -ge 25 || { echo "FAIL: too few D-08 rows"; exit 1; }; MISS=0; for f in $(grep -rl --include="*.cs" "Debug\.Log" Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script); do grep -qF "$f" "$F" || { echo "UNCOVERED: $f"; MISS=1; }; done; test "$MISS" -eq 0 && echo "ALL Debug.Log FILES COVERED"; grep -qF "VineSwingStrategy.cs" "$F" && echo "VineSwing OK"; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^| B-D08-" <fragment>` >= 25
    - `Debug.Log` 를 포함하는 **30개 파일 경로 전부**가 fragment 안에 존재한다 (verify의 `ALL Debug.Log FILES COVERED` 출력).
    - `grep -cP '^\| B-D08-\d{2} \| Assets/Enemy/[^|]+\.cs \| \d' <fragment>` 가 `grep -c "^| B-D08-" <fragment>` 와 같다.
    - `grep -c "VineSwingStrategy.cs" <fragment>` >= 1 이고 그 행의 Kind 칼럼이 `주석처리 코드` 다:
      `grep -c "VineSwingStrategy.cs.*주석처리 코드" <fragment>` >= 1
    - `grep -c "TODO/FIXME/HACK 주석: 이 범위에서 0건" <fragment>` == 1 또는 `grep -c "| TODO |" <fragment>` >= 1
    - `### D-08 회귀 위험 높음 — 신중 검토 필요` 표에 최소 20행이 있다 (NewBoss 14 + Tutorial 43 = 57건이 고위험 티어).
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>D-08 표가 총 25행 이상이고 위험 티어별로 분리되어 있으며, `Debug.Log` 를 가진 30개 파일이 빠짐없이 등장하고, VineSwingStrategy.cs 의 주석처리 코드가 기재되어 있다.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 복잡도 + D-09 후보 + 커버리지 표 + 요약</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md` — Task 1/2 결과
    - `.planning/PROJECT.md` — `## Key Architectural Decisions` (의도적 비공유 판단 근거)
    - `.planning/STATE.md` — `### Key Decisions` 중 Phase 7/8 항목
    - 이 범위의 200줄 초과 파일 원문: `Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs`(343줄), `Assets/Enemy/Tutorial/TutorialBoss/TentacleSwipeStrategy.cs`(246줄), `Assets/Enemy/NewBoss/Script/States/CombatState.cs`(226줄)
    - 100~200줄 파일: `Assets/Enemy/NewBoss/Script/BossController.cs`(192줄), `Assets/Enemy/Tutorial/TutorialBoss/TentaclePierceStrategy.cs`(183줄), `Assets/Enemy/Monster_Alpha/Script/EnemyBrain.cs`(162줄), `Assets/Enemy/Tutorial/WoodBoss/BossController/State/FloorSweepStrategy.cs`(150줄), `Assets/Enemy/Monster_Alpha/Script/Charge.cs`(138줄)
  </read_first>

  <action>
    1. **D-10 복잡도 스캔.** 판정 기준(둘 중 하나라도 초과 시 기록):
       - 메서드 본문 **40줄 초과**
       - 한 메서드 안의 분기 토큰 합계 **15 초과** (`if` `else if` `for` `foreach` `while` `switch` `case` `&&` `||` `?:`)
       `<read_first>` 에 나열한 8개 파일을 우선 검사하고, 나머지 파일도 `wc -l` 기준 상위부터 확인한다:
       ```bash
       find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -exec wc -l {} + | sort -rn
       ```
       각 항목을 `<risk_tier_rule>` 에 따라 일반/고위험 표에 나눠 기록하고 Metric 칼럼에
       `{본문 줄수}줄 / 분기 {N}` 형식의 정확한 수치를 적는다. Line(s) 칼럼은 `{시작줄}-{끝줄}` 범위로 적는다.
       `## D-10 — 과도하게 긴/복잡한 함수 (권장)` 헤딩 바로 아래에
       `> D-10은 권장 수준 관찰이다. 실제 수정은 사용자 승인 필수 (CONTEXT.md D-10).` 한 줄을 넣는다.

    2. **D-09 후보 관찰.** `## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)` 아래에 불릿으로 기록한다.
       각 불릿은 `파일경로:줄번호` 를 2곳 이상 포함해야 한다. 반드시 확인할 후보:
       - `Assets/Enemy/NewBoss/Script/States/IdleState.cs` 와
         `Assets/Enemy/Tutorial/WoodBoss/BossController/State/IdleState.cs` — **동명 클래스 2개**.
         실제로 로직이 겹치는지, 아니면 네임스페이스/클래스명이 달라 별개인지 확인해 결론을 적는다.
       - `Assets/Enemy/Tutorial/TutorialBoss/TentacleSwipeStrategy.cs` 와 `TentaclePierceStrategy.cs` 의 공통 골격
       - `Assets/Enemy/Monster_Alpha/Script/` 의 `enemy.cs`/`EnemyBrain.cs`/`Charge.cs`/`patorl.cs` 간 이동·추적 로직 중복

       **다음은 문서화된 의도적 비공유이므로 후보로 올리지 않는다:**
       - `BossController` / `BossStatsSystem` 기반 클래스 수준으로만 공유한다는 원칙 자체
         (PROJECT.md `## Key Architectural Decisions`, STATE.md `### Key Decisions` 반복 기재)
       - `CombatState.SelectWeightedPattern` 의 3번째 파라미터 기본값 방식 — 오버로드 대신 기본값을 쓴 것은
         `SpiritCombatState.cs` 호출부 0줄 변경 보장을 위한 Phase 8 D-05a 회귀 방지 결정
       - TutorialBoss/WoodBoss 가 `HandleDeath()` 에, WaterSpirit/WaterMonster 가 `Die()` 오버라이드에
         각각 저장 트리거를 넣은 것 — 두 아키텍처 그룹이 실제로 다른 것 (Phase 11 Plan 3)
       후보가 없으면 `- (없음)`.

    3. **커버리지 표.** `## 스캔 커버리지` 아래 `| # | File | Lines | Risk | Scanned |` 표를 만들고
       `find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -type f | sort`
       결과 **42개 파일 전부**를 1행씩 기록한다. 번호 1~42, 실제 줄 수, Risk 칼럼은 `고위험` 또는 `일반`
       (`<risk_tier_rule>` 의 경로 접두사로 결정), Scanned는 `yes`.

    4. **요약 표.** `## 요약` 아래에 `| 카테고리 | 항목 수 | 고위험 항목 수 |` 표를 만들고 D-07/D-08/D-10 행을
       실제 표 행 수와 일치하게 채운다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md"; C=$(grep -cP '^\| \d+ \| Assets/Enemy/[^|]+\.cs \| \d+ \| (고위험|일반) \| yes \|' "$F"); echo "coverage rows: $C"; test "$C" -eq 42 || { echo "FAIL: expected 42"; exit 1; }; MISS=0; for f in $(find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -type f | sort); do grep -qF "$f" "$F" || { echo "UNCOVERED: $f"; MISS=1; }; done; test "$MISS" -eq 0 || exit 1; test $(grep -c "^| B-D10-" "$F") -ge 4 || { echo "FAIL: D-10 rows"; exit 1; }; grep -qF "> D-10은 권장 수준 관찰이다" "$F" && grep -qF "IdleState.cs" "$F" && echo OK; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - 커버리지 표 행이 정확히 **42개**이고 정규식 `^\| \d+ \| Assets/Enemy/[^|]+\.cs \| \d+ \| (고위험|일반) \| yes \|` 에 전부 매치한다.
    - 42개 파일 경로가 전부 fragment 안에 문자열로 존재한다 (누락 0).
    - 커버리지 표에서 `Assets/Enemy/NewBoss/` 와 `Assets/Enemy/Tutorial/` 로 시작하는 32행의 Risk 칼럼이 전부 `고위험` 이고, 나머지 10행이 전부 `일반` 이다.
    - `grep -c "^| B-D10-" <fragment>` >= 4
    - `grep -cP '^\| B-D10-\d{2} \| Assets/Enemy/[^|]+\.cs \| \d+-\d+ \|' <fragment>` 가 `grep -c "^| B-D10-" <fragment>` 와 같다.
    - `grep -c "> D-10은 권장 수준 관찰이다. 실제 수정은 사용자 승인 필수 (CONTEXT.md D-10)." <fragment>` == 1
    - `## D-09 후보 관찰` 섹션이 `IdleState.cs` 동명 클래스 건을 포함한다: `grep -c "IdleState.cs" <fragment>` >= 2
    - `## 요약` 표의 각 카테고리 항목 수가 `grep -c "^| B-D07-"` / `"^| B-D08-"` / `"^| B-D10-"` 결과와 일치한다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>fragment가 D-07/D-08/D-10 표(위험 티어 분리) + CP949 16행 + D-09 후보 + 42행 커버리지 표 + 요약 표를 모두 갖추고, `Assets/` 는 0줄 변경이다.</done>
</task>

</tasks>

<verification>
```bash
F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md"
test -f "$F" && test $(wc -l < "$F") -ge 90
grep -cP '^\| \d+ \| Assets/Enemy/[^|]+\.cs \| \d+ \| (고위험|일반) \| yes \|' "$F"   # == 42
grep -cP '^\| B-D(07|08|10)-\d{2} \| Assets/Enemy/[^|]+\.cs \| \d' "$F"              # 모든 항목에 file:line
grep -c "WoodBossStatSystem" "$F"                                                     # >= 1 (고아 재확인)
# CP949 16개 전수 등재 확인
find Assets/Enemy/NewBoss Assets/Enemy/Tutorial Assets/Enemy/Boss Assets/Enemy/Monster_Alpha Assets/Enemy/Script -name "*.cs" -print0 \
 | while IFS= read -r -d '' f; do iconv -f UTF-8 -t UTF-8 "$f" >/dev/null 2>&1 || grep -qF "$f" "$F" || echo "UNLISTED $f"; done
git status --porcelain Assets   # 출력 없어야 함
```
</verification>

<success_criteria>
- Scope B 42개 파일이 커버리지 표에 100% 등장하고 Risk 칼럼이 경로 접두사 규칙과 정확히 일치한다.
- D-07 >= 8행, D-08 >= 25행, D-10 >= 4행이며 모든 행에 `Assets/Enemy/...cs` 경로와 숫자 줄번호가 있다.
- 고위험/일반 항목이 서로 다른 하위 섹션에 분리 기재되어 있다 (D-06).
- CP949 16개 파일이 `## D-04 — CP949 인코딩 위험 파일` 표에 전수 등재되어 있다 (D-04).
- `WoodBossStatSystem.cs` 가 참조 0건 고아 코드로 재확인 기재되어 있다.
- `git status --porcelain Assets` 가 비어 있다 — 소스 코드 0줄 변경.
</success_criteria>

<output>
완료 후 `.planning/phases/13-codebase-cleanup-audit/13-02-SUMMARY.md` 를 생성한다.
카테고리별/위험티어별 건수와 Plan 05가 이어받을 D-09 후보를 명시한다.
</output>
