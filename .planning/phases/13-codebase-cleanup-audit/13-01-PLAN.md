---
phase: 13-codebase-cleanup-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md
autonomous: true
requirements: [D-03, D-05, D-06, D-07, D-08, D-10]

must_haves:
  truths:
    - "Assets/Enemy/WaterMonster 와 Assets/Enemy/WaterSpirit 아래 38개 .cs 파일 전부가 스캔 커버리지 표에 나열된다"
    - "발견 항목마다 파일 경로 + 줄번호 + 카테고리 + 사유가 있다"
    - "이 범위의 모든 항목이 '회귀 위험 높음' 하위 섹션에 들어간다 (D-05/D-06 — 전부 Play 모드 검증된 보스 상태머신 코드)"
    - "소스 .cs 파일은 단 한 줄도 수정되지 않는다"
  artifacts:
    - path: ".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md"
      provides: "Scope A 발견 항목 fragment (D-07/D-08/D-10 + D-09 후보 + 커버리지 표)"
      contains: "## D-07 — 죽은 코드"
      min_lines: 80
  key_links:
    - from: "13-FINDINGS-A-watermonster-waterspirit.md"
      to: "Assets/Enemy/WaterMonster, Assets/Enemy/WaterSpirit"
      via: "커버리지 표의 파일 경로 38행"
      pattern: "Assets/Enemy/Water(Monster|Spirit)/.*\\.cs"
---

<objective>
Scope A(`Assets/Enemy/WaterMonster/**` + `Assets/Enemy/WaterSpirit/**`, 38개 .cs / 2453줄)를 대상으로
죽은 코드(D-07) / 디버그·주석 잔재(D-08) / 과도한 복잡도(D-10)를 스캔해 **보고서 fragment 한 개**를 만든다.

Purpose: 이 범위는 Phase 5~8에서 구현되고 Play 모드로 검증된 보스 상태머신 코드다 — CONTEXT.md D-05에
따라 스캔 대상에 포함하되, D-06에 따라 **모든 항목에 "회귀 위험 높음" 태그**를 붙여 다른 범위와
시각적으로 분리한다.
Output: `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md`

**보고서 전용 phase다. `Assets/` 아래 어떤 파일도 생성/수정/삭제하지 않는다 (CONTEXT.md D-01/D-02).**
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md

<scan_scope>
정확히 이 두 트리의 `*.cs` 파일 전부 (38개):
- `Assets/Enemy/WaterMonster/**/*.cs` — 26개 파일 / 1721줄
- `Assets/Enemy/WaterSpirit/**/*.cs` — 12개 파일 / 732줄

파일 목록 생성 명령:
```bash
find Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit -name "*.cs" -type f | sort
```
</scan_scope>

<false_positive_rules>
<!-- Unity 프로젝트에서 "참조 0건"이 곧 "죽은 코드"가 아닌 경우들. -->
<!-- 아래에 해당하면 D-07로 보고하지 말거나, 반드시 "제외 사유"를 적어라. -->

1. **Unity 매직 메서드** — 엔진이 리플렉션으로 호출하므로 코드 참조가 0건이어도 살아있다. 절대 D-07로 보고 금지:
   `Awake` `OnEnable` `Start` `FixedUpdate` `Update` `LateUpdate` `OnDisable` `OnDestroy`
   `OnTriggerEnter2D` `OnTriggerExit2D` `OnTriggerStay2D` `OnCollisionEnter2D` `OnCollisionExit2D`
   `OnCollisionStay2D` `OnDrawGizmos` `OnDrawGizmosSelected` `OnGUI` `OnValidate` `Reset`
   `OnApplicationQuit` `OnBecameVisible` `OnBecameInvisible` `OnAnimatorMove`

2. **Inspector 직렬화 필드** — `public` 필드나 `[SerializeField] private` 필드는 씬/프리팹에서 값이
   주입될 수 있다. 죽었다고 선언하기 전에 반드시 확인:
   ```bash
   grep -rl "<심볼명>" Assets --include="*.unity" --include="*.prefab" --include="*.asset" 2>/dev/null
   ```
   결과가 있으면 D-07로 보고하지 않는다.

3. **인터페이스 구현 멤버** — `IBossState`(`Enter`/`Execute`/`Exit`), `IAttackStrategy` 구현 멤버는
   인터페이스 디스패치로 호출된다. 직접 호출부가 없어도 죽은 코드가 아니다.

4. **애니메이션 이벤트 호출 메서드** — `.anim` / `.controller` 에서 문자열 이름으로 호출된다:
   ```bash
   grep -rl "<심볼명>" Assets --include="*.anim" --include="*.controller" 2>/dev/null
   ```

5. **속성 기반 진입점** — `[ContextMenu]` `[RuntimeInitializeOnLoadMethod]` `[MenuItem]` 이 붙은
   메서드는 참조 0건이어도 살아있다.

6. **문서화된 의도적 데드 코드 (보고는 하되 "삭제 권장 아님"으로 명시)** — STATE.md Key Decisions에
   이유가 기록된 것들. 이 범위에서 해당하는 것:
   - `Assets/Enemy/WaterSpirit/Script/SpiritController.cs:8` 의 `public float ChargeRange = 5.0f;`
     → 프로젝트 전체 참조 1건(선언부 자신)뿐인 진짜 데드 필드지만, STATE.md Key Decision
     "SpiritController.ChargeRange 는 기존 데드 필드로 재활용하지 않고 유지 (재활용 시
     SpiritFarProjectile 영구 선택 불가 발생)" 에 따라 **의도적으로 남긴 것**이다.
     D-07 표에 넣되 사유 칼럼에 `문서화된 의도적 유지 (STATE.md Key Decision) — 삭제 권장 아님` 이라고 적는다.
</false_positive_rules>

<encoding_note>
이 범위에는 CP949(비-UTF-8) 인코딩 파일이 없다 (프로젝트 전체 46개 CP949 파일 중 이 두 트리에는 0개).
따라서 Read 툴로 안전하게 읽을 수 있다. 그래도 **읽기만** 한다 — 이 plan은 소스를 수정하지 않는다.
</encoding_note>

<fragment_schema>
<!-- 4개 스캔 plan이 전부 이 스키마를 그대로 따른다. Plan 05가 기계적으로 병합한다. -->
<!-- 항목 ID 형식: {범위문자}-{카테고리}-{2자리 일련번호}  예: A-D07-01, A-D08-14 -->
<!-- 항목이 없는 하위 섹션에는 반드시 `(없음)` 한 줄을 적는다 — 섹션 헤딩은 항상 존재해야 한다. -->

```markdown
# Phase 13 Audit — Findings A: WaterMonster / WaterSpirit

**Scope:** `Assets/Enemy/WaterMonster/**/*.cs`, `Assets/Enemy/WaterSpirit/**/*.cs`
**Files scanned:** 38
**Risk tier:** 전 범위 회귀 위험 높음 (D-05/D-06 — Play 모드 검증된 보스 상태머신)
**Generated:** 2026-08-19

## D-07 — 죽은 코드

### D-07 일반 항목

(없음)

### D-07 회귀 위험 높음 — 신중 검토 필요

| ID | File | Line(s) | Symbol | Reason |
|----|------|---------|--------|--------|
| A-D07-01 | Assets/Enemy/... | 42 | `_unusedField` | 프로젝트 전체 참조 0건, 씬/프리팹 참조 없음 |

## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드

### D-08 일반 항목

(없음)

### D-08 회귀 위험 높음 — 신중 검토 필요

| ID | File | Line(s) | Kind | Reason |
|----|------|---------|------|--------|
| A-D08-01 | Assets/Enemy/... | 55, 61, 78 | Debug.Log | 개발용 상태 추적 로그 3건 |

## D-10 — 과도하게 긴/복잡한 함수 (권장)

### D-10 일반 항목

(없음)

### D-10 회귀 위험 높음 — 신중 검토 필요

| ID | File | Line(s) | Symbol | Metric | Reason |
|----|------|---------|--------|--------|--------|
| A-D10-01 | Assets/Enemy/... | 88-152 | `SelectAttackStrategy` | 65줄 / 분기 21 | 단일 메서드에 후보 구성+선택+쿨다운 처리 혼재 |

## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)

- `WaterMonsterController.cs` 와 `SpiritController.cs` 가 동일한 `Facing` 계산 블록을 각각 보유 (파일:줄)

## 스캔 커버리지

| # | File | Lines | Scanned |
|---|------|-------|---------|
| 1 | Assets/Enemy/WaterMonster/Script/WaterMonsterController.cs | 267 | yes |
```
</fragment_schema>
</context>

<tasks>

<task type="auto">
  <name>Task 1: fragment 스켈레톤 생성 + D-07 죽은 코드 스캔</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-01~D-10 잠금 결정 (특히 D-05/D-06 위험 티어링, D-07 정의)
    - `.planning/STATE.md` — `## Accumulated Context` > `### Key Decisions` 전체 (의도적 데드 코드/의도적 비공유 판정 근거)
    - `.planning/PROJECT.md` — `## Key Architectural Decisions` (BossController/BossStatsSystem 기반 클래스 수준 공유 원칙)
    - Scope A의 38개 .cs 파일 (`find Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit -name "*.cs" -type f | sort` 결과 전부)
  </read_first>

  <action>
    1. 출력 파일 `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` 를
       위 `<fragment_schema>` 의 **모든 섹션 헤딩을 포함해** 새로 만든다. 이 시점에 만들어야 하는 헤딩(정확한 문자열):
       - `# Phase 13 Audit — Findings A: WaterMonster / WaterSpirit`
       - `## D-07 — 죽은 코드`
       - `### D-07 일반 항목`
       - `### D-07 회귀 위험 높음 — 신중 검토 필요`
       - `## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드`
       - `### D-08 일반 항목`
       - `### D-08 회귀 위험 높음 — 신중 검토 필요`
       - `## D-10 — 과도하게 긴/복잡한 함수 (권장)`
       - `### D-10 일반 항목`
       - `### D-10 회귀 위험 높음 — 신중 검토 필요`
       - `## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)`
       - `## 스캔 커버리지`
       Task 2/3이 채울 섹션은 일단 `(없음)` 으로 채워둔다.

    2. 헤더 메타 4줄을 정확히 이렇게 적는다:
       ```
       **Scope:** `Assets/Enemy/WaterMonster/**/*.cs`, `Assets/Enemy/WaterSpirit/**/*.cs`
       **Files scanned:** 38
       **Risk tier:** 전 범위 회귀 위험 높음 (D-05/D-06 — Play 모드 검증된 보스 상태머신)
       **Generated:** 2026-08-19
       ```

    3. D-07 스캔을 수행한다. 파일 하나씩 다음 절차를 밟는다:
       a. 선언된 심볼을 뽑는다:
          ```bash
          grep -nE '^\s*(public|private|protected|internal|static|readonly|const|\[SerializeField\])' <file>
          ```
       b. 각 심볼 이름의 프로젝트 전체 참조 수를 센다 (선언부 1건 포함):
          ```bash
          grep -rown --include="*.cs" "\b<심볼명>\b" Assets | wc -l
          ```
          결과가 1이면(= 선언부만) 데드 후보다.
       c. 데드 후보를 `<false_positive_rules>` 6개 규칙으로 걸러낸다. 규칙 2/4는 실제로
          `.unity`/`.prefab`/`.asset`/`.anim`/`.controller` grep 을 돌려서 확인한다.
       d. 살아남은 항목을 `### D-07 회귀 위험 높음 — 신중 검토 필요` 표에 한 행씩 추가한다.
          Scope A는 전부 D-05/D-06 대상이므로 `### D-07 일반 항목` 은 `(없음)` 으로 남긴다.

    4. 추가로 아래 3종 D-07 유형도 같은 표에 넣는다:
       - **주석 처리된 코드 블록**: `grep -nE '^\s*//.*[;{}]' <file>` 로 찾고, 실제 코드가 주석
         처리된 것만(설명 문장은 제외) 기록.
       - **사용되지 않는 using**: 파일 상단 `using X;` 각각에 대해, 해당 네임스페이스 심볼이
         파일 본문에 쓰이는지 확인. `using UnityEngine;` 같이 명백히 쓰이는 것은 건너뛴다.
       - **참조 0건 클래스 전체**: 클래스명 프로젝트 참조가 1건(선언부)뿐인 파일.

    5. `SpiritController.cs:8` 의 `ChargeRange` 는 반드시 표에 포함하되, Reason 칼럼에
       `문서화된 의도적 유지 (STATE.md Key Decision) — 삭제 권장 아님` 을 그대로 적는다.

    **금지:** `Assets/` 아래 어떤 파일도 Write/Edit 하지 않는다. 오직 읽기 + fragment 작성만 한다.
  </action>

  <verify>
    <automated>test -f ".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md" && for h in "## D-07 — 죽은 코드" "### D-07 일반 항목" "### D-07 회귀 위험 높음 — 신중 검토 필요" "## D-08 — TODO/FIXME 잔재 및 임시 디버그 코드" "### D-08 일반 항목" "### D-08 회귀 위험 높음 — 신중 검토 필요" "## D-10 — 과도하게 긴/복잡한 함수 (권장)" "### D-10 일반 항목" "### D-10 회귀 위험 높음 — 신중 검토 필요" "## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)" "## 스캔 커버리지"; do grep -qF "$h" ".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md" || { echo "MISSING: $h"; exit 1; }; done; echo OK; git status --porcelain Assets | grep . && { echo "FAIL: Assets modified"; exit 1; } || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` 파일이 존재한다.
    - 위 verify 명령의 11개 헤딩 문자열이 전부 존재한다 (exit 0, "OK" 출력).
    - `grep -c "^\*\*Files scanned:\*\* 38$" <fragment>` == 1
    - `grep -c "^| A-D07-" <fragment>` >= 6 (D-07 항목 6건 이상)
    - `grep -cP '^\| A-D07-\d{2} \| Assets/Enemy/Water(Monster|Spirit)/[^|]+\.cs \| \d' <fragment>` 가
      `grep -c "^| A-D07-" <fragment>` 와 같다 (**모든 D-07 행이 실제 경로 + 숫자 줄번호를 가진다**).
    - `grep -c "ChargeRange" <fragment>` >= 1 이고 같은 행에 `삭제 권장 아님` 이 포함된다:
      `grep -c "ChargeRange.*삭제 권장 아님" <fragment>` == 1
    - `### D-07 일반 항목` 바로 다음 비어있지 않은 줄이 `(없음)` 이다 (Scope A 전 항목이 고위험 티어).
    - `git status --porcelain Assets` 출력이 비어 있다 (소스 무수정).
  </acceptance_criteria>

  <done>fragment 파일이 11개 규정 헤딩 전부와 D-07 표(6행 이상, 전 행이 `Assets/Enemy/Water*/....cs:줄번호` 형식)를 갖추고, `Assets/` 는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: D-08 디버그/주석 잔재 스캔</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` — Task 1이 만든 현재 상태
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-08 정의
    - `grep -rln --include="*.cs" "Debug\.Log" Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit` 로 나온 파일 전부 (기준: 17개 파일)
  </read_first>

  <action>
    fragment의 `### D-08 회귀 위험 높음 — 신중 검토 필요` 표를 채운다. `### D-08 일반 항목` 은 `(없음)` 유지.

    1. **Debug 로그 전수 수집** (이 범위 기준값: WaterMonster 11건 + WaterSpirit 35건 = 46건, 17개 파일):
       ```bash
       grep -rn --include="*.cs" "Debug\.Log\|Debug\.LogWarning\|Debug\.LogError" Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit
       ```
       **파일 단위로 1행씩** 표에 기록한다 (한 파일의 여러 줄은 Line(s) 칼럼에 `55, 61, 78` 처럼 쉼표로 나열).
       Kind 칼럼은 `Debug.Log` / `Debug.LogWarning` / `Debug.LogError` 중 해당하는 것을 적는다.
       Reason 칼럼에는 로그 내용을 보고 **정리 대상인지 유지 대상인지** 판단해 적는다:
       - `개발용 상태 추적 — 제거 권장` (상태 전환/패턴 선택 추적 등)
       - `오류 진단용 — 유지 권장` (`LogError` / null 가드 실패 등 실제 오류 상황)

    2. **TODO/FIXME/HACK 주석**:
       ```bash
       grep -rni --include="*.cs" "TODO\|FIXME\|HACK\|XXX" Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit
       ```
       프로젝트 전체 기준값은 **0건**이다. 이 범위에서도 0건이면 표 아래에
       `> TODO/FIXME/HACK 주석: 이 범위에서 0건 (프로젝트 전체도 0건).` 한 줄을 적는다.
       0건이 아니면 각각을 행으로 추가하고 Kind 칼럼에 `TODO` 등을 적는다.

    3. **임시로 주석 처리된 테스트/디버그 코드**: Task 1의 D-07 주석 블록 스캔에서 찾은 것 중
       `Debug.Log` / 테스트용 하드코딩이 주석 처리된 것은 D-08 쪽에도 행을 추가한다
       (Kind = `주석처리 코드`). Task 1과 중복 기재해도 되며, ID는 D-08 계열로 새로 부여한다.
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md"; N=$(grep -c "^| A-D08-" "$F"); echo "D-08 rows: $N"; test "$N" -ge 12 || { echo "FAIL: too few D-08 rows"; exit 1; }; MISS=0; for f in $(grep -rl --include="*.cs" "Debug\.Log" Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit); do grep -qF "$f" "$F" || { echo "UNCOVERED: $f"; MISS=1; }; done; test "$MISS" -eq 0 && echo "ALL Debug.Log FILES COVERED"; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "^| A-D08-" <fragment>` >= 12
    - `grep -rl --include="*.cs" "Debug\.Log" Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit` 가 반환하는
      **17개 파일 경로 전부**가 fragment 안에 문자열로 존재한다 (verify 명령의 `ALL Debug.Log FILES COVERED` 출력).
    - `grep -cP '^\| A-D08-\d{2} \| Assets/Enemy/Water(Monster|Spirit)/[^|]+\.cs \| \d' <fragment>` 가
      `grep -c "^| A-D08-" <fragment>` 와 같다 (모든 행에 경로+줄번호 존재).
    - `grep -c "TODO/FIXME/HACK 주석: 이 범위에서 0건" <fragment>` == 1 (0건인 경우) 또는
      `grep -c "| TODO |" <fragment>` >= 1 (0건이 아닌 경우) — 둘 중 하나는 반드시 참.
    - `### D-08 일반 항목` 바로 다음 비어있지 않은 줄이 `(없음)` 이다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>D-08 표가 12행 이상이고, 이 범위에서 `Debug.Log` 를 포함하는 17개 파일이 빠짐없이 fragment에 등장하며, TODO/FIXME 0건 사실이 명시되어 있다.</done>
</task>

<task type="auto">
  <name>Task 3: D-10 복잡도 스캔 + D-09 후보 관찰 + 커버리지 표</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` — Task 1/2 결과
    - `.planning/PROJECT.md` — `## Key Architectural Decisions` (의도적 비공유 판단 근거)
    - `.planning/STATE.md` — `### Key Decisions` 중 Phase 7/8 항목 (SpiritCombatState 완전배제 vs WaterMonsterCombatState 가중치 감쇠, BuildCandidates 재구성 시점 차이 — **의도된 차이이므로 중복으로 보고하면 안 된다**)
    - 이 범위에서 200줄을 넘는 파일: `Assets/Enemy/WaterMonster/Script/WaterMonsterController.cs`(267줄), `Assets/Enemy/WaterSpirit/Script/States/Stage2CombatState.cs`(173줄), `Assets/Enemy/WaterMonster/Script/States/WaterMonsterCombatState.cs`(140줄)
  </read_first>

  <action>
    1. **D-10 복잡도 스캔.** 판정 기준을 다음 2개 중 하나라도 넘으면 기록한다 (주관 배제):
       - 메서드 본문이 **40줄 초과**
       - 한 메서드 안의 분기 토큰 합계가 **15 초과** — 토큰은 `if` `else if` `for` `foreach` `while`
         `switch` `case` `&&` `||` `?:` 를 센다
       파일별 줄 수 확인:
       ```bash
       find Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit -name "*.cs" -exec wc -l {} + | sort -rn
       ```
       각 항목을 `### D-10 회귀 위험 높음 — 신중 검토 필요` 표에 기록하고 Metric 칼럼에
       `{본문 줄수}줄 / 분기 {N}` 형식으로 정확한 수치를 적는다. `### D-10 일반 항목` 은 `(없음)` 유지.
       Reason 칼럼에는 **무엇과 무엇이 한 메서드에 섞여 있는지**를 적는다 (예: `후보 구성 + 가중치 선택 + 쿨다운 갱신 3책임 혼재`).
       D-10은 CONTEXT.md D-10에 따라 "권장" 등급이므로, 표 바로 위에
       `> D-10은 권장 수준 관찰이다. 실제 수정은 사용자 승인 필수 (CONTEXT.md D-10).` 한 줄을 넣는다.

    2. **D-09 후보 관찰.** `## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)` 아래에 이 범위 안에서
       눈에 띄는 반복 코드 형태를 불릿으로 적는다. 각 불릿은 반드시 `파일경로:줄번호` 를 2곳 이상 포함한다.
       **다음은 문서화된 의도적 차이이므로 후보로 올리지 않는다:**
       - `SpiritCombatState` 의 완전배제 연속금지 vs `WaterMonsterCombatState` 의 가중치 0.5배 감쇠
         (Phase 8 D-01a~c — SpiritCombatState 0줄 변경 보장을 위한 의도적 공존)
       - `SpiritCombatState` 의 `Enter()` 1회 후보 캐싱 vs `WaterMonsterCombatState` 의 매 호출 재구성
         (Phase 8 D-06c — 전투 중 페이즈 전환 때문에 복사 불가)
       - `WaterMonster` 계열과 `Spirit` 계열이 별도 엔티티로 구현된 것 자체
         (PROJECT.md "물의 정령 독립 구현" — 코드 공유는 기반 클래스 수준으로만 제한)
       후보가 하나도 없으면 `- (없음)` 한 줄을 적는다.

    3. **커버리지 표.** `## 스캔 커버리지` 아래에 `| # | File | Lines | Scanned |` 표를 만들고,
       `find Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit -name "*.cs" -type f | sort` 결과
       **38개 파일 전부**를 1행씩, 번호 1~38, 실제 줄 수, `yes` 로 기록한다. 누락 0이어야 한다.

    4. 마지막에 fragment 맨 아래 `## 요약` 섹션을 추가하고 다음 표를 넣는다:
       ```
       | 카테고리 | 항목 수 | 고위험 항목 수 |
       |----------|---------|----------------|
       | D-07 | {n} | {n} |
       | D-08 | {n} | {n} |
       | D-10 | {n} | {n} |
       ```
       숫자는 실제 표 행 수와 일치해야 한다 (Scope A는 전부 고위험이므로 두 칼럼이 같다).
  </action>

  <verify>
    <automated>F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md"; C=$(grep -cP '^\| \d+ \| Assets/Enemy/Water(Monster|Spirit)/[^|]+\.cs \| \d+ \| yes \|' "$F"); echo "coverage rows: $C"; test "$C" -eq 38 || { echo "FAIL: expected 38 coverage rows"; exit 1; }; MISS=0; for f in $(find Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit -name "*.cs" -type f | sort); do grep -qF "$f" "$F" || { echo "UNCOVERED: $f"; MISS=1; }; done; test "$MISS" -eq 0 || exit 1; grep -qF "## 요약" "$F" && grep -qF "> D-10은 권장 수준 관찰이다" "$F" && echo OK; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - 커버리지 표 행이 정확히 **38개**이고 정규식 `^\| \d+ \| Assets/Enemy/Water(Monster|Spirit)/[^|]+\.cs \| \d+ \| yes \|` 에 전부 매치한다.
    - `find Assets/Enemy/WaterMonster Assets/Enemy/WaterSpirit -name "*.cs"` 의 38개 경로가 전부 fragment 안에 문자열로 존재한다 (누락 0).
    - `grep -c "^| A-D10-" <fragment>` >= 3
    - `grep -cP '^\| A-D10-\d{2} \| Assets/[^|]+\.cs \| \d+-\d+ \|' <fragment>` 가 `grep -c "^| A-D10-" <fragment>` 와 같다 (모든 D-10 행이 `시작줄-끝줄` 범위를 가진다).
    - `grep -c "> D-10은 권장 수준 관찰이다. 실제 수정은 사용자 승인 필수 (CONTEXT.md D-10)." <fragment>` == 1
    - `grep -qF "## 요약" <fragment>` 성공, 요약 표의 D-07/D-08/D-10 항목 수가 각각 `grep -c "^| A-D07-"` / `"^| A-D08-"` / `"^| A-D10-"` 결과와 일치한다.
    - `## D-09 후보 관찰` 섹션에 최소 1줄(`- (없음)` 포함)이 존재하고, 내용이 있다면 각 불릿이 `.cs:` 를 2회 이상 포함한다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>fragment가 D-07/D-08/D-10 표 + D-09 후보 + 38행 커버리지 표 + 요약 표를 모두 갖추고, `Assets/` 는 0줄 변경이다.</done>
</task>

</tasks>

<verification>
```bash
F=".planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md"
# 1. 파일 존재 + 최소 분량
test -f "$F" && test $(wc -l < "$F") -ge 80
# 2. 11개 규정 헤딩 존재
# 3. 커버리지 38행
grep -cP '^\| \d+ \| Assets/Enemy/Water(Monster|Spirit)/[^|]+\.cs \| \d+ \| yes \|' "$F"   # == 38
# 4. 모든 findings 행이 file:line 을 가진다
grep -cP '^\| A-D(07|08|10)-\d{2} \| Assets/[^|]+\.cs \| \d' "$F"
# 5. 일반 항목 3개 섹션이 전부 (없음)  — Scope A 전 범위 고위험
grep -c '(없음)' "$F"   # >= 3
# 6. 소스 무수정 (핵심 게이트)
git status --porcelain Assets   # 출력 없어야 함
```
</verification>

<success_criteria>
- Scope A 38개 파일이 커버리지 표에 100% 등장한다.
- D-07 >= 6행, D-08 >= 12행, D-10 >= 3행이며 모든 행에 `Assets/...cs` 경로와 숫자 줄번호가 있다.
- 이 범위의 모든 발견 항목이 `회귀 위험 높음 — 신중 검토 필요` 하위 섹션에 들어가 있고 `일반 항목` 섹션은 `(없음)` 이다 (D-06 시각적 분리).
- `SpiritController.ChargeRange` 가 `삭제 권장 아님` 사유와 함께 기재되어 있다.
- `git status --porcelain Assets` 가 비어 있다 — 소스 코드 0줄 변경 (CONTEXT.md D-01/D-02).
</success_criteria>

<output>
완료 후 `.planning/phases/13-codebase-cleanup-audit/13-01-SUMMARY.md` 를 생성한다.
SUMMARY에는 카테고리별 발견 건수와, Plan 05가 이어받아야 할 D-09 후보 목록을 명시한다.
</output>
