---
phase: 13-codebase-cleanup-audit
plan: 05
type: execute
wave: 2
depends_on: ["13-01", "13-02", "13-03", "13-04"]
files_modified:
  - .planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [D-01, D-02, D-04, D-06, D-09]

must_haves:
  truths:
    - "단일 최종 보고서 13-AUDIT-REPORT.md 가 존재하고 Assets/ 아래 168개 .cs 파일 전부의 스캔 커버리지를 담는다"
    - "D-09(중복 로직) 섹션이 4개 fragment의 후보를 교차 검증해 채워지고, 문서화된 의도적 비공유는 제외 근거와 함께 배제 목록에 남는다"
    - "회귀 위험 높은 항목이 일반 항목과 시각적으로 분리된 별도 섹션에 모여 있다 (D-06)"
    - "CP949 인코딩 위험 파일 46개가 프로젝트 전체 단일 목록으로 집계된다 (D-04)"
    - "모든 항목에 체크박스가 붙어 사용자가 항목별로 승인/보류를 표시할 수 있다 (D-01 2단계 워크플로)"
    - "소스 .cs 파일은 단 한 줄도 수정되지 않는다 (D-02 — 삭제는 승인 이후 별도 작업)"
  artifacts:
    - path: ".planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md"
      provides: "Phase 13 최종 통합 감사 보고서 (D-07/D-08/D-09/D-10 + CP949 + 승인 체크리스트)"
      contains: "## D-09 — 중복 로직 (DRY 위반)"
      min_lines: 200
  key_links:
    - from: "13-AUDIT-REPORT.md"
      to: "13-FINDINGS-A/B/C/D fragment 4종"
      via: "항목 ID 접두사 A-/B-/C-/D- 를 보존한 채 병합"
      pattern: "^\\| \\[ \\] \\| [ABCD]-D(07|08|09|10)-\\d{2}"
---

<objective>
Wave 1의 4개 fragment(`13-FINDINGS-A/B/C/D`)를 병합하고, 4개 fragment가 각자 볼 수 없었던
**프로젝트 전역 관점의 D-09(중복 로직)** 분석을 수행해, 단일 최종 산출물
`.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md` 를 만든다.

Purpose: D-09는 "여러 보스 스크립트에 반복되는 비슷한 패턴"이므로 한 범위 안에서는 판정할 수 없다 —
Scope A(WaterMonster/WaterSpirit)와 Scope B(NewBoss/Tutorial)를 **가로질러** 봐야 한다. 또한 CP949
파일 목록(46개)과 커버리지 총계(168개 파일)도 전역 집계가 필요하다. 마지막으로 CONTEXT.md D-01의
2단계 워크플로(보고 → 사용자 항목별 승인 → 수정)를 실제로 굴릴 수 있도록 **항목마다 체크박스**를 단다.
Output: `.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md`

**보고서 전용 phase다. `Assets/` 아래 어떤 파일도 생성/수정/삭제하지 않는다 (CONTEXT.md D-01/D-02).**
실제 삭제/리팩토링은 사용자가 이 보고서의 체크박스를 승인한 뒤 진행하는 별도 작업이다.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md

<inputs>
Wave 1이 생성한 fragment 4개 (전부 존재해야 이 plan을 시작할 수 있다):
- `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` — 38파일, 항목 ID 접두사 `A-`
- `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md` — 42파일, 접두사 `B-`
- `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md` — 32파일, 접두사 `C-`
- `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-D-core-map-editor-imported.md` — 56파일, 접두사 `D-`

합계: **168개 .cs 파일** = `find Assets -name "*.cs" -type f | wc -l` 의 값과 일치해야 한다.
</inputs>

<global_baselines>
<!-- 계획 단계에서 실측한 프로젝트 전역 수치. 최종 보고서의 "전체 통계" 표는 이 값과 대조 가능해야 한다. -->

| 지표 | 값 | 재측정 명령 |
|------|-----|------------|
| `.cs` 파일 총수 | 168 | `find Assets -name "*.cs" -type f \| wc -l` |
| 총 라인 수 | 12372 | `find Assets -name "*.cs" -type f -exec cat {} + \| wc -l` |
| CP949(비-UTF-8) 파일 | 46 | 아래 iconv 루프 |
| `Debug.Log*` 발생 건수 | 226 | `grep -r --include="*.cs" -o "Debug\.Log" Assets \| wc -l` |
| `Debug.Log*` 포함 파일 수 | 92 | `grep -rl --include="*.cs" "Debug\.Log" Assets \| wc -l` |
| TODO/FIXME/HACK/XXX | **0** | `grep -rni --include="*.cs" "TODO\|FIXME\|HACK\|XXX" Assets \| wc -l` |

CP949 전역 목록 생성 명령 (공백 경로 안전):
```bash
find Assets -name "*.cs" -type f -print0 \
  | while IFS= read -r -d '' f; do iconv -f UTF-8 -t UTF-8 "$f" >/dev/null 2>&1 || printf '%s\n' "$f"; done | sort
```
범위별 CP949 분포: Scope A 0개 / Scope B 16개 / Scope C 4개 / Scope D 26개 = 46개.
</global_baselines>

<d09_intentional_exclusions>
<!-- CONTEXT.md D-09: "왜 공유 안 됐는지 이유가 있는 의도된 중복은 문제로 보고하지 않는다".
     아래는 PROJECT.md / STATE.md 에 기록된 확정 목록이다. D-09 섹션의 "배제 목록"에
     근거 출처와 함께 그대로 기록해야 한다 — 배제했다는 사실 자체가 보고서의 가치다. -->

| # | 의도된 비공유/중복 | 근거 |
|---|---------------------|------|
| E-01 | 코드 공유를 `BossController` / `BossStatsSystem` 기반 클래스 수준으로만 제한하는 원칙 자체 | PROJECT.md `## Key Architectural Decisions`, STATE.md `### Key Decisions` (2회 반복 기재) |
| E-02 | 물의 정령(`SpiritController`)을 `WaterMonsterController` 와 별도 독립 엔티티로 구현 | PROJECT.md `## Key Architectural Decisions` "물의 정령 독립 구현" |
| E-03 | `SpiritCombatState` 의 직전 패턴 **완전배제** vs `WaterMonsterCombatState` 의 **가중치 0.5배 감쇠** | STATE.md Phase 8 Plan 1 (D-01a~c) — SpiritCombatState.cs 0줄 변경 보장 목적 |
| E-04 | `SpiritCombatState` 의 `Enter()` 1회 후보 캐싱 vs `WaterMonsterCombatState` 의 매 호출 `BuildCandidates()` 재구성 | STATE.md Phase 8 Plan 2 (D-06c) — 전투 중 페이즈 전환 때문에 복사 불가 |
| E-05 | `CombatState.SelectWeightedPattern` 이 오버로드 대신 기본값 3번째 파라미터를 쓰는 것 | STATE.md Phase 8 Plan 1 — 기존 2-인자 호출부 무변경 보장 (D-05a 회귀 방지) |
| E-06 | 저장 트리거가 Group A(`HandleDeath()`)와 Group B(`Die()` 오버라이드) 두 방식으로 갈린 것 | STATE.md Phase 11 Plan 3 — 두 보스 아키텍처가 실제로 다름 (`HP.OnDeath` 이벤트 유무) |
| E-07 | `SpiritController.ChargeRange` 데드 필드 유지 | STATE.md Phase 7 — 재활용 시 `SpiritFarProjectile` 영구 선택 불가 발생 |
| E-08 | `CameraZoomTrigger` 가 필드 0개인 것 | ROADMAP Phase 9 D-02/D-04/D-05 — 어느 보스 구역에나 드롭 가능해야 함, 줌 값은 `CameraController` Inspector 소유 |
| E-09 | `CameraController` 의 보스존 레거시 `Vector3.Lerp` 경로와 일반 스테이지 데드존 경로 분리 | ROADMAP Phase 10 D-15 |
| E-10 | `SaveLoadManager` 가 `async`/`await` 대신 코루틴을 쓰는 것 | ROADMAP Phase 11 성공 기준 6 — 프로젝트 무-async 컨벤션 |
| E-11 | `HP.cs` 와 `BossStatsSystem` 이 별개 HP 체계인 것 | ROADMAP Phase 11 성공 기준 8 / Phase 12 성공 기준 6 — `HP.cs` 0줄 변경이 명시 계약 |
| E-12 | `Assets/ImportedAsset/**` 내부 중복 | 외부 도입 에셋(벤더) — 재임포트 시 변경 소실 |
</d09_intentional_exclusions>

<report_structure>
<!-- 최종 보고서의 정확한 구조. 헤딩 문자열은 acceptance criteria에서 grep으로 검증된다. -->

```markdown
# Phase 13 — 코드베이스 정리 감사 보고서

**Phase:** 13-codebase-cleanup-audit
**Generated:** 2026-08-19
**Scope:** `Assets/` 아래 전체 C# 스크립트 168개 파일 / 12372줄 (CONTEXT.md D-03)
**Status:** 보고 단계 완료 — 코드 미수정. 항목별 사용자 승인 대기 (CONTEXT.md D-01/D-02)

## 이 보고서를 읽는 법

- 항목마다 체크박스가 있다. **승인하려는 항목만 `[x]` 로 바꿔서 알려주면** 그 항목만 수정 작업으로 넘어간다.
- `## 회귀 위험 높음` 섹션 항목은 이미 Play 모드로 검증된 코드에 있는 것이다 — 더 신중하게 판단해야 한다 (D-05/D-06).
- `삭제 권장 아님` 이 붙은 항목은 "발견은 됐지만 의도적으로 남긴 것"이다. 재확인용으로만 나열되어 있다.
- D-10은 **권장** 등급이다 (주관 판단이 개입, CONTEXT.md D-10).

## 전체 통계

| 지표 | 값 |
|------|-----|
| 스캔한 .cs 파일 | 168 |
...

| 카테고리 | 총 항목 | 일반 | 회귀 위험 높음 |
|----------|---------|------|----------------|
| D-07 죽은 코드 | n | n | n |
| D-08 TODO/디버그 잔재 | n | n | n |
| D-09 중복 로직 | n | n | n |
| D-10 긴/복잡한 함수 (권장) | n | n | n |

## 일반 항목

### D-07 — 죽은 코드
| 승인 | ID | File | Line(s) | Symbol | Reason |
|------|----|------|---------|--------|--------|
| [ ] | A-D07-01 | ... | ... | ... | ... |

### D-08 — TODO/FIXME 잔재 및 임시 디버그 코드
### D-09 — 중복 로직 (DRY 위반)
### D-10 — 과도하게 긴/복잡한 함수 (권장)

## 회귀 위험 높음 — 신중 검토 필요

> 아래 항목은 이미 Play 모드로 검증된 코드에 있다 (CONTEXT.md D-05/D-06). 수정 시 회귀 위험이 높으므로
> 개별적으로 더 신중하게 판단할 것.

### D-07 — 죽은 코드 (고위험)
### D-08 — TODO/FIXME 잔재 및 임시 디버그 코드 (고위험)
### D-09 — 중복 로직 (고위험)
### D-10 — 과도하게 긴/복잡한 함수 (고위험, 권장)

## D-09 배제 목록 — 문서화된 의도적 비공유

| # | 의도된 비공유/중복 | 근거 |

## D-04 — CP949 인코딩 위험 파일 (전역 46개)

## 기타 — 코드 외 정리 항목

## 스캔 커버리지 요약

| Scope | 대상 | 파일 수 | fragment |
|-------|------|---------|----------|
| A | WaterMonster / WaterSpirit | 38 | 13-FINDINGS-A-watermonster-waterspirit.md |
| B | NewBoss / Tutorial / Legacy Enemies | 42 | 13-FINDINGS-B-newboss-tutorial-legacy.md |
| C | Player / Camera / SaveSystem | 32 | 13-FINDINGS-C-player-camera-savesystem.md |
| D | Core Script / Map / Editor / ImportedAsset | 56 | 13-FINDINGS-D-core-map-editor-imported.md |
| | **합계** | **168** | |

## 다음 단계 (D-01 2단계 워크플로)
```
</report_structure>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전역 D-09 중복 로직 교차 분석</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` — `## D-09 후보 관찰` 섹션
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md` — `## D-09 후보 관찰` 섹션
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md` — `## D-09 후보 관찰` 섹션
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-D-core-map-editor-imported.md` — `## D-09 후보 관찰` 섹션
    - `.planning/PROJECT.md` — `## Key Architectural Decisions` (배제 판단 근거 원문)
    - `.planning/STATE.md` — `### Key Decisions` 전체 (배제 판단 근거 원문)
    - `Assets/Enemy/NewBoss/Script/States/CombatState.cs` — 공유 헬퍼 원문 (무엇이 이미 공유되어 있는지)
    - `Assets/Enemy/NewBoss/Script/BossController.cs`, `Assets/Enemy/NewBoss/Script/BossStatesSystem.cs` — 기반 클래스 원문
  </read_first>

  <action>
    작업 산출물은 **중간 파일이 아니라 최종 보고서의 D-09 섹션 초안**이다. 이 Task에서는
    `.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md` 를 **새로 만들고 D-09 관련 섹션만** 채운다.
    나머지 섹션은 Task 2가 채운다.

    1. 4개 fragment의 `## D-09 후보 관찰 (raw — Plan 05에서 교차 검증)` 불릿을 전부 수집한다.

    2. **전역 중복 시그니처 스캔**을 추가로 수행한다 (fragment가 범위 안에서만 봐서 놓친 것 회수):
       ```bash
       # 프로젝트 전체 메서드 시그니처를 이름 기준으로 그룹핑 — 2개 이상 파일에 같은 이름이 있으면 후보
       grep -rnE '^\s*(public|private|protected|internal)\s+[A-Za-z_<>,\[\]\. ]+\s+[A-Za-z_][A-Za-z0-9_]*\s*\(' \
         --include="*.cs" Assets \
         | sed -E 's/.*[[:space:]]([A-Za-z_][A-Za-z0-9_]*)[[:space:]]*\(.*/\1/' \
         | sort | uniq -c | sort -rn | head -60
       ```
       빈도가 높은 이름 중 Unity 매직 메서드(`Awake` `Start` `Update` `OnTriggerEnter2D` 등)와
       인터페이스 멤버(`Enter` `Execute` `Exit`)를 제외한 것을 조사 대상으로 삼고,
       실제로 **본문이 유사한지** 해당 파일들을 열어 확인한다. 이름만 같고 본문이 다르면 후보가 아니다.

    3. 각 후보를 `<d09_intentional_exclusions>` 의 E-01~E-12와 대조한다.
       - **어느 하나라도 해당하면** → D-09 항목으로 보고하지 **않는다**. 대신
         `## D-09 배제 목록 — 문서화된 의도적 비공유` 표에 남긴다.
       - **해당하지 않으면** → D-09 항목으로 채택한다.

    4. `13-AUDIT-REPORT.md` 를 새로 만들고 아래 3개 섹션을 채운다:
       - `### D-09 — 중복 로직 (DRY 위반)` (일반 항목용)
       - `### D-09 — 중복 로직 (고위험)` (고위험 항목용)
       - `## D-09 배제 목록 — 문서화된 의도적 비공유`

       D-09 항목 표 형식 (다른 카테고리와 칼럼 수를 맞추기 위해 위치 정보는 Line(s) 칼럼에 통합):
       ```
       | 승인 | ID | File | Line(s) | Symbol | Reason |
       |------|----|------|---------|--------|--------|
       | [ ] | X-D09-01 | Assets/a.cs + Assets/b.cs | a.cs:30-52, b.cs:44-66 | `ApplyKnockback` | 넉백 계산 블록이 두 파일에 동일 복제 — BossController 로 승격 가능 |
       ```
       ID 접두사는 후보를 제공한 fragment의 문자를 쓴다 (여러 범위에 걸친 항목은 `X-D09-nn` 을 쓴다).
       File 칼럼에는 중복된 **모든** 파일을 ` + ` 로 연결해 적고, Line(s) 칼럼에는 각 파일의 줄 범위를 적는다.

       위험 티어 판정: 중복에 참여한 파일 중 **하나라도** 아래 경로 접두사에 해당하면 고위험 표로 보낸다.
       ```
       Assets/Enemy/WaterMonster/   Assets/Enemy/WaterSpirit/   Assets/Enemy/NewBoss/   Assets/Enemy/Tutorial/
       Assets/Camera/               Assets/SaveSystem/
       Assets/Player/Script/PlayerStats.cs   Assets/Player/Script/PlayerController.cs   Assets/Player/Script/InputHandler.cs
       Assets/Script/HP.cs          Assets/Script/Combat/DamageInfo.cs
       Assets/map/script/Checkpoint.cs   Assets/map/script/PlayerSpawner.cs   Assets/map/script/GameManager.cs
       ```

       배제 목록 표는 `<d09_intentional_exclusions>` 의 12행을 **근거 칼럼까지 그대로** 옮긴다.
       배제 목록 위에 다음 문장을 넣는다:
       `> 아래는 "중복처럼 보이지만 문서화된 이유가 있어 의도적으로 공유하지 않은" 항목이다. CONTEXT.md D-09에 따라 문제로 보고하지 않는다. 재확인용으로만 나열한다.`

    **금지:** `Assets/` 아래 어떤 파일도 Write/Edit 하지 않는다.
  </action>

  <verify>
    <automated>R=".planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md"; test -f "$R" || exit 1; grep -qF "### D-09 — 중복 로직 (DRY 위반)" "$R" || { echo "MISSING D-09 normal"; exit 1; }; grep -qF "### D-09 — 중복 로직 (고위험)" "$R" || { echo "MISSING D-09 high"; exit 1; }; grep -qF "## D-09 배제 목록 — 문서화된 의도적 비공유" "$R" || { echo "MISSING exclusions"; exit 1; }; E=$(grep -cP '^\| E-\d{2} \|' "$R"); echo "exclusion rows: $E"; test "$E" -eq 12 || { echo "FAIL: expected 12 exclusion rows"; exit 1; }; N=$(grep -cP '^\| \[ \] \| [ABCDX]-D09-\d{2} \|' "$R"); echo "D-09 rows: $N"; test "$N" -ge 3 || { echo "FAIL: too few D-09 items"; exit 1; }; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md` 가 생성되었다.
    - `### D-09 — 중복 로직 (DRY 위반)`, `### D-09 — 중복 로직 (고위험)`, `## D-09 배제 목록 — 문서화된 의도적 비공유` 3개 헤딩이 전부 존재한다.
    - 배제 목록 표 행이 정확히 **12개**다: `grep -cP '^\| E-\d{2} \|' <report>` == 12, 그리고
      `E-01`~`E-12` 각각이 정확히 1회씩 등장한다.
    - D-09 항목이 3건 이상이다: `grep -cP '^\| \[ \] \| [ABCDX]-D09-\d{2} \|' <report>` >= 3
    - 모든 D-09 행의 Line(s) 칼럼이 `.cs:` 를 2회 이상 포함한다 (중복은 최소 2곳이므로):
      `grep -P '^\| \[ \] \| [ABCDX]-D09-\d{2} \|' <report> | grep -cP '\.cs:\d+.*\.cs:\d+'` 가 D-09 행 수와 같다.
    - `grep -c "> 아래는 \"중복처럼 보이지만 문서화된 이유가 있어 의도적으로 공유하지 않은\" 항목이다." <report>` == 1
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>13-AUDIT-REPORT.md 가 생성되고 D-09 일반/고위험 표와 12행 배제 목록이 채워졌으며, 모든 D-09 항목이 최소 2개 파일의 `파일:줄번호` 를 갖고, `Assets/` 는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 2: 4개 fragment 병합 + 전역 CP949/통계/커버리지 집계</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md` — Task 1이 만든 현재 상태
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-A-watermonster-waterspirit.md` — 전체
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-B-newboss-tutorial-legacy.md` — 전체
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-C-player-camera-savesystem.md` — 전체
    - `.planning/phases/13-codebase-cleanup-audit/13-FINDINGS-D-core-map-editor-imported.md` — 전체
    - `.planning/phases/13-codebase-cleanup-audit/13-CONTEXT.md` — D-01/D-02 (2단계 워크플로 문구 근거)
  </read_first>

  <action>
    `13-AUDIT-REPORT.md` 를 `<report_structure>` 의 **전체 구조로 완성**한다. Task 1이 만든 D-09 섹션 3개는
    구조상 올바른 위치로 옮기되 내용은 보존한다.

    1. **헤더 4줄**을 정확히 이렇게 쓴다:
       ```
       **Phase:** 13-codebase-cleanup-audit
       **Generated:** 2026-08-19
       **Scope:** `Assets/` 아래 전체 C# 스크립트 168개 파일 / 12372줄 (CONTEXT.md D-03)
       **Status:** 보고 단계 완료 — 코드 미수정. 항목별 사용자 승인 대기 (CONTEXT.md D-01/D-02)
       ```

    2. `## 이 보고서를 읽는 법` 섹션을 `<report_structure>` 의 4개 불릿 그대로 쓴다.

    3. `## 전체 통계` 섹션에 두 개의 표를 넣는다:
       - 지표 표: `<global_baselines>` 의 6개 지표를 **재측정해서** 채운다 (계획서 값과 다르면 실측값을 쓰고
         차이를 각주로 남긴다).
       - 카테고리 표: `| 카테고리 | 총 항목 | 일반 | 회귀 위험 높음 |` 4행(D-07/D-08/D-09/D-10).
         숫자는 병합 후 실제 표 행 수와 일치해야 한다.

    4. **항목 병합.** 4개 fragment의 D-07/D-08/D-10 표 행을 전부 옮긴다. 규칙:
       - fragment의 `### D-0X 일반 항목` 행 → 보고서 `## 일반 항목` 아래 해당 카테고리 표
       - fragment의 `### D-0X 회귀 위험 높음 — 신중 검토 필요` 행 → 보고서 `## 회귀 위험 높음 — 신중 검토 필요` 아래 해당 카테고리 표
       - **항목 ID(`A-D07-01` 등)를 절대 바꾸지 않는다** — fragment 역추적에 필요하다.
       - 각 행 맨 앞에 `| [ ] ` 승인 체크박스 칼럼을 새로 추가한다. 최종 칼럼 순서:
         `| 승인 | ID | File | Line(s) | Symbol | Reason |`
         (D-08은 Symbol 자리에 Kind, D-10은 Symbol 뒤에 Metric 칼럼이 추가되므로
         `| 승인 | ID | File | Line(s) | Symbol | Metric | Reason |` 를 쓴다.)
       - **항목을 요약하거나 병합하지 않는다.** fragment의 모든 행이 보고서에 1:1로 나타나야 한다.

    5. `## 회귀 위험 높음 — 신중 검토 필요` 헤딩 바로 아래에 다음 인용문을 넣는다:
       `> 아래 항목은 이미 Play 모드로 검증된 코드에 있다 (CONTEXT.md D-05/D-06). 수정 시 회귀 위험이 높으므로 개별적으로 더 신중하게 판단할 것.`

    6. `## D-04 — CP949 인코딩 위험 파일 (전역 46개)` 섹션을 만든다.
       `<global_baselines>` 의 iconv 루프를 **직접 실행**해 46개 경로를 얻고, 4개 fragment의 CP949 표
       (B 16개 + C 4개 + D 26개)와 합집합이 일치하는지 대조한다. 표 형식:
       `| # | File | Scope | 티어 |` 로 1~46행. 섹션 상단에 다음 문장을 넣는다:
       `> 이 파일들은 CP949 인코딩이다. 실제 수정 단계에서는 표준 Read/Edit 왕복이 비-ASCII 바이트를 U+FFFD로 훼손시키므로 \`git show HEAD:<path>\` + 순수 바이트 스크립트 프로토콜이 필요하다 (CONTEXT.md D-04, STATE.md Phase 11 Plan 3 기록).`
       하단에 `Assets/Camera/Script/CameraController.cs` 의 비-ASCII 5줄 게이트 비고(Scope C fragment에서 가져옴)를 넣는다.

    7. `## 기타 — 코드 외 정리 항목` 섹션에 Scope D fragment의 동명 섹션 내용(`InGame.unity` 스테일 엔트리 등)을 옮긴다.

    8. `## 스캔 커버리지 요약` 섹션에 `<report_structure>` 의 5행 표(A 38 / B 42 / C 32 / D 56 / 합계 168)를 넣고,
       각 fragment 파일명을 링크로 적는다. 표 아래에 다음 문장을 넣는다:
       `> 파일 단위 상세 커버리지는 각 fragment의 \`## 스캔 커버리지\` 표를 참조한다.`

    9. `## 다음 단계 (D-01 2단계 워크플로)` 섹션에 다음 내용을 쓴다:
       ```
       1. 사용자가 이 보고서를 읽고, 진행할 항목의 `[ ]` 를 `[x]` 로 바꾼다 (또는 항목 ID를 지정해 알려준다).
       2. 승인된 항목만 별도 작업(quick task 또는 후속 phase)에서 실제로 삭제/리팩토링한다.
       3. `## 회귀 위험 높음` 섹션 항목은 수정 후 해당 보스/카메라의 `Check.md` Play 모드 체크리스트를 재수행한다.
       4. CP949 파일을 수정할 때는 `git show HEAD:<path>` + 순수 바이트 스크립트 프로토콜을 쓴다 (D-04).
       5. `삭제 권장 아님` 항목은 승인하지 않는 것이 기본값이다.
       ```
  </action>

  <verify>
    <automated>R=".planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md"; for h in "## 이 보고서를 읽는 법" "## 전체 통계" "## 일반 항목" "### D-07 — 죽은 코드" "### D-08 — TODO/FIXME 잔재 및 임시 디버그 코드" "### D-09 — 중복 로직 (DRY 위반)" "### D-10 — 과도하게 긴/복잡한 함수 (권장)" "## 회귀 위험 높음 — 신중 검토 필요" "## D-09 배제 목록 — 문서화된 의도적 비공유" "## D-04 — CP949 인코딩 위험 파일 (전역 46개)" "## 기타 — 코드 외 정리 항목" "## 스캔 커버리지 요약" "## 다음 단계 (D-01 2단계 워크플로)"; do grep -qF "$h" "$R" || { echo "MISSING: $h"; exit 1; }; done; CP=$(grep -cP '^\| \d+ \| Assets/[^|]+\.cs \| [ABCD] \|' "$R"); echo "CP949 rows: $CP"; test "$CP" -eq 46 || { echo "FAIL: expected 46 CP949 rows"; exit 1; }; for L in A B C D; do FR=$(ls .planning/phases/13-codebase-cleanup-audit/13-FINDINGS-$L-*.md); SRC=$(grep -cP "^\| $L-D(07|08|10)-\d{2} \|" "$FR"); DST=$(grep -cP "^\| \[ \] \| $L-D(07|08|10)-\d{2} \|" "$R"); echo "scope $L: fragment=$SRC report=$DST"; test "$SRC" -eq "$DST" || { echo "FAIL: scope $L row count mismatch"; exit 1; }; done; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - verify의 **13개 헤딩 문자열**이 전부 존재한다.
    - `grep -c '^\*\*Scope:\*\* `Assets/` 아래 전체 C# 스크립트 168개 파일 / 12372줄 (CONTEXT.md D-03)$' <report>` == 1
    - CP949 표 행이 정확히 **46개**다: `grep -cP '^\| \d+ \| Assets/[^|]+\.cs \| [ABCD] \|' <report>` == 46,
      그리고 iconv 루프가 반환하는 46개 경로가 하나도 빠짐없이 표에 존재한다.
    - **행 수 보존 검증 (핵심):** 각 Scope 문자 `A`/`B`/`C`/`D` 에 대해
      fragment의 `^\| {L}-D(07|08|10)-\d{2} \|` 행 수 == 보고서의 `^\| \[ \] \| {L}-D(07|08|10)-\d{2} \|` 행 수.
      네 범위 전부 일치해야 한다 (요약/누락 없이 1:1 병합).
    - `grep -c "> 아래 항목은 이미 Play 모드로 검증된 코드에 있다 (CONTEXT.md D-05/D-06)." <report>` == 1
    - `## 스캔 커버리지 요약` 표에 `| | **합계** | **168** | |` 행이 존재한다.
    - `## 전체 통계` 의 카테고리 표에서 D-07/D-08/D-09/D-10 각 행의 `총 항목` 값이
      `grep -cP '^\| \[ \] \| [ABCDX]-D07-'` 등 실제 행 수와 일치한다.
    - `## 다음 단계 (D-01 2단계 워크플로)` 에 5개 번호 항목이 있고 `git show HEAD:<path>` 문자열을 포함한다.
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>13-AUDIT-REPORT.md 가 13개 규정 섹션을 갖추고, 4개 fragment의 D-07/D-08/D-10 행이 범위별로 개수 일치하게 1:1 병합되었으며, CP949 46행 전역 표와 커버리지 합계 168이 기록되고, `Assets/` 는 0줄 변경이다.</done>
</task>

<task type="auto">
  <name>Task 3: 최종 무결성 검증 + ROADMAP/STATE 반영</name>
  <files>.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md, .planning/ROADMAP.md, .planning/STATE.md</files>

  <read_first>
    - `.planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md` — Task 1/2 결과 전체
    - `.planning/ROADMAP.md` — `### Phase 13:` 절
    - `.planning/STATE.md` — `## Current Position`, `### Key Decisions`, `### Active TODOs`
  </read_first>

  <action>
    1. **무결성 검증 스크립트를 실제로 실행**하고 결과를 보고서 맨 아래
       `## 무결성 검증 결과` 섹션에 **명령 + 실제 출력값** 형태로 기록한다. 검증 6종:
       ```bash
       R=".planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md"
       # V1: 커버리지 총합 == 실제 파일 수
       find Assets -name "*.cs" -type f | wc -l                       # 168 이어야 함
       # V2: 4개 fragment 커버리지 행 합계 == 168
       for L in A B C D; do grep -cP '^\| \d+ \| Assets/[^|]+\.cs \|' .planning/phases/13-codebase-cleanup-audit/13-FINDINGS-$L-*.md; done
       # V3: 모든 항목에 file:line 이 있다 (경로 없는 행 = 0)
       grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \| Assets/[^|]+\.cs \|' "$R"
       grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \|' "$R"   # 위 값과 같아야 함
       # V4: CP949 46개 전수 등재
       find Assets -name "*.cs" -type f -print0 | while IFS= read -r -d '' f; do iconv -f UTF-8 -t UTF-8 "$f" >/dev/null 2>&1 || grep -qF "$f" "$R" || echo "UNLISTED $f"; done
       # V5: 승인 체크박스가 전부 미체크 상태다 (보고 단계이므로)
       grep -cP '^\| \[x\] \|' "$R"                                   # 0 이어야 함
       # V6: 소스 무수정 (D-01/D-02 핵심 게이트)
       git status --porcelain Assets                                   # 출력 없어야 함
       ```
       각 검증에 `PASS` / `FAIL` 을 명시하고, FAIL이 있으면 해당 항목을 고친 뒤 재실행한다.

    2. `.planning/ROADMAP.md` 의 `### Phase 13:` 절을 갱신한다:
       - `**Goal:** [To be planned]` → 실제 목표 문장으로 교체:
         `**Goal:** \`Assets/\` 아래 168개 C# 스크립트 전체를 스캔해 죽은 코드(D-07) / TODO·디버그 잔재(D-08) / 중복 로직(D-09) / 과도하게 긴 함수(D-10) 4개 카테고리로 분류한 감사 보고서를 만든다. 이미 Play 모드로 검증된 보스·카메라·세이브 코드의 발견 항목은 "회귀 위험 높음"으로 분리하고, CP949 인코딩 파일은 별도 목록으로 표시한다. **이 phase는 보고서만 만든다 — 실제 삭제/리팩토링은 사용자가 항목별로 승인한 뒤 별도 작업으로 진행한다 (D-01/D-02).**`
       - `**Requirements**: TBD` → `**Requirements**: D-01 ~ D-10 (13-CONTEXT.md 잠금 결정 — 공식 REQ-ID 미할당 페이즈)`
       - `**Plans:** 0 plans` → `**Plans:** 5 plans`
       - Plans 목록의 `- [ ] TBD (run /gsd:plan-phase 13 to break down)` 를 5개 플랜 항목으로 교체하고,
         완료된 플랜은 `[x]` 로 표시한다.
       - `**Execution Waves:**` 표를 추가한다 (Wave 1: 13-01/13-02/13-03/13-04 전부 autonomous yes,
         Wave 2: 13-05 autonomous yes).

    3. `.planning/STATE.md` 를 갱신한다:
       - `## Current Position` 을 Phase 13 완료 상태로 갱신
       - `### Key Decisions` 에 다음 항목 추가:
         `- Phase 13은 보고서 전용 phase — \`Assets/\` 0줄 변경을 각 플랜의 인수 기준(\`git status --porcelain Assets\` 빈 출력)으로 강제했다. 실제 삭제/리팩토링은 \`13-AUDIT-REPORT.md\` 의 체크박스를 사용자가 승인한 뒤 별도 작업으로 진행한다 (CONTEXT.md D-01/D-02, CLAUDE.md 3번 원칙의 범위 한정 예외) (Phase 13)`
         `- CP949(비-UTF-8) 인코딩 .cs 파일이 프로젝트 전체에 46개 존재한다 — 그동안 Phase 9~12에서 개별적으로 발견해온 \`Checkpoint.cs\`/\`WoodBossController.cs\` 2개는 빙산의 일각이었다. 분포: Enemy 16 / Player 4 / Script+map+ImportedAsset 26 / Camera·SaveSystem·Editor 0. 전수 목록은 \`13-AUDIT-REPORT.md\` \`## D-04\` 섹션 (Phase 13)`
         `- 프로젝트 전체 TODO/FIXME/HACK 주석이 0건이고, \`Debug.Log*\` 는 92개 파일에 226건 — D-08 카테고리는 사실상 Debug.Log 정리와 주석처리 코드 정리 두 갈래다 (Phase 13)`
       - `### Active TODOs` 에 다음 추가:
         `- Phase 13 감사 보고서(\`13-AUDIT-REPORT.md\`) 항목별 사용자 승인 대기. 승인된 항목만 후속 작업으로 삭제/리팩토링한다 (D-01 2단계).`
       - `### Roadmap Evolution` 의 Phase 13 항목은 이미 존재하므로 그대로 둔다.
  </action>

  <verify>
    <automated>R=".planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md"; grep -qF "## 무결성 검증 결과" "$R" || { echo "MISSING integrity section"; exit 1; }; test $(grep -c "PASS" "$R") -ge 6 || { echo "FAIL: fewer than 6 PASS marks"; exit 1; }; test $(grep -cP '^\| \[x\] \|' "$R") -eq 0 || { echo "FAIL: pre-approved checkboxes found"; exit 1; }; T=$(grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \|' "$R"); P=$(grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \| Assets/[^|]+\.cs \|' "$R"); echo "items=$T withpath=$P"; test "$T" -eq "$P" || { echo "FAIL: some items lack a file path"; exit 1; }; grep -qF "[To be planned]" .planning/ROADMAP.md && { echo "FAIL: ROADMAP still has placeholder"; exit 1; }; grep -qF "**Plans:** 5 plans" .planning/ROADMAP.md || { echo "FAIL: ROADMAP plan count"; exit 1; }; grep -qF "CP949(비-UTF-8) 인코딩 .cs 파일이 프로젝트 전체에 46개 존재한다" .planning/STATE.md || { echo "FAIL: STATE key decision missing"; exit 1; }; git status --porcelain Assets | grep . && exit 1 || echo "Assets untouched"</automated>
  </verify>

  <acceptance_criteria>
    - `## 무결성 검증 결과` 섹션이 존재하고 V1~V6 각각에 실제 명령과 출력값, `PASS`/`FAIL` 표기가 있다 (`PASS` 최소 6회).
    - `grep -cP '^\| \[x\] \|' <report>` == 0 (보고 단계이므로 사전 승인된 항목이 없어야 한다).
    - 모든 항목 행이 `Assets/...cs` 경로를 갖는다: `grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \|'` ==
      `grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \| Assets/[^|]+\.cs \|'`
    - iconv 루프가 반환하는 CP949 46개 경로가 전부 보고서에 존재한다 (V4의 `UNLISTED` 출력 0줄).
    - `.planning/ROADMAP.md` 에 `[To be planned]` 문자열이 **더 이상 없다**.
    - `.planning/ROADMAP.md` Phase 13 절이 `**Plans:** 5 plans` 와 `**Requirements**: D-01 ~ D-10 (13-CONTEXT.md 잠금 결정 — 공식 REQ-ID 미할당 페이즈)` 를 포함하고, `**Execution Waves:**` 표에 Wave 1(4개 플랜)과 Wave 2(1개 플랜)가 있다.
    - `.planning/STATE.md` `### Key Decisions` 에 Phase 13 항목 3개가 추가되었다 (`grep -c "(Phase 13)" .planning/STATE.md` >= 3).
    - `.planning/STATE.md` `### Active TODOs` 에 승인 대기 항목이 추가되었다: `grep -c "Phase 13 감사 보고서" .planning/STATE.md` >= 1
    - `git status --porcelain Assets` 출력이 비어 있다.
  </acceptance_criteria>

  <done>보고서에 V1~V6 무결성 검증 결과가 실제 출력값과 함께 기록되어 전부 PASS이고, ROADMAP Phase 13의 placeholder가 실제 Goal/Requirements/Plans/Waves로 대체되었으며, STATE.md에 Phase 13 Key Decisions 3건과 승인 대기 TODO가 반영되고, `Assets/` 는 0줄 변경이다.</done>
</task>

</tasks>

<verification>
```bash
R=".planning/phases/13-codebase-cleanup-audit/13-AUDIT-REPORT.md"
test -f "$R" && test $(wc -l < "$R") -ge 200

# 1. 4개 fragment 항목이 1:1로 병합되었는가 (요약/누락 없음)
for L in A B C D; do
  FR=$(ls .planning/phases/13-codebase-cleanup-audit/13-FINDINGS-$L-*.md)
  echo "$L fragment=$(grep -cP "^\| $L-D(07|08|10)-\d{2} \|" "$FR") report=$(grep -cP "^\| \[ \] \| $L-D(07|08|10)-\d{2} \|" "$R")"
done

# 2. CP949 46개 전역 전수 등재
find Assets -name "*.cs" -type f -print0 \
 | while IFS= read -r -d '' f; do iconv -f UTF-8 -t UTF-8 "$f" >/dev/null 2>&1 || grep -qF "$f" "$R" || echo "UNLISTED $f"; done

# 3. 모든 항목에 file:line
T=$(grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \|' "$R")
P=$(grep -cP '^\| \[ \] \| [ABCDX]-D(07|08|09|10)-\d{2} \| Assets/[^|]+\.cs \|' "$R")
echo "items=$T withpath=$P"    # 같아야 함

# 4. D-09 배제 목록 12행
grep -cP '^\| E-\d{2} \|' "$R"   # == 12

# 5. 사전 승인 없음
grep -cP '^\| \[x\] \|' "$R"     # == 0

# 6. 소스 무수정 (D-01/D-02 핵심 게이트)
git status --porcelain Assets    # 출력 없어야 함
```
</verification>

<success_criteria>
- 단일 최종 보고서 `13-AUDIT-REPORT.md` 가 존재하고 `<report_structure>` 의 13개 섹션을 전부 갖는다.
- 4개 fragment의 D-07/D-08/D-10 항목이 범위별 개수 일치로 1:1 병합되었다 (요약·누락 없음).
- D-09 섹션이 전역 교차 분석으로 채워졌고, 문서화된 의도적 비공유 12건이 배제 목록에 근거와 함께 남아 있다 (D-09).
- 회귀 위험 높은 항목이 `## 회귀 위험 높음 — 신중 검토 필요` 아래에 모여 일반 항목과 시각적으로 분리되었다 (D-06).
- CP949 46개 파일이 전역 단일 표로 집계되었다 (D-04).
- 모든 항목에 미체크 승인 체크박스 `[ ]` 가 있고, 체크된 항목이 하나도 없다 (D-01 2단계 워크플로 시작 상태).
- ROADMAP Phase 13 placeholder가 제거되고 STATE.md에 Phase 13 결정/TODO가 반영되었다.
- `git status --porcelain Assets` 가 비어 있다 — 소스 코드 0줄 변경 (D-01/D-02).
</success_criteria>

<output>
완료 후 `.planning/phases/13-codebase-cleanup-audit/13-05-SUMMARY.md` 를 생성한다.
카테고리별 최종 항목 수, 고위험 항목 수, 배제 항목 수, 그리고 사용자가 다음에 해야 할 일
(보고서 체크박스 승인)을 명시한다.
</output>
