---
phase: 16-interval-autosave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/SaveSystem/Script/AutoSaveTimer.cs
  - Assets/SaveSystem/Script/AutoSaveTimer.cs.meta
  - Assets/SaveSystem/Script/SaveLoadManager.cs
autonomous: true
requirements: [D-01, D-03, D-03b, D-03c, D-04, D-05, D-07, D-07b, D-07c]
must_haves:
  truths:
    - "플레이 중 3분(실제 플레이 가능 시간 기준)이 지나면 현재 슬롯 세이브 파일이 자동으로 갱신된다"
    - "메인메뉴에서는 자동저장이 절대 발생하지 않는다 (씬 로드가 CurrentState를 Playing으로 되돌려도)"
    - "일시정지/인벤토리/퍼즐/게임클리어 중에는 자동저장이 발생하지 않고 그 시간은 3분 카운트에도 포함되지 않는다"
    - "자동저장은 슬롯 선택 대화상자를 띄우지 않고 CurrentSlot에 조용히 쓴다"
    - "자동저장은 플레이어를 회복시키지 않는다"
    - "체크포인트/보스 격파/일시정지 메뉴 저장이 일어나면 3분 카운트가 0부터 다시 시작된다"
    - "기존 수동 저장 3종은 한 건도 막히지 않는다 (SaveAnywhere 내부에 상태 게이트가 없다)"
  artifacts:
    - path: "Assets/SaveSystem/Script/AutoSaveTimer.cs"
      provides: "시간 기반 네 번째 저장 트리거 - 자기 부트스트랩 DontDestroyOnLoad 싱글톤 + 1초 틱 코루틴 + D-03/D-03c 게이트 + SaveAnywhere() 직접 호출"
      contains: "public const float IntervalSeconds = 180f;"
      min_lines: 90
    - path: "Assets/SaveSystem/Script/AutoSaveTimer.cs.meta"
      provides: "Unity 에셋 guid (다중 기기 동기화 시 guid 충돌 방지용으로 손으로 생성해 커밋)"
      contains: "fileFormatVersion: 2"
    - path: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      provides: "D-05 타이머 리셋 훅 1줄 (Save() 말미). 그 외 무수정"
      contains: "AutoSaveTimer.NotifySaveWritten();"
  key_links:
    - from: "Assets/SaveSystem/Script/AutoSaveTimer.cs"
      to: "SaveLoadManager.SaveAnywhere()"
      via: "게이트 통과 후 직접 호출 (D-07b, 래퍼 없음)"
      pattern: "SaveLoadManager\\.Instance\\.SaveAnywhere\\(\\)"
    - from: "Assets/SaveSystem/Script/AutoSaveTimer.cs"
      to: "GameStateManager.CurrentState / PlayerStats.Instance"
      via: "CanAutoSave() 이중 게이트 (D-03 + D-03c)"
      pattern: "PlayerStats\\.Instance != null"
    - from: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      to: "AutoSaveTimer.NotifySaveWritten()"
      via: "Save() 말미 1줄 - 3개 수동 트리거 전부가 지나가는 유일한 디스크 쓰기 지점 (D-05)"
      pattern: "AutoSaveTimer\\.NotifySaveWritten\\(\\);"
---

<objective>
Phase 16 의 핵심인 **시간 기반 네 번째 저장 트리거**를 신규 컴포넌트 `AutoSaveTimer` 로 구현하고, D-05 타이머 리셋 훅 1줄을 `SaveLoadManager.Save()` 말미에 추가한다.

- D-04: 간격 180초(3분).
- D-03 + D-03c: `GameStateManager.CurrentState == Playing` **그리고** `PlayerStats.Instance != null` 일 때만 저장한다. 두 번째 가드는 필수다 — `GameStateManager.HandleSceneLoaded()` 가 메인메뉴 포함 **모든 씬 로드마다** `CurrentState` 를 `Playing` 으로 되돌리므로, 이 가드가 없으면 메인메뉴에서 자동저장이 발동해 `SceneName = "MainMenu"` 가 현재 슬롯에 박히고 그 슬롯이 영구히 못 쓰게 된다.
- D-03b: 게이트는 **전부 호출자(타이머) 쪽에만** 둔다. `SaveAnywhere()` 본문은 한 글자도 바꾸지 않는다.
- D-07b: `SaveLoadManager.Instance.SaveAnywhere()` 를 그대로 호출한다. `SaveAuto()` 래퍼 신설 금지, `SaveAtCheckpoint()` 재사용 금지(3분마다 무료 풀힐이 된다), `Save()` 직접 호출 금지(D-07c: `SceneName` 을 갱신하지 않아 완전한 no-op).
- D-01: 항상 `CurrentSlot` 에 쓴다. `SaveSlotDialog` 를 거치지 않는다.
- D-05: 체크포인트 / 보스 격파 / 일시정지 메뉴 저장이 발생하면 카운트를 0으로 되돌린다.

Purpose: 현재 저장 트리거는 3종 전부 명시적 사용자 행동에서만 발생하고 **시간 기반 트리거는 0건**이다. 체크포인트를 밟지 않은 채 오래 플레이하면 그 구간이 통째로 미저장이다.
Output: 신규 파일 `AutoSaveTimer.cs` + `.meta`, `SaveLoadManager.cs` 에 1줄 추가.
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 실행한다. 아래 내용만으로 완결되어야 하며, `16-CONTEXT.md` 를 다시 읽지 않아도 실행 가능해야 한다.
이 프로젝트에는 자동화된 테스트 하네스가 없다. 검증 수단은 (a) `dotnet build` 컴파일 확인, (b) grep 정적 조건, (c) 별도 플랜(16-03)의 사용자 Play 모드 실측 세 가지뿐이다.
</execution_context>

<context>
@.planning/phases/16-interval-autosave/16-CONTEXT.md
@Assets/SaveSystem/Script/SaveLoadManager.cs
@Assets/Player/Script/GameStateManager.cs
@Assets/Player/Script/PlayerStats.cs
@Assets/Script/SaveSlotDialog.cs
@Assets/Script/PersistentManagers.cs
</context>

<design_decisions>
CONTEXT.md 가 계획 단계 재량으로 남긴 항목을 **여기서 확정한다. 실행자는 재검토하지 말고 그대로 구현한다.**

1. **타이머 소유 위치 = 별도 경량 컴포넌트 `AutoSaveTimer`** (`SaveLoadManager` 내부가 아니다).
   근거: P11 D-01 은 "매니저는 저장 로직만 소유, 호출자가 직접 호출"이다. 자동저장 타이머는 **호출자**이고 D-03/D-03c 게이트는 호출자 정책이다. 이것을 `SaveLoadManager` 안에 넣으면 D-03b 가 금지한 "매니저 안의 상태 게이트"로 한 걸음씩 흘러가게 된다.

2. **씬/프리팹 YAML 을 한 줄도 건드리지 않는다. 컴포넌트가 스스로 부트스트랩한다.**
   근거: `Assets/Resources/PersistentManagers.prefab` 실측 결과 자식은 `GameStateManager` / `AudioManager` / `InputHandler` 3개뿐이고 Canvas 는 없다. 프리팹에 자식을 손으로 추가하는 것은 YAML 직접 편집이라 위험하고, 신규 스크립트는 Unity 에디터가 임포트하기 전까지 프리팹이 참조할 수도 없다. 따라서 `SaveLoadManager.Bootstrap()` 과 **똑같은** `[RuntimeInitializeOnLoadMethod(BeforeSceneLoad)]` 패턴으로 `new GameObject("AutoSave")` 를 만들고 `DontDestroyOnLoad` 한다. → 씬 10개 배치도, 에디터 수동 배선도 불필요하다.

3. **타이머 구현 = 1초 틱 코루틴 + "플레이 가능 시간만 누적"** (`Update()` 매 프레임 아님, `async`/`Task` 아님).
   - 근거 A: 프로젝트 관행이 코루틴(`IEnumerator`/`StartCoroutine`)이고 `async`/`Task` 사용처가 0건이다.
   - 근거 B: `PlayerStats.Instance` 는 캐시가 비면 `FindAnyObjectByType<PlayerStats>()` 를 실행한다(`PlayerStats.cs:14-24`). 메인메뉴에서는 항상 캐시가 비어 있으므로 **매 프레임 폴링하면 매 프레임 씬 전체 탐색**이 된다. 1초에 1회면 무해하다.
   - `WaitForSeconds` 는 스케일 시간을 쓰므로 `Time.timeScale == 0`(Paused/Inventory/Puzzle/GameClear) 구간에서는 **아예 진행하지 않는다.** 즉 D-03 의 "일시정지 중 저장 금지"가 게이트 이전에 구조적으로 보장되고, 일시정지 시간이 3분 카운트에 포함되지도 않는다.

4. **씬 전환 직후 처리 = 카운트를 리셋하지 않고 이어서 센다.** 단, 누적은 `CanAutoSave()` 가 참일 때만 한다.
   근거: 카운터가 "실제 플레이 가능 시간"을 재므로 (a) 씬을 자주 넘나드는 플레이가 자동저장을 무한 연기할 수 없고(페이즈 목표가 "미저장 구간 제거"다), (b) 메인메뉴/일시정지에 머문 시간은 카운트되지 않아 게임에 들어온 직후 난데없이 자동저장이 뜨는 일도 없다.

5. **D-05 리셋 = `SaveLoadManager.Save()` 말미에 1줄** (각 트리거 호출부 개별 훅 아님).
   근거: 대안은 `Checkpoint.cs` + `GameSettingsPanel.cs` + 보스 4종 사망 지점, 총 6개 파일을 고쳐야 하고 그중 다수가 **CP949 인코딩**이다. 이 저장소는 Phase 9/10/11 에서 CP949 파일 왕복 편집으로 비-ASCII 바이트가 U+FFFD 로 훼손된 사고 이력이 있다. `Save()` 는 이 파일 헤더 주석이 이미 선언한 "유일한 디스크 쓰기 지점"이고 3개 수동 트리거 + 자동저장 자신이 전부 지나간다. 1개 파일 1줄로 전부 커버된다.
   **D-03b 와의 관계를 명시한다:** D-03b 가 절대 금지하는 것은 `SaveAnywhere()`/`SaveLoadManager` 안에 **상태 게이트**를 넣는 것이다(일시정지 메뉴 저장이 `Paused` 상태에서 실행되므로 게이트를 넣으면 수동 저장이 통째로 죽는다). D-07b 는 반대로 "`Save()` 한 곳에 걸면 자동으로 커버된다"고 리셋 훅을 명시적으로 승인한다. 이 플랜은 **게이트는 0줄, 리셋 알림만 1줄**을 넣어 양쪽을 동시에 만족시킨다. `SaveAnywhere()` 본문은 무수정이다.

6. **알림 UI(D-10)는 이 플랜 범위 밖이다** — 16-02 에서 `AutoSaveNotice` 로 추가한다. 이 플랜은 알림을 호출하지 않는다.
</design_decisions>

<interfaces>
<!-- 실행자가 필요로 하는 계약 전부. 코드베이스 탐색 없이 이 블록만 보고 작업할 것. -->

Assets/Player/Script/GameStateManager.cs (이 플랜에서 **수정 금지**):
```csharp
public class GameStateManager : MonoBehaviour
{
    public enum GameState { Playing, Paused, Inventory, Loading, GameClear, Puzzle }
    public static GameStateManager Instance { get; private set; }
    public GameState CurrentState { get; private set; } = GameState.Playing;
    public event Action<GameState> OnGameStateChange;
    // SetState(): Paused/Inventory/GameClear/Puzzle -> Time.timeScale = 0f, Playing -> 1f
    // HandleSceneLoaded(Scene, LoadSceneMode): 씬 종류를 가리지 않고 CurrentState = Playing,
    //   Time.timeScale = 1f 로 무조건 되돌린다. 메인메뉴도 예외가 아니다. <- D-03c 의 존재 이유
}
```

Assets/Player/Script/PlayerStats.cs (이 플랜에서 **수정 금지**):
```csharp
public class PlayerStats : HP
{
    private static PlayerStats instance;
    public static PlayerStats Instance    // instance == null 이면 FindAnyObjectByType<PlayerStats>() 실행
    { get { if (instance == null) instance = FindAnyObjectByType<PlayerStats>(); return instance; } }
}
```
파괴된 MonoBehaviour 참조는 Unity 에서 `== null` 이 true 이므로, 게임 플레이 씬을 떠나면 `PlayerStats.Instance` 는 다시 탐색하고 메인메뉴에서는 `null` 을 돌려준다. 그래서 "게임 플레이 씬 안에 있는가"의 판정으로 쓸 수 있다.

Assets/SaveSystem/Script/SaveLoadManager.cs (Task 2 에서 **1문장만** 추가, 그 외 무수정):
```csharp
public static SaveLoadManager Instance { get; private set; }  // RuntimeInitializeOnLoadMethod(BeforeSceneLoad) 자기 부트스트랩
public int CurrentSlot { get; private set; }
public string SavePath { get { return GetSavePath(CurrentSlot); } }
public void Save()               // 유일한 디스크 쓰기: CapturePlayerStats() -> SerializeObject -> File.WriteAllText(SavePath)
public void SaveAtCheckpoint(string checkpointName)  // 씬+스폰포인트 기록 + ResetHealthToMax() 풀회복 -> Save()   <- 자동저장 재사용 금지
public void SaveAnywhere()       // 현재 씬 기록 + 씬 불일치 시 SpawnPointName="" -> Save()                        <- 자동저장이 쓰는 것
public void SaveOnBossDefeated(string bossId)
public void LoadGame()
```
`SaveAnywhere()` 현재 본문 (무수정 계약 — 참고용):
```csharp
    public void SaveAnywhere()
    {
        string currentScene = SceneManager.GetActiveScene().name;
        if (_data.SceneName != currentScene) _data.SpawnPointName = "";
        _data.SceneName = currentScene;
        Save();
    }
```

Assets/Script/SaveSlotDialog.cs (이 플랜에서 **수정 금지**. 자동저장이 절대 거치지 않아야 하는 대화상자):
```csharp
public static bool IsOpen { get; }
public static void Open(Action saveAction, Action savedAction = null)  // 내부에서 SetState(GameState.Paused) 호출
```
→ 수동 저장 2종은 이 대화상자를 거치며 그 동안 상태가 `Paused` 다. 따라서 대화상자가 열려 있는 동안 자동저장은 D-03 게이트에서 이미 차단된다 (추가 코드 불필요).

자기 부트스트랩 참조 구현 (`Assets/SaveSystem/Script/SaveLoadManager.cs:29-49` — 이 형태를 그대로 미러링할 것):
```csharp
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Bootstrap()
    {
        if (Instance != null) return;
        GameObject go = new GameObject("SaveLoadManager");
        go.AddComponent<SaveLoadManager>();
    }
```
`Assets/Script/PersistentManagers.cs:12-16` 의 statics 리셋 패턴 (도메인 리로드 비활성화 대비):
```csharp
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]
    private static void ResetStatics() { instance = null; }
```

기존 `[ContextMenu]` 검증 훅 관행 (`SaveLoadManager.cs:450-485`) — Play 모드에서 Hierarchy 의 런타임 오브젝트를 선택해 컴포넌트 기어 메뉴로 구동한다. 이 프로젝트의 유일한 수동 검증 수단이며 Task 1 도 같은 관행을 따른다.
</interfaces>

<encoding_note>
- **신규 파일 `Assets/SaveSystem/Script/AutoSaveTimer.cs` 는 순수 ASCII 로만 작성한다.** 주석도 영어로 쓴다. 한국어 문자를 넣지 않는다 (`Assets/SaveSystem/Script/` 아래 기존 3개 파일 모두 ASCII 다). 줄끝은 CRLF, BOM 없음.
- `Assets/SaveSystem/Script/SaveLoadManager.cs` 는 **순수 ASCII + CRLF + BOM 없음**이다 (`file` 출력: `ASCII text, with CRLF, LF line terminators`, `head -c 3` = `2f2f20`). 추가하는 주석/코드도 전부 ASCII 여야 하고 BOM 을 새로 붙이지 말 것.
- 이 저장소는 Phase 9/10/11 에서 인코딩 왕복으로 비-ASCII 바이트가 U+FFFD 로 훼손된 사고 이력이 있다. 이 플랜에서 CP949 파일(`Assets/Script/HP.cs`, `Assets/map/script/Checkpoint.cs`, 보스 스크립트 다수)은 **읽지도 쓰지도 않는다.**
- `.meta` 파일은 ASCII + LF, 마지막에 개행 1개. 기존 `Assets/SaveSystem/Script/SaveData.cs.meta` 와 동일한 2줄 형식이다.
</encoding_note>

<build_note>
`Assembly-CSharp.csproj` / `*.sln` 은 Unity 가 생성하는 파일이고 `.gitignore` 에 등재되어 **git 추적 대상이 아니다** (`.gitignore:27 *.sln`, `:39 *.csproj`). 그리고 이 csproj 는 `<Compile Include="..." />` 를 **파일마다 명시적으로 나열**한다 (현재 130줄). 따라서 신규 `.cs` 파일은 csproj 에 줄을 추가하지 않으면 `dotnet build` 가 **컴파일조차 하지 않는다** — 통과했다고 착각하게 된다.

빌드 기준선 (이 플랜 시작 시점, 실측): `dotnet build Assembly-CSharp.csproj --nologo -v quiet` → `경고 6개` / `오류 0개`.
신규 코드는 **경고를 1개도 늘리지 않아야 한다** (기준선 그대로 6개).
</build_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AutoSaveTimer.cs 신규 작성 — 자기 부트스트랩 + 1초 틱 코루틴 + D-03/D-03c 이중 게이트 + SaveAnywhere() 직접 호출 (D-01/D-03/D-03c/D-04/D-07/D-07b)</name>
  <files>Assets/SaveSystem/Script/AutoSaveTimer.cs, Assets/SaveSystem/Script/AutoSaveTimer.cs.meta, Assembly-CSharp.csproj</files>

  <read_first>
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` (전체 — `Bootstrap()`/`Awake()` 싱글톤 형태, `SaveAnywhere()` 시그니처, `[ContextMenu]` 검증 훅 관행)
    - `Assets/Player/Script/GameStateManager.cs` (전체 — `GameState` 중첩 enum 경로가 `GameStateManager.GameState.Playing` 임을 확인, `HandleSceneLoaded` 가 모든 씬에서 `Playing` 으로 되돌리는 것 확인)
    - `Assets/Player/Script/PlayerStats.cs` 의 1~35행 (`Instance` getter 가 `FindAnyObjectByType` 를 돈다는 사실 확인 — 매 프레임 폴링 금지 근거)
    - `Assets/Script/PersistentManagers.cs` (전체 — `ResetStatics()` + `DontDestroyOnLoad` 패턴)
    - `Assets/SaveSystem/Script/SaveData.cs.meta` (신규 `.meta` 가 따라야 할 2줄 형식)
    - `Assembly-CSharp.csproj` 의 130~140행 (`<Compile Include>` 삽입 위치)
  </read_first>

  <action>
**(1) 신규 파일 `Assets/SaveSystem/Script/AutoSaveTimer.cs` 를 아래 내용 그대로 생성한다.** 인코딩 ASCII + CRLF, BOM 없음. 내용을 바꾸거나 "개선"하지 말 것 — 상수값·메서드명·문자열 전부에 검증 게이트가 걸려 있다.

```csharp
// Phase 16 - AutoSaveTimer (the fourth save trigger: time)
// Decisions: D-01 (always writes CurrentSlot, never opens SaveSlotDialog),
//            D-03 + D-03c (fires only while Playing AND a PlayerStats exists),
//            D-04 (180 second interval),
//            D-05 (any disk write restarts the countdown; see SaveLoadManager.Save()),
//            D-07 / D-07b (reuses SaveAnywhere() as-is - no wrapper, no heal),
//            D-11 (always on; no setting is exposed).
//
// D-03b: every guard lives HERE, in the caller, and NOT inside SaveLoadManager. The pause
// menu saves while CurrentState is Paused, so a Playing gate inside SaveAnywhere() would
// silently kill manual saving. This file is the only place that decides "may we write now".
//
// Why a separate component instead of a timer inside SaveLoadManager: P11 D-01 gives the
// manager the save logic and leaves the decision to call it to the caller. This timer IS a
// caller.
//
// Why it bootstraps itself instead of sitting on the PersistentManagers prefab: a brand new
// script cannot be referenced from prefab or scene YAML before the Unity editor has imported
// it, and hand-editing that YAML is how scenes get corrupted. Creating the object in code
// mirrors SaveLoadManager.Bootstrap() and needs no editor wiring at all.

using System.Collections;
using UnityEngine;

public class AutoSaveTimer : MonoBehaviour
{
    // D-04: three minutes of actual playable time.
    public const float IntervalSeconds = 180f;

    // The countdown is sampled once a second instead of accumulated every frame, because
    // PlayerStats.Instance falls back to FindAnyObjectByType whenever its cached instance is
    // gone - which is every single call while the main menu is up.
    public const float TickSeconds = 1f;

    private static AutoSaveTimer _instance;

    // Playable time accumulated since the last disk write, in seconds.
    private float _elapsedSeconds;

    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.SubsystemRegistration)]
    private static void ResetStatics()
    {
        _instance = null;
    }

    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Bootstrap()
    {
        if (_instance != null) return;
        GameObject go = new GameObject("AutoSave");
        go.AddComponent<AutoSaveTimer>();
    }

    private void Awake()
    {
        if (_instance != null && _instance != this)
        {
            Destroy(gameObject);
            return;
        }

        _instance = this;
        DontDestroyOnLoad(gameObject);
    }

    private void OnDestroy()
    {
        if (_instance == this) _instance = null;
    }

    private void Start()
    {
        StartCoroutine(AutoSaveRoutine());
    }

    // D-05: called from SaveLoadManager.Save(), the single disk write that every save trigger
    // (checkpoint, boss defeat, pause menu and this timer itself) funnels through. Static and
    // null-tolerant on purpose, so the manager never has to care whether a timer exists.
    public static void NotifySaveWritten()
    {
        if (_instance != null) _instance._elapsedSeconds = 0f;
    }

    // Coroutine based (IEnumerator / StartCoroutine) to match the codebase, which uses no
    // C# Task-based keywords anywhere.
    //
    // WaitForSeconds runs on scaled time, so while Time.timeScale is 0 - pause, inventory,
    // puzzle, game clear, and the save slot dialog - this loop does not advance at all. Paused
    // time therefore cannot trigger a write and does not count towards the interval either.
    private IEnumerator AutoSaveRoutine()
    {
        while (true)
        {
            yield return new WaitForSeconds(TickSeconds);

            // Time spent outside actual gameplay (main menu, loading, a dialog) is not counted,
            // so the player never walks into a gameplay scene and gets an instant autosave.
            if (!CanAutoSave()) continue;

            _elapsedSeconds += TickSeconds;
            if (_elapsedSeconds < IntervalSeconds) continue;

            TryAutoSave();
        }
    }

    // Returns false when the gate blocked the write. Split out of the routine so the
    // ContextMenu hook below drives the exact same path during manual verification.
    private bool TryAutoSave()
    {
        if (!CanAutoSave()) return false;

        // Reset before the write so a throwing File.WriteAllText cannot spin this every tick.
        // SaveLoadManager.Save() also calls NotifySaveWritten(), which lands on the same 0.
        _elapsedSeconds = 0f;

        // D-07 / D-07b: SaveAnywhere() already records the current scene and clears
        // SpawnPointName when the scene changed, and it is the only existing method that
        // refreshes SceneName. Deliberately NOT SaveAtCheckpoint() - that one heals the player
        // to full and would hand out a free heal every three minutes. Deliberately NOT Save()
        // either - it never touches SceneName, so it would rewrite an identical file (D-07c).
        // D-01: no slot dialog is involved. SaveSlotDialog lives in the manual callers, not in
        // SaveAnywhere(), so this writes CurrentSlot silently.
        SaveLoadManager.Instance.SaveAnywhere();
        return true;
    }

    // D-03 + D-03c. Both halves are mandatory:
    //  - CurrentState == Playing keeps the write out of pause / inventory / puzzle / clear.
    //  - PlayerStats.Instance != null is what actually keeps the main menu out.
    //    GameStateManager.HandleSceneLoaded() forces CurrentState back to Playing on EVERY
    //    scene load, the main menu included. Without this second check the timer would write
    //    SceneName = "MainMenu" into the current slot; loading that slot later would try to
    //    open the main menu as a gameplay scene, find no PlayerStats and abort, leaving the
    //    slot permanently unusable.
    private static bool CanAutoSave()
    {
        if (SaveLoadManager.Instance == null) return false;

        GameStateManager state = GameStateManager.Instance;
        if (state == null) return false;
        if (state.CurrentState != GameStateManager.GameState.Playing) return false;

        return PlayerStats.Instance != null;
    }

    // ---- Verification hooks (no automated test harness exists in this project) ----
    // Select the runtime "AutoSave" GameObject in the Hierarchy during Play mode and use the
    // component gear menu, exactly like the Phase11 / Phase14 hooks on SaveLoadManager.

    [ContextMenu("Phase16/1. Force AutoSave Now")]
    private void DebugForceAutoSave()
    {
        if (!TryAutoSave())
            Debug.LogWarning("[AutoSaveTimer] Gate blocked the write - see Phase16/2 for the reason.");
    }

    [ContextMenu("Phase16/2. Log Timer State")]
    private void DebugLogTimerState()
    {
        Debug.Log("[AutoSaveTimer] elapsed=" + _elapsedSeconds + "/" + IntervalSeconds +
                  " canAutoSave=" + CanAutoSave() +
                  " state=" + (GameStateManager.Instance != null
                      ? GameStateManager.Instance.CurrentState.ToString() : "no GameStateManager") +
                  " hasPlayerStats=" + (PlayerStats.Instance != null) +
                  " slot=" + (SaveLoadManager.Instance != null ? SaveLoadManager.Instance.CurrentSlot : -1));
    }
}
```

**(2) 신규 `.meta` 파일 `Assets/SaveSystem/Script/AutoSaveTimer.cs.meta` 를 손으로 만든다.**
Unity 에디터를 이 환경에서 열 수 없어 자동 생성이 불가하고, 이 저장소는 **두 대의 기기에서 동기화**되므로 각 기기가 서로 다른 guid 를 생성하면 충돌한다. 그래서 지금 만들어 함께 커밋한다.
형식은 기존 `Assets/SaveSystem/Script/SaveData.cs.meta` 와 동일하게 정확히 2줄 + 마지막 개행 1개:
```
fileFormatVersion: 2
guid: <새로 생성한 32자 소문자 16진수>
```
guid 생성: `openssl rand -hex 16` (없으면 `python -c "import uuid;print(uuid.uuid4().hex)"`).
생성 후 **저장소 전체에서 유일한지 반드시 확인**한다: `grep -rl "<생성한guid>" Assets Packages ProjectSettings` 가 새 `.meta` 1개만 출력해야 한다. 충돌하면 다시 생성한다.

**(3) `Assembly-CSharp.csproj` 에 컴파일 항목 1줄을 추가한다** (그래야 `dotnet build` 가 신규 파일을 실제로 컴파일한다).
기존 줄:
```xml
    <Compile Include="Assets\SaveSystem\Script\SaveLoadManager.cs" />
```
**바로 위에** 다음 줄을 삽입한다 (들여쓰기 4칸, 백슬래시 경로):
```xml
    <Compile Include="Assets\SaveSystem\Script\AutoSaveTimer.cs" />
```
이 파일은 `.gitignore` 대상이라 커밋되지 않는다. 되돌리지 말고 그대로 둔다 (다음에 Unity 가 열리면 재생성한다).

**절대 하지 말 것:**
- `Assets/SaveSystem/Script/SaveLoadManager.cs` 수정 (이 태스크에서는 0줄. Task 2 에서만 1문장).
- `SaveAnywhere()` / `Save()` / `SaveAtCheckpoint()` / `SaveOnBossDefeated()` 본문 수정.
- `Assets/Player/Script/GameStateManager.cs`, `Assets/Player/Script/PlayerStats.cs`, `Assets/Script/SaveSlotDialog.cs`, `Assets/Script/PersistentManagers.cs` 수정.
- `Assets/Resources/PersistentManagers.prefab` 또는 `Assets/Scenes/**/*.unity` 수정 (부트스트랩이 코드로 해결한다).
- `Assets/SaveSystem/Script/SettingsData.cs` / `Assets/Player/Script/Menu/GameSettingsPanel.cs` 수정 (D-11: 설정 노출 금지).
- `SaveAuto()` 같은 래퍼 메서드 신설 (D-07b).
- `.bak` 백업 로직 추가 (D-02).
- '전투 중' 판정 플래그 신설 (D-05b).
- 알림 UI 관련 코드 (D-10 은 16-02 범위).
- `Update()` / `LateUpdate()` / `async` / `Task` / `await` 사용.
  </action>

  <verify>
    <automated>dotnet build Assembly-CSharp.csproj --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `test -f Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 성공한다.
    - `dotnet build Assembly-CSharp.csproj --nologo -v quiet` 출력에 `오류 0개` 가 포함되고 경고 개수가 기준선과 같은 `경고 6개` 다.
    - `grep -c "AutoSaveTimer.cs" Assembly-CSharp.csproj` 가 `1` 을 출력한다 (신규 파일이 실제로 빌드에 포함됨).
    - `grep -c "public const float IntervalSeconds = 180f;" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다 (D-04).
    - `grep -c "public const float TickSeconds = 1f;" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다.
    - `grep -c "SaveLoadManager.Instance.SaveAnywhere();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다 (D-07b: 정확히 1회, 래퍼 없음).
    - `grep -c "SaveAtCheckpoint\|SaveOnBossDefeated\|SaveSlotDialog\|ResetHealthToMax" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `0` 을 출력한다 (풀힐 재사용·대화상자 경유 금지: D-01, D-07).
    - `grep -c "state.CurrentState != GameStateManager.GameState.Playing" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다 (D-03).
    - `grep -c "return PlayerStats.Instance != null;" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다 (D-03c — 필수 가드).
    - `grep -c "public static void NotifySaveWritten()" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다 (D-05 진입점).
    - `grep -c "RuntimeInitializeLoadType.BeforeSceneLoad" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1`, `grep -c "RuntimeInitializeLoadType.SubsystemRegistration" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1`, `grep -c "DontDestroyOnLoad(gameObject);" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다.
    - `grep -c "StartCoroutine(AutoSaveRoutine());" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다.
    - `grep -cE "void Update\(\)|void LateUpdate\(\)|async |Task<|await " Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `0` 을 출력한다 (코루틴 관행 준수).
    - `grep -c "\.bak" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `0` 을 출력한다 (D-02).
    - `grep -cP "[^\x00-\x7F]" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `0` 을 출력한다 (순수 ASCII).
    - `file Assets/SaveSystem/Script/AutoSaveTimer.cs` 출력에 `CRLF` 가 포함되고, `head -c 3 Assets/SaveSystem/Script/AutoSaveTimer.cs | xxd -p` 가 `efbbbf` 가 **아니다** (BOM 없음).
    - `wc -l < Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `90` 이상이다.
    - `cat Assets/SaveSystem/Script/AutoSaveTimer.cs.meta` 가 정확히 2줄이고 1행이 `fileFormatVersion: 2`, 2행이 `guid: ` + 32자 소문자 16진수다.
    - `grep -rl "<생성한guid>" Assets Packages ProjectSettings` 가 `Assets/SaveSystem/Script/AutoSaveTimer.cs.meta` 한 줄만 출력한다 (guid 유일).
    - `git status --porcelain Assets/SaveSystem/Script/SaveLoadManager.cs Assets/Player/Script/GameStateManager.cs Assets/Player/Script/PlayerStats.cs Assets/Script/SaveSlotDialog.cs Assets/Script/PersistentManagers.cs Assets/SaveSystem/Script/SettingsData.cs Assets/Player/Script/Menu/GameSettingsPanel.cs Assets/Resources/PersistentManagers.prefab` 가 빈 출력이다 (이 태스크는 신규 파일만 만든다. 단, `PersistentManagers.prefab`/`PersistentManagers.cs` 는 실행 전 이미 untracked 상태일 수 있으므로 그 경우 `?? ` 줄이 그대로 유지되고 내용이 바뀌지 않았음을 확인할 것).
    - `git status --porcelain Assets/Scenes/` 의 출력이 이 태스크 실행 **전과 동일**하다 (씬 파일을 새로 변경하지 않음. 실행 전 이미 있던 미커밋 씬 변경은 건드리지도, 스테이징하지도 말 것).
    - 커밋 1개. pathspec 은 `Assets/SaveSystem/Script/AutoSaveTimer.cs Assets/SaveSystem/Script/AutoSaveTimer.cs.meta` 두 개만.
  </acceptance_criteria>

  <done>`AutoSaveTimer` 가 컴파일되고, 스스로 `DontDestroyOnLoad` 오브젝트를 만들며, 1초 틱 코루틴이 `Playing` + `PlayerStats` 존재 조건에서만 시간을 누적해 180초에 `SaveAnywhere()` 를 정확히 1회 호출한다. `.meta` guid 가 확정돼 함께 커밋됐다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: SaveLoadManager.Save() 말미에 D-05 타이머 리셋 훅 1줄 추가 (D-05, D-03b 무침해 확인)</name>
  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>

  <read_first>
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` 의 1~200행 (헤더 주석의 "File I/O happens ONLY inside Save()" 선언, `Save()`(138~144행), `SaveAtCheckpoint()`, `SaveAnywhere()`, `SaveOnBossDefeated()`)
    - `Assets/SaveSystem/Script/AutoSaveTimer.cs` (Task 1 산출물 — `public static void NotifySaveWritten()` 시그니처 확인)
  </read_first>

  <action>
`Assets/SaveSystem/Script/SaveLoadManager.cs` 의 `Save()` 메서드에 **딱 한 개의 실행 문장**과 주석 6줄, 빈 줄 1줄을 추가한다. 다른 메서드는 한 글자도 건드리지 않는다.

변경 전 (현재 137~144행):
```csharp
    // Core write. Captures live player stats into the memory cache, then serializes.
    public void Save()
    {
        CapturePlayerStats();
        string json = JsonConvert.SerializeObject(_data, JsonSettings);
        File.WriteAllText(SavePath, json);
        Debug.Log("[SaveLoadManager] Saved to " + SavePath);
    }
```

변경 후 (정확히 이것):
```csharp
    // Core write. Captures live player stats into the memory cache, then serializes.
    public void Save()
    {
        CapturePlayerStats();
        string json = JsonConvert.SerializeObject(_data, JsonSettings);
        File.WriteAllText(SavePath, json);
        Debug.Log("[SaveLoadManager] Saved to " + SavePath);

        // Phase 16 (D-05): every save trigger - checkpoint, boss defeat, pause menu and the
        // interval autosave itself - funnels through this one method, so restarting the
        // autosave countdown here covers all of them with a single line. Placed after the
        // write so a throwing File.WriteAllText does not reset anything. This is a
        // notification, NOT a gate: D-03b forbids any state check inside this file, and none
        // is added - AutoSaveTimer still owns the entire Playing / PlayerStats decision.
        AutoSaveTimer.NotifySaveWritten();
    }
```

주의:
- `AutoSaveTimer.NotifySaveWritten()` 은 static 이고 타이머 인스턴스가 없으면 아무 일도 하지 않으므로 별도 null 가드나 `using` 추가가 필요 없다 (같은 `Assembly-CSharp` 어셈블리, 전역 네임스페이스).
- 추가 문장은 `Save()` **안에만** 넣는다. `SaveAtCheckpoint()` / `SaveAnywhere()` / `SaveOnBossDefeated()` 는 모두 `Save()` 를 호출하므로 자동으로 커버된다. 각 메서드에 개별로 넣지 말 것.
- **`SaveAnywhere()` 본문은 절대 수정하지 않는다.** 상태 게이트(`CurrentState == Playing` 등)를 이 파일 어디에도 추가하지 않는다 — 일시정지 메뉴 저장이 `Paused` 상태에서 실행되므로 게이트를 넣으면 기존 수동 저장이 통째로 죽는다 (D-03b).
- 이 파일은 ASCII + CRLF + BOM 없음이다. 추가하는 주석도 전부 ASCII.
- 기존 `[ContextMenu]` 훅, `LoadGame()`, 설정 관련 코드는 무수정.
  </action>

  <verify>
    <automated>dotnet build Assembly-CSharp.csproj --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "AutoSaveTimer.NotifySaveWritten();" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1` 을 출력한다 (정확히 1곳).
    - `grep -n "AutoSaveTimer.NotifySaveWritten();" Assets/SaveSystem/Script/SaveLoadManager.cs` 의 줄번호가 `grep -n "File.WriteAllText(SavePath, json);" Assets/SaveSystem/Script/SaveLoadManager.cs` 의 줄번호보다 크다 (쓰기 이후에 리셋).
    - `git diff --numstat -- Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `8	0	Assets/SaveSystem/Script/SaveLoadManager.cs` 다 (추가 8줄 = 빈 줄 1 + 주석 6 + 코드 1, 삭제 0줄).
    - `grep -c "GameStateManager\|CurrentState\|PlayerStats.Instance != null" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (**D-03b: 이 파일에 상태 게이트가 없다**).
    - `git diff -U0 -- Assets/SaveSystem/Script/SaveLoadManager.cs | grep -c "^-[^-]"` 가 `0` 을 출력한다 (기존 줄 삭제/수정 0건).
    - 다음 3개 본문이 무변경이다: `grep -c 'if (_data.SceneName != currentScene) _data.SpawnPointName = "";' Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1`, `grep -c "if (ps != null) ps.ResetHealthToMax();" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1`, `grep -c "_data.BossProgress\[bossId\] = true;" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `1`.
    - `grep -c "File.WriteAllText" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `2` 를 출력한다 (`Save()` + `SaveSettings()`, 증가 없음).
    - `grep -c "SaveAuto\|\.bak" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (D-07b 래퍼 금지, D-02 백업 금지).
    - `grep -cP "[^\x00-\x7F]" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (순수 ASCII 유지).
    - `head -c 3 Assets/SaveSystem/Script/SaveLoadManager.cs | xxd -p` 가 `2f2f20` 을 출력한다 (BOM 이 새로 붙지 않음).
    - `file Assets/SaveSystem/Script/SaveLoadManager.cs` 출력에 `CRLF` 가 포함된다.
    - `dotnet build Assembly-CSharp.csproj --nologo -v quiet` 가 `오류 0개` / `경고 6개` 다.
    - `git status --porcelain Assets/SaveSystem/Script/SettingsData.cs Assets/Player/Script/Menu/GameSettingsPanel.cs Assets/map/script/Checkpoint.cs` 가 빈 출력이다 (D-11 무수정, CP949 파일 무수정).
    - 커밋 1개. pathspec 은 `Assets/SaveSystem/Script/SaveLoadManager.cs` 만.
  </acceptance_criteria>

  <done>`Save()` 가 디스크 쓰기 직후 `AutoSaveTimer.NotifySaveWritten()` 을 호출해 3개 수동 트리거 전부가 자동저장 카운트를 리셋하고, `SaveAnywhere()` 를 포함한 나머지 저장 경로는 한 줄도 바뀌지 않았다.</done>
</task>

</tasks>

<verification>
1. `dotnet build Assembly-CSharp.csproj --nologo -v quiet` → `오류 0개`, `경고 6개` (기준선 동일).
2. 이 플랜이 만든 커밋 2개의 변경 파일 합집합이 정확히 `Assets/SaveSystem/Script/AutoSaveTimer.cs`, `Assets/SaveSystem/Script/AutoSaveTimer.cs.meta`, `Assets/SaveSystem/Script/SaveLoadManager.cs` 3개뿐이다.
3. `grep -rn "GameStateManager" Assets/SaveSystem/Script/` 의 출력이 `AutoSaveTimer.cs` 안에만 있다 (D-03b: 게이트가 매니저로 새지 않았다).
4. `git status --porcelain Assets/Resources/PersistentManagers.prefab` 의 상태가 실행 전과 동일하다 (프리팹 YAML 무수정).
5. `grep -rn "SaveAnywhere" Assets --include=*.cs` 가 정확히 3줄이다: 선언(`SaveLoadManager.cs`), 기존 수동 호출(`GameSettingsPanel.cs`), 신규 자동저장 호출(`AutoSaveTimer.cs`).
</verification>

<success_criteria>
- `AutoSaveTimer.cs` 가 존재하고 컴파일되며 `IntervalSeconds = 180f` / `TickSeconds = 1f` / `SaveAnywhere()` 1회 호출 / `PlayerStats.Instance != null` 가드를 모두 갖는다.
- `AutoSaveTimer.cs.meta` 의 guid 가 저장소에서 유일하다.
- `SaveLoadManager.cs` 의 변경이 `Save()` 말미 1문장 + 주석뿐이고 삭제 줄이 0이다.
- `SaveAnywhere()` / `SettingsData` / `GameSettingsPanel` / 씬 / 프리팹 변경 0건.
- 컴파일 에러 0, 경고 증가 0, 계획 밖 파일 변경 0.
</success_criteria>

<output>
완료 후 `.planning/phases/16-interval-autosave/16-01-SUMMARY.md` 를 작성한다.
</output>
