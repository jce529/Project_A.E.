---
phase: 16-interval-autosave
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - Assets/SaveSystem/Script/AutoSaveNotice.cs
  - Assets/SaveSystem/Script/AutoSaveNotice.cs.meta
  - Assets/SaveSystem/Script/AutoSaveTimer.cs
autonomous: true
requirements: [D-10]
must_haves:
  truths:
    - "자동저장이 실행되면 화면 오른쪽 아래에 '자동 저장됨' 텍스트가 나타난다"
    - "그 텍스트는 약 1.2초 유지된 뒤 0.6초에 걸쳐 사라진다 (총 약 1.8초, D-10의 1~2초 범위)"
    - "알림은 클릭을 가로막지 않는다 (GraphicRaycaster 없음)"
    - "알림 캔버스는 씬을 옮겨도 다시 만들어지지 않고 유지된다"
    - "알림은 자동저장에만 붙고 수동 저장 3종의 기존 피드백을 바꾸지 않는다"
  artifacts:
    - path: "Assets/SaveSystem/Script/AutoSaveNotice.cs"
      provides: "D-10 자동저장 알림 - 런타임 생성 ScreenSpaceOverlay 캔버스 + TMP 라벨 + 유지/페이드아웃 코루틴"
      contains: "public const string Message = \"자동 저장됨\";"
      min_lines: 60
    - path: "Assets/SaveSystem/Script/AutoSaveNotice.cs.meta"
      provides: "Unity 에셋 guid (손으로 생성해 커밋)"
      contains: "fileFormatVersion: 2"
    - path: "Assets/SaveSystem/Script/AutoSaveTimer.cs"
      provides: "부트스트랩에 AutoSaveNotice 부착 + 저장 성공 직후 Show() 호출"
      contains: "if (notice != null) notice.Show();"
  key_links:
    - from: "Assets/SaveSystem/Script/AutoSaveTimer.cs"
      to: "AutoSaveNotice.Show()"
      via: "TryAutoSave() 안에서 SaveAnywhere() 직후 GetComponent 로 해석해 호출"
      pattern: "notice\\.Show\\(\\)"
    - from: "Assets/SaveSystem/Script/AutoSaveNotice.cs"
      to: "런타임 생성 ScreenSpaceOverlay Canvas + TextMeshProUGUI"
      via: "Build() 에서 코드로 생성, AutoSave 오브젝트(DontDestroyOnLoad) 자식으로 부착"
      pattern: "RenderMode\\.ScreenSpaceOverlay"
---

<objective>
D-10 을 구현한다: 자동저장이 발생하면 화면 구석에 짧은 텍스트를 띄우고 페이드아웃한다.

- 문구: `자동 저장됨`
- 위치: 화면 **오른쪽 아래**, 우하단에서 48px 안쪽
- 타이밍: 1.2초 유지 → 0.6초 페이드아웃 (총 1.8초, D-10 의 "1~2초" 범위 안)
- 프로젝트에 범용 토스트/알림 UI 가 **없어서** 새로 만드는 것이다. `HealPopup` / `HealPopupSpawner` 는 전투용 월드 스페이스 팝업이라 재사용 대상이 아니다.
- **범용 알림 프레임워크로 일반화하지 않는다** (아이템 획득·퀘스트 등은 명시적으로 이번 범위 밖).

Purpose: 조용히 저장하면 플레이어가 진행이 보존됐는지 알 길이 없다.
Output: 신규 파일 `AutoSaveNotice.cs` + `.meta`, `AutoSaveTimer.cs` 에 7줄 추가.
</objective>

<execution_context>
이 플랜은 `agy` CLI 가 실행한다. 아래 내용만으로 완결되어야 한다.
선행 플랜 16-01 이 이미 `Assets/SaveSystem/Script/AutoSaveTimer.cs` 를 만들어 커밋한 상태를 전제한다.
이 프로젝트에는 자동화된 테스트 하네스가 없다. 알림의 실제 표시 여부는 16-03 의 사용자 Play 모드 실측에서 확인한다.
</execution_context>

<context>
@.planning/phases/16-interval-autosave/16-CONTEXT.md
@.planning/phases/16-interval-autosave/16-01-PLAN.md
@Assets/SaveSystem/Script/AutoSaveTimer.cs
@Assets/Script/SaveSlotDialog.cs
@Assets/Player/Script/PlayerInteractionPrompt.cs
</context>

<design_decisions>
CONTEXT.md 가 재량으로 남긴 알림 관련 항목을 **여기서 확정한다. 실행자는 재검토하지 말고 그대로 구현한다.**

1. **캔버스는 씬 10개에 배치하지 않는다. 영속 오브젝트 아래에 런타임 생성한다.**
   근거(실측): `Assets/Resources/PersistentManagers.prefab` 에는 Canvas 가 없고 자식이 `GameStateManager`/`AudioManager`/`InputHandler` 3개뿐이다. 프리팹/씬 YAML 을 손으로 고치는 것은 위험하고, 신규 스크립트는 Unity 가 임포트하기 전엔 프리팹이 참조할 수도 없다. 그래서 16-01 이 만든 `AutoSave` 오브젝트(`DontDestroyOnLoad`)에 `AutoSaveNotice` 컴포넌트를 함께 붙이고, 캔버스는 `Build()` 가 코드로 만들어 그 자식으로 둔다. 씬 배치 0건, 에디터 수동 배선 0건, 씬을 옮겨도 유지된다.
   선례: `Assets/Script/SaveSlotDialog.cs:51-89`(ScreenSpaceOverlay + CanvasScaler 1920x1080 + TMP 라벨 전부 코드 생성), `Assets/Player/Script/PlayerInteractionPrompt.cs:53-70`(TMP 를 런타임에 AddComponent — 폰트 에셋 직렬화 참조 없이 TMP 기본 폰트가 자동 적용됨).

2. **문구 = `자동 저장됨`** (D-10 의 예시 그대로). 폰트는 지정하지 않는다 — `AddComponent<TextMeshProUGUI>()` 가 TMP Settings 의 기본 폰트를 자동으로 잡으므로 직렬화된 폰트 참조가 필요 없다(신규 스크립트라 인스펙터 배선이 불가능하므로 이것이 유일하게 가능한 방법이다). 크기 32, 흰색, `outlineWidth 0.2f`(밝은 배경에서도 읽히게 — `PlayerInteractionPrompt` 와 동일한 처리).

3. **위치 = 오른쪽 아래.** 좌상단/상단 중앙은 체력 UI(`PlayerHealthUI`)·보스 체력바가 쓰는 영역이라 피한다. 앵커/피봇 `(1, 0)`, `anchoredPosition (-48, 48)`, `sizeDelta (420, 60)`, 정렬 `BottomRight`.

4. **타이밍 = 1.2초 유지 + 0.6초 페이드아웃, `Time.unscaledDeltaTime` 사용.**
   근거: 저장 직후 플레이어가 ESC 를 눌러 `Time.timeScale = 0` 이 되면 스케일 시간으로는 알림이 화면에 얼어붙는다. 알림은 게임플레이가 아니므로 unscaled 가 맞다.

5. **`GraphicRaycaster` 와 `EventSystem` 을 붙이지 않는다.** 이 라벨은 클릭 대상이 아니고, 레이캐스터를 붙이면 아래에 있는 일시정지 메뉴 클릭을 삼킬 수 있다. `label.raycastTarget = false` 도 함께 둔다.

6. **`sortingOrder = 20000`** — 게임플레이 HUD 위, `SaveSlotDialog`(30000) 아래. 저장 대화상자가 열린 동안 알림이 그 위를 덮지 않는다.

7. **타이머와의 연결은 `GetComponent` 해석 1회**(캐시 필드 아님). 3분에 1회만 실행되므로 비용이 무의미하고, `Bootstrap()` 에서 두 컴포넌트를 붙이는 **순서에 의존하지 않는다**(`AddComponent<AutoSaveTimer>()` 는 그 즉시 `Awake()` 를 실행하므로, `Awake()` 에서 캐시하면 순서를 잘못 쓸 때 조용히 null 이 된다).

8. **알림은 자동저장에만 붙인다.** 수동 저장 3종은 이미 각자 피드백이 있다(`GameSettingsPanel.SetSaveFeedback`, `SaveSlotDialog` 닫힘). `SaveLoadManager.Save()` 에 알림을 걸면 수동 저장에도 중복으로 뜨므로 하지 않는다 — 호출은 `AutoSaveTimer.TryAutoSave()` 안에만 둔다.
</design_decisions>

<interfaces>
<!-- 실행자가 필요로 하는 계약 전부. 코드베이스 탐색 없이 이 블록만 보고 작업할 것. -->

`Assets/SaveSystem/Script/AutoSaveTimer.cs` (16-01 산출물). 이 플랜이 수정할 두 지점의 **현재** 내용:
```csharp
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Bootstrap()
    {
        if (_instance != null) return;
        GameObject go = new GameObject("AutoSave");
        go.AddComponent<AutoSaveTimer>();
    }
```
```csharp
    private bool TryAutoSave()
    {
        if (!CanAutoSave()) return false;

        // Reset before the write so a throwing File.WriteAllText cannot spin this every tick.
        // SaveLoadManager.Save() also calls NotifySaveWritten(), which lands on the same 0.
        _elapsedSeconds = 0f;

        // (... D-07 / D-07b / D-01 주석 6줄 ...)
        SaveLoadManager.Instance.SaveAnywhere();
        return true;
    }
```

런타임 UI 생성 선례 `Assets/Script/SaveSlotDialog.cs:51-59` (이 형태를 미러링):
```csharp
        var canvas = gameObject.AddComponent<Canvas>();
        canvas.renderMode = RenderMode.ScreenSpaceOverlay;
        canvas.sortingOrder = 30000;
        var scaler = gameObject.AddComponent<CanvasScaler>();
        scaler.uiScaleMode = CanvasScaler.ScaleMode.ScaleWithScreenSize;
        scaler.referenceResolution = new Vector2(1920, 1080);
        gameObject.AddComponent<GraphicRaycaster>();   // <- 알림에는 붙이지 않는다 (결정 5)
```
런타임 TMP 생성 선례 `Assets/Player/Script/PlayerInteractionPrompt.cs:61-69`:
```csharp
        var textObject = new GameObject("Key", typeof(RectTransform));
        textObject.transform.SetParent(canvasObject.transform, false);
        label = textObject.AddComponent<TextMeshProUGUI>();
        label.fontSize = 28;
        label.alignment = TextAlignmentOptions.Center;
        label.color = Color.white;
        label.outlineWidth = 0.2f;
        label.raycastTarget = false;
        label.rectTransform.sizeDelta = new Vector2(240f, 60f);
```
필요한 타입의 네임스페이스: `Canvas`/`RenderMode`/`RectTransform` = `UnityEngine`, `CanvasScaler` = `UnityEngine.UI`, `TMP_Text`/`TextMeshProUGUI`/`TextAlignmentOptions` = `TMPro`. `TMP_Text.alpha` 는 0~1 float 프로퍼티로, 색을 재조립하지 않고 투명도만 바꾼다.
</interfaces>

<encoding_note>
- **신규 파일 `Assets/SaveSystem/Script/AutoSaveNotice.cs` 는 UTF-8 (BOM 없음) + LF** 로 작성한다. 선례는 같은 성격의 런타임 UI 이면서 한국어 문자열을 담은 `Assets/Script/SaveSlotDialog.cs` 다 (`file` 출력: `Unicode text, UTF-8 text`, `head -c 3` = `757369`).
- **이 파일에서 허용되는 비-ASCII 는 문자열 리터럴 `"자동 저장됨"` 하나뿐이다.** 주석은 전부 영어/ASCII 로 쓴다. `자동 저장됨` 의 UTF-8 바이트는 정확히 `ec9e90 eb8f99 20 eca080 ec9ea5 eb90a8` 이다 (아래 인수 기준에서 바이트 단위로 확인한다).
- `Assets/SaveSystem/Script/AutoSaveTimer.cs` 는 **순수 ASCII + CRLF + BOM 없음**이다. 여기에 추가하는 줄도 전부 ASCII 여야 하고, 파일 전체 인코딩·줄끝을 바꾸지 말 것. 이 저장소는 Phase 9/10/11 에서 인코딩 왕복으로 비-ASCII 바이트가 U+FFFD 로 훼손된 사고 이력이 있다.
- `.meta` 는 ASCII + LF, 2줄 + 마지막 개행 1개.
</encoding_note>

<build_note>
`Assembly-CSharp.csproj` 는 `<Compile Include="..." />` 를 파일마다 명시적으로 나열하고 `.gitignore` 대상(추적 안 됨)이다. **신규 `.cs` 는 줄을 추가하지 않으면 `dotnet build` 가 컴파일하지 않는다.**
기준선: `dotnet build Assembly-CSharp.csproj --nologo -v quiet` → `경고 6개` / `오류 0개`. 신규 코드는 경고를 늘리지 않아야 한다.
</build_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AutoSaveNotice.cs 신규 작성 — 런타임 생성 우하단 라벨 + 1.2초 유지 / 0.6초 페이드아웃 (D-10)</name>
  <files>Assets/SaveSystem/Script/AutoSaveNotice.cs, Assets/SaveSystem/Script/AutoSaveNotice.cs.meta, Assembly-CSharp.csproj</files>

  <read_first>
    - `Assets/Script/SaveSlotDialog.cs` 의 51~89행 + 191~204행 (런타임 Canvas/CanvasScaler/TMP 라벨 생성 선례, 한국어 리터럴이 들어간 UTF-8 파일 형태)
    - `Assets/Player/Script/PlayerInteractionPrompt.cs` 의 53~79행 (`AddComponent<TextMeshProUGUI>()` 로 폰트 에셋 참조 없이 TMP 생성하는 선례)
    - `Assets/SaveSystem/Script/AutoSaveTimer.cs` (16-01 산출물 — 이 컴포넌트가 붙을 `AutoSave` 오브젝트가 `DontDestroyOnLoad` 임을 확인)
    - `Assets/SaveSystem/Script/SaveData.cs.meta` (신규 `.meta` 2줄 형식)
    - `Assembly-CSharp.csproj` 의 130~140행 (`<Compile Include>` 삽입 위치)
  </read_first>

  <action>
**(1) 신규 파일 `Assets/SaveSystem/Script/AutoSaveNotice.cs` 를 아래 내용 그대로 생성한다.** 인코딩 UTF-8(BOM 없음) + LF. 상수값·문자열·메서드명을 바꾸지 말 것 — 전부 검증 게이트가 걸려 있다.

```csharp
// Phase 16 - AutoSaveNotice (D-10)
// One purpose only: after an interval autosave, hold a short label in the corner for
// HoldSeconds and then fade it out over FadeSeconds, which lands inside D-10's "1 to 2
// seconds". Without it the player has no way of knowing progress was preserved.
//
// Deliberately NOT a general toast system: generalising this to item pickups, quests and the
// like is explicitly out of scope for this phase. It also deliberately does not reuse
// HealPopup / HealPopupSpawner - those are world space combat popups anchored to a character.
//
// The canvas is built in code and parented under the DontDestroyOnLoad "AutoSave" object, so
// no scene and no prefab has to be edited and the label survives every scene load. This
// mirrors SaveSlotDialog.Build(), the project's existing runtime-built UI.

using System.Collections;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class AutoSaveNotice : MonoBehaviour
{
    public const string Message = "자동 저장됨";
    public const float HoldSeconds = 1.2f;
    public const float FadeSeconds = 0.6f;

    private TMP_Text _label;
    private Coroutine _routine;

    // Called by AutoSaveTimer right after a successful write. Manual saves do not use this:
    // they already have their own feedback in GameSettingsPanel / SaveSlotDialog.
    public void Show()
    {
        if (_label == null) Build();
        if (_routine != null) StopCoroutine(_routine);
        _label.text = Message;
        _routine = StartCoroutine(ShowRoutine());
    }

    private void Build()
    {
        GameObject canvasObject = new GameObject("AutoSave Notice Canvas", typeof(RectTransform));
        canvasObject.transform.SetParent(transform, false);

        Canvas canvas = canvasObject.AddComponent<Canvas>();
        canvas.renderMode = RenderMode.ScreenSpaceOverlay;
        // Above the gameplay HUD, below SaveSlotDialog (30000) so a save dialog still wins.
        canvas.sortingOrder = 20000;

        CanvasScaler scaler = canvasObject.AddComponent<CanvasScaler>();
        scaler.uiScaleMode = CanvasScaler.ScaleMode.ScaleWithScreenSize;
        scaler.referenceResolution = new Vector2(1920, 1080);

        // No GraphicRaycaster and no EventSystem on purpose: this label is never clicked, and a
        // raycaster would let it swallow clicks meant for whatever sits underneath.

        GameObject labelObject = new GameObject("Label", typeof(RectTransform));
        labelObject.transform.SetParent(canvasObject.transform, false);

        TextMeshProUGUI label = labelObject.AddComponent<TextMeshProUGUI>();
        // No font asset is assigned: AddComponent picks up the TMP Settings default font, which
        // is the only option for a script that no inspector can wire up yet.
        label.fontSize = 32;
        label.alignment = TextAlignmentOptions.BottomRight;
        label.color = Color.white;
        label.outlineWidth = 0.2f;
        label.raycastTarget = false;

        // Bottom right corner, 48 px in from both edges. Top left and top centre are taken by
        // the player health UI and the boss health bar.
        RectTransform rect = label.rectTransform;
        rect.anchorMin = new Vector2(1f, 0f);
        rect.anchorMax = new Vector2(1f, 0f);
        rect.pivot = new Vector2(1f, 0f);
        rect.sizeDelta = new Vector2(420f, 60f);
        rect.anchoredPosition = new Vector2(-48f, 48f);

        _label = label;
    }

    // Unscaled time: if the player opens the pause menu right after a save, Time.timeScale
    // goes to 0 and a scaled fade would freeze the label on screen.
    private IEnumerator ShowRoutine()
    {
        _label.alpha = 1f;

        float held = 0f;
        while (held < HoldSeconds)
        {
            held += Time.unscaledDeltaTime;
            yield return null;
        }

        float faded = 0f;
        while (faded < FadeSeconds)
        {
            faded += Time.unscaledDeltaTime;
            _label.alpha = 1f - Mathf.Clamp01(faded / FadeSeconds);
            yield return null;
        }

        _label.alpha = 0f;
        _routine = null;
    }
}
```

**(2) 신규 `.meta` 파일 `Assets/SaveSystem/Script/AutoSaveNotice.cs.meta` 를 손으로 만든다.** 16-01 Task 1 과 동일한 절차다 — 정확히 2줄 + 마지막 개행 1개:
```
fileFormatVersion: 2
guid: <새로 생성한 32자 소문자 16진수>
```
guid 생성: `openssl rand -hex 16` (없으면 `python -c "import uuid;print(uuid.uuid4().hex)"`). 생성 후 `grep -rl "<생성한guid>" Assets Packages ProjectSettings` 가 새 `.meta` 1개만 출력하는지 확인한다. 16-01 이 만든 `AutoSaveTimer.cs.meta` 의 guid 와도 달라야 한다.

**(3) `Assembly-CSharp.csproj` 에 컴파일 항목 1줄을 추가한다.**
16-01 이 추가한 `<Compile Include="Assets\SaveSystem\Script\AutoSaveTimer.cs" />` 줄 **바로 위에** 삽입한다:
```xml
    <Compile Include="Assets\SaveSystem\Script\AutoSaveNotice.cs" />
```
(16-01 의 줄이 Unity 재생성으로 사라져 있으면 두 줄을 모두 넣는다.) 이 파일은 `.gitignore` 대상이라 커밋되지 않는다.

**절대 하지 말 것:**
- `Assets/SaveSystem/Script/AutoSaveTimer.cs` 수정 (이 태스크에서는 0줄. Task 2 에서만).
- `Assets/SaveSystem/Script/SaveLoadManager.cs` 수정 (알림을 `Save()` 에 걸면 수동 저장에도 중복으로 뜬다 — 결정 8).
- `Assets/Script/Combat/HealPopup.cs` / `HealPopupSpawner.cs` 수정 또는 재사용 (D-10: 전투용 월드 스페이스라 대상 아님).
- `Assets/Script/SaveSlotDialog.cs`, `Assets/Player/Script/PlayerInteractionPrompt.cs` 수정 (읽기 전용 선례).
- `Assets/SaveSystem/Script/SettingsData.cs` / `Assets/Player/Script/Menu/GameSettingsPanel.cs` 수정 (D-11: 알림 on/off 설정 금지).
- 씬 `.unity` / `Assets/Resources/PersistentManagers.prefab` 수정.
- 범용 알림/토스트 매니저, 큐, 여러 메시지 타입, 인스펙터 노출 필드 추가 (명시적 범위 밖 — "일반화하지 않는다").
- 폰트 에셋을 `Resources.Load` 로 찾거나 `[SerializeField]` 로 노출하는 코드 추가.
  </action>

  <verify>
    <automated>dotnet build Assembly-CSharp.csproj --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `test -f Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 성공한다.
    - `dotnet build Assembly-CSharp.csproj --nologo -v quiet` 출력에 `오류 0개` 가 포함되고 경고 개수가 기준선과 같은 `경고 6개` 다.
    - `grep -c "AutoSaveNotice.cs" Assembly-CSharp.csproj` 가 `1` 을 출력한다.
    - `grep -c 'public const string Message = "자동 저장됨";' Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1` 을 출력한다.
    - `grep -o "자동 저장됨" Assets/SaveSystem/Script/AutoSaveNotice.cs | head -1 | tr -d "\n" | xxd -p` 가 `ec9e90eb8f9920eca080ec9ea5eb90a8` 을 출력한다 (한국어 바이트 무손상).
    - `grep -c "public const float HoldSeconds = 1.2f;" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1`, `grep -c "public const float FadeSeconds = 0.6f;" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1` 을 출력한다.
    - `grep -c "canvas.renderMode = RenderMode.ScreenSpaceOverlay;" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1`, `grep -c "canvas.sortingOrder = 20000;" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1` 을 출력한다.
    - `grep -c "GraphicRaycaster\|EventSystem" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `0` 을 출력한다 (결정 5 — 입력을 가로막지 않음).
    - `grep -c "label.raycastTarget = false;" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1` 을 출력한다.
    - `grep -c "rect.anchoredPosition = new Vector2(-48f, 48f);" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1`, `grep -c "rect.pivot = new Vector2(1f, 0f);" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1` 을 출력한다 (우하단 고정).
    - `grep -c "Time.unscaledDeltaTime" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `2` 를 출력한다 (유지 루프 + 페이드 루프).
    - `grep -c "Time.deltaTime" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `0` 을 출력한다 (스케일 시간 미사용).
    - `grep -c "HealPopup\|Resources.Load\|SerializeField" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `0` 을 출력한다 (재사용 금지 + 인스펙터 배선 없음).
    - `grep -cE "void Update\(\)|async |Task<|await " Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `0` 을 출력한다 (코루틴 관행).
    - `grep -c $'\xef\xbf\xbd' Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `0` 을 출력한다 (U+FFFD 훼손 없음).
    - `head -c 3 Assets/SaveSystem/Script/AutoSaveNotice.cs | xxd -p` 가 `efbbbf` 가 **아니다** (BOM 없음).
    - `grep -cP "[^\x00-\x7F]" Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `1` 을 출력한다 (비-ASCII 는 `Message` 리터럴 1줄뿐 — 주석은 전부 ASCII).
    - `wc -l < Assets/SaveSystem/Script/AutoSaveNotice.cs` 가 `60` 이상이다.
    - `cat Assets/SaveSystem/Script/AutoSaveNotice.cs.meta` 가 정확히 2줄이고 1행 `fileFormatVersion: 2`, 2행 `guid: ` + 32자 소문자 16진수다. `grep -rl "<생성한guid>" Assets Packages ProjectSettings` 가 이 `.meta` 한 줄만 출력한다.
    - `git status --porcelain Assets/SaveSystem/Script/AutoSaveTimer.cs Assets/SaveSystem/Script/SaveLoadManager.cs Assets/Script/SaveSlotDialog.cs Assets/Script/Combat/HealPopup.cs Assets/Script/Combat/HealPopupSpawner.cs Assets/Player/Script/PlayerInteractionPrompt.cs Assets/SaveSystem/Script/SettingsData.cs Assets/Player/Script/Menu/GameSettingsPanel.cs` 가 빈 출력이다.
    - `git status --porcelain Assets/Scenes/` 의 출력이 이 태스크 실행 **전과 동일**하다.
    - 커밋 1개. pathspec 은 `Assets/SaveSystem/Script/AutoSaveNotice.cs Assets/SaveSystem/Script/AutoSaveNotice.cs.meta` 두 개만.
  </acceptance_criteria>

  <done>`AutoSaveNotice.Show()` 가 호출되면 우하단에 `자동 저장됨` 라벨이 나타나 1.2초 유지 후 0.6초에 걸쳐 알파 0 이 되고, 캔버스는 최초 1회만 생성돼 재사용된다. 컴파일 통과.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: AutoSaveTimer 에 알림 연결 — 부트스트랩에 컴포넌트 부착 + 저장 성공 직후 Show() 호출 (D-10)</name>
  <files>Assets/SaveSystem/Script/AutoSaveTimer.cs</files>

  <read_first>
    - `Assets/SaveSystem/Script/AutoSaveTimer.cs` (전체 — `Bootstrap()` 과 `TryAutoSave()` 의 현재 정확한 본문)
    - `Assets/SaveSystem/Script/AutoSaveNotice.cs` (Task 1 산출물 — `public void Show()` 시그니처 확인)
  </read_first>

  <action>
`Assets/SaveSystem/Script/AutoSaveTimer.cs` 에 **정확히 7줄만 추가**한다 (삭제·수정 0줄).

**(1) `Bootstrap()` 에 1줄 추가.**
변경 전:
```csharp
        if (_instance != null) return;
        GameObject go = new GameObject("AutoSave");
        go.AddComponent<AutoSaveTimer>();
```
변경 후:
```csharp
        if (_instance != null) return;
        GameObject go = new GameObject("AutoSave");
        go.AddComponent<AutoSaveTimer>();
        go.AddComponent<AutoSaveNotice>();
```

**(2) `TryAutoSave()` 의 `SaveLoadManager.Instance.SaveAnywhere();` 줄 **바로 아래**에 빈 줄 1 + 주석 3줄 + 코드 2줄을 추가한다.**
변경 전 (메서드 말미):
```csharp
        SaveLoadManager.Instance.SaveAnywhere();
        return true;
    }
```
변경 후 (정확히 이것):
```csharp
        SaveLoadManager.Instance.SaveAnywhere();

        // D-10: tell the player the write happened. Resolved with GetComponent instead of a
        // cached field because this runs once every three minutes, and it keeps the call free
        // of any assumption about the order Bootstrap() adds the two components in.
        AutoSaveNotice notice = GetComponent<AutoSaveNotice>();
        if (notice != null) notice.Show();

        return true;
    }
```

주의:
- 호출 지점은 `TryAutoSave()` **안뿐**이다. `AutoSaveRoutine()` 이나 `NotifySaveWritten()` 에 넣지 말 것 — 후자는 수동 저장에서도 호출되므로 알림이 중복으로 뜬다 (결정 8).
- `Awake()` 에 `GetComponent<AutoSaveNotice>()` 캐시 필드를 만들지 말 것. `AddComponent<AutoSaveTimer>()` 가 그 자리에서 `Awake()` 를 실행하므로 부착 순서에 따라 조용히 null 이 된다.
- `using` 추가 불필요 (같은 어셈블리, 전역 네임스페이스).
- 이 파일은 **순수 ASCII + CRLF + BOM 없음**이다. 추가 주석도 전부 ASCII. 기존 줄을 재포맷하지 말 것.
- `SaveLoadManager.cs` 는 이 태스크에서 0줄 수정.
  </action>

  <verify>
    <automated>dotnet build Assembly-CSharp.csproj --nologo -v quiet</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c "go.AddComponent<AutoSaveNotice>();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다.
    - `grep -c "AutoSaveNotice notice = GetComponent<AutoSaveNotice>();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다.
    - `grep -c "if (notice != null) notice.Show();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `1` 을 출력한다.
    - `grep -n "notice.Show();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 의 줄번호가 `grep -n "SaveLoadManager.Instance.SaveAnywhere();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 의 줄번호보다 크다 (저장 성공 뒤에 알림).
    - `git diff --numstat -- Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `7	0	Assets/SaveSystem/Script/AutoSaveTimer.cs` 다 (추가 7줄, 삭제 0줄).
    - `git diff -U0 -- Assets/SaveSystem/Script/AutoSaveTimer.cs | grep -c "^-[^-]"` 가 `0` 을 출력한다.
    - `grep -c "AutoSaveNotice" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `3` 을 출력한다 (AddComponent 1 + 지역변수 선언 2회 등장한 1줄 → 실제 등장 3회: `AddComponent<AutoSaveNotice>`, `AutoSaveNotice notice`, `GetComponent<AutoSaveNotice>`).
    - `grep -c "AutoSaveNotice" Assets/SaveSystem/Script/SaveLoadManager.cs` 가 `0` 을 출력한다 (결정 8 — 수동 저장에 알림이 붙지 않음).
    - `grep -c "SaveLoadManager.Instance.SaveAnywhere();" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 여전히 `1` 이고, `grep -c "return PlayerStats.Instance != null;" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 여전히 `1`, `grep -c "public const float IntervalSeconds = 180f;" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 여전히 `1` 이다 (16-01 무회귀).
    - `grep -cP "[^\x00-\x7F]" Assets/SaveSystem/Script/AutoSaveTimer.cs` 가 `0` 을 출력한다 (ASCII 유지).
    - `file Assets/SaveSystem/Script/AutoSaveTimer.cs` 출력에 `CRLF` 가 포함된다.
    - `dotnet build Assembly-CSharp.csproj --nologo -v quiet` 가 `오류 0개` / `경고 6개` 다.
    - `git status --porcelain Assets/SaveSystem/Script/SaveLoadManager.cs` 가 빈 출력이다.
    - 커밋 1개. pathspec 은 `Assets/SaveSystem/Script/AutoSaveTimer.cs` 만.
  </acceptance_criteria>

  <done>자동저장이 성공하면 `AutoSaveNotice.Show()` 가 정확히 1회 호출되고, 수동 저장 경로에는 알림이 전혀 붙지 않는다.</done>
</task>

</tasks>

<verification>
1. `dotnet build Assembly-CSharp.csproj --nologo -v quiet` → `오류 0개`, `경고 6개`.
2. 이 플랜이 만든 커밋 2개의 변경 파일 합집합이 정확히 `Assets/SaveSystem/Script/AutoSaveNotice.cs`, `Assets/SaveSystem/Script/AutoSaveNotice.cs.meta`, `Assets/SaveSystem/Script/AutoSaveTimer.cs` 3개뿐이다.
3. `grep -rn "notice.Show()" Assets --include=*.cs` 가 정확히 1줄이고 `AutoSaveTimer.cs` 안이다.
4. `grep -rn "AutoSaveNotice" Assets --include=*.cs` 의 파일이 `AutoSaveNotice.cs` 와 `AutoSaveTimer.cs` 둘뿐이다.
5. `git status --porcelain Assets/Script/Combat/` 이 빈 출력이다 (`HealPopup` 계열 무수정).
6. `.meta` 2개(`AutoSaveTimer.cs.meta`, `AutoSaveNotice.cs.meta`)의 guid 가 서로 다르고 저장소에서 각각 유일하다.
</verification>

<success_criteria>
- `AutoSaveNotice.cs` 가 존재하고 컴파일되며 `Message = "자동 저장됨"` / `HoldSeconds = 1.2f` / `FadeSeconds = 0.6f` / 우하단 앵커 / unscaled 페이드를 갖는다.
- 한국어 리터럴 바이트가 무손상이고 U+FFFD 가 0건이다.
- `AutoSaveTimer.cs` 변경이 정확히 7줄 추가 / 0줄 삭제다.
- `SaveLoadManager.cs`, `HealPopup` 계열, `SettingsData`, `GameSettingsPanel`, 씬, 프리팹 변경 0건.
- 컴파일 에러 0, 경고 증가 0.
</success_criteria>

<output>
완료 후 `.planning/phases/16-interval-autosave/16-02-SUMMARY.md` 를 작성한다.
</output>
