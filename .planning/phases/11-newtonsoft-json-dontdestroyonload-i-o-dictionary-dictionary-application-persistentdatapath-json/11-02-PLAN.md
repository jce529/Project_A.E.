---
phase: 11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - Assets/SaveSystem/Script/SaveLoadManager.cs
autonomous: true
requirements: [D-01, D-02, D-04, D-05, D-06]
must_haves:
  truths:
    - "SaveLoadManager exists as a DontDestroyOnLoad singleton reachable via SaveLoadManager.Instance from any scene without manual scene placement"
    - "Calling Save() writes a single save.json under Application.persistentDataPath"
    - "During normal play no file I/O happens - only the in-memory SaveData is mutated"
    - "HasSaveFile() reports whether a save exists without loading it"
    - "NewGame() resets memory only and does not overwrite the existing file"
    - "LoadGame() reads the file, then asynchronously loads the saved scene and restores player stats after the scene is active"
  artifacts:
    - path: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      provides: "Singleton save/load manager, file I/O, async scene load coroutine, public API"
      contains: "public static SaveLoadManager Instance"
      min_lines: 120
  key_links:
    - from: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      to: "PlayerSpawner.targetSpawnPointName"
      via: "static field assignment before SceneManager.LoadSceneAsync"
      pattern: "PlayerSpawner\\.targetSpawnPointName\\s*="
    - from: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      to: "PlayerStats.RestoreStats"
      via: "post-scene-load stat restore"
      pattern: "RestoreStats\\("
    - from: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      to: "Application.persistentDataPath/save.json"
      via: "Path.Combine + File.WriteAllText / File.ReadAllText"
      pattern: "Path\\.Combine\\(Application\\.persistentDataPath"
---

<objective>
`SaveLoadManager` 싱글톤을 신규 작성한다. 메모리 캐시 기반으로 동작하며, 파일 I/O 는 `Save()` / `LoadGame()` 호출 시점에만 발생한다. 저장 파일은 `Application.persistentDataPath/save.json` 단일 슬롯. 로드 시 프로젝트 최초의 `LoadSceneAsync` 를 코루틴으로 수행하고, 씬 활성화 후 플레이어 스탯을 복원한다.

Purpose: D-01 이 말하는 "매니저만 저장 로직을 소유하고 나머지는 전부 호출" 구조의 매니저 본체. 11-03 의 통합 지점들(Checkpoint, 보스 4종)이 호출할 공개 API 를 확정한다.
Output: `Assets/SaveSystem/Script/SaveLoadManager.cs` 신규 파일 1개.
</objective>

<execution_context>
@C:/Users/chang/Downloads/Projeect_A.E (2) (1)/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chang/Downloads/Projeect_A.E (2) (1)/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-CONTEXT.md
@.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-RESEARCH.md
@.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-01-SUMMARY.md

<interfaces>
<!-- 실행자가 필요로 하는 계약 전부. 코드베이스를 뒤질 필요 없다. -->

11-01 이 만든 스키마 (Assets/SaveSystem/Script/SaveData.cs):
```csharp
public class SaveData
{
    public int SaveVersion = 1;
    public string SceneName = "";
    public string SpawnPointName = "";
    public PlayerStatsSaveData PlayerStats = new PlayerStatsSaveData();
    public Dictionary<string, bool> BossProgress = new Dictionary<string, bool>();
    public Dictionary<string, bool> MapGimmickState = new Dictionary<string, bool>();
    public List<string> Items = new List<string>();
}
public class PlayerStatsSaveData { public float Health; public float MaxHealth; public float MaxTotalHealth; }
```

11-01 이 추가한 복원 API (Assets/Player/Script/PlayerStats.cs):
```csharp
public class PlayerStats : HP
{
    public static PlayerStats Instance { get; }   // lazy FindAnyObjectByType<PlayerStats>()
    public float Health { get; }        // HP 상속, getter only
    public float MaxHealth { get; }     // HP 상속, getter only
    public float MaxTotalHealth { get; }
    public void RestoreStats(float savedHealth, float savedMaxHealth, float savedMaxTotalHealth);
}
```

좌표 복원의 유일한 실동작 경로 (Assets/map/script/PlayerSpawner.cs) — Player 프리팹에 부착되어 있고 DontDestroyOnLoad 가 아니다. 씬마다 새 Player 가 생기고 그 Start() 가 ApplySpawn() 을 자동 호출한다:
```csharp
public class PlayerSpawner : MonoBehaviour
{
    public static string targetSpawnPointName = "";   // 씬 로드 "전에" 세팅해두면 새 씬의 Player 가 스스로 소비
    void Start() { ApplySpawn(); }
    public void ApplySpawn();   // 이름이 일치하는 GameObject 를 찾아 transform.position 대입 후 targetSpawnPointName = "" 로 self-clear
}
```

실제 사용 중인 호출자 (Assets/map/script/SignpostPortal.cs) — LoadGame 이 그대로 미러링해야 할 패턴:
```csharp
PlayerSpawner.targetSpawnPointName = spawnPointName;
SceneManager.LoadScene(nextSceneName);
```

프로젝트 싱글톤 관례 (GameManager.cs / GameStateManager.cs / InputHandler.cs 가 전부 동일 형태):
```csharp
private void Awake()
{
    if (Instance == null) { Instance = this; DontDestroyOnLoad(gameObject); }
    else { Destroy(gameObject); }
}
```

빌드 세팅에 등록된 씬: `Assets/Scenes/InGame.unity`(파일 부재 - 스테일 엔트리), `Assets/Scenes/MainMenu.unity`, `Assets/Scenes/1 stage.unity`.
따라서 새 게임 기본 씬 이름은 `1 stage` 다 (`Tutorial Map` 은 빌드 세팅 미등록이라 LoadSceneAsync 로 로드 불가).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SaveLoadManager.cs 신규 작성 — 싱글톤 + 부트스트랩 + 메모리 캐시 + 저장 API</name>
  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>
  <read_first>
    - Assets/SaveSystem/Script/SaveData.cs (11-01 산출물 — 필드명/타입 확인)
    - Assets/map/script/GameManager.cs (프로젝트 싱글톤 Awake 관례 원본)
    - .planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-RESEARCH.md (Pattern 1, Pattern 4, Pitfall 5)
  </read_first>
  <action>
`Assets/SaveSystem/Script/SaveLoadManager.cs` 를 신규 작성한다. 이 태스크에서는 아래 코드를 그대로 작성한다 (로드 흐름은 Task 2 에서 이어 붙인다). 주석은 전부 ASCII 문자만 사용한다.

```csharp
// Phase 11 - SaveLoadManager
// Decisions: D-01 (manager owns save/load logic; callers call in, no event bus),
//            D-02 (single slot: one save.json under Application.persistentDataPath),
//            D-04 (public API only - no menu UI, no loading screen),
//            D-05 (restore position via PlayerSpawner.targetSpawnPointName, not raw XY),
//            D-06 (NewGame resets memory only; the file is overwritten on the next Save()).
//
// In-play behaviour: everything mutates _data in memory. File I/O happens ONLY inside
// Save() (checkpoint interaction / boss defeat) and LoadGame() (continue / checkpoint revive).

using System.Collections;
using System.Collections.Generic;
using System.IO;
using Newtonsoft.Json;
using UnityEngine;
using UnityEngine.SceneManagement;

public class SaveLoadManager : MonoBehaviour
{
    #region Singleton
    public static SaveLoadManager Instance { get; private set; }

    // The manager must exist in every scene without the designer placing it by hand,
    // because Checkpoint.cs and the four boss death sites call SaveLoadManager.Instance
    // directly. Creating it before the first scene loads removes both the manual editor
    // step and the null-reference risk when entering Play mode from an arbitrary scene.
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    private static void Bootstrap()
    {
        if (Instance != null) return;
        GameObject go = new GameObject("SaveLoadManager");
        go.AddComponent<SaveLoadManager>();
    }

    private void Awake()
    {
        if (Instance == null)
        {
            Instance = this;
            DontDestroyOnLoad(gameObject);
        }
        else
        {
            Destroy(gameObject);
        }
    }
    #endregion

    public const string SaveFileName = "save.json";

    // Fallback location used by NewGame(). "1 stage" is the only gameplay scene currently
    // registered in EditorBuildSettings, so it is the only value LoadSceneAsync can resolve.
    [SerializeField] private string defaultSceneName = "1 stage";
    [SerializeField] private string defaultSpawnPointName = "";

    private static readonly JsonSerializerSettings SaveSettings = new JsonSerializerSettings
    {
        Formatting = Formatting.Indented,
        NullValueHandling = NullValueHandling.Include,
    };

    private SaveData _data = new SaveData();

    // Read-only view for future systems (boss progress / gimmick queries).
    public SaveData Data { get { return _data; } }

    public static string SavePath
    {
        get { return Path.Combine(Application.persistentDataPath, SaveFileName); }
    }

    public bool HasSaveFile()
    {
        return File.Exists(SavePath);
    }

    // D-06: memory-only reset. The existing file on disk is intentionally left untouched;
    // it gets overwritten on the next Save() trigger (checkpoint or boss defeat).
    public void NewGame()
    {
        _data = new SaveData();
        _data.SceneName = defaultSceneName;
        _data.SpawnPointName = defaultSpawnPointName;
        Debug.Log("[SaveLoadManager] NewGame - memory reset only, file untouched.");
    }

    // Core write. Captures live player stats into the memory cache, then serializes.
    public void Save()
    {
        CapturePlayerStats();
        string json = JsonConvert.SerializeObject(_data, SaveSettings);
        File.WriteAllText(SavePath, json);
        Debug.Log("[SaveLoadManager] Saved to " + SavePath);
    }

    // D-01 integration point A: Checkpoint.cs calls this on S-key activation.
    // The checkpoint's own GameObject name doubles as the PlayerSpawner spawn point name.
    public void SaveAtCheckpoint(string checkpointName)
    {
        _data.SceneName = SceneManager.GetActiveScene().name;
        _data.SpawnPointName = checkpointName;
        Save();
    }

    // D-01 integration point B: the four boss death sites call this.
    // RESEARCH Open Question 1 resolution: a boss defeat does NOT introduce a new spawn
    // point. It records the boss id and reuses whatever scene/spawn point the last
    // checkpoint activation stored, so respawn semantics stay checkpoint-based.
    public void SaveOnBossDefeated(string bossId)
    {
        if (!string.IsNullOrEmpty(bossId))
        {
            _data.BossProgress[bossId] = true;
        }
        Save();
    }

    public bool IsBossDefeated(string bossId)
    {
        bool defeated;
        return _data.BossProgress.TryGetValue(bossId, out defeated) && defeated;
    }

    private void CapturePlayerStats()
    {
        PlayerStats ps = PlayerStats.Instance;
        if (ps == null)
        {
            Debug.LogWarning("[SaveLoadManager] PlayerStats.Instance is null - player stats not captured.");
            return;
        }
        _data.PlayerStats.Health = ps.Health;
        _data.PlayerStats.MaxHealth = ps.MaxHealth;
        _data.PlayerStats.MaxTotalHealth = ps.MaxTotalHealth;
    }
}
```

지켜야 할 제약:
- `async` / `await` / `Task` 를 어디에도 쓰지 않는다. 이 코드베이스에는 전례가 0건이며 모든 비동기 유사 동작은 `IEnumerator` / `StartCoroutine` 이다.
- `Update()` 에서 저장하지 않는다 — 플레이 중 파일 I/O 0회가 이 페이즈의 핵심 요구다.
- `Portal.cs` / `GameManager.NextSpawnPointName` / `WoodBossStatSystem.cs` 를 참조하지 않는다 (고아 코드, D-05).
- `MainMenuUI.cs` 를 수정하지 않는다 (D-04, UI 범위 밖).
  </action>
  <verify>
    <automated>grep -c "public static SaveLoadManager Instance" "Assets/SaveSystem/Script/SaveLoadManager.cs"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `Assets/SaveSystem/Script/SaveLoadManager.cs` 존재
    - `grep -c "public static SaveLoadManager Instance { get; private set; }" ...SaveLoadManager.cs` == 1
    - `grep -c "DontDestroyOnLoad(gameObject);" ...SaveLoadManager.cs` == 1
    - `grep -c "RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)" ...SaveLoadManager.cs` == 1
    - `grep -c "using Newtonsoft.Json;" ...SaveLoadManager.cs` == 1
    - `grep -c "Path.Combine(Application.persistentDataPath, SaveFileName)" ...SaveLoadManager.cs` == 1
    - `grep -c "File.WriteAllText(SavePath, json);" ...SaveLoadManager.cs` == 1
    - 다음 공개 메서드가 각각 정확히 1개씩 존재: `public bool HasSaveFile()`, `public void NewGame()`, `public void Save()`, `public void SaveAtCheckpoint(string checkpointName)`, `public void SaveOnBossDefeated(string bossId)`
    - `grep -cE "\basync\b|\bawait\b|System\.Threading\.Tasks" ...SaveLoadManager.cs` == 0 (프로젝트 컨벤션: 코루틴 전용)
    - `grep -cE "void Update\(\)|void FixedUpdate\(\)|void LateUpdate\(\)" ...SaveLoadManager.cs` == 0 (플레이 중 주기적 I/O 없음)
    - `grep -cE "NextSpawnPointName|WoodBossStatsSystem|MainMenuUI" ...SaveLoadManager.cs` == 0 (고아 코드 / UI 미접촉)
    - `grep -cP "[^\x00-\x7F]" ...SaveLoadManager.cs` == 0 (비-ASCII 0개)
  </acceptance_criteria>
  <done>`SaveLoadManager` 싱글톤이 존재하고, 부트스트랩으로 씬 배치 없이 생성되며, `Save()` / `SaveAtCheckpoint()` / `SaveOnBossDefeated()` / `HasSaveFile()` / `NewGame()` 5개 공개 API 가 컴파일 가능한 형태로 선언되어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: LoadGame + 비동기 씬 로드 코루틴 + 스탯 복원 추가</name>
  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>
  <read_first>
    - Assets/SaveSystem/Script/SaveLoadManager.cs (Task 1 산출물)
    - Assets/map/script/PlayerSpawner.cs (targetSpawnPointName self-clear 동작 확인)
    - Assets/map/script/SignpostPortal.cs (미러링할 실사용 패턴)
  </read_first>
  <action>
Task 1 에서 만든 `SaveLoadManager.cs` 의 `CapturePlayerStats()` 메서드 아래, 클래스 닫는 중괄호 앞에 아래 블록을 **삽입만** 한다. Task 1 이 작성한 기존 멤버는 수정하지 않는다.

```csharp

    // ---- Load flow -------------------------------------------------------------
    // Entry point for BOTH "continue game" and "checkpoint revive". There is no separate
    // revive method: reviving at a checkpoint is exactly "load the last saved state".
    public void LoadGame()
    {
        if (!HasSaveFile())
        {
            Debug.LogWarning("[SaveLoadManager] LoadGame aborted - no save file at " + SavePath);
            return;
        }

        SaveData loaded = null;
        try
        {
            string json = File.ReadAllText(SavePath);
            loaded = JsonConvert.DeserializeObject<SaveData>(json, SaveSettings);
        }
        catch (System.Exception e)
        {
            Debug.LogError("[SaveLoadManager] Failed to read save file: " + e.Message);
            return;
        }

        if (loaded == null)
        {
            Debug.LogError("[SaveLoadManager] Save file deserialized to null - aborting load.");
            return;
        }

        _data = loaded;
        EnsureCollections();

        if (string.IsNullOrEmpty(_data.SceneName))
        {
            Debug.LogError("[SaveLoadManager] Saved SceneName is empty - aborting load.");
            return;
        }

        StartCoroutine(LoadSceneAndRestoreRoutine(_data.SceneName, _data.SpawnPointName));
    }

    // A hand-edited or older save file can contain nulls where the schema expects
    // collections. Re-materialize them so callers never have to null-check.
    private void EnsureCollections()
    {
        if (_data.PlayerStats == null) _data.PlayerStats = new PlayerStatsSaveData();
        if (_data.BossProgress == null) _data.BossProgress = new Dictionary<string, bool>();
        if (_data.MapGimmickState == null) _data.MapGimmickState = new Dictionary<string, bool>();
        if (_data.Items == null) _data.Items = new List<string>();
    }

    // Coroutine, not async/await - the codebase has zero async/Task usage.
    private IEnumerator LoadSceneAndRestoreRoutine(string sceneName, string spawnPointName)
    {
        // D-05: set the static field BEFORE the load, exactly like SignpostPortal does.
        // The freshly instantiated Player in the target scene consumes it in its own
        // PlayerSpawner.Start() -> ApplySpawn(), so no explicit "move the player" call is
        // needed here and no second respawn code path is invented.
        PlayerSpawner.targetSpawnPointName = spawnPointName;

        AsyncOperation op = SceneManager.LoadSceneAsync(sceneName);
        if (op == null)
        {
            Debug.LogError("[SaveLoadManager] LoadSceneAsync returned null for scene '" + sceneName +
                           "'. Is it registered in Build Settings?");
            yield break;
        }

        yield return op;

        // By the time the yield resumes, the new scene is active and its Awake/Start have
        // run - including HP.Awake() which forces health = maxHealth. Stats must therefore
        // be restored AFTER this point, not before.
        ApplyPlayerStatsFromSave();
    }

    private void ApplyPlayerStatsFromSave()
    {
        PlayerStats ps = PlayerStats.Instance;
        if (ps == null)
        {
            Debug.LogError("[SaveLoadManager] PlayerStats.Instance is null after scene load - stats NOT restored.");
            return;
        }

        ps.RestoreStats(_data.PlayerStats.Health, _data.PlayerStats.MaxHealth, _data.PlayerStats.MaxTotalHealth);
        Debug.Log("[SaveLoadManager] Restored stats: " + ps.Health + "/" + ps.MaxHealth +
                  " (maxTotal " + ps.MaxTotalHealth + "), scene=" + SceneManager.GetActiveScene().name +
                  ", spawnPoint=" + _data.SpawnPointName);
    }
```

핵심 순서 제약 (RESEARCH Pitfall 5 / Pattern 2):
1. `PlayerSpawner.targetSpawnPointName` 대입은 반드시 `LoadSceneAsync` **호출 전**이어야 한다. 나중에 대입하면 새 씬 Player 의 `Start()` 가 이미 지나가버려 위치 복원이 조용히 실패한다.
2. `ApplyPlayerStatsFromSave()` 는 반드시 `yield return op;` **뒤**여야 한다. 앞에 두면 `HP.Awake()` 의 `health = maxHealth` 가 복원값을 덮어쓴다.
3. `allowSceneActivation` 을 건드리지 않는다 (기본값 true). D-04 로 로딩 화면이 범위 밖이므로 수동 활성화 제어가 필요 없다.

마지막 `Debug.Log` 는 Pitfall 5 검증용이다 — Play 모드에서 이 로그 한 줄로 "씬 활성화 직후 PlayerStats.Instance 가 non-null 이고 복원값이 들어갔는지"를 눈으로 확인한다. 제거하지 말 것 (11-04 체크리스트가 이 로그를 참조한다).
  </action>
  <verify>
    <automated>grep -c "private IEnumerator LoadSceneAndRestoreRoutine(string sceneName, string spawnPointName)" "Assets/SaveSystem/Script/SaveLoadManager.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "public void LoadGame()" ...SaveLoadManager.cs` == 1
    - `grep -c "private IEnumerator LoadSceneAndRestoreRoutine" ...SaveLoadManager.cs` == 1
    - `grep -c "StartCoroutine(LoadSceneAndRestoreRoutine(" ...SaveLoadManager.cs` == 1
    - `grep -c "PlayerSpawner.targetSpawnPointName = spawnPointName;" ...SaveLoadManager.cs` == 1
    - `grep -c "SceneManager.LoadSceneAsync(sceneName)" ...SaveLoadManager.cs` == 1
    - `grep -c "yield return op;" ...SaveLoadManager.cs` == 1
    - 순서 게이트: `awk '/PlayerSpawner.targetSpawnPointName = spawnPointName;/{a=NR} /SceneManager.LoadSceneAsync\(sceneName\)/{b=NR} /yield return op;/{c=NR} /ApplyPlayerStatsFromSave\(\);/{d=NR} END{print (a<b && b<c && c<d) ? "OK" : "FAIL"}' Assets/SaveSystem/Script/SaveLoadManager.cs` == `OK`
    - `grep -c "ps.RestoreStats(_data.PlayerStats.Health, _data.PlayerStats.MaxHealth, _data.PlayerStats.MaxTotalHealth);" ...SaveLoadManager.cs` == 1
    - `grep -c "private void EnsureCollections()" ...SaveLoadManager.cs` == 1
    - `grep -cE "\basync\b|\bawait\b|System\.Threading\.Tasks" ...SaveLoadManager.cs` == 0
    - `grep -c "allowSceneActivation" ...SaveLoadManager.cs` == 0
    - `grep -cP "[^\x00-\x7F]" ...SaveLoadManager.cs` == 0
    - `grep -c "JsonConvert.DeserializeObject<SaveData>(json, SaveSettings)" ...SaveLoadManager.cs` == 1
  </acceptance_criteria>
  <done>`LoadGame()` 이 파일을 읽어 `_data` 로 캐싱하고, 코루틴이 스폰포인트 세팅 → 비동기 씬 로드 → 스탯 복원 순서로 정확히 실행되며, `async`/`await` 가 0건이다.</done>
</task>

</tasks>

<verification>
1. `Assets/SaveSystem/Script/SaveLoadManager.cs` 가 존재하고 공개 API 6종(`Save`, `SaveAtCheckpoint`, `SaveOnBossDefeated`, `LoadGame`, `HasSaveFile`, `NewGame`)을 전부 노출
2. 코루틴 순서 게이트(awk) 가 `OK`
3. `grep -rn "async\|await" Assets/SaveSystem/` 결과 0줄 — 프로젝트 무-async 컨벤션 유지
4. `git status --porcelain` 에 `Assets/map/script/portal.cs`, `Assets/map/script/GameManager.cs`, `Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossStatSystem.cs`, `Assets/Script/MainMenuUI.cs` 가 나타나지 않음 (고아 코드 + UI 범위 불가침)
5. `git diff --numstat Assets/Player/Script/PlayerStats.cs Packages/manifest.json` — 이 플랜에서 추가 변경 0
</verification>

<success_criteria>
- 씬 배치 없이 `SaveLoadManager.Instance` 가 항상 유효 (부트스트랩)
- 저장이 `Application.persistentDataPath/save.json` 단일 파일로 기록됨
- 로드가 코루틴 기반 `LoadSceneAsync` 로 수행되고, 좌표는 기존 `PlayerSpawner` 경로 재사용, 스탯은 씬 활성화 후 복원
- `NewGame()` 이 파일을 즉시 덮어쓰지 않음 (D-06)
</success_criteria>

<output>
완료 후 `.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-02-SUMMARY.md` 를 작성한다.
</output>
