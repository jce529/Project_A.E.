---
phase: 11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - Assets/SaveSystem/Script/SaveLoadManager.cs
  - Assets/SaveSystem/Check.md
autonomous: false
requirements: [D-01, D-02, D-03, D-03b, D-03c, D-04, D-05, D-06]
must_haves:
  truths:
    - "A tester can trigger Save / LoadGame / NewGame at runtime without any menu UI"
    - "A written checklist exists that a human can follow in Play mode to prove every phase decision holds"
    - "Static regression checks prove the five save triggers are wired and no forbidden file was touched"
    - "Play mode run confirms save.json is written to Application.persistentDataPath and that loading restores scene, spawn point, and player stats"
  artifacts:
    - path: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      provides: "ContextMenu verification hooks for runtime testing without UI"
      contains: "[ContextMenu("
    - path: "Assets/SaveSystem/Check.md"
      provides: "Play mode verification checklist + static regression command list"
      min_lines: 60
  key_links:
    - from: "Assets/SaveSystem/Check.md"
      to: "SaveLoadManager ContextMenu hooks"
      via: "checklist steps instruct the tester to use the component gear menu"
      pattern: "ContextMenu|Context Menu"
---

<objective>
Phase 11 의 검증 단계. UI 가 범위 밖(D-04)이라 저장/로드를 눌러볼 버튼이 없으므로, 인스펙터 컨텍스트 메뉴 훅을 추가해 런타임 검증 경로를 만들고, 프로젝트 관례(`Assets/Camera/Check.md`, `Assets/Enemy/WaterSpirit/Check.md`)에 따라 `Assets/SaveSystem/Check.md` 체크리스트를 작성한 뒤, 정적 회귀 검사와 Play 모드 실측 검증을 수행한다.

Purpose: "컴파일은 되지만 실제로는 저장이 안 된다" 류의 조용한 실패(특히 Group B 보스, 비동기 로드 타이밍)를 사람 눈으로 잡는다.
Output: `SaveLoadManager.cs` 에 ContextMenu 훅 추가, `Assets/SaveSystem/Check.md` 신규, Play 모드 검증 체크포인트.
</objective>

<execution_context>
@C:/Users/chang/Downloads/Projeect_A.E (2) (1)/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/chang/Downloads/Projeect_A.E (2) (1)/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-CONTEXT.md
@.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-RESEARCH.md
@.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-03-SUMMARY.md
@Assets/Camera/Check.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SaveLoadManager.cs 에 ContextMenu 런타임 검증 훅 추가</name>
  <files>Assets/SaveSystem/Script/SaveLoadManager.cs</files>
  <read_first>
    - Assets/SaveSystem/Script/SaveLoadManager.cs (11-02 산출물 전체 — 기존 메서드 시그니처 확인)
  </read_first>
  <action>
D-04 로 메뉴 UI 가 범위 밖이라 Play 모드에서 저장/로드를 눌러볼 수단이 없다. 인스펙터 톱니바퀴 메뉴로 호출 가능한 검증 전용 래퍼를 클래스 끝(닫는 중괄호 앞)에 **삽입만** 한다. 기존 메서드는 수정하지 않는다. 주석/문자열 전부 ASCII.

```csharp

    // ---- Verification hooks (D-04: no menu UI exists in this phase) -------------
    // Select the runtime "SaveLoadManager" GameObject in the Hierarchy during Play mode
    // and use the component's gear menu to drive these. See Assets/SaveSystem/Check.md.

    [ContextMenu("Phase11/1. Save Now")]
    private void DebugSaveNow()
    {
        Save();
    }

    [ContextMenu("Phase11/2. Load Game")]
    private void DebugLoadGame()
    {
        LoadGame();
    }

    [ContextMenu("Phase11/3. New Game (memory only)")]
    private void DebugNewGame()
    {
        NewGame();
    }

    [ContextMenu("Phase11/4. Log State")]
    private void DebugLogState()
    {
        Debug.Log("[SaveLoadManager] path=" + SavePath +
                  " exists=" + HasSaveFile() +
                  " scene=" + _data.SceneName +
                  " spawnPoint=" + _data.SpawnPointName +
                  " hp=" + _data.PlayerStats.Health + "/" + _data.PlayerStats.MaxHealth +
                  " maxTotal=" + _data.PlayerStats.MaxTotalHealth +
                  " bossProgress=" + _data.BossProgress.Count +
                  " gimmicks=" + _data.MapGimmickState.Count +
                  " items=" + _data.Items.Count);
    }
```

제약:
- 이 4개 메서드는 전부 `private` 이고 파라미터가 없어야 한다 (`[ContextMenu]` 요구사항).
- 기존 공개 API(`Save`, `LoadGame`, `NewGame`, `HasSaveFile`, `SaveAtCheckpoint`, `SaveOnBossDefeated`)의 시그니처/본문을 수정하지 않는다.
- 새 인스펙터 필드나 별도 디버그 MonoBehaviour 를 만들지 않는다 — 컴포넌트 1개, 파일 1개 원칙 유지.
- `#if UNITY_EDITOR` 로 감싸지 않는다. `[ContextMenu]` 는 런타임 코드에 있어도 빌드에 무해하며, 감싸면 Play 모드 중 인스펙터에서 안 보이는 경우가 생긴다.
  </action>
  <verify>
    <automated>grep -c "\[ContextMenu(\"Phase11/" "Assets/SaveSystem/Script/SaveLoadManager.cs"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[ContextMenu("Phase11/' Assets/SaveSystem/Script/SaveLoadManager.cs` == 4
    - 다음 4개 메서드가 각각 1개씩 존재: `private void DebugSaveNow()`, `private void DebugLoadGame()`, `private void DebugNewGame()`, `private void DebugLogState()`
    - `git diff --numstat Assets/SaveSystem/Script/SaveLoadManager.cs` 삭제 라인 수 == 0
    - `grep -c "public void Save()" Assets/SaveSystem/Script/SaveLoadManager.cs` == 1 그리고 `grep -c "public void LoadGame()" ...` == 1 (기존 API 보존)
    - `grep -cP "[^\x00-\x7F]" Assets/SaveSystem/Script/SaveLoadManager.cs` == 0
    - `grep -c "#if UNITY_EDITOR" Assets/SaveSystem/Script/SaveLoadManager.cs` == 0
  </acceptance_criteria>
  <done>Play 모드에서 Hierarchy 의 `SaveLoadManager` 오브젝트를 선택해 톱니바퀴 메뉴로 Save/Load/NewGame/LogState 4가지를 호출할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: Assets/SaveSystem/Check.md 검증 체크리스트 작성 + 정적 회귀 검사 실행</name>
  <files>Assets/SaveSystem/Check.md</files>
  <read_first>
    - Assets/Camera/Check.md (프로젝트의 Check.md 서술 형식/톤 전례)
    - Assets/SaveSystem/Script/SaveLoadManager.cs (ContextMenu 메뉴 경로명 확인)
    - .planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-CONTEXT.md (D-01~D-06 원문)
  </read_first>
  <action>
`Assets/SaveSystem/Check.md` 를 신규 작성한다. UTF-8 로 저장하며 한글 서술을 사용해도 된다 (신규 파일이라 인코딩 충돌 없음). 아래 섹션을 모두 포함한다.

**섹션 1) 정적 회귀 검사** — 아래 15개 명령과 기대값을 표로 기록하고, 이 태스크에서 **실제로 실행해 결과 열(PASS/FAIL + 실측값)을 채워 넣는다**:

| # | 명령 | 기대값 |
|---|------|--------|
| 1 | `grep -c '"com.unity.nuget.newtonsoft-json": "3.2.2"' Packages/manifest.json` | 1 |
| 2 | `grep -c "SaveLoadManager.Instance.SaveAtCheckpoint" Assets/map/script/Checkpoint.cs` | 1 |
| 3 | `grep -rn "SaveLoadManager.Instance.SaveOnBossDefeated" Assets/ \| wc -l` | 4 |
| 4 | `grep -rno 'SaveOnBossDefeated("[A-Za-z]*")' Assets/ \| sort -u -t: -k3 \| wc -l` | 4 (bossId 4종이 서로 다름) |
| 5 | `grep -rnE "\basync\b\|\bawait\b" Assets/SaveSystem/ \| wc -l` | 0 (코루틴 전용 컨벤션) |
| 6 | `grep -c "PlayerSpawner.targetSpawnPointName = spawnPointName;" Assets/SaveSystem/Script/SaveLoadManager.cs` | 1 |
| 7 | `grep -cE "float +x\|float +y\|Vector2\|Vector3" Assets/SaveSystem/Script/SaveData.cs` | 0 (D-05: 원시 좌표 저장 금지) |
| 8 | `grep -cP "[^\x00-\x7F]" Assets/map/script/Checkpoint.cs` | 9 (CP949 보존) |
| 9 | `grep -cP "[^\x00-\x7F]" Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossController.cs` | 7 (CP949 보존) |
| 10 | `grep -cP "[^\x00-\x7F]" Assets/Enemy/Tutorial/TutorialBoss/TutorialBossController.cs` | 92 |
| 11 | `grep -cP "[^\x00-\x7F]" Assets/Enemy/WaterSpirit/Script/SpiritStats.cs` | 8 |
| 12 | `grep -cP "[^\x00-\x7F]" Assets/Enemy/WaterMonster/Script/WaterMonsterStats.cs` | 8 |
| 13 | `grep -cP "[^\x00-\x7F]" Assets/Player/Script/PlayerStats.cs` | 12 |
| 14 | `git status --porcelain Assets/Script/HP.cs Assets/Enemy/NewBoss/Script/BossStatesSystem.cs Assets/Enemy/Tutorial/WoodBoss/BossController/WoodBossStatSystem.cs Assets/map/script/portal.cs Assets/map/script/GameManager.cs Assets/Script/MainMenuUI.cs` | 출력 없음 (불가침 6파일) |
| 15 | Phase 11 이 수정한 기존 6개 파일(manifest.json, PlayerStats.cs, Checkpoint.cs, TutorialBossController.cs, WoodBossController.cs, SpiritStats.cs, WaterMonsterStats.cs)에 대한 `git diff --numstat` 삭제 라인 합계 | 0 (전부 순수 삽입) |

**섹션 2) Play 모드 체크리스트 — 저장 (D-01, D-02)**
- [ ] Play 진입 직후 Hierarchy 에 `SaveLoadManager` GameObject 가 자동 생성되어 있고 DontDestroyOnLoad 섹션에 있다 (씬에 수동 배치하지 않았음에도)
- [ ] 컨텍스트 메뉴 `Phase11/4. Log State` 실행 → Console 에 `path=...save.json exists=False` (첫 실행 시)
- [ ] `1 stage` 씬에서 체크포인트에 들어가 S키를 누른다 → Console 에 `[SaveLoadManager] Saved to <경로>` 가 출력된다
- [ ] 출력된 경로(`Application.persistentDataPath`, Windows 기준 `%userprofile%/AppData/LocalLow/<회사명>/<제품명>/save.json`)를 탐색기로 열어 `save.json` 이 실제로 생성되었는지 확인한다
- [ ] `save.json` 내용에 `"SceneName": "1 stage"` 와 `"SpawnPointName": "<S키를 누른 체크포인트 GameObject 이름>"` 이 들어 있다
- [ ] `save.json` 에 `"PlayerStats"` 의 Health/MaxHealth/MaxTotalHealth 가 당시 실제 체력과 일치한다
- [ ] `save.json` 에 `"BossProgress": {}`, `"MapGimmickState": {}`, `"Items": []` 스텁이 존재한다 (D-03, D-03b)
- [ ] 체크포인트 저장 이후 그냥 돌아다니는 동안 Console 에 `Saved to` 로그가 추가로 찍히지 않는다 (플레이 중 파일 I/O 0회)

**섹션 3) Play 모드 체크리스트 — 보스 격파 자동 저장 (D-01, 두 아키텍처)**
- [ ] **Group A / TutorialBoss** (`HP.OnDeath` → `HandleDeath()` 경로): 튜토리얼 보스 처치 → Console 에 `Saved to` 출력 → `save.json` 의 `BossProgress` 에 `"TutorialBoss": true` 추가
- [ ] **Group A / WoodBoss** (`HP.OnDeath` → `HandleDeath()` 경로): 우드 보스 처치 → `BossProgress` 에 `"WoodBoss": true` 추가 (클리어 패널이 뜨기 전에 저장이 끝나야 함)
- [ ] **Group B / WaterSpirit** (`BossStatsSystem.Die()` 오버라이드 경로 — 이벤트 없음): 물의 정령 처치 → `BossProgress` 에 `"WaterSpirit": true` 추가 — **이 항목이 Group B 의 조용한 실패를 잡는 핵심 체크다**
- [ ] **Group B / WaterMonster** (`BossStatsSystem.Die()` 오버라이드 경로 — 이벤트 없음): 물괴물 처치 → `BossProgress` 에 `"WaterMonster": true` 추가
- [ ] 보스 격파 저장 후에도 `SceneName` / `SpawnPointName` 이 **직전 체크포인트 값 그대로**다 (보스 위치로 바뀌지 않음 — 연구 Open Question 1 확정 사항)
- [ ] 물의 정령 분신(IsDummy)이 사라질 때는 저장이 발생하지 않는다 (Console 에 추가 `Saved to` 없음)

**섹션 4) Play 모드 체크리스트 — 로드 (D-05, 비동기 씬 로드)**
- [ ] `MainMenu` 씬에서 Play 시작 → `SaveLoadManager` 컨텍스트 메뉴 `Phase11/2. Load Game` 실행
- [ ] 씬이 저장된 `SceneName` 으로 전환되고, 전환 중 프레임이 멈추지 않는다 (`LoadSceneAsync`)
- [ ] 전환 완료 후 플레이어가 저장된 체크포인트 위치에 서 있다 (`PlayerSpawner` 경로 재사용)
- [ ] Console 에 `[SaveLoadManager] Restored stats: <health>/<maxHealth> (maxTotal ...), scene=..., spawnPoint=...` 가 출력되고 값이 `save.json` 과 일치한다 — `PlayerStats.Instance is null after scene load` 에러가 **없어야** 한다 (연구 Pitfall 5)
- [ ] 체력 UI 가 복원된 체력을 반영한다 (`ClampHealth()` 가 `onHealthChangedCallback` 을 호출하므로)
- [ ] 저장 파일이 없는 상태에서 `Phase11/2. Load Game` 실행 → `LoadGame aborted - no save file at ...` 경고만 뜨고 씬 전환이 일어나지 않는다

**섹션 5) Play 모드 체크리스트 — 새 게임 (D-06)**
- [ ] `save.json` 이 존재하는 상태에서 `Phase11/3. New Game (memory only)` 실행
- [ ] Console 에 `NewGame - memory reset only, file untouched.` 출력
- [ ] 디스크의 `save.json` 파일 내용과 수정 시각이 **변하지 않았다** (D-06 핵심)
- [ ] 이후 체크포인트 S키를 누르면 그 시점에 파일이 새 내용으로 덮어써진다

**섹션 6) 알려진 한계 / 범위 밖**
- 메인 메뉴 "이어하기" 버튼 연동은 이번 페이즈 범위 밖 (D-04). `LoadGame()` 은 컨텍스트 메뉴로만 호출 가능하다.
- 다중 슬롯 미지원 (D-02).
- `MapGimmickState` 와 `Items` 는 스키마만 존재하고 실제로 채워 넣는 코드가 없다 (D-03, D-03b).
- `Assets/Scenes/InGame.unity` 는 Build Settings 에 등록되어 있으나 파일이 존재하지 않는 스테일 엔트리다. `MainMenuUI.OnClickStart()` 가 이 씬을 로드하려 하며 실패한다 — **기존 이슈이며 이번 페이즈에서 수정하지 않았다** (D-04 UI 범위 밖). 새 게임 기본 씬은 `1 stage` 로 설정되어 있다.
- `Portal.cs` / `GameManager.NextSpawnPointName` / `WoodBossStatSystem.cs` 는 고아 코드이며 이번 페이즈에서 언급만 하고 건드리지 않았다.

작성 후 섹션 1 의 15개 명령을 **실제로 실행**하고 결과를 표에 PASS/FAIL 과 실측값으로 기록한다. FAIL 이 하나라도 나오면 원인을 Check.md 에 적고 이 태스크를 완료로 표시하지 않는다.
  </action>
  <verify>
    <automated>test -f "Assets/SaveSystem/Check.md" && grep -c "WaterSpirit" "Assets/SaveSystem/Check.md"</automated>
  </verify>
  <acceptance_criteria>
    - 파일 `Assets/SaveSystem/Check.md` 가 존재하고 60줄 이상
    - 정적 검사 표에 최소 15개 항목 행이 있다 (`grep -cE "^\| *[0-9]+ *\|" Assets/SaveSystem/Check.md` >= 15)
    - Check.md 안에 `PASS` 문자열이 15회 이상 등장 (모든 정적 검사 항목에 실측 결과가 기록됨)
    - Check.md 에 `TutorialBoss`, `WoodBoss`, `WaterSpirit`, `WaterMonster` 4개 문자열이 전부 등장
    - Check.md 에 `Group A` 와 `Group B` 문자열이 전부 등장 (두 아키텍처를 구분해 검증)
    - Check.md 에 `D-01`, `D-02`, `D-03`, `D-04`, `D-05`, `D-06` 6개 결정 ID 가 전부 등장
    - Check.md 에 `Phase11/2. Load Game` 문자열이 등장 (ContextMenu 경로가 체크리스트와 일치)
    - 섹션 1 의 15개 명령을 실제 실행한 결과가 전부 PASS 로 기록됨 (FAIL 이 있으면 미완료)
  </acceptance_criteria>
  <done>`Assets/SaveSystem/Check.md` 에 정적 회귀 검사 15항목(실측 결과 포함, 전부 PASS)과 Play 모드 체크리스트 5개 섹션이 기록되어 있다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Unity Play 모드 실측 검증 체크포인트</name>
  <files>Assets/SaveSystem/Check.md</files>
  <read_first>
    - Assets/SaveSystem/Check.md (Task 2 산출물 — 섹션 2~5 체크리스트)
  </read_first>
  <what-built>
    Phase 11 전체:
    - `Packages/manifest.json` 에 Newtonsoft.Json 3.2.2 직접 의존성 고정
    - `Assets/SaveSystem/Script/SaveData.cs` — 저장 스키마 (씬+스폰포인트 문자열, 플레이어 스탯, 보스 진행도/맵 기믹 Dictionary, 아이템 List)
    - `Assets/SaveSystem/Script/SaveLoadManager.cs` — DontDestroyOnLoad 싱글톤(씬 배치 불필요, 부트스트랩 자동 생성), 메모리 캐시, `save.json` 단일 슬롯 I/O, 코루틴 기반 `LoadSceneAsync` + 스탯 복원
    - `Assets/Player/Script/PlayerStats.cs` — `RestoreStats(float,float,float)` additive 메서드
    - 저장 트리거 5곳: `Checkpoint.cs`(S키) + 보스 4종(TutorialBoss/WoodBoss = `HP.OnDeath` 경로, WaterSpirit/WaterMonster = `BossStatsSystem.Die()` 오버라이드 경로)
    - `Assets/SaveSystem/Check.md` 검증 체크리스트 (정적 검사 15항목은 이미 전부 PASS)

    자동화로 확인 불가능한 것: Unity 에디터 컴파일 성공 여부, 실제 파일 생성, 비동기 씬 로드 타이밍, 보스 4종 격파 시 실제 저장 발생 여부.
  </what-built>
  <action>
이 태스크는 사람이 수행한다. 실행자(Claude)는 코드를 수정하지 않는다.

실행자가 할 일:
1. 아래 `<how-to-verify>` 내용을 사용자에게 그대로 제시하고 응답을 기다린다.
2. 사용자가 확인한 항목만 `Assets/SaveSystem/Check.md` 의 `- [ ]` 를 `- [x]` 로 바꾼다.
3. 사용자가 확인하지 못했다고 한 항목은 절대 `- [x]` 로 바꾸지 않는다 (Phase 9 Plan 3 전례: 허위 PASS 기록 금지). 대신 Check.md 하단에 `## 미확인 항목` 섹션을 만들어 항목명과 사유를 기록한다.
4. 사용자가 컴파일 에러나 실패 항목을 보고하면, 원인을 진단해 보고하되 이 플랜 범위 밖의 수정은 하지 않고 사용자 판단을 받는다.
  </action>
  <how-to-verify>
    1. Unity 에디터를 열고 **Console 에 컴파일 에러가 0건**인지 확인한다. (에러가 있으면 여기서 중단하고 내용을 알려주세요.)
    2. `Assets/SaveSystem/Check.md` 를 열고 **섹션 2) 저장** 8개 항목을 순서대로 확인한다.
    3. **섹션 3) 보스 격파 자동 저장** 6개 항목을 확인한다. 4종 보스를 전부 처치하기 어렵다면 최소한 **Group A 1종 + Group B 1종**(예: TutorialBoss + WaterSpirit)은 반드시 확인해 주세요 — 두 아키텍처가 서로 다른 코드 경로라 한쪽만 되고 다른 쪽은 조용히 안 될 수 있습니다.
    4. **섹션 4) 로드** 6개 항목을 확인한다. 특히 Console 에 `Restored stats: ...` 로그가 뜨는지, `PlayerStats.Instance is null after scene load` 에러가 없는지 확인해 주세요.
    5. **섹션 5) 새 게임** 4개 항목을 확인한다 (`save.json` 의 수정 시각이 안 바뀌는지가 핵심).
    6. 확인한 항목 / 실패한 항목을 알려주시면 Check.md 체크박스를 대신 갱신하겠습니다.

    저장 파일 경로 찾는 법: Play 모드에서 Hierarchy 의 `SaveLoadManager` 선택 → 인스펙터 컴포넌트 톱니바퀴 → `Phase11/4. Log State` → Console 에 절대 경로가 출력됩니다.
  </how-to-verify>
  <verify>
    <automated>grep -c "^- \[x\]" "Assets/SaveSystem/Check.md"</automated>
  </verify>
  <acceptance_criteria>
    - Unity Console 컴파일 에러 0건이 사용자에 의해 확인됨
    - `Assets/SaveSystem/Check.md` 의 섹션 2~5 항목 중 사용자가 확인한 것만 `- [x]` 로 표시됨 (`grep -c "^- \[x\]" Assets/SaveSystem/Check.md` >= 1)
    - 최소한 Group A 보스 1종과 Group B 보스 1종의 격파 자동 저장 항목이 `- [x]` 로 확인됨
    - 미확인 항목은 `- [ ]` 로 남아 있고, Check.md 에 `## 미확인 항목` 섹션이 사유와 함께 존재한다 (미확인이 하나라도 있을 경우)
    - 이 태스크에서 `Assets/SaveSystem/Check.md` 외의 파일은 0줄 변경 — `git status --porcelain` 에 다른 파일이 새로 나타나지 않음
  </acceptance_criteria>
  <resume-signal>"approved" 를 입력하거나, 실패한 항목/컴파일 에러 내용을 알려주세요.</resume-signal>
  <done>사용자가 Play 모드 실측을 마쳤고, Check.md 의 확인 항목이 `- [x]` 로, 미확인 항목이 사유와 함께 기록되어 있다.</done>
</task>

</tasks>

<verification>
1. `grep -c '\[ContextMenu("Phase11/' Assets/SaveSystem/Script/SaveLoadManager.cs` == 4
2. `Assets/SaveSystem/Check.md` 존재, 정적 검사 15항목 전부 PASS 기록
3. Check.md 에 Group A / Group B 두 아키텍처가 구분되어 명시됨
4. Play 모드 체크포인트 승인 (또는 미확인 항목이 사유와 함께 명시적으로 남음)
5. 불가침 파일 6종(`HP.cs`, `BossStatesSystem.cs`, `WoodBossStatSystem.cs`, `portal.cs`, `GameManager.cs`, `MainMenuUI.cs`) 0줄 변경
</verification>

<success_criteria>
- UI 없이도 Play 모드에서 저장/로드/새게임을 트리거할 수 있는 검증 경로 존재
- 정적 회귀 검사 15항목 전부 PASS (인코딩 보존, 순수 삽입, 5개 트리거 연결, 무-async, 원시좌표 미사용, 고아코드 불가침)
- 사용자가 Play 모드에서 저장 파일 생성, 보스 격파 저장(양쪽 아키텍처), 비동기 로드 후 좌표+스탯 복원, 새 게임 파일 보존을 실측 확인
</success_criteria>

<output>
완료 후 `.planning/phases/11-newtonsoft-json-dontdestroyonload-i-o-dictionary-dictionary-application-persistentdatapath-json/11-04-SUMMARY.md` 를 작성한다.
</output>
