---
phase: quick-260805-q2u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Camera/Script/CameraController.cs
  - Assets/Camera/Script/CameraBoundsTrigger.cs
  - Assets/Camera/Check.md
autonomous: true
requirements: [Q2-01, Q2-02, Q2-03, Q2-04, Q2-05, Q2-06, Q2-07]
must_haves:
  truths:
    - "카메라 X 경계가 구역 진입 시 즉시 점프하지 않고 boundsSmoothing 속도로 몇 프레임에 걸쳐 이동한다"
    - "구역 이탈 시 경계가 항상 CameraController 의 고정 minX/maxX 로 복귀한다 (진입 직전 캐시값이 아님)"
    - "Inspector 의 minX/maxX 값은 Play 중에도 절대 변하지 않는다 (SetXBounds 가 쓰지 않음)"
    - "Play 모드에서 Gizmo 빨간 세로선 2개가 라이브 경계를 따라 움직이고, 에디터 모드에서는 minX/maxX 위치에 그려진다"
    - "프레임 1 에서 카메라가 x=0 부근으로 클램프되지 않는다 (Start 에서 _current/_target 시드)"
    - "레벨 디자이너가 Check.md 만 읽고 구역 타일링 + Y 범위 분리 배치 규칙을 알 수 있다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "boundsSmoothing 필드 + _target/_current 경계 2단 구조 + LateUpdate 경계 Lerp + Gizmo 라이브 반영"
      contains: "boundsSmoothing"
    - path: "Assets/Camera/Script/CameraBoundsTrigger.cs"
      provides: "캐시 없는 진입/이탈 트리거 (이탈 = 고정 기본값 복귀)"
      contains: "CameraController.Instance.minX"
    - path: "Assets/Camera/Check.md"
      provides: "260805-m41 섹션 갱신 + 260805-q2u 신규 섹션 (필드표/레벨디자인지침/Play 체크리스트/정적검사 기록)"
      contains: "260805-q2u"
  key_links:
    - from: "CameraBoundsTrigger.OnTriggerEnter2D"
      to: "CameraController._targetMinX/_targetMaxX"
      via: "SetXBounds(zoneMinX, zoneMaxX)"
      pattern: "SetXBounds\\(zoneMinX, zoneMaxX\\)"
    - from: "CameraBoundsTrigger.OnTriggerExit2D"
      to: "CameraController.minX/maxX (고정 기본값)"
      via: "SetXBounds(CameraController.Instance.minX, CameraController.Instance.maxX)"
      pattern: "SetXBounds\\(CameraController\\.Instance\\.minX"
    - from: "CameraController.LateUpdate"
      to: "CameraController.ApplyXClamp"
      via: "_currentMinX/_currentMaxX Lerp (줌 Lerp 직후 / 클램프 직전)"
      pattern: "_currentMinX = Mathf\\.Lerp"
---

<objective>
카메라 X 경계 시스템을 "즉시 덮어쓰기 + 직전값 캐시/복원" 방식(quick task 260805-m41)에서
**"구역별 타일링 + 부드러운 전환"** 방식으로 재설계한다.

Purpose: 현재 구조는 (a) 경계가 프레임 단위로 순간이동하고, (b) `CameraBoundsTrigger` 가 진입 직전
경계를 캐시했다가 복원하기 때문에 구역이 겹치거나 순서가 꼬이면 stale 한 값으로 되돌아간다.
`minX`/`maxX` 를 "스테이지 고정 기본 경계"로 재정의하고, 실제 클램프에 쓰이는 라이브 값을
`zoomSmoothing`/`_targetZoom` 과 **동일한 패턴**으로 Lerp 시키면 두 문제가 동시에 사라진다.
캐시 로직 전체가 삭제되므로 코드도 더 짧아진다.

Output:
- `CameraController.cs` — `boundsSmoothing` 신규 Inspector 필드 + `_targetMinX/_targetMaxX` /
  `_currentMinX/_currentMaxX` 2단 구조 + LateUpdate 경계 Lerp + Gizmo 라이브 반영
- `CameraBoundsTrigger.cs` — 캐시 필드 3개 및 관련 로직 전면 제거, 이탈 시 항상 고정 기본값 복귀
- `Check.md` — 260805-m41 섹션 갱신(해소된 알려진 한계) + 260805-q2u 신규 섹션(레벨 디자인 타일링 지침 포함)
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Assets/Camera/Script/CameraController.cs
@Assets/Camera/Script/CameraBoundsTrigger.cs
@Assets/Camera/Check.md

## 확정된 설계 (사용자와 논의 완료 — 대안 재검토 금지)

| ID | 결정 |
|----|------|
| Q2-01 | `minX`/`maxX` (public, Inspector) = **스테이지 고정 기본 경계**. 런타임에 절대 쓰이지 않는다. `SetXBounds` 가 더 이상 대입하지 않는다. |
| Q2-02 | `boundsSmoothing` (public, float, 기본 3) 신규 — 경계 전환 Lerp 속도. `zoomSmoothing` 과 동일한 성격/동일한 위치의 형제 필드. |
| Q2-03 | `_targetMinX`/`_targetMaxX` (SetXBounds 가 쓰는 목표값) + `_currentMinX`/`_currentMaxX` (매 프레임 Lerp 되는 라이브 값). 넷 다 `Start()` 에서 `minX`/`maxX` 로 시드. `ApplyXClamp` 는 `_current*` 를 사용. |
| Q2-04 | LateUpdate 삽입 위치: **줌 Lerp 직후 / `ApplyXClamp()` 직전**. `_currentMinX = Mathf.Lerp(_currentMinX, _targetMinX, boundsSmoothing * Time.deltaTime);` (max 도 동일) |
| Q2-05 | `OnDrawGizmos` 빨간 경계선: Play 모드 = `_currentMinX`/`_currentMaxX`, 에디터 모드 = `minX`/`maxX`. 기존 데드존 박스의 `Application.isPlaying ? _deadzoneCenterX : transform.position.x` 폴백 패턴과 동일. |
| Q2-06 | `CameraBoundsTrigger` 의 `_prevMinX`/`_prevMaxX`/`_hasCachedPrev` 및 관련 로직 **전부 제거**. 이탈 시 항상 `CameraController.Instance.minX`/`maxX` 로 복귀. `Player` 태그 가드는 유지. |
| Q2-07 | 레벨 디자인 지침(코드 변경 아님, 사용자 수동 작업): 구역을 **타일링**하듯 벽이 있는 모든 구간에 이어붙인다. `BoxCollider2D` 는 Y 범위도 가지므로 같은 X 라도 위/아래 통로에 Y 범위가 안 겹치는 별도 트리거를 두면 자동으로 올바르게 동작한다. `minX`/`maxX` 는 "어떤 zone 에도 속하지 않을 때"의 최외곽 폴백일 뿐이다. → Check.md 에 문서화. |

## 범위 밖 (하지 않을 것)

- 물리 충돌 기반 카메라 차단(Rigidbody2D + Layer) — 논의 끝에 **기각됨**. 구현 금지.
- Y축 경계(`minY`/`maxY`) — 이번 범위 아님. 기존 "Y 는 클램프하지 않는다" 결정 유지.
- `PlayerController.cs` / `InputHandler.cs` / `BossZoomTrigger.cs` — **읽기 전용, 수정 금지**.
- 씬 파일(`.unity`)에 실제 트리거 오브젝트 배치 — 사용자 수동 작업. 계획/실행에 포함 금지.
- Unity Editor 를 열어 Play 모드 실측 검증 — 이 계획의 범위 밖. **정적 코드 검증만** 수행하고,
  Check.md 에는 "미검증" 상태의 Play 모드 체크리스트만 남긴다. **PASS 허위 기록 절대 금지.**

## 실행 시 반드시 지킬 제약

1. **인코딩 게이트**: `CameraController.cs` 에는 레거시 한글 주석(깨진 인코딩)이 있는 줄이 **정확히 5줄**
   존재한다. 반드시 **`Edit` 툴로 국소 치환**할 것 — `Write` 로 파일 전체를 덮어쓰면 이 5줄이 파괴된다.
   신규로 작성하는 주석/코드는 **전부 ASCII** 로만 쓴다 (한글 금지).
2. **정밀 변경 (CLAUDE.md 원칙 3)**: 이 플랜에 명시된 줄 외의 인접 코드/주석/포맷을 "개선"하지 말 것.
3. **작업 트리 사전 오염 주의**: 이 저장소는 작업 시작 시점에 이미 여러 파일이 modified 상태다
   (`Tutorial Map.unity`, `TutorialBossController.cs` 등). 따라서 `git status` / `git diff --name-only HEAD`
   를 **범위 지정 없이** 돌려 "수정 파일 3개"를 기대하면 반드시 실패한다. 검증 시 항상
   `-- Assets/Camera` 로 경로를 한정할 것. (STATE.md 에 반복 기록된 "플랜 자체 검증 스크립트 오류" 패턴)

## 현재 코드의 관련 계약 (executor 는 아래를 그대로 소비할 것 — 탐색 불필요)

```csharp
// CameraController.cs (현재)
public float zoomSmoothing = 3f;        // boundsSmoothing 이 미러링할 형제 필드
public float minX = -1000f;
public float maxX = 1000f;
private float _targetZoom;              // _targetMinX/_targetMaxX 가 미러링할 형제 필드
public static CameraController Instance { get; private set; }
public void SetBossZoom(bool isBossStage)
public void SetXBounds(float min, float max)   // 현재: minX = min; maxX = max;
private void ApplyXClamp()                     // 현재: Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth)

// LateUpdate 현재 순서
//   레거시 추종 Lerp -> (_isBossZone ? ResetNormalStageState : ApplyNormalStageCamera)
//   -> 줌 Lerp -> [여기에 경계 Lerp 삽입] -> ApplyXClamp() -> 데드존 앵커 재동기화

// Start 현재 순서
//   transform.position = target.position + offset;
//   _cam.orthographicSize = normalZoom;
//   [여기에 4개 시드 삽입] -> ApplyXClamp();   <-- 시드가 clamp 보다 먼저여야 함 (안 그러면 프레임1에 x=0 클램프)
//   -> ResetNormalStageState(); -> TryBindMoveEvent(); -> _playerRef = ...
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: CameraController 에 2단 경계 구조 + boundsSmoothing Lerp 도입</name>
  <files>Assets/Camera/Script/CameraController.cs</files>
  <action>
`Edit` 툴로만 아래 6개 지점을 국소 수정한다 (`Write` 금지 — 인코딩 게이트). 신규 텍스트는 전부 ASCII.

**(1) `[Header("Camera X Bounds")]` 블록 (현재 20-26행 부근)** — Q2-01 / Q2-02.
`public float maxX = 1000f;` 바로 아래에 `boundsSmoothing` 을 추가하고, minX/maxX 주석에 "고정 기본값" 의미를 덧붙인다:

```csharp
    [Header("Camera X Bounds")]
    // World-space X limits of the visible area (D-09 / D-10).
    // These two are the STAGE BASE bounds and are never written at runtime (260805-q2u / Q2-01):
    // SetXBounds feeds _targetMinX / _targetMaxX instead, so this Inspector pair stays the
    // fallback the camera falls back to whenever the player is inside no zone at all.
    // Defaults are intentionally wide so existing scenes keep their current behaviour
    // until minX / maxX are tuned per scene in the Inspector.
    // Y is NOT clamped in this phase by design (D-09).
    public float minX = -1000f;
    public float maxX = 1000f;
    // Lerp rate for the bounds transition ONLY - the exact role zoomSmoothing plays for the
    // zoom, and kept separate from both 'smoothing' and 'zoomSmoothing' on purpose (Q2-02).
    public float boundsSmoothing = 3f;
```

**(2) private 필드 (현재 `private float _targetZoom;` 바로 아래)** — Q2-03:

```csharp
    // Bounds the camera is easing TOWARD. Written by SetXBounds only, seeded from minX / maxX
    // in Start. Mirrors the _targetZoom / zoomSmoothing pair exactly (Q2-03).
    private float _targetMinX;
    private float _targetMaxX;
    // Live bounds actually consumed by ApplyXClamp. Lerped toward the target pair every frame,
    // which is what turns a zone handoff into a slide instead of a snap (Q2-03).
    private float _currentMinX;
    private float _currentMaxX;
```

**(3) `SetXBounds` 본문 + 주석** — Q2-01. 메서드 계약(단순 대입, 검증/클램프 없음)은 유지하되,
"트리거가 직전 값을 캐시했다 되먹인다"는 서술만 새 방식으로 교체한다:

```csharp
    // Called by CameraBoundsTrigger on enter and on exit: hands the camera the X limits of the
    // zone the player is currently scoped to, so a single scene can hard clamp room by room
    // (260805-m41, retargeted by 260805-q2u). On exit the trigger simply feeds back this
    // controller's own minX / maxX, so no bounds history is kept anywhere (Q2-06).
    // Idempotent, last call wins - same contract as SetBossZoom above.
    // Plain field assignment on purpose: LateUpdate eases _currentMinX / _currentMaxX toward
    // this pair every frame and ApplyXClamp consumes the eased values, so a bounds swap needs
    // no extra logic here. Do NOT clamp, validate or interpolate in this method.
    // A zone narrower than the camera view collapses the clamp onto a single X - that is the
    // designer's responsibility when authoring the zone, it is not guarded here.
    public void SetXBounds(float min, float max)
    {
        _targetMinX = min;
        _targetMaxX = max;
    }
```

**(4) `ApplyXClamp` 클램프 줄 1줄만 교체** — Q2-03:
`pos.x = Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth);`
-> `pos.x = Mathf.Clamp(pos.x, _currentMinX + halfWidth, _currentMaxX - halfWidth);`
(위 메서드 주석의 `minX / maxX` 표현은 건드리지 않는다 — 의미상 여전히 맞고, 정밀 변경 원칙.)

**(5) `Start()` — `ApplyXClamp();` 바로 위에 시드 4줄 삽입** — Q2-03. **순서가 핵심이다**:

```csharp
        // Seed both bounds pairs from the Inspector base values BEFORE the first clamp:
        // ApplyXClamp now reads _currentMinX / _currentMaxX, so leaving them at their default
        // 0 would clamp the camera onto x = 0 on frame 1 (Q2-03).
        _targetMinX = minX;
        _targetMaxX = maxX;
        _currentMinX = minX;
        _currentMaxX = maxX;
        ApplyXClamp();
```

**(6) `LateUpdate()` — 줌 Lerp 직후 / `ApplyXClamp();` 직전에 삽입** — Q2-04:

```csharp
        // Bounds Lerp, mirroring the zoom Lerp directly above (Q2-04). Sits before the clamp so
        // the clamp always consumes this frame's freshly eased bounds.
        _currentMinX = Mathf.Lerp(_currentMinX, _targetMinX, boundsSmoothing * Time.deltaTime);
        _currentMaxX = Mathf.Lerp(_currentMaxX, _targetMaxX, boundsSmoothing * Time.deltaTime);
```

**(7) `OnDrawGizmos` 빨간 선 블록** — Q2-05. `DrawLine` 2줄을 지역변수 경유로 교체:

```csharp
        // X bound visualization (D-09 / D-10) - lets minX/maxX be eyeballed without Play mode.
        // In play mode the LIVE eased bounds are drawn so a zone handoff is visible on screen;
        // in edit mode it falls back to the Inspector base pair, exactly like the deadzone box
        // above falls back to transform.position (Q2-05).
        Gizmos.color = Color.red;
        const float boundLineHeight = 20f;
        float halfLine = boundLineHeight / 2f;
        float drawMinX = Application.isPlaying ? _currentMinX : minX;
        float drawMaxX = Application.isPlaying ? _currentMaxX : maxX;
        Gizmos.DrawLine(new Vector3(drawMinX, transform.position.y - halfLine, 0f), new Vector3(drawMinX, transform.position.y + halfLine, 0f));
        Gizmos.DrawLine(new Vector3(drawMaxX, transform.position.y - halfLine, 0f), new Vector3(drawMaxX, transform.position.y + halfLine, 0f));
```

이 7개 지점 외에는 한 줄도 건드리지 않는다. `minY`/`maxY`/`Rigidbody`/`Cinemachine` 등 범위 밖 심볼 도입 금지.
  </action>
  <verify>
    <automated>
F=Assets/Camera/Script/CameraController.cs
echo "1 nonascii lines (expect 5): $(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)"
echo "2 boundsSmoothing decl (expect 1): $(grep -c 'public float boundsSmoothing = 3f;' $F)"
echo "3 target assign in SetXBounds (expect 2): $(grep -cE '^\s*_target(Min|Max)X = (min|max);' $F)"
echo "4 old direct assign (expect 0): $(grep -cE '^\s*(minX = min|maxX = max);' $F)"
echo "5 clamp uses _current (expect 1): $(grep -c '_currentMinX + halfWidth' $F)"
echo "6 clamp old ref (expect 0): $(grep -cE '(^|[^_[:alnum:]])minX \+ halfWidth' $F)"
echo "7 Start seed lines (expect 4): $(grep -cE '^\s*_(target|current)(Min|Max)X = (min|max)X;' $F)"
echo "8 bounds Lerp lines (expect 2): $(grep -cE '_current(Min|Max)X = Mathf\.Lerp\(_current(Min|Max)X, _target(Min|Max)X, boundsSmoothing \* Time\.deltaTime\);' $F)"
echo "9 gizmo isPlaying bounds (expect 2): $(grep -cE 'float draw(Min|Max)X = Application\.isPlaying \? _current(Min|Max)X : (min|max)X;' $F)"
echo "10 out-of-scope symbols (expect 0 0 0): $(grep -c 'minY' $F) $(grep -c 'maxY' $F) $(grep -c 'Rigidbody' $F)"
SEED=$(grep -n '_currentMaxX = maxX;' $F | cut -d: -f1); S1=$(grep -n 'ApplyXClamp();' $F | head -1 | cut -d: -f1)
echo "11 Start order seed($SEED) < firstClamp($S1)"
ZL=$(grep -n 'orthographicSize = Mathf.Lerp' $F | cut -d: -f1); BL=$(grep -n '_currentMinX = Mathf.Lerp' $F | cut -d: -f1); CL=$(grep -n 'ApplyXClamp();' $F | tail -1 | cut -d: -f1)
echo "12 LateUpdate order zoom($ZL) < bounds($BL) < clamp($CL)"
echo "13 deleted lines (expect <=10): $(git diff -U0 -- $F | grep -c '^-[^-]')"
echo "14 deleted non-ASCII (expect 0): $(git diff -U0 -- $F | grep '^-[^-]' | LC_ALL=C grep -c '[^[:print:][:space:]]')"
git diff -U0 -- $F | grep '^-[^-]'
    </automated>
  </verify>
  <done>
게이트 1-14 전부 기대값과 일치하고, 11/12 의 행 번호 부등식이 성립한다.
13 의 삭제 라인 목록이 플랜 명세(SetXBounds 대입 2줄, ApplyXClamp 클램프 1줄, DrawLine 2줄,
SetXBounds 캐시 관련 주석 1-3줄)와 정확히 대응하며 그 외 삭제가 없다.
  </done>
</task>

<task type="auto">
  <name>Task 2: CameraBoundsTrigger 캐시 로직 제거 및 고정 기본값 복귀로 전환</name>
  <files>Assets/Camera/Script/CameraBoundsTrigger.cs</files>
  <action>
Q2-06. 이 파일은 비-ASCII 가 0줄인 신규 파일이므로 전체 교체해도 안전하다. 아래 내용으로 파일을 만든다
(기존 `zoneMinX`/`zoneMaxX` 필드명·기본값·`[Header]`·태그 가드는 그대로 유지 — Inspector 설정이 깨지면 안 된다):

```csharp
using UnityEngine;

// Put this on a GameObject with a BoxCollider2D (Is Trigger = on) that covers one room / zone
// (260805-m41). Entering the zone hands CameraController the X limits that room should clamp to,
// so a single scene can frame room by room instead of sharing one pair of scene-wide bounds.
// Wide easter egg areas are the same script with wider values - no extra component needed.
//
// A zone is a SCOPED OVERRIDE, not a permanent handoff. On exit the camera is always sent back to
// CameraController's own minX / maxX - the fixed stage base bounds - instead of to whatever was
// active right before entering (Q2-06, supersedes MX-05). Nothing is remembered per trigger, so
// overlapping or out-of-order zones can no longer hand back a logically stale pair, and a player
// who SPAWNS inside a zone and walks out simply lands on the base bounds with no guard needed.
//
// The swap itself is not instant: CameraController eases its live bounds toward this pair at
// boundsSmoothing, so crossing a zone edge slides the clamp instead of snapping it (Q2-02).
//
// Because a zone only ever hands off to another zone or falls back to the base bounds, rooms are
// meant to be TILED - put a trigger on every stretch that has its own walls. Level design guide:
// Assets/Camera/Check.md, section "quick task 260805-q2u".
//
// The trigger volumes themselves are placed manually per zone by the designer, exactly like
// BossZoomTrigger: this script never creates or edits scene objects.
public class CameraBoundsTrigger : MonoBehaviour
{
    [Header("Zone X Bounds")]
    // World-space X limits handed to the camera on enter. Tune per zone in the Inspector.
    // Defaults match CameraController's own wide defaults, so a zone left untuned is a no-op
    // rather than a camera that snaps to the origin.
    // Keep the zone at least as wide as the camera view, or the clamp collapses onto a single X.
    public float zoneMinX = -1000f;
    public float zoneMaxX = 1000f;

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        CameraController.Instance.SetXBounds(zoneMinX, zoneMaxX);
    }

    // Leaving the zone always falls back to the controller's fixed base bounds, the same way
    // BossZoomTrigger reverts to normalZoom on exit (Q2-06).
    private void OnTriggerExit2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        CameraController.Instance.SetXBounds(CameraController.Instance.minX, CameraController.Instance.maxX);
    }
}
```

필드/스택/구역 매니저를 새로 추가하지 않는다. `.meta` 파일은 건드리지 않는다.
  </action>
  <verify>
    <automated>
G=Assets/Camera/Script/CameraBoundsTrigger.cs
echo "1 nonascii (expect 0): $(LC_ALL=C grep -c '[^[:print:][:space:]]' $G)"
echo "2 cache symbols gone (expect 0): $(grep -cE '_prevMinX|_prevMaxX|_hasCachedPrev' $G)"
echo "3 private fields (expect 0): $(grep -cE '^\s*private (float|bool|int) ' $G)"
echo "4 enter call (expect 1): $(grep -c 'CameraController.Instance.SetXBounds(zoneMinX, zoneMaxX);' $G)"
echo "5 exit call (expect 1): $(grep -c 'CameraController.Instance.SetXBounds(CameraController.Instance.minX, CameraController.Instance.maxX);' $G)"
echo "6 tag guards (expect 2): $(grep -c 'CompareTag("Player")' $G)"
echo "7 enter/exit defs (expect 1 1): $(grep -c 'private void OnTriggerEnter2D' $G) $(grep -c 'private void OnTriggerExit2D' $G)"
echo "8 no stack/list/static (expect 0): $(grep -cE 'List<|Stack<|static ' $G)"
echo "9 public lines (expect 3: class + 2 fields): $(grep -c '^\s*public ' $G)"
echo "10 zone field defaults preserved (expect 2): $(grep -cE '^\s*public float zone(Min|Max)X = -?1000f;' $G)"
echo "11 read-only files untouched (expect empty): $(git diff --name-only HEAD -- Assets/Camera/Script/BossZoomTrigger.cs Assets/Camera/Script/CameraBoundsTrigger.cs.meta)"
    </automated>
  </verify>
  <done>
게이트 1-11 전부 기대값과 일치한다. `SetXBounds` 호출은 정확히 2곳(enter/exit)이고, 인스턴스 상태를
들고 있는 private 필드가 하나도 남아 있지 않다.
  </done>
</task>

<task type="auto">
  <name>Task 3: Check.md 갱신 (m41 섹션 정정 + q2u 신규 섹션 + 레벨 디자인 타일링 지침)</name>
  <files>Assets/Camera/Check.md</files>
  <action>
기존 문서 패턴(표 / 체크리스트 / 알려진 한계 / 결과 기록 섹션 스타일)을 그대로 따른다. 한글 본문 유지.

**(A) 기존 `# quick task 260805-m41` 섹션 정정 (surgical)**
1. 섹션 제목 바로 아래에 대체 배너 1문단 추가:
   `> **이 섹션의 캐시/복원 방식(MX-05)은 quick task 260805-q2u 에서 폐기되었다.** 아래 내용은 이력 보존용이며,
   현행 동작은 문서 맨 아래 "quick task 260805-q2u" 섹션을 따른다.`
2. `## 알려진 한계` 의 **첫 번째 항목(겹치는 구역 stale 복원)** 을 "**[해소됨 — 260805-q2u]** 이탈 시 항상
   고정 기본 경계로 복귀하도록 바뀌어 stale 복원 자체가 발생하지 않는다. 단, 구역을 겹치면 어느 트리거의
   Exit 가 먼저 오느냐에 따라 잠깐 기본 경계로 떨어졌다 다시 잡히므로 여전히 겹치지 않게 배치하는 편이 좋다."
   로 갱신한다. 두 번째 항목(구역 사이 빈틈)에는 "→ 260805-q2u 의 **타일링 지침** 참조" 를 덧붙인다.
   세 번째 항목(구역 폭 < 화면 폭)은 그대로 둔다.
3. `### 6) 구역별 카메라 X 경계` 체크리스트 제목 아래에 `> **폐기됨 — 아래 7) 로 대체.**` 한 줄 추가.
   **개별 체크박스는 지우지도, 체크하지도 않는다** (허위 PASS 금지 / 이력 보존).

**(B) 문서 맨 끝에 신규 섹션 추가** — `---` 구분선 뒤:

`# quick task 260805-q2u — 구역 타일링 + 부드러운 경계 전환 (260805-m41 재설계)`

다음 하위 섹션을 포함할 것:

1. `## 무엇이 바뀌었나` — 이전/이후 대조표:
   | 항목 | 260805-m41 (이전) | 260805-q2u (현행) |
   행: 경계 전환 / `minX`,`maxX` 의미 / `SetXBounds` 가 쓰는 대상 / 이탈 시 복귀값 / 트리거 상태 / 겹침 stale.

2. `## Inspector 필드` 표 (기존 표 형식 그대로):
   | `minX` | -1000 | 스테이지 **고정 기본** 좌측 경계 (어떤 zone 에도 없을 때의 폴백) | Q2-01 |
   | `maxX` | 1000 | 스테이지 고정 기본 우측 경계 | Q2-01 |
   | `boundsSmoothing` | 3 | 경계 전환 Lerp 속도 (`zoomSmoothing` 과 동일 성격, `smoothing`/`zoomSmoothing` 과 별개) | Q2-02 |
   | `zoneMinX`/`zoneMaxX` (트리거) | -1000/1000 | 이 구역이 적용할 좌/우 월드 X | Q2-06 |

3. `## 내부 구조` — 2단 구조 설명 + 갱신된 LateUpdate 순서 1줄:
   `레거시 추종 Lerp -> 보스/일반 분기 -> 줌 Lerp -> **경계 Lerp** -> X 클램프 -> 데드존 앵커 재동기화`
   그리고 `Start` 에서 4개 필드를 `ApplyXClamp()` **이전에** 시드하는 이유(프레임1 x=0 클램프 방지)를 명시.
   Gizmo 규칙(Play=라이브, 에디터=기본값, Q2-05)도 여기에 1줄.

4. `## 레벨 디자인 지침 (Q2-07 — 코드가 아니라 배치로 푸는 문제)`:
   - **타일링**: 벽이 있는 모든 구간마다 그 위치에 맞춘 `CameraBoundsTrigger` 를 이어붙인다.
     "일반 구역"이라고 트리거를 비워두면 그 구간에서는 최외곽 기본 경계가 적용되어 **카메라가 벽 너머를 미리 비춘다.**
     `minX`/`maxX` 는 어떤 zone 에도 속하지 않을 때의 최외곽 폴백일 뿐, "정상 상태"가 아니다.
   - **Y 범위 분리**: `CameraBoundsTrigger` 는 `BoxCollider2D` 라 X 뿐 아니라 **Y 범위도 가진다.** 같은 X 구간이라도
     위쪽 통로(벽 없음)와 아래쪽 통로(벽 있음)에 **Y 범위가 겹치지 않는 별도 트리거 박스**를 두면 층별로 다른
     경계가 자동 적용된다. **코드 변경 불필요 — 트리거 박스 배치만 하면 된다 (사용자 수동 작업).**
   - **이어붙이되 겹치지는 않게**: 방과 방을 맞붙이면 전환이 끊기지 않는다. 겹치면 Exit/Enter 순서에 따라
     한 프레임 기본 경계로 튀었다 잡힐 수 있다(전환이 Lerp 라 눈에 잘 안 띄지만 권장하지 않음).
   - **구역 폭 >= 카메라 가로폭**(`2 * orthographicSize * aspect`, 일반 줌 5·16:9 기준 약 17.8) 유지.
     기존 m41 섹션의 8단계 수동 설정 절차는 그대로 유효하므로 참조 링크만 남기고 재작성하지 않는다.

5. `## 알려진 한계`:
   - 전환이 Lerp 이므로 `boundsSmoothing` 이 낮으면 좁은 구역에 빠르게 진입할 때 **경계가 아직 다 좁혀지기 전에**
     카메라가 잠깐 벽 너머를 비출 수 있다. 대응: `boundsSmoothing` 을 올리거나 트리거를 벽보다 앞쪽에서 시작.
   - 구역 폭 < 화면 폭이면 여전히 클램프가 한 점으로 무너진다 (설계상 designer 책임, 방어 코드 없음).
   - 물리 충돌 기반 카메라 차단은 **논의 끝에 기각** — 재도입은 별도 재논의 항목.

6. `## Play 모드 검증 항목` > `### 7) 구역 타일링 & 부드러운 경계 전환 (quick task 260805-q2u)`
   — **전부 미체크 `- [ ]`** 로, 최소 10개 항목. 다음을 반드시 포함:
   - 부드러운 전환: 구역 진입 시 카메라 좌우 여백이 **점프 없이** 몇 프레임에 걸쳐 좁아진다.
   - `boundsSmoothing` 튜닝: Play 중 1 로 낮추면 경계 전환만 느려지고 줌/추종 속도는 그대로다.
   - **`Min X`/`Max X` 불변**: Play 중 구역을 드나들어도 `Main Camera` Inspector 의 `Min X`/`Max X` 값이
     **전혀 변하지 않는다** (변하면 Q2-01 위반 = 옛 방식이 남아 있는 것).
   - 이탈 시 기본값 복귀: 구역을 나오면 경계가 **진입 직전 값이 아니라 항상 Inspector 기본값**으로 돌아간다.
   - 겹치지 않는 인접 구역 연속 통과 시 매번 들어간 방 값이 적용된다.
   - 구역 A 안에서 구역 B 로 넘어가도 stale 복원이 없다 (m41 의 한계 해소 확인).
   - 구역 안 스폰 후 이탈: 카메라가 한 지점에 고정되지 않는다 (가드 없이도 안전한지 확인).
   - Gizmo: Play 중 빨간 세로선 2개가 현재 구역 경계로 **미끄러지듯** 이동하고, Play 종료 시 기본값 위치로 돌아온다.
   - 프레임 1 안전성: Play 시작 순간 카메라가 x=0 부근으로 끌려가지 않는다 (Start 시드 확인).
   - Y 범위 분리 배치(위/아래 통로에 각각 트리거)에서 층마다 다른 경계가 적용된다.
   - 타일링된 일반 구역에서 카메라가 벽 너머를 미리 비추지 않는다.
   - `Player` 태그 가드: 적/투사체가 드나들어도 경계가 바뀌지 않는다.
   - Phase 9/10 회귀 없음: 줌/데드존/동적 오프셋/피킹 동작이 이전과 동일하다.

7. `## quick task 260805-q2u 결과 기록` > `### 정적 회귀 검사 (2026-08-05)`
   Task 1/Task 2 의 검증 게이트 결과를 기존 문서와 동일한 `| # | 검사 | 기대값 | 실제값 | 판정 |` 표로 기록한다.
   마지막 줄: `**Play 모드 검증 상태**: 미검증 (위 "7)" 체크리스트는 사용자가 씬에 트리거를 타일링 배치한 뒤 직접 확인해야 한다. PASS 로 허위 기록 금지.)`
  </action>
  <verify>
    <automated>
C=Assets/Camera/Check.md
echo "1 new section header (expect 1): $(grep -c '^# quick task 260805-q2u' $C)"
echo "2 q2u cross-refs (expect >=5): $(grep -c '260805-q2u' $C)"
echo "3 no false PASS checkbox (expect 0): $(grep -c '^- \[[xX]\]' $C)"
echo "4 section 7 checklist items (expect >=10): $(awk '/^### 7\)/,0' $C | grep -c '^- \[ \]')"
echo "5 boundsSmoothing documented (expect >=2): $(grep -c 'boundsSmoothing' $C)"
echo "6 tiling guide (expect >=2): $(grep -c '타일링' $C)"
echo "7 Y-range guide (expect >=1): $(grep -c 'Y 범위' $C)"
echo "8 m41 supersede banner (expect >=1): $(grep -c '해소됨' $C)"
echo "9 section 6 deprecated marker (expect >=1): $(grep -c '폐기됨' $C)"
echo "10 static result table (expect >=1): $(grep -c '정적 회귀 검사 (2026-08-05)' $C)"
echo "11 unverified marker (expect >=1): $(grep -c 'PASS 로 허위 기록 금지' $C)"
echo "12 files changed under Assets/Camera (expect exactly 3):"; git diff --name-only HEAD -- Assets/Camera
echo "13 read-only guards (expect empty):"; git diff --name-only HEAD -- Assets/Camera/Script/BossZoomTrigger.cs '*PlayerController.cs' '*InputHandler.cs'
    </automated>
  </verify>
  <done>
게이트 1-11 전부 기대값 충족. 12 가 정확히 `CameraController.cs`, `CameraBoundsTrigger.cs`, `Check.md` 3개만
출력하고, 13 은 아무것도 출력하지 않는다. 새 체크리스트 항목은 전부 미체크 상태다.
  </done>
</task>

</tasks>

<verification>
1. **인코딩 무결성**: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == 5,
   `... CameraBoundsTrigger.cs` == 0.
2. **Q2-01 관철**: `CameraController.cs` 안에 `minX = ` / `maxX = ` 형태의 런타임 대입이 **선언부 2줄 외에 0건**.
   (`grep -nE '^\s*(minX|maxX) = ' Assets/Camera/Script/CameraController.cs` 결과가 선언 2줄뿐)
3. **경로 연결**: 트리거 enter -> `_target*`, LateUpdate Lerp -> `_current*`, `ApplyXClamp` -> `_current*` 사슬이
   끊기지 않는다 (`_currentMinX` 가 선언/시드/Lerp/클램프/Gizmo 5곳에 등장).
4. **범위 준수**: `git diff --name-only HEAD -- Assets/Camera` 가 정확히 3개 파일.
   `BossZoomTrigger.cs` / `PlayerController.cs` / `InputHandler.cs` / `*.unity` 0줄 변경.
   (주의: 작업 트리에 사전 오염된 modified 파일이 있으므로 경로 한정 없이 판단하지 말 것)
5. **범위 밖 심볼 부재**: `minY`, `maxY`, `Rigidbody`, `Cinemachine`, `Stack<`, `List<` 전부 0.
6. **Play 모드 미검증 명시**: Check.md 에 체크된 `- [x]` 항목이 0개.
</verification>

<success_criteria>
- `CameraController` 가 `_targetMinX/_targetMaxX` -> `_currentMinX/_currentMaxX` -> `ApplyXClamp` 2단 구조로
  동작하며, 전환 속도는 신규 `boundsSmoothing`(기본 3) Inspector 필드가 지배한다.
- `SetXBounds` 는 `minX`/`maxX` 를 더 이상 쓰지 않는다 — Inspector 기본값은 런타임 불변 폴백이 되었다.
- `CameraBoundsTrigger` 에 인스턴스 상태 필드가 0개이고, 이탈 시 항상 `CameraController.Instance.minX/maxX` 로 복귀한다.
- `Check.md` 의 260805-m41 섹션에서 캐시 stale 관련 서술이 "해소됨"으로 갱신되고, 신규 260805-q2u 섹션에
  필드표 / 내부구조 / 타일링·Y분리 레벨 디자인 지침 / 알려진 한계 / 미검증 Play 체크리스트(10개 이상) /
  정적 검사 결과표가 모두 존재한다.
- 정적 검증 게이트 전부 통과. Unity Play 모드 실측은 수행하지 않으며 어디에도 PASS 로 기록하지 않는다.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-q2u-x-cameracontroller-cameraboundstrigger/260805-q2u-SUMMARY.md` 를 생성한다.

커밋 (문서/코드 분리 없이 1회, 기존 quick task 패턴과 동일):
```bash
node ".claude/get-shit-done/bin/gsd-tools.cjs" commit "refactor(camera): retarget X bounds to tiled zones with smoothed transition" --files Assets/Camera/Script/CameraController.cs Assets/Camera/Script/CameraBoundsTrigger.cs Assets/Camera/Check.md
```
(작업 트리에 사전 오염된 파일이 있으므로 `--files` 로 반드시 3개 경로만 지정할 것 — `git add -A` 금지)

STATE.md 갱신 시: Quick Tasks Completed 표에 `260805-q2u` 행 추가, Active TODOs 의 260805-m41 gap 항목에
"MX-05 캐시/복원 방식은 260805-q2u 로 대체됨 — 새 체크리스트는 Check.md '7)' 섹션" 를 덧붙인다.
</output>
