---
phase: quick/260805-m41-cameracontroller-setxbounds-min-max-boss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Camera/Script/CameraController.cs
  - Assets/Camera/Script/CameraBoundsTrigger.cs
  - Assets/Camera/Check.md
autonomous: true
requirements: [MX-01, MX-02, MX-03, MX-04, MX-05]
must_haves:
  truths:
    - "런타임에 카메라 X 경계(minX/maxX)를 구역별로 교체할 수 있다"
    - "플레이어가 CameraBoundsTrigger 볼륨에 들어가면 그 구역의 X 경계가 즉시 적용된다"
    - "경계 교체 후 다음 프레임부터 카메라가 새 경계 안으로 클램프된다 (추가 클램프 로직 없이 기존 ApplyXClamp 재사용)"
    - "플레이어가 구역을 벗어나면 그 구역에 들어가기 직전에 활성이던 경계로 되돌아간다 (구역 = 범위 한정 오버라이드)"
    - "구역에 한 번도 진입하지 않은 트리거는 이탈 시 아무것도 되돌리지 않는다 (스폰-인-구역 안전장치)"
    - "이스터에그 확장 구역처럼 넓은 구역도 동일 스크립트를 값만 바꿔 드롭하면 동작한다"
    - "BossZoomTrigger 의 줌 동작과 CameraBoundsTrigger 의 경계 동작이 서로 간섭하지 않는다"
    - "CameraController.cs 의 기존 비-ASCII(깨진 한글) 주석 5줄과 기존 로직이 한 줄도 변경되지 않는다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "런타임 X 경계 교체 진입점 SetXBounds(float, float)"
      contains: "public void SetXBounds(float min, float max)"
    - path: "Assets/Camera/Script/CameraBoundsTrigger.cs"
      provides: "구역별 X 경계 트리거 (BoxCollider2D + zoneMinX/zoneMaxX + 이전 경계 1슬롯 캐시)"
      contains: "public class CameraBoundsTrigger : MonoBehaviour"
    - path: "Assets/Camera/Check.md"
      provides: "수동 에디터 배치 가이드 + Play 모드 검증 체크리스트 + 알려진 한계"
      contains: "6) 구역별 카메라 X 경계"
  key_links:
    - from: "CameraBoundsTrigger.OnTriggerEnter2D"
      to: "CameraController.SetXBounds"
      via: "씬 로컬 싱글톤 Instance (BossZoomTrigger 와 동일 경로)"
      pattern: "CameraController\\.Instance\\.SetXBounds\\(zoneMinX, zoneMaxX\\);"
    - from: "CameraBoundsTrigger.OnTriggerExit2D"
      to: "CameraController.SetXBounds"
      via: "진입 직전 값 1슬롯 캐시 복원 (MX-05)"
      pattern: "CameraController\\.Instance\\.SetXBounds\\(_prevMinX, _prevMaxX\\);"
    - from: "CameraBoundsTrigger.OnTriggerEnter2D"
      to: "_prevMinX / _prevMaxX"
      via: "덮어쓰기 직전 현재 경계 읽어 캐시 (순서 의존)"
      pattern: "_prevMinX = CameraController\\.Instance\\.minX;"
    - from: "SetXBounds"
      to: "ApplyXClamp"
      via: "minX / maxX 필드 대입 (매 프레임 소비, 신규 로직 없음)"
      pattern: "minX = min;"
    - from: "CameraBoundsTrigger"
      to: "Player"
      via: "태그 체크 (BossZoomTrigger 와 동일 가드)"
      pattern: "if \\(!other\\.CompareTag\\(\"Player\"\\)\\) return;"
---

<objective>
`CameraController` 에 `SetXBounds(float min, float max)` 퍼블릭 메서드를 추가하고, `BossZoomTrigger` 와 동일한 패턴의 신규 트리거 스크립트 `CameraBoundsTrigger` 를 작성한다.

Purpose: 현재 `minX` / `maxX` 는 씬당 1쌍의 Inspector 값으로 고정되어 있어(Phase 9 D-09/D-10), 한 씬 안에서 방마다 다른 카메라 경계를 줄 수 없다. 실크송 스타일의 "방 단위 하드 클램프"와 이스터에그 확장 구역을 구현하려면 런타임 교체 진입점이 필요하다.

Output: `CameraController.cs` 수정 1개(순수 삽입) + `CameraBoundsTrigger.cs` 신규 1개 + `Check.md` 갱신 1개. **씬(`.unity`) 편집 없음, `.meta` 생성 없음.**

핵심: 클램프 로직은 이미 `ApplyXClamp()` 가 매 프레임 수행하고 있다. 이 작업은 **그 입력값을 런타임에 갈아끼우는 얇은 배선(wiring)일 뿐**이며, 새 클램프 계산을 단 한 줄도 추가하지 않는다.

구역은 **범위 한정 오버라이드**다: 진입 시 직전 경계를 1슬롯에 캐시해두고 이탈 시 그 값으로 되돌린다 (MX-05).
</objective>

<context>
@.planning/STATE.md
@Assets/Camera/Script/CameraController.cs
@Assets/Camera/Script/BossZoomTrigger.cs
@Assets/Camera/Check.md
@.planning/quick/260804-q6h-y-cameracontroller-cs/260804-q6h-PLAN.md
</context>

<interfaces>
실행자가 코드베이스를 탐색할 필요 없도록 필요한 계약을 여기에 옮겨둔다.

`Assets/Camera/Script/CameraController.cs` (현재 상태, 관련 부분만):
```csharp
    [Header("Camera X Bounds")]
    public float minX = -1000f;   // 25행 — public 필드이므로 트리거에서 직접 읽을 수 있다(캐시에 사용)
    public float maxX = 1000f;    // 26행 — 동일

    public static CameraController Instance { get; private set; }  // 63행, 씬 로컬 싱글톤

    public void SetBossZoom(bool isBossStage) { ... }  // 150행 — 미러링할 퍼블릭 API 스타일

    private void ApplyXClamp()    // 159행 — minX/maxX 를 매 프레임 소비하는 유일한 지점
    {
        float halfWidth = _cam.orthographicSize * _cam.aspect;
        Vector3 pos = transform.position;
        pos.x = Mathf.Clamp(pos.x, minX + halfWidth, maxX - halfWidth);
        transform.position = pos;
    }

    void LateUpdate()             // 340행 — 순서: 레거시 Lerp → 보스/일반 분기 → 줌 Lerp
    {                             //         → ApplyXClamp() → X 재앵커(D-17)
        ...
        ApplyXClamp();
        if (!_isBossZone) _deadzoneCenterX = transform.position.x + _currentBoxOffsetX;
    }
```

> **중요**: `minX` / `maxX` 는 이미 `public` 필드다. 따라서 이전 경계를 캐시하기 위해 **게터를 새로 만들 필요가 없고, `CameraController.cs` 는 Task 1 의 삽입 외에 어떤 변경도 필요 없다.**

`Assets/Camera/Script/BossZoomTrigger.cs` (전문 — 이 구조를 그대로 미러링한다):
```csharp
using UnityEngine;

// Put this on a GameObject with a BoxCollider2D (Is Trigger = on) that covers a boss arena (D-01).
// ...
public class BossZoomTrigger : MonoBehaviour
{
    private void OnTriggerEnter2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        CameraController.Instance.SetBossZoom(true);
    }

    // Leaving the zone reverts to the normal stage zoom immediately, no boss-death event needed (D-03).
    private void OnTriggerExit2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        CameraController.Instance.SetBossZoom(false);
    }
}
```
</interfaces>

<locked_decisions>
**MX-01 ~ MX-05 는 사용자 지시사항이다. 실행 중 재논의·변경 금지.**

- **MX-01 — 단순 대입만**: `SetXBounds` 는 기존 `minX` / `maxX` 필드에 대입만 한다. 클램프 계산, 유효성 검사, 즉시 재클램프 호출, 보간(Lerp/SmoothDamp) **전부 금지**. 두 필드는 이미 `ApplyXClamp()` 가 매 프레임 소비하므로 신규 로직이 필요 없다.
- **MX-02 — BossZoomTrigger 패턴 완전 미러링**: 동일한 `OnTriggerEnter2D` / `OnTriggerExit2D` 시그니처, 동일한 `if (!other.CompareTag("Player")) return;` 가드, 동일한 `CameraController.Instance.XXX(...)` 싱글톤 접근, 동일한 헤더 주석 스타일(영문 ASCII). **다른 아키텍처(이벤트 버스, ScriptableObject 구역 에셋, Cinemachine Confiner 등) 도입 금지.**
- **MX-03 — 필드 구성**: `BoxCollider2D`(Is Trigger) 전제 + `zoneMinX` / `zoneMaxX` 두 개의 public Inspector 필드. **public 필드는 이 2개가 전부다.** (MX-05 캐시용 private 필드는 별도 — 아래 참조.)
- **MX-04 — 코드 전용**: 씬(`.unity`) 파일에 GameObject 생성/수정 금지. 에디터 배치는 사용자가 직접 수행할 수동 후속 작업이며, 이 플랜은 그 **가이드만 문서로 남긴다**.

- **MX-05 — 이탈 시 이전 경계로 복원 (사용자 결정, 2026-08-05 확정)**

  `CameraBoundsTrigger` 는 `OnTriggerExit2D` 를 **가진다**. `BossZoomTrigger` 가 이탈 시 일반 줌으로 되돌리는 것과 동일한 성격이며, 구역은 **영구 핸드오프가 아니라 범위 한정 오버라이드(scoped override)** 다.

  - **Enter**: `CameraController.Instance.minX` / `.maxX` 의 **현재 값을 먼저 캐시**한 뒤 `SetXBounds(zoneMinX, zoneMaxX)` 를 호출한다. **순서가 핵심이다** — 덮어쓴 뒤에 캐시하면 자기 자신의 값을 캐시하게 되어 복원이 무의미해진다.
  - **Exit**: `SetXBounds(_prevMinX, _prevMaxX)` 로 캐시 값을 복원한다.
  - **캐시는 트리거 인스턴스당 단일 슬롯**이다. 스택/히스토리/구역 매니저를 만들지 않는다 (사용자 명시: over-engineering 금지).
  - **진입 없이 이탈하는 경우 아무것도 복원하지 않는다.** `_hasCachedPrev` 플래그 1개로 가드한다. 플레이어가 구역 **안에서 스폰**하면 Enter 가 발생하지 않으므로 캐시가 비어 있고, 가드가 없으면 이탈 시 `0 / 0` 으로 복원되어 `Mathf.Clamp(min > max)` 상태가 되고 **카메라가 한 지점에 고정되는 눈에 띄는 버그**가 된다. 이 가드는 3줄이며 요구사항의 일부다.

  **알려진 한계 (수정하지 않는다 — 문서화만 한다, 사용자 명시):** 두 구역 트리거가 **겹치거나** 정상 순서를 벗어나 통과되면, 두 번째 트리거가 캐시한 "이전 경계"가 원래 기본 구역이 아니라 **첫 번째 트리거의 구역 값**일 수 있다. 이 경우 복원 결과는 프레임 단위로는 유효하지만 논리적으로는 낡은(stale) 값이다. **레벨 디자인에서 트리거 구역을 겹치지 않게 배치하는 것으로 회피한다.** 코드로 방어하지 않는다.
</locked_decisions>

<critical_constraints>
## 1. `CameraController.cs` 파일 인코딩 — 최우선 주의사항

`CameraController.cs` 는 **UTF-8 + CRLF 개행**이며, **5줄(5, 8, 9, 327, 328행)에 이미 깨진 한글 주석 바이트**가 들어 있다. 이것은 파일의 기존 상태이며 이번 작업으로 고치지 않는다.

- **`Write` 도구로 `CameraController.cs` 전체를 다시 쓰지 말 것.** 반드시 `Edit` 도구로 지정된 ASCII 앵커만 사용한다.
- `old_string` / `new_string` 에 깨진 한글 주석 라인을 절대 포함하지 말 것.
- **새로 추가하는 주석은 ASCII 영문만 사용한다 (한글 금지).** 이 플랜의 코드 블록을 그대로 복사한다.
- 인코딩 게이트: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == **5** (작업 전후 동일)

`CameraBoundsTrigger.cs` 는 **신규 파일이므로 `Write` 도구로 생성한다.** `BossZoomTrigger.cs` 와 동일하게 **순수 ASCII(비-ASCII 0)** 여야 한다. `Check.md` 는 UTF-8 문서이므로 한글 작성 가능하다.

**`Edit` 도구를 쓸 수 없는 실행 환경일 경우의 대체 절차** (인코딩 파괴 방지 — 반드시 이 순서를 지킨다):
`Write` 로 `CameraController.cs` 전체를 재작성하지 말고, 아래처럼 파일을 라인 단위로 쪼개 삽입 블록만
끼워 넣는다. 삽입 블록 파일은 반드시 ASCII 이고 CRLF(`\r\n`) 로 끝나야 한다.
```
cd "$(git rev-parse --show-toplevel)"
F=Assets/Camera/Script/CameraController.cs
N=$(grep -nF '    // Clamps X so the visible left/right edges never pass' $F | cut -d: -f1)   # 삽입 지점 = 이 줄 바로 앞
printf '%s\r\n' "<삽입 블록의 각 줄>" > /tmp/insert.txt   # 스크래치 디렉토리 사용
head -n $((N-1)) $F > /tmp/new.cs && cat /tmp/insert.txt >> /tmp/new.cs && tail -n +$N $F >> /tmp/new.cs
mv /tmp/new.cs $F
```
적용 후 **반드시** `LC_ALL=C grep -c '[^[:print:][:space:]]' $F` == 5 와 `file $F` 의 "CRLF line terminators"
표시, 그리고 삭제 라인 0 게이트를 재확인한다. 하나라도 어긋나면 `git checkout -- $F` 로 되돌리고 다시 시도한다.

## 2. `CameraController.cs` 는 순수 삽입(pure insertion) 이어야 한다

이 작업은 기존 코드를 **한 줄도 수정하거나 삭제하지 않는다.** 새 메서드 하나를 통째로 끼워 넣을 뿐이다.
**MX-05(이탈 복원)는 `CameraController.cs` 에 아무 영향이 없다** — `minX`/`maxX` 가 이미 public 이므로 게터 추가가 불필요하다.

- 하드 게이트: `git diff HEAD -- Assets/Camera/Script/CameraController.cs | grep '^-' | grep -v '^---' | wc -l` == **0**
- `.planning/STATE.md` 에 기록된 Phase 9/10 의 반복 실패 패턴(오래된 baseline 기준 삭제 라인 개수 게이트 오작동)을 피하기 위해 baseline 은 **`HEAD`** 를 쓴다. 작업 시작 시점에 `Assets/Camera/` 하위는 `.meta` 2개(untracked)를 제외하면 깨끗하다.

## 3. 주석 리터럴 주의 (Phase 9/10 반복 교훈)

아래 검증 게이트가 `CameraController.cs` 내 `minX` / `maxX` 등장 **라인 수를 7 로 고정**한다 (기존 6 + 신규 대입 1). 따라서 **`CameraController.cs` 에 새로 쓰는 주석에 `minX` / `maxX` 리터럴을 쓰지 말고 "the two fields above" 등으로 표현한다.** 이 플랜의 주석 문구를 그대로 쓰면 자동으로 만족한다.

(`.planning/STATE.md` 기록: Phase 9 Plan 1 의 `DontDestroyOnLoad`, Phase 10 Plan 1 의 `deadzoneHeight` — 플랜이 지정한 주석 문구가 플랜 자신의 카운트 게이트와 충돌한 사례가 2회 있었다.)

> 이 게이트는 `CameraController.cs` 에만 적용된다. `CameraBoundsTrigger.cs` 는 `.minX` / `.maxX` 를 캐시 목적으로 읽어야 하므로 해당 리터럴이 등장하는 것이 정상이다.

## 4. `Instance` null 체크를 추가하지 말 것

`BossZoomTrigger` 는 `CameraController.Instance` 를 null 체크 없이 호출한다. **MX-02(완전 미러링)에 따라 동일하게 null 체크 없이 호출한다.** 씬에 `CameraController` 가 없는 것은 씬 설정 오류이며 조용히 무시하는 것보다 즉시 NullReference 로 드러나는 편이 낫다.

## 5. 범위 밖 (하지 말 것)

- `Assets/Player/Script/PlayerController.cs`, `Assets/Player/Script/InputHandler.cs` 수정 금지 (0줄).
- `Assets/Camera/Script/BossZoomTrigger.cs` 수정 금지 (0줄). **참고용으로 읽기만 한다.**
- 씬(`.unity`) 파일 수정/추가 금지 (MX-04).
- `.meta` 파일 **직접 생성 금지.** Unity 에디터가 다음 포커스 시 `CameraBoundsTrigger.cs.meta` 를 자동 생성한다. (참고: `BossZoomTrigger.cs.meta` / `Check.md.meta` 도 현재 untracked 상태다 — 기존 리포 상태이므로 이 작업에서 건드리지 않는다.)
- `minY` / `maxY` 등 Y축 경계 추가 금지 (범위 밖 — 카운트 0 유지).
- **MX-05 의 알려진 한계(겹친 구역 / 비정상 순서 통과)를 코드로 해결하려 하지 말 것.** 구역 스택, 트리거 우선순위, 활성 구역 매니저, `List<>` 히스토리 등 **일체 금지** — 문서화만 한다.
- `SetYBounds`, `SetBounds(Rect)`, 구역 전환 보간, `OnDrawGizmos` 등 요청되지 않은 확장 금지 (CLAUDE.md §2 오버엔지니어링 금지).
- 기존 데드존 / 동적 오프셋 / 피킹 / 줌 로직 수정 금지.
- Cinemachine 도입 금지.

## 6. 커밋

작업 트리에 이 작업과 무관한 미커밋 변경(TutorialBoss, 메뉴 패널, 씬 등)이 다수 존재한다. **`git add -A` / `git commit -a` 절대 금지.** 경로 3개만 명시해 커밋한다.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: CameraController.cs — SetXBounds(float, float) 퍼블릭 메서드 삽입 (순수 삽입)</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **작업 직전에 반드시 Read 로 확인한다.** `Edit` 의 `old_string` 은 이 Read 결과에서 그대로 복사한 ASCII 라인이어야 한다 (기억으로 재작성 금지).
  </read_first>

  <action>
`Edit` 도구로 **1회** 편집한다. `SetBossZoom` 과 `ApplyXClamp` 사이에 새 메서드를 삽입한다 (퍼블릭 트리거 API 끼리 인접시키기 위함).

`old_string` (현재 157-158행, 파일 내 유일):
```
    // Clamps X so the visible left/right edges never pass minX / maxX (D-09 / D-11).
    // Uses the CURRENT orthographicSize so the bound stays correct mid zoom transition.
```

`new_string` (아래 블록 + 위 2줄을 그대로 뒤에 붙임):
```csharp
    // Called by CameraBoundsTrigger on enter and on exit: hands the camera the X limits of the
    // zone the player is currently scoped to, so a single scene can hard clamp room by room
    // (260805-m41). The trigger caches the previous pair itself and feeds it back on exit,
    // which is why this method needs no history of its own.
    // Idempotent, last call wins - same contract as SetBossZoom above.
    // Plain field assignment on purpose: the two fields above are already consumed by
    // ApplyXClamp every frame, and LateUpdate re-anchors the deadzone box on the clamped
    // position (D-17), so a bounds swap is absorbed on the very next frame with no extra
    // logic here. Do NOT clamp, validate or interpolate in this method.
    // A zone narrower than the camera view collapses the clamp onto a single X - that is the
    // designer's responsibility when authoring the zone, it is not guarded here.
    public void SetXBounds(float min, float max)
    {
        minX = min;
        maxX = max;
    }

    // Clamps X so the visible left/right edges never pass minX / maxX (D-09 / D-11).
    // Uses the CURRENT orthographicSize so the bound stays correct mid zoom transition.
```

### 하지 말 것 (재확인)
- `SetXBounds` 본문에 `Mathf.Clamp` / `ApplyXClamp()` / `Lerp` / `SmoothDamp` / `_deadzoneCenterX` 호출 추가 금지 (MX-01).
- **이전 경계 캐시를 `CameraController` 에 두지 말 것** — 캐시는 트리거 인스턴스가 소유한다 (MX-05). 여기에 `_prevMinX` 같은 필드를 만들면 모든 구역이 하나의 슬롯을 공유해 복원이 깨진다.
- 기존 라인 수정/삭제 금지 — 이 편집은 순수 삽입이다 (제약 §2).
- 새 주석에 `minX` / `maxX` 리터럴 사용 금지 (제약 §3). 위 문구는 이미 만족한다.
- 필드 선언부(`[Header("Camera X Bounds")]` 블록) 수정 금지.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "public void SetXBounds(float min, float max)" $F && grep -qF "minX = min;" $F && grep -qF "maxX = max;" $F && [ "$(grep -cF 'minX' $F)" = "7" ] && [ "$(grep -cF 'maxX' $F)" = "7" ] && [ "$(grep -cF 'SetXBounds' $F)" = "1" ] && [ "$(grep -cF '_prevMinX' $F)" = "0" ] && [ "$(awk '/public void SetXBounds\(float min, float max\)/,/^    \}$/' $F | grep -cE 'Clamp|Lerp|SmoothDamp|_deadzoneCenterX|ApplyXClamp|orthographicSize')" = "0" ] && [ "$(awk '/public void SetXBounds\(float min, float max\)/,/^    \}$/' $F | grep -c ';')" = "2" ] && [ "$(grep -cF 'minY' $F)" = "0" ] && [ "$(grep -cF 'maxY' $F)" = "0" ] && [ "$(grep -cF 'SetYBounds' $F)" = "0" ] && [ "$(git diff HEAD -- $F | grep '^-' | grep -v '^---' | wc -l | tr -d ' ')" = "0" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && SB=$(grep -nF 'public void SetBossZoom' $F | cut -d: -f1) && SX=$(grep -nF 'public void SetXBounds' $F | cut -d: -f1) && AC=$(grep -nF 'private void ApplyXClamp()' $F | cut -d: -f1) && [ "$SB" -lt "$SX" ] && [ "$SX" -lt "$AC" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "public void SetXBounds(float min, float max)"` 성공 — 시그니처가 지시사항과 정확히 일치.
    - `grep -qF "minX = min;"` / `"maxX = max;"` 성공 — 기존 필드에 직접 대입 (MX-01).
    - **본문이 정확히 2문장**: `SetXBounds` 본문의 `;` 개수 == **2**. 3 이상이면 로직을 추가한 것 → MX-01 위반.
    - **본문 금지 심볼 0회**: `Clamp` / `Lerp` / `SmoothDamp` / `_deadzoneCenterX` / `ApplyXClamp` / `orthographicSize` (MX-01).
    - **캐시가 CameraController 에 없음**: `grep -cF '_prevMinX'` == **0** (MX-05 — 캐시는 트리거 인스턴스 소유).
    - `grep -cF 'minX'` == **7**, `grep -cF 'maxX'` == **7** (기존 6 + 대입 1). 8 이상이면 주석에 리터럴을 쓴 것 → 문구 수정 (제약 §3).
    - `grep -cF 'SetXBounds'` == **1** — 정의 1회뿐, CameraController 내부에서 자기 호출하지 않는다.
    - `grep -cF 'minY'` == 0, `grep -cF 'maxY'` == 0, `grep -cF 'SetYBounds'` == 0 (범위 밖).
    - **순수 삽입 게이트**: `git diff HEAD -- $F` 의 삭제 라인 수 == **0**.
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' $F` == **5**.
    - **배치 순서**: `SetBossZoom` 행 < `SetXBounds` 행 < `ApplyXClamp` 행.
  </acceptance_criteria>

  <done>`CameraController` 가 런타임 X 경계 교체 진입점을 노출한다. 기존 코드는 한 줄도 변경되지 않았고(삭제 라인 0), 깨진 한글 주석 5줄과 CRLF 개행이 보존되었다.</done>
</task>

<task type="auto">
  <name>Task 2: CameraBoundsTrigger.cs 신규 작성 — Enter 적용 / Exit 복원 (MX-05)</name>

  <files>Assets/Camera/Script/CameraBoundsTrigger.cs</files>

  <read_first>
    - `Assets/Camera/Script/BossZoomTrigger.cs` — 미러링할 원본 구조/주석 스타일 (Enter/Exit 쌍 포함). **수정하지 말고 읽기만 한다.**
    - Task 1 적용 후의 `Assets/Camera/Script/CameraController.cs` (`SetXBounds` 시그니처, `minX`/`maxX` 가 public 인지 확인).
  </read_first>

  <action>
`Write` 도구로 `Assets/Camera/Script/CameraBoundsTrigger.cs` 를 **아래 내용 그대로** 생성한다. 순수 ASCII, 한글 주석 금지 (`BossZoomTrigger.cs` 와 동일).

```csharp
using UnityEngine;

// Put this on a GameObject with a BoxCollider2D (Is Trigger = on) that covers one room / zone
// (260805-m41). Entering the zone hands CameraController the X limits that room should clamp to,
// so a single scene can frame room by room instead of sharing one pair of scene-wide bounds.
// Wide easter egg areas are the same script with wider values - no extra component needed.
//
// A zone is a SCOPED OVERRIDE, not a permanent handoff: the bounds that were active right before
// entering are cached and restored on exit, mirroring how BossZoomTrigger reverts its zoom (MX-05).
//
// The trigger volumes themselves are placed manually per zone by the designer, exactly like
// BossZoomTrigger: this script never creates or edits scene objects.
public class CameraBoundsTrigger : MonoBehaviour
{
    [Header("Zone X Bounds")]
    // World-space X limits handed to the camera on enter. Tune per zone in the Inspector.
    // Defaults match CameraController's own wide defaults, so a zone left untuned is a no-op
    // rather than a camera that snaps to the origin.
    // Keep the zone at least as wide as the camera view, or the clamp collapses onto a single X.
    public float zoneMinX = -1000f;
    public float zoneMaxX = 1000f;

    // Single slot holding the bounds that were active immediately before this trigger fired.
    // One slot per trigger instance on purpose: no stack, no history, no zone manager (MX-05).
    // Known limitation, documented in Check.md and NOT fixed here: if two zones overlap or are
    // traversed out of order, this slot can hold the neighbouring zone's values instead of the
    // original base bounds, so the restore is frame-correct but logically stale. Level design
    // avoids it by not overlapping trigger volumes.
    private float _prevMinX;
    private float _prevMaxX;
    // False until this trigger has actually applied its own bounds. Guards the case where the
    // player SPAWNS inside the zone: enter never fires, so restoring would push 0 / 0 into the
    // camera and freeze it, since the clamp would then have its min above its max.
    private bool _hasCachedPrev;

    private void OnTriggerEnter2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        // Cache BEFORE overwriting - reading after the call would just cache this zone's own values.
        _prevMinX = CameraController.Instance.minX;
        _prevMaxX = CameraController.Instance.maxX;
        _hasCachedPrev = true;
        CameraController.Instance.SetXBounds(zoneMinX, zoneMaxX);
    }

    // Leaving the zone restores the previous bounds immediately, the same way BossZoomTrigger
    // reverts its zoom on exit (MX-05).
    private void OnTriggerExit2D(Collider2D other)
    {
        if (!other.CompareTag("Player")) return;
        if (!_hasCachedPrev) return;
        CameraController.Instance.SetXBounds(_prevMinX, _prevMaxX);
    }
}
```

### 하지 말 것 (재확인)
- **캐시 순서 뒤집기 금지.** `_prevMinX` / `_prevMaxX` 대입이 반드시 `SetXBounds(zoneMinX, zoneMaxX)` **앞**에 와야 한다 (MX-05). 뒤에 오면 자기 자신의 값을 캐시해 복원이 무의미해진다.
- `_hasCachedPrev` 가드 제거 금지 (MX-05 — 스폰-인-구역 안전장치).
- **구역 스택 / 히스토리 / 우선순위 / 활성 구역 매니저 / `List<>` 추가 절대 금지** (제약 §5 — 알려진 한계는 문서화만 한다).
- `CameraController.Instance` null 체크 추가 금지 (제약 §4).
- `Start` / `Awake` / `Update` / `LateUpdate` / `OnDrawGizmos` / `Reset` 추가 금지.
- `RequireComponent`, `SerializeField private`, `[Tooltip]`, 프로퍼티, 인터페이스 추가 금지 — `BossZoomTrigger` 스타일 그대로 public 필드 + 주석 (MX-02).
- `.meta` 파일 수동 생성 금지 (제약 §5).
- 파일에 한글/비-ASCII 문자 사용 금지.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && T=Assets/Camera/Script/CameraBoundsTrigger.cs && [ -f $T ] && grep -qF "public class CameraBoundsTrigger : MonoBehaviour" $T && grep -qF "public float zoneMinX" $T && grep -qF "public float zoneMaxX" $T && [ "$(grep -c 'if (!other.CompareTag("Player")) return;' $T)" = "2" ] && grep -qF "CameraController.Instance.SetXBounds(zoneMinX, zoneMaxX);" $T && grep -qF "CameraController.Instance.SetXBounds(_prevMinX, _prevMaxX);" $T && grep -qF "_prevMinX = CameraController.Instance.minX;" $T && grep -qF "_prevMaxX = CameraController.Instance.maxX;" $T && grep -qF "if (!_hasCachedPrev) return;" $T && [ "$(grep -c 'private void OnTriggerEnter2D' $T)" = "1" ] && [ "$(grep -c 'private void OnTriggerExit2D' $T)" = "1" ] && CACHE=$(grep -nF '_prevMinX = CameraController.Instance.minX;' $T | cut -d: -f1) && APPLY=$(grep -nF 'CameraController.Instance.SetXBounds(zoneMinX, zoneMaxX);' $T | cut -d: -f1) && REST=$(grep -nF 'CameraController.Instance.SetXBounds(_prevMinX, _prevMaxX);' $T | cut -d: -f1) && GUARD=$(grep -nF 'if (!_hasCachedPrev) return;' $T | cut -d: -f1) && [ "$CACHE" -lt "$APPLY" ] && [ "$APPLY" -lt "$REST" ] && [ "$GUARD" -lt "$REST" ] && [ "$(grep -cE '(void|IEnumerator) +(Start|Awake|Update|LateUpdate|FixedUpdate|Reset|OnDrawGizmos)' $T)" = "0" ] && [ "$(grep -cE 'RequireComponent|SerializeField|Tooltip|SetBossZoom|Cinemachine|Mathf\.|List<|Stack<|Queue<|static ' $T)" = "0" ] && [ "$(grep -cE '^ *public ' $T)" = "3" ] && [ "$(grep -cE '^ *private float |^ *private bool ' $T)" = "3" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $T)" = "0" ] && [ "$(wc -l < $T | tr -d ' ')" -le 56 ] && [ -z "$(git status --porcelain -- Assets/Camera/Script/BossZoomTrigger.cs Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs)" ] && [ -z "$(git status --porcelain -- '*.unity')" ] && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - 파일이 존재하고 `public class CameraBoundsTrigger : MonoBehaviour` 를 선언한다.
    - `zoneMinX` / `zoneMaxX` public 필드 2개 존재 (MX-03).
    - **Enter/Exit 쌍 존재**: `private void OnTriggerEnter2D` 1회, `private void OnTriggerExit2D` 1회 (MX-05).
    - 태그 가드가 **2회** 등장하며 `BossZoomTrigger` 와 문자 단위로 동일하다 (MX-02).
    - `SetXBounds(zoneMinX, zoneMaxX)` (적용) 과 `SetXBounds(_prevMinX, _prevMaxX)` (복원) 이 모두 존재한다.
    - **캐시 순서 게이트 (MX-05 핵심)**: `_prevMinX = CameraController.Instance.minX;` 행 번호 < `SetXBounds(zoneMinX, zoneMaxX);` 행 번호. 역순이면 자기 값을 캐시한 것 → 복원이 무의미해짐.
    - **복원 순서 게이트**: 적용 행 < 복원 행, 그리고 `if (!_hasCachedPrev) return;` 행 < 복원 행 (가드가 복원보다 먼저).
    - private 필드가 **정확히 3개** (`_prevMinX`, `_prevMaxX`, `_hasCachedPrev`). 4개 이상이면 히스토리/스택을 만든 것 → 제약 §5 위반.
    - `List<` / `Stack<` / `Queue<` / `static ` **0회** (구역 매니저 금지).
    - `Start`/`Awake`/`Update`/`LateUpdate`/`FixedUpdate`/`Reset`/`OnDrawGizmos` 정의 **0회**.
    - `RequireComponent` / `SerializeField` / `Tooltip` / `Mathf.` / `Cinemachine` **0회**.
    - `public` 로 시작하는 줄 == **3** (클래스 1 + 필드 2). 4 이상이면 요청되지 않은 API 를 추가한 것.
    - **비-ASCII 0** (`BossZoomTrigger.cs` 와 동일한 순수 ASCII 파일).
    - 총 라인 수 **56줄 이하** (CLAUDE.md §2 코드 다이어트).
    - `BossZoomTrigger.cs` / `PlayerController.cs` / `InputHandler.cs` / `.unity` 파일의 `git status --porcelain` 결과가 비어 있다 (0줄 변경).
  </acceptance_criteria>

  <done>`CameraBoundsTrigger` 가 `BossZoomTrigger` 와 동일한 구조·가드·싱글톤 접근으로 존재하며, 진입 시 직전 경계를 캐시한 뒤 구역 경계를 적용하고 이탈 시 캐시 값으로 복원한다. 캐시는 인스턴스당 1슬롯이며 스택/매니저가 없다. 다른 파일은 하나도 변경되지 않았다.</done>
</task>

<task type="auto">
  <name>Task 3: Check.md 에 수동 배치 가이드 + Play 모드 체크리스트 + 알려진 한계 추가, 정적 검사 기록 후 커밋</name>

  <files>Assets/Camera/Check.md</files>

  <read_first>
    - `Assets/Camera/Check.md` — 현재 253행. 이 파일은 **UTF-8 이므로 한글 작성 가능**하다 (`CameraController.cs` 와 다르다). 맨 끝 줄(`**Play 모드 검증 상태**: 미검증 ...`)이 이번 추가의 앵커다.
  </read_first>

  <action>
### 3-1. `Check.md` 편집 (`Edit` 도구 1회 — 기존 내용 삭제 절대 금지, 파일 끝에 추가)

`old_string` (현재 파일의 **마지막 줄**, Read 결과에서 그대로 복사):
```
**Play 모드 검증 상태**: 미검증 (위 "5) Y축 하드컷 데드존" 체크리스트는 사용자가 직접 확인해야 한다. PASS 로 허위 기록 금지.)
```

`new_string` = 위 줄 + 아래 블록:
```
**Play 모드 검증 상태**: 미검증 (위 "5) Y축 하드컷 데드존" 체크리스트는 사용자가 직접 확인해야 한다. PASS 로 허위 기록 금지.)

---

# quick task 260805-m41 — 구역별 카메라 X 경계 (CameraBoundsTrigger)

## 변경사항

- `CameraController.SetXBounds(float min, float max)` 신규 — `minX`/`maxX` 에 단순 대입만 한다(MX-01).
  클램프 계산은 기존 `ApplyXClamp()` 가 매 프레임 그대로 수행하므로 신규 로직이 없다.
- `Assets/Camera/Script/CameraBoundsTrigger.cs` 신규 — `BossZoomTrigger` 와 동일 패턴(Player 태그 가드 +
  `CameraController.Instance` 싱글톤). `zoneMinX` / `zoneMaxX` 두 개의 Inspector 필드를 가진다(MX-03).
- **구역 = 범위 한정 오버라이드 (MX-05)**: 진입 시 **직전에 활성이던 경계를 캐시**한 뒤 구역 경계를 적용하고,
  이탈 시 그 캐시 값으로 **되돌린다**. `BossZoomTrigger` 가 이탈 시 일반 줌으로 복귀하는 것과 같은 성격이다.
  캐시는 **트리거 인스턴스당 단일 슬롯**이며 스택/히스토리/구역 매니저는 없다(의도적 단순화).
- 구역 안에서 스폰해 진입 이벤트가 발생하지 않은 트리거는 이탈 시 **아무것도 복원하지 않는다**
  (`_hasCachedPrev` 가드). 가드가 없으면 `0 / 0` 이 적용되어 카메라가 한 지점에 고정된다.

## 알려진 한계 (코드로 방어하지 않음 — 레벨 디자인으로 회피)

- **구역이 겹치거나 비정상 순서로 통과되면 복원 값이 낡을 수 있다.** 구역 A 안에서 구역 B 에 진입하면
  B 는 "직전 경계"로 **A 의 값**을 캐시한다. 이후 B 를 벗어나면 A 로 복원되는데, 이때 플레이어가 이미
  A 밖이라면 논리적으로는 기본 경계로 돌아가야 맞다. 프레임 단위로는 유효한 값이지만 의미상 stale 하다.
  → **트리거 볼륨을 서로 겹치지 않게 배치**하면 발생하지 않는다. 단일 슬롯 캐시로 충분하다고 판단해
  구역 스택/우선순위 로직은 의도적으로 만들지 않았다.
- **구역 사이의 빈틈**에서는 직전 구역 진입 전의 경계(보통 씬 기본값)로 돌아간다. 이는 설계상 정상이며,
  방과 방 사이가 곧바로 이어지길 원하면 트리거를 맞붙여 배치한다.
- **구역 폭 < 카메라 가로폭**이면 `Mathf.Clamp` 의 min 이 max 를 넘어 카메라가 한 지점에 고정된다.
  코드에서 막지 않는다(설계상 designer 책임) — 아래 설정 5단계 참조.

## 수동 에디터 설정 (사용자 작업 — 코드로는 하지 않음, MX-04)

구역 하나당 아래를 반복한다.

1. Hierarchy 에서 빈 GameObject 생성 → 이름 예: `CameraBounds_Room01`.
2. `Box Collider 2D` 추가 → **`Is Trigger` 체크**.
3. Collider 의 Size/Offset 을 해당 방 전체를 덮도록 조정한다. **다른 구역 트리거와 겹치지 않게** 배치한다
   (겹침은 위 "알려진 한계"의 stale 복원 원인이다). 방과 방을 곧바로 잇고 싶으면 맞붙이되 겹치지는 않게 한다.
4. `Camera Bounds Trigger` 스크립트 추가 → `Zone Min X` / `Zone Max X` 에 그 방의 좌/우 월드 X 좌표 입력.
5. **구역 폭 >= 카메라 가로폭** 을 지킨다. 카메라 가로폭 = `2 * orthographicSize * aspect`
   (일반 스테이지 `normalZoom = 5`, 16:9 기준 약 17.8 월드 유닛). 이보다 좁으면 클램프가 한 점으로
   무너져 카메라가 고정된다 — 코드에서 막지 않는다(설계상 designer 책임).
6. `Main Camera` 의 `Min X` / `Max X` Inspector 값이 **기본(fallback) 경계**가 된다. 어떤 구역에도 속하지
   않을 때 이 값이 쓰이므로, 맵 전체를 감싸는 넉넉한 값으로 맞춰 둔다.
7. 이스터에그 확장 구역은 **같은 스크립트에 넓은 값만** 넣으면 된다. 별도 컴포넌트 불필요.
8. 보스 구역은 `BossZoomTrigger`(줌)와 `CameraBoundsTrigger`(경계)를 **각각 독립적으로** 붙인다.
   한 GameObject 에 둘 다 붙여도 되고 따로 둬도 된다 — 서로 간섭하지 않는다.

## Play 모드 검증 항목

### 6) 구역별 카메라 X 경계 (quick task 260805-m41)

- [ ] **구역 진입 시 경계 교체**: 방 A 에서 방 B 로 넘어가면 `Main Camera` Inspector 의 `Min X` / `Max X`
      값이 방 B 트리거의 `Zone Min X` / `Zone Max X` 로 바뀐다 (Play 중 Inspector 실시간 확인).
- [ ] **구역 이탈 시 복원 (MX-05)**: 방 B 를 빠져나오면 `Min X` / `Max X` 가 **진입 직전 값**(보통 씬 기본값)
      으로 되돌아간다. 값이 그대로 남아 있으면 `OnTriggerExit2D` 가 없거나 태그 가드에서 걸린 것이다.
- [ ] **캐시 순서 확인**: 구역 이탈 후 복원된 값이 **그 구역 자신의 값과 다르다**. 같다면 진입 시
      `SetXBounds` 를 호출한 **뒤에** 캐시한 것이다 (MX-05 순서 위반).
- [ ] **다음 프레임 클램프**: 값이 바뀐 직후 카메라가 새 경계 안쪽으로 즉시(1프레임) 들어온다.
      별도 재클램프 코드 없이 기존 `ApplyXClamp()` 만으로 동작하는지 확인 (MX-01 근거).
- [ ] **좌우 하드 클램프**: 방 안에서 좌/우 끝까지 달리면 카메라가 `Zone Min X`/`Zone Max X` 에서 정확히
      멈추고 플레이어만 계속 이동한다.
- [ ] **경계에서 되돌아올 때 즉시 반응**: 클램프된 상태에서 반대 방향으로 걸으면 카메라가 지연 없이
      따라온다 (D-17 재앵커가 여전히 동작하는지 = 데드존 회귀 확인).
- [ ] **인접 구역 연속 통과**: 방 A ↔ B 를 빠르게 여러 번 오가도 경계가 튀지 않고 매번 들어간 방의 값이
      적용되며, 나올 때마다 직전 값으로 복원된다.
- [ ] **구역 안 스폰 후 이탈 (안전장치)**: 플레이어가 트리거 **안에서 시작**한 뒤 밖으로 걸어나가도
      카메라가 한 지점에 고정되지 않는다 (`_hasCachedPrev` 가드가 `0 / 0` 복원을 막는지 확인).
- [ ] **이스터에그 확장 구역**: 넓은 값을 넣은 구역에 들어가면 카메라가 훨씬 멀리까지 따라가고,
      나오면 원래 좁은 경계로 복원된다.
- [ ] **보스 구역과 독립**: `BossZoomTrigger` 구역에 들어가 줌이 바뀌어도 경계는 그대로이고,
      줌 전환 중에도 클램프가 깨지지 않는다 (`ApplyXClamp` 가 현재 `orthographicSize` 를 쓰므로
      화면이 넓어지면 유효 이동 범위가 좁아지는 것이 정상).
- [ ] **Player 태그 가드**: 적/투사체가 트리거를 드나들어도 경계가 바뀌거나 복원되지 않는다.
- [ ] **Gizmo 반영**: Play 중 `Main Camera` 의 빨간 세로선 2개가 현재 구역 경계 위치로 이동하고,
      이탈 시 원래 위치로 돌아온다 (`CameraController.OnDrawGizmos` 가 필드를 그대로 그리므로 자동 반영).
- [ ] **Phase 9/10 회귀 없음**: 데드존/동적 오프셋/피킹/줌 동작이 이전과 동일하다.

## quick task 260805-m41 결과 기록

### 정적 회귀 검사 (2026-08-05)

| # | 검사 | 기대값 | 실제값 | 판정 |
|---|---|---|---|---|
| 1 | `CameraController.cs` 삭제 라인 (순수 삽입) | 0 | | |
| 2 | `CameraController.cs` 인코딩 게이트 (비-ASCII 라인) | 5 | | |
| 3 | `minX` / `maxX` 등장 라인 (`CameraController.cs`) | 7 / 7 | | |
| 4 | `SetXBounds` 본문 `;` 개수 / 금지 심볼 | 2 / 0 | | |
| 5 | `CameraBoundsTrigger.cs` 비-ASCII / 라인 수 | 0 / <=56 | | |
| 6 | Enter/Exit 정의 + 태그 가드 (MX-05) | 1 / 1 / 2 | | |
| 7 | 캐시 순서 (캐시 행 < 적용 행 < 복원 행, 가드 행 < 복원 행) | 성립 | | |
| 8 | private 필드 개수 / `List<`,`Stack<`,`static ` | 3 / 0 | | |
| 9 | `public` 시작 줄 (클래스 1 + 필드 2) | 3 | | |
| 10 | 읽기 전용 파일 무수정 (`BossZoomTrigger.cs`/`PlayerController.cs`/`InputHandler.cs`/`*.unity`) | 0줄 | | |
| 11 | 커밋 파일 목록 | 3개 | | |

**Play 모드 검증 상태**: 미검증 (위 "6) 구역별 카메라 X 경계" 체크리스트 13개는 사용자가 씬에 트리거를
직접 배치한 뒤 확인해야 한다. PASS 로 허위 기록 금지.)
```

### 3-2. 정적 검사 실행 및 표 채우기

아래 `verify` 블록 및 Task 1/Task 2 의 `verify` 블록을 실행해 실제 측정값을 표에 채운다. **측정하지 않은 항목을 PASS 로 적지 말 것.**

### 3-3. 커밋

경로 **3개만** 명시해 커밋한다. `git add -A` 금지. `.meta` 는 포함하지 않는다.

```
git add Assets/Camera/Script/CameraController.cs Assets/Camera/Script/CameraBoundsTrigger.cs Assets/Camera/Check.md
git commit -m "feat(camera): add per-zone X bounds via SetXBounds and CameraBoundsTrigger" -m "..."
```

커밋 본문에 포함할 것: `SetXBounds` 가 단순 대입인 이유(기존 `ApplyXClamp` 재사용, MX-01), `CameraBoundsTrigger` 가 `BossZoomTrigger` 패턴 미러링(MX-02), **진입 시 캐시 / 이탈 시 복원 설계와 단일 슬롯 캐시의 알려진 한계(MX-05)**, 씬 배치는 사용자 수동 후속 작업(MX-04), Play 모드 미검증 상태.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && T=Assets/Camera/Script/CameraBoundsTrigger.cs && C=Assets/Camera/Check.md && grep -qF "6) 구역별 카메라 X 경계" $C && grep -qF "CameraBoundsTrigger" $C && grep -qF "Is Trigger" $C && grep -qF "MX-05" $C && grep -qF "알려진 한계" $C && grep -qF "구역 이탈 시 복원" $C && [ "$(grep -c '^- \[ \]' $C)" -ge 65 ] && [ "$(grep -cF 'quick task 260805-m41 결과 기록' $C)" = "1" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $T)" = "0" ] && [ "$(git diff HEAD -- $F | grep '^-' | grep -v '^---' | wc -l | tr -d ' ')" = "0" ] && [ -z "$(git status --porcelain -- Assets/Camera/Script/BossZoomTrigger.cs Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs)" ] && [ -z "$(git status --porcelain -- '*.unity')" ] && [ "$(git show --name-only --format='' HEAD | grep -c .)" = "3" ] && git show --name-only --format='' HEAD | grep -qF "$T" && git show --name-only --format='' HEAD | grep -qF "$F" && git show --name-only --format='' HEAD | grep -qF "$C" && [ "$(git show --name-only --format='' HEAD | grep -c '\.meta$')" = "0" ] && echo T3_OK</automated>
  </verify>

  <acceptance_criteria>
    - `Check.md` 에 `6) 구역별 카메라 X 경계` 섹션과 신규 체크박스 **13개**가 존재한다.
    - **이탈 복원(MX-05) 검증 항목이 존재한다**: "구역 이탈 시 복원", "캐시 순서 확인", "구역 안 스폰 후 이탈" 3개 항목.
    - `## 알려진 한계` 섹션에 겹친 구역 stale 복원, 구역 사이 빈틈, 구역 폭 부족 3가지가 문서화되어 있고 **코드로 방어하지 않는다는 사실**이 명시되어 있다.
    - 수동 에디터 설정 가이드 8단계가 존재하고 `Is Trigger`, **겹치지 않게 배치**, 구역 폭 >= 카메라 가로폭, 기본 fallback 경계 조건이 명시되어 있다.
    - **기존 체크박스 삭제 없음**: 전체 `- [ ]` 개수가 **65개 이상**이다 (작업 전 52 + 신규 13). 감소하면 기존 항목을 지운 것 → 복구.
    - `CameraController.cs` 비-ASCII 라인 == 5, 삭제 라인 == 0 (Task 1 게이트 재확인).
    - `CameraBoundsTrigger.cs` 비-ASCII == 0.
    - `BossZoomTrigger.cs` / `PlayerController.cs` / `InputHandler.cs` / `*.unity` 의 `git status --porcelain` 이 비어 있다.
    - **커밋 파일이 정확히 3개**이며 세 파일 모두 포함되어 있고, `.meta` 파일은 **0개** 포함이다.
    - 결과 기록 표 11개 항목의 실제값이 전부 채워져 있고, Play 모드 항목은 **미체크**로 남아 있다 (허위 PASS 금지).
  </acceptance_criteria>

  <done>사용자가 씬에 트리거를 배치할 수 있는 수동 가이드, 이탈 복원 동작과 알려진 한계 문서, Play 모드 체크리스트 13개가 `Check.md` 에 남았고, 정적 검사 11항목 결과가 기록되었으며, 3개 파일만 커밋되었다.</done>
</task>

</tasks>

<verification>
1. `CameraController.SetXBounds(float min, float max)` 가 존재하고 본문이 `minX = min; maxX = max;` 2문장뿐이다 (MX-01).
2. `CameraController.cs` 는 순수 삽입 — `git diff HEAD` 삭제 라인 0, 비-ASCII 라인 5 유지. 캐시 필드(`_prevMinX`)가 여기에 0회 등장한다.
3. `CameraBoundsTrigger.cs` 가 `BossZoomTrigger` 와 동일한 태그 가드/싱글톤 접근을 쓰고, `OnTriggerEnter2D` / `OnTriggerExit2D` 정의가 각각 1회다 (MX-02 / MX-05).
4. **캐시 순서**: `_prevMinX = CameraController.Instance.minX;` 행 < `SetXBounds(zoneMinX, zoneMaxX);` 행 < `SetXBounds(_prevMinX, _prevMaxX);` 행, 그리고 `if (!_hasCachedPrev) return;` 행 < 복원 행.
5. `CameraBoundsTrigger.cs` 의 private 필드가 정확히 3개이고 `List<`/`Stack<`/`Queue<`/`static ` 이 0회다 (단일 슬롯 캐시 — 구역 매니저 금지).
6. `CameraBoundsTrigger.cs` 에 `Start`/`Awake`/`Update`/`OnDrawGizmos`/`RequireComponent`/`SerializeField` 가 0회, `public` 시작 줄이 3줄이다.
7. `BossZoomTrigger.cs`, `PlayerController.cs`, `InputHandler.cs`, 모든 `.unity` 씬 파일이 0줄 변경이다.
8. `.meta` 파일을 직접 생성하거나 커밋하지 않았다 (Unity 가 자동 생성하도록 둔다).
9. 이 작업으로 커밋된 파일은 `CameraController.cs`, `CameraBoundsTrigger.cs`, `Check.md` 3개뿐이다.
</verification>

<success_criteria>
- 런타임에 `CameraController.Instance.SetXBounds(min, max)` 로 카메라 X 경계를 교체할 수 있다.
- `CameraBoundsTrigger` 를 `BoxCollider2D`(Is Trigger) 오브젝트에 붙이고 `zoneMinX`/`zoneMaxX` 만 채우면 구역별 하드 클램프가 동작한다 — 코드 수정 없이 값만 바꿔 무한히 복제 가능(이스터에그 확장 구역 포함).
- **구역 진입 시 직전 경계가 캐시되고 이탈 시 그 값으로 복원된다 (MX-05).** 구역 안에서 스폰해 진입 이벤트가 없던 경우에는 아무것도 복원하지 않는다.
- 캐시는 트리거 인스턴스당 1슬롯이며, 겹친 구역에서 발생 가능한 stale 복원은 코드가 아니라 `Check.md` 의 "알려진 한계" + 레벨 디자인 가이드로 다룬다.
- 클램프 계산은 기존 `ApplyXClamp()` 하나뿐이며, 이 작업이 추가한 실행 로직은 대입 2줄 + 트리거 캐시/적용/복원이 전부다.
- Phase 9 줌 / Phase 10 데드존·오프셋·피킹 로직과 `BossZoomTrigger` 가 손상되지 않았다.
- 씬 배치는 사용자가 수행할 수 있도록 `Check.md` 에 8단계 가이드와 13개 검증 항목으로 문서화되었다.
</success_criteria>

<output>
완료 후 `.planning/quick/260805-m41-cameracontroller-setxbounds-min-max-boss/260805-m41-SUMMARY.md` 를 생성한다.
SUMMARY 에는 (a) 정적 검사 11항목 실제 결과, (b) MX-05(진입 캐시 / 이탈 복원) 동작과 단일 슬롯 캐시의 알려진 한계(겹친 구역 stale 복원 — 레벨 디자인으로 회피, 코드 미방어), (c) Play 모드 미검증 상태 및 사용자 수동 배치가 선행되어야 한다는 사실, (d) `CameraBoundsTrigger.cs.meta` 는 Unity 가 자동 생성하며 `BossZoomTrigger.cs.meta` 와 동일하게 untracked 로 남는다는 기존 리포 상태 메모, (e) `.planning/STATE.md` 의 "Quick Tasks Completed" 표에 추가할 항목을 기록한다.
</output>
