---
phase: quick-260827-h5y
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/SaveSystem/Script/SettingsData.cs
  - Assets/SaveSystem/Script/SaveLoadManager.cs
  - Assets/Script/AudioManager.cs
  - Assets/Player/Script/Menu/GameSettingsPanel.cs
  - Assets/Player/Script/Menu/GraphicsSettingsPanel.cs
  - Assets/Player/Script/Menu/SoundSettingsPanel.cs
  - Assets/Player/Script/InputHandler.cs
  - Assets/Player/Script/Menu/PauseMenu.cs
autonomous: false
requirements: [SET-01, SET-02, SET-03, SET-04, SET-05, SET-06, SET-07]

must_haves:
  truths:
    - "설정 패널에서 값을 바꾸면 화면/오디오에 즉시 반영된다 (라이브 프리뷰 유지)"
    - "설정 패널에서 값을 바꿔도 디스크에는 아무 파일도 쓰이지 않는다"
    - "[설정 저장] 버튼을 누르면 Application.persistentDataPath/setting.json 이 생성/갱신된다"
    - "게임을 껐다 켜면 setting.json 의 값(언어/화면모드/볼륨/토글/키 리바인딩)이 복원된다"
    - "setting.json 이 없으면 기존 하드코딩 기본값(Language=1, ScreenShake=on, TutorialHint=on, ScreenMode=0, BGM=1, SFX=1, 바인딩 없음)이 쓰인다"
    - "사운드 볼륨이 영구 저장된다 (기존 버그 해소)"
    - "Assets 전체에서 PlayerPrefs 사용이 0건이다"
    - "save.json / SaveData 게임 진행도 저장·로드 흐름은 동작이 변하지 않는다"
  artifacts:
    - path: "Assets/SaveSystem/Script/SettingsData.cs"
      provides: "설정 단일 소스 POCO (SettingsData)"
      contains: "class SettingsData"
    - path: "Assets/SaveSystem/Script/SaveLoadManager.cs"
      provides: "SaveSettings()/LoadSettings()/Settings/CurrentSettings/SettingsPath"
      contains: "public void SaveSettings()"
    - path: "Assets/Player/Script/Menu/PauseMenu.cs"
      provides: "[설정 저장] 버튼 OnClick 진입점"
      contains: "OnSaveSettingsBtnClick"
  key_links:
    - from: "Assets/Player/Script/Menu/GameSettingsPanel.cs"
      to: "SaveLoadManager.CurrentSettings"
      via: "메모리 필드 대입 (파일 I/O 없음)"
      pattern: "SaveLoadManager\\.CurrentSettings"
    - from: "Assets/Player/Script/Menu/GraphicsSettingsPanel.cs"
      to: "SaveLoadManager.CurrentSettings"
      via: "메모리 필드 대입 + 시작 시 복원"
      pattern: "SaveLoadManager\\.CurrentSettings"
    - from: "Assets/Player/Script/Menu/SoundSettingsPanel.cs"
      to: "SaveLoadManager.CurrentSettings"
      via: "메모리 필드 대입 (AudioManager 는 라이브 적용만)"
      pattern: "SaveLoadManager\\.CurrentSettings"
    - from: "Assets/Player/Script/InputHandler.cs"
      to: "SaveLoadManager.CurrentSettings.InputBindingsJson"
      via: "SaveBindingOverrides/LoadBindingOverrides"
      pattern: "CurrentSettings\\.InputBindingsJson"
    - from: "Assets/Player/Script/Menu/PauseMenu.cs"
      to: "SaveLoadManager.Instance.SaveSettings()"
      via: "저장 버튼 OnClick"
      pattern: "SaveSettings\\(\\)"
---

<objective>
설정 저장 방식을 "PlayerPrefs 즉시 저장"에서 "메모리(SettingsData) + [설정 저장] 버튼 → setting.json 단일 소스"로 전환한다.

Purpose:
- 값이 바뀔 때마다 디스크에 쓰던 구조를 없애고, 사용자가 저장을 명시적으로 결정하게 한다.
- 사운드 볼륨이 영구 저장되지 않던 기존 버그를 같은 경로로 해소한다.
- 진행도(save.json)와 설정(setting.json)을 같은 매니저·같은 직렬화 방식으로 통일한다.

Output:
- `SettingsData` POCO 신규
- `SaveLoadManager` 에 `SaveSettings()`/`LoadSettings()`/`Settings`/`CurrentSettings`/`SettingsPath` 추가 (기존 save.json 흐름 무수정)
- 3개 설정 패널 + `AudioManager` + `InputHandler` 를 메모리 기반으로 전환
- `PauseMenu` 에 저장 버튼 진입점 추가

Requirement IDs (이 quick task 로컬 정의):
- SET-01: SettingsData 단일 소스 POCO + 기존 하드코딩 기본값 보존
- SET-02: SaveLoadManager 에 setting.json 전용 저장/로드 API 추가 (save.json 흐름 불변)
- SET-03: GameSettingsPanel PlayerPrefs 제거 → 라이브 반영 + 메모리 기록
- SET-04: GraphicsSettingsPanel PlayerPrefs 제거 → 라이브 반영 + 메모리 기록 + 시작 시 복원
- SET-05: SoundSettingsPanel + AudioManager PlayerPrefs 제거 → 볼륨 영구 저장 버그 해소
- SET-06: InputHandler 키 리바인딩 결과를 PlayerPrefs 대신 SettingsData 로 이관
- SET-07: [설정 저장] 버튼 진입점 (PauseMenu) + 에디터 OnClick 연결
</objective>

<execution_context>
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/workflows/execute-plan.md
@D:/새 폴더/Projeect_A.E/Projeect_A.E/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@Assets/SaveSystem/Script/SaveLoadManager.cs
@Assets/SaveSystem/Script/SaveData.cs
@Assets/Script/AudioManager.cs
@Assets/Player/Script/Menu/GameSettingsPanel.cs
@Assets/Player/Script/Menu/GraphicsSettingsPanel.cs
@Assets/Player/Script/Menu/SoundSettingsPanel.cs
@Assets/Player/Script/Menu/ControlsSettingsPanel.cs
@Assets/Player/Script/Menu/PauseMenu.cs
@Assets/Player/Script/InputHandler.cs
</context>

<interfaces>
<!-- 실행자는 아래 계약을 그대로 사용한다. 코드베이스 탐색 불필요. -->

기존 SaveLoadManager.cs (Assets/SaveSystem/Script/SaveLoadManager.cs) — 절대 건드리면 안 되는 부분:
```csharp
public static SaveLoadManager Instance { get; private set; }
[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)] private static void Bootstrap()
public const string SaveFileName = "save.json";
public static string SavePath { get; }
public bool HasSaveFile()
public void NewGame()
public void Save()
public void SaveAtCheckpoint(string checkpointName)
public void SaveOnBossDefeated(string bossId)
public bool IsBossDefeated(string bossId)
public void LoadGame()
public SaveData Data { get; }
```

기존 private 멤버 (Task 1 에서 이름만 바뀜):
```csharp
private static readonly JsonSerializerSettings SaveSettings = new JsonSerializerSettings { ... };  // 56행
// 사용처: 91행 JsonConvert.SerializeObject(_data, SaveSettings)
//         152행 JsonConvert.DeserializeObject<SaveData>(json, SaveSettings)
```

Task 1 이 만들어낼 새 계약 (Task 2/3 는 이것만 보고 작성한다):
```csharp
// SettingsData.cs
public class SettingsData
{
    public int    SettingsVersion   = 1;
    public int    Language          = 1;      // 0 = Korean, 1 = English
    public bool   ScreenShake       = true;
    public bool   TutorialHint      = true;
    public int    ScreenMode        = 0;      // GraphicsSettingsPanel.ScreenModes index
    public float  BgmVolume         = 1f;
    public float  SfxVolume         = 1f;
    public string InputBindingsJson = "";
}

// SaveLoadManager.cs (추가분)
public const string SettingsFileName = "setting.json";
public static string SettingsPath { get; }                 // persistentDataPath/setting.json
public SettingsData Settings { get; }                      // 인스턴스 뷰
public static SettingsData CurrentSettings { get; }        // Instance 널이어도 안전한 정적 접근자
public bool HasSettingsFile();
public void SaveSettings();                                // 메모리 -> setting.json (유일한 디스크 쓰기)
public void LoadSettings();                                // setting.json -> 메모리 (없으면 기본값 유지)
```

AudioManager (Assets/Script/AudioManager.cs):
```csharp
public static AudioManager Instance { get; private set; }
public float BgmVolume { get; private set; }
public float SfxVolume { get; private set; }
public void SetBGMVolume(float value);   // 현재 PlayerPrefs 도 씀 -> 제거 대상
public void SetSFXVolume(float value);   // 현재 PlayerPrefs 도 씀 -> 제거 대상
private void LoadVolumes();              // 현재 PlayerPrefs 읽음 -> 제거 대상
```

InputHandler (Assets/Player/Script/InputHandler.cs):
```csharp
public static InputHandler Instance { get; private set; }
public InputActionAsset inputActions;
private const string SAVE_KEY = "InputBindings";   // 54~55행, 삭제 대상
public void SaveBindingOverrides();                // 145~155행, 본문 교체 대상
public void LoadBindingOverrides();                // 158~166행, 본문 교체 대상 (Awake 104행에서 호출됨)
public InputAction GetAction(string actionName);
```

ControlsSettingsPanel (Assets/Player/Script/Menu/ControlsSettingsPanel.cs):
```csharp
// 이 파일은 이 플랜에서 수정하지 않는다.
// 81행 FinishRebind()  -> InputHandler.Instance?.SaveBindingOverrides();
// 102행 ResetAllBindings() -> InputHandler.Instance?.SaveBindingOverrides();
// 두 호출부는 시그니처가 그대로이므로 InputHandler 본문만 바꾸면 자동으로 새 흐름을 탄다.
```
</interfaces>

<encoding_note>
이 프로젝트는 CP949/UTF-8 인코딩 훼손 이력이 있다 (Phase 11 Plan 3, STATE.md 참고).

현재 상태 (플랜 작성 시점 실측):
| 파일 | UTF-8 유효 | 비-ASCII 바이트 | U+FFFD(훼손된 글자) |
|------|-----------|----------------|---------------------|
| SaveLoadManager.cs | O | 0 | 0 |
| SaveData.cs | O | 0 | 0 |
| AudioManager.cs | O | 정상 한글 | 0 |
| GameSettingsPanel.cs | O | 정상 한글 | 0 |
| GraphicsSettingsPanel.cs | O | 정상 한글 | 0 |
| SoundSettingsPanel.cs | O | 정상 한글 | 0 |
| PauseMenu.cs | O | 정상 한글 | 0 |
| ControlsSettingsPanel.cs | O | 정상 한글 | 0 |
| **InputHandler.cs** | O | 2540 | **716 (이미 훼손됨)** |

규칙:
1. `InputHandler.cs` 는 이미 주석 대부분이 U+FFFD 로 깨져 있다. **파일 전체를 다시 쓰지 말고** 지정된 3개 구간만 targeted edit 한다. 깨진 주석 줄을 "복원"하려고 시도하지 말 것 — 원문은 소실됐다.
2. `InputHandler.cs` 에 **추가하는 모든 줄은 ASCII 전용**으로 작성한다 (영문 주석). 한글 주석 금지.
3. 나머지 파일은 정상 UTF-8 이므로 한글 주석을 그대로 써도 되지만, 기존 주석을 이유 없이 재작성하지 않는다.
4. 새로 만드는 `SettingsData.cs` 는 `SaveData.cs` 와 동일하게 **ASCII 전용**으로 작성한다.
5. 모든 파일은 BOM 없는 UTF-8 로 저장한다.
</encoding_note>

<verification_gate_note>
STATE.md 에 기록된 반복 사고: 플랜에 박아둔 `git diff` 라인 수 게이트가 baseline 커밋 선택 오류로
항상 0 을 반환한 사례가 Phase 9/10/11 에 걸쳐 4번 있었다.

따라서 이 플랜의 diff 라인 수 게이트는 **직전 커밋(HEAD) 기준**으로만 실행한다 (`git diff HEAD -- <path>`).
숫자가 기대값과 다르면 곧바로 실패로 처리하지 말고, diff 를 눈으로 확인해서
"의도한 변경만 있는가"를 판단하고 SUMMARY 에 실제 숫자와 이유를 적는다.

또한 Phase 9/10/11 에서 "플랜 액션이 지정한 주석 문자열"과 "플랜 검증 게이트의 grep 카운트"가
서로 충돌한 사고가 3번 있었다. 이 플랜은 이를 피하기 위해:
- `PlayerPrefs` 전역 카운트 0 게이트가 있으므로, **새로 추가하는 어떤 주석에도 `PlayerPrefs` 라는
  리터럴 문자열을 쓰지 않는다.** (예: "PlayerPrefs 대신 setting.json 사용" 같은 주석 금지 →
  "설정은 setting.json 에만 보관" 처럼 쓴다.)
</verification_gate_note>

<tasks>

<task type="auto">
  <name>Task 1: SettingsData POCO 신설 + SaveLoadManager 에 setting.json API 추가</name>
  <files>Assets/SaveSystem/Script/SettingsData.cs (신규), Assets/SaveSystem/Script/SaveLoadManager.cs</files>
  <action>
**1-A. `Assets/SaveSystem/Script/SettingsData.cs` 신규 생성 (ASCII 전용, `.meta` 는 Unity 가 생성).**

`SaveData.cs` 와 동일한 스타일의 순수 POCO. 필드 기본값은 **현재 코드의 하드코딩 기본값과 정확히 일치**해야 한다 (SET-01):

```csharp
// Settings schema (POCO). Serialized by Newtonsoft.Json into setting.json,
// alongside but separate from save.json / SaveData (game progress).
// Field defaults MUST match the previous hard-coded fallbacks so that a
// missing setting.json behaves exactly like a fresh install did before.

public class SettingsData
{
    // Bump only when field meaning changes.
    public int SettingsVersion = 1;

    // Game tab. Language: 0 = Korean, 1 = English.
    public int  Language     = 1;
    public bool ScreenShake  = true;
    public bool TutorialHint = true;

    // Graphics tab. Index into GraphicsSettingsPanel.ScreenModes.
    public int ScreenMode = 0;

    // Sound tab. Linear 0..1 slider values.
    public float BgmVolume = 1f;
    public float SfxVolume = 1f;

    // Controls tab. Raw output of InputActionAsset.SaveBindingOverridesAsJson().
    // Empty string means "no rebinding applied".
    public string InputBindingsJson = "";
}
```

**1-B. `SaveLoadManager.cs` — private 필드 이름 변경 (CS0102 회피, 필수).**

기존 56행에 `private static readonly JsonSerializerSettings SaveSettings` 라는 **필드**가 이미 있다.
같은 클래스에 `public void SaveSettings()` **메서드**를 추가하면 CS0102 (duplicate member name) 컴파일 에러가 난다.
따라서 이 private 필드를 `JsonSettings` 로 이름만 바꾼다. **동작 변경 없음, 총 3줄만 수정:**
- 56행 선언: `private static readonly JsonSerializerSettings JsonSettings = new JsonSerializerSettings`
- 91행: `JsonConvert.SerializeObject(_data, JsonSettings)`
- 152행: `JsonConvert.DeserializeObject<SaveData>(json, JsonSettings)`

이 3줄 외에 `Save()`, `LoadGame()`, `SaveAtCheckpoint()`, `SaveOnBossDefeated()`, `NewGame()`,
`CapturePlayerStats()`, `EnsureCollections()`, `LoadSceneAndRestoreRoutine()`, `ApplyPlayerStatsFromSave()`
의 **로직은 한 글자도 바꾸지 않는다.**

**1-C. `SaveLoadManager.cs` — setting.json 전용 API 추가 (SET-02).**

`IsBossDefeated()` 아래, `CapturePlayerStats()` 위쪽에 새 region 을 추가한다:

```csharp
    // ---- Settings (setting.json) ------------------------------------------------
    // Separate file, separate model, separate API from save.json / SaveData.
    // Panels mutate CurrentSettings in memory during play; the only disk write is
    // SaveSettings(), triggered by the settings save button.

    public const string SettingsFileName = "setting.json";

    public static string SettingsPath
    {
        get { return Path.Combine(Application.persistentDataPath, SettingsFileName); }
    }

    // Used when Instance is not up yet (edit-mode inspectors, early static callers).
    // Keeps every caller free of null checks.
    private static readonly SettingsData _fallbackSettings = new SettingsData();

    private SettingsData _settings = new SettingsData();

    public SettingsData Settings { get { return _settings; } }

    public static SettingsData CurrentSettings
    {
        get { return Instance != null ? Instance._settings : _fallbackSettings; }
    }

    public bool HasSettingsFile()
    {
        return File.Exists(SettingsPath);
    }

    // The one and only settings disk write.
    public void SaveSettings()
    {
        string json = JsonConvert.SerializeObject(_settings, JsonSettings);
        File.WriteAllText(SettingsPath, json);
        Debug.Log("[SaveLoadManager] Settings saved to " + SettingsPath);
    }

    // Called once from Awake(). A missing or unreadable file leaves the defaults intact.
    public void LoadSettings()
    {
        if (!HasSettingsFile())
        {
            Debug.Log("[SaveLoadManager] No settings file - using defaults.");
            return;
        }

        SettingsData loaded = null;
        try
        {
            string json = File.ReadAllText(SettingsPath);
            loaded = JsonConvert.DeserializeObject<SettingsData>(json, JsonSettings);
        }
        catch (System.Exception e)
        {
            Debug.LogError("[SaveLoadManager] Failed to read settings file: " + e.Message);
            return;
        }

        if (loaded == null)
        {
            Debug.LogError("[SaveLoadManager] Settings file deserialized to null - keeping defaults.");
            return;
        }

        if (loaded.InputBindingsJson == null) loaded.InputBindingsJson = "";
        _settings = loaded;
    }
```

**1-D. `Awake()` 에 딱 한 줄 추가.**

`Instance == null` 분기의 `DontDestroyOnLoad(gameObject);` **바로 다음 줄**에:
```csharp
            LoadSettings();
```
`Bootstrap()` 이 `BeforeSceneLoad` 에서 `AddComponent` 하므로 `Awake()` 는 첫 씬의
어떤 `Awake()`/`Start()` 보다도 먼저 실행된다. 따라서 이 시점 이후 모든 패널·AudioManager·InputHandler 가
복원된 설정을 볼 수 있다. (Awake 의 나머지 줄은 수정 금지.)

**1-E. ContextMenu 검증 훅 추가 (Phase 11 D-04 패턴 재사용).**

파일 맨 아래 기존 `[ContextMenu("Phase11/...")]` 블록들 뒤에 추가:
```csharp
    [ContextMenu("Settings/1. Save Settings")]
    private void DebugSaveSettings()
    {
        SaveSettings();
    }

    [ContextMenu("Settings/2. Load Settings")]
    private void DebugLoadSettings()
    {
        LoadSettings();
    }

    [ContextMenu("Settings/3. Log Settings")]
    private void DebugLogSettings()
    {
        Debug.Log("[SaveLoadManager] settingsPath=" + SettingsPath +
                  " exists=" + HasSettingsFile() +
                  " lang=" + _settings.Language +
                  " shake=" + _settings.ScreenShake +
                  " hint=" + _settings.TutorialHint +
                  " screenMode=" + _settings.ScreenMode +
                  " bgm=" + _settings.BgmVolume +
                  " sfx=" + _settings.SfxVolume +
                  " bindingsLen=" + (_settings.InputBindingsJson != null ? _settings.InputBindingsJson.Length : 0));
    }
```
이 훅 덕분에 UI 버튼을 씬에 배치하기 전에도 저장/로드 검증이 가능하다.

`using` 추가는 필요 없다 (`System.IO`, `Newtonsoft.Json`, `UnityEngine` 모두 이미 있음).
  </action>
  <verify>
    <automated>
cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && \
test -f Assets/SaveSystem/Script/SettingsData.cs && \
python -c "import sys;b=open('Assets/SaveSystem/Script/SettingsData.cs','rb').read();assert all(x<128 for x in b),'SettingsData.cs must be ASCII-only';assert b'class SettingsData' in b" && \
python -c "import sys;b=open('Assets/SaveSystem/Script/SaveLoadManager.cs','rb').read();assert all(x<128 for x in b),'SaveLoadManager.cs must stay ASCII-only'" && \
[ "$(grep -c 'JsonSettings' Assets/SaveSystem/Script/SaveLoadManager.cs)" = "3" ] && \
[ "$(grep -c 'JsonSerializerSettings SaveSettings' Assets/SaveSystem/Script/SaveLoadManager.cs)" = "0" ] && \
grep -q 'public void SaveSettings()' Assets/SaveSystem/Script/SaveLoadManager.cs && \
grep -q 'public void LoadSettings()' Assets/SaveSystem/Script/SaveLoadManager.cs && \
grep -q 'public static SettingsData CurrentSettings' Assets/SaveSystem/Script/SaveLoadManager.cs && \
grep -q 'SettingsFileName = "setting.json"' Assets/SaveSystem/Script/SaveLoadManager.cs && \
[ "$(grep -c 'LoadSettings();' Assets/SaveSystem/Script/SaveLoadManager.cs)" = "2" ] && \
[ "$(git diff HEAD -- Assets/SaveSystem/Script/SaveLoadManager.cs | grep -c '^-[^-]')" = "3" ] && \
echo "TASK1 PASS"
    </automated>
  </verify>
  <done>
- `SettingsData.cs` 존재, ASCII 전용, 8개 필드 기본값이 기존 하드코딩 값과 일치
- `SaveLoadManager` 에 `SaveSettings()`/`LoadSettings()`/`Settings`/`CurrentSettings`/`SettingsPath`/`HasSettingsFile()` 존재
- private 필드가 `JsonSettings` 로 리네임되어 3곳에서만 등장, `SaveSettings` 라는 필드는 0건
- `git diff HEAD` 기준 `SaveLoadManager.cs` 삭제 라인 정확히 3줄 (전부 리네임)
- `Save()`/`LoadGame()`/`SaveAtCheckpoint()`/`SaveOnBossDefeated()`/`NewGame()` 로직 무변경
  </done>
</task>

<task type="auto">
  <name>Task 2: 3개 설정 패널 + AudioManager 를 메모리 기반으로 전환</name>
  <files>Assets/Player/Script/Menu/GameSettingsPanel.cs, Assets/Player/Script/Menu/GraphicsSettingsPanel.cs, Assets/Player/Script/Menu/SoundSettingsPanel.cs, Assets/Script/AudioManager.cs</files>
  <action>
공통 규칙: **읽기는 `SaveLoadManager.CurrentSettings` 에서, 쓰기도 `SaveLoadManager.CurrentSettings` 로만.**
어떤 파일에서도 파일 I/O 를 호출하지 않는다. 라이브 적용(화면/오디오 반영)은 전부 그대로 유지한다.
`<verification_gate_note>` 대로, 새로 쓰는 주석에 `PlayerPrefs` 리터럴을 넣지 않는다.

**2-A. `GameSettingsPanel.cs` (SET-03)**

- `OnEnable()`:
```csharp
        var s = SaveLoadManager.CurrentSettings;
        _langIndex = s.Language;
        RefreshLanguageText();

        if (screenShakeToggle  != null) screenShakeToggle.isOn  = s.ScreenShake;
        if (tutorialHintToggle != null) tutorialHintToggle.isOn = s.TutorialHint;
```
- `OnScreenShakeChanged(bool value)` 본문 → `SaveLoadManager.CurrentSettings.ScreenShake = value;`
- `OnTutorialHintChanged(bool value)` 본문 → `SaveLoadManager.CurrentSettings.TutorialHint = value;`
- `ApplyLanguage()` 본문 →
```csharp
        SaveLoadManager.CurrentSettings.Language = _langIndex;
        RefreshLanguageText();
```
- **`RestoreOnStartup()` 메서드와 그 위 주석, `[RuntimeInitializeOnLoadMethod]` 어트리뷰트를 통째로 삭제한다.**
  기본값 시딩만 하던 메서드였고, 이제 `SettingsData.Language = 1` 기본값이 그 역할을 한다.
- `OnLanguagePrev()`/`OnLanguageNext()`/`RefreshLanguageText()`/필드 선언은 **무수정**.

**2-B. `GraphicsSettingsPanel.cs` (SET-04)**

- `OnEnable()` 첫 줄 → `_screenModeIndex = SaveLoadManager.CurrentSettings.ScreenMode;`
- `Apply()` →
```csharp
        Screen.fullScreenMode = ScreenModes[_screenModeIndex];
        SaveLoadManager.CurrentSettings.ScreenMode = _screenModeIndex;
        RefreshText();
```
- `RestoreOnStartup()` 은 **유지하되** 내부만 교체 (시작 시 화면 모드 실제 적용이 필요하므로):
```csharp
        int saved = SaveLoadManager.CurrentSettings.ScreenMode;
        if (saved >= 0 && saved < ScreenModes.Length)
            Screen.fullScreenMode = ScreenModes[saved];
```
  `AfterSceneLoad` 는 `SaveLoadManager.Bootstrap()`(`BeforeSceneLoad`) 뒤에 돌므로 `Instance` 가 이미 있다.
- `ScreenModeNames`/`ScreenModes`/`OnScreenModePrev`/`OnScreenModeNext`/`RefreshText()` 는 **무수정**.

**2-C. `AudioManager.cs` (SET-05, 라이브 적용 전담으로 축소)**

- `LoadVolumes()` →
```csharp
    private void LoadVolumes()
    {
        var s = SaveLoadManager.CurrentSettings;
        BgmVolume = s.BgmVolume;
        SfxVolume = s.SfxVolume;
        if (bgmSource != null) bgmSource.volume = BgmVolume;
    }
```
- `SetBGMVolume(float value)` → 라이브 적용만 남긴다 (저장 줄 2개 제거):
```csharp
    public void SetBGMVolume(float value)
    {
        BgmVolume = value;
        if (bgmSource != null) bgmSource.volume = value;
    }
```
- `SetSFXVolume(float value)` → 동일하게 저장 줄 2개 제거, `SfxVolume = value;` 만 남긴다.
- 파일 상단 주석 3~5행 중 5행("SFX는 ... PlaySFX()를 사용하세요.")은 그대로 두고,
  **저장 관련 서술이 들어있는 문장이 있으면 "볼륨 영구 저장은 설정 패널 저장 흐름이 담당" 정도로 고친다**
  (금지 리터럴 주의).
- `PlaySFX()`/싱글톤 `Awake()` 는 **무수정**.

> 발견 사항(중요): 원래 작업 설명은 "SoundSettingsPanel 에 저장 코드가 아예 없다"고 했는데,
> 실제로는 저장이 `AudioManager.SetBGMVolume/SetSFXVolume` 안에 숨어 있었다.
> 다만 `AudioManager.LoadVolumes()` 는 `Awake()` 에서 **한 번만** 읽고, `SoundSettingsPanel.OnEnable()` 은
> 별도 경로로 읽고 있어서 두 소스가 갈렸다. 이번 전환으로 두 경로 모두 `CurrentSettings` 단일 소스가 된다.

**2-D. `SoundSettingsPanel.cs` (SET-05)**

- 파일 상단 6행 주석을 저장 경로 변경에 맞게 갱신 (금지 리터럴 주의):
  `// AudioManager 싱글톤으로 볼륨을 즉시 적용하고, 값은 설정 메모리에만 기록 (영구 저장은 [설정 저장] 버튼)`
- `OnEnable()` 첫 두 줄 →
```csharp
        var s = SaveLoadManager.CurrentSettings;
        float bgm = s.BgmVolume;
        float sfx = s.SfxVolume;
```
  (이하 슬라이더 리스너 등록/`SetValueWithoutNotify`/텍스트 갱신 로직은 **무수정**)
- `OnBGMChanged(float value)` →
```csharp
        AudioManager.Instance?.SetBGMVolume(value);
        SaveLoadManager.CurrentSettings.BgmVolume = value;
        UpdateBGMText(value);
```
- `OnSFXChanged(float value)` → 같은 형태로 `SetSFXVolume` + `SfxVolume = value;` + `UpdateSFXText(value);`
- `OnDisable()`/`UpdateBGMText()`/`UpdateSFXText()` 는 **무수정**.
  </action>
  <verify>
    <automated>
cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && \
[ "$(grep -c 'PlayerPrefs' Assets/Player/Script/Menu/GameSettingsPanel.cs Assets/Player/Script/Menu/GraphicsSettingsPanel.cs Assets/Player/Script/Menu/SoundSettingsPanel.cs Assets/Script/AudioManager.cs | grep -v ':0$' | wc -l)" = "0" ] && \
[ "$(grep -c 'SaveLoadManager.CurrentSettings' Assets/Player/Script/Menu/GameSettingsPanel.cs)" -ge 4 ] && \
[ "$(grep -c 'SaveLoadManager.CurrentSettings' Assets/Player/Script/Menu/GraphicsSettingsPanel.cs)" -ge 3 ] && \
[ "$(grep -c 'SaveLoadManager.CurrentSettings' Assets/Player/Script/Menu/SoundSettingsPanel.cs)" -ge 3 ] && \
grep -q 'SaveLoadManager.CurrentSettings' Assets/Script/AudioManager.cs && \
[ "$(grep -c 'RuntimeInitializeOnLoadMethod' Assets/Player/Script/Menu/GameSettingsPanel.cs)" = "0" ] && \
[ "$(grep -c 'RuntimeInitializeOnLoadMethod' Assets/Player/Script/Menu/GraphicsSettingsPanel.cs)" = "1" ] && \
grep -q 'Screen.fullScreenMode = ScreenModes\[_screenModeIndex\];' Assets/Player/Script/Menu/GraphicsSettingsPanel.cs && \
grep -q 'AudioManager.Instance?.SetBGMVolume(value);' Assets/Player/Script/Menu/SoundSettingsPanel.cs && \
grep -q 'AudioManager.Instance?.SetSFXVolume(value);' Assets/Player/Script/Menu/SoundSettingsPanel.cs && \
[ "$(grep -rc 'File\.\|Application.persistentDataPath' Assets/Player/Script/Menu/GameSettingsPanel.cs Assets/Player/Script/Menu/GraphicsSettingsPanel.cs Assets/Player/Script/Menu/SoundSettingsPanel.cs Assets/Script/AudioManager.cs | grep -v ':0$' | wc -l)" = "0" ] && \
python -c "
import io
for f in ['Assets/Player/Script/Menu/GameSettingsPanel.cs','Assets/Player/Script/Menu/GraphicsSettingsPanel.cs','Assets/Player/Script/Menu/SoundSettingsPanel.cs','Assets/Script/AudioManager.cs']:
    s=io.open(f,encoding='utf-8').read()
    assert s.count(chr(0xFFFD))==0, f+' got encoding corruption'
    assert not s.startswith('\ufeff'), f+' has BOM'
print('encoding OK')
" && \
echo "TASK2 PASS"
    </automated>
  </verify>
  <done>
- 4개 파일 모두 PlayerPrefs 참조 0건, 파일 I/O 0건
- 라이브 적용 경로(`Screen.fullScreenMode`, `AudioManager.SetBGMVolume/SetSFXVolume`, 텍스트/토글 갱신) 전부 유지
- 값 변경 시 `SaveLoadManager.CurrentSettings` 필드에만 기록
- `GameSettingsPanel.RestoreOnStartup()` 삭제, `GraphicsSettingsPanel.RestoreOnStartup()` 은 유지하되 설정 메모리에서 읽음
- 4개 파일 U+FFFD 0건, BOM 없음
  </done>
</task>

<task type="auto">
  <name>Task 3: InputHandler 키 리바인딩 저장 이관 + PauseMenu 저장 버튼 진입점</name>
  <files>Assets/Player/Script/InputHandler.cs, Assets/Player/Script/Menu/PauseMenu.cs</files>
  <action>
**3-A. `InputHandler.cs` (SET-06) — `<encoding_note>` 규칙 필수 준수.**

이 파일은 이미 716개의 U+FFFD 를 포함한다. **파일 전체 재작성 금지.** 아래 3개 구간만 targeted edit 하고,
**추가하는 모든 줄은 ASCII 전용**으로 쓴다.

구간 1 — 54~55행 (상수 + 그 위 깨진 주석) **삭제**:
```
    // (깨진 주석 줄)
    private const string SAVE_KEY = "InputBindings";
```
→ 두 줄 모두 제거. (빈 줄 정리는 최소한으로.)

구간 2 — 145~155행 `SaveBindingOverrides()` **본문 교체**:
```csharp
    public void SaveBindingOverrides()
    {
        if (inputActions == null) return;

        // Rebinding result is kept in memory only. It reaches disk when the
        // settings save button calls SaveLoadManager.Instance.SaveSettings().
        SaveLoadManager.CurrentSettings.InputBindingsJson = inputActions.SaveBindingOverridesAsJson();
    }
```
바로 위 144행의 깨진 주석은 그대로 두거나, 지우고 ASCII 영문 주석으로 대체해도 된다(둘 다 허용).

구간 3 — 158~166행 `LoadBindingOverrides()` **본문 교체**:
```csharp
    public void LoadBindingOverrides()
    {
        if (inputActions == null) return;

        string json = SaveLoadManager.CurrentSettings.InputBindingsJson;
        if (string.IsNullOrEmpty(json)) return;

        inputActions.LoadBindingOverridesFromJson(json);
    }
```
(기존 코드에 없던 `inputActions == null` 가드를 넣는다 — 기존 `LoadBindingOverrides` 는 Awake 의
early-return 경로 때문에 `inputActions` 가 null 인 채로 호출될 일이 없었지만, 이제 방어해도 무해하다.)

**나머지 전부 무수정**: 싱글톤 `Awake()`(104행 `LoadBindingOverrides();` 호출 포함), `OnEnable()`,
`OnDisable()`, `GetAction()`, 이벤트 선언, 액션 필드 선언, 깨진 주석 블록 전부.

`ControlsSettingsPanel.cs` 는 **건드리지 않는다** — `SaveBindingOverrides()` 시그니처가 유지되므로
81행/102행 호출부가 자동으로 새 흐름을 탄다.

**3-B. `PauseMenu.cs` (SET-07) — 저장 버튼 진입점 추가.**

`PauseMenu` 가 일시정지 패널의 최상위 컨트롤러이고 이미 `OnResumeBtnClick()`/`OnQuitBtnClick()` 같은
버튼 진입점을 갖고 있으므로, 새 컨트롤러 스크립트를 만들지 않고 여기에 붙인다.

`OnQuitBtnClick()` 아래에 추가:
```csharp
    // [설정 저장] 버튼 OnClick - 4개 설정 탭의 현재 메모리 값을 setting.json 으로 기록
    public void OnSaveSettingsBtnClick()
    {
        if (SaveLoadManager.Instance == null)
        {
            Debug.LogError("[PauseMenu] SaveLoadManager.Instance is null - settings not saved.");
            return;
        }
        SaveLoadManager.Instance.SaveSettings();
        Debug.Log("[PauseMenu] 설정 저장 완료");
    }
```
다른 메서드(`Start`, `OnDestroy`, `OnPauseInput`, `Open`, `Close`)는 **무수정**.

> 에디터 수동 작업 (Task 4 체크포인트에서 처리): 일시정지 패널에 [설정 저장] 버튼 UI 를 만들고
> OnClick 을 `PauseMenu.OnSaveSettingsBtnClick` 에 연결해야 한다. 이 연결은 씬/프리팹 데이터이므로
> 정적 코드 검사로 검증 불가하다. 그동안은 Task 1 에서 넣은 `[ContextMenu("Settings/1. Save Settings")]`
> 로 저장 동작 자체를 검증할 수 있다 (Phase 11 D-04 와 동일한 우회).
  </action>
  <verify>
    <automated>
cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E" && \
[ "$(grep -rc 'PlayerPrefs' Assets --include=*.cs | grep -v ':0$' | wc -l)" = "0" ] && \
[ "$(grep -c 'SAVE_KEY' Assets/Player/Script/InputHandler.cs)" = "0" ] && \
[ "$(grep -c 'CurrentSettings.InputBindingsJson' Assets/Player/Script/InputHandler.cs)" = "2" ] && \
grep -q 'SaveBindingOverridesAsJson()' Assets/Player/Script/InputHandler.cs && \
grep -q 'LoadBindingOverridesFromJson(json)' Assets/Player/Script/InputHandler.cs && \
[ "$(git diff HEAD -- Assets/Player/Script/Menu/ControlsSettingsPanel.cs | wc -l)" = "0" ] && \
grep -q 'public void OnSaveSettingsBtnClick()' Assets/Player/Script/Menu/PauseMenu.cs && \
grep -q 'SaveLoadManager.Instance.SaveSettings();' Assets/Player/Script/Menu/PauseMenu.cs && \
python -c "
import io,subprocess
s=io.open('Assets/Player/Script/InputHandler.cs',encoding='utf-8').read()
n=s.count(chr(0xFFFD))
assert n<=716, 'InputHandler U+FFFD grew from 716 to %d - new corruption introduced' % n
print('InputHandler U+FFFD =', n, '(baseline 716)')
d=subprocess.run(['git','diff','HEAD','--','Assets/Player/Script/InputHandler.cs'],capture_output=True).stdout.decode('utf-8','replace')
added=[l for l in d.split('\n') if l.startswith('+') and not l.startswith('+++')]
bad=[l for l in added if any(ord(c)>127 for c in l)]
assert not bad, 'added lines must be ASCII-only: %r' % bad[:3]
print('added lines ASCII-only OK (%d added)' % len(added))
" && \
python -c "
import io
s=io.open('Assets/Player/Script/Menu/PauseMenu.cs',encoding='utf-8').read()
assert s.count(chr(0xFFFD))==0, 'PauseMenu.cs got encoding corruption'
" && \
echo "TASK3 PASS"
    </automated>
  </verify>
  <done>
- `Assets` 전체 `PlayerPrefs` 참조 0건 (전역 게이트)
- `InputHandler.SAVE_KEY` 제거, 리바인딩 JSON 이 `CurrentSettings.InputBindingsJson` 로 왕복
- `InputHandler.cs` U+FFFD 개수가 716 이하 (새 훼손 없음), 추가된 줄 전부 ASCII
- `ControlsSettingsPanel.cs` diff 0줄 (무수정)
- `PauseMenu.OnSaveSettingsBtnClick()` 존재하고 `SaveLoadManager.Instance.SaveSettings()` 호출
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Unity 컴파일 확인 + [설정 저장] 버튼 OnClick 연결 + Play 모드 실측</name>
  <files>(코드 변경 없음 - 씬/프리팹 에디터 작업 + 런타임 검증)</files>
  <action>Claude 는 여기서 코드를 수정하지 않는다. 사용자가 Unity 에디터에서 아래 how-to-verify 절차를 수행하고, 실패 항목이 나오면 그 로그를 근거로 후속 수정을 진행한다.</action>
  <what-built>
설정 저장 시스템을 PlayerPrefs 즉시 저장에서 setting.json 명시적 저장으로 전환 완료:
- `SettingsData` POCO 신규 + `SaveLoadManager.SaveSettings()/LoadSettings()` (save.json 흐름 무수정)
- GameSettingsPanel / GraphicsSettingsPanel / SoundSettingsPanel / AudioManager 메모리 기반 전환
- InputHandler 리바인딩 결과를 SettingsData 로 이관 (ControlsSettingsPanel 무수정)
- PauseMenu 에 `OnSaveSettingsBtnClick()` 진입점 + SaveLoadManager 에 ContextMenu 검증 훅
  </what-built>
  <how-to-verify>
**A. 컴파일 (이 실행 환경에서는 불가 — 사용자가 Unity 에디터에서 확인)**
1. Unity 에디터로 전환 후 스크립트 리컴파일 대기. Console 에 에러 0건이어야 한다.
   - 특히 CS0102 (SaveSettings 중복 이름)가 안 나는지 확인 — Task 1-B 리네임이 이걸 막는다.
2. `Assets/Player/Script/InputHandler.cs` 를 에디터에서 열어 한글 주석이 **더** 깨지지 않았는지 확인
   (원래부터 깨져 있던 줄은 그대로여야 정상).

**B. 저장 버튼 UI 연결 (필수 수동 작업, 정적 검사 불가)**
3. 일시정지 패널(PauseMenu 가 붙은 오브젝트 하위)에 [설정 저장] 버튼 UI 를 만든다.
4. 버튼 OnClick → PauseMenu 오브젝트 드래그 → `PauseMenu.OnSaveSettingsBtnClick` 선택.

**C. Play 모드 실측**
5. Play 진입 → ESC → 각 탭에서 값을 바꾼다:
   - 게임 탭: 언어 ◀▶ 전환 시 텍스트가 즉시 바뀌는가? 토글 2개 즉시 반응하는가?
   - 그래픽 탭: 화면 모드 ◀▶ 전환 시 실제 창 모드가 즉시 바뀌는가?
   - 사운드 탭: 슬라이더를 움직이면 BGM 볼륨이 즉시 바뀌고 % 텍스트가 갱신되는가?
   - 컨트롤 탭: 키를 리바인딩하면 라벨이 즉시 바뀌고 실제 조작에 반영되는가?
6. **이 시점에 아직 저장하지 말고** Play 를 중지 → 다시 Play → ESC. 값이 전부 **원래대로 되돌아와야** 한다
   (= 디스크에 안 썼다는 증거).
7. 다시 값들을 바꾸고 [설정 저장] 버튼 클릭. Console 에 `[SaveLoadManager] Settings saved to ...` 로그 확인.
   - 버튼 UI 를 아직 안 만들었다면: Hierarchy 에서 런타임 `SaveLoadManager` 오브젝트 선택 →
     컴포넌트 기어 메뉴 → `Settings/1. Save Settings` 실행으로 대체 가능.
8. 로그에 찍힌 경로(`%USERPROFILE%\AppData\LocalLow\<Company>\<Product>\setting.json`)를 열어
   Language/ScreenShake/TutorialHint/ScreenMode/BgmVolume/SfxVolume/InputBindingsJson 이 들어있는지 확인.
   같은 폴더의 `save.json` 은 그대로 남아 있어야 한다.
9. Play 중지 → 다시 Play → ESC. **7번에서 저장한 값들이 전부 복원되어 있어야 한다** (볼륨/화면모드/키 포함).
10. 회귀 확인: 체크포인트에서 S키 저장 → `save.json` 갱신 여부, 이어하기(LoadGame) 정상 동작 확인
    (Phase 11 기능이 깨지지 않았는지).

**D. 기본값 폴백**
11. `setting.json` 을 삭제하고 Play → 언어=English, 토글 2개 ON, 화면모드=Fullscreen, 볼륨 100%,
    키 바인딩 기본값으로 뜨는지 확인. Console 에 `[SaveLoadManager] No settings file - using defaults.` 로그.
  </how-to-verify>
  <resume-signal>"approved" 를 입력하거나, Console 에러 / 동작 이상을 그대로 붙여넣어 주세요.</resume-signal>
</task>

</tasks>

<verification>
정적 검사 (Task 3 완료 후 일괄 실행):

```bash
cd "C:/Users/MSI/Projeect_A.E/Projeect_A.E"

# 1. PlayerPrefs 전멸
grep -rn "PlayerPrefs" Assets --include=*.cs        # 기대: 출력 없음

# 2. setting.json 경로가 save.json 과 같은 폴더, 파일명은 분리
grep -n "SaveFileName\|SettingsFileName" Assets/SaveSystem/Script/SaveLoadManager.cs

# 3. 설정 디스크 쓰기는 SaveSettings() 한 곳뿐
grep -rn "File.WriteAllText" Assets --include=*.cs  # 기대: SaveLoadManager.cs 2줄 (Save, SaveSettings)

# 4. 패널은 파일 I/O 를 전혀 모른다
grep -rn "File\.\|persistentDataPath" Assets/Player/Script/Menu/ Assets/Script/AudioManager.cs
                                                     # 기대: 출력 없음

# 5. save.json 흐름 무수정 (직전 커밋 기준, 리네임 3줄만)
git diff HEAD -- Assets/SaveSystem/Script/SaveLoadManager.cs | grep '^-[^-]'   # 기대: 3줄, 전부 SaveSettings->JsonSettings

# 6. ControlsSettingsPanel 무수정
git diff HEAD --stat -- Assets/Player/Script/Menu/ControlsSettingsPanel.cs     # 기대: 출력 없음

# 7. 인코딩 무결성
python -c "
import io
files=['Assets/SaveSystem/Script/SettingsData.cs','Assets/SaveSystem/Script/SaveLoadManager.cs',
       'Assets/Script/AudioManager.cs','Assets/Player/Script/Menu/GameSettingsPanel.cs',
       'Assets/Player/Script/Menu/GraphicsSettingsPanel.cs','Assets/Player/Script/Menu/SoundSettingsPanel.cs',
       'Assets/Player/Script/Menu/PauseMenu.cs','Assets/Player/Script/Menu/ControlsSettingsPanel.cs']
for f in files:
    s=io.open(f,encoding='utf-8').read()
    assert s.count(chr(0xFFFD))==0, f
    assert not s.startswith('\ufeff'), f+' BOM'
s=io.open('Assets/Player/Script/InputHandler.cs',encoding='utf-8').read()
assert s.count(chr(0xFFFD))<=716
print('ALL ENCODING OK')
"
```

런타임 검증은 Task 4 체크포인트에서 사용자가 Unity Play 모드로 수행한다
(이 실행 환경에는 Unity 컴파일러/런타임이 없다 — Phase 11 과 동일한 제약).
</verification>

<success_criteria>
- [ ] `Assets` 전체에서 `PlayerPrefs` 참조 0건
- [ ] `SettingsData.cs` 신규 생성, ASCII 전용, 기본값이 기존 하드코딩 값과 1:1 일치
- [ ] `SaveLoadManager` 에 `SaveSettings()`/`LoadSettings()`/`Settings`/`CurrentSettings`/`SettingsPath`/`HasSettingsFile()` 추가
- [ ] `SaveLoadManager` 의 save.json 흐름 diff 가 private 필드 리네임 3줄 + Awake 1줄 추가 + 신규 region/ContextMenu 추가분뿐
- [ ] 설정 값 변경 시 디스크 쓰기 0회, [설정 저장] 시에만 setting.json 기록
- [ ] 4개 탭 값 + 키 리바인딩이 재실행 후 복원됨 (Play 모드 실측)
- [ ] setting.json 부재 시 기존 기본값으로 동작
- [ ] 사운드 볼륨 영구 저장 버그 해소
- [ ] `ControlsSettingsPanel.cs` diff 0줄
- [ ] `InputHandler.cs` U+FFFD 개수 716 이하 + 추가 줄 전부 ASCII
- [ ] Unity Console 컴파일 에러 0건 (특히 CS0102 없음)
</success_criteria>

<output>
완료 후 `.planning/quick/260827-h5y-playerprefs-setting-json/260827-h5y-SUMMARY.md` 작성.

SUMMARY 에 반드시 기록할 항목:
- `SaveSettings` private 필드 → `JsonSettings` 리네임이 CS0102 회피를 위해 **필수**였다는 사실
- 볼륨 저장이 실제로는 `SoundSettingsPanel` 이 아니라 `AudioManager` 안에 있었고, `AudioManager.LoadVolumes()`(Awake 1회)와
  `SoundSettingsPanel.OnEnable()` 이 서로 다른 경로로 읽고 있었다는 발견 → 이제 `CurrentSettings` 단일 소스로 통합
- `git diff HEAD` 기준 각 파일 실제 변경 줄 수 (기대값과 다르면 그 이유)
- `InputHandler.cs` 의 U+FFFD 716 baseline 대비 최종 개수
- [설정 저장] 버튼 OnClick 에디터 연결 완료 여부 (미완이면 Active TODO 로 STATE.md 에 남길 것)
</output>

