---
phase: quick/260804-q6h-y-cameracontroller-cs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Assets/Camera/Script/CameraController.cs
  - Assets/Camera/Check.md
autonomous: true
requirements: [DY-01, DY-02, DY-03, DY-04]
must_haves:
  truths:
    - "일반 스테이지에서 플레이어가 데드존 박스 세로 범위 안에 있는 동안 카메라 Y 가 전혀 움직이지 않는다"
    - "플레이어가 박스 상/하 경계를 넘으면 카메라 Y 가 넘어간 만큼 즉시(하드컷) 따라붙는다"
    - "Scene 뷰의 노란 박스 세로 범위 밖에 플레이어가 서 있는 장면이 더 이상 발생하지 않는다"
    - "Y 데드존은 동적 오프셋(_currentBoxOffsetX)/피킹(_currentPeekY)과 상호작용하지 않고, 공중/지상 분기도 없다"
    - "피킹 Y 는 Y 데드존 결과 위에 그대로 얹혀 동작한다"
    - "보스 구역에서는 Y 데드존이 완전히 비활성이고 Phase 9 레거시 Lerp 만 동작한다"
    - "CameraController.cs 의 기존 비-ASCII(깨진 한글) 주석 5줄이 한 줄도 변경되지 않는다"
  artifacts:
    - path: "Assets/Camera/Script/CameraController.cs"
      provides: "Y축 하드컷 데드존 (_deadzoneCenterY + UpdateDeadzoneCenterY) 및 Gizmo Y 중심 반영"
      contains: "private void UpdateDeadzoneCenterY()"
    - path: "Assets/Camera/Check.md"
      provides: "Y축 하드컷 데드존 Play 모드 검증 체크리스트 (가정 A1 폐기 반영)"
      contains: "5) Y축 하드컷 데드존"
  key_links:
    - from: "ApplyNormalStageCamera"
      to: "transform.position.y"
      via: "_deadzoneCenterY 하드컷 + 피킹 가산 (Lerp 없음)"
      pattern: "p\\.y = _deadzoneCenterY \\+ _currentPeekY;"
    - from: "UpdateDeadzoneCenterY"
      to: "deadzoneHeight"
      via: "박스 반높이 게이트 (Gizmo 전용 → 실동작 승격)"
      pattern: "float halfH = deadzoneHeight \\* 0\\.5f;"
    - from: "LateUpdate 보스 분기"
      to: "ResetNormalStageState"
      via: "_deadzoneCenterY 시드 (보스 구역 우회 유지)"
      pattern: "_deadzoneCenterY = transform\\.position\\.y;"
---

<objective>
`Assets/Camera/Script/CameraController.cs` 의 Y축을 **X축과 동일 계열의 하드컷 데드존**으로 교체한다.

Purpose: Phase 10 의 잠금 가정 A1("Y축 데드존 없음, `deadzoneHeight` 는 Gizmo 표시 전용")을 사용자 Play 모드 실측 결과에 따라 **폐기**한다. 현재 카메라 Y 는 `_followBaseY` 의 `smoothing` Lerp 로만 움직여서, 노란 데드존 박스 세로 범위 밖에 플레이어가 서 있는 상황이 발생한다 (2026-08-04 사용자 보고, `Assets/Camera/Check.md` "Play 모드 실측 결과").

Output: `CameraController.cs` 1개 + `Check.md` 1개. 신규 파일 없음, 씬 편집 없음.

**핵심 교체**: `_followBaseY`(Lerp) → `_deadzoneCenterY`(하드컷). 필드가 완전히 대체되므로 `_followBaseY` 는 이 작업 이후 파일에 0회 등장해야 한다 (CLAUDE.md §3 — 내가 만든 고아만 제거).
</objective>

<context>
@.planning/STATE.md
@Assets/Camera/Script/CameraController.cs
@Assets/Camera/Check.md
@.planning/phases/10-3-base-deadzone-dynamic-asymmetrical-deadzone-input-based-peeking-phase-9-cameracontroller/10-01-PLAN.md
</context>

<locked_decisions>
사용자가 확정한 4개 결정 (`.planning/STATE.md` "Phase 10 gap" 기록). **실행 중 재논의 금지.**

- **DY-01 — 하드컷 계열**: Y 데드존은 X축 `UpdateDeadzoneCenter()` 와 같은 하드컷이다. `Mathf.Lerp` / `Mathf.SmoothDamp` 금지. `deadzoneHeight` 를 Gizmo 전용이 아닌 실제 게이트 값으로 승격한다.
- **DY-02 — 완전 독립**: Y 데드존은 동적 오프셋(`_currentBoxOffsetX`, `_deadzonePushSign`, `_lastPushSign`)과 피킹(`_currentPeekY`, `_peekTimer`)에 아무 영향을 주지도 받지도 않는다. **공중/점프 상태 분기 금지** — `IsGrounded()` 등을 Y 데드존 경로에서 호출하지 않는다.
- **DY-03 — 보스 구역 우회**: `_isBossZone` 일 때는 X축과 똑같이 완전 비활성. `LateUpdate` 의 `if (_isBossZone) ResetNormalStageState(); else ApplyNormalStageCamera();` 분기 구조를 바꾸지 않는다.
- **DY-04 — 피킹은 위에 얹는다**: `p.y = _deadzoneCenterY + _currentPeekY;`. 즉 Y 데드존 중심이 기존 `_followBaseY` 자리를 그대로 물려받는 새 Y 베이스라인이 된다.
</locked_decisions>

<critical_constraints>
## 1. 파일 인코딩 — 최우선 주의사항

`CameraController.cs` 는 **CRLF 개행**이며, 5줄(5, 8, 9, 304, 305행)에 이미 깨진 한글 주석 바이트가 들어 있다. 이것은 이 파일의 기존 상태이며 **이번 작업으로 고치지 않는다.**

- **`Write` 도구로 파일 전체를 다시 쓰지 말 것.** 반드시 `Edit` 도구로 아래 지정된 앵커만 교체한다 (전체 재작성은 CRLF/깨진 바이트를 동시에 망가뜨린다).
- **`old_string` / `new_string` 에 깨진 한글 주석 라인(5, 8, 9, 304, 305행)을 절대 포함하지 말 것.** 아래 앵커는 전부 ASCII 전용이다.
- **새로 추가하는 주석은 ASCII 영문만 사용한다 (한글 금지).** 이 플랜의 코드 블록 영문 주석을 그대로 쓴다.
- 인코딩 게이트: `LC_ALL=C grep -c '[^[:print:][:space:]]' Assets/Camera/Script/CameraController.cs` == **5** (작업 전후 동일)
- 삭제 라인 순수성 게이트: `git diff HEAD -- <file> | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]'` == **0**

## 2. 삭제 라인 개수는 "정확한 수"로 게이트하지 않는다

`.planning/STATE.md` 에 기록된 Phase 9/10 의 반복 실패 패턴: `git diff <오래된-baseline>` 기준 삭제 라인 정확 개수 게이트는 diff 알고리즘 특성 때문에 항상 어긋났다(10-01/10-02/10-03 전부). 이 플랜은 그 교훈을 적용해 **개수가 아니라 성질을 게이트한다**:
- baseline 은 `ef6f164` 가 아니라 **`HEAD`** 를 쓴다 (이 파일의 마지막 커밋 = `07f9db3`, 작업 트리 깨끗함).
- 하드 게이트는 "삭제 라인에 비-ASCII 0" + "`_followBaseY` 잔존 0" + "레거시 Lerp 라인 미삭제" 세 가지다.
- 삭제 라인 수 **약 10줄**은 참고값(informational)일 뿐 PASS/FAIL 기준이 아니다.

## 3. 레거시 Lerp 라인 보존 (D-15 / Phase 9 회귀 방지)

`LateUpdate` 의 아래 2줄은 **바이트 단위로 그대로 둔다.** 보스 구역 경로가 전적으로 이 라인에 의존한다.
```
        Vector3 targetCamPos = target.position + offset;
        transform.position = Vector3.Lerp(transform.position, targetCamPos, smoothing * Time.deltaTime);
```

## 4. Y 재앵커(re-anchor)를 추가하지 말 것 — 의도된 비대칭

X축은 `LateUpdate` 끝에서 `if (!_isBossZone) _deadzoneCenterX = transform.position.x + _currentBoxOffsetX;` 로 재동기화한다. **이건 `ApplyXClamp()` 가 X 를 잘라내기 때문에 필요한 보정이다.**

Y 에는 클램프가 없다(`minY`/`maxY` 없음, D-09). `ApplyXClamp` 도 줌 Lerp 도 `position.y` 를 건드리지 않으므로, `LateUpdate` 끝의 `transform.position.y` 는 `ApplyNormalStageCamera` 가 쓴 `_deadzoneCenterY + _currentPeekY` 와 항상 같다. 따라서 Y 재앵커(`_deadzoneCenterY = transform.position.y - _currentPeekY;`)는 **수학적으로 항등(no-op)이며 부동소수 노이즈만 추가한다. 넣지 말 것.**

## 5. 범위 밖 (하지 말 것)

- `Assets/Player/Script/PlayerController.cs`, `Assets/Player/Script/InputHandler.cs` 수정 금지 (0줄).
- `Assets/Camera/Script/BossZoomTrigger.cs` 수정 금지 (0줄).
- `minY` / `maxY` Y축 클램프 추가 금지 (범위 밖 — 카운트 0 유지).
- 기존 X축 `UpdateDeadzoneCenter()` **본문** 수정 금지 (헤더 주석 1줄만 사실 정정). Y 로직을 이 함수 안에 합치지 말 것 — 합치면 `_deadzonePushSign` 을 Y 가 오염시켜 DY-02 를 위반한다.
- `UpdateDynamicOffset()` / `UpdatePeekOffset()` 본문 수정 금지.
- `offset` / `smoothing` / 줌 필드 / `ApplyXClamp()` 수정 금지.
- 씬(`.unity`) 파일 및 `.meta` 파일 수정/추가 금지.
- Cinemachine 도입 금지.

## 6. `offset.y` 취급 (설계 근거 — 임의 변경 금지)

삭제되는 라인은 `_followBaseY = Mathf.Lerp(_followBaseY, target.position.y + offset.y, ...)` 로 `offset.y` 를 포함했다. 새 하드컷 게이트는 **X축을 정확히 미러링해 `target.position.y` 원값을 쓴다** (X축 `UpdateDeadzoneCenter` 도 `offset.x` 를 쓰지 않는다).
- 기본값 `offset = (0, 0, 10)` 이므로 `offset.y == 0` → 실제 동작 변화 없음.
- `offset.y` 가 0이 아닐 경우, `Start()` 의 `transform.position = target.position + offset` → `ResetNormalStageState()` 시드를 통해 초기 바이어스로 1회 반영된다. **이것은 X축이 `offset.x` 를 다루는 방식과 동일한 의도된 대칭이다.**
- `offset` 필드 자체는 `Start()` 와 레거시 Lerp 가 계속 쓰므로 고아가 아니다. 삭제 금지.

## 7. 커밋

작업 트리에 이 작업과 무관한 미커밋 변경(TutorialBoss, 메뉴 패널, 씬 등)이 다수 존재한다. **`git add -A` / `git commit -a` 절대 금지.** 경로 2개만 명시해 커밋한다.
`Assets/Camera/Check.md` 에는 이 작업 이전부터 미커밋 상태인 9줄(10-04 Task 3 실측 기록 = 바로 이 이슈의 원인 기록)이 있다. 같은 맥락의 문서이므로 함께 커밋하되 커밋 본문에 언급한다.
</critical_constraints>

<tasks>

<task type="auto">
  <name>Task 1: CameraController.cs — _followBaseY(Lerp)를 _deadzoneCenterY(하드컷)로 교체</name>

  <files>Assets/Camera/Script/CameraController.cs</files>

  <read_first>
    - `Assets/Camera/Script/CameraController.cs` — **작업 직전에 반드시 Read 로 전체를 확인한다.** `Edit` 의 `old_string` 은 이 Read 결과에서 그대로 복사한 ASCII 라인이어야 한다 (기억으로 재작성 금지).
    - `.planning/phases/10-3-.../10-01-PLAN.md` — X축 하드컷 원본 패턴 및 CP949 삽입 프로토콜.
  </read_first>

  <action>
`Edit` 도구로 **10회** 편집한다. 아래 순서대로 수행한다. 각 `old_string` 은 현재 파일에서 유일하다(행 번호는 현재 상태 기준 참고값).

> **주석 리터럴 주의 (Phase 9/10 반복 교훈)**: 아래 검증 게이트가 `deadzoneHeight` 등장 횟수를 **3** 으로 고정한다(필드 선언 1 + `UpdateDeadzoneCenterY` 1 + Gizmo 1). 따라서 **새로 쓰는 주석에는 `deadzoneHeight` / `deadzoneWidth` 리터럴을 쓰지 말고 "the height field" / "the width field" 로 표현한다.** 아래 제시된 주석 문구를 그대로 쓰면 자동으로 만족한다.

### 편집 1 — 필드 헤더 주석 사실 정정 (30-31행)

`old_string`:
```
    // deadzoneWidth gates camera X movement. The height field below is Gizmo / Inspector only
    // in this phase - the hard cut deadzone is X axis only (locked assumption A1 in 10-01-PLAN).
```
`new_string`:
```
    // Both fields below are real hard cut gates: the width field gates camera X and the
    // height field gates camera Y. This supersedes locked assumption A1 of 10-01-PLAN,
    // which had the height field as Gizmo / Inspector only (quick task 260804-q6h).
```

### 편집 2 — `_followBaseY` 필드를 `_deadzoneCenterY` 로 교체 (74-76행)

`old_string`:
```
    // Un-peeked Y baseline the camera follows with the legacy 'smoothing' rate.
    // Kept separate from transform.position.y so later offset layers cannot feed back into it.
    private float _followBaseY;
```
`new_string`:
```
    // Resting Y of the deadzone box center - the Y axis mirror of _deadzoneCenterX
    // (quick task 260804-q6h). Kept separate from transform.position.y so the peek offset
    // layered on top of it cannot feed back into the box.
    private float _deadzoneCenterY;
```

### 편집 3 — 피킹 필드 주석의 옛 이름 정정 (102행)

`old_string`:
```
    // Current vertical peek offset added on top of _followBaseY.
```
`new_string`:
```
    // Current vertical peek offset added on top of _deadzoneCenterY.
```

### 편집 4 — X축 함수 헤더 주석 사실 정정 (167행, **본문은 무변경**)

`old_string`:
```
    // X axis only - camera Y keeps following the target (locked assumption A1 in 10-01-PLAN).
```
`new_string`:
```
    // X axis only - the Y axis has its own mirror below, UpdateDeadzoneCenterY (260804-q6h).
```

### 편집 5 — `UpdateDeadzoneCenterY()` 신규 메서드 삽입 (185-186행 뒤)

`old_string` (2줄 연속, `UpdateDeadzoneCenter` 의 마지막 줄 + 닫는 중괄호):
```
        if (_deadzonePushSign != 0f) _lastPushSign = _deadzonePushSign;
    }
```
`new_string` = 위 2줄 + 아래 블록(앞 빈 줄 포함):
```csharp

    // Y axis mirror of UpdateDeadzoneCenter (quick task 260804-q6h). Same hard cut family:
    // the camera does not move vertically while the target stays inside the box, and the box
    // center snaps by exactly the overrun once an edge is crossed.
    // Deliberately independent of the dynamic offset and of peeking - it writes no push sign
    // and has no grounded / airborne branch, so it behaves identically in the air.
    // Do NOT Lerp or SmoothDamp this, for the same reason as the X axis (D-14).
    private void UpdateDeadzoneCenterY()
    {
        float halfH = deadzoneHeight * 0.5f;
        float py = target.position.y;
        if (py < _deadzoneCenterY - halfH)
        {
            _deadzoneCenterY = py + halfH;
        }
        else if (py > _deadzoneCenterY + halfH)
        {
            _deadzoneCenterY = py - halfH;
        }
    }
```

### 편집 6 — `ApplyNormalStageCamera` 에서 Y 갱신 호출 (195행)

`old_string`:
```
        UpdateDeadzoneCenter();
```
`new_string`:
```
        UpdateDeadzoneCenter();
        UpdateDeadzoneCenterY();
```

### 편집 7 — Y Lerp 라인 삭제 (198행)

`old_string` (2줄 연속):
```
        _followBaseY = Mathf.Lerp(_followBaseY, target.position.y + offset.y, smoothing * Time.deltaTime);
        Vector3 p = transform.position;
```
`new_string` (1줄 — 위 Lerp 라인이 사라진다):
```
        Vector3 p = transform.position;
```

### 편집 8 — Y 합성식 교체 (201행, DY-04)

`old_string`:
```
        p.y = _followBaseY + _currentPeekY;
```
`new_string`:
```
        p.y = _deadzoneCenterY + _currentPeekY;
```

### 편집 9 — 보스 구역 리셋 시드 교체 (213행, DY-03)

`old_string`:
```
        _followBaseY = transform.position.y;
```
`new_string`:
```
        _deadzoneCenterY = transform.position.y;
```

### 편집 10 — Gizmo 가 실제 Y 중심을 그리도록 (286-287행)

`old_string` (2줄 연속):
```
        float centerX = Application.isPlaying ? _deadzoneCenterX : transform.position.x;
        Vector3 center = new Vector3(centerX, transform.position.y, 0f);
```
`new_string`:
```
        float centerX = Application.isPlaying ? _deadzoneCenterX : transform.position.x;
        float centerY = Application.isPlaying ? _deadzoneCenterY : transform.position.y;
        Vector3 center = new Vector3(centerX, centerY, 0f);
```

> **X 경계선 Gizmo(298-299행)의 `transform.position.y` 는 건드리지 않는다.** 그건 빨간 세로선을 카메라 높이에 그리기 위한 것으로 데드존 박스와 무관하다.

### 하지 말 것 (재확인)
- `LateUpdate` 끝에 Y 재앵커 라인 추가 금지 (제약 §4 — 항등 no-op).
- Y 로직을 `UpdateDeadzoneCenter()` 본문에 병합 금지 (DY-02 — `_deadzonePushSign` 오염).
- `_deadzonePushSign` / `_lastPushSign` / `_offsetHoldTimer` 를 Y 경로에서 읽거나 쓰지 말 것.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && grep -qF "private void UpdateDeadzoneCenterY()" $F && grep -qF "float halfH = deadzoneHeight * 0.5f;" $F && grep -qF "float py = target.position.y;" $F && grep -qF "_deadzoneCenterY = py + halfH;" $F && grep -qF "_deadzoneCenterY = py - halfH;" $F && grep -qF "private float _deadzoneCenterY;" $F && grep -qF "UpdateDeadzoneCenterY();" $F && grep -qF "p.y = _deadzoneCenterY + _currentPeekY;" $F && grep -qF "_deadzoneCenterY = transform.position.y;" $F && grep -qF "float centerY = Application.isPlaying ? _deadzoneCenterY : transform.position.y;" $F && grep -qF "Vector3 center = new Vector3(centerX, centerY, 0f);" $F && [ "$(grep -cF '_followBaseY' $F)" = "0" ] && [ "$(grep -cF 'deadzoneHeight' $F)" = "3" ] && [ "$(grep -cF 'minY' $F)" = "0" ] && [ "$(grep -cF 'maxY' $F)" = "0" ] && [ "$(grep -cF 'IsGrounded' $F)" = "1" ] && [ "$(awk '/private void UpdateDeadzoneCenterY\(\)/,/^    \}$/' $F | grep -cE 'Lerp|SmoothDamp|_deadzonePushSign|_lastPushSign|_currentPeekY|_currentBoxOffsetX|IsGrounded')" = "0" ] && [ "$(git diff HEAD -- $F | grep -c '^-.*Vector3\.Lerp(transform\.position, targetCamPos')" = "0" ] && [ "$(git diff HEAD -- $F | grep -c '^-.*_deadzoneCenterX = transform\.position\.x')" = "0" ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(git diff HEAD -- $F | grep '^-' | grep -v '^---' | LC_ALL=C grep -c '[^[:print:][:space:]]')" = "0" ] && DY=$(grep -nF 'UpdateDeadzoneCenterY();' $F | head -1 | cut -d: -f1) && PY=$(grep -nF 'p.y = _deadzoneCenterY + _currentPeekY;' $F | cut -d: -f1) && [ "$DY" -lt "$PY" ] && echo T1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `grep -qF "private void UpdateDeadzoneCenterY()"` 성공 — X축 함수와 **별도 함수**다 (DY-02).
    - `grep -qF "float halfH = deadzoneHeight * 0.5f;"` 성공 — 높이 필드가 실제 게이트 값으로 승격됨 (DY-01).
    - `grep -qF "_deadzoneCenterY = py + halfH;"` / `"_deadzoneCenterY = py - halfH;"` 성공 — X축과 동일한 하드컷 수식.
    - `UpdateDeadzoneCenterY` 본문에 `Lerp` / `SmoothDamp` 가 **0회** (DY-01 하드컷).
    - `UpdateDeadzoneCenterY` 본문에 `_deadzonePushSign` / `_lastPushSign` / `_currentPeekY` / `_currentBoxOffsetX` / `IsGrounded` 가 **0회** (DY-02 완전 독립).
    - `grep -qF "p.y = _deadzoneCenterY + _currentPeekY;"` 성공 — 피킹이 데드존 결과 위에 얹힘 (DY-04).
    - `grep -qF "_deadzoneCenterY = transform.position.y;"` 성공 — `ResetNormalStageState` 시드 (DY-03 보스 구역 복귀 시 점프 방지).
    - `grep -cF '_followBaseY'` == **0** — 필드가 완전히 대체되어 고아가 남지 않음 (주석 2곳 포함).
    - `grep -cF 'deadzoneHeight'` == **3** (필드 선언 1 + Y 게이트 1 + Gizmo 1). 4 이상이면 주석에 리터럴을 쓴 것 → 문구 수정.
    - `grep -cF 'minY'` == 0, `grep -cF 'maxY'` == 0 (Y 클램프 추가 금지).
    - `grep -cF 'IsGrounded'` == **1** — 기존 `UpdatePeekOffset` 의 1회뿐. 2 이상이면 Y 데드존에 공중 분기를 넣은 것 (DY-02 위반).
    - **레거시 Lerp 무삭제**: `git diff HEAD -- $F | grep -c '^-.*Vector3\.Lerp(transform\.position, targetCamPos'` == 0.
    - **X 재앵커 무삭제**: `git diff HEAD -- $F | grep -c '^-.*_deadzoneCenterX = transform\.position\.x'` == 0.
    - **인코딩 게이트**: `LC_ALL=C grep -c '[^[:print:][:space:]]' $F` == **5**.
    - **삭제 라인 순수성**: 삭제된 라인 중 비-ASCII 포함 == **0** (삭제 라인 수는 약 10줄 예상 — 참고값이며 게이트 아님).
    - **호출 순서**: `UpdateDeadzoneCenterY();` 행 번호 < `p.y = _deadzoneCenterY + _currentPeekY;` 행 번호.
  </acceptance_criteria>

  <done>일반 스테이지 카메라 Y 가 `_deadzoneCenterY` 하드컷 + 피킹 가산으로만 결정되고, `_followBaseY` 와 그 Lerp 는 파일에서 사라졌으며, Gizmo 박스가 실제 Y 중심에 그려진다. 보스 구역 경로(레거시 Lerp)와 X축 파이프라인은 무변경. 비-ASCII 라인 수 5 유지, 삭제 라인에 비-ASCII 0.</done>
</task>

<task type="auto">
  <name>Task 2: Check.md 갱신(가정 A1 폐기 + Y 하드컷 검증 항목) 및 정적 회귀 검사 후 커밋</name>

  <files>Assets/Camera/Check.md</files>

  <read_first>
    - `Assets/Camera/Check.md` — 현재 내용 확인. 이 파일은 **UTF-8** 이므로 한글 작성 가능하다 (CameraController.cs 와 다르다).
    - Task 1 적용 후의 `Assets/Camera/Script/CameraController.cs`.
  </read_first>

  <action>
### 2-1. `Check.md` 편집 (`Edit` 도구 4회 — 기존 체크리스트 항목 삭제 금지)

**편집 A — 필드 표의 `deadzoneHeight` 행 (98행)**
`old_string`:
```
| `deadzoneHeight` | 2 | 데드존 박스 세로. **이번 Phase 에서는 Gizmo 표시 전용** | D-01 (가정 A1) |
```
`new_string`:
```
| `deadzoneHeight` | 2 | 데드존 박스 세로. **카메라 Y 하드컷 게이트** (Gizmo 전용 → 실동작 승격) | D-01 / 가정 A1 폐기 (260804-q6h) |
```

**편집 B — 합성식 설명 (110행)**
`old_string`:
```
일반 스테이지 합성식: `카메라X = 데드존중심X - 박스오프셋X`, `카메라Y = 추종베이스Y + 피킹Y`.
```
`new_string`:
```
일반 스테이지 합성식: `카메라X = 데드존중심X - 박스오프셋X`, `카메라Y = 데드존중심Y + 피킹Y` (260804-q6h 에서 추종베이스Y Lerp → 데드존중심Y 하드컷으로 교체).
```

**편집 C — 기존 "Y축 동작 (가정 A1)" 불릿 (161행) 대체.** 이 불릿 하나만 교체하고, 같은 섹션의 다른 항목은 손대지 않는다.
`old_string`: 161행 불릿 전문(`- [ ] **Y축 동작 (가정 A1)**: ...` 로 시작하는 한 줄 전체 — Read 결과에서 그대로 복사)
`new_string`:
```
- [ ] **Y축 동작 (가정 A1 폐기됨)**: 가정 A1("Y축 데드존 없음")은 quick task `260804-q6h` 에서 폐기되었다. 일반 스테이지 점프/낙하 시의 카메라 Y 동작은 아래 **5) Y축 하드컷 데드존** 섹션에서 검증한다. (옛 기대값이던 "Y 는 스무딩으로 따라온다"는 더 이상 정상 동작이 아니다. 이 섹션에서는 **보스 구역 안에서만** Y 가 레거시 Lerp 로 부드럽게 따라오는지 확인한다.)
```

**편집 D — 새 섹션 추가.** `## Phase 10 튜닝 값 기록` 바로 앞에 삽입한다.
`old_string`:
```
## Phase 10 튜닝 값 기록
```
`new_string` = 아래 블록 + 위 헤딩:
```
### 5) Y축 하드컷 데드존 (quick task 260804-q6h — 가정 A1 폐기)

`deadzoneHeight`(기본 2)가 Gizmo 표시 전용에서 **실제 카메라 Y 게이트**로 승격되었다. `UpdateDeadzoneCenterY()` 는
X축 `UpdateDeadzoneCenter()` 와 동일 계열의 하드컷이며(DY-01), 기존 `_followBaseY` 의 `smoothing` Lerp 는 제거되었다.
동적 오프셋/피킹과 완전 독립이고 공중/지상 분기가 없으며(DY-02), 보스 구역에서는 비활성이다(DY-03).
피킹 Y 는 이 결과 위에 그대로 얹힌다(DY-04).

- [ ] **박스 안에서 Y 완전 정지**: 박스 세로 범위 안에서 작은 단차를 오르내려도 `Main Camera` Transform Y 가 고정이다.
- [ ] **위 경계 밀기 = 즉시 추종 (하드컷)**: 점프해서 박스 상단 경계를 넘으면 카메라가 지연 없이 따라붙고, 플레이어가 상단 경계에 붙은 채로 올라간다. 스르륵 늦게 따라오면 Lerp 가 남아 있는 것이다 (DY-01 위반).
- [ ] **아래 경계 대칭**: 낙하로 하단 경계를 넘으면 동일하게 즉시 따라온다.
- [ ] **플레이어가 항상 박스 안 (이 작업의 목적)**: Scene 뷰에서 **노란 박스 세로 범위 밖에 플레이어가 서 있는 장면이 더 이상 나오지 않는다** (2026-08-04 사용자 보고 이슈 해소 확인).
- [ ] **Gizmo Y 중심 반영**: Play 중 노란 박스가 카메라 Y 가 아니라 데드존 중심 Y 에 그려진다. 피킹 중에는 박스가 화면 세로 중앙에서 피킹 거리만큼 어긋나 보이는 것이 **정상**이다 (X축에서 오프셋이 걸렸을 때와 동일한 구조).
- [ ] **Inspector 튜닝**: Play 중 `Deadzone Height` 를 6 으로 키우면 Y 정지 구간이 눈에 띄게 넓어진다.
- [ ] **피킹과 독립 (DY-04)**: 위/아래 피킹이 정상 동작하고, 피킹이 끝나면 박스 중심은 그대로인 채 카메라만 되돌아온다 (박스가 피킹을 따라 끌려가면 되먹임 버그).
- [ ] **동적 오프셋과 독립 (DY-02)**: 좌우로 달려 X 오프셋이 걸린 상태에서도 Y 데드존 동작이 달라지지 않는다.
- [ ] **공중/지상 구분 없음 (DY-02)**: 점프 중에도 Y 데드존이 지상과 완전히 동일하게 동작한다 (공중 전용 분기 없음).
- [ ] **보스 구역 비활성 (DY-03)**: 보스 구역 안에서는 Y 가 기존 Phase 9 레거시 Lerp 로 부드럽게 따라오고, 진입/이탈 시 카메라 Y 가 크게 튀지 않는다.
- [ ] **점프 체감 (튜닝 포인트)**: 하드컷이라 높은 점프에서 Y 가 딱딱하게 느껴질 수 있다. 거슬리면 `Deadzone Height` 를 키워 대응한다. **스무딩 재도입은 DY-01 위반이므로 금지** — 필요하면 별도 재논의 항목으로 남긴다.

## Phase 10 튜닝 값 기록
```

### 2-2. 정적 회귀 검사 (아래 verify 블록 실행 후 결과를 Check.md 맨 끝에 기록)

`Check.md` 맨 끝에 아래 형식으로 결과 기록 섹션을 **추가**한다 (기존 내용 삭제 금지). 실제 측정값을 채운다.

```
## quick task 260804-q6h 결과 기록

### 정적 회귀 검사 (2026-08-04)

| # | 검사 | 기대값 | 실제값 | 판정 |
|---|---|---|---|---|
| 1 | 인코딩 게이트 (`LC_ALL=C grep -c '[^[:print:][:space:]]'`) | 5 | | |
| 2 | 삭제 라인 비-ASCII | 0 | | |
| 3 | `_followBaseY` 잔존 | 0 | | |
| 4 | `deadzoneHeight` 등장 | 3 | | |
| 5 | `UpdateDeadzoneCenterY` 본문 Lerp/SmoothDamp/오프셋·피킹 심볼 | 0 | | |
| 6 | 레거시 Lerp / X 재앵커 라인 삭제 | 0, 0 | | |
| 7 | `minY`/`maxY`/`IsGrounded` | 0 / 0 / 1 | | |
| 8 | 읽기 전용 파일 무수정 (`PlayerController.cs`/`InputHandler.cs`/`BossZoomTrigger.cs`) | 0줄 | | |
| 9 | 수정 파일 목록 | `CameraController.cs`, `Check.md` 2개 | | |

**Play 모드 검증 상태**: 미검증 (위 "5) Y축 하드컷 데드존" 체크리스트는 사용자가 직접 확인해야 한다. PASS 로 허위 기록 금지.)
```

### 2-3. 커밋

경로 2개만 명시해 커밋한다. `git add -A` 금지.
```
git add Assets/Camera/Script/CameraController.cs Assets/Camera/Check.md
git commit -m "feat(camera): add Y axis hard-cut deadzone mirroring the X axis" -m "..."
```
커밋 본문에 포함할 것: 가정 A1 폐기, `_followBaseY` Lerp → `_deadzoneCenterY` 하드컷 교체, DY-01~DY-04, 그리고 `Check.md` 에 이 작업 이전부터 미커밋 상태였던 10-04 Task 3 실측 기록 9줄이 함께 커밋된다는 사실.
  </action>

  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=Assets/Camera/Script/CameraController.cs && C=Assets/Camera/Check.md && grep -qF "5) Y축 하드컷 데드존" $C && grep -qF "카메라Y = 데드존중심Y + 피킹Y" $C && grep -qF "가정 A1 폐기" $C && [ "$(grep -cF '이번 Phase 에서는 Gizmo 표시 전용' $C)" = "0" ] && [ "$(grep -c '^- \[ \]' $C)" -ge 40 ] && [ "$(LC_ALL=C grep -c '[^[:print:][:space:]]' $F)" = "5" ] && [ "$(grep -cF '_followBaseY' $F)" = "0" ] && [ -z "$(git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs Assets/Camera/Script/BossZoomTrigger.cs)" ] && [ -z "$(git status --porcelain -- '*.unity')" ] && echo T2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `Check.md` 에 `5) Y축 하드컷 데드존` 섹션이 존재하고 신규 체크박스 11개가 들어 있다.
    - 옛 문구 `이번 Phase 에서는 Gizmo 표시 전용` 이 파일에서 0회 (표 행 정정 완료).
    - 합성식 줄이 `카메라Y = 데드존중심Y + 피킹Y` 로 갱신됨.
    - **기존 체크박스 삭제 없음**: 전체 `- [ ]` 개수가 작업 전(약 40개) 이상이다. 감소하면 기존 항목을 지운 것 → 복구.
    - `PlayerController.cs` / `InputHandler.cs` / `BossZoomTrigger.cs` 의 `git status --porcelain` 결과가 비어 있다 (0줄 변경).
    - `.unity` 씬 파일이 이 작업으로 변경되지 않았다.
    - 커밋에 포함된 파일이 `Assets/Camera/Script/CameraController.cs` 와 `Assets/Camera/Check.md` **2개뿐**이다 (`git show --stat HEAD` 로 확인).
    - 결과 기록 표의 9개 항목 실제값이 전부 채워져 있고, Play 모드 항목은 **미체크**로 남아 있다 (허위 PASS 금지).
  </acceptance_criteria>

  <done>`Check.md` 가 Y축 하드컷 동작을 검증할 수 있는 상태로 갱신되고(가정 A1 폐기 반영), 정적 회귀 검사 9항목 결과가 기록되었으며, 두 파일만 커밋되었다.</done>
</task>

</tasks>

<verification>
1. `_followBaseY` 가 `CameraController.cs` 에 0회 등장하고 `_deadzoneCenterY` 가 그 역할을 완전히 대체한다.
2. `UpdateDeadzoneCenterY()` 본문에 `Lerp` / `SmoothDamp` / `_deadzonePushSign` / `_lastPushSign` / `_currentPeekY` / `_currentBoxOffsetX` / `IsGrounded` 가 0회 (DY-01 / DY-02).
3. `ApplyNormalStageCamera` 내부 순서: `UpdateDeadzoneCenter()` → `UpdateDeadzoneCenterY()` → `UpdateDynamicOffset()` → `UpdatePeekOffset()` → `p.x`/`p.y` 대입 → `_lastTargetPos` 갱신.
4. `LateUpdate` 순서 무변경: 레거시 Lerp → 보스/일반 분기 → 줌 Lerp → `ApplyXClamp()` → X 재앵커. **Y 재앵커 라인은 추가되지 않았다** (제약 §4).
5. 인코딩 게이트 통과: 비-ASCII 포함 라인 수 == 5, 삭제 라인 중 비-ASCII == 0.
6. `git status --porcelain -- Assets/Player/Script/PlayerController.cs Assets/Player/Script/InputHandler.cs Assets/Camera/Script/BossZoomTrigger.cs` 결과가 비어 있다.
7. 이 작업으로 수정된 파일은 `CameraController.cs`, `Check.md` 2개뿐이다.
</verification>

<success_criteria>
- 일반 스테이지에서 카메라 Y 가 하드컷 데드존으로만 움직인다 — Y 경로에 Lerp/SmoothDamp 없음 (DY-01).
- Y 데드존이 동적 오프셋·피킹·공중 상태와 완전히 독립적이다 (DY-02).
- 보스 구역에서는 Y 데드존이 꺼지고 Phase 9 레거시 Lerp 동작이 그대로 유지된다 (DY-03).
- 피킹 Y 가 Y 데드존 중심 위에 얹혀 동작한다 (DY-04).
- Gizmo 노란 박스가 실제 데드존 중심(X, Y)에 그려져, 플레이어가 박스 밖에 서 있는 장면이 사라진다.
- 기존 깨진 한글 주석 5줄, CRLF 개행, Phase 9/10 로직이 손상되지 않았다.
</success_criteria>

<output>
완료 후 `.planning/quick/260804-q6h-y-cameracontroller-cs/260804-q6h-SUMMARY.md` 를 생성한다.
SUMMARY 에는 (a) 삭제 라인 실제 개수와 내용, (b) 정적 회귀 검사 9항목 결과, (c) Play 모드 미검증 상태, (d) `.planning/STATE.md` "Phase 10 gap" TODO 를 "코드 반영 완료, Play 모드 검증 대기" 로 갱신할 것을 기록한다.
</output>
